import type { LessonContent } from "./types";

/**
 * Pass 002 bounded content batch: Phase 1 opener (L1).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P1: LessonContent[] = [
  {
    id: "p01-l1",
    phaseId: "p01",
    title: "Semantic HTML & Landmarks",
    level: "Foundation",
    minutes: 35,
    summary:
      "HTML is not 'the part before CSS'. It is the skeleton that tells browsers, screen readers, and search engines what things ARE. This lesson replaces div soup with landmarks, heading outlines, links vs buttons, lists, tables, and labeled forms — then shows you how to verify it all with your keyboard and DevTools.",
    prerequisites: ["p00-l4 — you can inspect any page in DevTools"],
    objectives: [
      "Explain what semantic markup gives machines (roles, names, structure) and why styling cannot substitute for it.",
      "Structure a page with landmarks: header, nav, main (exactly one), aside, footer — labeling duplicates with aria-label.",
      "Write heading outlines that document hierarchy, not font size.",
      "Choose links for navigation and buttons for actions — and never fake either with div onclick.",
      "Give every input a real label and verify any page's accessible names and roles in the DevTools accessibility pane.",
    ],
    simple:
      "HTML elements come with meaning pre-installed: <button> announces 'I do a thing', <a> announces 'I go somewhere', <main> announces 'this is the point of the page'. When you build with anonymous <div> and <span> and bolt behavior on with JavaScript, the page still looks right — but to a screen reader it is an unlabeled pile of identical boxes, and to a search engine it is text with no structure. Semantic HTML means writing the page so the meaning is stated once, in markup, and everything else — styles, scripts, assistive tech — inherits it for free.",
    why:
      "Roughly 1 in 6 people worldwide lives with some form of disability, and keyboard-only power users are the visible tip of that iceberg. Accessible pages also rank better, survive redesigns because structure and styling are separate, and have fewer bugs — because the browser handles focus, activation, and roles you would otherwise reimplement badly. The European Accessibility Act made this a legal requirement for much of the web in 2025. And the payoff is immediate: most of what this lesson teaches costs zero extra code, because the element IS the behavior.",
    mentalModel: {
      title: "Signs in a dark building",
      body: "Imagine walking through an unfamiliar building with the lights off, guided by a friend who can only read signs. Landmarks are the signs: ENTRANCE (header), DIRECTORY (nav), 'YOU ARE HERE: THE EXHIBIT' (main), CAFÉ TO THE SIDE (aside), EXIT (footer). Headings are floor numbers — skip from 1 to 4 and your guide gets lost. A div is a bare wall: it holds the building up but tells nobody anything. Semantic HTML is hanging the signs before you ever think about paint.",
    },
    sections: [
      {
        heading: "Div soup vs structure: same pixels, different meaning",
        body: [
          "Two pages can render identically and mean completely different things to a machine. The first version below builds a tiny blog page out of divs with behavior bolted on. The second uses the elements designed for the job. Same pixels; the second one is navigable, focusable, and outline-able without a single line of JavaScript.",
          "This is the core trade: semantic elements give away behavior (focus, keyboard activation, announced roles) that you would otherwise rebuild with scripts — and every home-made version is worse than the browser's built-in one.",
        ],
        code: [
          {
            file: "soup.html — looks like a blog, reads like a box of boxes",
            lang: "html",
            code: [
              "<div class=\"top\">",
              "  <div class=\"logo\">Byte &amp; Bean</div>",
              "  <div class=\"links\">",
              "    <div class=\"link\" onclick=\"go('/menu')\">Menu</div>",
              "    <div class=\"link\" onclick=\"go('/hours')\">Hours</div>",
              "  </div>",
              "</div>",
              "<div class=\"middle\">",
              "  <div class=\"big\">Why we roast our own beans</div>",
              "  <div class=\"words\">…400 words of story…</div>",
              "</div>",
              "",
              "<!-- Screen reader experience: six anonymous groups.",
              "     No navigation landmark, no headings, no links —",
              "     the 'link' divs aren't focusable and ignore Enter. -->",
            ].join("\n"),
          },
          {
            file: "semantic.html — the same page, signed and lit",
            lang: "html",
            code: [
              "<header class=\"top\">",
              "  <a class=\"logo\" href=\"/\">Byte &amp; Bean</a>",
              "  <nav aria-label=\"Main\">",
              "    <a href=\"/menu\">Menu</a>",
              "    <a href=\"/hours\">Hours</a>",
              "  </nav>",
              "</header>",
              "<main class=\"middle\">",
              "  <article>",
              "    <h1>Why we roast our own beans</h1>",
              "    <p>…400 words of story…</p>",
              "  </article>",
              "</main>",
              "",
              "<!-- Now a screen reader jumps: banner → navigation → main.",
              "     The outline says 'one page, one topic, one h1'.",
              "     Links are focusable and answer to Enter — for free. -->",
            ].join("\n"),
            caption: "Everything the second page gives away is behavior you'd otherwise re-implement in JavaScript — badly.",
          },
        ],
      },
      {
        heading: "Landmarks — the seven signs",
        body: [
          "Landmarks are the navigable regions assistive tech jumps between: banner (a page-level <header>), navigation (<nav>), main (exactly one per page — the point of it), complementary (<aside>), contentinfo (a page-level <footer>), region (a <section> that has an accessible name), form (a <form> that has one), and search. Think of them as the rooms of the building, each with a sign on the door.",
          "Three rules cover 95% of real pages. One: exactly one <main>, because it is the target of skip-to-content links and the answer to 'where am I?'. Two: when a landmark type repeats — two navs, two asides — give each an aria-label ('Main' vs 'Breadcrumb') so the signs are distinct. Three: <section> only becomes a region landmark when it has a name (aria-label or aria-labelledby); an unnamed section is a generic grouping, which is fine for wrappers but means don't expect it in the jump list.",
          "The skip link is the first focusable element on the page — <a href=\"#main\">Skip to content</a> — letting keyboard users vault straight past the chrome. Visually hidden until focused, it is three lines of HTML that change someone's entire day.",
        ],
        code: [
          {
            file: "landmarks.html — a signed building",
            lang: "html",
            code: [
              "<a class=\"skip\" href=\"#main\">Skip to content</a>",
              "",
              "<header>…logo…</header>                 <!-- banner -->",
              "<nav aria-label=\"Main\">…</nav>           <!-- navigation 'Main' -->",
              "<nav aria-label=\"Breadcrumb\">…</nav>     <!-- navigation 'Breadcrumb' -->",
              "",
              "<main id=\"main\">",
              "  <section aria-labelledby=\"open\">       <!-- region 'Opening hours' -->",
              "    <h2 id=\"open\">Opening hours</h2>",
              "    …",
              "  </section>",
              "</main>",
              "",
              "<aside aria-label=\"Specials\">…</aside>   <!-- complementary -->",
              "<footer>…</footer>                        <!-- contentinfo -->",
            ].join("\n"),
            caption: "Every named landmark appears in a screen reader's jump list — the table of contents of the building.",
          },
        ],
      },
      {
        heading: "Headings are an outline, not a font size",
        body: [
          "Headings are the page's table of contents. Exactly one <h1> states the topic; <h2> through <h6> subdivide it without skipping levels. Screen reader users routinely navigate by heading — listing them is how they skim — and a jump from h1 to h4 reads like a building with floors 2 and 3 missing.",
          "The number is structure; the size is CSS. Want a visually huge h2? Style it. Want small grey text? That is a <p> wearing a costume, not an h6. If designers reach for heading levels as font sizes, the outline breaks and the fix is always the same: restore the outline, move the sizing to a class.",
        ],
        code: [
          {
            file: "headings — wrong outline vs right outline",
            lang: "html",
            code: [
              "<!-- WRONG: headings chosen for size -->",
              "<h1>Shop</h1>",
              "<h4>New arrivals</h4>      <!-- skipped 2 and 3: outline broken -->",
              "<h6 class=\"huge\">SALE</h6> <!-- heading used to shout -->",
              "",
              "<!-- RIGHT: outline first, CSS for size -->",
              "<h1>Shop</h1>",
              "<h2>New arrivals</h2>",
              "<h2>Sale</h2>",
              "<style>",
              "  .loud { font-size: 3rem; }  /* size is a styling decision */",
              "</style>",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Links navigate, buttons act — never fake either",
        body: [
          "A link (<a href>) goes somewhere: it changes the URL, activates with Enter, supports right-click 'open in new tab' and middle-click. A button (<button>) does something: submits a form, opens a dialog, toggles state; it activates with both Space and Enter. The browser bundles focus, activation, and role announcement with each — you get them by choosing the right element.",
          "div onclick ships four bugs at once: it is invisible to the keyboard (not focusable), dead to Enter and Space, nameless to screen readers (announced as a group, or nothing), and un-middle-clickable. The classic 'fix' — tabindex=\"0\" plus a hand-rolled keydown handler — rebuilds a worse version of what <button> already provides, and then you maintain it forever. The rule survives every framework you will ever use: acts → button, navigates → link; CSS handles the costume.",
        ],
        code: [
          {
            file: "jobs.html — match the element to the job",
            lang: "html",
            code: [
              "<!-- WRONG: a button in a link costume, a link in a button costume -->",
              "<a href=\"#\" onclick=\"openDialog(); return false;\">Settings</a>",
              "<div class=\"btn\" onclick=\"save()\">Save</div>",
              "",
              "<!-- RIGHT: jobs match elements -->",
              "<button type=\"button\" onclick=\"openDialog()\">Settings</button>",
              "<button type=\"button\" onclick=\"save()\">Save</button>",
              "<a href=\"/settings\">Settings page</a>   <!-- navigates: stays a link -->",
              "",
              "<!-- Free with the right element: Enter/Space activation, focus ring,",
              "     announced role, form semantics. type=\"button\" stops accidental",
              "     form submits — a bug you WILL meet in Phase 4. -->",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Lists, tables, labels: structure that carries meaning",
        body: [
          "Lists tell machines 'these are siblings': <ul> when order is irrelevant, <ol> when it matters (recipes, steps — screen readers announce 'list, 5 items' and positions). A column of divs carries none of that. Tables are for two-dimensional data only — never layout — and earn their keep with <caption> (the table's title) and <th scope=\"col\"|\"row\"> (which header labels which cell), turning noise into a grid a screen reader can walk cell by cell.",
          "Forms get the strictest rule: every input needs a real <label for=\"…\">. A placeholder is a hint that evaporates on typing, is easy to miss at low vision, and is not a reliable accessible name. Clicking a true label focuses its input — free UX. Add required for native validation and autocomplete for password managers; Phase 2 takes forms deep, but the naming law is passed today.",
        ],
        code: [
          {
            file: "forms-labels.html — the naming law",
            lang: "html",
            code: [
              "<form>",
              "  <!-- WRONG: placeholder posing as a label -->",
              "  <input type=\"email\" placeholder=\"Email\">",
              "",
              "  <!-- RIGHT: visible label, real name, native validation -->",
              "  <label for=\"email\">Email</label>",
              "  <input id=\"email\" name=\"email\" type=\"email\" required",
              "         autocomplete=\"email\" aria-describedby=\"email-hint\">",
              "  <p id=\"email-hint\">We only use this for order receipts.</p>",
              "",
              "  <button type=\"submit\">Create account</button>",
              "</form>",
              "",
              "<!-- Icon-only controls need a name too: -->",
              "<button type=\"button\" aria-label=\"Delete task\">🗑</button>",
            ].join("\n"),
            caption: "aria-label gives invisible names where no visible text exists — icon buttons, close buttons, logo links.",
          },
        ],
      },
      {
        heading: "Test it: keyboard tour + the accessibility pane",
        body: [
          "The sixty-second audit you will run for the rest of your career. First, the keyboard tour: click the address bar, press Tab, and walk the page without a mouse. Does focus visit everything interactive in a sensible order? Anything Tab cannot reach does not exist for keyboard users. Second, the accessibility pane: in DevTools → Elements, select any node and open the Accessibility sidebar — it shows the computed Name, Role, and keyboard-focusability. A button that reads 'group' with no name is a bug visible in one click.",
          "When you are ready to go deeper, real screen readers are free: VoiceOver on macOS (Cmd+F5) and NVDA on Windows. Ten minutes with one is worth a hundred articles about them. Phase 31 automates all of this with axe and Playwright — but the habit of seeing the tree with your own eyes starts now.",
        ],
      },
    ],
    mistake: {
      title: "The div button",
      wrong: [
        "<div class=\"button\" onclick=\"deleteTask(7)\">Delete</div>",
        "<!-- Looks right. But: not in the Tab order, Enter/Space do nothing,",
        "     screen reader announces 'group' (or nothing),",
        "     middle-click is dead. Four bugs, one element. -->",
      ].join("\n"),
      right: [
        "<button type=\"button\" class=\"button\" onclick=\"deleteTask(7)\">",
        "  Delete",
        "</button>",
        "<!-- If design demands custom looks, style the REAL button —",
        "     never rebuild its behavior. Focus, activation, role: free. -->",
      ].join("\n"),
      explain:
        "Every div-onclick ships four bugs: invisible to keyboards, dead to Enter/Space, nameless to screen readers, un-middle-clickable. The tabindex='0' plus hand-rolled keydown 'fix' recreates a worse button you then maintain forever. Rule of the whole course: acts → <button>, navigates → <a>; CSS handles the costume.",
    },
    tryIt: [
      "Open any news site. In Elements, click <body> and open the Accessibility pane. How many landmarks exist? Now use the site keyboard-only — where does Tab land first?",
      "Create index.html with the div-soup snippet from section one. Open it and press Tab. How many interactive controls can you reach? (Expected: zero.)",
      "Rebuild the same page with the semantic version. Tab again. Write down everything that improved without a single line of JavaScript.",
      "Add a 'Skip to content' link as the first focusable element, pointing at <main id=\"main\">. Test it keyboard-only: one Tab, one Enter, and you're in the content.",
      "Give your nav aria-label=\"Main\", add a second nav labeled \"Breadcrumb\", and confirm both appear as distinct navigation landmarks in the accessibility tree.",
    ],
    challenge: {
      prompt:
        "Convert this profile dashboard from div soup to semantic HTML. Requirements: labeled landmarks (banner, main, two navs or a nav + complementary, contentinfo), a correct heading outline (one h1, no skips), links vs buttons matched to their jobs, and a labeled search form.\n\n<div class=\"app\">\n  <div class=\"bar\"><div class=\"name\">TaskPilot</div>\n    <div class=\"menu\"><span onclick=\"go('/tasks')\">Tasks</span><span onclick=\"go('/team')\">Team</span></div>\n  </div>\n  <div class=\"cols\">\n    <div class=\"main\">\n      <div class=\"h\">Sprint 14</div>\n      <div class=\"sub\">In progress</div>\n      <div class=\"task\"><span>Fix login redirect</span><span class=\"x\" onclick=\"done(1)\">done</span></div>\n    </div>\n    <div class=\"side\">\n      <div class=\"h2\">Filters</div>\n      <div class=\"searchbox\"><input placeholder=\"Search tasks\"></div>\n    </div>\n  </div>\n  <div class=\"foot\"><span>© 2026 TaskPilot</span></div>\n</div>",
      hints: [
        "Which div appears exactly once as 'the point of the page'? That is main. The bar and foot are page-level header/footer (banner/contentinfo).",
        "'Tasks' and 'Team' change the URL → links. 'done' performs an action → button. 'Sprint 14' is the page topic → h1; 'In progress' and 'Filters' → h2.",
        "The side column is complementary (<aside>). Make the search a real <form role=\"search\"> with a <label for> — placeholder is not a name.",
      ],
      solution: [
        "<header>",
        "  <a href=\"/\">TaskPilot</a>",
        "  <nav aria-label=\"Primary\">",
        "    <a href=\"/tasks\">Tasks</a>",
        "    <a href=\"/team\">Team</a>",
        "  </nav>",
        "</header>",
        "",
        "<main id=\"main\">",
        "  <h1>Sprint 14</h1>",
        "  <section aria-labelledby=\"in-progress\">",
        "    <h2 id=\"in-progress\">In progress</h2>",
        "    <ul>",
        "      <li>",
        "        Fix login redirect",
        "        <button type=\"button\" onclick=\"done(1)\">Mark done</button>",
        "      </li>",
        "    </ul>",
        "  </section>",
        "</main>",
        "",
        "<aside aria-label=\"Filters\">",
        "  <h2>Filters</h2>",
        "  <form role=\"search\" action=\"/tasks\">",
        "    <label for=\"q\">Search tasks</label>",
        "    <input id=\"q\" name=\"q\" type=\"search\" autocomplete=\"off\">",
        "    <button type=\"submit\">Search</button>",
        "  </form>",
        "</aside>",
        "",
        "<footer><p>© 2026 TaskPilot</p></footer>",
        "",
        "<!-- Audit: screen readers jump banner → nav → main → complementary",
        "     → contentinfo; the outline reads Sprint 14 → In progress /",
        "     Filters; every control is keyboard-operable; the search field",
        "     has a real name. Zero JavaScript was added. -->",
      ].join("\n"),
    },
    quiz: [
      {
        q: "How many <main> elements belong on a page?",
        options: ["Exactly one", "One per section", "As many as needed", "Zero — main is deprecated"],
        answer: 0,
        explain: "main marks 'the point of this page'. Multiple mains destroy skip-to-content targets and the landmark jump list; use sections and articles inside it.",
      },
      {
        q: "When does a <section> become a landmark (region)?",
        options: ["When it has an accessible name (aria-label / aria-labelledby)", "Always", "When it contains a heading", "Never"],
        answer: 0,
        explain: "Unnamed sections are generic groups. Name the ones worth jumping to — 'Opening hours', 'Filters' — and leave pure wrappers unnamed.",
      },
      {
        q: "Which element is correct for 'show a dialog'?",
        options: ["<button>", "<a href=\"#\">", "<div onclick>", "<span tabindex=\"0\">"],
        answer: 0,
        explain: "Actions are buttons: Space and Enter activate, focus is built in, the role is announced. Links change the URL — and '#' is not a place.",
      },
      {
        q: "Why is a placeholder not a label?",
        options: ["It vanishes on typing and is no reliable accessible name", "It renders slower", "It only works on mobile", "It is deprecated"],
        answer: 0,
        explain: "Names must persist. <label for> stays visible, is announced, and focuses the input on click — a placeholder is a hint at best.",
      },
      {
        q: "The DevTools accessibility pane shows, for a selected node…",
        options: ["Computed name, role, and keyboard-focusability", "CSS specificity", "Network requests it made", "Its Git history"],
        answer: 0,
        explain: "Name / Role / Keyboard-focusable is the ten-second check that catches most markup bugs — including buttons that are secretly divs.",
      },
      {
        q: "The outline 'h1 → h4' is a problem because…",
        options: [
          "Screen reader users navigate by level; skips read like missing floors",
          "It is invalid HTML and won't render",
          "CSS cannot style an h4",
          "Search engines reject the page",
        ],
        answer: 0,
        explain: "Headings are the table of contents for people and machines. Size is a styling decision — use CSS and keep the outline honest.",
      },
    ],
    flashcards: [
      { front: "Semantic HTML", back: "Markup that states what content IS (article, nav, label), not just how it looks. Screen readers and search engines consume the meaning." },
      { front: "Landmark", back: "A navigable page region: banner, navigation, main, complementary, contentinfo, region, form, search. Assistive tech jumps between them like rooms." },
      { front: "main", back: "The 'point of the page' landmark — exactly one per page, the target of skip links, home of primary content." },
      { front: "aria-label", back: "An invisible name for assistive tech. Required when landmarks repeat (two navs) or an icon button has no text." },
      { front: "Accessible name", back: "What a control is announced as: text content, <label for>, or aria-label. No name = a mystery box to a screen reader." },
      { front: "Link vs button", back: "Links navigate (change URL, Enter). Buttons act (Enter + Space). Never div-onclick; CSS styles, elements behave." },
      { front: "Heading outline", back: "One h1 (the topic), h2–h6 with no skips. The page's table of contents; visual size is CSS's job." },
      { front: "Div soup", back: "A page of anonymous div/span with bolted-on JS: invisible landmarks, dead keyboards, nameless controls. The disease semantics cures." },
    ],
    recap: [
      "Elements are pre-declared meaning: buttons act, links navigate, main is the point, labels name.",
      "Landmarks are rooms with signs — exactly one main; duplicates get aria-labels.",
      "The heading outline is a table of contents: one h1, no skips, CSS for size.",
      "Placeholders vanish, labels persist: every input gets a <label for>.",
      "Verify with a keyboard tour and the accessibility pane — the sixty-second audit.",
    ],
    references: [
      { label: "MDN — HTML elements reference", url: "https://developer.mozilla.org/en-US/docs/Web/HTML/Element" },
      { label: "WAI-ARIA APG — landmark regions", url: "https://www.w3.org/WAI/ARIA/apg/practices/landmark-regions/" },
      { label: "web.dev — Learn HTML", url: "https://web.dev/learn/html" },
      { label: "W3C WAI — forms tutorial", url: "https://www.w3.org/WAI/tutorials/forms/" },
      { label: "MDN — ARIA roles", url: "https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Roles" },
    ],
    nextBridge:
      "You can now write structure machines understand. Next lesson, forms go deeper — fieldsets, native validation, accessible error messages — before CSS arrives to answer the eternal question: when ten rules target one element, which one wins? The cascade.",
  },
  {
    id: "p01-l2",
    phaseId: "p01",
    title: "Forms, Labels & Native Validation",
    level: "Foundation",
    minutes: 40,
    summary:
      "Forms are where users hand you their data — and where most accessibility and validation bugs live. This lesson installs the browser's free validation engine: constraints, the validation lifecycle, ValidityState flags, accessible error messages — and draws the line between friendly frontend checking and authoritative backend truth.",
    prerequisites: ["p01-l1 — semantic HTML, landmarks, and the naming law"],
    objectives: [
      "Structure multi-part forms with fieldset/legend so groups have names, not just spacing.",
      "Apply constraints — required, type, pattern, min/max, minlength — as data attributes instead of hand-rolled regex.",
      "Walk the validation lifecycle: input events, submit blocking, checkValidity, reportValidity, and ValidityState flags.",
      "Raise cross-field errors with setCustomValidity without breaking the native flow.",
      "Present errors accessibly: aria-invalid, aria-describedby, a summarized focus target — never color alone.",
      "State the trust-boundary rule: native validation is UX; the server (NestJS, Phase 20) re-validates everything.",
    ],
    simple:
      "The browser already knows what an email looks like, what a number in range looks like, and what 'required' means — you just have to tell it which rules apply, with attributes like type=\"email\", required, and pattern. When a user submits, the browser checks every rule and, if anything fails, it blocks the submit and points at the first problem. All of that is free, works on every platform, and speaks the user's language. Your job shrinks to three things: name every field with a real label, declare the rules as attributes, and design how errors look.",
    why:
      "Hand-rolled validation is the number-one accessibility bug on the web: custom error text that screen readers never announce, checks that fire only on click, messages that vanish on the next keystroke. The native engine solves all of that — it focuses the failing field, announces the message, localizes it, and picks the right mobile keyboard per input type. But it has a hard limit: it lives in the user's browser, which the user owns. Anyone can flip novalidate, call submit() from the console, or skip your page entirely and hit your API with curl. So the law of this course: the browser validates for friendliness, the server validates for truth. Today you learn the friendly half — and exactly where its authority ends.",
    mentalModel: {
      title: "The bouncer and the accountant",
      body: "A form is a club entrance. The browser is the bouncer: quick, polite, checks IDs at the door so the line keeps moving — 'this email looks wrong, mate'. The server is the accountant in the back office: re-checks every credential against the ledger before recording anything, because a charming stranger can always talk past a bouncer. A good club has both. A club that trusts only the bouncer gets robbed (bad data in your database). A club with only the accountant has a queue out the door (users discover errors only after a slow round trip). Frontend validation is speed; backend validation is truth.",
    },
    sections: [
      {
        heading: "Anatomy of a real form",
        body: [
          "A serious form is a labeled machine. Every control gets a <label for>; related controls get grouped in a <fieldset> with a <legend> (screen readers announce the legend as the group's name — 'Payment, card number'); inputs declare autocomplete so browsers and password managers do their jobs; input types pick the right keyboard on mobile and enable free constraints.",
          "Notice what is NOT here: no divs styled like inputs, no icons that act as buttons, no placeholder doing a label's job. Every interactive thing is a real element with a real name — the rule from the previous lesson, applied where it matters most.",
        ],
        code: [
          {
            file: "signup.html — a form machines can read",
            lang: "html",
            code: [
              "<form action=\"/signup\" method=\"post\">",
              "  <p>",
              "    <label for=\"email\">Email</label>",
              "    <input id=\"email\" name=\"email\" type=\"email\" required",
              "           autocomplete=\"email\">",
              "  </p>",
              "  <p>",
              "    <label for=\"pw\">Password <span class=\"hint\">8+ characters</span></label>",
              "    <input id=\"pw\" name=\"password\" type=\"password\" required",
              "           minlength=\"8\" autocomplete=\"new-password\">",
              "  </p>",
              "",
              "  <fieldset>",
              "    <legend>Notifications</legend>",
              "    <p><label><input type=\"checkbox\" name=\"news\" value=\"yes\">",
              "       Product news</label></p>",
              "    <p><label><input type=\"checkbox\" name=\"tips\" value=\"yes\">",
              "       Weekly tips</label></p>",
              "  </fieldset>",
              "",
              "  <button type=\"submit\">Create account</button>",
              "</form>",
              "",
              "<!-- type=\"email\" = format check + email keyboard on mobile.",
              "     minlength = length check. required = presence check.",
              "     Three constraints, zero JavaScript. -->",
            ].join("\n"),
            caption: "Wrapping the checkbox in its label is legal and practical for single-word options; long text still prefers label-for.",
          },
        ],
      },
      {
        heading: "Constraints are data, not code",
        body: [
          "Each constraint is one attribute the validation engine reads: required (must have a value), type (email/url/tel/number/date — each with its own format rules and mobile keyboard), pattern (a regex, always paired with title so the browser can phrase the failure), min/max (numeric or date bounds), minlength/maxlength (character count), and step (allowed increments on numbers).",
          "Two habits separate professionals from beginners. One: every pattern carries a title — the browser includes it in the message ('match: 3–16 letters, digits, underscores'). Two: constraints describe what is structurally possible ('a date not in the past', 'at least 8 characters') — never business logic ('this username is not taken'), which needs a server round trip and belongs to Phase 20.",
        ],
        code: [
          {
            file: "constraints.html",
            lang: "html",
            code: [
              "<label for=\"handle\">Username</label>",
              "<input id=\"handle\" name=\"username\" required",
              "       pattern=\"[A-Za-z0-9_]{3,16}\"",
              "       title=\"3 to 16 characters: letters, digits, underscores\"",
              "       autocomplete=\"username\">",
              "",
              "<label for=\"when\">Preferred date</label>",
              "<input id=\"when\" name=\"when\" type=\"date\" required",
              "       min=\"2026-02-01\">   <!-- no past dates: a data attribute -->",
              "",
              "<label for=\"qty\">Quantity</label>",
              "<input id=\"qty\" name=\"qty\" type=\"number\"",
              "       min=\"1\" max=\"99\" step=\"1\" value=\"1\">",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The validation lifecycle — the engine in motion",
        body: [
          "The engine has a heartbeat you can observe. On every keystroke an input event fires — ideal for live feedback. On submit, the browser runs checkValidity over the whole form; if any field fails, it blocks the submit event entirely and shows its own bubble at the first failing field. You never write 'is the email valid?' — the platform answers.",
          "When you need the engine's verdict yourself — to style fields, enable a button, or build a custom summary — three APIs do everything: form.checkValidity() asks quietly (true/false), form.reportValidity() asks and shows the native bubble, and field.validity is a ValidityState object whose flags name the failure precisely: valueMissing, typeMismatch, patternMismatch, rangeUnderflow, rangeOverflow, tooShort, tooLong. The validationMessage property is the localized sentence.",
          "Cross-field rules ('confirm password must match') are where hand-rolled code usually sneaks in — and where setCustomValidity keeps you inside the native flow: set a message and the field becomes invalid with your reason; set an empty string and it becomes valid again. The submit-blocking, the focus, the announcement — all still free.",
        ],
        code: [
          {
            file: "validate.js — ask the engine, don't rebuild it",
            lang: "js",
            code: [
              "const form = document.querySelector(\"form\");",
              "const pw = document.querySelector(\"#pw\");",
              "const confirm = document.querySelector(\"#pw2\");",
              "",
              "// Cross-field rule, expressed INSIDE the native engine:",
              "confirm.addEventListener(\"input\", () => {",
              "  confirm.setCustomValidity(",
              "    confirm.value === pw.value ? \"\" : \"Passwords do not match\"",
              "  );",
              "});",
              "",
              "form.addEventListener(\"submit\", (event) => {",
              "  // This handler only runs if EVERY constraint already passed —",
              "  // the browser blocked invalid submits before you got here.",
              "  event.preventDefault(); // (here we'd hand off to the server)",
              "  console.log(\"all constraints green — safe to proceed\");",
              "});",
              "",
              "// Programmatic checks:",
              "form.checkValidity();       // quiet true/false",
              "form.reportValidity();      // true/false AND show bubbles",
              "pw.validity.tooShort;       // named flags, no string parsing",
              "pw.validationMessage;       // localized human sentence",
            ].join("\n"),
            caption: "The submit handler is the last place validity is checked — the engine ran first and blocked failures.",
          },
        ],
        demo: "form-validation",
      },
      {
        heading: "Errors people can perceive",
        body: [
          "Native bubbles are great, but real products design their own error text — which means re-doing the announcement work the platform gave you for free. The recipe: give the failing input aria-invalid=\"true\", point aria-describedby at the error element, keep the label visible above both, and never rely on color alone (add an icon or text — red/green is invisible to some users and meaningless on poor screens).",
          "For long forms, add an error summary at the top listing every problem as links that focus their fields — the pattern government-scale forms (GOV.UK, tax portals) converged on because it measurably reduces abandonment. And when errors change, announce them: a small aria-live region turns silent text into something a screen reader says out loud.",
        ],
        code: [
          {
            file: "errors.html — designed, yet still announced",
            lang: "html",
            code: [
              "<div class=\"summary\" role=\"alert\" tabindex=\"-1\" id=\"summary\">",
              "  <h2>There is 1 problem</h2>",
              "  <ul><li><a href=\"#email\">Enter an email address</a></li></ul>",
              "</div>",
              "",
              "<label for=\"email\">Email</label>",
              "<input id=\"email\" name=\"email\" type=\"email\" required",
              "       aria-invalid=\"true\" aria-describedby=\"email-error\">",
              "<p id=\"email-error\" class=\"error\">",
              "  ⚠ Enter an email address, like ada@example.com",
              "</p>",
              "",
              "<!-- role=\"alert\" announces the summary on appearance.",
              "     aria-describedby links field → explanation.",
              "     The ⚠ makes it non-color-dependent. -->",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Reading the form, stopping double-submits, and the trust line",
        body: [
          "When the form finally submits, FormData reads every named field in one line — no querySelector scavenging. For async submissions, disable the submit button the moment the request leaves and re-enable on completion; 'clicked Save twice, got two orders' is a rite of passage you skip. (Idempotency keys make even that safe on the server side — Phase 29.)",
          "Now the line that defines this course's architecture. Everything in this lesson can be bypassed in ten seconds: open the console, run document.querySelector('form').noValidate = true, call form.submit(). Or never open your page at all and POST straight at your API with curl. The browser's verdict is advice the user can ignore. So: friendly validation shapes the UX here; authoritative validation re-checks every rule in NestJS (Phase 20), and PostgreSQL constraints (Phase 13) hold the final line. The frontend is the bouncer. It never stops being one.",
        ],
        code: [
          {
            file: "submit.js — read, guard, hand off",
            lang: "js",
            code: [
              "const form = document.querySelector(\"form\");",
              "const submit = form.querySelector('button[type=\"submit\"]');",
              "",
              "form.addEventListener(\"submit\", async (event) => {",
              "  event.preventDefault();          // we're shipping data ourselves",
              "  const data = new FormData(form); // every named field, one call",
              "  submit.disabled = true;          // no double-submits",
              "  try {",
              "    const res = await fetch(\"/api/signup\", {",
              "      method: \"POST\",",
              "      body: JSON.stringify(Object.fromEntries(data)),",
              "      headers: { \"Content-Type\": \"application/json\" },",
              "    });",
              "    // The server now RE-VALIDATES everything. Its word is final.",
              "    console.log(res.status);       // 201 created, or 400 with field errors",
              "  } finally {",
              "    submit.disabled = false;",
              "  }",
              "});",
            ].join("\n"),
            caption: "form.reset() clears values (not files) when you need a fresh start — the summary of the form's small API surface.",
          },
        ],
      },
    ],
    mistake: {
      title: "The hand-rolled gate",
      wrong: [
        "function check() {",
        "  const email = document.querySelector(\"#email\").value;",
        "  if (!email.includes(\"@\")) {",
        "    alert(\"Bad email!\");        // not announced, blocks the thread,",
        "    return false;                // no focus, no i18n, color-blind-safe? no.",
        "  }",
        "  document.querySelector(\"form\").submit();",
        "}",
        "// <button type=\"button\" onclick=\"check()\">Sign up</button>",
      ].join("\n"),
      right: [
        "<input id=\"email\" name=\"email\" type=\"email\" required>",
        "// The engine blocks invalid submits and focuses the field.",
        "// Your handler only ever sees valid data:",
        "form.addEventListener(\"submit\", (e) => {",
        "  e.preventDefault();",
        "  // ...hand valid data to the server, which checks AGAIN.",
        "});",
      ].join("\n"),
      explain:
        "The hand-rolled gate ships five bugs: a bypassed type='button' that can miss Enter-key submits, alert() that screen readers handle poorly, no focus on the problem field, no localized messages, and a fragile string test that misses 'a@b' vs a real address. Constraints plus the submit-blocking engine remove all five — and the server re-check makes the sixth bug (trusting the browser) impossible.",
    },
    tryIt: [
      "Copy the signup form from section one into a file and open it. Submit empty — count how much feedback you got without writing any JavaScript.",
      "Add a username field with the pattern from section two, and deliberately break the title attribute. Compare how the browser phrases the two failure messages.",
      "Build the password-confirm pair from section three. Watch what happens to the native bubble when the fields match again (setCustomValidity('')).",
      "In the live lab above, fill only the email, flip novalidate ON, submit, then press the checkValidity() button. Read the flags it reports — which one names your empty username?",
      "Add the aria-describedby error paragraph from section four to a field, trigger the error, and inspect the accessibility pane: the field's description should now include your error text.",
    ],
    challenge: {
      prompt:
        "Build an RSVP form for a dinner event, entirely with native validation. Requirements: full name (required, min 2 chars); email (required, valid format); party size (number, 1–8); arrival date (required, cannot be before 2026-03-01, cannot be after 2026-12-31); dietary notes (optional textarea, max 200 chars); a required 'I agree to the house rules' checkbox; a labeled fieldset grouping two radio options (Vegetarian / Standard menu). On submit (prevented), log a FormData summary and disable the button while 'sending'.",
      hints: [
        "Radio groups: same name on both inputs, and wrap them in <fieldset><legend>Menu</legend> — the legend is the group's accessible name.",
        "The date bounds are min and max attributes with ISO values (YYYY-MM-DD). The checkbox uses required — 'must be checked'.",
        "maxlength=\"200\" covers the textarea. Object.fromEntries(new FormData(form)) turns entries into a loggable object; the checkbox only appears when checked.",
      ],
      solution: [
        "<form id=\"rsvp\">",
        "  <p>",
        "    <label for=\"name\">Full name</label>",
        "    <input id=\"name\" name=\"name\" required minlength=\"2\" autocomplete=\"name\">",
        "  </p>",
        "  <p>",
        "    <label for=\"email\">Email</label>",
        "    <input id=\"email\" name=\"email\" type=\"email\" required autocomplete=\"email\">",
        "  </p>",
        "  <p>",
        "    <label for=\"size\">Party size</label>",
        "    <input id=\"size\" name=\"size\" type=\"number\" min=\"1\" max=\"8\" value=\"2\">",
        "  </p>",
        "  <p>",
        "    <label for=\"date\">Arrival date</label>",
        "    <input id=\"date\" name=\"date\" type=\"date\" required",
        "           min=\"2026-03-01\" max=\"2026-12-31\">",
        "  </p>",
        "  <fieldset>",
        "    <legend>Menu</legend>",
        "    <p><label><input type=\"radio\" name=\"menu\" value=\"veg\" checked> Vegetarian</label></p>",
        "    <p><label><input type=\"radio\" name=\"menu\" value=\"standard\"> Standard</label></p>",
        "  </fieldset>",
        "  <p>",
        "    <label for=\"diet\">Dietary notes (optional)</label>",
        "    <textarea id=\"diet\" name=\"diet\" maxlength=\"200\"></textarea>",
        "  </p>",
        "  <p>",
        "    <label><input type=\"checkbox\" name=\"agree\" required>",
        "      I agree to the house rules</label>",
        "  </p>",
        "  <button type=\"submit\">Send RSVP</button>",
        "</form>",
        "",
        "<script>",
        "  const form = document.querySelector(\"#rsvp\");",
        "  const btn = form.querySelector('button[type=\"submit\"]');",
        "  form.addEventListener(\"submit\", (e) => {",
        "    e.preventDefault();              // engine already blocked invalid data",
        "    btn.disabled = true;             // double-submit guard",
        "    const payload = Object.fromEntries(new FormData(form));",
        "    console.log(\"RSVP payload:\", payload);",
        "    setTimeout(() => (btn.disabled = false), 1200); // fake network",
        "  });",
        "</script>",
        "",
        "<!-- Try it: submit with the checkbox unticked, pick party size 12,",
        "     choose a January date. Every failure is blocked, focused,",
        "     and explained — by the platform, in the user's language. -->",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A submit handler attached to a constrained form only runs when…",
        options: ["Every constraint already passed — the browser blocked invalid submits first", "Whenever the button is clicked", "After your own validation returns true", "Only if novalidate is present"],
        answer: 0,
        explain: "The native engine gates the submit event itself. If your handler fires, the form was valid at that moment — which is exactly why the server must still re-check: the gate lives on the user's machine.",
      },
      {
        q: "Which ValidityState flag names an empty required field?",
        options: ["valueMissing", "typeMismatch", "tooShort", "badInput"],
        answer: 0,
        explain: "valueMissing = required but empty. typeMismatch is format (bad email), tooShort is minlength. Named flags beat parsing message strings.",
      },
      {
        q: "setCustomValidity('') does what?",
        options: ["Clears a custom error — the field becomes valid again (if its other constraints pass)", "Deletes the field", "Disables validation", "Resets the form"],
        answer: 0,
        explain: "Any non-empty string makes the field invalid with that message; the empty string lifts the custom error. That's the whole cross-field API.",
      },
      {
        q: "Why pair pattern with title?",
        options: ["The browser weaves the title into its failure message", "title is required for pattern to work", "It improves SEO", "It sets the placeholder"],
        answer: 0,
        explain: "Without a title the bubble says only 'match the requested format' — useless. With one: 'match: 3 to 16 letters, digits, underscores'.",
      },
      {
        q: "The accessible way to attach an error message to a field is…",
        options: ["aria-invalid=\"true\" on the field + aria-describedby pointing at the error element", "Red text below the field", "alert() with the message", "title attribute"],
        answer: 0,
        explain: "The pairing is announced by screen readers and survives restyling. Color alone fails color-blind users; title hovers fail everyone on mobile.",
      },
      {
        q: "A user opens DevTools and sets form.noValidate = true. What just happened to your validation?",
        options: ["All browser-side checking is bypassed — which is why the server re-validates everything", "Nothing — constraints are server-enforced", "The form can no longer submit", "Only pattern checks are skipped"],
        answer: 0,
        explain: "The bouncer works for the user who owns them. noValidate, console submit(), and curl all bypass frontend checks — NestJS and Postgres constraints are the accountants.",
      },
    ],
    flashcards: [
      { front: "Constraint Validation API", back: "The browser's built-in form engine: checks required/type/pattern/min/max on submit, blocks invalid submissions, focuses and explains the first failing field." },
      { front: "ValidityState", back: "field.validity — an object of named failure flags: valueMissing, typeMismatch, patternMismatch, rangeUnderflow, rangeOverflow, tooShort, tooLong, valid." },
      { front: "checkValidity vs reportValidity", back: "checkValidity() returns true/false quietly; reportValidity() also shows the native bubble and focuses the field. Both are programmatic questions to the engine." },
      { front: "setCustomValidity(msg)", back: "Makes a field invalid with your message (cross-field rules like 'passwords match'). Pass '' to clear it. Keeps custom rules inside the native flow." },
      { front: "fieldset + legend", back: "Groups related controls (radios, checkboxes) under one announced name. Screen readers prefix each option with the legend — spacing alone groups nothing." },
      { front: "autocomplete attributes", back: "Hints like autocomplete=\"email\" / \"new-password\" that let browsers and password managers fill correctly. Free UX; wrong values cost users real time." },
      { front: "FormData", back: "new FormData(form) collects every named field — files included. Object.fromEntries() flattens it for JSON bodies (files need the entries as-is)." },
      { front: "The form trust line", back: "Native validation is UX (bouncer); NestJS re-validates every rule (accountant); Postgres constraints hold the final integrity line. Frontend verdicts are bypassable advice." },
    ],
    recap: [
      "Real elements with real labels: every field named, every group fieldset+legend.",
      "Constraints are attributes — required, type, pattern+title, min/max, minlength — not regex soup.",
      "The engine blocks invalid submits; checkValidity/reportValidity and ValidityState answer your questions.",
      "Errors get aria-invalid + aria-describedby, non-color cues, and an announced summary.",
      "The browser's verdict is advice: the server re-validates, the database constrains.",
    ],
    references: [
      { label: "MDN — Constraint validation", url: "https://developer.mozilla.org/en-US/docs/Web/HTML/Constraint_validation" },
      { label: "MDN — ValidityState", url: "https://developer.mozilla.org/en-US/docs/Web/API/ValidityState" },
      { label: "W3C WAI — Forms tutorial", url: "https://www.w3.org/WAI/tutorials/forms/" },
      { label: "web.dev — Learn Forms", url: "https://web.dev/learn/forms" },
      { label: "MDN — FormData", url: "https://developer.mozilla.org/en-US/docs/Web/API/FormData" },
    ],
    nextBridge:
      "Your forms now validate, explain, and hand off cleanly. Next: the other half of CSS mastery — why some styles win over others (the cascade, specificity, source order), what a box actually is (border-box vs content-box), and which units to reach for. Bring the DevTools habits from Phase 0.",
  },
  {
    id: "p01-l3",
    phaseId: "p01",
    title: "Cascade, Specificity & the Box Model",
    level: "Foundation",
    minutes: 40,
    summary:
      "'My CSS doesn't apply' is the number-one beginner bug — and it always has one of four causes: origin, !important, specificity, or source order. This lesson makes the cascade a courtroom you can predict, then covers the box model (border-box vs content-box), margin collapse, units, and inheritance — the mechanics every layout in this course stands on.",
    prerequisites: ["p01-l1 — semantic structure", "p01-l2 — forms (styling hooks used in examples)"],
    objectives: [
      "Predict which declaration wins using the cascade order: origin → !important → specificity → source order.",
      "Compute specificity triplets (id · class · element) and explain why inline styles beat selectors.",
      "Use :where() to zero specificity and explain why ID selectors in component CSS hurt reuse.",
      "Describe the box model and switch between border-box and content-box math without guessing.",
      "Explain vertical margin collapse and choose rem/em/%/px deliberately.",
      "Connect inheritance and custom properties to this course's own design tokens.",
    ],
    simple:
      "CSS stands for Cascading Style Sheets — the cascade is the whole point. When several rules want to style the same element, the browser runs a fixed tie-break: whose stylesheet wins (the browser's defaults lose to yours), whether anyone shouted !important (rarely, and it backfires), then specificity (a rule aimed through an ID outranks one aimed through a class, which outranks one aimed at a bare element), and finally, on a perfect tie, whoever was written later wins. That is the entire mystery of 'my style didn't work' — four questions, always in that order.",
    why:
      "Developers who don't know the cascade fight it: they nudge selectors until something sticks, then spray !important, then inherit a stylesheet where nothing can be overridden by anything. Developers who know it write CSS like law: low-specificity rules that are trivially overridable, one place where decisions live, and debugging that takes thirty seconds because the winner is predictable. The box model earns its slot the same way — every 'why is my card 40px too wide?' traces back to content-box math or a collapsed margin. These two systems are the physics of layout; everything in Phases 1–9 assumes you can do this arithmetic in your head.",
    mentalModel: {
      title: "The courtroom",
      body: "Picture a courtroom deciding one question: what color is this paragraph? First, jurisdiction (origin): the browser's built-in stylesheet testifies, then yours — author evidence outranks browser defaults. Next, conduct: a lawyer shouting !important demands attention — but judges hate shouters, other lawyers start shouting back, and the whole trial becomes noise; seasoned lawyers almost never shout. Then, rank (specificity): an ID is a detective, a class a sergeant, a bare element a rookie — rank beats headcount, so one detective outranks ten rookies. Finally, timing: equal rank, equal conduct? The last witness (source order) is freshest in memory and wins. Learn to run this trial in your head and CSS stops being weather and becomes law.",
    },
    sections: [
      {
        heading: "The cascade, in exact order",
        body: [
          "The algorithm answers four questions, in order, and stops at the first one that produces a winner. One — Origin: whose sheet is this? Browser defaults (user-agent) < your author styles < (rarely) user styles. This is why a bare <h1> has any styling at all before you write a line. Two — !important: an author !important beats normal declarations, which tempts people — until their own component needs to override it and must shout louder. Use it roughly never; framework overrides and user-preference styles are the legitimate niches.",
          "Three — Specificity: the rank of the selector that targeted the property (next section, with a live battle). Four — Source order: perfect tie? Later declaration wins. Two corollaries matter in practice: the cascade decides per property (color can come from one rule while padding comes from another), and inheritance fills gaps — properties like color and font flow down to children that nothing targeted, while layout properties like margin and border do not.",
          "Modern CSS adds @layer, which lets teams rank whole sheets of rules so a low-ranking layer can never beat a high-ranking one regardless of specificity — the tool design systems use to stay sane. You don't need layers for course projects yet, but when you join a team that uses them, you'll recognize the move: specificity wars outsourced to an explicit order.",
        ],
      },
      {
        heading: "Specificity — rank, not headcount",
        body: [
          "Every selector scores a triplet: (IDs · classes · elements). Count #things, then count .classes plus attributes ([type=email]) and pseudo-classes (:hover, :checked), then count element names and pseudo-elements (::before). Compare triplets left to right — the first non-zero column decides, like a three-digit number. So .nav a (0·1·1) beats nav ul li a (0·0·4): one sergeant outranks four rookies, forever.",
          "Three rules of thumb carry you through real codebases. Combinators and * add nothing. Inline styles (style=\"…\") beat every selector — another reason to avoid them; they're !important-light. And the modern escape hatches: :where(…) always scores zero — perfect for utility styles that must never fight — while :is(…) and :not(…) score whatever is inside them, so they don't lower your rank.",
          "The professional habit this installs: keep component CSS at class-level specificity (.card, .card__title). When everything sits at the same low rank, source order is the only tie-break, and 'who wins' becomes 'where do I write it' — a question you control. Sprinkle IDs through component styles and you've built a specificity ladder only !important can climb.",
        ],
        code: [
          {
            file: "specificity-cheatsheet.css",
            lang: "css",
            code: [
              "/* (0·0·2)  two elements */",
              "nav a { color: slategray; }",
              "",
              "/* (0·1·1)  class + element beats any number of elements */",
              ".nav a { color: teal; }",
              "",
              "/* (1·0·1)  one ID outranks every class-based selector */",
              "#nav a { color: gold; }",
              "",
              "/* (0·1·1 + :hover's 0·1·0 → 0·2·1) pseudo-classes count as classes */",
              ".nav a:hover { color: tomato; }",
              "",
              "/* (0·0·0)  :where() is ALWAYS zero — the override escape hatch */",
              ":where(.card) .title { margin-block: 0; }",
              "",
              "/* beats all selectors above — and is why we avoid inline styles */",
              "/* <a style=\"color: rebeccapurple\"> */",
            ].join("\n"),
          },
        ],
        demo: "specificity",
      },
      {
        heading: "The box model — what 'width' actually measures",
        body: [
          "Every element is a box of four nested bands: content (your text or image), padding (breathing room inside, colored with the element), border (the edge), and margin (push-away space outside, always transparent). The question that breaks beginners: when you write width: 300px, which band is 300px wide?",
          "Under the default content-box, width measures content only — padding and border are added on top, so a 300px card with 20px padding and a 2px border occupies 344px on screen. Under border-box, width measures the content+padding+border box, and the browser shrinks the content to fit. Every professional codebase — and this course's token system — sets * { box-sizing: border-box } on line one, because it makes width mean what it says. Margins sit outside either scheme and never count toward the box's width.",
          "One more physical law: vertical margin collapse. When two vertical margins meet (a heading above a paragraph), they don't add — the larger one wins. It feels like a bug until you realize it's why paragraphs get even spacing without double gaps. Flexbox and Grid containers don't collapse margins among children, which is one quiet reason layout feels more predictable there.",
        ],
        code: [
          {
            file: "box-model.css — do this on day one of every project",
            lang: "css",
            code: [
              "/* The two-line reset every project starts with: */",
              "*, *::before, *::after {",
              "  box-sizing: border-box;   /* width includes padding + border */",
              "  margin: 0;                /* take control of spacing yourself */",
              "}",
              "",
              ".card {",
              "  width: 320px;             /* the WHOLE card is 320px. Period. */",
              "  padding: 1.25rem;",
              "  border: 1px solid var(--line);",
              "  margin-bottom: 1rem;      /* sits outside the 320px */",
              "}",
              "",
              "h2 + p { margin-top: 0.5rem; }  /* siblings: no collapse worries */",
            ].join("\n"),
            caption: "var(--line) is one of this course's design tokens — a custom property, which the next section explains.",
          },
        ],
        demo: "box-model",
      },
      {
        heading: "Units — reaching for the right ruler",
        body: [
          "px is an absolute device-independent pixel — perfect for hairlines (1px borders) and anything that must not scale. rem is relative to the root font-size (16px by default): type and spacing in rem respect the user's browser font-size setting, which low-vision users change — making rem the accessibility-correct default for text and most spacing. em is relative to the current element's font-size: powerful inside components (padding that grows with the component's type) and dangerous when nested (sizes compound).",
          "% is relative to the parent — the fluid-layout workhorse — while vw/vh track the viewport itself. The modern sweet spot is clamp(min, preferred, max), which makes type and widths fluid between bounds in one line: clamp(1rem, 2.5vw, 1.5rem). Course rule of thumb: rem for type and spacing, % and clamp for fluid widths, px only for borders and shadows. If a layout 'breaks' when the user zooms text to 200%, px-based type is usually the culprit.",
        ],
      },
      {
        heading: "Inheritance, custom properties, and this course's tokens",
        body: [
          "Inheritance is how styles travel: color, font-family, line-height and friends flow from parent to child automatically — which is why setting font on <body> styles the whole page. Layout properties (margin, border, width, position) never inherit; every box negotiates its own. The DevTools Computed pane shows which side each property came from — inheritance or a direct rule.",
          "Custom properties (--brand, --surface, --line) are the one modern feature designed to inherit: declared once at the root, readable by every descendant, and overridable anywhere below. That is exactly how this platform's design tokens work — src/index.css defines the palette on :root, dark and light themes are just two mappings of the same names under [data-theme], and every component consumes variables instead of hard-coded colors. Flip the theme in the top bar: no component changed, only the mapping. In Phase 1 you'll build your own token set for the accessible-site milestone; the /design-tokens page documents the live system meanwhile.",
        ],
        code: [
          {
            file: "tokens-pattern.css — the inheritance trick behind themes",
            lang: "css",
            code: [
              ":root {",
              "  --surface: #ffffff;",
              "  --ink: #131c17;",
              "}",
              "[data-theme=\"dark\"] {",
              "  --surface: #121a16;   /* same names, new mapping */",
              "  --ink: #e9f0eb;",
              "}",
              "",
              ".panel {",
              "  background: var(--surface);  /* inherits the mapping,",
              "  color: var(--ink);              wherever it lives in the tree */",
              "}",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The !important arms race",
      wrong: [
        ".card { background: white !important; }",
        "",
        "/* later: the dark theme can't win without shouting louder */",
        "[data-theme=\"dark\"] .card {",
        "  background: #111 !important; /* now overrides need MORE importance… */",
        "}",
        "",
        "/* …until someone writes !important in a <style> tag's inline style.",
        "   Nobody can override anything anymore. The stylesheet is dead. */",
      ].join("\n"),
      right: [
        ".card { background: var(--surface); }  /* low specificity, token-driven */",
        "",
        "/* override by re-mapping the token — zero shouting: */",
        "[data-theme=\"dark\"] { --surface: #121a16; }",
        "",
        "/* or by source order at equal rank:",
        "   .card.dark-variant { … } written after .card wins the tie. */",
      ].join("\n"),
      explain:
        "!important wins today and loses tomorrow: the next override must shout louder, specificity stops meaning anything, and eventually an inline style plus !important ends the war with a scorched stylesheet. The escapes are the ones you now own — re-map a custom property, reorder equal-rank rules, or zero your base styles with :where() so anything can beat them.",
    },
    tryIt: [
      "Open the specificity battle above and type nav ul li.active a as a challenger. Before it ranks, predict its triplet (0·1·4) on paper — then verify.",
      "On any styled page, open DevTools → Elements, select an element, and read the Styles pane: crossed-out declarations are cascade losers. Find one that lost to specificity and one that lost to source order.",
      "Create a 300px-wide div with 20px padding and a 4px border under default content-box. Measure its on-screen width with DevTools (expected: 348px). Then apply the border-box reset and re-measure.",
      "Stack an <h1> and a <p>, each with 24px vertical margins. Inspect the gap between them (expected: 24px, collapsed — not 48px). Wrap both in a flex container and watch collapse disappear.",
      "Set html { font-size: 20px } and give a paragraph font-size: 1rem plus padding: 1rem. Watch both grow — then change the paragraph to em-based padding, nest it inside a bigger-font parent, and observe the compounding.",
    ],
    challenge: {
      prompt:
        "A teammate's stylesheet renders the Save button green when it must be the brand teal. WITHOUT adding selectors, without !important, and without inline styles: (1) determine the winner among the four rules below by computing each specificity and applying source order, (2) explain why it won, and (3) refactor so a single class-level rule controls the button and the team's override habit stays sustainable.\n\n/* file: theme.css — loaded second */\nbutton { background: green; }\n.form button { background: green; }\n\n/* file: base.css — loaded first */\n#save { background: green; }\n.save { background: green; }",
      hints: [
        "Score each: button (0·0·1), .form button (0·1·1), #save (1·0·0), .save (0·1·0). Which column decides first?",
        "Specificity is compared before source order — file load order only breaks ties. So 'loaded first' is a red herring for three of the four.",
        "Sustainable fix: remove the ID rule from markup/styles, keep one .save rule at class rank, and let load order or later equal-rank rules do the overriding. Tokens (--brand) make the value itself one variable.",
      ],
      solution: [
        "/* 1) Specificity scores:",
        "     button         → (0·0·1)",
        "     .form button   → (0·1·1)",
        "     .save          → (0·1·0)",
        "     #save          → (1·0·0)  ← WINNER: first non-zero column",
        "        (ID rank beats any count of classes; load order never",
        "         gets consulted because rank settled it.) */",
        "",
        "/* 2) The markup must have carried id=\"save\" — remove the ID",
        "     styling entirely; IDs in component CSS are a ladder that",
        "     only !important can climb. */",
        "",
        "/* 3) Refactor — one low-rank rule, token-driven: */",
        "/* base.css */",
        ":root { --brand: #3ddc97; }",
        ".save { background: var(--brand); }        /* (0·1·0) */",
        "",
        "/* theme.css — equal rank, later source order wins ties: */",
        ".save.save--secondary {                    /* (0·2·0) when needed */",
        "  background: var(--surface-2);",
        "}",
        "",
        "/* Now every future override is a choice, not a shouting match:",
        "   re-map --brand for themes, add a modifier class for variants,",
        "   or simply write a later rule at the same rank. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "The specificity of #nav ul li.active a is…",
        options: ["(1 · 1 · 3)", "(1 · 2 · 3)", "(0 · 1 · 4)", "(2 · 1 · 2)"],
        answer: 0,
        explain: "One ID (#nav), one class (.active), three elements (ul, li, a). Combinators (spaces) contribute nothing. Compare triplets left to right.",
      },
      {
        q: "Two rules tie on specificity. Who wins?",
        options: ["The one written later in the source (or loaded later)", "The one in the <head>", "Alphabetically first property", "Neither — the browser averages them"],
        answer: 0,
        explain: "Source order is the cascade's final tie-break, which is why stylesheet order and rule order are real decisions, not trivia.",
      },
      {
        q: "What does :where(.card .title) score?",
        options: ["Zero — always. It's the deliberate escape hatch.", "(0·2·0), same as the inside", "(1·0·0)", "It depends on the browser"],
        answer: 0,
        explain: ":where() is specificity-neutral by design: base styles written with it can be overridden by any later rule at any rank. :is() scores its insides instead — pick deliberately.",
      },
      {
        q: "Under box-sizing: border-box, width: 300px means…",
        options: ["Content + padding + border total 300px; content shrinks to fit", "Content is 300px; padding and border add on", "The margin is included too", "The element cannot have padding"],
        answer: 0,
        explain: "border-box makes width honest: the visible box stays 300px. Margins always sit outside both schemes.",
      },
      {
        q: "An h2 with margin-bottom: 24px sits above a p with margin-top: 24px. The gap is…",
        options: ["24px — vertical margins collapse to the larger", "48px — margins always add", "0px — margins cancel", "Undefined"],
        answer: 0,
        explain: "Vertical sibling margins collapse: largest wins. Flex/Grid children don't collapse — one reason modern layout feels predictable.",
      },
      {
        q: "1rem equals…",
        options: ["The root element's font-size (16px by default)", "The parent's font-size", "Always exactly 16px", "1% of the viewport width"],
        answer: 0,
        explain: "rem tracks the root font-size, so users who raise their browser default get your spacing and type scaled too — the accessibility argument for rem over px. em tracks the parent instead (compounding risk); 1vw is the viewport unit.",
      },
    ],
    flashcards: [
      { front: "The cascade", back: "CSS's tie-break order: origin → !important → specificity → source order. Per property, always in that sequence." },
      { front: "Specificity triplet", back: "(IDs · classes + attributes + pseudo-classes · elements + pseudo-elements), compared left to right. Combinators and * score zero." },
      { front: "!important", back: "A declaration that shouts past normal cascade order — and invites an arms race. Use ~never; prefer tokens, source order, or :where()." },
      { front: ":where() vs :is()", back: ":where() always scores zero (override-friendly defaults); :is() scores its contents (grouping without typing). Same matching, different rank." },
      { front: "Box model", back: "Content → padding → border → margin, nested outward. Padding colors with the element; margin is transparent push-away outside everything." },
      { front: "border-box", back: "width = content + padding + border. Set it globally (* { box-sizing: border-box }) so width means the whole visible box." },
      { front: "Margin collapse", back: "Adjacent vertical margins merge to the larger one (block flow only). Flexbox/Grid children never collapse." },
      { front: "rem vs em", back: "rem = root font-size (user settings respected — the a11y default). em = current font-size (great inside components, compounds when nested)." },
    ],
    recap: [
      "The cascade answers four questions in order: origin, !important, specificity, source order — and stops at the first winner.",
      "Specificity is rank: (id · class · element), left column decides; inline styles beat selectors; :where() is your zero.",
      "border-box on line one of every project; width becomes honest; margins never count.",
      "Vertical margins collapse; flex/grid children don't.",
      "rem for type and spacing, % / clamp for fluid, px for hairlines — and tokens are inheritance made programmable.",
    ],
    references: [
      { label: "MDN — Cascade, specificity, and inheritance", url: "https://developer.mozilla.org/en-US/docs/Web/CSS/Cascade" },
      { label: "MDN — Specificity", url: "https://developer.mozilla.org/en-US/docs/Web/CSS/Specificity" },
      { label: "MDN — box-sizing", url: "https://developer.mozilla.org/en-US/docs/Web/CSS/box-sizing" },
      { label: "web.dev — Learn CSS", url: "https://web.dev/learn/css" },
      { label: "MDN — CSS values and units", url: "https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Styling_basics/Values_and_units" },
    ],
    nextBridge:
      "You can now predict winners and measure boxes — the physics is installed. Next lesson puts boxes in formation: Flexbox for one-dimensional flow, Grid for two-dimensional plans, and the responsive patterns (fluid grids, clamp, container thinking) that make one layout serve every screen.",
  },
  {
    id: "p01-l4",
    phaseId: "p01",
    title: "Flexbox, Grid & Responsive Layout",
    level: "Foundation",
    minutes: 45,
    summary:
      "Two layout systems cover every interface you will ever build: Flexbox arranges items along one axis; Grid places them on a two-dimensional map. This lesson teaches when to reach for which, the five properties that do 90% of the work, and the modern responsive toolkit — gap, fr, auto-fill/minmax, and clamp — that makes media queries the last resort instead of the first.",
    prerequisites: ["p01-l3 — the box model, units, and the border-box reset"],
    objectives: [
      "Choose between Flexbox (content-out, one axis) and Grid (layout-in, two axes) without folklore.",
      "Drive a flex container with direction, justify-content, align-items, wrap, and gap — and an item with the flex shorthand.",
      "Build fluid grids with repeat(auto-fill, minmax(…, 1fr)) and place whole page regions with grid-template-areas.",
      "Make layouts responsive with fluid units and intrinsic sizing first, media queries last.",
      "Verify reflow at 200% zoom and across viewport widths using the DevTools device toolbar.",
    ],
    simple:
      "Flexbox is a bookshelf: one rail, and you decide how the books sit on it — pushed left, centered, spread evenly, wrapped to a second shelf if they overflow. Grid is a city plan: you draw the streets first (columns and rows), then place buildings on the blocks — a header spanning the top, a sidebar column, a main block. You don't city-plan a bookshelf and you don't put a city on a shelf. Learn the two metaphors and every layout question becomes 'rail or map?'.",
    why:
      "Before these two systems, responsive design meant floating divs and pixel-counting breakpoints — and tutorials from that era still float around, which is why old code looks alien. Today, the five properties in this lesson build toolbars, navbars, card walls, dashboards, and whole page skeletons with almost no media queries. Every React component tree you style from Phase 4 onward is arranged by flex and grid; the developers who struggle with UI are almost always the ones who never learned the axis rules and reach for absolute positioning instead.",
    mentalModel: {
      title: "Bookshelf vs city plan",
      body: "Flexbox asks: 'these items share a line — how should the space be shared?' You answer with justify (along the rail), align (across the rail), and wrap (overflow to the next rail). The items' sizes can bend: grow takes leftover space, shrink gives some back. Grid asks the opposite question: 'here is the map — where does each region live?' You answer with tracks (columns/rows sized in fr shares or minmax bounds) and placement (grid-area names read like an ASCII sketch of the page). Content deciding the layout → flex. Layout deciding where content lives → grid. And they nest happily: a grid page full of flex toolbars is the normal shape of a real app.",
    },
    sections: [
      {
        heading: "Flexbox — one axis, five properties",
        body: [
          "A flex container takes over one axis (the main axis, set by flex-direction: row or column). justify-content distributes items ALONG that axis; align-items lines them up ACROSS it. Swap the direction and the two swap jobs — that one fact explains most 'why didn't my centering work' confusion.",
          "The rest of the toolkit is small: flex-wrap lets items overflow onto new rails instead of squishing; gap puts even space between items (no more margin hacks on last children); and items negotiate size with the flex shorthand — flex: 1 1 200px means 'grow to take leftovers, shrink if forced, start from 200px'. order resequences visually without touching the DOM, but remember screen readers and Tab order still follow the DOM — visual reorder is a layout spice, not a structural tool.",
        ],
        code: [
          {
            file: "toolbar.css — the shape of every nav you'll build",
            lang: "css",
            code: [
              ".toolbar {",
              "  display: flex;",
              "  align-items: center;     /* vertical centering, done */",
              "  gap: 0.75rem;",
              "}",
              ".toolbar nav { display: flex; gap: 0.5rem; }",
              "",
              "/* 'push this to the far end' — the classic move: */",
              ".toolbar .actions { margin-inline-start: auto; }",
              "",
              "/* items that share leftovers equally: */",
              ".stat { flex: 1 1 160px; }  /* grow shrink basis */",
            ].join("\n"),
          },
        ],
        demo: "flex-grid",
      },
      {
        heading: "Grid — the map comes first",
        body: [
          "Grid inverts flexbox: you define tracks first, then items flow into cells. grid-template-columns: 200px 1fr says 'a fixed rail plus one column that drinks all leftover space' — fr units are shares of leftover space, not percentages of the whole. repeat(4, 1fr) is four equal shares; repeat(auto-fill, minmax(220px, 1fr)) is the responsive workhorse: lay as many ≥220px tracks as fit, stretch them to fill — tracks appear and vanish as the window resizes, with zero media queries.",
          "For whole-page skeletons, grid-template-areas turns layout into an ASCII drawing: 'header header / nav main / footer footer' reads exactly like the page it builds, and each region claims its name with grid-area. Re-arranging the page for mobile is editing a three-line string, not rewriting selectors. auto-fill vs auto-fit differ only when items are few: fit stretches the last items to fill the row, fill keeps empty tracks — for card walls, fill keeps widths stable.",
        ],
        code: [
          {
            file: "page.css — a skeleton you will reuse in every project",
            lang: "css",
            code: [
              ".app {",
              "  display: grid;",
              "  min-height: 100dvh;",
              "  grid-template-rows: auto 1fr auto;   /* header / stretchy middle / footer */",
              "}",
              "",
              ".cards {",
              "  display: grid;",
              "  grid-template-columns: repeat(auto-fill, minmax(min(100%, 240px), 1fr));",
              "  gap: 1rem;",
              "}",
              "/* min(100%, 240px): never demand more than the container —",
              "   one card on a phone, five on a desktop, no breakpoint. */",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Which one, when — the decision takes four seconds",
        body: [
          "Ask one question: does the CONTENT decide the layout, or does the LAYOUT decide where content lives? A row of tags, a toolbar, a breadcrumb — content of unknown count sharing a line — that is flex. A page skeleton, a card wall, a settings form with label/control columns — space allocated first — that is grid. When in doubt: single line of unknown-length items → flex; anything you could sketch as named regions → grid.",
          "They compose constantly and should: this very course platform is a grid page (sidebar rail + content column) full of flex rows (topbar, chips, buttons). Anti-patterns to retire today: float layouts, position: absolute for centering (use place-items: center), and tables-for-layout (tables are for data — remember P01-L1).",
        ],
      },
      {
        heading: "Responsive without breakpoint soup",
        body: [
          "Modern responsiveness is three tools in priority order. One, intrinsic and fluid sizing: %, fr, minmax, and min()/max()/clamp() let elements negotiate with their container — the cards grid above is fully responsive with no breakpoint. Two, wrapping systems: flex-wrap and auto-fill grids reflow themselves. Three — only when structure genuinely changes (sidebar moves under content, a table becomes cards) — a media query, written mobile-first with min-width so rules ADD as screens grow instead of undoing each other.",
          "Two habits make this stick. Test with the DevTools device toolbar AND by dragging the window edge slowly — watch where your layout actually breaks, not where Bootstrap says it should. And test zoom: WCAG requires content to reflow at 200% zoom on a desktop-width window — if your layout needs horizontal scrolling there, fluid units are missing somewhere. Container queries (Baseline 2023) let a card respond to ITS panel instead of the viewport; know they exist for component libraries — viewport media queries still cover course projects.",
        ],
        code: [
          {
            file: "responsive.css — structure changes, not size tweaks",
            lang: "css",
            code: [
              "/* mobile-first: single column is the default */",
              ".with-sidebar {",
              "  display: grid;",
              "  gap: 1.5rem;",
              "}",
              "",
              "/* ONLY when there is room for two real columns: */",
              "@media (min-width: 860px) {",
              "  .with-sidebar { grid-template-columns: 220px 1fr; }",
              "}",
              "",
              "/* fluid type — one line instead of three breakpoints: */",
              "h1 { font-size: clamp(1.6rem, 1rem + 2.5vw, 2.6rem); }",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "Breakpoint soup",
      wrong: [
        ".card { width: 300px; }",
        "@media (max-width: 1100px) { .card { width: 260px; } }",
        "@media (max-width: 900px)  { .card { width: 220px; } }",
        "@media (max-width: 700px)  { .card { width: 45%; } }",
        "@media (max-width: 480px)  { .card { width: 100%; } }",
        "/* five overrides, still wrong at 1280px on a 13\" laptop,",
        "   and every new screen size is a new bug report. */",
      ].join("\n"),
      right: [
        ".cards {",
        "  display: grid;",
        "  grid-template-columns: repeat(auto-fill, minmax(min(100%, 240px), 1fr));",
        "  gap: 1rem;",
        "}",
        "/* one rule, every width: the container decides the count,",
        "   the cards share leftovers. No breakpoints to maintain. */",
      ].join("\n"),
      explain:
        "Fixed widths force you to enumerate screen sizes you can't know — every device between your breakpoints is a coin flip. Intrinsic sizing hands the decision to the container: minmax bounds each card, auto-fill counts tracks, 1fr distributes leftovers. Keep media queries for genuine structure changes (one column becoming two), never for size nudges.",
    },
    tryIt: [
      "In the playground above, set flexbox to direction: row, justify: space-between, align: center — the universal toolbar recipe. Then switch direction to column and predict what justify/align now control before you look.",
      "Add items until the flex row wraps, then turn wrap off and watch items shrink instead. Which property changed the negotiation?",
      "Switch to the grid 'cards' preset and drag the min track slider while resizing the window. Count how many breakpoints exist (expected: zero).",
      "Build the 'holy grail': copy the grid-template-areas skeleton from section two into a file, fill each area with a landmark element (header/nav/main/footer), and make the nav area disappear below 700px with ONE mobile-first media query.",
      "Open this course site's dashboard, press Ctrl/Cmd + and zoom to 200%. Does anything force horizontal scrolling? That is the WCAG reflow check — run it on everything you build.",
    ],
    challenge: {
      prompt:
        "Build a responsive 'release dashboard' page: a header bar (logo left, nav center-ish, user chip right — flex, with the chip pushed to the far end), a stats row of four equal cards that wrap on narrow screens, and a main grid of release cards using repeat(auto-fill, minmax(min(100%, 240px), 1fr)). Requirements: exactly ONE media query, and it must change structure (e.g., header stacks), not sizes; every gap via the gap property; verify at 360px, 768px, and 1440px widths and at 200% zoom.",
      hints: [
        "Header: display:flex; align-items:center; gap; then .user-chip { margin-inline-start: auto }. At small widths, flex-direction: column inside your single min-width query… or better: keep flex and let it wrap — either way, one query total.",
        "Stats row: a second grid with repeat(auto-fit, minmax(min(100%, 180px), 1fr)) — auto-fit stretches the last cards so four fill a wide row cleanly.",
        "The release-card grid is the exact rule from section two. If you are writing widths in px anywhere, stop — the minmax line is doing that job.",
      ],
      solution: [
        "<!doctype html>",
        "<html lang=\"en\">",
        "<head>",
        "  <meta charset=\"utf-8\">",
        "  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">",
        "  <title>Release dashboard</title>",
        "  <style>",
        "    * { box-sizing: border-box; margin: 0; }",
        "    body { font: 16px/1.5 system-ui; padding: 1rem; }",
        "",
        "    .bar {",
        "      display: flex; align-items: center; gap: 1rem;",
        "      padding: .75rem 1rem; border: 1px solid #ddd; border-radius: 10px;",
        "    }",
        "    .bar nav { display: flex; gap: .75rem; }",
        "    .bar .me { margin-inline-start: auto; }  /* pushed to far end */",
        "",
        "    .stats {",
        "      display: grid; margin-top: 1rem;",
        "      grid-template-columns: repeat(auto-fit, minmax(min(100%, 180px), 1fr));",
        "      gap: 1rem;",
        "    }",
        "",
        "    .releases {",
        "      display: grid; margin-top: 1rem;",
        "      grid-template-columns: repeat(auto-fill, minmax(min(100%, 240px), 1fr));",
        "      gap: 1rem;",
        "    }",
        "",
        "    .card { border: 1px solid #ddd; border-radius: 10px; padding: 1rem; }",
        "",
        "    /* THE one structural breakpoint: header stacks vertically */",
        "    @media (max-width: 560px) {",
        "      .bar { flex-direction: column; align-items: stretch; }",
        "      .bar .me { margin-inline-start: 0; }",
        "    }",
        "  </style>",
        "</head>",
        "<body>",
        "  <header class=\"bar\">",
        "    <strong>ShipYard</strong>",
        "    <nav aria-label=\"Main\"><a href=\"#\">Deploys</a><a href=\"#\">Queue</a></nav>",
        "    <span class=\"me\">ada@shipyar​d.dev</span>",
        "  </header>",
        "",
        "  <section class=\"stats\" aria-label=\"Stats\">",
        "    <article class=\"card\"><h2>142</h2><p>deploys</p></article>",
        "    <article class=\"card\"><h2>99.98%</h2><p>uptime</p></article>",
        "    <article class=\"card\"><h2>3</h2><p>rollbacks</p></article>",
        "    <article class=\"card\"><h2>61s</h2><p>median build</p></article>",
        "  </section>",
        "",
        "  <main class=\"releases\">",
        "    <!-- any number of cards; the grid counts tracks itself -->",
        "    <article class=\"card\"><h2>v2.4.1</h2><p>fix: token refresh</p></article>",
        "    <article class=\"card\"><h2>v2.4.0</h2><p>feat: dark mode</p></article>",
        "    <article class=\"card\"><h2>v2.3.2</h2><p>fix: rate limits</p></article>",
        "  </main>",
        "</body>",
        "</html>",
        "",
        "<!-- Verify: 360px → single column, header stacked. 768px → cards",
        "     wrap to 2–3 tracks. 1440px → 4–5 tracks. 200% zoom → still",
        "     no horizontal scroll. One media query total, zero px widths. -->",
      ].join("\n"),
    },
    quiz: [
      {
        q: "In a flex row, justify-content controls…",
        options: ["Distribution along the main axis (horizontal for row)", "Vertical alignment of items", "Item order in the DOM", "Wrapping to new lines"],
        answer: 0,
        explain: "justify works along the main axis — which flips to vertical when direction is column. align-items handles the cross axis. Swapping axes is the #1 centering confusion.",
      },
      {
        q: "flex: 1 1 200px means…",
        options: ["Grow to take leftovers, shrink under pressure, start from a 200px basis", "The item is exactly 200px always", "Grow only, never shrink, from 1px", "It's invalid shorthand"],
        answer: 0,
        explain: "The triple is grow · shrink · basis: items start at 200px, share leftover space equally (grow 1), and give space back when crowded (shrink 1).",
      },
      {
        q: "repeat(auto-fill, minmax(220px, 1fr)) creates…",
        options: ["As many ≥220px tracks as fit, each stretching to share leftover space", "Exactly 220 columns", "One column of 220px", "A 220px gap between tracks"],
        answer: 0,
        explain: "auto-fill lays tracks until none fit; minmax bounds each track's size; 1fr distributes leftovers. Together: responsive columns with zero media queries.",
      },
      {
        q: "The fr unit measures…",
        options: ["A share of leftover space in a grid track", "A fraction of the viewport", "Pixels divided by 16", "A percentage of the parent"],
        answer: 0,
        explain: "1fr 2fr splits leftovers 1:2 AFTER fixed tracks are subtracted — unlike %, which ignores siblings' demands.",
      },
      {
        q: "When is a media query the right tool?",
        options: ["When the layout structure genuinely changes (one column → two)", "To nudge card widths at common screen sizes", "To set font sizes at three breakpoints instead of clamp", "Always — mobile-first demands them"],
        answer: 0,
        explain: "Fluid sizing and wrapping handle size changes; breakpoints are for structural re-arrangement. Mobile-first means min-width rules that add, not max-width rules that undo.",
      },
      {
        q: "grid-template-areas earns its keep because…",
        options: ["The page skeleton reads like an ASCII sketch and re-arranges by editing strings", "It's the only way to make columns", "It's faster to render than flex", "It replaces the need for semantic HTML"],
        answer: 0,
        explain: "'header header / nav main / footer footer' IS the layout document — regions claim areas by name, and responsive re-arrangement edits the sketch, not selectors.",
      },
    ],
    flashcards: [
      { front: "Flexbox", back: "One-axis layout: items share a line. justify = along the axis, align = across it, wrap = overflow rails, gap = even spacing." },
      { front: "Grid", back: "Two-axis layout: define tracks first (columns/rows), place items into cells. fr shares leftovers; minmax bounds; areas sketch the page." },
      { front: "fr unit", back: "One share of leftover grid space. 200px 1fr = fixed rail + fluid column. 1fr 1fr 1fr = three equal shares of what remains." },
      { front: "auto-fill + minmax", back: "repeat(auto-fill, minmax(min(100%, Xpx), 1fr)) — responsive card walls with zero breakpoints: track count follows the container." },
      { front: "flex shorthand", back: "grow · shrink · basis (flex: 1 1 200px). Start at the basis, grow into leftovers, shrink under crowding." },
      { front: "margin-inline-start: auto", back: "The flex trick that pushes an item to the far end — auto margins eat all leftover space before it." },
      { front: "Mobile-first media queries", back: "Default styles serve small screens; @media (min-width: …) adds structure as space appears. Rules accumulate instead of undoing." },
      { front: "Reflow test", back: "Zoom to 200% at desktop width — WCAG requires no horizontal scrolling. Fluid units and wrapping grids pass; fixed px widths fail." },
    ],
    recap: [
      "Rail or map? Content sharing a line → flex; space allocated first → grid. They nest constantly.",
      "Flex: direction sets the axis, justify works along it, align across it, gap spaces it, the flex shorthand negotiates size.",
      "Grid: tracks in fr/minmax, pages sketched with template-areas, card walls with auto-fill.",
      "Responsive order of operations: fluid/intrinsic sizing → wrapping → media queries for structure only.",
      "Verify with the device toolbar and 200% zoom — reflow is an accessibility requirement, not a nice-to-have.",
    ],
    references: [
      { label: "MDN — Flexbox", url: "https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_flexible_box_layout" },
      { label: "MDN — Grid layout", url: "https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout" },
      { label: "web.dev — Learn CSS", url: "https://web.dev/learn/css" },
      { label: "CSS-Tricks — A Complete Guide to Flexbox", url: "https://css-tricks.com/snippets/css/a-guide-to-flexbox/" },
      { label: "CSS-Tricks — A Complete Guide to Grid", url: "https://css-tricks.com/snippets/css/complete-guide-grid/" },
    ],
    nextBridge:
      "Layout is solved — now make it a system. The final Phase 1 lesson turns every decision you've been hard-coding (colors, radii, spacing, type) into named design tokens on custom properties, builds a dark mode that is a mapping rather than a rewrite, and dissects this course platform's own token file as the working example.",
  },
  {
    id: "p01-l5",
    phaseId: "p01",
    title: "Design Tokens, Custom Properties & Dark Mode",
    level: "Foundation",
    minutes: 35,
    summary:
      "A design system is decisions, named once. This lesson builds the token vocabulary — brand, surfaces, ink, states, spacing, radii, motion — on CSS custom properties, then implements dark mode the professional way: not an inverted copy, but a second mapping under [data-theme]. The working example is this course platform itself — its real token file, its real toggle.",
    prerequisites: ["p01-l3 — cascade & custom-property inheritance", "p01-l4 — layout patterns the tokens will style"],
    objectives: [
      "Refactor hard-coded values into semantic custom properties and explain why role names beat appearance names.",
      "Structure a token set: brand, surfaces, ink hierarchy, states, spacing scale, radii, shadows, motion.",
      "Implement theme switching as a remapping under [data-theme] — one component set, many skins.",
      "Build dark mode correctly: separate mappings, contrast-verified pairs (4.5:1), tuned shadows and images.",
      "Respect prefers-color-scheme for the first paint and persist the user's explicit choice.",
    ],
    simple:
      "Right now, somewhere in a stylesheet, a color is spelled #3ddc97 in forty places. When the brand changes, that is forty hunts. Design tokens flip it: the decision lives once — --brand — and every button, link, and badge just says 'use the brand'. A theme (light, dark, high-contrast) is then just a second set of values for the same names, hung off a data attribute. The components never change; only the dictionary does. Flip the theme toggle in this site's top bar — that entire effect is one attribute changing which dictionary is active.",
    why:
      "Consistency is not a vibe; it is a structural property. Tokenized UIs change their mind in one line, pass audits because contrast pairs are decided deliberately instead of per-component, and onboard new developers because the palette is a readable list instead of an archaeology dig. Dark mode is the forcing function: sites that hard-coded 'white background, near-black text' discover in an afternoon that they have 300 decisions to re-make — while tokenized sites remap a dozen names. Every design system you will meet in your career (Material, Radix, shadcn, this course) is tokens under the hood.",
    mentalModel: {
      title: "The paint store swatch wall",
      body: "A builder doesn't tell the crew 'mix 18% green with 82%…' — they point at the swatch wall: 'the hallway green'. Every room references the swatch, never the formula. Repaint the swatch and the whole house updates; add a 'night shift' wall with the same swatch names in different formulas and the crew can work either shift without relearning anything. Custom properties are the swatch wall; :root is where it hangs; [data-theme=\"dark\"] is the night-shift wall. The one rule that keeps it sane: swatches are named for ROLES ('trim', 'hallway'), never for formulas ('that greenish one') — because night-shift trim won't be green at all.",
    },
    sections: [
      {
        heading: "From hard-coded to named",
        body: [
          "The refactor is mechanical and instantly legible. Every repeated value becomes a custom property declared once at :root — the pseudo-element that matches the document root, so every descendant inherits it. Consumers use var(--name), optionally with a fallback var(--name, #333) that only matters if the token is undefined (handy in isolated widgets; in an app, a missing token should be loud, not silently patched).",
          "Name by role, never by appearance. --ink-2 says 'secondary text' and stays correct in both themes; --dark-gray is a lie the moment dark mode ships and it becomes light gray. The discipline feels slow for an hour and pays for itself for years: when a designer says 'make secondary text softer', you touch one line and the answer is consistent everywhere, by construction.",
        ],
        code: [
          {
            file: "tokens-refactor.css — before and after",
            lang: "css",
            code: [
              "/* BEFORE: the same decision, forty spellings */",
              ".btn  { background: #3ddc97; color: #06251a; }",
              ".link { color: #3ddc97; }",
              ".chip-active { border-color: #3ddc97; background: #123527; }",
              "",
              "/* AFTER: the decision lives once */",
              ":root {",
              "  --brand: #3ddc97;",
              "  --brand-contrast: #06251a;  /* text ON the brand */",
              "  --brand-soft: #123527;      /* tinted background */",
              "}",
              ".btn  { background: var(--brand); color: var(--brand-contrast); }",
              ".link { color: var(--brand); }",
              ".chip-active { border-color: var(--brand); background: var(--brand-soft); }",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Anatomy of a token set",
        body: [
          "A complete set has layers. Primitives are the raw palette (a green ramp, a gray ramp) — most app code never touches them. Semantic tokens are the working vocabulary: surfaces (bg, surface, surface-2…), ink (ink, ink-2, muted…), lines (line, line-2), the brand family, and state colors (success, warn, danger, info). Each layer above consumes the one below, so re-theming means remapping semantics, not hunting primitives.",
          "Beyond color: a spacing scale (keep it tight — 4·8·12·16·24·32·48 — and name the steps), radii (--r-sm, --r, --r-lg), elevation (--shadow-1/2 for resting vs lifted), type (a display face, a body face, a mono face as font-family tokens), and motion (one easing curve, two or three durations). The course platform's /design-tokens page renders this exact inventory live. Resist tokenizing one-off values — a token is a decision you expect to reuse or re-make; a magic number used once should stay a number.",
        ],
      },
      {
        heading: "Themes are mappings, not copies",
        body: [
          "Here is the whole trick, exactly as this site implements it in src/index.css: declare every token under :root (the dark mapping is the default), then re-declare the SAME names under :root[data-theme=\"light\"] with light-appropriate values. Components reference only names, so the entire UI re-skins when one attribute flips — no component knows or cares which theme is live.",
          "Scope works downward too: override tokens on a subtree and only that subtree re-skins — the Token Lab below does exactly this on its preview pane, which is also how you build widget-level themes, email-safe sections, or per-tenant branding later. Custom properties inherit; that single fact is why the pattern scales from a toggle to a multi-brand platform.",
        ],
        code: [
          {
            file: "src/index.css — the real pattern from this platform (abridged)",
            lang: "css",
            code: [
              ":root, :root[data-theme=\"dark\"] {",
              "  --bg: #0b100e;      --surface: #121a16;",
              "  --ink: #e9f0eb;     --muted: #8aa196;",
              "  --line: #233029;    --brand: #3ddc97;",
              "  color-scheme: dark; /* native controls + scrollbars follow */",
              "}",
              "",
              ":root[data-theme=\"light\"] {",
              "  --bg: #eef2ec;      --surface: #fafbf8;",
              "  --ink: #131c17;     --muted: #66786e;",
              "  --line: #d8e0d7;    --brand: #0b7a55;  /* darker: same role,",
              "  color-scheme: light;                        readable on light */",
              "}",
              "",
              "/* components consume names only — they never change: */",
              ".panel { background: var(--surface); border: 1px solid var(--line); }",
            ].join("\n"),
            caption: "color-scheme tells the browser to theme scrollbars, form controls, and autofill to match — a one-line detail beginners miss.",
          },
        ],
        demo: "token-lab",
      },
      {
        heading: "Dark mode, done properly",
        body: [
          "Dark mode is a redesign, not an invert. Three rules separate the professional version. One: re-map, don't flip — pure black backgrounds glare and pure white text halos; use off-black surfaces (like #0b100e here) and off-white ink, and desaturate the brand (light theme's #0b7a55 vs dark's #3ddc97 — same role, tuned per surface). Two: verify contrast per pair — WCAG asks 4.5:1 for body text, 3:1 for large text and UI parts; the Token Lab computes the ratio live, and browser DevTools now show it inline in the color picker. Three: tune the physics — shadows barely read on dark (rely on lighter borders and elevation tints), images may need slight dimming, and focus rings must stay visible on both.",
          "For behavior: read prefers-color-scheme for the FIRST paint so the site matches the OS, let the user's explicit toggle win afterwards, and persist that choice (this platform stores it and applies it before first paint, so there is no theme flash). Never trap users in a theme they didn't choose — the system setting is a default, not a verdict.",
        ],
        code: [
          {
            file: "theme-boot.js — OS default, user override, no flash",
            lang: "js",
            code: [
              "// Runs in a <script> in <head>, BEFORE first paint:",
              "const stored = localStorage.getItem(\"theme\");        // user's explicit choice",
              "const prefersDark = matchMedia(\"(prefers-color-scheme: dark)\").matches;",
              "document.documentElement.dataset.theme =",
              "  stored ?? (prefersDark ? \"dark\" : \"light\");",
              "",
              "// The toggle simply writes the choice back:",
              "function toggleTheme() {",
              "  const next = document.documentElement.dataset.theme === \"dark\" ? \"light\" : \"dark\";",
              "  document.documentElement.dataset.theme = next;",
              "  localStorage.setItem(\"theme\", next);",
              "}",
              "",
              "// CSS does all the visual work — JS never touches a style.",
            ].join("\n"),
            caption: "localStorage is a browser API — it does not exist on the server. Guard it (typeof check or try/catch) in any code that might run in Node, a lesson Phase 8 repeats for SSR.",
          },
        ],
      },
    ],
    mistake: {
      title: "Appearance-named tokens",
      wrong: [
        ":root {",
        "  --green: #3ddc97;",
        "  --dark-gray: #121a16;",
        "  --light-text: #e9f0eb;",
        "}",
        "/* dark mode arrives: */",
        ":root[data-theme=\"dark\"] {",
        "  --dark-gray: #121a16;   /* …already dark. The name now lies",
        "  --light-text: #e9f0eb;      in BOTH themes, and every consumer",
        "}                              must be audited by eye. */",
      ].join("\n"),
      right: [
        ":root {",
        "  --surface: #121a16;   /* 'the card background' — a ROLE */",
        "  --ink: #e9f0eb;       /* 'primary text' */",
        "}",
        ":root[data-theme=\"light\"] {",
        "  --surface: #fafbf8;   /* same role, honest value */",
        "  --ink: #131c17;",
        "}",
        "/* consumers never change; names stay true in every theme. */",
      ].join("\n"),
      explain:
        "Appearance names encode the VALUE in the NAME, so any re-mapping makes the name a liar and forces a visual audit of every use. Role names (--surface, --ink, --brand-soft) describe the JOB, so themes remap values while meaning holds. The same logic kills --primary-blue the day marketing wants red.",
    },
    tryIt: [
      "Open this site's src/index.css equivalent in DevTools (Elements → select any panel → Computed → filter by 'background'): trace the value back to var(--surface). Then flip the theme and watch the same variable resolve differently.",
      "In the Token Lab, drag the brand color toward pale yellow and watch the contrast ratio badge drop below 4.5:1 — note which ink the lab auto-picks and why.",
      "Take any page you built in P01-L4 and extract every repeated color, radius, and gap into :root tokens. Count how many raw hex/px values survive (goal: near zero).",
      "Add a [data-theme=\"light\"] mapping to that page and a 10-line toggle script using the boot pattern above. Test: reload — does the choice persist? Delete localStorage — does the OS preference take over?",
      "Run the contrast check on your pairs: open DevTools color picker on your --ink over --surface and read the ratio. Fix any pair under 4.5:1.",
    ],
    challenge: {
      prompt:
        "Build a 'reading app' theme system from scratch: a page with a header, an article (h1, two paragraphs, a blockquote, a code span), and a footer. Requirements: (1) a complete semantic token set on :root — at least bg, surface, ink, ink-2, muted, line, brand, brand-soft, r; (2) zero raw hex or px color/radius values outside the :root blocks; (3) a light mapping under [data-theme=\"light\"] with every pair ≥ 4.5:1 contrast (verify in DevTools); (4) the no-flash boot script; (5) the toggle button styled only with tokens.",
      hints: [
        "Start from roles you actually consume: write the HTML, style it with var(--…) names that don't exist yet, then define exactly those names. The consumers list IS your token inventory.",
        "Contrast shortcut: dark theme wants ink around #e8e8e8 on surfaces around #121212–#1a1a1a (≈ 12:1); light theme wants ink around #1a1a1a on #fafafa. Then tune brand pairs separately — colored text on colored backgrounds is where pairs fail.",
        "The blockquote and code span are great token customers: a --brand-soft background plus an --ink-2 border-left reads as 'aside' in both themes without new colors.",
      ],
      solution: [
        "<!doctype html>",
        "<html lang=\"en\">",
        "<head>",
        "  <meta charset=\"utf-8\">",
        "  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">",
        "  <title>Fieldnotes — a reading app</title>",
        "  <script>",
        "    // no-flash boot: OS default, user override wins after",
        "    try {",
        "      const stored = localStorage.getItem(\"theme\");",
        "      const dark = matchMedia(\"(prefers-color-scheme: dark)\").matches;",
        "      document.documentElement.dataset.theme = stored ?? (dark ? \"dark\" : \"light\");",
        "    } catch { document.documentElement.dataset.theme = \"dark\"; }",
        "  </script>",
        "  <style>",
        "    /* ——— tokens: the only place values live ——— */",
        "    :root, :root[data-theme=\"dark\"] {",
        "      --bg: #101418;      --surface: #171c22;   --ink: #e7ebee;",
        "      --ink-2: #b9c2ca;   --muted: #7d8894;     --line: #2a323b;",
        "      --brand: #6fb7ff;   --brand-soft: #1b2c3d;",
        "      --r: 10px;          color-scheme: dark;",
        "    }",
        "    :root[data-theme=\"light\"] {",
        "      --bg: #f4f3ef;      --surface: #fdfcfa;   --ink: #1c232a;",
        "      --ink-2: #45505b;   --muted: #6d7883;     --line: #ddd8cf;",
        "      --brand: #0b62c4;   --brand-soft: #e2edf9;",
        "      color-scheme: light;",
        "    }",
        "",
        "    /* ——— consumers: names only ——— */",
        "    * { box-sizing: border-box; margin: 0; }",
        "    body {",
        "      background: var(--bg); color: var(--ink);",
        "      font: 17px/1.7 Georgia, serif; max-width: 42rem;",
        "      margin-inline: auto; padding: 2rem 1.25rem;",
        "    }",
        "    header, footer {",
        "      display: flex; justify-content: space-between; align-items: center;",
        "      padding-block: 1rem; color: var(--muted); font-size: .85rem;",
        "    }",
        "    h1 { font-size: 2rem; line-height: 1.2; margin-block: 1.5rem 1rem; }",
        "    p + p { margin-top: 1rem; color: var(--ink-2); }",
        "    blockquote {",
        "      margin-block: 1.5rem; padding: 1rem 1.25rem;",
        "      background: var(--brand-soft);",
        "      border-left: 3px solid var(--brand);",
        "      border-radius: var(--r); color: var(--ink-2);",
        "    }",
        "    code {",
        "      font: .85em ui-monospace, monospace;",
        "      background: var(--surface); border: 1px solid var(--line);",
        "      border-radius: var(--r); padding: .1em .4em; color: var(--brand);",
        "    }",
        "    button {",
        "      font: inherit; cursor: pointer; color: var(--ink);",
        "      background: var(--surface); border: 1px solid var(--line);",
        "      border-radius: var(--r); padding: .4rem .9rem;",
        "    }",
        "    button:hover { border-color: var(--brand); color: var(--brand); }",
        "  </style>",
        "</head>",
        "<body>",
        "  <header>",
        "    <strong>Fieldnotes</strong>",
        "    <button id=\"t\" type=\"button\">Toggle theme</button>",
        "  </header>",
        "",
        "  <article>",
        "    <h1>Why margins collapse (and when they don't)</h1>",
        "    <p>Block-flow siblings merge their vertical margins — the larger wins…</p>",
        "    <blockquote>Flexbox and Grid children never collapse, which is a quiet",
        "      reason modern layout feels predictable.</blockquote>",
        "    <p>Set <code>box-sizing: border-box</code> globally and the width you",
        "      write is the width you get.</p>",
        "  </article>",
        "",
        "  <footer><span>© 2026 Fieldnotes</span><span>built on tokens</span></footer>",
        "",
        "  <script>",
        "    document.querySelector(\"#t\").addEventListener(\"click\", () => {",
        "      const root = document.documentElement;",
        "      const next = root.dataset.theme === \"dark\" ? \"light\" : \"dark\";",
        "      root.dataset.theme = next;",
        "      try { localStorage.setItem(\"theme\", next); } catch { /* private mode */ }",
        "    });",
        "  </script>",
        "</body>",
        "</html>",
        "",
        "<!-- Verify: every color/radius traces to a token; both themes pass",
        "     4.5:1 in the DevTools picker; reload persists; clearing",
        "     localStorage falls back to the OS preference. -->",
      ].join("\n"),
    },
    quiz: [
      {
        q: "var(--brand, #333) — when does the fallback apply?",
        options: ["Only when --brand is undefined for that element", "Whenever the theme is dark", "When the browser prefers light mode", "Never — fallbacks are ignored"],
        answer: 0,
        explain: "The fallback guards against a missing token. In a tokenized app a missing token should be loud (a broken-looking value), so fallbacks are mainly for portable widgets.",
      },
      {
        q: "The professional dark mode is…",
        options: ["A second mapping of the same token names under [data-theme]", "filter: invert(1) on the body", "A duplicated stylesheet with inverted hex codes", "A separate HTML page"],
        answer: 0,
        explain: "Components consume names; themes remap values. One attribute flip re-skins the app, and each mapping is tuned — not mathematically inverted.",
      },
      {
        q: "Which token name survives a rebrand and a theme change?",
        options: ["--brand-soft", "--dark-green-400", "--lightish-bg", "--hex-3ddc97"],
        answer: 0,
        explain: "Role names describe the job ('tinted brand background'); appearance names encode values and become lies the first time values change.",
      },
      {
        q: "WCAG minimum contrast for body text is…",
        options: ["4.5:1", "2:1", "7:1 for everything", "There is no numeric rule"],
        answer: 0,
        explain: "4.5:1 for normal text, 3:1 for large text and UI components; 7:1 is the AAA tier. The DevTools color picker reports the ratio inline.",
      },
      {
        q: "Why run the theme-boot script in <head> before first paint?",
        options: ["To set data-theme before the browser renders, avoiding a theme flash", "Scripts only work in <head>", "To beat the CSS parser", "localStorage is only available early"],
        answer: 0,
        explain: "If the attribute lands after first paint, users see the wrong theme for a frame. Boot script reads stored choice → OS preference → default, synchronously.",
      },
      {
        q: "Overriding tokens on a wrapper div re-skins only that subtree because…",
        options: ["Custom properties inherit — the override wins for all descendants", "CSS variables are global constants", "The browser caches the nearest value", "It doesn't — overrides are document-wide"],
        answer: 0,
        explain: "Inheritance is the mechanism: the nearest declaration wins downward. That's the Token Lab trick, widget theming, and per-tenant branding in one fact.",
      },
    ],
    flashcards: [
      { front: "Design token", back: "A design decision, named once and consumed everywhere: --brand, --surface, --r. Tokens are the vocabulary a design system speaks." },
      { front: "Custom property", back: "A --name: value declaration that inherits down the tree; read with var(--name, fallback). The mechanism tokens run on." },
      { front: "Semantic vs primitive tokens", back: "Primitives are the raw palette (green-500); semantics name roles (brand, surface, ink). App code consumes semantics so themes remap cleanly." },
      { front: "[data-theme] mapping", back: "Re-declaring the same token names under a selector like :root[data-theme='light']. Components never change; the dictionary does." },
      { front: "color-scheme", back: "Tells the browser to theme native controls, scrollbars, and autofill to match (dark/light). One line beginners forget." },
      { front: "WCAG contrast", back: "4.5:1 for body text, 3:1 for large text and UI parts. Verify pairs per theme — colored-on-colored is where they fail." },
      { front: "No-flash theme boot", back: "A <head> script sets data-theme before first paint: stored choice → prefers-color-scheme → default. JS toggles; CSS does the visual work." },
      { front: "What NOT to tokenize", back: "One-off values. A token is a decision you expect to reuse or re-make; a magic number used once should stay a number." },
    ],
    recap: [
      "Tokens are decisions named once: role-based names, declared at :root, consumed with var().",
      "A theme is a mapping — same names, new values under [data-theme]; components never know which is live.",
      "Dark mode is a redesign: off-black surfaces, tuned brand, verified 4.5:1 pairs, shadows → borders.",
      "Boot script before first paint: stored choice beats OS preference beats default; toggle persists.",
      "Inheritance scopes themes: override on a subtree and only that subtree re-skins.",
    ],
    references: [
      { label: "MDN — Using CSS custom properties", url: "https://developer.mozilla.org/en-US/docs/Web/CSS/Using_CSS_custom_properties" },
      { label: "web.dev — Learn CSS: Custom properties", url: "https://web.dev/learn/css/custom-properties" },
      { label: "WCAG — Contrast (Minimum)", url: "https://www.w3.org/WAI/WCAG21/Understanding/contrast-minimum.html" },
      { label: "web.dev — prefers-color-scheme", url: "https://web.dev/articles/prefers-color-scheme" },
      { label: "Style Dictionary — design tokens guide", url: "https://styledictionary.com/guide/what-are-design-tokens/" },
    ],
    nextBridge:
      "Phase 1 is complete: structure machines can read, forms that validate, a cascade you can predict, layout without breakpoint soup, and a token system you just proved by building one. The Foundation stage now turns to the language itself — Phase 2 starts with JavaScript values, types, and control flow, and everything you styled becomes something you can program.",
  },
];

export const LESSON_CONTENT_P1: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P1.map((l) => [l.id, l]),
);
