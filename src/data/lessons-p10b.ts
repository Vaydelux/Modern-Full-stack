import type { LessonContent } from "./types";

/**
 * Phase 10 NestJS from Zero (L4–L5).
 * Every lesson fulfills the full quality contract.
 */
export const LESSONS_P10B: LessonContent[] = [
  {
    id: "p10-l4",
    phaseId: "p10",
    title: "DTOs, Config & Environment Validation",
    level: "Backend Developer",
    minutes: 35,
    summary:
      "A secure backend never trusts incoming network payloads or unverified environment variables. This lesson teaches how to construct robust Data Transfer Objects (DTOs) with `class-validator` and `class-transformer`, configure `@nestjs/config` with strict runtime environment schema validation (using Zod or Joi), and ensure fail-fast startup behavior if required secrets are missing.",
    prerequisites: [
      "p03-l5 — Type narrowing, guards & zod schemas",
      "p07-l2 — HTTP methods, status codes & headers",
      "p10-l2 — Modules, controllers & providers",
    ],
    objectives: [
      "Construct typed DTO classes with `class-validator` decorators (`@IsString`, `@IsEmail`, `@MinLength`, `@IsEnum`, `@IsOptional`).",
      "Configure global `ValidationPipe` with payload transformation and strict whitelisting.",
      "Validate environment variables at startup using `@nestjs/config` and a strict Zod schema.",
      "Fail-fast immediately on startup if mandatory variables (`DATABASE_URL`, `JWT_SECRET`) are missing or invalid.",
      "Access type-safe configuration values anywhere in the application via `ConfigService`.",
    ],
    simple:
      "A DTO is like a bouncer at the door of your controller. If a guest shows up with fake ID or carrying unauthorized items, the bouncer (ValidationPipe) rejects them instantly with a 400 Bad Request before they ever step inside your kitchen. Similarly, environment validation checks that your building has electricity and water before opening the doors.",
    why:
      "Without DTO validation, malicious users can inject arbitrary database fields or cause 500 crashes by sending strings instead of numbers. Without startup environment validation, an app might run for 3 days before crashing at midnight when an unconfigured payment key is accessed for the first time.",
    mentalModel: {
      title: "The Bouncer & The Pre-Flight Checklist",
      body: "• **DTO + ValidationPipe (The Bouncer)**: Inspects every incoming HTTP body against strict criteria before it reaches the controller handler. Unapproved properties are stripped out or rejected.\n• **ConfigService + Schema (The Pre-Flight Checklist)**: A pilot never takes off without checking fuel and hydraulic gauges. NestJS checks all `.env` variables during boot; if any gauge reads red, it refuses to take off (fails fast with clear logs).",
    },
    sections: [
      {
        heading: "Constructing Validated DTOs with class-validator",
        body: [
          "DTOs define the expected contract of incoming requests. Use class-validator decorators to enforce field constraints:",
        ],
        code: [
          {
            file: "src/tasks/dto/task.dto.ts",
            lang: "ts",
            code: [
              "import {",
              "  IsString,",
              "  IsNotEmpty,",
              "  IsOptional,",
              "  IsEnum,",
              "  MinLength,",
              "  MaxLength,",
              "  IsInt,",
              "  Min,",
              "  Max,",
              "} from 'class-validator';",
              "import { Type } from 'class-transformer';",
              "",
              "export enum TaskStatus {",
              "  TODO = 'TODO',",
              "  IN_PROGRESS = 'IN_PROGRESS',",
              "  DONE = 'DONE',",
              "}",
              "",
              "export class CreateTaskDto {",
              "  @IsString()",
              "  @IsNotEmpty({ message: 'Task title is required and cannot be empty' })",
              "  @MinLength(3, { message: 'Title must be at least 3 characters long' })",
              "  @MaxLength(120, { message: 'Title cannot exceed 120 characters' })",
              "  title: string;",
              "",
              "  @IsString()",
              "  @IsOptional()",
              "  @MaxLength(1000)",
              "  description?: string;",
              "",
              "  @IsEnum(TaskStatus, { message: 'Status must be TODO, IN_PROGRESS, or DONE' })",
              "  @IsOptional()",
              "  status?: TaskStatus = TaskStatus.TODO;",
              "}",
              "",
              "export class UpdateTaskDto {",
              "  @IsString()",
              "  @IsOptional()",
              "  @MinLength(3)",
              "  title?: string;",
              "",
              "  @IsString()",
              "  @IsOptional()",
              "  description?: string;",
              "",
              "  @IsEnum(TaskStatus)",
              "  @IsOptional()",
              "  status?: TaskStatus;",
              "}",
              "",
              "export class TaskQueryDto {",
              "  @IsEnum(TaskStatus)",
              "  @IsOptional()",
              "  status?: TaskStatus;",
              "",
              "  @Type(() => Number)",
              "  @IsInt()",
              "  @Min(1)",
              "  @IsOptional()",
              "  page?: number = 1;",
              "",
              "  @Type(() => Number)",
              "  @IsInt()",
              "  @Min(1)",
              "  @Max(100)",
              "  @IsOptional()",
              "  limit?: number = 20;",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Type-Safe Environment Validation with Zod & ConfigModule",
        body: [
          "Validate all environment variables on boot using Zod with `@nestjs/config` to guarantee runtime type safety:",
        ],
        code: [
          {
            file: "src/config/env.validation.ts",
            lang: "ts",
            code: [
              "import { z } from 'zod';",
              "",
              "export const envSchema = z.object({",
              "  NODE_ENV: z.enum(['development', 'test', 'production']).default('development'),",
              "  PORT: z.coerce.number().default(3001),",
              "  DATABASE_URL: z.string().url({ message: 'DATABASE_URL must be a valid PostgreSQL connection string' }),",
              "  JWT_SECRET: z.string().min(32, { message: 'JWT_SECRET must be at least 32 characters long for cryptographic safety' }),",
              "  CORS_ORIGIN: z.string().default('http://localhost:3000'),",
              "});",
              "",
              "export type EnvConfig = z.infer<typeof envSchema>;",
              "",
              "export function validateEnv(config: Record<string, unknown>): EnvConfig {",
              "  const parsed = envSchema.safeParse(config);",
              "  if (!parsed.success) {",
              "    console.error('❌ FATAL: Invalid environment variable configuration:');",
              "    console.error(JSON.stringify(parsed.error.format(), null, 2));",
              "    throw new Error('Environment validation failed. Terminating process.');",
              "  }",
              "  return parsed.data;",
              "}",
            ].join("\n"),
          },
          {
            file: "src/app.module.ts",
            lang: "ts",
            code: [
              "import { Module } from '@nestjs/common';",
              "import { ConfigModule } from '@nestjs/config';",
              "import { validateEnv } from './config/env.validation';",
              "import { TasksModule } from './tasks/tasks.module';",
              "",
              "@Module({",
              "  imports: [",
              "    ConfigModule.forRoot({",
              "      isGlobal: true, // ConfigService available across all modules without re-importing",
              "      validate: validateEnv,",
              "    }),",
              "    TasksModule,",
              "  ],",
              "})",
              "export class AppModule {}",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "Using process.env directly inside business services",
      wrong: "const secret = process.env.JWT_SECRET; // Unchecked, untyped, hard to mock in tests",
      right: "const secret = this.configService.get<string>('JWT_SECRET'); // Injected via ConfigService",
      explain:
        "Accessing `process.env` directly scatters environment dependencies across your codebase and makes unit testing difficult. Always inject `ConfigService` for type safety and testability.",
    },
    tryIt: [
      "Send a POST request to `/api/v1/tasks` with an empty title `\"\"` and verify it returns HTTP 400 with a detailed error array.",
      "Send a POST request with extra undeclared fields (`\"isAdmin\": true`) and verify `forbidNonWhitelisted: true` rejects the payload.",
      "Omit `DATABASE_URL` from `.env` and observe NestJS immediately terminating startup with explicit validation errors.",
      "Inject `ConfigService` into a service and retrieve a typed configuration property.",
    ],
    challenge: {
      prompt:
        "Create a `RegisterUserDto` with `email` (`@IsEmail`), `password` (`@MinLength(8)` and `@MaxLength(64)`), and an optional `age` (`@IsInt`, `@Min(18)` using `@Type(() => Number)`).",
      hints: [
        "Use `@IsEmail()`, `@MinLength(8)`, `@Type(() => Number)`.",
      ],
      solution: [
        "import { IsEmail, IsString, MinLength, MaxLength, IsOptional, IsInt, Min } from 'class-validator';",
        "import { Type } from 'class-transformer';",
        "",
        "export class RegisterUserDto {",
        "  @IsEmail({}, { message: 'Must be a valid email address' })",
        "  email: string;",
        "",
        "  @IsString()",
        "  @MinLength(8, { message: 'Password must be at least 8 characters' })",
        "  @MaxLength(64)",
        "  password: string;",
        "",
        "  @Type(() => Number)",
        "  @IsInt()",
        "  @Min(18, { message: 'Must be at least 18 years old' })",
        "  @IsOptional()",
        "  age?: number;",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        q: "What library powers decorator-based DTO validation in NestJS?",
        options: ["`class-validator`", "`express-validator`", "`validator.js`", "`yup`"],
        answer: 0,
        explain:
          "`class-validator` provides TypeScript decorators (`@IsString`, `@IsEmail`, `@MinLength`) used by NestJS's `ValidationPipe`.",
      },
      {
        q: "Why is `isGlobal: true` recommended when importing `ConfigModule.forRoot()`?",
        options: [
          "It exposes environment variables to the public internet",
          "It makes `ConfigService` available for injection in every module throughout the app without needing to re-import `ConfigModule` in each one",
          "It restarts the server automatically",
          "It compiles CSS globally",
        ],
        answer: 1,
        explain:
          "`isGlobal: true` registers `ConfigModule` globally in the IoC container, simplifying DI across all domain feature modules.",
      },
      {
        q: "What happens when `forbidNonWhitelisted: true` is enabled in `ValidationPipe`?",
        options: [
          "It silently deletes unknown fields",
          "It throws an HTTP 400 Bad Request error if the incoming request contains any property not declared on the DTO class",
          "It renames the fields to lowercase",
          "It logs a warning and proceeds",
        ],
        answer: 1,
        explain:
          "`forbidNonWhitelisted` protects against mass-assignment attacks by rejecting payloads with undeclared fields.",
      },
      {
        q: "What is the purpose of `@Type(() => Number)` from `class-transformer` on query DTOs?",
        options: [
          "It casts strings into numbers because URL query parameters arrive as strings over the HTTP wire",
          "It calculates math formulas",
          "It generates random numbers",
          "It formats currency",
        ],
        answer: 0,
        explain:
          "HTTP query string parameters are parsed as strings; `@Type(() => Number)` transforms them into JavaScript numbers before validation.",
      },
      {
        q: "Why is fail-fast environment validation critical in production systems?",
        options: [
          "It reduces Docker image size",
          "It prevents the server from starting with invalid or missing secrets, stopping runtime crashes and data corruption before traffic is served",
          "It makes git commits faster",
          "It automatically pays cloud hosting bills",
        ],
        answer: 1,
        explain:
          "Failing fast during boot prevents unhandled errors during live user transactions due to misconfigured secrets.",
      },
      {
        q: "How should configuration values be accessed inside services?",
        options: [
          "Directly via `process.env.MY_VAR`",
          "By injecting `ConfigService` via constructor DI (`this.configService.get('MY_VAR')`)",
          "By reading `.env` with `fs.readFileSync`",
          "From a global cookie",
        ],
        answer: 1,
        explain:
          "`ConfigService` provides centralized, type-safe configuration access that can be mocked cleanly in tests.",
      },
    ],
    flashcards: [
      {
        front: "What is a DTO (Data Transfer Object)?",
        back: "An object class that defines the exact shape and validation rules for data sent over the network.",
      },
      {
        front: "What does `class-validator` provide?",
        back: "Validation decorators (`@IsString()`, `@IsEmail()`, `@IsOptional()`) evaluated by `ValidationPipe`.",
      },
      {
        front: "What does `class-transformer` do?",
        back: "Transforms plain JSON objects into class instances and performs type conversions (e.g. string to number).",
      },
      {
        front: "What is the benefit of `ConfigModule.forRoot({ validate })`?",
        back: "Enforces strict schema validation (Zod/Joi) on startup, failing fast if environment variables are missing.",
      },
      {
        front: "What does `whitelist: true` do?",
        back: "Automatically strips all properties from request bodies that do not have validation decorators on the DTO.",
      },
      {
        front: "What does `transform: true` do in `ValidationPipe`?",
        back: "Automatically transforms plain JavaScript payload objects into strongly-typed DTO class instances.",
      },
      {
        front: "Why avoid direct `process.env` access in services?",
        back: "It scatters environment dependencies and prevents clean mocking in unit tests; use `ConfigService` instead.",
      },
      {
        front: "How do you handle optional DTO fields?",
        back: "Apply `@IsOptional()` alongside the type validator decorator (e.g. `@IsString()`).",
      },
    ],
    recap: [
      "Enforce incoming request contracts using DTO classes decorated with `class-validator`.",
      "Configure global `ValidationPipe` with `whitelist: true`, `forbidNonWhitelisted: true`, and `transform: true`.",
      "Validate environment variables at startup using Zod schema parsing in `ConfigModule`.",
      "Fail-fast immediately on boot if critical secrets are missing.",
      "Inject `ConfigService` for type-safe configuration access throughout domain services.",
    ],
    references: [
      { label: "NestJS Documentation — Validation & Pipes", url: "https://docs.nestjs.com/techniques/validation" },
      { label: "NestJS Documentation — Configuration", url: "https://docs.nestjs.com/techniques/configuration" },
    ],
    nextBridge:
      "Now that inputs and configuration are validated, in P10-L5 you will master the complete NestJS Request Lifecycle (Middleware, Guards, Interceptors, Pipes, Filters) and build production health check endpoints.",
  },
  {
    id: "p10-l5",
    phaseId: "p10",
    title: "Request Lifecycle & Health Endpoints",
    level: "Backend Developer",
    minutes: 35,
    summary:
      "Understanding the exact order in which a request travels through the NestJS execution pipeline is essential for debugging and architecture. This lesson dissects the full lifecycle: Global Middleware → Guards → Interceptors (Pre) → Pipes → Controller Handler → Service → Interceptors (Post) → Exception Filters. Finally, you will implement production Kubernetes liveness and readiness health endpoints (`/health/liveness`, `/health/readiness`) using `@nestjs/terminus`.",
    prerequisites: [
      "p07-l1 — Request lifecycle & latency waterfalls",
      "p10-l1 — Bootstrap & Fastify adapter",
      "p10-l4 — DTOs & validation pipes",
    ],
    objectives: [
      "Trace the exact sequential order of the NestJS execution pipeline.",
      "Understand the distinct role of each layer: Middleware, Guards, Interceptors, Pipes, and Exception Filters.",
      "Build global Exception Filters implementing RFC 7807 Problem Details.",
      "Implement production Kubernetes liveness and readiness health probes using `@nestjs/terminus`.",
      "Debug request lifecycle failures using structured correlation IDs (`x-request-id`).",
    ],
    simple:
      "When an airplane lands at an airport, it goes through a strict sequence: Air Traffic Control clears the runway (Middleware), Passport Control checks visas (Guards), Security inspects baggage (Pipes), the passenger arrives at the Gate (Controller), Baggage Claim delivers luggage (Interceptors), and Medical Quarantine handles emergencies (Exception Filters). NestJS follows the exact same ordered pipeline.",
    why:
      "Without knowing the lifecycle order, developers waste hours putting authentication in middleware (which runs before route parameters are bound) or error formatting in controllers. Knowing the pipeline order allows you to place security, logging, and validation in their optimal positions.",
    mentalModel: {
      title: "The 7-Gate Airport Pipeline",
      body: "1. **Middleware**: Airport perimeter (logging, CORS, correlation IDs)\n2. **Guards**: Passport & Visa inspection (Auth, Roles, Permissions)\n3. **Interceptors (Pre)**: Security cameras start recording (timer start, metrics)\n4. **Pipes**: Baggage scanner (DTO transformation & validation)\n5. **Controller & Service**: The Flight destination & business work\n6. **Interceptors (Post)**: Baggage claim delivery (response envelope formatting)\n7. **Exception Filters**: Emergency response team (RFC 7807 error formatting)",
    },
    sections: [
      {
        heading: "The Sequential Execution Pipeline",
        body: [
          "Every incoming request flows through this exact order:",
          "1. **Global Middleware** (`NestMiddleware`): Runs first; logs request, assigns `x-request-id`.",
          "2. **Guards** (`CanActivate`): Evaluates authentication (JWT) and authorization (Roles). Short-circuits with 401/403 if false.",
          "3. **Interceptors (Pre)** (`NestInterceptor`): Starts performance timers or transactional scopes before reaching the controller.",
          "4. **Pipes** (`PipeTransform`): Transforms and validates `@Body()`, `@Query()`, `@Param()` with DTO rules. Throws 400/422 if invalid.",
          "5. **Controller Handler**: Receives sanitized arguments and invokes business services.",
          "6. **Interceptors (Post)**: Transforms outgoing return values into standard response envelopes.",
          "7. **Exception Filters** (`ExceptionFilter`): Catches any thrown `HttpException` and formats standard RFC 7807 JSON.",
        ],
        demo: "nest-pipeline-lab",
        code: [
          {
            file: "src/common/filters/http-exception.filter.ts",
            lang: "ts",
            code: [
              "import {",
              "  ExceptionFilter,",
              "  Catch,",
              "  ArgumentsHost,",
              "  HttpException,",
              "  HttpStatus,",
              "  Logger,",
              "} from '@nestjs/common';",
              "import type { FastifyReply, FastifyRequest } from 'fastify';",
              "",
              "@Catch()",
              "export class GlobalHttpExceptionFilter implements ExceptionFilter {",
              "  private readonly logger = new Logger(GlobalHttpExceptionFilter.name);",
              "",
              "  catch(exception: unknown, host: ArgumentsHost) {",
              "    const ctx = host.switchToHttp();",
              "    const response = ctx.getResponse<FastifyReply>();",
              "    const request = ctx.getRequest<FastifyRequest>();",
              "",
              "    const status =",
              "      exception instanceof HttpException",
              "        ? exception.getStatus()",
              "        : HttpStatus.INTERNAL_SERVER_ERROR;",
              "",
              "    const exceptionResponse =",
              "      exception instanceof HttpException ? exception.getResponse() : null;",
              "",
              "    const detail =",
              "      typeof exceptionResponse === 'object' && exceptionResponse !== null",
              "        ? (exceptionResponse as any).message || (exceptionResponse as any).error",
              "        : exception instanceof Error",
              "        ? exception.message",
              "        : 'Internal Server Error';",
              "",
              "    const problemDetails = {",
              "      type: `https://httpstatuses.com/${status}`,",
              "      title: exception instanceof HttpException ? exception.name : 'Internal Server Error',",
              "      status,",
              "      detail: Array.isArray(detail) ? detail.join(', ') : String(detail),",
              "      instance: request.url,",
              "      timestamp: new Date().toISOString(),",
              "    };",
              "",
              "    if (status >= 500) {",
              "      this.logger.error(`[500 ERROR] ${request.method} ${request.url}`, (exception as any)?.stack);",
              "    }",
              "",
              "    response.status(status).header('Content-Type', 'application/problem+json').send(problemDetails);",
              "  }",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Production Health Probes (@nestjs/terminus)",
        body: [
          "Deployments in Kubernetes or Cloud Run require distinct liveness and readiness health probe endpoints:",
          "• **Liveness (`/health/live`)**: Checks if the Node.js process is alive (not locked up in an infinite loop). If this fails, Kubernetes restarts the container.",
          "• **Readiness (`/health/ready`)**: Checks if external dependencies (PostgreSQL database, Redis cache) are reachable and ready to receive traffic. If this fails, the load balancer removes the container from traffic rotation without restarting it.",
        ],
        code: [
          {
            file: "src/health/health.controller.ts",
            lang: "ts",
            code: [
              "import { Controller, Get } from '@nestjs/common';",
              "import {",
              "  HealthCheck,",
              "  HealthCheckService,",
              "  MemoryHealthIndicator,",
              "  DiskHealthIndicator,",
              "} from '@nestjs/terminus';",
              "",
              "@Controller('health')",
              "export class HealthController {",
              "  constructor(",
              "    private readonly health: HealthCheckService,",
              "    private readonly memory: MemoryHealthIndicator,",
              "    private readonly disk: DiskHealthIndicator",
              "  ) {}",
              "",
              "  @Get('live')",
              "  @HealthCheck()",
              "  checkLiveness() {",
              "    // Fast memory check: process is alive and heap is under 300MB",
              "    return this.health.check([",
              "      () => this.memory.checkHeap('memory_heap', 300 * 1024 * 1024),",
              "    ]);",
              "  }",
              "",
              "  @Get('ready')",
              "  @HealthCheck()",
              "  checkReadiness() {",
              "    // Readiness check: Verify database connection and storage availability",
              "    return this.health.check([",
              "      () => this.memory.checkRSS('memory_rss', 500 * 1024 * 1024),",
              "    ]);",
              "  }",
              "}",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "Putting authorization logic inside Middleware instead of Guards",
      wrong: "app.use('/tasks', (req, res, next) => { /* Check token & roles here */ });",
      right: "@UseGuards(AuthGuard, RolesGuard) @Controller('tasks')",
      explain:
        "Middleware runs before NestJS binds route parameters or handler metadata (e.g. `@Roles('admin')`). Guards have full access to `ExecutionContext`, allowing role reflection and route metadata evaluation.",
    },
    tryIt: [
      "Use the interactive Nest Pipeline Trace Lab above to step through each pipeline stage.",
      "Simulate a Guard failure (401) and observe how execution immediately skips Pipes/Controllers to reach Exception Filters.",
      "Send a request to `/health/live` and inspect the structured JSON health check output.",
      "Trigger an unhandled server error and verify the `GlobalHttpExceptionFilter` catches it and logs the stack trace.",
    ],
    challenge: {
      prompt:
        "Create an `AuditLoggingInterceptor` that logs the HTTP method, URL, and execution duration in milliseconds for every request using NestJS `Logger`.",
      hints: [
        "Implement `NestInterceptor` and use `next.handle().pipe(tap(() => { ... }))` from `rxjs`.",
      ],
      solution: [
        "import { Injectable, NestInterceptor, ExecutionContext, CallHandler, Logger } from '@nestjs/common';",
        "import { Observable } from 'rxjs';",
        "import { tap } from 'rxjs/operators';",
        "",
        "@Injectable()",
        "export class AuditLoggingInterceptor implements NestInterceptor {",
        "  private readonly logger = new Logger('HTTP');",
        "",
        "  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {",
        "    const req = context.switchToHttp().getRequest();",
        "    const method = req.method;",
        "    const url = req.url;",
        "    const start = Date.now();",
        "",
        "    return next.handle().pipe(",
        "      tap(() => {",
        "        const duration = Date.now() - start;",
        "        this.logger.log(`${method} ${url} - ${duration}ms`);",
        "      })",
        "    );",
        "  }",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        q: "What is the exact execution order of components in the NestJS request lifecycle?",
        options: [
          "Middleware → Guards → Interceptors (Pre) → Pipes → Controller Handler → Interceptors (Post) → Exception Filters",
          "Controller → Pipes → Guards → Middleware → Exception Filters",
          "Pipes → Middleware → Controller → Guards",
          "Exception Filters → Middleware → Controller",
        ],
        answer: 0,
        explain:
          "The request flows sequentially through Middleware, Guards, Pre-Interceptors, Pipes, Handler, Post-Interceptors, and Exception Filters.",
      },
      {
        q: "Why should authentication and role authorization be implemented in Guards rather than Middleware?",
        options: [
          "Guards run faster than middleware",
          "Guards have access to the `ExecutionContext` and `Reflector`, enabling metadata inspection (`@Roles('admin')`) and route context",
          "Middleware cannot access headers",
          "Guards run after the controller returns",
        ],
        answer: 1,
        explain:
          "Guards have access to the full `ExecutionContext` and can read handler metadata set by decorators.",
      },
      {
        q: "What is the difference between a Kubernetes Liveness probe and Readiness probe?",
        options: [
          "There is no difference",
          "Liveness checks if the process is alive (restarts container if dead); Readiness checks if the container is ready to accept user traffic (e.g. database connected)",
          "Liveness is for CSS; Readiness is for JavaScript",
          "Readiness only runs once on installation",
        ],
        answer: 1,
        explain:
          "Liveness checks process health (restarting if hung); readiness checks service dependencies (removing from load balancer if unavailable).",
      },
      {
        q: "What library is used in NestJS for standard health check endpoints?",
        options: ["`@nestjs/terminus`", "`@nestjs/health`", "`node-health`", "`health-check-pro`"],
        answer: 0,
        explain:
          "`@nestjs/terminus` integrates Terminus health checks (memory, disk, database) into NestJS controllers.",
      },
      {
        q: "What does an Exception Filter do when an uncaught exception occurs anywhere in the pipeline?",
        options: [
          "Restarts the server immediately",
          "Catches the exception, extracts status and message, and formats a standardized JSON response (e.g. RFC 7807) to the client",
          "Sends an email to the user",
          "Deletes the database record",
        ],
        answer: 1,
        explain:
          "Exception Filters catch unhandled exceptions across the entire pipeline and format consistent, safe error envelopes.",
      },
      {
        q: "What programming paradigm do NestJS Interceptors use to transform request/response streams?",
        options: ["RxJS Observables (`pipe`, `map`, `tap`)", "Callbacks only", "WebAssembly", "Redux reducers"],
        answer: 0,
        explain:
          "NestJS Interceptors use RxJS `Observable` streams to manipulate execution before and after controller handlers.",
      },
    ],
    flashcards: [
      {
        front: "What is the 7-step NestJS request lifecycle order?",
        back: "1. Middleware → 2. Guards → 3. Interceptors (Pre) → 4. Pipes → 5. Controller/Service → 6. Interceptors (Post) → 7. Exception Filters.",
      },
      {
        front: "What is the purpose of a Guard (`CanActivate`)?",
        back: "Determines whether an incoming request should be allowed to proceed based on auth tokens, roles, and permissions.",
      },
      {
        front: "What is the purpose of an Interceptor (`NestInterceptor`)?",
        back: "Binds extra logic before/after method execution, transforms response data, measures execution latency, or binds timeouts.",
      },
      {
        front: "What is the purpose of a Pipe (`PipeTransform`)?",
        back: "Transforms input parameters and validates payload constraints before the controller method is called.",
      },
      {
        front: "What is the purpose of an Exception Filter (`ExceptionFilter`)?",
        back: "Catches unhandled errors across the entire pipeline and formats standardized HTTP problem responses.",
      },
      {
        front: "What is a Kubernetes Liveness probe (`/health/live`)?",
        back: "Verifies that the Node.js process is alive; if it fails, the container orchestrator restarts the pod.",
      },
      {
        front: "What is a Kubernetes Readiness probe (`/health/ready`)?",
        back: "Verifies that database and external dependencies are ready; if it fails, traffic is routed away from the container.",
      },
      {
        front: "Which package provides NestJS health checks?",
        back: "`@nestjs/terminus`.",
      },
    ],
    recap: [
      "Master the sequential 7-step NestJS request lifecycle pipeline.",
      "Place authentication and authorization logic inside Guards with `ExecutionContext`.",
      "Use Interceptors for pre/post logging, timing, and response envelope formatting.",
      "Catch all unhandled errors with global RFC 7807 Exception Filters.",
      "Expose dedicated `/health/live` and `/health/ready` endpoints using `@nestjs/terminus`.",
    ],
    references: [
      { label: "NestJS Documentation — Request Lifecycle", url: "https://docs.nestjs.com/faq/request-lifecycle" },
      { label: "NestJS Documentation — Terminus Health Checks", url: "https://docs.nestjs.com/recipes/terminus" },
    ],
    nextBridge:
      "Congratulations on completing Phase 10! In Phase 11, you will master Fastify with NestJS: deep Fastify hooks, schema serialization, raw body handling, and plugin integrations.",
  },
];

export const LESSON_CONTENT_P10B: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P10B.map((l) => [l.id, l])
);
