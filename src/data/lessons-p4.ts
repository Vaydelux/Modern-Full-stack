import type { LessonContent } from "./types";

/**
 * Pass 010 bounded content batch: Phase 4 opener (L1–L2) — React Developer stage begins.
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P4: LessonContent[] = [
  {
    id: "p04-l1",
    phaseId: "p04",
    title: "Declarative UI & JSX",
    level: "React Developer",
    minutes: 30,
    summary:
      "For three phases you've told the DOM what to DO — create this element, append it, clear the list, re-render. React inverts the relationship: you describe what the UI should LOOK LIKE for a given state, and React figures out the doing. This lesson makes that mental flip, teaches JSX as a typed description language (not HTML-in-JS), and shows the same Todo app you built by hand rendered as a pure description of state.",
    prerequisites: ["p03-l6 — the typed vanilla Todo app", "p02-l4 — the imperative render loop you're about to retire", "p01-l1 — semantic HTML"],
    objectives: [
      "Articulate the declarative flip: UI = f(state) — describe the target, let the library compute the diff.",
      "Write valid JSX: one root (or fragment), expressions in {}, camelCase attributes, className over class.",
      "Render conditional content with ternaries and &&, and explain the 0-truthy pitfall.",
      "Render lists with map and explain why each item needs a stable key (formal treatment in P04-L4).",
      "Mount an app with createRoot and describe what React does between your description and the DOM.",
      "Contrast the vanilla render() loop with the JSX description and name what React now owns.",
    ],
    simple:
      "Imperative UI is giving turn-by-turn driving directions: 'create a div, set its text, append it here, and if the list changes, clear everything and start over.' It works, but you're responsible for every step and every edge case. Declarative UI is handing someone a destination and a map: 'here's what the screen looks like when there are 3 todos and the filter is Open.' You describe the END STATE for the current data; React works out which DOM changes turn the old screen into the new one. You stop managing the journey.",
    why:
      "The imperative loop you wrote in Phase 2 is correct but doesn't scale: every new view multiplies the places you manually sync DOM to state, and every one is a potential 'forgot to re-render' or 'rendered stale data' bug. React's bet — proven across a decade of production apps — is that describing UI as a function of state eliminates that entire bug class, because there's a single source of truth and one reconciliation step. Understanding the flip (not just the syntax) is what separates developers who use React from developers who fight it; every hook, every performance tool, every debugging technique in Phases 4–6 flows from UI = f(state).",
    mentalModel: {
      title: "The blueprint and the contractor",
      body: "In the imperative world you're the construction foreman: you place every brick (createElement), tear down walls (textContent = ''), and rebuild rooms whenever the plan changes. In React you're the architect: you hand over a blueprint — a JSX description of what the building should look like given the current requirements (state). A contractor (the reconciler) compares your new blueprint to the building as it stands, and makes the MINIMAL set of changes to match. You never touch a brick; you only ever revise the blueprint. The blueprint is a pure function of the requirements: same requirements in, same blueprint out. That purity is what makes the whole system predictable, testable, and fast.",
    },
    sections: [
      {
        heading: "The imperative loop you already built (Then)",
        body: [
          "Recall the vanilla Todo from Phase 2: a state array, a render() that clears the list and rebuilds every <li> with createElement/textContent/append, and action functions that mutate-then-call-render. It works — and it carries three costs that grow with every feature. First, you own the diffing: clearing and rebuilding the whole list is simple but wasteful, and doing it selectively is fiddly and bug-prone. Second, you own correctness: forget to call render() after a mutation and the UI lies. Third, you own structure: the shape of the DOM is assembled imperatively, so reading the code tells you HOW it's built, not WHAT it is.",
          "None of this is a personal failing — it's the cost of the imperative model. The browser only offers mutation as a primitive, so every framework-less app re-derives the same loop. React's contribution is to make the DESCRIPTION the artifact and automate the diff.",
        ],
        code: [
          {
            file: "vanilla-render.ts — the loop you're retiring (Then)",
            lang: "ts",
            code: [
              "function render(): void {",
              "  listEl.textContent = \"\";                    // 1. tear down",
              "  for (const todo of visibleTodos()) {         // 2. rebuild everything",
              "    const li = document.createElement(\"li\");",
              "    li.textContent = todo.title;",
              "    listEl.append(li);                         // 3. place the brick",
              "  }",
              "  counterEl.textContent = `${openCount()} open`; // 4. don't forget this one…",
              "  // …or the counter goes stale. You own every sync point.",
              "}",
              "// every action must remember to call render(). Miss one → UI lies.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The declarative description (Now) — UI = f(state)",
        body: [
          "The same app, described instead of commanded: a function that takes the current todos and returns a description of the UI. There is no clear, no append, no 'don't forget to update X' — the counter, the list, and the empty state are all part of ONE description derived from the SAME state. Change the state, re-run the function, and React applies the minimal DOM diff. The function is pure: same state in, same description out, no side effects. That purity is the whole game — it's why React can re-run it freely, why tests are trivial, and why bugs become 'the description is wrong for this state' instead of 'I forgot to sync X'.",
          "This is the equation to tattoo on your brain: UI = f(state). Everything else in React — hooks, context, suspense, server components — is machinery for getting state INTO that function cleanly. When a React app misbehaves, the question is almost always 'what state produced this description?' not 'which DOM call went wrong?'",
        ],
        code: [
          {
            file: "declarative.tsx — the same app, described (Now)",
            lang: "tsx",
            code: [
              "import type { Todo } from \"./types\";",
              "",
              "// UI = f(state): a pure description, not a procedure",
              "function TodoApp({ todos }: { todos: Todo[] }) {",
              "  const open = todos.filter((t) => !t.done).length; // derived, never stored",
              "",
              "  return (",
              "    <section>",
              "      <p>{open} of {todos.length} open</p>",
              "      {todos.length === 0 ? (",
              "        <p>Nothing yet — add your first task.</p>",
              "      ) : (",
              "        <ul>",
              "          {todos.map((todo) => (",
              "            <li key={todo.id}>{todo.title}</li>",
              "          ))}",
              "        </ul>",
              "      )}",
              "    </section>",
              "  );",
              "}",
              "// No textContent, no append, no 'remember to update'.",
              "// The counter, list, and empty state are ONE description of `todos`.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "JSX is a typed description language — the rules",
        body: [
          "JSX looks like HTML but is JavaScript: the compiler rewrites <li>…</li> into a function call that builds a description object. That parentage explains the rules. One root element (or a <>…</> fragment, which adds no DOM node). Expressions go in { } — anything that evaluates to a renderable value (string, number, element, array, null). Attributes are camelCase because they're JS properties: className not class, htmlFor not for, onClick not onclick. And because it's TypeScript, your JSX is type-checked — a mistyped prop or a missing key is a compile error, a safety net the string-template world never had.",
          "Two renderable subtleties bite beginners. false, null, and undefined render NOTHING (which is how conditional rendering works) — but 0 and '' DO render (as '0' and an empty string), so {count && <Badge/>} prints a stray 0 when count is 0; use {count > 0 && <Badge/>} or a ternary. And { } can hold any expression but not statements: no if/for inside JSX — express branches with ternaries/&& and loops with map.",
        ],
        code: [
          {
            file: "jsx-rules.tsx",
            lang: "tsx",
            code: [
              "// one root — or a fragment that adds no DOM node:",
              "function TwoSiblings() {",
              "  return (",
              "    <>",
              "      <h1>Title</h1>",
              "      <p>Body</p>",
              "    </>",
              "  );",
              "}",
              "",
              "// attributes are JS properties — camelCase:",
              "function Card() {",
              "  return <label className=\"card\" htmlFor=\"email\">Email</label>;",
              "}",
              "",
              "// conditionals: expressions, not statements",
              "function Status({ done }: { done: boolean }) {",
              "  return done ? <span>✓ done</span> : <span>open</span>;",
              "}",
              "",
              "// the 0-truthy trap:",
              "// {errors.length && <p>{errors.length} errors</p>}  → prints \"0\"",
              "// {errors.length > 0 && <p>…</p>}                  → renders nothing ✓",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Mounting — from description to DOM",
        body: [
          "A component function is just a function; React brings it to life by mounting it into a real DOM node with createRoot(el).render(<App/>). From that moment, React owns the reconciliation loop: when state changes, it re-runs the affected descriptions, diffs the new description against the previous one, and patches only the DOM nodes that differ. You never call render() yourself again — you update state, and the description-to-DOM pipeline runs on its own.",
          "This is the division of labor to internalize: YOU own the description (JSX) and the state it depends on. REACT owns rendering, diffing, and DOM patching. The boundary is clean: you never touch the DOM in a React component (refs are the rare, explicit exception in Phase 5), and React never invents state. Bugs at this stage are almost always 'my description doesn't match the state I think I have' — a question you can answer by reading, not by stepping through DOM calls.",
        ],
        code: [
          {
            file: "main.tsx — the entry point",
            lang: "tsx",
            code: [
              "import { createRoot } from \"react-dom/client\";",
              "import { TodoApp } from \"./TodoApp\";",
              "import type { Todo } from \"./types\";",
              "",
              "const initialTodos: Todo[] = [/* …from your typed api module… */];",
              "",
              "// hand React the root DOM node + the top-level description:",
              "const root = createRoot(document.querySelector(\"#root\")!);",
              "root.render(<TodoApp todos={initialTodos} />);",
              "",
              "// From here: update state → React re-runs the description →",
              "// diffs → patches the DOM. You never call render() again.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Then vs Now — what changed, and who owns what",
        body: [
          "Set the two versions side by side. The vanilla app: ~40 lines of DOM assembly, a render() you must remember to call, and correctness resting on your vigilance. The React app: a ~15-line pure function describing the UI, with rendering and diffing delegated. The pixels are identical; the OWNERSHIP is inverted. This is the honest pitch for every framework you'll use: not that it's magic, but that it takes a well-understood loop (state → describe → diff → patch) and makes it the platform's job instead of yours.",
          "Keep one foot in the imperative world on purpose: React is doing under the hood exactly what you did by hand, just automatically and minimally. When you later debug a 'why didn't it re-render?' (Phase 4–5), you'll reason about it as the foreman you used to be — now with the contractor doing the brickwork. That dual perspective is the course's through-line from here to the capstones.",
        ],
      },
    ],
    mistake: {
      title: "Using JSX like a template string",
      wrong: [
        "function TodoRow({ todo }: { todo: Todo }) {",
        "  // building HTML as a string, then injecting it:",
        "  return <li dangerouslySetInnerHTML={{",
        "    __html: `<b>${todo.title}</b>`,",
        "  }} />;",
        "}",
        "// Two sins: you've re-entered the string-concatenation world",
        "// (XSS returns — todo.title is unescaped), and you've thrown",
        "// away React's diffing and type-checking for that node.",
      ].join("\n"),
      right: [
        "function TodoRow({ todo }: { todo: Todo }) {",
        "  return (",
        "    <li>",
        "      <b>{todo.title}</b>   {/* auto-escaped, diffed, type-checked */}",
        "    </li>",
        "  );",
        "}",
        "// JSX expressions are escaped by default. Stay in the",
        "// description world; let React own the DOM.",
      ].join("\n"),
      explain:
        "JSX's core safety property is that { } expressions are HTML-escaped automatically — user data rendered through JSX cannot inject markup. dangerouslySetInnerHTML deliberately turns that off (the name is the warning) and hands React a raw string it must trust. Almost every use is a sign of trying to make JSX behave like the imperative string templates it replaced. Describe structure with elements and inject only text through { }; reserve the escape hatch for the rare, fully-controlled case (and Phase 30 will make you prove you need it).",
    },
    tryIt: [
      "Take one view from your vanilla Todo (the counter, the empty state, a single row) and write it as a pure function returning JSX. Resist any urge to call document.* — if you need the DOM, you're still thinking imperatively.",
      "Deliberately return two sibling elements from a component and read the compile/runtime error. Fix it with a fragment, then with a wrapper <div>, and note the difference in the rendered DOM.",
      "Render {0 && <p>hi</p>} and watch the stray 0 appear. Then fix it with {0 > 0 && …} and a ternary. Explain aloud why false renders nothing but 0 renders.",
      "Write a <ul> with todos.map and forget the key prop. Read React's console warning, add key={todo.id}, and explain what React uses the key for (preview: P04-L4).",
      "Mount a tiny component with createRoot and, in DevTools, inspect the DOM React produced. Confirm it matches your description exactly — React built it from your blueprint.",
    ],
    challenge: {
      prompt:
        "Rebuild your vanilla Todo's ENTIRE read path (no actions yet — display only) as a React description. Deliver a single TodoApp({ todos, filter }) component that: (1) derives the visible list and open count from props (no stored copies); (2) renders a semantic layout — <header> with the count in an <p aria-live='polite'>, a <main> with the list, and an empty state when nothing matches; (3) renders each todo as a <TodoRow> in its own file with typed props; (4) uses a fragment or single root, no dangerouslySetInnerHTML, no document.* anywhere; (5) mounts via createRoot. Then write a short 'Then vs Now' comment at the top: name two things you no longer own, and one thing you now own that you didn't before.",
      hints: [
        "Derive, don't store: const visible = todos.filter(…) and const open = todos.filter(t => !t.done).length computed inside the function body on every render. They can't go stale because they're recomputed from the single source of truth.",
        "The empty state is just a branch of the description: visible.length === 0 ? <EmptyState/> : <ul>…</ul>. Because it's part of ONE description, you can't 'forget' to show/hide it.",
        "You now own the DESCRIPTION (getting the JSX right for each state) and the STATE SHAPE (the Todo/filter types). You no longer own DOM assembly or remembering to re-render. That's the trade.",
      ],
      solution: [
        "// ——— types.ts ———",
        "export interface Todo { id: number; title: string; done: boolean; }",
        "export type Filter = \"all\" | \"open\" | \"done\";",
        "",
        "// ——— TodoRow.tsx ———",
        "import type { Todo } from \"./types\";",
        "",
        "export function TodoRow({ todo }: { todo: Todo }) {",
        "  return (",
        "    <li className={todo.done ? \"todo is-done\" : \"todo\"}>",
        "      <span>{todo.title}</span>",
        "      {todo.done ? <span aria-hidden=\"true\">✓</span> : null}",
        "    </li>",
        "  );",
        "}",
        "",
        "// ——— TodoApp.tsx ———",
        "// THEN vs NOW:",
        "//   no longer own: (1) DOM assembly — no createElement/append;",
        "//                  (2) remembering to re-render — state drives it.",
        "//   newly own:     the DESCRIPTION — JSX must be right for every",
        "//                  state, and the state/props types that feed it.",
        "import { TodoRow } from \"./TodoRow\";",
        "import type { Filter, Todo } from \"./types\";",
        "",
        "export function TodoApp({ todos, filter }: { todos: Todo[]; filter: Filter }) {",
        "  // derive on every render — single source of truth, can't go stale",
        "  const visible = todos.filter((t) =>",
        "    filter === \"all\" ? true : filter === \"open\" ? !t.done : t.done,",
        "  );",
        "  const open = todos.filter((t) => !t.done).length;",
        "",
        "  return (",
        "    <>",
        "      <header>",
        "        <h1>Todos</h1>",
        "        <p aria-live=\"polite\">{open} of {todos.length} open</p>",
        "      </header>",
        "      <main>",
        "        {visible.length === 0 ? (",
        "          <p>Nothing here — {filter === \"all\" ? \"add your first task.\" : `no ${filter} tasks.`}</p>",
        "        ) : (",
        "          <ul>",
        "            {visible.map((todo) => (",
        "              <TodoRow key={todo.id} todo={todo} />",
        "            ))}",
        "          </ul>",
        "        )}",
        "      </main>",
        "    </>",
        "  );",
        "}",
        "",
        "// ——— main.tsx ———",
        "import { createRoot } from \"react-dom/client\";",
        "import { TodoApp } from \"./TodoApp\";",
        "import type { Todo } from \"./types\";",
        "",
        "const todos: Todo[] = [/* from the typed api module (Phase 2/3) */];",
        "createRoot(document.querySelector(\"#root\")!).render(",
        "  <TodoApp todos={todos} filter=\"all\" />,",
        ");",
        "",
        "/* Acceptance: no document.* in any component; the counter, list,",
        "   and empty state are one pure description; change `todos` and",
        "   the whole view follows. You described; React did. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "The declarative flip is best stated as…",
        options: ["UI = f(state): describe the target UI for the current state; the library computes the DOM changes", "React is faster than the DOM", "You should never touch state", "JSX is faster than createElement"],
        answer: 0,
        explain: "You describe the END state as a pure function of data; React diffs and patches the DOM. The bug class 'forgot to re-render' disappears because rendering is the platform's job.",
      },
      {
        q: "In JSX, {count && <Badge/>} when count is 0 renders…",
        options: ["A stray '0' — because 0 is a renderable value, unlike false/null/undefined", "Nothing", "A runtime error", "An empty <Badge/>"],
        answer: 0,
        explain: "false/null/undefined render nothing, but 0 and '' render. Guard with count > 0 && <Badge/> or a ternary. This is the classic 0-truthy bug.",
      },
      {
        q: "Why className instead of class in JSX?",
        options: ["class is a reserved word in JS; JSX attributes are JS properties, so they use camelCase (className, htmlFor, onClick)", "class doesn't work in React", "className is faster", "It's optional"],
        answer: 0,
        explain: "JSX compiles to JS where class is reserved. Attributes map to DOM properties, hence camelCase. TypeScript checks them against the real property types.",
      },
      {
        q: "A component that returns two sibling elements without a wrapper…",
        options: ["Must use a fragment <>…</> (or a single root) — a component returns ONE description", "Automatically wraps them in a div", "Returns an array to the DOM", "Is fine as-is"],
        answer: 0,
        explain: "A component returns one root description. <>…</> groups siblings without adding a DOM node; a <div> works but changes the markup.",
      },
      {
        q: "After createRoot(el).render(<App/>), re-rendering happens when…",
        options: [
          "State changes — React re-runs the description and patches the DOM; you never call render() yourself",
          "You call render() after each change",
          "The browser repaints on a timer",
          "Only on page reload",
        ],
        answer: 0,
        explain: "React owns the render loop. You update state; it re-derives the description, diffs, and applies minimal DOM patches. That's the ownership inversion.",
      },
      {
        q: "dangerouslySetInnerHTML is dangerous because…",
        options: ["It injects a raw, unescaped HTML string — reintroducing XSS and opting out of React's diffing/type-checking", "It's slow", "It only works in class components", "It removes event listeners"],
        answer: 0,
        explain: "JSX escapes { } expressions by default; this prop bypasses that protection. Reserve it for rare, fully-controlled cases — and expect to justify it in review (Phase 30).",
      },
    ],
    flashcards: [
      { front: "UI = f(state)", back: "The declarative contract: the UI is a pure function of state. Describe the target; the library diffs and patches the DOM. The mental model behind all of React." },
      { front: "JSX", back: "A typed description language that compiles to element-building calls. Rules: one root/fragment, expressions in {}, camelCase attributes, className over class." },
      { front: "Renderable falsy values", back: "false, null, undefined render nothing (the basis of conditional rendering). 0 and '' DO render — hence the 0-truthy bug. Use count > 0 && … or ternaries." },
      { front: "Fragment <>…</>", back: "Groups sibling elements into one root description without adding a DOM node. The answer to 'my component returns two elements'." },
      { front: "createRoot / render", back: "createRoot(el).render(<App/>) mounts the top description. Afterwards React owns re-rendering: state change → re-describe → diff → minimal patch." },
      { front: "Ownership inversion", back: "You own the description + state; React owns rendering/diffing/DOM. You never call render() or touch the DOM in a component (refs are the Phase 5 exception)." },
      { front: "Pure component function", back: "Same state in → same description out, no side effects. Purity lets React re-run it freely and makes 'what state produced this UI?' the debugging question." },
      { front: "Auto-escaping", back: "JSX { } expressions are HTML-escaped, so user data can't inject markup. dangerouslySetInnerHTML turns that off — the named escape hatch, used rarely and deliberately." },
    ],
    recap: [
      "The flip: describe the UI for the current state (UI = f(state)); let React compute the DOM diff.",
      "JSX is typed JavaScript: one root/fragment, {} expressions, camelCase attributes, no statements inside.",
      "false/null/undefined render nothing; 0 and '' render — guard conditionals accordingly.",
      "createRoot mounts; afterwards state changes drive re-rendering — you never render manually.",
      "You own the description and state; React owns the DOM. That division is the rest of the course.",
    ],
    references: [
      { label: "react.dev — Thinking in React", url: "https://react.dev/learn/thinking-in-react" },
      { label: "react.dev — Writing Markup with JSX", url: "https://react.dev/learn/writing-markup-with-jsx" },
      { label: "react.dev — JavaScript in JSX with Curly Braces", url: "https://react.dev/learn/javascript-in-jsx-with-curly-braces" },
      { label: "react.dev — Rendering Lists", url: "https://react.dev/learn/rendering-lists" },
      { label: "react.dev — createRoot", url: "https://react.dev/reference/react-dom/client/createRoot" },
    ],
    nextBridge:
      "You can describe a screen. Real apps are descriptions made of descriptions — next lesson breaks the UI into components, passes data down with typed props, and composes small pieces into pages. The Todo app becomes a tree of functions, and 'where does this prop come from?' becomes your new navigation skill.",
  },
  {
    id: "p04-l2",
    phaseId: "p04",
    title: "Components, Props & Composition",
    level: "React Developer",
    minutes: 35,
    summary:
      "A React app is a tree of small functions, each describing a piece of the UI. This lesson installs the component as the unit of reuse: functions that take typed, read-only props and return JSX; children as the composition slot; and the discipline of splitting a page into focused pieces. You'll refactor a flat Todo screen into a tree of named components and learn when to compose instead of adding more props.",
    prerequisites: ["p04-l1 — declarative UI & JSX", "p03-l1 — typed objects and interfaces"],
    objectives: [
      "Define a component as a pure function from typed props to JSX, and follow naming/file conventions that scale.",
      "Type props with interfaces, distinguish required from optional-with-defaults, and treat props as read-only.",
      "Use children to build layout/wrapper components and prefer composition over prop explosion.",
      "Split a screen into components by responsibility, keeping each small enough to read in one sitting.",
      "Pass callbacks down to let children report events up — the one-way data flow you'll formalize with state in P04-L3.",
      "Recognize the props-drilling smell and its honest limits (context arrives in Phase 5).",
    ],
    simple:
      "A component is just a function with a job: hand it some inputs (props), and it hands back a description of a piece of the screen (JSX). That's the entire mechanism. The craft is in the CUT — deciding what each function is responsible for, what inputs it needs, and how small pieces snap together into a page. Get good at cutting and every app becomes a set of small, readable, reusable, testable functions instead of one 500-line screen.",
    why:
      "Components are how React apps stay comprehensible at size. A flat page that renders everything inline is the declarative version of the Phase 2 god-render() — it works until it doesn't. Splitting into components gives you named boundaries (you can read <FilterBar/> instead of 40 lines of filter markup), isolated re-rendering (React can skip subtrees whose inputs didn't change — Phase 6), and reuse (<StatCard/> used five times is one bug-fix away from being fixed everywhere). Equally important is the anti-skill: knowing when NOT to split (premature abstraction) and when to compose (children) instead of inventing a prop for every variation. This judgment — more than any syntax — is what code review actually examines.",
    mentalModel: {
      title: "LEGO, not clay",
      body: "Sculpting with clay (one big page) gives you total freedom and zero structure: every change reshapes the whole blob, and nothing is reusable. LEGO (components) gives you standardized bricks with studs (props) on top and sockets (children/ composition) — each brick is small, named, and snap-together-able. You build a castle from a few brick types, and when the drawbridge breaks you replace ONE brick, not re-sculpt the castle. The studs are the contract: a brick promises 'give me these inputs and I'll render this'. Props are that promise, typed. The skill is designing bricks with the right studs — too few and the brick is useless, too many and nothing fits.",
    },
    sections: [
      {
        heading: "A component is a typed function",
        body: [
          "The shape: a PascalCase function that takes a single props object and returns JSX. PascalCase is load-bearing — it's how the JSX compiler tells <TodoRow/> (your component) apart from <div/> (a built-in element). Props are typed with an interface (or inline object type), and because you're in TypeScript, every prop is checked at every call site — pass a number where a string is expected and it's a compile error, at every usage, forever.",
          "Props are READ-ONLY. A component must never mutate its props — it describes the UI FROM them. This is the component-level expression of the immutability discipline from Phase 2: data flows in, a description flows out, nothing is rewritten in place. If a component needs to change something, it asks its parent (via a callback prop, below) or manages its own state (P04-L3).",
        ],
        code: [
          {
            file: "TodoRow.tsx — the unit of reuse",
            lang: "tsx",
            code: [
              "import type { Todo } from \"./types\";",
              "",
              "// props typed once, checked at every call site:",
              "interface TodoRowProps {",
              "  todo: Todo;             // required",
              "  showDate?: boolean;     // optional",
              "}",
              "",
              "export function TodoRow({ todo, showDate = false }: TodoRowProps) {",
              "  // props are READ-ONLY — describe from them, never mutate:",
              "  return (",
              "    <li className={todo.done ? \"todo is-done\" : \"todo\"}>",
              "      <span>{todo.title}</span>",
              "      {showDate && <time>{todo.createdAt}</time>}",
              "    </li>",
              "  );",
              "}",
              "",
              "// usage — TypeScript verifies the contract here and everywhere:",
              "// <TodoRow todo={t} />              ✓",
              "// <TodoRow todo={t} showDate />     ✓ (boolean shorthand = true)",
              "// <TodoRow todo=\"oops\" />           ✗ compile error: string ≠ Todo",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "children — the composition slot",
        body: [
          "The most flexible prop is children: whatever JSX you put BETWEEN a component's opening and closing tags. It's how you build layout and wrapper components — a <Card> that provides chrome (border, padding) around ANY content, a <Section> that adds a heading, a <Button> that standardizes styling. The parent decides WHAT goes inside; the component decides HOW it's framed. That inversion is composition, and it's the single most reused idea in UI code.",
          "Prefer children over props for content. The smell to avoid is 'prop explosion': <Card title=… subtitle=… icon=… footer=… actions=…/> with a new prop for every variation. When you find yourself adding content-shaped props, switch to children (and maybe one or two semantic slots). Content is infinite; props should be a small, stable contract.",
        ],
        code: [
          {
            file: "Card.tsx — chrome around anything",
            lang: "tsx",
            code: [
              "import type { ReactNode } from \"react\";",
              "",
              "interface CardProps {",
              "  children: ReactNode;   // the composition slot",
              "  tone?: \"default\" | \"highlight\";",
              "}",
              "",
              "export function Card({ children, tone = \"default\" }: CardProps) {",
              "  return <section className={`card card--${tone}`}>{children}</section>;",
              "}",
              "",
              "// the caller supplies the content; Card supplies the chrome:",
              "// <Card tone=\"highlight\">",
              "//   <h2>Sprint 14</h2>",
              "//   <StatRow …/>",
              "// </Card>",
              "",
              "// prop-explosion smell — don't:",
              "// <Card title=… subtitle=… icon=… footer=… actions=… />",
              "// compose instead:",
              "// <Card><CardHeader/><CardBody/><CardFooter/></Card>",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Splitting a screen — the cut",
        body: [
          "Decompose by RESPONSIBILITY, not by visual box. A good cut: <FilterBar/> owns the filter buttons and reports the choice; <TodoList/> owns rendering the visible rows; <TodoRow/> owns one row's markup; <EmptyState/> owns the nothing-to-show case. Each is a few dozen lines, named after its job, and readable in one sitting. The parent <TodoApp/> becomes a short composition that reads like an outline of the screen.",
          "Two guards against over- and under-splitting. Under: if a component is 80+ lines or does two jobs, cut it. Over: if you're creating a component used once that just wraps a <div> with no abstraction payoff, inline it — splitting has a cost (indirection, prop-passing) and should buy you reuse, isolation, or clarity. The test: can you name the piece's single job in one phrase? If yes and it's used more than once or improves readability, split.",
        ],
        code: [
          {
            file: "TodoApp.tsx — the screen as an outline",
            lang: "tsx",
            code: [
              "import { FilterBar } from \"./FilterBar\";",
              "import { TodoList } from \"./TodoList\";",
              "import { EmptyState } from \"./EmptyState\";",
              "import type { Filter, Todo } from \"./types\";",
              "",
              "interface TodoAppProps {",
              "  todos: Todo[];",
              "  filter: Filter;",
              "  onFilterChange: (f: Filter) => void;  // callback prop — events flow UP",
              "}",
              "",
              "export function TodoApp({ todos, filter, onFilterChange }: TodoAppProps) {",
              "  const visible = todos.filter((t) =>",
              "    filter === \"all\" ? true : filter === \"open\" ? !t.done : t.done,",
              "  );",
              "",
              "  return (",
              "    <main>",
              "      <FilterBar value={filter} onChange={onFilterChange} />",
              "      {visible.length === 0 ? (",
              "        <EmptyState filter={filter} />",
              "      ) : (",
              "        <TodoList todos={visible} />",
              "      )}",
              "    </main>",
              "  );",
              "}",
              "// The parent reads like a table of contents. Each child owns its job.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Data down, events up — the one-way flow",
        body: [
          "Props only travel ONE way: parent to child. A child can never reach up and change a parent's data — but it can be HANDED a function (a callback prop) that it calls when something happens. <FilterBar/> doesn't set the filter; it calls onChange(choice), and the parent (whoever owns the state) decides what to do. This 'data down, events up' is the spine of React architecture: state lives with its owner, descriptions flow down, and intent flows back up through callbacks.",
          "This pattern is the setup for P04-L3, where the parent's handler will call a state setter and the whole tree re-renders from the new state. Even before state, the shape is right: children are pure describers + reporters; parents own decisions. And when callbacks start passing through many layers untouched to reach a distant child, that's 'prop drilling' — a real smell whose honest fix (context) arrives in Phase 5. A little drilling is fine; it's explicit and typed.",
        ],
        code: [
          {
            file: "FilterBar.tsx — data down, events up",
            lang: "tsx",
            code: [
              "import type { Filter } from \"./types\";",
              "",
              "const FILTERS: Filter[] = [\"all\", \"open\", \"done\"];",
              "",
              "interface FilterBarProps {",
              "  value: Filter;                      // data flows DOWN",
              "  onChange: (next: Filter) => void;   // events flow UP",
              "}",
              "",
              "export function FilterBar({ value, onChange }: FilterBarProps) {",
              "  return (",
              "    <div role=\"group\" aria-label=\"Filter\">",
              "      {FILTERS.map((f) => (",
              "        <button",
              "          key={f}",
              "          type=\"button\"",
              "          aria-pressed={value === f}",
              "          onClick={() => onChange(f)}   // report intent; parent decides",
              "        >",
              "          {f}",
              "        </button>",
              "      ))}",
              "    </div>",
              "  );",
              "}",
              "// FilterBar owns NO filter state. It describes buttons and reports",
              "// the choice. The owner of `filter` (TodoApp / a hook) decides.",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The config-bag component (prop explosion)",
      wrong: [
        "interface StatCardProps {",
        "  title: string;",
        "  value: string;",
        "  unit?: string;",
        "  icon?: string;",
        "  trend?: \"up\" | \"down\";",
        "  footerText?: string;",
        "  footerLink?: string;",
        "  highlight?: boolean;",
        "  badge?: string;",
        "  badgeTone?: \"ok\" | \"warn\";",
        "}",
        "// Every new design request adds a prop. The component becomes a",
        "// config file; callers pass 8 props; the body is 40 lines of",
        "// {x && …} branches. Reviewers can't tell what a StatCard IS.",
      ].join("\n"),
      right: [
        "interface StatCardProps {",
        "  title: string;",
        "  value: string;",
        "  children?: ReactNode; // optional extra content — the escape valve",
        "}",
        "",
        "function StatCard({ title, value, children }: StatCardProps) {",
        "  return (",
        "    <section className=\"stat\">",
        "      <h3>{title}</h3>",
        "      <p className=\"stat-value\">{value}</p>",
        "      {children}",
        "    </section>",
        "  );",
        "}",
        "",
        "// callers compose the variations:",
        "// <StatCard title=\"Deploys\" value=\"142\">",
        "//   <Trend direction=\"up\"/>",
        "// </StatCard>",
        "// The contract stays tiny; content is infinite and caller-owned.",
      ].join("\n"),
      explain:
        "Each content-shaped prop doubles the component's internal branches and couples it to every caller's needs; soon it's a god-component nobody can change safely. children (plus maybe one or two semantic slots) inverts the coupling: the component owns a small, stable contract (the chrome), and callers supply the variable content. The rule of thumb: props describe CONFIGURATION (few, stable, typed); children carry CONTENT (open-ended, caller-owned). When a prop is really 'a piece of UI', it should be children.",
    },
    tryIt: [
      "Take the single TodoApp from P04-L1 and cut it into TodoList, TodoRow, and EmptyState. The parent should shrink to a short composition. Read the parent aloud — does it sound like an outline?",
      "Build a <Card> with children and use it to wrap three different pieces of content. Then try to add a fourth variation WITHOUT adding a prop — only by composing children. Feel the difference.",
      "Give TodoRow a required todo and an optional showDate with a default. At a call site, pass a wrong type and read the compile error. Note that the error appears at EVERY usage, automatically.",
      "Wire FilterBar's onChange to console.log(next) in the parent. Click around and watch intent flow up while the child stays stateless. Predict what P04-L3 will put in that handler.",
      "Find a component in your code (or write one) that does two jobs, and split it. Then find one you over-split (a used-once wrapper with no payoff) and inline it. Practice both directions.",
    ],
    challenge: {
      prompt:
        "Build a small 'release dashboard' screen entirely from composed, typed components — no component may exceed ~30 lines, and no component may use document.* or hold data it wasn't given. Deliver: (1) <Page> (a layout wrapper using children); (2) <StatCard title value> with a children slot, used for three stats; (3) <ReleaseList releases> that maps <ReleaseRow release> — ReleaseRow shows name, version, and a <StatusBadge status> where status is a literal union 'stable'|'beta'|'deprecated'; (4) <StatusBadge> that renders differently per status with exhaustive handling (a never-check like Phase 3); (5) a parent <Dashboard> that composes it all from a typed const. Then answer in a comment: which component would you split further if ReleaseRow grew a menu, and which would you inline if it stayed tiny?",
      hints: [
        "StatusBadge is the discriminated-union payoff: type Status = 'stable'|'beta'|'deprecated', and a switch (or record) over it with an assertNever default so a new status breaks the build until handled.",
        "Page and StatCard are your two composition lessons: Page = pure children wrapper; StatCard = small contract + children escape valve. Resist adding icon/trend/footer props — compose them as children instead.",
        "The 'split vs inline' answer: if ReleaseRow grows a menu, extract <ReleaseMenu/> as its own component (new responsibility). If StatusBadge were a 3-line used-once span, inline it into ReleaseRow. The test is responsibility + reuse, not line count alone.",
      ],
      solution: [
        "// ——— types.ts ———",
        "export type Status = \"stable\" | \"beta\" | \"deprecated\";",
        "export interface Release { id: number; name: string; version: string; status: Status; }",
        "",
        "// ——— Page.tsx (pure layout wrapper) ———",
        "import type { ReactNode } from \"react\";",
        "export function Page({ children }: { children: ReactNode }) {",
        "  return <main className=\"page\">{children}</main>;",
        "}",
        "",
        "// ——— StatCard.tsx (small contract + children escape valve) ———",
        "import type { ReactNode } from \"react\";",
        "interface StatCardProps { title: string; value: string; children?: ReactNode; }",
        "export function StatCard({ title, value, children }: StatCardProps) {",
        "  return (",
        "    <section className=\"stat\">",
        "      <h3>{title}</h3>",
        "      <p className=\"stat-value\">{value}</p>",
        "      {children}",
        "    </section>",
        "  );",
        "}",
        "",
        "// ——— StatusBadge.tsx (exhaustive over the literal union) ———",
        "import type { Status } from \"./types\";",
        "function assertNever(x: never): never { throw new Error(`Unhandled ${x}`); }",
        "export function StatusBadge({ status }: { status: Status }) {",
        "  switch (status) {",
        "    case \"stable\":     return <span className=\"badge badge--ok\">stable</span>;",
        "    case \"beta\":       return <span className=\"badge badge--warn\">beta</span>;",
        "    case \"deprecated\": return <span className=\"badge badge--err\">deprecated</span>;",
        "    default:           return assertNever(status); // new status → build breaks",
        "  }",
        "}",
        "",
        "// ——— ReleaseRow.tsx / ReleaseList.tsx ———",
        "import type { Release } from \"./types\";",
        "import { StatusBadge } from \"./StatusBadge\";",
        "export function ReleaseRow({ release }: { release: Release }) {",
        "  return (",
        "    <li className=\"release\">",
        "      <span>{release.name}</span>",
        "      <code>{release.version}</code>",
        "      <StatusBadge status={release.status} />",
        "    </li>",
        "  );",
        "}",
        "export function ReleaseList({ releases }: { releases: Release[] }) {",
        "  return <ul>{releases.map((r) => <ReleaseRow key={r.id} release={r} />)}</ul>;",
        "}",
        "",
        "// ——— Dashboard.tsx (the composition) ———",
        "import { Page } from \"./Page\";",
        "import { StatCard } from \"./StatCard\";",
        "import { ReleaseList } from \"./ReleaseList\";",
        "import type { Release } from \"./types\";",
        "",
        "const releases: Release[] = [/* typed data from the api module */];",
        "",
        "export function Dashboard() {",
        "  return (",
        "    <Page>",
        "      <h1>Releases</h1>",
        "      <div className=\"stats\">",
        "        <StatCard title=\"Total\" value=\"24\" />",
        "        <StatCard title=\"Stable\" value=\"18\" />",
        "        <StatCard title=\"Beta\" value=\"4\" />",
        "      </div>",
        "      <ReleaseList releases={releases} />",
        "    </Page>",
        "  );",
        "}",
        "",
        "/* SPLIT vs INLINE: if ReleaseRow grew a dropdown menu, extract",
        "   <ReleaseMenu release> — a new responsibility with its own props.",
        "   If StatusBadge were a 3-line span used only here, inline it into",
        "   ReleaseRow. The criterion is single-responsibility + reuse,",
        "   not line count. Components are cuts, not decorations. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A React component is…",
        options: ["A PascalCase function that takes typed props and returns JSX", "A class that extends the DOM", "A template file", "Any function that touches the DOM"],
        answer: 0,
        explain: "PascalCase tells JSX it's a component (vs a built-in element). Props in, description out. Purity and read-only props keep it predictable.",
      },
      {
        q: "Props are…",
        options: ["Read-only inputs — a component describes from them but never mutates them", "Mutable, like local variables", "Global state", "Only for strings"],
        answer: 0,
        explain: "Props flow in and are treated as immutable, mirroring Phase 2 immutability. To change something, a child reports via a callback prop or manages its own state.",
      },
      {
        q: "The children prop is for…",
        options: ["Composition — letting the caller supply the content between a component's tags", "Passing numbers", "Internal state", "Event handlers only"],
        answer: 0,
        explain: "children is the JSX between <Tag>…</Tag>. It's how layout/wrapper components work and the antidote to prop explosion: content is infinite, the contract stays small.",
      },
      {
        q: "The 'prop explosion' smell is best fixed by…",
        options: ["Switching content-shaped props to children (composition), keeping the prop contract small and stable", "Adding more props", "Making all props optional", "Using any"],
        answer: 0,
        explain: "Props should be configuration (few, typed, stable); content goes through children. A component that grows a prop per design request is mis-cut — compose instead.",
      },
      {
        q: "'Data down, events up' means…",
        options: ["Props (data) flow parent→child; children report intent by calling callback props handed to them", "Children mutate parent state directly", "Data is global", "Events are handled by the DOM"],
        answer: 0,
        explain: "The one-way flow: descriptions and data flow down; intent flows up through callbacks. The state owner (parent/hook) decides. This is the spine React state builds on in P04-L3.",
      },
      {
        q: "You should split a component when…",
        options: ["It has a single nameable responsibility and splitting buys reuse, isolation, or clarity", "It exceeds exactly 50 lines", "It uses a hook", "Every <div> deserves its own component"],
        answer: 0,
        explain: "Decompose by responsibility, not box. Over-splitting (used-once wrappers with no payoff) costs indirection. The test: one job, named, and the cut buys something.",
      },
    ],
    flashcards: [
      { front: "Component", back: "A PascalCase pure function: typed props in, JSX description out. PascalCase distinguishes it from built-in elements in JSX." },
      { front: "Props", back: "Typed, read-only inputs. Data flows down from parent. Checked at every call site by TypeScript. Never mutated by the receiver." },
      { front: "children", back: "The JSX between a component's tags. The composition slot for layout/wrapper components; the fix for prop explosion (content via children, config via props)." },
      { front: "Prop explosion", back: "Adding a content-shaped prop per variation until the component is a config bag. Smell. Fix: children + a small, stable, typed contract." },
      { front: "Data down, events up", back: "Props carry data parent→child; children report intent by calling callback props. The state owner decides. The one-way flow under all of React." },
      { front: "Callback prop", back: "A function passed down (onChange, onSelect) that a child calls to report an event. Lets stateless children participate in state they don't own." },
      { front: "Prop drilling", back: "Passing a prop through many layers that don't use it, to reach a distant child. A little is fine (explicit, typed); context (Phase 5) is the honest fix for a lot." },
      { front: "Split vs inline", back: "Split when there's a single nameable responsibility + reuse/isolation/clarity payoff. Inline used-once wrappers with no abstraction value. Cuts, not decorations." },
    ],
    recap: [
      "Components are typed functions: props in (read-only), JSX out; PascalCase naming is load-bearing.",
      "children is the composition slot — small stable contract for chrome, open-ended content from callers.",
      "Split by responsibility; guard against both the 80-line god-component and the pointless wrapper.",
      "Data down, events up: callback props let stateless children report intent to the state owner.",
      "The Todo/dashboard is now a tree of named functions — the shape every later phase refines.",
    ],
    references: [
      { label: "react.dev — Your First Component", url: "https://react.dev/learn/your-first-component" },
      { label: "react.dev — Passing Props to a Component", url: "https://react.dev/learn/passing-props-to-a-component" },
      { label: "react.dev — Passing JSX as children", url: "https://react.dev/learn/passing-props-to-a-component#passing-jsx-as-children" },
      { label: "react.dev — Components must be pure", url: "https://react.dev/learn/keeping-components-pure" },
      { label: "react.dev — Thinking in React (composition)", url: "https://react.dev/learn/thinking-in-react" },
    ],
    nextBridge:
      "Your components describe and report — but nobody owns changing state yet. Next lesson introduces useState: state as a queue, events as the trigger, and the immutable update recipes from Phase 2 finally paying off as the render loop turns on its own.",
  },
];

export const LESSON_CONTENT_P4: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P4.map((l) => [l.id, l]),
);
