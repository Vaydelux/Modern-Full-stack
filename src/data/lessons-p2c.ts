import type { LessonContent } from "./types";

/**
 * Pass 007 bounded content batch: Phase 2 close (L5–L6).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P2C: LessonContent[] = [
  {
    id: "p02-l5",
    phaseId: "p02",
    title: "fetch, Promises & async/await",
    level: "Foundation",
    minutes: 45,
    summary:
      "The network is slow and can fail — so JavaScript models it with Promises: placeholders for results that will arrive later. This lesson installs the Promise state machine, fetch's two-stage async reality (headers arrive, THEN the body), async/await as readable control flow over promises, structured error handling (HTTP errors vs network errors), and Promise.all — the difference between sequential and parallel waiting. Your Todo app stops being a localStorage toy and starts speaking HTTP.",
    prerequisites: ["p02-l4 — DOM, events, and the vanilla Todo skeleton", "p00-l3 — the raw HTTP request/response shape"],
    objectives: [
      "Describe a Promise's three states and the settle-once guarantee — and why a Promise is a value, not a callback.",
      "Read fetch's two-stage shape: await the Response (headers), then await .json() (body) — and spot the classic double-async bug.",
      "Write async/await with try/catch/finally and explain it as syntax over .then chains.",
      "Separate HTTP errors (response.ok === false) from network failures (rejected Promise) and handle both.",
      "Choose sequential awaits vs Promise.all / allSettled / race deliberately, and name the latency math.",
      "Wire the Todo app to an HTTP API: optimistic-feeling UX, loading flags, and no lost error states.",
    ],
    simple:
      "When you order food, the restaurant hands you a buzzer — not the meal. You don't stand frozen at the counter; you keep living, and the buzzer tells you when the result is ready. A Promise is that buzzer: a value that says 'the answer is pending, I will notify you when it settles, exactly once.' await means 'pause THIS function here (not the whole page) and resume me when the buzzer goes off.' Everything in this lesson is variations on one idea: code that waits well instead of code that blocks.",
    why:
      "Every data your future apps display crosses a network — and networks are where 'it works on my machine' goes to die. Promises are the currency of that world: fetch, Supabase clients, TanStack Query, NestJS controllers, Prisma — all of them hand you buzzers, and the quality of your async discipline is visible directly in your product (double-submits, lost errors, waterfall requests that take 3 seconds when they could take 1). Phase 5's race-condition debugging labs and Phase 19's typed API client are built on exactly the mental model you install today.",
    mentalModel: {
      title: "The buzzer, not the meal",
      body: "A Promise is an object you can hold BEFORE the result exists — pending when you get it, later settling into fulfilled (with a value) or rejected (with a reason), and never changing again. await is a polite pause: only the async function stops; the event loop keeps serving other work (you'll see the machinery next lesson). .then/.catch are the same idea spelled with callbacks. And because the buzzer settles exactly once, you can hand the same Promise to ten consumers and all ten get the same final answer — which is why caching libraries can store them.",
    },
    sections: [
      {
        heading: "The Promise state machine — one settling, forever settled",
        body: [
          "A Promise is always in exactly one of three states: pending (no answer yet), fulfilled (a value arrived), or rejected (a reason arrived). The moment it leaves pending it has settled — and settled promises are frozen: a second resolve or reject call is silently ignored. That single-settle guarantee is what makes promises safe to share, cache, and retry-around.",
          "The consumer side has three verbs: .then(fn) runs on fulfillment, .catch(fn) runs on rejection (and passes failures down the chain until someone catches), .finally(fn) runs either way — perfect for 'stop the spinner' bookkeeping. And a Promise is a VALUE: you can return it, store it in a variable, put it in an array. Returning a Promise from a .then chains it; returning a plain value wraps it. That composition rule is the whole of callback-style async.",
        ],
        code: [
          {
            file: "promise-basics.js",
            lang: "js",
            code: [
              "// a promise you settle by hand — the mechanics, exposed:",
              "const buzzer = new Promise((resolve, reject) => {",
              "  setTimeout(() => resolve(\"order #42 ready\"), 1500);",
              "  // resolve() again later? ignored. settled is forever.",
              "});",
              "",
              "buzzer.then((value) => console.log(\"✓\", value));",
              "// (1.5s later) ✓ order #42 ready",
              "",
              "// the three verbs, one chain:",
              "fetchLater()",
              "  .then((data) => render(data))     // fulfilled path",
              "  .catch((err) => showError(err))   // ANY rejection up the chain",
              "  .finally(() => hideSpinner());    // runs either way",
              "",
              "// a promise is a value — hold it, share it:",
              "const p = fetch(\"/api/me\");",
              "p.then((r) => console.log(\"first listener\"));",
              "p.then((r) => console.log(\"second listener, same result\"));",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "fetch — the two awaits beginners collapse into one",
        body: [
          "fetch(url) returns a Promise that fulfills with a RESPONSE — headers and status, usually fast. The BODY is a second stream, read by calling response.json() (or .text()), which returns ANOTHER Promise. Two async steps, and the classic beginner bug is stopping at the first: logging the Response object and wondering where the data went, or calling .json() without returning/awaiting its Promise.",
          "The second trap: fetch only rejects on NETWORK failure (offline, DNS, CORS block). An HTTP 404 or 500 is a fulfilled promise with response.ok === false — the server answered, just unhappily. Professional code checks response.ok (or the status) and throws into the catch path itself, so one try/catch covers both failure families with different messages.",
        ],
        code: [
          {
            file: "fetch-shape.js",
            lang: "js",
            code: [
              "// stage 1: headers arrive (fast) — stage 2: body streams in",
              "async function loadTodo(id) {",
              "  const response = await fetch(`/api/todos/${id}`);",
              "  // response is NOT the data yet — it's the envelope:",
              "  // response.status, response.ok, response.headers…",
              "",
              "  if (!response.ok) {",
              "    // 4xx/5xx fulfill the promise! Throw to unify error handling:",
              "    throw new Error(`Server answered ${response.status}`);",
              "  }",
              "",
              "  const todo = await response.json(); // stage 2: parse the body",
              "  return todo;                        // { id, title, done }",
              "}",
              "",
              "// ✗ the double-async bug, for the museum:",
              "// const data = await fetch(url).json(); // .json exists on Response,",
              "//                                         // but this hides stage 1's",
              "//                                         // status — a 500 'works' here.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "async/await — promises wearing readable clothes",
        body: [
          "An async function always returns a Promise, and await pauses that function (not the page) until the awaited Promise settles, handing you its value — or throwing its rejection, which plain try/catch can now hold. await/try/catch/finally is syntax over .then/.catch/.finally; the mental model never changes. The discipline that separates professionals: every await either lives inside try/catch or is deliberately returned to a caller who handles it — unhandled rejections are the async version of a swallowed error.",
          "finally carries the bookkeeping that must run regardless: disabling the submit button on request, re-enabling in finally, so a failure can never strand your UI in 'loading forever'. The shape — optimistic-disable → request → success/failure branch → finally restore — is the skeleton of every mutation you will write from here through the capstones.",
        ],
        code: [
          {
            file: "await-discipline.js",
            lang: "js",
            code: [
              "async function saveTodo(todo) {",
              "  const button = document.querySelector(\"#save\");",
              "  button.disabled = true;                 // double-submit guard (P01-L2)",
              "  try {",
              "    const res = await fetch(\"/api/todos\", {",
              "      method: \"POST\",",
              "      headers: { \"Content-Type\": \"application/json\" },",
              "      body: JSON.stringify(todo),",
              "    });",
              "    if (!res.ok) throw new Error(`Save failed (${res.status})`);",
              "    return await res.json();",
              "  } catch (err) {",
              "    // network failure OR our own throw — show it, don't swallow:",
              "    showBanner(err.message);",
              "    throw err;                            // let callers know too",
              "  } finally {",
              "    button.disabled = false;              // ALWAYS restore the UI",
              "  }",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Sequential vs parallel — the latency math",
        body: [
          "Three awaits in a row wait three round trips in a line: if each takes 300ms, you've spent 900ms — even though none depended on the others. When requests are independent, start them together with Promise.all([pa, pb, pc]) and await the combined Promise: total time collapses to the SLOWEST one (~300ms). Promise.all rejects fast if ANY member rejects; Promise.allSettled waits for all and reports each outcome ({status: 'fulfilled'|'rejected', value|reason}) — the right tool when partial success is acceptable (loading three widgets where one may fail).",
          "Promise.race([p, timeout(ms)]) is the classic timeout wrapper: whichever settles first wins, so a hung request loses to a reject after your budget. And sequential awaits remain correct when there IS a dependency (login → then fetch profile with the token). The judgment question every time: 'Does B need A's result?' No → parallel. Yes → sequential. The waterfall is a bug you create by default and fix on purpose.",
        ],
        code: [
          {
            file: "parallel.js — three requests, two strategies",
            lang: "js",
            code: [
              "// ✗ waterfall: 300 + 300 + 300 = ~900ms",
              "const user = await fetch(\"/api/user\").then((r) => r.json());",
              "const tasks = await fetch(\"/api/tasks\").then((r) => r.json());",
              "const notes = await fetch(\"/api/notes\").then((r) => r.json());",
              "",
              "// ✓ parallel: max(300, 300, 300) = ~300ms",
              "const [user2, tasks2, notes2] = await Promise.all([",
              "  fetch(\"/api/user\").then((r) => r.json()),",
              "  fetch(\"/api/tasks\").then((r) => r.json()),",
              "  fetch(\"/api/notes\").then((r) => r.json()),",
              "]);",
              "",
              "// partial failure is fine → allSettled reports each fate:",
              "const results = await Promise.allSettled(widgetPromises);",
              "// [{ status: \"fulfilled\", value }, { status: \"rejected\", reason }]",
              "",
              "// budget a hung request:",
              "const withTimeout = (p, ms) =>",
              "  Promise.race([p, new Promise((_, rej) =>",
              "    setTimeout(() => rej(new Error(\"timed out\")), ms))]);",
              "await withTimeout(fetch(\"/api/slow\"), 3000);",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Milestone upgrade — the Todo app speaks HTTP",
        body: [
          "The skeleton from P02-L4 gets a network brain with three changes. One: a small api module exposing typed-ish functions (listTodos, createTodo, toggleTodo, deleteTodo) that fetch, check ok, parse json, and throw — one place where HTTP lives. Two: actions become async — optimistic-first discipline arrives later (Phase 19); today, await then re-render, with the list showing a loading state while the first paint arrives. Three: errors surface in a visible banner instead of dying in the console.",
          "Test against any scratch API (or the json-server classic: npx json-server db.json) — the contract is what matters: GET /todos, POST /todos, PATCH /todos/:id, DELETE /todos/:id. This is the last lesson where the data layer is hand-rolled; Phase 7 formalizes the REST contract and Phase 10 replaces your scratch server with NestJS — and every pattern here survives the swap untouched. That survival IS the architecture lesson.",
        ],
        code: [
          {
            file: "todo-api.js — the data layer, isolated",
            lang: "js",
            code: [
              "const BASE = \"http://localhost:3000\"; // scratch API (json-server)",
              "",
              "async function request(path, options = {}) {",
              "  const res = await fetch(`${BASE}${path}`, {",
              "    headers: { \"Content-Type\": \"application/json\" },",
              "    ...options,",
              "  });",
              "  if (!res.ok) throw new Error(`${options.method ?? \"GET\"} ${path} → ${res.status}`);",
              "  return res.status === 204 ? null : res.json();",
              "}",
              "",
              "export const listTodos = () => request(\"/todos\");",
              "export const createTodo = (title) =>",
              "  request(\"/todos\", { method: \"POST\", body: JSON.stringify({ title, done: false }) });",
              "export const toggleTodo = (todo) =>",
              "  request(`/todos/${todo.id}`, { method: \"PATCH\", body: JSON.stringify({ done: !todo.done }) });",
              "export const deleteTodo = (id) =>",
              "  request(`/todos/${id}`, { method: \"DELETE\" });",
              "",
              "// actions in app.js become async:",
              "// async function onToggle(id) {",
              "//   const updated = await toggleTodo(find(id));  // server is now truth",
              "//   todos = todos.map((t) => (t.id === id ? updated : t));",
              "//   render();",
              "// }",
            ].join("\n"),
            caption: "Everything the UI knows about HTTP fits in this file. When NestJS replaces the scratch server, only BASE changes.",
          },
        ],
      },
    ],
    mistake: {
      title: "Awaiting the envelope, not the letter",
      wrong: [
        "async function showUser() {",
        "  const user = await fetch(\"/api/user\");",
        "  renderName(user.name);        // undefined — user is a Response!",
        "  console.log(user);            // Response { status: 200, … }",
        "}",
        "",
        "// sibling bug: forgetting to RETURN the inner promise",
        "function load() {",
        "  fetch(\"/api/user\").then((res) => { res.json(); });",
        "  // json()'s promise floats free — its result goes nowhere",
        "}",
      ].join("\n"),
      right: [
        "async function showUser() {",
        "  const res = await fetch(\"/api/user\");   // stage 1: the envelope",
        "  if (!res.ok) throw new Error(`HTTP ${res.status}`);",
        "  const user = await res.json();          // stage 2: the letter",
        "  renderName(user.name);                  // \"Ada\"",
        "}",
        "",
        "// and every promise is either awaited, returned, or caught:",
        "async function load() {",
        "  const res = await fetch(\"/api/user\");",
        "  return res.json();   // returned → caller owns the chain",
        "}",
      ].join("\n"),
      explain:
        "fetch resolves when HEADERS arrive — the body is a second stream with its own promise (.json()/.text()). Treating the Response as the data yields undefined everywhere and a 500 that 'worked'. The two-stage discipline (await response → check ok → await json) is the fetch idiom; violating either stage is the most common async bug in beginner code.",
    },
    tryIt: [
      "Build a promise by hand with new Promise and setTimeout; attach two .then listeners and confirm both get the identical settled value. Then try to settle it twice — observe the silence.",
      "Fetch any public JSON endpoint (e.g. https://jsonplaceholder.typicode.com/todos/1). Log the Response first, then the parsed body. Say the two stages out loud.",
      "Take three independent fetches you wrote sequentially and measure the waterfall (performance.now() before/after). Convert to Promise.all and re-measure. Write the ratio down.",
      "Break networking on purpose: point fetch at a dead port, then at a live URL that returns 404. Which one rejects the promise, which one fulfills with ok=false? Verify, then explain why the distinction matters for error UI.",
      "Add a timeout wrapper (Promise.race) around a fetch to httpbin's /delay/5 endpoint with a 2000ms budget. Watch the race end it early.",
    ],
    challenge: {
      prompt:
        "Upgrade the vanilla Todo app to HTTP against a scratch API (json-server: npx json-server --watch db.json, with db.json = { \"todos\": [] }). Requirements: (1) an api module exactly like section five — every fetch, ok-check, and JSON parse lives there and nowhere else; (2) the app boots with a loading state ('Loading tasks…' in the list area) until GET resolves, and an error banner (with a Retry button) if it rejects; (3) Add/Toggle/Delete call the API, then update state from the SERVER's response — not from optimistic guesses; (4) the Add button is disabled during the request via try/finally; (5) independent startup requests (todos + a /stats endpoint you invent, e.g. total count) load in PARALLEL with Promise.all.",
      hints: [
        "Boot: const [todos, stats] = await Promise.all([listTodos(), getStats()]) — one await, both in flight simultaneously.",
        "The Retry button just re-runs the boot function; keep boot() idempotent so it can be called twice safely.",
        "json-server echoes the created/updated object back — use it: todos = [...todos, created]. When DELETE returns 204, your request() should return null and your action should filter by id.",
      ],
      solution: [
        "// ——— api.js (the ONLY file that knows HTTP) ———",
        "const BASE = \"http://localhost:3000\";",
        "",
        "async function request(path, options = {}) {",
        "  const res = await fetch(`${BASE}${path}`, {",
        "    headers: { \"Content-Type\": \"application/json\" },",
        "    ...options,",
        "  });",
        "  if (!res.ok) {",
        "    throw new Error(`${options.method ?? \"GET\"} ${path} → ${res.status}`);",
        "  }",
        "  return res.status === 204 ? null : res.json();",
        "}",
        "",
        "export const listTodos = () => request(\"/todos\");",
        "export const getStats = () => request(\"/stats\");  // db.json: {\"todos\":[],\"stats\":{\"total\":0}}",
        "export const createTodo = (title) =>",
        "  request(\"/todos\", { method: \"POST\", body: JSON.stringify({ title, done: false }) });",
        "export const patchTodo = (id, patch) =>",
        "  request(`/todos/${id}`, { method: \"PATCH\", body: JSON.stringify(patch) });",
        "export const removeTodo = (id) =>",
        "  request(`/todos/${id}`, { method: \"DELETE\" });",
        "",
        "// ——— app.js (state → render → async actions) ———",
        "import * as api from \"./api.js\"; // <script type=\"module\"> in index.html",
        "",
        "let todos = [];",
        "let stats = null;",
        "let bootError = null;",
        "",
        "const listEl = document.querySelector(\"#todos\");",
        "const banner = document.querySelector(\"#banner\");",
        "const form = document.querySelector(\"#todo-form\");",
        "",
        "function render() {",
        "  banner.hidden = !bootError;",
        "  if (bootError) banner.textContent = `⚠ ${bootError}`;",
        "",
        "  listEl.textContent = \"\";",
        "  if (stats) {",
        "    const p = document.createElement(\"p\");",
        "    p.textContent = `Server says: ${stats.total} tasks recorded`;",
        "    listEl.append(p);",
        "  }",
        "  for (const todo of todos) { /* same row builder as P02-L4 —",
        "    createElement + textContent + data-action, delegated clicks */ }",
        "}",
        "",
        "async function boot() {",
        "  bootError = null;",
        "  listEl.textContent = \"Loading tasks…\";   // visible loading state",
        "  try {",
        "    // INDEPENDENT requests → in flight together:",
        "    const [t, s] = await Promise.all([api.listTodos(), api.getStats()]);",
        "    todos = t;",
        "    stats = s;",
        "  } catch (err) {",
        "    bootError = err.message;",
        "  }",
        "  render();",
        "}",
        "",
        "form.addEventListener(\"submit\", async (e) => {",
        "  e.preventDefault();",
        "  const input = form.querySelector(\"input\");",
        "  const submit = form.querySelector(\"button\");",
        "  submit.disabled = true;                  // try/finally, not hope",
        "  try {",
        "    const created = await api.createTodo(input.value.trim());",
        "    todos = [...todos, created];           // server response = truth",
        "    input.value = \"\";",
        "  } catch (err) {",
        "    bootError = err.message;",
        "  } finally {",
        "    submit.disabled = false;",
        "  }",
        "  render();",
        "});",
        "",
        "// toggle/delete follow the same await-then-update shape via",
        "// the delegated listener from P02-L4.",
        "",
        "document.querySelector(\"#retry\").addEventListener(\"click\", boot);",
        "boot();",
        "",
        "// acceptance: kill json-server mid-session → actions show the",
        "// banner; restart it → Retry recovers. The UI never lies about",
        "// what the server knows.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A Promise that has fulfilled will…",
        options: ["Never change state again — settled is forever", "Revert to pending if unhandled", "Reject after a timeout", "Re-run its executor"],
        answer: 0,
        explain: "pending → fulfilled OR rejected, exactly once. Later resolve/reject calls are ignored — which is what makes promises shareable and cacheable.",
      },
      {
        q: "await fetch(url) gives you…",
        options: ["The Response (headers/status) — the body needs a second await on .json()", "The parsed JSON immediately", "A string", "null until you call .data()"],
        answer: 0,
        explain: "Stage 1 settles when headers arrive; the body streams in via .json()/.text(), each returning its own promise. The two-stage shape is the fetch idiom.",
      },
      {
        q: "fetch('/missing') resolves with a 404. Why didn't the promise reject?",
        options: ["fetch rejects only on network failure; HTTP errors fulfill with ok === false", "404 is a success code", "The server swallowed the error", "fetch can't see status codes"],
        answer: 0,
        explain: "The server ANSWERED — unhappily, but it answered. Check response.ok and throw yourself so catch covers both failure families.",
      },
      {
        q: "Three independent 300ms requests: sequential awaits vs Promise.all take…",
        options: ["~900ms vs ~300ms", "~300ms for both", "~900ms for both", "~100ms vs ~300ms"],
        answer: 0,
        explain: "Sequential = sum; parallel = max. Promise.all starts all three at once and settles when the slowest lands. Use sequential only when B needs A's result.",
      },
      {
        q: "The finally block exists so that…",
        options: ["Bookkeeping (stop spinner, re-enable button) runs on success AND failure", "Errors are automatically fixed", "The promise resolves faster", "try can be omitted"],
        answer: 0,
        explain: "Without finally, a rejected request strands your UI mid-flight — disabled buttons, eternal spinners. finally is the restore-the-world clause.",
      },
      {
        q: "Promise.allSettled beats Promise.all when…",
        options: ["Partial failure is acceptable — you want every outcome reported", "You need fail-fast behavior", "The promises are dependent", "You only care about the fastest"],
        answer: 0,
        explain: "all rejects on the first failure; allSettled waits for all and returns {status, value|reason} per member — right for independent widgets where one may fail.",
      },
    ],
    flashcards: [
      { front: "Promise states", back: "pending → fulfilled (value) OR rejected (reason). Settles exactly once; later settle calls are ignored. A promise is a value you can hold, share, and store." },
      { front: "fetch's two stages", back: "await fetch(url) → Response (headers, status). await res.json() → body. Two promises; beginners collapse them and lose the status check." },
      { front: "response.ok", back: "True for status 200–299. fetch fulfills even on 404/500 — throw on !ok so HTTP errors and network errors share one catch path." },
      { front: "async/await", back: "Syntax over .then/.catch. async functions return promises; await pauses the function (not the page) until settlement; rejections surface as catchable throws." },
      { front: "Promise.all vs allSettled", back: "all: parallel, fail-fast, values array. allSettled: parallel, waits for all, per-member {status, value|reason}. Both turn a waterfall into max(latencies)." },
      { front: "The waterfall bug", back: "Sequential awaits on independent requests sum the latencies. Question: does B need A's result? No → Promise.all. The fix is a shape change, not a speed change." },
      { front: "finally", back: "Runs on fulfillment AND rejection — the restore-the-world clause (spinners off, buttons enabled). A mutation without finally can strand the UI." },
      { front: "Unhandled rejection", back: "A rejected promise with no catch — the async swallowed error. Every await is try/catch'd or returned to a handler; browsers warn, and Node can crash on them." },
    ],
    recap: [
      "A Promise is a buzzer: pending → settled once, and shareable as a value.",
      "fetch = await the envelope (check ok) → await the letter (.json()).",
      "async/await is promise syntax; try/catch/finally map to then/catch/finally.",
      "Independent work goes parallel (Promise.all); dependent work stays sequential.",
      "The Todo app now has an api module — the seam where NestJS will slot in unchanged.",
    ],
    references: [
      { label: "MDN — Using the Fetch API", url: "https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch" },
      { label: "MDN — Promise", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise" },
      { label: "MDN — async function", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/async_function" },
      { label: "javascript.info — Promises, async/await", url: "https://javascript.info/async" },
      { label: "MDN — Response.json()", url: "https://developer.mozilla.org/en-US/docs/Web/API/Response/json" },
    ],
    nextBridge:
      "Your code waits well — but WHO decides when the buzzer gets checked? Next lesson opens the machine room: the event loop, microtasks vs macrotasks, why setTimeout(0) is never 'now', and how one blocked thread freezes an entire app — with a live loop to play with and async debugging drills to finish Phase 2.",
  },
  {
    id: "p02-l6",
    phaseId: "p02",
    title: "Event Loop, Tasks & Debugging Labs",
    level: "Foundation",
    minutes: 40,
    summary:
      "JavaScript runs one thing at a time — and still serves timers, network, clicks, and paint without freezing. The event loop is the scheduler making that possible: run a task, drain every microtask, take one macrotask, maybe paint, repeat. This lesson makes the loop visible (with a live lab), explains why promise callbacks beat setTimeout to the queue, why a heavy sync block freezes everything, and drills the nine-step debugging method on a real async race — the last habit Phase 2 owes you before React.",
    prerequisites: ["p02-l5 — promises and async/await", "p00-l4 — the nine-step debugging method"],
    objectives: [
      "Recite the loop's law: stack task → drain microtasks → one macrotask → possible paint → repeat.",
      "Classify work: promise callbacks/queueMicrotask are microtasks; setTimeout/setInterval/I-O are macrotasks — and predict their ordering.",
      "Explain why setTimeout(fn, 0) is a floor (≥ ~4ms, after microtasks), not an instruction to run 'now'.",
      "Diagnose a blocked main thread and split heavy work without worker threads.",
      "Apply the nine-step method to an async race condition: out-of-order responses overwriting fresh state.",
      "Read console ordering puzzles as loop traces, not magic.",
    ],
    simple:
      "Picture a single chef in a kitchen with an endless ticket rail. The chef does one dish at a time, start to finish — never mid-chop interruptions. But the chef has assistants: a timer kid (setTimeout), a delivery person (fetch), a waiter (clicks). When their jobs finish, they pin a ticket to the rail. The chef's rule: finish the current dish, then clear ALL the tiny sticky notes (microtasks — promises) before taking exactly ONE big ticket (macrotask), and if there's a breath left, glance at the pass (paint the screen). One chef, well-organized rail, zero chaos. The 'chef never stops mid-dish' rule is why a heavy synchronous loop freezes every click, timer, and paint at once.",
    why:
      "Every async mystery in this course reduces to the loop: why a setTimeout(0) ran after a promise; why the UI froze during a big import parse; why two fast fetches can land out of order and show stale data (the exact bug you'll debug below, and again in Phase 5 with effects); why requestAnimationFrame is the home for visual work. Developers without this model sprinkle await and setTimeout like seasoning and pray. With it, you can trace any ordering bug from symptom to queue to fix — and 'it's async' stops being an explanation and starts being a question.",
    mentalModel: {
      title: "The chef and the ticket rail",
      body: "The call stack is the cutting board: one dish (task) at a time. Microtasks are sticky notes with top priority — after each dish, the chef clears ALL of them (and any notes those notes generate) before anything else. Macrotasks are the big rail tickets: timers, network completions, events — the chef takes ONE per round. Between rounds, if the rail allows, the browser paints. So: promises cut in front of timers, timers never run mid-dish, and a five-second sync loop means five seconds of un-painted, un-clickable, un-timed silence. Learn to ask 'which rail is this on, and what dish is the chef finishing first?'",
    },
    sections: [
      {
        heading: "One thread, many rails — the architecture",
        body: [
          "The JavaScript engine executes one call stack. Everything concurrent about the browser lives OUTSIDE that stack: timers, the network stack, DOM events, and paint all run in browser-managed threads. When that external work completes, its callback joins a queue; the event loop is the scheduler that feeds the single stack from those queues, in a strict order, forever.",
          "This division of labor explains the whole platform's personality. Your code never preempts; it monopolizes between tasks — which is why responsiveness is about keeping tasks short, not about threads. Web Workers exist precisely for genuinely heavy computation (they get their own stack), but 95% of 'my app janks' is solved by splitting work into tasks the loop can interleave with paint and input.",
        ],
        code: [
          {
            file: "architecture.js — who runs where",
            lang: "js",
            code: [
              "// YOUR code: one call stack, one thing at a time.",
              "// BROWSER threads (outside your stack):",
              "//   timers thread     → schedules setTimeout/setInterval callbacks",
              "//   network threads   → complete fetches, fire their promise reactions",
              "//   event dispatch    → queue click/keydown handlers",
              "//   paint/composite   → draws frames BETWEEN tasks when allowed",
              "",
              "// The loop glues them: stack → microtasks → ONE macrotask → paint? → repeat.",
              "// Consequence: a long-running task starves every other rail.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The loop's law — microtasks cut in line",
        body: [
          "The order, precisely: (1) run one macrotask (or the initial script) to completion; (2) drain the ENTIRE microtask queue — promise reactions, queueMicrotask, MutationObserver — including microtasks queued BY those microtasks; (3) take exactly one macrotask (the oldest ready timer, an event, an I-O completion); (4) the browser MAY paint; repeat. Two practical readings: promise callbacks always beat pending timers, no matter who was scheduled first; and a microtask that queues microtasks can starve paint just like a sync loop (an infinite promise chain is a freeze).",
          "The classic ordering puzzle writes itself: console.log(1) runs first (script task), a promise's .then logs before any setTimeout callback (microtask drain beats the timer rail), and setTimeout(…, 0) lands after both — despite being 'scheduled' earliest. Run the lab below and rearrange the queue until the ordering feels boring; boring is the goal.",
        ],
        code: [
          {
            file: "ordering-puzzle.js — predict EVERY line before running",
            lang: "js",
            code: [
              "console.log(\"1: script start\");",
              "",
              "setTimeout(() => console.log(\"5: timer (macrotask)\"), 0);",
              "",
              "Promise.resolve()",
              "  .then(() => console.log(\"3: first microtask\"))",
              "  .then(() => console.log(\"4: microtask queued by a microtask\"));",
              "",
              "queueMicrotask(() => console.log(\"2: explicit microtask\"));",
              "",
              "console.log(\"…script still synchronous\");",
              "",
              "// output order: 1, sync log, 2, 3, 4, 5",
              "// the script task finishes → ALL microtasks drain (2, 3, 4 —",
              "// including the one 3 created) → THEN the timer rail pays out.",
            ].join("\n"),
          },
        ],
        demo: "event-loop",
      },
      {
        heading: "setTimeout(0) is a floor — and blocking is a freeze",
        body: [
          "setTimeout(fn, 0) does not mean 'run fn now'. It means 'fn may run after at least ~0ms (browsers clamp nested timers to ≥4ms), after the current task, after all microtasks, when the timer rail's turn comes.' People use it to 'wait for the DOM' or 'let React settle' — both are superstitions with real APIs (event listeners, useEffect) that express the intent honestly.",
          "The darker half: while your task runs, NOTHING else does. A 3-second sync loop means 3 seconds of dead clicks, frozen animations, overdue timers all firing at once afterwards (timers don't queue per missed tick — a setInterval(100) blocked for 3s fires ONCE, immediately, when the thread frees). The cure, pre-Workers: split work into chunks across tasks — process a batch, then setTimeout(nextBatch, 0), letting paint and input breathe between chunks. Modern sugar for the same idea: scheduler.yield() where available.",
        ],
        code: [
          {
            file: "chunking.js — un-freezing a heavy job",
            lang: "js",
            code: [
              "// ✗ one 800ms task: no paint, no clicks, timers pile up",
              "function processAll(rows) {",
              "  for (const row of rows) heavyWork(row); // 10,000 rows",
              "}",
              "",
              "// ✓ the same work, in 200-row tasks the loop can interleave:",
              "function processInChunks(rows, size = 200) {",
              "  const queue = [...rows];",
              "  function nextChunk() {",
              "    const batch = queue.splice(0, size);",
              "    for (const row of batch) heavyWork(row);",
              "    if (queue.length > 0) setTimeout(nextChunk, 0);",
              "    else onDone();",
              "  }",
              "  nextChunk();",
              "}",
              "// between chunks the loop drains events and paints —",
              "// the UI stays alive while the job runs.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Timing guarantees are floors, not contracts",
        body: [
          "No timer is precise: nested setTimeouts clamp to ≥4ms, background tabs throttle to ~1s or pause entirely, a busy main thread delays every callback by however long it's blocked, and setInterval coalesces missed ticks instead of replaying them. Date.now() deltas around timers therefore measure the LOOP's health as much as your code — a 'why did my 100ms timer take 2.4s' investigation starts at the thread, not the timer.",
          "This reframes real decisions: animations belong in requestAnimationFrame (aligned to paint, paused in background), recurring background work accepts drift or moves to a worker, and any product feature promising millisecond timing in a browser tab is a promise the platform never made. When your future queue workers (Phase 25) need real timing contracts, they run server-side where the loop is yours alone.",
        ],
      },
      {
        heading: "Debugging lab — the race that shows stale data",
        body: [
          "The bug: fast typing fires two fetches; the first (slow) response lands AFTER the second (fast) one and overwrites fresh results with stale. Nine steps. READ: UI shows results that don't match the query. LAYER: frontend async ordering, not network, not server. REPRODUCE: type 'a', then quickly 'ab' with artificial latency on 'a'. EVIDENCE: log response order vs request order — they differ. HYPOTHESIS: responses are processed in ARRIVAL order, not REQUEST order. ONE CHANGE: track the latest request and ignore answers to older ones (a request id or AbortController.abort() on new input). VERIFY: stale response arrives, guard drops it, fresh data stays. ROOT CAUSE: concurrent fetches resolve unordered; nothing correlated request to response. PREVENT: a lint-level rule — every fetch whose result renders must be abortable-or-guarded; Phase 19's TanStack Query enforces exactly this with query keys.",
          "Notice what the method bought you: no library, no prayer — a named mechanism, a one-line guard, and a regression test you can write in Phase 31 (fire overlapping requests, assert last-write-wins-by-request-not-arrival).",
        ],
        code: [
          {
            file: "race-fix.js — ignore the ghosts of old requests",
            lang: "js",
            code: [
              "let latestRequestId = 0;",
              "",
              "async function search(query) {",
              "  const myId = ++latestRequestId;        // claim the newest slot",
              "  const results = await api.search(query);",
              "  if (myId !== latestRequestId) return;  // a newer request exists —",
              "  render(results);                       // this answer is a ghost",
              "}",
              "",
              "// stronger cousin: cancel the in-flight request itself",
              "let controller;",
              "async function searchWithAbort(query) {",
              "  controller?.abort();                   // kill the previous fetch",
              "  controller = new AbortController();",
              "  try {",
              "    const res = await fetch(`/api/search?q=${query}`, {",
              "      signal: controller.signal,",
              "    });",
              "    render(await res.json());",
              "  } catch (err) {",
              "    if (err.name !== \"AbortError\") throw err; // aborted = expected",
              "  }",
              "}",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "setTimeout as a synchronization primitive",
      wrong: [
        "function saveAndRedirect() {",
        "  saveDraft();              // async, not awaited",
        "  setTimeout(redirect, 500); // 'give it time to finish' — 500ms of",
        "}                            //  guessing. On a slow network the draft",
        "                             //  is lost; on a fast one the user waits",
        "                             //  half a second for nothing.",
        "",
        "// same superstition: setTimeout(render, 0) to 'let state settle'",
      ].join("\n"),
      right: [
        "async function saveAndRedirect() {",
        "  await saveDraft();        // the COMPLETION is the signal —",
        "  redirect();               // no time guessed, no race possible",
        "}",
        "",
        "// and for ordering: express intent with the real tools —",
        "// events, awaits, effect dependencies — not with delay folklore.",
      ].join("\n"),
      explain:
        "setTimeout(fn, ms) is a MINIMUM delay on a low-priority rail, not a synchronization signal. Basing correctness on a guessed duration creates two failure modes at once: too short (work unfinished → lost data, stale UI) and too long (users pay for your uncertainty). Await the completion, listen for the event, or abort the stale work — time-based hacks are how async bugs colonize a codebase.",
    },
    tryIt: [
      "Predict the full output order of ordering-puzzle.js on paper, THEN run it in node. If any line surprised you, re-trace it through the loop's law until it's boring.",
      "In the lab, queue: sync log, timer, microtask, heavy sync. Run it. Describe exactly when the freeze badge appears and why the microtask still beats the timer afterwards.",
      "Freeze your tab on purpose: run a 3-second while loop in the console of any page (carefully!). Click buttons during it. Then run the chunking pattern over 100,000 trivial iterations and feel the difference.",
      "Measure timer reality: schedule setTimeout(() => console.log(performance.now() - start), 0) on an idle page vs right after a busy loop. Compare. What does the delta prove?",
      "Reproduce the search race with any slow endpoint (or add an artificial delay): fire search('a'), then 50ms later search('ab'), with 'a' taking longer. Watch the stale overwrite, then apply the request-id guard and verify the ghost is dropped.",
    ],
    challenge: {
      prompt:
        "Build a 'task importer' page that processes 5,000 synthetic rows WITHOUT freezing the UI, with honest progress. Requirements: (1) process in chunks of 250, yielding between chunks via setTimeout(…, 0); (2) a progress bar (<progress> or a meter div) and a 'processed N of 5000' label that actually repaint between chunks — prove it by watching it move; (3) a Cancel button that stops processing at the next chunk boundary (a flag read each chunk — no mid-row interruption exists in single-threaded JS, and you should be able to explain why); (4) a final summary rendered via a promise chain (not async/await — practice both spellings), logging the elapsed ms; (5) a short written note: why does the progress bar update at all, given there is only one thread?",
      hints: [
        "Structure: function processChunk(start) { for 250 rows…; updateProgress(); if (cancelled || done) finish() else setTimeout(() => processChunk(start + 250), 0); } — the setTimeout is the paint window.",
        "Cancellation is cooperative: let cancelled = false; the button sets true; each chunk checks. The current chunk ALWAYS completes — that's not a bug, it's the single-threaded contract.",
        "For the promise-chain finale: new Promise(resolve => …) wrapping the kickoff, .then(({done, ms}) => renderSummary(…)), .finally(reset UI). And the written answer: paint happens BETWEEN macrotasks, and every chunk is its own macrotask.",
      ],
      solution: [
        "<!doctype html>",
        "<html lang=\"en\">",
        "<head><meta charset=\"utf-8\"><title>Importer — chunked</title>",
        "<style> body { font: 16px/1.6 system-ui; max-width: 30rem; margin: 2rem auto; }",
        " progress { width: 100%; } </style></head>",
        "<body>",
        "  <h1>Task importer</h1>",
        "  <p><button id=\"start\">Import 5,000 rows</button>",
        "     <button id=\"cancel\" disabled>Cancel</button></p>",
        "  <progress id=\"bar\" max=\"5000\" value=\"0\"></progress>",
        "  <p id=\"status\" aria-live=\"polite\">Idle.</p>",
        "  <script>",
        "    const TOTAL = 5000, CHUNK = 250;",
        "    const bar = document.querySelector(\"#bar\");",
        "    const status = document.querySelector(\"#status\");",
        "    const startBtn = document.querySelector(\"#start\");",
        "    const cancelBtn = document.querySelector(\"#cancel\");",
        "    let cancelled = false;",
        "",
        "    function heavyWork(i) {",
        "      // simulate real work: hash-ish busy math, ~microseconds each",
        "      let x = i;",
        "      for (let k = 0; k < 400; k++) x = (x * 31 + k) % 9973;",
        "      return x;",
        "    }",
        "",
        "    function importAll() {",
        "      return new Promise((resolve) => {",
        "        cancelled = false;",
        "        let processed = 0;",
        "        const t0 = performance.now();",
        "",
        "        function chunk() {",
        "          const end = Math.min(processed + CHUNK, TOTAL);",
        "          for (let i = processed; i < end; i++) heavyWork(i);",
        "          processed = end;",
        "",
        "          // ——— between-chunk UI work: THIS is what repaints ———",
        "          bar.value = processed;",
        "          status.textContent = `Processed ${processed} of ${TOTAL}` +",
        "            (cancelled ? \" — cancelling…\" : \"\");",
        "",
        "          if (cancelled) {",
        "            resolve({ done: false, processed, ms: performance.now() - t0 });",
        "          } else if (processed >= TOTAL) {",
        "            resolve({ done: true, processed, ms: performance.now() - t0 });",
        "          } else {",
        "            setTimeout(chunk, 0); // yield: one macrotask per chunk →",
        "          }                       // the loop paints & hears clicks between",
        "        }",
        "        chunk();",
        "      });",
        "    }",
        "",
        "    // promise-chain spelling for the finale:",
        "    startBtn.addEventListener(\"click\", () => {",
        "      startBtn.disabled = true;",
        "      cancelBtn.disabled = false;",
        "      importAll()",
        "        .then(({ done, processed, ms }) => {",
        "          status.textContent = done",
        "            ? `✓ Imported ${processed} rows in ${ms.toFixed(0)} ms`",
        "            : `Stopped at ${processed} rows after ${ms.toFixed(0)} ms`;",
        "        })",
        "        .finally(() => {",
        "          startBtn.disabled = false;",
        "          cancelBtn.disabled = true;   // runs on BOTH paths — that's the point",
        "        });",
        "    });",
        "",
        "    // cooperative cancellation: the CURRENT chunk always finishes;",
        "    // the flag is read at the next boundary. Single-threaded JS has",
        "    // no mid-row interruption — and doesn't need one if chunks are small.",
        "    cancelBtn.addEventListener(\"click\", () => { cancelled = true; });",
        "  </script>",
        "</body>",
        "</html>",
        "",
        "/* the written note: the bar moves because each chunk is its own",
        "   macrotask. After every chunk the loop drains microtasks, takes",
        "   our timer, and — crucially — gets a paint turn, so the DOM writes",
        "   we made inside the chunk actually render. One thread, many turns.",
        "   Remove the setTimeout (call chunk() directly) and the bar jumps",
        "   0 → 5000 in a single frozen frame. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "After the current task finishes, the loop next…",
        options: ["Drains the ENTIRE microtask queue, then takes ONE macrotask", "Takes the oldest macrotask", "Paints immediately", "Runs the next timer only"],
        answer: 0,
        explain: "Stack task → all microtasks (including ones they queue) → one macrotask → maybe paint. Promises therefore always cut ahead of timers.",
      },
      {
        q: "Which of these is a microtask?",
        options: ["A promise .then callback", "A setTimeout callback", "A click handler", "A fetch body read scheduled by the network thread"],
        answer: 0,
        explain: "Promise reactions, queueMicrotask, and MutationObserver callbacks are microtasks. Timers, events, and I-O completions queue as macrotasks.",
      },
      {
        q: "setTimeout(fn, 0) actually means…",
        options: ["'fn may run after ≥ ~0ms, after microtasks, when the timer rail's turn comes'", "'run fn immediately after this line'", "'run fn before any promise callback'", "'run fn on the next paint frame'"],
        answer: 0,
        explain: "It's a minimum-delay macrotask, not an instruction. Browsers clamp nested timers to ≥4ms, and any busy thread delays it further.",
      },
      {
        q: "During a 3-second synchronous loop, the page…",
        options: ["Cannot paint, dispatch clicks, or fire due timers — one thread is fully occupied", "Keeps animating on the GPU only", "Queues clicks and replays them instantly", "Switches to a worker thread"],
        answer: 0,
        explain: "Everything shareable — input, paint, timers — waits for the stack to free. Overdue intervals fire ONCE when it does. Short tasks are the whole game.",
      },
      {
        q: "Two search requests overlap; the first response arrives last and clobbers fresh results. The fix is…",
        options: ["Ignore responses whose request id is stale (or abort the old fetch)", "Sort responses by arrival", "Increase the timeout", "Refetch on every render"],
        answer: 0,
        explain: "Responses complete in network order, not request order. Correlate (latest-id guard) or cancel (AbortController). TanStack Query's query keys solve this same race in Phase 19.",
      },
      {
        q: "requestAnimationFrame beats setTimeout for animation because…",
        options: ["It fires in step with paint and pauses in background tabs", "It runs on a faster thread", "It has no delay clamp", "It's a microtask"],
        answer: 0,
        explain: "rAF callbacks run just before each frame the browser intends to paint — aligned to the display, battery-friendly, throttled when hidden. It's a macrotask-class citizen with paint privileges.",
      },
    ],
    flashcards: [
      { front: "The event loop", back: "The browser's scheduler for one JS thread: run a stack task → drain all microtasks → one macrotask → maybe paint → repeat. Forever." },
      { front: "Microtasks", back: "The priority lane: promise reactions, queueMicrotask, MutationObserver. All drain after each task — and can starve paint if they chain infinitely." },
      { front: "Macrotasks", back: "The big rail: setTimeout/setInterval, I-O completions, event dispatch. The loop takes exactly ONE per round, oldest ready first." },
      { front: "setTimeout(0) reality", back: "A minimum-delay macrotask — after microtasks, clamped ≥4ms nested, delayed by any busy thread. A floor, never 'immediately'." },
      { front: "Blocked thread", back: "One long task freezes paint, input, and timers together. Overdue intervals coalesce into one catch-up fire. Fix: split into chunked tasks (or Workers)." },
      { front: "The race condition", back: "Overlapping fetches resolve in network order, not request order; a slow stale answer overwrites fresh state. Fix: latest-id guard or AbortController." },
      { front: "AbortController", back: "signal: controller.signal on fetch; controller.abort() rejects it with AbortError — cancel the stale work instead of ignoring its ghost." },
      { front: "Why timers aren't sync tools", back: "A delay is a guessed duration with two failure modes (too short: unfinished; too long: wasted). Await the completion or listen for the event instead." },
    ],
    recap: [
      "One thread; the browser runs everything else and queues callbacks for your stack.",
      "Task → all microtasks → one macrotask → maybe paint. Promises cut in line; timers take a number.",
      "setTimeout(0) is a floor, not a signal; never synchronize on guessed durations.",
      "Long tasks freeze the world — chunk work across macrotasks and the loop keeps the UI alive.",
      "The nine-step method cracked a real race: correlate requests, drop ghosts, test last-write-wins. Phase 2: complete.",
    ],
    references: [
      { label: "MDN — The event loop", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Event_loop" },
      { label: "MDN — queueMicrotask()", url: "https://developer.mozilla.org/en-US/docs/Web/API/queueMicrotask" },
      { label: "MDN — AbortController", url: "https://developer.mozilla.org/en-US/docs/Web/API/AbortController" },
      { label: "web.dev — Inside look at modern web browsers (part 2)", url: "https://web.dev/articles/inside-browser-part2" },
      { label: "HTML spec — Timers (nesting clamp)", url: "https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html" },
    ],
    nextBridge:
      "Phase 2 is done: the language, the DOM, the network, and the machine that schedules all of it — plus a working Todo app that speaks HTTP. Phase 3 asks the next question: how do you PROVE your types before runtime ever gets a chance to disagree? TypeScript, starting with inference, annotations, and the strict compiler that becomes your co-pilot for the rest of the course.",
  },
];

export const LESSON_CONTENT_P2C: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P2C.map((l) => [l.id, l]),
);
