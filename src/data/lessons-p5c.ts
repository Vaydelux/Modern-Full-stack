import type { LessonContent } from "./types";

/**
 * Pass 015 bounded content batch: Phase 5 closers (L5–L8).
 * L8 is the first cumulative Boss Battle (BB-1). No placeholders.
 */
export const LESSONS_P5C: LessonContent[] = [
  {
    id: "p05-l5",
    phaseId: "p05",
    title: "useTransition, useDeferredValue & use",
    level: "React Developer",
    minutes: 35,
    summary:
      "Not every update deserves the same urgency. Typing in a filter box must paint instantly; the thousand-row list that re-sorts behind it can afford to be a frame late. This lesson installs React's concurrency tools: useTransition marks a state update as interruptible so urgent paints win, useDeferredValue lets a derived view lag behind a fast-changing input, and use unwraps promises and context with a Suspense-friendly syntax. You'll learn when these are the right tool — and, just as importantly, that they are a last resort after memoization and correct modeling.",
    prerequisites: ["p05-l4 — useMemo, useCallback & useId", "p04-l7 — reconciliation & the render model"],
    objectives: [
      "Distinguish urgent updates (input, toggle) from transition updates (filter results, navigation) and mark them accordingly.",
      "Use useTransition to keep an input responsive while expensive work renders in the background, reading isPending for a progress cue.",
      "Apply useDeferredValue to defer an expensive derived render without duplicating state.",
      "Explain how use unwraps a Promise (with Suspense) and reads context, and its current rules and limits.",
      "Reach for these tools only after profiling shows a real paint-blocking render — never as preemptive decoration.",
    ],
    simple:
      "Imagine you're mid-sentence in a conversation and someone hands you a heavy box. You have two choices: stop talking, wrestle the box, then resume — or keep talking while a helper carries the box in behind you. React's urgent updates are your words: they must not be interrupted. Transitions are the box: React can start rendering them, pause when something urgent arrives, and resume later. useTransition says 'this update is the box'; useDeferredValue says 'this derived view is the box'; use says 'this value might arrive later, I'll wait gracefully.' None of them make work faster — they just decide what the user gets to see first.",
    why:
      "A filter box that freezes for 400ms on every keystroke is a product that feels broken, even if every line of logic is correct. Concurrency is how professional React apps stay responsive under real data loads — the difference between a prototype and the search page of a serious tool. But these APIs are the most misused in the toolkit: sprayed onto every component as performance magic, they add complexity for zero gain and hide the real problem (an un-memoized render). Learn them with the discipline this course insists on: profile first, fix the cause, and reach for concurrency only when some render genuinely must lose to a paint.",
    mentalModel: {
      title: "Urgent words, carried boxes",
      body: "React renders in priority order. Urgent updates — a keystroke, a checkbox, a route change the user initiated — go to the front of the line and must paint now. A transition is an update you've explicitly downgraded: React renders it, but if an urgent update arrives mid-render, it throws the transition's work away and restarts after painting. That throw-away-and-restart is why transition renders must be pure and interruptible. useTransition hands you isPending — 'the box is still being carried' — for a tasteful loading cue. useDeferredValue is the same idea for a VALUE: you hand React the latest input immediately, but the expensive view may render against a slightly older copy until it's quiet. use is a different animal: it's how a component SUSPENDS on a value that isn't ready yet (a promise) or reads context with cleaner syntax — the building block the Server Components world leans on.",
    },
    sections: [
      {
        heading: "useTransition — downgrade the heavy update",
        body: [
          "The shape: const [isPending, startTransition] = useTransition(). When the user acts, split the work: set the urgent state normally (the input value), and wrap the expensive downstream state in startTransition(() => setFiltered(bigComputation(input))). React keeps the input painting every frame; the filtered list re-renders when it can, and isPending is true while it's in flight — your cue to dim the results or show a thin progress bar.",
          "Two rules keep transitions honest. First, the function inside startTransition must be a pure state update — no async work, no side effects; it's render work React may restart. Second, a transition only helps when the expensive part is RE-RENDERING, not the computation itself — if bigComputation is a 400ms synchronous loop, no transition saves you (that's a Web Worker or a memo problem). Transitions move the render off the urgent path; they don't make slow code fast.",
        ],
        code: [
          {
            file: "transition.tsx — input stays crisp, list catches up",
            lang: "tsx",
            code: [
              "function SearchableList({ items }: { items: Item[] }) {",
              "  const [query, setQuery] = useState(\"\");        // urgent: paints every keystroke",
              "  const [filtered, setFiltered] = useState(items);",
              "  const [isPending, startTransition] = useTransition();",
              "",
              "  function onChange(next: string) {",
              "    setQuery(next);                              // urgent",
              "    startTransition(() => {                      // transition: interruptible",
              "      setFiltered(items.filter((i) => i.name.includes(next)));",
              "    });",
              "  }",
              "",
              "  return (",
              "    <div>",
              "      <input value={query} onChange={(e) => onChange(e.target.value)} />",
              "      {isPending && <progress aria-label=\"Filtering…\" />}  {/* the box is being carried */}",
              "      <List items={filtered} />",
              "    </div>",
              "  );",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "useDeferredValue — let the view lag one beat",
        body: [
          "Sometimes you don't own the setState — the value just flows in as a prop or piece of state — but you still want the expensive view to defer. const deferred = useDeferredValue(value) returns the latest value during quiet periods, but may return a slightly STALE value while urgent updates are landing. Render the expensive subtree against deferred; React re-renders it against the fresh value once it's caught up. It's useTransition for a value you didn't produce.",
          "The key discipline: deferred is not a throttle and not debouncing. It still renders the fresh value eventually, and it renders the stale one only while the main thread is busy with urgent work. If you want 'wait until the user stops typing' (debounce), that's a timer job (you built it in P02-L2) — different tool, different guarantee. And because the deferred render can show stale data, design the UI so a moment of staleness reads as 'catching up' (a subtle opacity shift), not as wrong results.",
        ],
        code: [
          {
            file: "deferred.tsx — prop in, heavy view out",
            lang: "tsx",
            code: [
              "function Results({ query }: { query: string }) {   // query arrives as a prop",
              "  const deferredQuery = useDeferredValue(query);",
              "  const isStale = query !== deferredQuery;         // catching-up cue",
              "",
              "  const rows = useMemo(",
              "    () => heavyRank(deferredQuery),                // expensive render deferred",
              "    [deferredQuery],",
              "  );",
              "",
              "  return <section style={{ opacity: isStale ? 0.6 : 1 }}>{rows}</section>;",
              "}",
              "",
              "// NOT debouncing: deferredQuery still reaches every value,",
              "// just after the urgent keystrokes have painted.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "use — promises and context, unwrapped",
        body: [
          "use is the odd one out: it's a function you can call conditionally (unlike every other hook), and it does two jobs. Pass it a Promise and the component SUSPENDS — React shows the nearest <Suspense> fallback until the promise resolves, then re-renders with the value. Pass it a Context and it's a useContext you can call inside loops and conditionals. In the Server Components + Suspense world this is how data boundaries read async values.",
          "Current rules that matter for client code: use(promise) expects the promise to be created OUTSIDE the component render (cached in a loader, context, or module) — a fresh promise every render suspends forever. It has no built-in error or refetch story; you wrap with an ErrorBoundary for failures and manage lifecycle yourself. For most client-side fetching today, TanStack Query (Phase 19) remains the higher-level answer; use earns its keep at Suspense boundaries and in server-rendered code. Know it, respect its edges, don't hand-roll your fetching with it.",
        ],
        code: [
          {
            file: "use.tsx — suspending on a cached promise",
            lang: "tsx",
            code: [
              "// promise created OUTSIDE render (cached), not inside:",
              "function loadUser(id: string) {",
              "  return fetch(`/api/users/${id}`).then((r) => r.json());",
              "}",
              "const userPromiseCache = new Map<string, Promise<User>>();",
              "const getUser = (id: string) => {",
              "  if (!userPromiseCache.has(id)) userPromiseCache.set(id, loadUser(id));",
              "  return userPromiseCache.get(id)!;",
              "};",
              "",
              "function Profile({ id }: { id: string }) {",
              "  const user = use(getUser(id));   // suspends until resolved",
              "  return <h1>{user.name}</h1>;",
              "}",
              "",
              "<Suspense fallback={<Skeleton />}>   {/* shown while pending */}",
              "  <ErrorBoundary fallback={<Failed />}>  {/* catches a rejection */}",
              "    <Profile id=\"42\" />",
              "  </ErrorBoundary>",
              "</Suspense>",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "When NOT to reach for concurrency",
        body: [
          "These tools have a cost: more code, more states to reason about, and they only help a specific symptom (a render blocking a paint). Run the checklist before writing startTransition anywhere. Is the render actually slow, measured in the Profiler (P05-L4 / P06-L4)? Can memoization fix it (useMemo the expensive derive, memo the heavy child)? Is the slow part computation rather than rendering (then it's a Worker or an algorithm problem, not a concurrency one)? Only when the answer is 'yes it's a slow render, no memoization won't cut it, and I measured' does a transition belong.",
          "The failure mode to name: wrapping a fast update in a transition buys you nothing but an extra API and an isPending you have to design around. And a transition around an async fetch doesn't dedupe, cache, cancel, or retry — it just paints around the render. That's why the course routes real data fetching to TanStack Query in Phase 19: concurrency is a paint-scheduling tool, not a data layer.",
        ],
      },
    ],
    mistake: {
      title: "Spraying transitions as performance magic",
      wrong: [
        "function Settings({ user }: { user: User }) {",
        "  const [isPending, startTransition] = useTransition();",
        "  // wrapping a tiny update that paints in <1ms:",
        "  function toggle() {",
        "    startTransition(() => setDark((d) => !d));  // ← why? this was instant",
        "  }",
        "  // meanwhile the REAL problem — an un-memoized 300-row table —",
        "  // re-renders on every keystroke, untouched.",
        "}",
      ].join("\n"),
      right: [
        "// 1) Profile: the table, not the toggle, blocks paint.",
        "// 2) Fix the cause: memo the table + useMemo the sorted rows.",
        "const Table = memo(RawTable);",
        "const sorted = useMemo(() => heavySort(rows), [rows, sortKey]);",
        "// 3) toggle stays a normal, instant setState — no ceremony.",
        "function toggle() { setDark((d) => !d); }",
        "// 4) Only if a legitimately heavy render still blocks an urgent",
        "//    input do we wrap THAT update in startTransition.",
      ].join("\n"),
      explain:
        "A transition only moves a slow RENDER off the urgent path. Wrapping a fast update adds an API, a pending state to design, and zero benefit — while the real bottleneck (an un-memoized subtree) keeps blocking. Concurrency is a last resort ordered after measurement and memoization, not a default. The checklist is: measure → fix the cause → memoize → then, and only then, transition.",
    },
    tryIt: [
      "Build a 5,000-item filterable list WITHOUT concurrency. Type fast and watch the input lag. Then add useTransition and feel the input become crisp while isPending shows.",
      "Convert the same list to useDeferredValue (receiving the query as a prop) and add the isStale opacity cue. Compare the UX to the transition version.",
      "Add a debounce (from P02-L2) in front of the deferred render and articulate, out loud, the difference in guarantee between 'deferred' and 'debounced'.",
      "Wrap a fast toggle in startTransition and confirm isPending is essentially never visible — proof that you paid for nothing.",
      "Write a component that calls use(freshPromise) where the promise is created INSIDE render, observe the infinite suspend, then fix it with the module-level cache.",
    ],
    challenge: {
      prompt:
        "Build a 'command palette' search: an input, and a results panel that ranks up to 20,000 string matches with a simple scoring function. Requirements: (1) the input paints on every keystroke (urgent); (2) the ranking render is a transition, with isPending driving a thin top progress bar (role=\"progressbar\", aria-valuetext); (3) an empty state and a no-results state, both announced via aria-live; (4) the scoring function is memoized so identical queries don't re-score; (5) a comment in the code justifying, in one sentence each, why you chose transition over deferral (or vice versa) and why memoization alone wasn't enough. Do NOT add debounce.",
      hints: [
        "The urgent/transition split: setQuery(input) is urgent; startTransition(() => setResults(rank(input))) is the deferred part. isPending from useTransition drives the progress bar.",
        "Memoize rank over the query and the dataset: useMemo(() => rank(query, items), [query, items]). Without it, every transition re-scores even when the query didn't change.",
        "Justify honestly: the ranking is a render-bound cost on a large list that must not block the input — memoization alone can't help when the query genuinely changes on every keystroke, so the render itself must be interruptible.",
      ],
      solution: [
        "import { useMemo, useState, useTransition } from \"react\";",
        "",
        "function score(item: string, query: string): number {",
        "  // simple subsequence score; cheap per item, expensive × 20k",
        "  let i = 0;",
        "  for (const ch of item.toLowerCase()) {",
        "    if (ch === query[i]) i++;",
        "    if (i === query.length) return item.length; // matched, shorter ranks higher",
        "  }",
        "  return -1;",
        "}",
        "",
        "export function CommandPalette({ items }: { items: string[] }) {",
        "  const [query, setQuery] = useState(\"\");",
        "  const [results, setResults] = useState<string[]>([]);",
        "  const [isPending, startTransition] = useTransition();",
        "",
        "  // memoized: identical query → cached ranking",
        "  const ranked = useMemo(() => {",
        "    if (!query) return [];",
        "    return items",
        "      .map((item) => ({ item, s: score(item, query) }))",
        "      .filter((x) => x.s >= 0)",
        "      .sort((a, b) => a.s - b.s)",
        "      .slice(0, 50)",
        "      .map((x) => x.item);",
        "  }, [query, items]);",
        "",
        "  function onChange(next: string) {",
        "    setQuery(next);                 // urgent: input paints every keystroke",
        "    startTransition(() => {",
        "      setResults(rankedRef(next));  // transition: interruptible render",
        "    });",
        "  }",
        "  // (in practice you'd compute ranked from query in a memo and set it",
        "  //  inside the transition against the deferred query — shown simplified)",
        "  function rankedRef(q: string) {",
        "    if (!q) return [];",
        "    return items",
        "      .map((item) => ({ item, s: score(item, q) }))",
        "      .filter((x) => x.s >= 0).sort((a, b) => a.s - b.s)",
        "      .slice(0, 50).map((x) => x.item);",
        "  }",
        "",
        "  const empty = query.length === 0;",
        "  const none = !empty && results.length === 0;",
        "",
        "  // WHY transition (one sentence): ranking 20k items is a render-bound",
        "  // cost that must not block the input, and memoization can't help when",
        "  // the query genuinely changes on every keystroke.",
        "  return (",
        "    <div>",
        "      {isPending && (",
        "        <div role=\"progressbar\" aria-valuetext=\"Searching…\" aria-label=\"Searching\" />",
        "      )}",
        "      <input value={query} onChange={(e) => onChange(e.target.value)} aria-label=\"Search\" />",
        "      <ul aria-live=\"polite\">",
        "        {empty && <li>Type to search…</li>}",
        "        {none && <li>No matches.</li>}",
        "        {results.map((r) => <li key={r}>{r}</li>)}",
        "      </ul>",
        "    </div>",
        "  );",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        q: "startTransition is appropriate for…",
        options: ["An expensive re-render that must not block an urgent paint (typing, toggling)", "Any async fetch", "A fast state toggle", "Synchronous computation that's slow"],
        answer: 0,
        explain: "Transitions move a slow RENDER off the urgent path and let React interrupt it. They don't make slow code fast, don't wrap async work, and add nothing for fast updates.",
      },
      {
        q: "While a transition is in flight, isPending is…",
        options: ["true — your cue for a progress/loading indicator", "false until it finishes", "undefined", "only true on the first render"],
        answer: 0,
        explain: "isPending reflects the in-flight transition, letting you show a tasteful 'catching up' cue without blocking the urgent UI.",
      },
      {
        q: "useDeferredValue differs from debounce because…",
        options: ["It still renders every value, just after urgent work; debounce drops intermediate values on a timer", "It's faster", "It cancels requests", "It memoizes"],
        answer: 0,
        explain: "Deferred may show a briefly stale value while the thread is busy, but always converges to the latest. Debounce deliberately waits for a pause and skips intermediate values.",
      },
      {
        q: "The function passed to startTransition must be…",
        options: ["A pure, synchronous state update React may restart", "An async function", "Allowed to fetch data", "Allowed to mutate external state"],
        answer: 0,
        explain: "React can throw away and restart transition renders, so the work must be pure and interruptible. Async and side effects don't belong inside.",
      },
      {
        q: "A promise passed to use(promise) must be…",
        options: ["Created outside render and cached, or it suspends forever", "Created fresh on each render", "A resolved value", "Wrapped in useEffect"],
        answer: 0,
        explain: "use suspends until the promise resolves; a fresh promise every render means it never settles. Cache the promise (loader/context/module) outside render.",
      },
      {
        q: "Before adding useTransition you should have…",
        options: ["Profiled to confirm a slow render, and ruled out memoization and a computation fix", "Added it everywhere by default", "Replaced useState", "Disabled Strict Mode"],
        answer: 0,
        explain: "Concurrency is a last resort for a measured, render-bound cost that memoization can't solve. Spraying it adds complexity for zero gain and hides real bottlenecks.",
      },
    ],
    flashcards: [
      { front: "useTransition", back: "Downgrades a state update to interruptible: React can pause it to paint urgent work first. Returns [isPending, startTransition]." },
      { front: "isPending", back: "True while a transition is in flight — the signal for a progress/loading cue. Lets you show 'working' without blocking the input." },
      { front: "useDeferredValue", back: "Returns the latest value when idle, but may return a stale value while urgent work lands. Defers an expensive derived render without extra state." },
      { front: "Deferred vs debounce", back: "Deferred renders every value, just after urgent work; debounce waits for a pause and drops intermediate values on a timer. Different guarantees." },
      { front: "use", back: "Unwraps a cached Promise (suspending until resolved, needs Suspense + ErrorBoundary) or reads a Context. Callable in conditionals — unlike other hooks." },
      { front: "Transition render rules", back: "The startTransition body must be a pure, synchronous state update. No async, no side effects — React may restart it." },
      { front: "When NOT to transition", back: "Fast updates, async fetching, or slow computation. Only a measured slow RENDER that memoization can't fix justifies it." },
      { front: "Concurrency ≠ data layer", back: "Transitions schedule paint priority; they don't cache, dedupe, cancel, or retry. Real fetching belongs to TanStack Query (Phase 19)." },
    ],
    recap: [
      "Urgent updates paint now; transitions are interruptible work React can defer.",
      "isPending and deferred-staleness are signals to design 'catching up' cues around.",
      "use suspends on a cached promise (Suspense + ErrorBoundary) or reads context.",
      "Concurrency is a last resort: measure → fix the cause → memoize → then transition.",
      "It schedules paint priority; it never replaces a data layer.",
    ],
    references: [
      { label: "react.dev — useTransition", url: "https://react.dev/reference/react/useTransition" },
      { label: "react.dev — useDeferredValue", url: "https://react.dev/reference/react/useDeferredValue" },
      { label: "react.dev — use", url: "https://react.dev/reference/react/use" },
      { label: "react.dev — Suspense", url: "https://react.dev/reference/react/Suspense" },
      { label: "react.dev — Working with Transitions", url: "https://react.dev/reference/react/useTransition#working-with-transitions" },
    ],
    nextBridge:
      "You can now schedule work by urgency. The last of the current-stable hooks meet FORMS — the place async, pending state, and optimistic feedback collide. Next lesson: useActionState, useFormStatus, and useOptimistic, the trio that makes async submission feel instant while staying honest.",
  },
  {
    id: "p05-l6",
    phaseId: "p05",
    title: "Action Hooks: useActionState, useFormStatus & useOptimistic",
    level: "React Developer",
    minutes: 40,
    summary:
      "Forms are where async, pending state, and user trust collide. React's action hooks give you the current-stable toolkit: useActionState binds an async form action to state (pending flag, result, error), useFormStatus lets a submit button know it's submitting without lifting state, and useOptimistic shows the user their intent instantly while the server confirms — with a built-in rollback when it reverts. This lesson builds the three together into a submission flow that feels instant yet never lies, and names exactly when optimism becomes a liability.",
    prerequisites: ["p05-l5 — useTransition & use", "p04-l5 — forms & controlled inputs", "p02-l5 — async/await"],
    objectives: [
      "Wire an async form action with useActionState and read its [state, dispatch, isPending] tuple.",
      "Use useFormStatus inside a child of a <form> to drive a pending submit button without prop drilling.",
      "Implement optimistic updates with useOptimistic and explain the automatic rollback to committed state.",
      "Distinguish actions that are safe to be optimistic (a like, a reorder) from those that are not (a payment, a delete).",
      "Explain the Server Actions / progressive-enhancement context these hooks live in, and where a client-side fetch action fits.",
    ],
    simple:
      "When you tap 'like', the honest sequence is: request flies to the server, server writes, response returns, UI updates. That round trip is 200–800ms of nothing — the moment where users double-tap and wonder if it worked. Optimism says: show the heart filled NOW, because it almost certainly will be; if the server says no, quietly take it back. useOptimistic is that contract in one hook — the optimistic value renders until the real action settles, then it's replaced (or rolled back). useActionState manages the async action itself — its pending flag and its outcome. useFormStatus is the tiny helper that lets a button know, from inside the form, that a submission is in flight. Together they make async feel instant while the source of truth stays on the server.",
    why:
      "Perceived latency is a product metric. The difference between a form that freezes for a second and one that responds instantly — while still validating and confirming on the server — is the difference between software that feels responsive and software that feels fragile. These hooks encode hard-won patterns (pending flags, double-submit prevention, optimistic rollback, error surfacing) that every team used to hand-roll badly. And they sit at the exact trust line this course repeats: the UI may be optimistic, but the server remains authoritative (Phase 20/21). Learn them now and Phase 19/21's real submissions are mostly wiring.",
    mentalModel: {
      title: "The confident waiter",
      body: "A good waiter takes your order and immediately writes it on the pad in front of you — you SEE your intent accepted — while the kitchen (the server) does the real work. If the kitchen runs out of the special, the waiter comes back and crosses it out: the optimistic note is replaced by the truth. useOptimistic is the pad: it shows your intent instantly, keyed to the pending action, and snaps to committed reality when the action settles. useActionState is the waiter's clipboard: it tracks whether an order is in flight (isPending) and what came back (state, including errors). useFormStatus is the waiter's glance at the table — any element inside the form can see 'we're mid-order' without being told. The kitchen is still the authority; the pad just makes the wait humane.",
    },
    sections: [
      {
        heading: "useActionState — an async action, tracked",
        body: [
          "const [state, dispatch, isPending] = useActionState(actionFn, initialState). You hand it an async action (a function receiving the previous state and a payload — commonly the FormData from a form) and an initial state; it returns the latest result, a dispatch to invoke the action (usable as a form's action), and a pending flag that's true while the action is in flight. It unifies what teams used to assemble from useState + try/catch + a manual setSubmitting.",
          "The action's return value becomes the new state — so returning a discriminated result ({ ok: true, data } | { ok: false, error }) from the Phase 3 playbook lets the render branch cleanly on success vs error. isPending gives you the double-submit guard for free (disable the submit while in flight) and the loading cue. Because the action is bound to the form, Enter-to-submit, progressive enhancement, and the action lifecycle come along — this is the shape Server Actions use, and it works identically for a client-side async fetch action.",
        ],
        code: [
          {
            file: "action-state.tsx — pending + result in one tuple",
            lang: "tsx",
            code: [
              "async function submitComment(prev: Result, formData: FormData) {",
              "  const text = String(formData.get(\"text\") ?? \"\").trim();",
              "  const res = await postComment(text);        // Result from Phase 3",
              "  return res;                                  // becomes the new state",
              "}",
              "",
              "function CommentBox() {",
              "  const [result, submit, isPending] = useActionState(submitComment, { ok: true });",
              "",
              "  return (",
              "    <form action={submit}>",
              "      <textarea name=\"text\" required aria-label=\"Comment\" />",
              "      <button disabled={isPending}>{isPending ? \"Posting…\" : \"Post\"}</button>",
              "      {result.ok === false && <p role=\"alert\">{result.error}</p>}",
              "    </form>",
              "  );",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "useFormStatus — pending, from inside the form",
        body: [
          "Inside any component rendered as a DESCENDANT of a <form>, const { pending, data, method, action } = useFormStatus() tells you whether that form is mid-submission. The classic win: a SubmitButton component that shows a spinner and disables itself while pending — with zero state lifted and zero props drilled. It only works inside the form subtree (it reads the nearest form context), which is a feature: it can't accidentally report a sibling form's status.",
          "This is context doing exactly what P05-L3 said it's for: a rare, form-wide fact (submission in flight) that many children need. You don't wire it yourself; you consume it. Pair it with useActionState's isPending when you're not inside a form, and you have pending coverage for both worlds.",
        ],
        code: [
          {
            file: "form-status.tsx — a self-aware submit button",
            lang: "tsx",
            code: [
              "function SubmitButton({ children }: { children: ReactNode }) {",
              "  const { pending } = useFormStatus();   // only valid inside a <form>",
              "  return (",
              "    <button type=\"submit\" disabled={pending} aria-busy={pending}>",
              "      {pending ? \"Saving…\" : children}",
              "    </button>",
              "  );",
              "}",
              "",
              "<form action={saveProfile}>",
              "  <input name=\"name\" />",
              "  <SubmitButton>Save</SubmitButton>   {/* knows it's submitting */}",
              "</form>",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "useOptimistic — show intent, snap to truth",
        body: [
          "const [optimistic, addOptimistic] = useOptimistic(realState, mergeFn). Render optimistic instead of realState. During an async action, call addOptimistic(optimisticPatch) to immediately layer your intent on top of the current state; React shows it until the surrounding action settles, then discards it and the UI renders the committed realState. If the action succeeds, realState already matches your optimism (no visible change); if it fails, the optimistic layer evaporates — a free rollback, no manual undo bookkeeping.",
          "The mental contract: optimism is a temporary overlay keyed to a pending action, not a second source of truth. You never edit realState optimistically; you only add a preview that the action's resolution replaces. This is why it's safe where hand-rolled optimism is dangerous — there's no window where the UI believes a lie it must remember to retract; the retraction is structural.",
        ],
        code: [
          {
            file: "optimistic.tsx — like it now, confirm it shortly",
            lang: "tsx",
            code: [
              "function LikeButton({ likes, onLike }: { likes: number; onLike: () => Promise<void> }) {",
              "  const [optimisticLikes, addOptimistic] = useOptimistic(likes);",
              "",
              "  async function handleLike() {",
              "    addOptimistic(optimisticLikes + 1);   // instant: fill the heart",
              "    await onLike();                        // real write; on settle the",
              "  }                                        // overlay snaps to committed likes",
              "",
              "  return <button onClick={handleLike}>♥ {optimisticLikes}</button>;",
              "}",
              "// success: committed likes === optimistic → no flicker",
              "// failure: overlay discarded → count reverts. No manual rollback.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "When optimism is a lie — choosing what to be optimistic about",
        body: [
          "Optimism is a bet that failure is rare and cheap to reverse. A like, a reorder, marking a task done — failures are visible, reversible, and low-stakes, so instant feedback wins. A payment, an account deletion, a seat reservation — failures are costly, sometimes irreversible, and users must not be shown a success that didn't happen; there, pending + confirmation beats false confidence. The rule: be optimistic only when the optimistic state, if wrong, can be taken back without harm or confusion.",
          "Two more guardrails. Show a recoverable error when the rollback happens — a silent revert reads as a glitch, so pair useOptimistic with a toast or inline message when the action fails. And keep the real action authoritative: optimism is presentation, the server's committed state is truth (the Phase 20/21 line). Never let an optimistic value be persisted, sent to another endpoint, or used to compute something the user relies on.",
        ],
      },
    ],
    mistake: {
      title: "Optimism with no rollback plan",
      wrong: [
        "// hand-rolled 'optimism' — edits the real state, trusts the future:",
        "async function transfer() {",
        "  setBalance((b) => b - 500);        // moved the money NOW…",
        "  const res = await sendMoney(500);",
        "  if (!res.ok) { /* …and forgot to put it back */ }",
        "}",
        "// failure leaves the UI showing money that never moved.",
        "// users now believe a lie the code must remember to retract.",
      ].join("\n"),
      right: [
        "const [optimisticBalance, addOptimistic] = useOptimistic(balance);",
        "",
        "async function transfer(formData: FormData) {",
        "  addOptimistic(balance - 500);        // overlay, not truth",
        "  const res = await sendMoney(500);    // the real write",
        "  if (!res.ok) notify(res.error);      // rollback is structural;",
        "  return res;                          // the error is surfaced",
        "}",
        "// on settle: overlay discarded → UI renders committed balance.",
        "// money (a non-reversible action) arguably shouldn't be optimistic",
        "// at all — pending + confirmation is the honest pattern here.",
      ].join("\n"),
      explain:
        "Hand-rolled optimism mutates the source of truth on a promise the future may break, and the revert is a branch someone forgets. useOptimistic makes the revert structural — the overlay only exists while the action is pending. But the deeper lesson is judgment: a balance transfer isn't a 'like'. Optimism belongs to cheap, reversible intents; costly or irreversible actions get pending + confirmation, because a false success is worse than a short wait.",
    },
    tryIt: [
      "Build a comment form with useActionState returning a Result. Submit with an empty server response simulated as an error and read the error branch render.",
      "Add a SubmitButton using useFormStatus. Verify it disables only during THIS form's submission, not a sibling form's.",
      "Wire the LikeButton with useOptimistic against a fake action that succeeds, then one that rejects. Watch the count snap forward on success and revert on failure.",
      "Make the failing action show a toast on rollback instead of reverting silently, and articulate why silent reverts read as glitches.",
      "Identify three actions in an app you use daily and classify each: safe-to-be-optimistic or needs-confirmation. Justify each in one sentence.",
    ],
    challenge: {
      prompt:
        "Build a 'quick task' widget: an input + Add button (useActionState + useFormStatus), a list with a toggle-done on each task (useOptimistic over the whole list), and a Delete that is deliberately NOT optimistic. Requirements: (1) adding shows 'Adding…' and disables via useFormStatus; (2) toggling done is optimistic — the checkbox flips instantly, reverts if the fake server rejects (make ~30% reject randomly); (3) on a reverted toggle, show an inline error naming the task; (4) Delete shows a pending state but waits for confirmation (no optimism) — justify in a comment why; (5) the committed list is the source of truth; optimistic overlays never persist. Deliver the component plus a 3-sentence defense of your optimism boundaries.",
      hints: [
        "useOptimistic over the list: const [optTasks, addOptimistic] = useOptimistic(tasks); on toggle, addOptimistic(tasks.map(t => t.id===id ? {...t, done:!t.done} : t)) then await the action. On settle, render tasks (committed) again.",
        "For the toggle action, return a Result; if it fails, call the error callback with the task's title so the inline message can name it.",
        "Delete justification: deletion is irreversible — an optimistic delete that fails would have to resurrect a row the user believes is gone, which is more confusing than a brief pending state. Hence pending + confirmation, no overlay.",
      ],
      solution: [
        "import { useActionState, useFormStatus, useOptimistic } from \"react\";",
        "",
        "type Task = { id: string; title: string; done: boolean };",
        "type Result = { ok: true } | { ok: false; error: string };",
        "const flaky = () => Math.random() < 0.3;",
        "",
        "function AddButton() {",
        "  const { pending } = useFormStatus();        // valid: inside <form>",
        "  return <button disabled={pending}>{pending ? \"Adding…\" : \"Add\"}</button>;",
        "}",
        "",
        "export function QuickTasks() {",
        "  const [tasks, setTasks] = useState<Task[]>([]);",
        "  const [error, setError] = useState<string | null>(null);",
        "",
        "  // ——— add: pending + confirmation, no optimism needed for a create ———",
        "  async function addTask(_prev: Result, formData: FormData): Promise<Result> {",
        "    const title = String(formData.get(\"title\") ?? \"\").trim();",
        "    if (!title) return { ok: false, error: \"Title required\" };",
        "    await fakeCreate(title);",
        "    setTasks((t) => [...t, { id: crypto.randomUUID(), title, done: false }]);",
        "    return { ok: true };",
        "  }",
        "  const [, addAction] = useActionState(addTask, { ok: true as const });",
        "",
        "  // ——— toggle: optimistic over the committed list ———",
        "  const [optTasks, addOptimistic] = useOptimistic(tasks);",
        "",
        "  async function toggle(id: string) {",
        "    const target = tasks.find((t) => t.id === id)!;",
        "    addOptimistic(                          // instant intent…",
        "      tasks.map((t) => (t.id === id ? { ...t, done: !t.done } : t)),",
        "    );",
        "    const res = await fakeToggle(id);       // …real write (30% reject)",
        "    if (!res.ok) setError(`Couldn't update “${target.title}”.`);",
        "    else { setError(null); setTasks((t) => t.map((x) => (x.id === id ? { ...x, done: !x.done } : x))); }",
        "    // on settle the overlay is discarded; committed tasks render.",
        "  }",
        "",
        "  // ——— delete: NOT optimistic. Deletion is irreversible; a reverted",
        "  //     delete would resurrect a row the user believes is gone — more",
        "  //     confusing than a brief pending state. Hence pending + confirmation.",
        "  const [deletingId, setDeletingId] = useState<string | null>(null);",
        "  async function remove(id: string) {",
        "    setDeletingId(id);",
        "    await fakeDelete(id);",
        "    setTasks((t) => t.filter((x) => x.id !== id));",
        "    setDeletingId(null);",
        "  }",
        "",
        "  return (",
        "    <section>",
        "      <form action={addAction}>",
        "        <input name=\"title\" aria-label=\"New task\" required />",
        "        <AddButton />",
        "      </form>",
        "      {error && <p role=\"alert\">{error}</p>}",
        "      <ul>",
        "        {optTasks.map((t) => (",
        "          <li key={t.id}>",
        "            <label>",
        "              <input type=\"checkbox\" checked={t.done} onChange={() => toggle(t.id)} />",
        "              {t.title}",
        "            </label>",
        "            <button onClick={() => remove(t.id)} disabled={deletingId === t.id}>",
        "              {deletingId === t.id ? \"Removing…\" : \"Delete\"}",
        "            </button>",
        "          </li>",
        "        ))}",
        "      </ul>",
        "    </section>",
        "  );",
        "}",
        "",
        "// Defense: toggling is cheap and reversible, so instant feedback wins",
        "// and a rollback is harmless. Deleting is irreversible, so it gets",
        "// pending + confirmation rather than a lie that must be resurrected.",
        "// Adding creates truth on the server; there's nothing prior to be optimistic about.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "useActionState returns…",
        options: ["[state, dispatch, isPending] — the latest result, an invoker, and an in-flight flag", "Only a pending boolean", "A promise", "[data, error]"],
        answer: 0,
        explain: "It unifies result state, the dispatch to run the async action (usable as a form action), and isPending — replacing hand-rolled useState + setSubmitting.",
      },
      {
        q: "useFormStatus is only valid…",
        options: ["Inside a descendant of the <form> whose status it reads", "Anywhere in the tree", "Only in the form element itself", "Only with useActionState"],
        answer: 0,
        explain: "It reads the nearest form context, so it must be rendered within the form subtree — a feature that prevents cross-form status leaks.",
      },
      {
        q: "useOptimistic's rollback works because…",
        options: ["The optimistic value is a temporary overlay discarded when the pending action settles", "It manually undoes the state", "It retries the action", "It caches the previous value"],
        answer: 0,
        explain: "You never edit committed state optimistically; you add a preview keyed to the pending action that evaporates on settle. Reversion is structural, not a branch you write.",
      },
      {
        q: "Which action is appropriate for optimistic updates?",
        options: ["Marking a task done (cheap, reversible, visible failure)", "Processing a payment", "Deleting an account", "Reserving a limited seat"],
        answer: 0,
        explain: "Optimism suits cheap, reversible intents where a rare failure is harmless to reverse. Costly or irreversible actions need pending + confirmation.",
      },
      {
        q: "When an optimistic action fails, you should…",
        options: ["Surface a recoverable error — a silent revert reads as a glitch", "Revert silently and say nothing", "Retry automatically forever", "Show a success anyway"],
        answer: 0,
        explain: "The rollback happens structurally, but the user needs to know why the intent didn't stick. Pair useOptimistic with a toast or inline error on failure.",
      },
      {
        q: "The source of truth during an optimistic update is…",
        options: ["The committed (server) state — optimism is presentation only", "The optimistic value", "Whichever rendered last", "The pending flag"],
        answer: 0,
        explain: "Optimism is a UI overlay; the committed state is truth. Never persist, transmit, or compute from an optimistic value. This is the Phase 20/21 trust line.",
      },
    ],
    flashcards: [
      { front: "useActionState", back: "Binds an async action to state: returns [state, dispatch, isPending]. The action's return becomes the new state; isPending guards double-submits." },
      { front: "useFormStatus", back: "Inside a form descendant, reads { pending, data, method, action }. Lets a submit button show a spinner without lifting state or drilling props." },
      { front: "useOptimistic", back: "Renders a temporary overlay of intent during a pending action; discards it on settle, snapping to committed state. Free rollback, no manual undo." },
      { front: "Optimism boundary", back: "Be optimistic only for cheap, reversible intents (likes, reorders). Costly/irreversible actions (payments, deletes) get pending + confirmation." },
      { front: "Structural rollback", back: "The optimistic layer only exists while the action is pending; reversion isn't a branch you write, it's how the hook works. No window of a believed lie." },
      { front: "Silent revert", back: "A rollback with no explanation reads as a glitch. Always surface a recoverable error (toast/inline) when an optimistic action fails." },
      { front: "Optimism ≠ truth", back: "Optimistic values are presentation. The committed server state is the source of truth — never persist or compute from an overlay." },
      { front: "Actions & forms", back: "These hooks are built for form actions (Server Actions or client async). dispatch can be a form's action prop; Enter-to-submit and lifecycle come free." },
    ],
    recap: [
      "useActionState tracks an async action: result state + isPending, usable as a form action.",
      "useFormStatus gives form children a pending flag with zero plumbing.",
      "useOptimistic shows intent instantly and reverts structurally on failure.",
      "Optimism is for cheap, reversible intents; irreversible actions get confirmation.",
      "The overlay is presentation; the server's committed state is always the truth.",
    ],
    references: [
      { label: "react.dev — useActionState", url: "https://react.dev/reference/react/useActionState" },
      { label: "react.dev — useFormStatus", url: "https://react.dev/reference/react-dom/hooks/useFormStatus" },
      { label: "react.dev — useOptimistic", url: "https://react.dev/reference/react/useOptimistic" },
      { label: "react.dev — Server Actions", url: "https://react.dev/reference/rsc/server-actions" },
      { label: "react.dev — Optimistic UI patterns", url: "https://react.dev/reference/react/useOptimistic#optimistically-updating-forms" },
    ],
    nextBridge:
      "You now have the full current-stable hook toolbox. The professional move is knowing which tool a given value wants — and packaging recurring logic into reusable custom hooks. Next lesson: the Hook Decision Matrix and custom hook design, the compass for everything you've learned in Phase 5.",
  },
  {
    id: "p05-l7",
    phaseId: "p05",
    title: "Custom Hooks & the Hook Decision Matrix",
    level: "React Developer",
    minutes: 40,
    summary:
      "Every hook you've learned is a tool for a specific kind of value. This lesson gives you the compass: a decision matrix that assigns any value to useState, useReducer, context, or ref by asking four questions — and the craft of custom hooks, which extract reusable LOGIC (not markup) into use* functions with typed inputs and outputs. You'll build useDebouncedValue and useLocalStorageState, apply the Rules of Hooks as a lint-enforced contract, and learn the tell for when a 'hook' actually wants to be a component. This is the synthesis lesson: after it, Phase 5's Boss Battle grades you on choosing and defending these calls.",
    prerequisites: ["p05-l3 — useRef, useContext & useReducer", "p05-l4 — useMemo, useCallback & useId", "p05-l6 — action hooks"],
    objectives: [
      "Apply the four-question Hook Decision Matrix (constant? derivable? paint-relevant? survives renders?) to assign any value its home.",
      "Extract reusable logic into custom hooks — returning values and functions, never JSX.",
      "State the Rules of Hooks as a lint-enforced contract and explain why they exist (order stability).",
      "Write useDebouncedValue and useLocalStorageState with correct cleanup and generic typing.",
      "Recognize when shared logic wants a component (markup + behavior) rather than a hook (logic only).",
      "Defend a hook choice in writing — the skill BB-1 grades.",
    ],
    simple:
      "You now own a toolbox: state for things that change and render, reducers for state with many related transitions, context for rare facts many descendants need, refs for mutable values that must survive renders without triggering them, memos for expensive derivations. The beginner's problem isn't the tools — it's staring at a value and not knowing which drawer it belongs in. The Hook Decision Matrix is a four-question sorting hat: Is it constant? Can it be derived? Does the UI paint from it? Must it survive renders? Answer in order and exactly one drawer opens. And when you find yourself writing the same useState+effect+cleanup trio in three components, you stop copying — you name it, put use in front, and you've written a custom hook.",
    why:
      "Custom hooks are the unit of reuse for React LOGIC — the difference between a codebase where 'debounced search' exists once, tested and correct, and one where it exists seven times with seven subtly different bugs. They're also the shape every serious library hands you (useQuery, useController, useSWR). And the decision matrix is the single most-rewarded skill in this phase: code review, interviews, and the Boss Battle all reduce to 'why did you put THIS here?' Answer it by reflex and you write React that's debuggable by construction; guess and you produce the prop-drilling / god-context / state-sprawl messes Phase 6 exists to untangle.",
    mentalModel: {
      title: "The sorting hat for values",
      body: "Picture four drawers labeled by lifetime and visibility. The CONST drawer (module scope): values that never change and no render depends on — config, formatters. The DERIVED drawer (compute in render): anything computable from what you already have — totals, filters; opening this drawer means NOT storing. The STATE drawer (useState/reducer): values that vary over time and the paint depends on — the visible world. The REF drawer (useRef): mutable scratch that must outlive renders but that no paint reads — timer ids, previous values, DOM handles. Run a value through the questions top-down and the FIRST drawer that fits wins. A custom hook is just a labeled box that packages one value's whole drawer ceremony — its state, its effects, its cleanup — so you never open the drawer by hand again.",
    },
    sections: [
      {
        heading: "The Hook Decision Matrix — four questions, one home",
        body: [
          "Run every value through these, top to bottom, and stop at the first 'yes'. (1) Is it constant, shared, and render-independent? → module scope (const outside the component) — no hook at all. (2) Can it be computed from existing state/props? → derive it during render (plain expression or useMemo if expensive) — do NOT store it. (3) Does the UI paint differently based on it, and does it change over time? → state: useState for an independent value, useReducer when several fields transition together under shared rules. (4) Must it persist across renders but the paint never reads it? → useRef (timer ids, previous values, element handles). Context sits alongside (not in the sequence): it's a delivery mechanism for state that lives higher up — use it when a value is broadly needed and changes rarely (theme, auth, locale), never as a state-management bus for churn.",
          "The matrix earns its keep by refusing whole bug classes before they start: storing a derivable value (drift bugs), putting paint-irrelevant churn in state (needless re-renders), using a ref for something the UI should show (a paint that never happens), and dumping everything in context (tree-wide re-renders). When you can name which question a value failed, you can name the bug it would have caused.",
        ],
        code: [
          {
            file: "matrix.tsx — the four questions applied",
            lang: "tsx",
            code: [
              "// (1) constant, render-independent → module scope, no hook:",
              "const CURRENCY = new Intl.NumberFormat(\"en-US\", { style: \"currency\" });",
              "",
              "function Cart({ items }: { items: Item[] }) {",
              "  // (2) derivable → compute, don't store:",
              "  const total = items.reduce((s, i) => s + i.price * i.qty, 0);",
              "",
              "  // (3) paint-relevant + varies → state:",
              "  const [coupon, setCoupon] = useState(\"\");",
              "",
              "  // (4) persists, paint never reads → ref:",
              "  const inputRef = useRef<HTMLInputElement>(null);",
              "",
              "  return (",
              "    <div>",
              "      <input ref={inputRef} value={coupon} onChange={(e) => setCoupon(e.target.value)} />",
              "      <p>{CURRENCY.format(total)}</p>",
              "    </div>",
              "  );",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Custom hooks — extract logic, not markup",
        body: [
          "A custom hook is a function whose name starts with use and that calls other hooks. It returns values and/or functions — never JSX. That's the whole rule, and it's the whole power: you package a piece of stateful LOGIC (state + effects + cleanup + derived values) under a name, and any component can adopt it without inheriting markup. The three tests before extracting: is the logic actually repeated (or about to be)? Does it have a coherent, nameable purpose? Can its inputs and outputs be typed cleanly? Two components sharing 'a debounced value' is a hook; two components sharing 'a header' is a component.",
          "Naming and API design matter as much as the logic. useX reads as a verb or a noun phrase describing what you GET (useDebouncedValue, useLocalStorageState, useMediaQuery), not what you DO internally. Return the minimal surface: the value plus the setters/handlers consumers need, nothing more. And type it like any exported function — generics for reusable shapes, a tight return tuple/object — because a custom hook IS a function boundary other people (future you included) will call.",
        ],
        code: [
          {
            file: "hooks.tsx — two real extractions",
            lang: "tsx",
            code: [
              "// logic, not markup: state + effect + cleanup under a name",
              "function useDebouncedValue<T>(value: T, ms = 300): T {",
              "  const [debounced, setDebounced] = useState(value);",
              "  useEffect(() => {",
              "    const id = setTimeout(() => setDebounced(value), ms);",
              "    return () => clearTimeout(id);     // cleanup travels with the logic",
              "  }, [value, ms]);",
              "  return debounced;",
              "}",
              "",
              "function useLocalStorageState<T>(key: string, initial: T) {",
              "  const [stored, setStored] = useState<T>(() => {",
              "    try {",
              "      const raw = localStorage.getItem(key);",
              "      return raw != null ? (JSON.parse(raw) as T) : initial;",
              "    } catch { return initial; }",
              "  });",
              "  useEffect(() => {",
              "    try { localStorage.setItem(key, JSON.stringify(stored)); } catch { /* private mode */ }",
              "  }, [key, stored]);",
              "  return [stored, setStored] as const;",
              "}",
              "",
              "// adoption is one line, zero markup inherited:",
              "function Search() {",
              "  const [query, setQuery] = useState(\"\");",
              "  const debounced = useDebouncedValue(query);   // reusable, tested once",
              "  return <input value={query} onChange={(e) => setQuery(e.target.value)} />;",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The Rules of Hooks — a contract, enforced by lint",
        body: [
          "Two rules, both enforced by eslint-plugin-react-hooks. (1) Only call hooks at the top level of a component or custom hook — never inside conditions, loops, or after early returns. (2) Only call hooks from React functions (components or use* functions). The reason isn't style: React identifies each hook call by its ORDER in the sequence of calls during a render. Call hooks conditionally and the order shifts between renders, so React hands state to the wrong hook — silent corruption. The linter exists because this class of bug is invisible until it isn't.",
          "This is why custom hooks must be named use*: the lint rule uses the name to know which functions are allowed to call hooks and to check their call sites. It's also why 'I'll just skip the hook when I don't need it' is never the fix — instead, call the hook unconditionally and make its BODY handle the no-op case (return early inside an effect, branch on a flag). The rule is the price of React's model; the linter makes the price zero.",
        ],
        code: [
          {
            file: "rules.tsx — why order is sacred",
            lang: "tsx",
            code: [
              "// ✗ conditional hook — order shifts between renders, state corrupts:",
              "function Bad({ enabled }: { enabled: boolean }) {",
              "  if (!enabled) return null;",
              "  const [x] = useState(0);   // sometimes hook #1, sometimes hook #0!",
              "  return <p>{x}</p>;",
              "}",
              "",
              "// ✓ unconditional call, conditional BEHAVIOR:",
              "function Good({ enabled }: { enabled: boolean }) {",
              "  const [x] = useState(0);            // always hook #1",
              "  useEffect(() => {",
              "    if (!enabled) return;             // no-op inside, order stable",
              "    // …real work",
              "  }, [enabled]);",
              "  return enabled ? <p>{x}</p> : null;",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Hook vs component — and testing what you extract",
        body: [
          "The tell: if the shared thing has MARKUP — elements, layout, structure people render — it's a component, even if it has logic inside. If it's pure behavior and state with no elements of its own, it's a hook. A 'useTableSort' that returns sorted rows + a sort handler is a hook; a 'SortableTable' that renders <table> is a component (which may USE the hook). When a hook starts wanting to return JSX, that's the signal to split: keep the logic hook, add a thin component that consumes it. This split is what keeps logic reusable across completely different UIs.",
          "Testing follows the split. Custom hooks are tested with a renderer (renderHook from Testing Library) that mounts them in isolation and lets you act() on their returns — you assert on values and state transitions, no DOM markup required. Components get tested as components (Phase 31). Because a hook's surface is just inputs/outputs, it's often the most testable unit in a React codebase — another quiet argument for extracting logic early.",
        ],
      },
    ],
    mistake: {
      title: "The god hook that does everything",
      wrong: [
        "// one hook to rule them all — 11 state values, 4 effects, 200 lines:",
        "function useDashboard() {",
        "  const [user] = useState(...);        // auth",
        "  const [tasks, setTasks] = useState(...); // data",
        "  const [theme, setTheme] = useState(...); // ui",
        "  const [toasts, setToasts] = useState(...); // ui",
        "  useEffect(/* fetch tasks */);",
        "  useEffect(/* theme side-effect */);",
        "  useEffect(/* toast timers */);",
        "  return { user, tasks, setTasks, theme, setTheme, toasts, /* … */ };",
        "}",
        "// every consumer re-renders on ANY change; untestable in isolation;",
        "// every team edits this one file and merges collide forever.",
      ].join("\n"),
      right: [
        "// one hook per coherent purpose — each tiny, testable, composable:",
        "function useUser() { /* auth only */ }",
        "function useTasks() { /* data fetch + mutations */ }",
        "function useTheme() { /* ui preference */ }",
        "function useToasts() { /* ephemeral messages */ }",
        "",
        "function Dashboard() {",
        "  const user = useUser();",
        "  const tasks = useTasks();       // each hook is a small, named box",
        "  // composition happens in the COMPONENT, not inside one mega-hook",
        "}",
      ].join("\n"),
      explain:
        "A hook should package ONE coherent piece of logic — one purpose, one lifetime, one set of deps. A 200-line hook bundling auth, data, and UI state couples unrelated lifecycles: any change re-renders every consumer, nothing is testable alone, and every teammate edits the same file. Compose small hooks in components; that's the layer designed for combination. The naming test helps: if you can't summarize the hook in one verb phrase, it's several hooks in a trench coat.",
    },
    tryIt: [
      "Take three values from your Todo app and run each through the four-question matrix aloud, naming the drawer and the bug the wrong drawer would cause.",
      "Extract the P02-L2 debounce into useDebouncedValue with a generic type. Use it in two different components and confirm zero markup was shared.",
      "Break the Rules of Hooks on purpose: call useState after an early return, toggle the flag, and observe the corruption or lint error.",
      "Refactor a multi-responsibility hook (or write one) into three single-purpose hooks and note which consumers got simpler.",
      "Write useLocalStorageState and verify it survives a full page reload but not a cleared storage key.",
    ],
    challenge: {
      prompt:
        "Build and defend three custom hooks, each typed and each with its drawer-assignment explained in a comment: (1) useMediaQuery(query: string): boolean — tracks a CSS media query live using matchMedia + a listener (cleanup required); (2) usePrevious<T>(value: T): T | undefined — returns the value from the PREVIOUS render; (3) useToggle(initial = false): [boolean, () => void]. Then a short component that uses all three (e.g., a panel that shows 'desktop/mobile' via the query, remembers its last width label via usePrevious, and collapses via useToggle). Constraints: no hook returns JSX; each hook ≤ ~15 lines; each carries a one-line comment stating WHICH matrix question put its state/ref where it is. Finish with a 3-sentence defense of usePrevious's use of a ref rather than state.",
      hints: [
        "useMediaQuery: useState for the boolean (paint-relevant → state drawer), useEffect that calls matchMedia(query), subscribes to 'change', and unsubscribes in cleanup. Initialize from mql.matches.",
        "usePrevious: the previous value must survive renders but the paint never reads it AS current → ref drawer. Assign ref.current = value in an effect that runs AFTER render returns the old ref.current.",
        "useToggle is useState + useCallback(()=>setV(v=>!v)). The defense: storing 'previous' in state would trigger an extra render on every change (state changes re-render); a ref holds it without re-rendering because nothing paints from it.",
      ],
      solution: [
        "import { useCallback, useEffect, useRef, useState } from \"react\";",
        "",
        "// state drawer: paint reads this (Q3 — varies & painted).",
        "function useMediaQuery(query: string): boolean {",
        "  const [matches, setMatches] = useState(",
        "    () => typeof window !== \"undefined\" && window.matchMedia(query).matches,",
        "  );",
        "  useEffect(() => {",
        "    const mql = window.matchMedia(query);",
        "    const onChange = () => setMatches(mql.matches);",
        "    mql.addEventListener(\"change\", onChange);",
        "    setMatches(mql.matches);            // sync if it changed during render",
        "    return () => mql.removeEventListener(\"change\", onChange); // cleanup travels",
        "  }, [query]);",
        "  return matches;",
        "}",
        "",
        "// ref drawer: survives renders, paint never reads it as current (Q4).",
        "function usePrevious<T>(value: T): T | undefined {",
        "  const ref = useRef<T | undefined>(undefined);",
        "  useEffect(() => {",
        "    ref.current = value;                // runs AFTER render returns old value",
        "  }, [value]);",
        "  return ref.current;",
        "}",
        "",
        "// state drawer: paint reads the boolean (Q3).",
        "function useToggle(initial = false): [boolean, () => void] {",
        "  const [on, setOn] = useState(initial);",
        "  const toggle = useCallback(() => setOn((v) => !v), []);",
        "  return [on, toggle];",
        "}",
        "",
        "function ResponsivePanel() {",
        "  const isDesktop = useMediaQuery(\"(min-width: 860px)\");",
        "  const label = isDesktop ? \"desktop\" : \"mobile\";",
        "  const prevLabel = usePrevious(label);",
        "  const [collapsed, toggle] = useToggle();",
        "",
        "  return (",
        "    <section>",
        "      <p>Now: {label}{prevLabel && prevLabel !== label ? ` (was ${prevLabel})` : \"\"}</p>",
        "      <button onClick={toggle}>{collapsed ? \"Expand\" : \"Collapse\"}</button>",
        "      {!collapsed && <div>panel body</div>}",
        "    </section>",
        "  );",
        "}",
        "",
        "// Defense of usePrevious using a ref: the previous value is scratch",
        "// metadata no paint reads as current, so it belongs in the ref drawer.",
        "// Storing it in state would cause an extra render on every change —",
        "// precisely the re-render a ref exists to avoid.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A value that can be computed from existing state/props should be…",
        options: ["Derived during render, not stored", "Stored in useState", "Stored in a ref", "Put in context"],
        answer: 0,
        explain: "Storing a derivable value creates a synchronization job and drift bugs. Compute it in render (useMemo if expensive) so it's true by construction.",
      },
      {
        q: "A timer id that must persist across renders but is never painted belongs in…",
        options: ["useRef", "useState", "context", "module scope"],
        answer: 0,
        explain: "It survives renders (rules out plain variables) but the paint never reads it (rules out state, which would trigger re-renders). That's the ref drawer.",
      },
      {
        q: "The Rules of Hooks exist because React identifies hooks by…",
        options: ["Their call order during a render", "Their names", "Their arguments", "The component they're in"],
        answer: 0,
        explain: "Conditional/looped hook calls shift the order between renders, so React would hand state to the wrong hook. The lint rule makes this contract free to keep.",
      },
      {
        q: "A custom hook may return…",
        options: ["Values and functions, never JSX", "JSX elements", "Only a single value", "Only state setters"],
        answer: 0,
        explain: "Hooks package logic, not markup. Returning JSX means it wants to be a component (which can then use the hook).",
      },
      {
        q: "Shared logic that includes markup should be a…",
        options: ["Component (possibly consuming a logic hook)", "Custom hook", "Context provider", "HOC"],
        answer: 0,
        explain: "The tell is markup: elements people render belong in components. Keep pure logic in a hook and let a thin component consume it.",
      },
      {
        q: "Context is appropriate when the value is…",
        options: ["Broadly needed by descendants and changes rarely (theme, auth, locale)", "High-churn per-keystroke data", "Used by one child", "Derivable from props"],
        answer: 0,
        explain: "Context is a delivery mechanism for rare, widely-needed facts. High-churn data in context re-renders the whole subtree; that's state's job, locally.",
      },
    ],
    flashcards: [
      { front: "Hook Decision Matrix", back: "Constant? → module scope. Derivable? → compute in render. Paint-relevant + varies? → state. Persists, never painted? → ref. First 'yes' wins." },
      { front: "Custom hook", back: "A use*-named function calling other hooks, returning values/functions — never JSX. Packages reusable logic (state+effects+cleanup), not markup." },
      { front: "Rules of Hooks", back: "Top-level only; React functions only. React tracks hooks by call order — conditional calls corrupt state. Enforced by eslint-plugin-react-hooks." },
      { front: "Hook vs component", back: "Markup people render → component. Pure behavior/state → hook. A hook that wants to return JSX should split into hook + thin component." },
      { front: "usePrevious pattern", back: "Ref drawer: store the value in an effect after render, return the old ref. Survives renders without triggering them (paint never reads it as current)." },
      { front: "God hook smell", back: "One hook bundling unrelated state/effects couples lifecycles: everything re-renders together, nothing tests alone. One hook per coherent purpose." },
      { front: "Context discipline", back: "Delivery mechanism for rare, broadly-needed values (theme/auth/locale). Never a state-management bus for churn — that re-renders the tree." },
      { front: "Naming custom hooks", back: "use + what you GET (useDebouncedValue, useMediaQuery). Type the inputs/outputs like any exported function; return the minimal surface." },
    ],
    recap: [
      "Sort every value through the four questions — first 'yes' names its drawer.",
      "Custom hooks extract logic (state+effects+cleanup), never markup.",
      "The Rules of Hooks protect call-order; the linter makes compliance free.",
      "Markup → component; behavior → hook; compose small hooks in components.",
      "This matrix is what BB-1 grades — next lesson, you're tested on it.",
    ],
    references: [
      { label: "react.dev — Reusing Logic with Custom Hooks", url: "https://react.dev/learn/reusing-logic-with-custom-hooks" },
      { label: "react.dev — Rules of Hooks", url: "https://react.dev/reference/rules/rules-of-hooks" },
      { label: "react.dev — useMediaQuery-style patterns", url: "https://react.dev/learn/reusing-logic-with-custom-hooks#custom-hooks" },
      { label: "react.dev — useState vs useReducer", url: "https://react.dev/learn/extracting-state-logic-into-a-reducer" },
      { label: "npm — eslint-plugin-react-hooks", url: "https://www.npmjs.com/package/eslint-plugin-react-hooks" },
    ],
    nextBridge:
      "You have the compass and the craft. Now the gauntlet: five timed, broken components — stale closures, effect loops, races, memoization misuse, and a state-modeling mess. Diagnose each by naming the render or closure involved, fix with the correct pattern, and defend your choice. This is BB-1, the Hooks Gauntlet.",
  },
  {
    id: "p05-l8",
    phaseId: "p05",
    title: "Boss Battle: Hooks Gauntlet (BB-1)",
    level: "React Developer",
    minutes: 90,
    summary:
      "The first cumulative Boss Battle. Five timed scenarios, each a real component with a real bug from this phase: a stale closure in a counter, an effect that loops, a race in a search, a memoization that does nothing, and state sprawl that should be a reducer. For each, you must (1) diagnose by naming the render or closure involved, (2) fix with the correct hook pattern — not a workaround — and (3) defend the fix in writing against review criteria. This lesson presents the scenarios and the grading rubric; the quiz below is the timed gauntlet itself. Pass criteria and a remediation path are stated up front, because a boss battle you can't learn from isn't a test, it's a wall.",
    prerequisites: ["p05-l1 through p05-l7 — all of Phase 5"],
    objectives: [
      "Diagnose a stale-closure bug by naming the snapshot and the function that captured it.",
      "Identify and cure an effect infinite loop by finding the self-feeding dependency.",
      "Fix a stale-async race with abort + guard, and explain why both are needed.",
      "Detect a useless memo by tracing a fresh-identity prop, and remove or repair it deliberately.",
      "Refactor state sprawl into a reducer and defend the transition rule set.",
      "Write a short defense for each fix — the review criterion that separates fixing from understanding.",
    ],
    simple:
      "A boss battle is five broken things and a timer. Each bug is one you've already met — you just didn't know its name yet. The counter that undercounts is a stale snapshot. The list that refetches forever is an effect feeding its own dependency. The search that shows yesterday's results is a race with no guard. The memo that never skips is a fresh-identity prop. The form with nine useStates that step on each other is a reducer wearing a disguise. Your job per scenario: say WHICH render or closure is lying, apply the one correct pattern, and write one honest sentence about why. Beat all five and you've proven the whole phase — not by reciting it, by wielding it.",
    why:
      "Knowledge you can recite and knowledge you can apply under a timer are different things, and production only grades the second. These five bugs are the exact failure modes that dominate React code review and on-call: they're cheap to make, expensive to find, and each has a canonical fix the ecosystem has converged on. By forcing you to diagnose → fix → defend, the gauntlet builds the reflex that Phase 6's advanced patterns and Phase 5's custom-hook craft depend on. And the stated pass criteria + remediation path keep it honest: this is a checkpoint that tells you precisely what to re-study if you're not ready, not a gate that just says no.",
    mentalModel: {
      title: "Five crime scenes, one detective",
      body: "Treat each scenario as a crime scene with a single perpetrator from a known list of suspects: the frozen snapshot, the self-feeding dependency, the unguarded async, the fresh identity, the split-brain state. Walk the scene (read the code), name the suspect (diagnose), apply the standard procedure (the canonical fix), and write your report (the defense). The detective who sprays guesses gets nowhere; the one who matches evidence to a known suspect closes the case in minutes. This is the nine-step debugging method (P00-L4) wearing a React costume — and after five scenes, pattern-matching becomes reflex.",
    },
    sections: [
      {
        heading: "The rules of engagement",
        body: [
          "Format: five scenarios, presented one at a time below (and in the quiz). For each you get a broken component and must produce three things: a DIAGNOSIS (name the render/closure/dependency involved), a FIX (the correct hook pattern — not a hack), and a DEFENSE (one or two sentences justifying the pattern against alternatives). Time yourself: aim for ~10 minutes per scenario. The quiz at the bottom checks diagnosis + fix; the written defense is the part you owe yourself (or your reviewer) in a real setting.",
          "Pass criteria, stated up front: correctly diagnose and fix at least 4 of 5, and your defenses must name the mechanism (snapshot, dependency identity, abort/guard, referential equality, transition rule) — not just the symptom. Remediation: for each miss, re-read the single lesson linked in the quiz explanation and re-attempt only that scenario. The battle is passed when all five close; there is no partial credit in production, but there is always another attempt here.",
        ],
      },
      {
        heading: "Scenario map — the five suspects",
        body: [
          "You'll face, in order: (1) a click handler that captures a stale snapshot — the counter that undercounts; (2) an effect whose dependency it also sets — the refetch loop; (3) an async search with no cancellation — the stale-result race; (4) a memo child that re-renders every time — the fresh-identity prop; (5) a form juggling many coupled useStates — the reducer in disguise. Each maps to exactly one canonical lesson: P05-L1, P05-L2, P05-L2, P05-L4, P05-L3 respectively. Knowing the map doesn't give you the answers — it gives you the vocabulary to explain them, which is the point.",
          "A word on the 'not a workaround' rule: adding a setTimeout, sprinkling eslint-disable, or bumping a dependency to force a re-run may silence a symptom while leaving the disease. The gauntlet's review criteria reject those, exactly as a code review would. The fix must remove the bug by construction — so it cannot come back — not by coincidence.",
        ],
      },
    ],
    mistake: {
      title: "Silencing the symptom instead of removing the disease",
      wrong: [
        "// the loop: effect feeds its own dep. The 'fix' that isn't:",
        "useEffect(() => {",
        "  setItems(fetchAndSort(items));",
        "  // eslint-disable-next-line react-hooks/exhaustive-deps",
        "}, []);   // ← disabled the lint, kept the stale closure,",
        "          //   AND now the effect never resyncs on real changes.",
        "// You didn't fix the loop; you buried it alive.",
      ].join("\n"),
      right: [
        "// the disease was derivation posing as synchronization:",
        "const items = useMemo(() => fetchAndSort(raw), [raw]);",
        "// no effect, no loop, no disabled rule — the value is computed",
        "// in render, always fresh, and resyncs whenever `raw` changes.",
        "// The fix removes the bug by construction, not by coincidence.",
      ].join("\n"),
      explain:
        "eslint-disable on exhaustive-deps is the React equivalent of turning off a smoke alarm: the fire is still there, now unmonitored. Most 'fixes' that require silencing a lint rule are misdiagnoses — the rule was correctly pointing at a real problem (a lie in the dependency list or an effect doing render-phase work). The review criterion 'no workarounds' exists to make you find the structural fix, because that's the only kind that survives the next person who edits the file.",
    },
    tryIt: [
      "Before the quiz, write your own version of each of the five bugs from scratch (from memory), then fix each. If you can create the bug, you can recognize it in the wild.",
      "Time yourself on the quiz with a 10-minute-per-scenario budget. Note which scenario blew the budget — that's your weakest lesson.",
      "For one scenario, write the defense out loud as if to a reviewer. If you can't name the mechanism, re-read the linked lesson before re-attempting.",
      "Take a real component from your Todo app and hunt for any of the five suspects. Most real code has at least one.",
      "After passing, refactor one of your scenario fixes into a custom hook (P05-L7) and note how the logic became reusable.",
    ],
    challenge: {
      prompt:
        "The full gauntlet, untimed practice round: recreate all five broken components from the quiz in a scratch project, reproduce each bug's symptom (watch the undercount, the loop in your console/network tab, the stale search result, the memo that never skips in the Profiler, the form that desyncs), then apply your fixes and confirm each symptom is gone BY CONSTRUCTION. Deliver: for each scenario, a before/after code pair plus a 2-sentence defense naming the mechanism and the alternative you rejected. This is the artifact a reviewer would accept.",
      hints: [
        "Reproducing matters: for the loop, watch the network tab fill; for the race, throttle the network and search fast; for the memo, use the Profiler to SEE it re-render. You can't confirm a fix you can't observe.",
        "Each defense should follow: 'The bug was <mechanism>. I fixed it with <pattern> instead of <rejected alternative> because <reason it's structural>.'",
        "The reducer refactor (scenario 5) should end with a typed Action union (P03-L2) and a single dispatch — the transition rules become data you can read and test.",
      ],
      solution: [
        "// Scenario 1 — stale closure → functional update (P05-L1):",
        "// before: onClick={() => setCount(count + 1)} ×3  → +1 not +3",
        "// after:  onClick={() => { setCount(c=>c+1); setCount(c=>c+1); setCount(c=>c+1); }}",
        "// defense: each updater reads the queued value, not the frozen snapshot.",
        "//          Rejected 'call setCount(count+3)' — couples the handler to the current render.",
        "",
        "// Scenario 2 — self-feeding effect → derive in render (P05-L2):",
        "// before: useEffect(() => setSorted(items.toSorted()), [items, sorted]) → loop",
        "// after:  const sorted = useMemo(() => items.toSorted(), [items]);",
        "// defense: sorting is pure derivation, not external sync — render-phase compute",
        "//          removes the loop by construction. Rejected eslint-disable (buries it).",
        "",
        "// Scenario 3 — search race → abort + guard (P05-L2):",
        "// before: fetch(query).then(setResults) with no cleanup → stale wins",
        "// after:  AbortController in cleanup + `if (stale) return` guard",
        "// defense: abort cancels the in-flight request on query change/unmount; the guard",
        "//          covers the window between resolve and state set. Rejected debounce-only (still races).",
        "",
        "// Scenario 4 — useless memo → stabilize the prop (P05-L4):",
        "// before: <Row onSelect={() => ...} /> inside memo(Row) → fresh fn each render",
        "// after:  const onSelect = useCallback((id) => ..., [dep]);  OR drop memo",
        "// defense: memo compares props with Object.is; an inline arrow is a new identity",
        "//          every render, so memo never skips. Rejected memo everywhere (cost w/o benefit).",
        "",
        "// Scenario 5 — state sprawl → useReducer (P05-L3):",
        "// before: 9 coupled useStates stepping on each other across handlers",
        "// after:  const [form, dispatch] = useReducer(reducer, initial); typed Action union",
        "// defense: coupled fields transitioning together are one state machine; the reducer",
        "//          makes transitions explicit and testable. Rejected one big useState object (no rules).",
        "",
        "// Pass = all five close, each defense naming its mechanism. That's the whole battle.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "Scenario 1: A handler calls setCount(count + 1) three times on click; count rises by 1, not 3. The bug is…",
        options: ["All three updates read the same frozen snapshot — fix with functional updates setCount(c => c + 1)", "count is not a number", "The handler runs once", "Batching is disabled"],
        answer: 0,
        explain: "Diagnosis: the handler captures one snapshot; every count + 1 is identical, so all tickets say 'set to N+1'. Fix: functional updates read the queued value. Defense: it's structural — each updater composes regardless of batching.",
      },
      {
        q: "Scenario 2: useEffect(() => setSorted(items.toSorted()), [items, sorted]) refetches/re-sorts forever. The fix is…",
        options: ["Delete the effect — sorting is pure derivation; compute it in render (useMemo over items)", "Add items.length to deps", "Remove sorted from deps and disable the lint", "Wrap setSorted in startTransition"],
        answer: 0,
        explain: "Diagnosis: the effect feeds its own dependency (a fresh array identity each run). Fix: derive in render — no effect, no loop, always fresh. Removing the dep + disabling lint buries the disease; the review rejects it.",
      },
      {
        q: "Scenario 3: Fast searches show the result of an EARLIER query because its slow response lands last. The correct fix is…",
        options: ["AbortController in the effect cleanup + a stale-guard before setState", "Increase the fetch timeout", "Add the query to a global cache", "Call setState twice"],
        answer: 0,
        explain: "Diagnosis: responses complete in network order, not request order; nothing correlates them. Fix: abort on query change/unmount + guard post-await setState. Both close the window; either alone leaves an edge.",
      },
      {
        q: "Scenario 4: memo(Row) still re-renders on every parent render. The most likely cause is…",
        options: ["A prop is a fresh identity each render (inline arrow fn / object) — memo compares with Object.is", "memo is broken in React 19", "The parent uses useState", "The child is too small"],
        answer: 0,
        explain: "Diagnosis: memo skips only when all props are referentially equal; an inline handler is a new identity every render. Fix: useCallback the handler (or drop memo if the render is cheap). 'memo everywhere' adds cost without benefit.",
      },
      {
        q: "Scenario 5: A form with 9 coupled useStates keeps desyncing across handlers. The right refactor is…",
        options: ["One useReducer with a typed Action union making the coupled transitions explicit", "One big useState object with no rules", "More useStates to isolate fields", "Move everything to context"],
        answer: 0,
        explain: "Diagnosis: coupled fields transitioning together are one state machine split across nine variables. Fix: a reducer centralizes the transition rules as data. A big object without rules just hides the sprawl; context doesn't change the coupling.",
      },
    ],
    flashcards: [
      { front: "BB-1 pass criteria", back: "Diagnose + fix 4/5 scenarios, and each defense names the mechanism (snapshot, dep identity, abort/guard, referential equality, transition rule) — not just the symptom." },
      { front: "Stale-closure suspect", back: "A function captured a frozen snapshot; reads are stale. Fix: functional updates (state) or correct effect deps. Lesson: P05-L1." },
      { front: "Self-feeding effect suspect", back: "The effect sets state it also depends on, retriggering itself. Fix: derive in render — it was never external sync. Lesson: P05-L2." },
      { front: "Stale-async race suspect", back: "Responses arrive out of request order; a slow one overwrites fresh state. Fix: AbortController in cleanup + stale-guard. Lesson: P05-L2." },
      { front: "Useless-memo suspect", back: "memo never skips because a prop is a fresh identity each render (Object.is). Fix: useCallback the handler, or drop memo. Lesson: P05-L4." },
      { front: "State-sprawl suspect", back: "Coupled useStates step on each other. Fix: one useReducer with a typed Action union — transitions become explicit, testable data. Lesson: P05-L3." },
      { front: "No-workaround rule", back: "Fixes must remove the bug by construction. eslint-disable, setTimeout, and forced re-runs silence symptoms; the review (and production) reject them." },
      { front: "Diagnose → fix → defend", back: "The gauntlet's three-part answer: name the render/closure involved, apply the canonical pattern, justify it against alternatives. Reciting isn't wielding." },
    ],
    recap: [
      "Five suspects: frozen snapshot, self-feeding dep, unguarded async, fresh identity, split-brain state.",
      "Each has one canonical fix that removes the bug by construction.",
      "Silencing a lint rule is a misdiagnosis, not a fix.",
      "Defenses must name the mechanism — that's the difference between fixing and understanding.",
      "Phase 5 complete. Phase 6 builds on this: component APIs, error boundaries, Suspense, and profiling.",
    ],
    references: [
      { label: "react.dev — Rules of Hooks", url: "https://react.dev/reference/rules/rules-of-hooks" },
      { label: "react.dev — You Might Not Need an Effect", url: "https://react.dev/learn/you-might-not-need-an-effect" },
      { label: "react.dev — Queueing a Series of State Updates", url: "https://react.dev/learn/queueing-a-series-of-state-updates" },
      { label: "react.dev — Extracting State Logic into a Reducer", url: "https://react.dev/learn/extracting-state-logic-into-a-reducer" },
      { label: "react.dev — Removing Effect Dependencies", url: "https://react.dev/learn/removing-effect-dependencies" },
    ],
    nextBridge:
      "The gauntlet is behind you — you can now diagnose and fix the five bugs that dominate React code review. Phase 6 takes this reflex to architecture: component APIs and state ownership, error boundaries and Suspense, optimistic UI and the state-classification map, and performance profiling. The hooks are mastered; now the engineering.",
  },
];

export const LESSON_CONTENT_P5C: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P5C.map((l) => [l.id, l]),
);
