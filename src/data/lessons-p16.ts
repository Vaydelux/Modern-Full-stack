import type { LessonContent } from "./types";

/**
 * Phase 16 Supabase Platform + Database Integration (L1–L3).
 */
export const LESSONS_P16: LessonContent[] = [
  {
    id: "p16-l1",
    phaseId: "p16",
    title: "Supabase Platform Tour & Key Categories",
    level: "Backend Developer",
    minutes: 30,
    summary:
      "Understand Supabase as an industrial, managed PostgreSQL platform. Distinguish between the anon public key and privileged service_role key, explore the SQL Editor, logs, and local development CLI.",
    prerequisites: [
      "p13-l1 — Why PostgreSQL: Schemas, Types & ACID",
      "p14-l1 — Install Exactly 7.9.15: Schema, Models & Relations",
    ],
    objectives: [
      "Navigate the Supabase platform architecture and understand the underlying PostgreSQL engine.",
      "Differentiate between the `anon` public key and the privileged `service_role` secret key.",
      "Inspect Postgres logs, query performance telemetry, and database backups in the Supabase Dashboard.",
      "Initialize and run local development environments using the official Supabase CLI (`supabase init`, `supabase start`).",
    ],
    simple:
      "Supabase is not a proprietary closed database; it is fully featured, production-grade PostgreSQL wrapped with managed cloud infrastructure, connection poolers, Auth, and Storage. When building backend APIs in NestJS or Next.js, our server acts as a trusted client connecting directly to Postgres via standard database connections. The frontend uses the `anon` key, while backend services hold the master `service_role` key or connect directly via Prisma.",
    why:
      "Accidentally exposing the `service_role` key in frontend client bundles completely bypasses Row-Level Security and exposes every record in your database. Knowing how Supabase partitions its keys, connection infrastructure, and observability tooling is critical to keeping production applications secure.",
    mentalModel: {
      title: "The Bank Vault & The Teller Window",
      body: "Think of Supabase like a bank. The `anon` key is the public lobby door: anyone can walk in, but they can only access what the security guards (Postgres Row-Level Security policies) permit. The `service_role` key and direct PostgreSQL connection credentials are the master bank manager vault keys: they bypass all lobby checks and have unrestricted access to every safe deposit box. Never leave the master key in the lobby.",
    },
    sections: [
      {
        heading: "Supabase Architecture: The Full-Stack PostgreSQL Platform",
        body: [
          "Supabase bundles battle-tested open-source components around a central PostgreSQL 15/16 database instance.",
          "Understanding these components prevents architectural confusion when configuring backend ORMs like Prisma 7.9.15.",
        ],
        code: [
          {
            file: "SUPABASE_STACK.md",
            lang: "text",
            code: [
              "┌────────────────────────────────────────────────────────────────────────┐",
              "│                        SUPABASE CLOUD PLATFORM                         │",
              "├────────────────────────────────────────────────────────────────────────┤",
              "│  [Supabase Studio / Dashboard]   → Visual management, SQL editor, logs │",
              "│  [Kong API Gateway]              → Routes HTTP REST & Realtime traffic │",
              "│  [GoTrue / Supabase Auth]        → JWT generation & identity tables    │",
              "│  [PostgREST]                     → Auto-generated REST from Postgres   │",
              "│  [Realtime Server (Elixir)]      → WebSocket postgres WAL streaming    │",
              "│  [Supavisor / PgBouncer]         → High-throughput Connection Pooler   │",
              "│  --------------------------------------------------------------------- │",
              "│  [PostgreSQL Core Database]      → Your tables, schemas, indexes, ACID │",
              "└────────────────────────────────────────────────────────────────────────┘",
            ].join("\n"),
            caption: "The core components of Supabase: Kong, GoTrue, PostgREST, Supavisor, and native PostgreSQL.",
          },
        ],
      },
      {
        heading: "The Security Boundary: anon Key vs service_role Key",
        body: [
          "Supabase generates two primary API keys for every project. Knowing where each key belongs is a strict security mandate.",
        ],
        code: [
          {
            file: ".env.example",
            lang: "text",
            code: [
              "# 🟢 SAFE FOR CLIENT (Browser / Mobile / React):",
              "# The 'anon' key identifies public requests. It MUST be restricted by Postgres RLS.",
              "NEXT_PUBLIC_SUPABASE_URL=\"https://xyzcompany.supabase.co\"",
              "NEXT_PUBLIC_SUPABASE_ANON_KEY=\"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...\"",
              "",
              "# 🔴 SERVER ONLY (NestJS Backend / Cloud Functions / Background Workers):",
              "# The 'service_role' key BYPASSES ALL ROW-LEVEL SECURITY (RLS) POLICIES.",
              "# NEVER prefix with NEXT_PUBLIC_ or expose to client bundles!",
              "SUPABASE_SERVICE_ROLE_KEY=\"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...\"",
            ].join("\n"),
            caption: "Strict classification of client-safe anon keys vs server-only service_role secrets.",
          },
        ],
      },
      {
        heading: "Local Development with the Supabase CLI",
        body: [
          "Professional engineering teams do not develop directly on shared staging or production cloud instances.",
          "Use the official Supabase CLI to run a full local Docker-based Supabase stack that mirrors production.",
        ],
        code: [
          {
            file: "terminal",
            lang: "bash",
            code: [
              "# 1. Initialize Supabase configuration in your workspace",
              "npx supabase init",
              "",
              "# 2. Start all local Docker services (Postgres, Auth, Inbucket email, Studio)",
              "npx supabase start",
              "",
              "# Output gives local connection strings:",
              "#   API URL: http://127.0.0.1:54321",
              "#   GraphQL URL: http://127.0.0.1:54321/graphql/v1",
              "#   DB URL: postgresql://postgres:postgres@127.0.0.1:54322/postgres",
              "#   Studio URL: http://127.0.0.1:54323",
              "#   Inbucket URL: http://127.0.0.1:54324",
            ].join("\n"),
            caption: "Spinning up a complete local Supabase development ecosystem using Docker and the Supabase CLI.",
          },
        ],
      },
    ],
    mistake: {
      title: "Exposing the service_role Key to the Frontend",
      wrong: [
        "// ❌ Client-side React / Next.js code",
        "import { createClient } from '@supabase/supabase-js';",
        "",
        "// FATAL: service_role key compiled into client bundle!",
        "export const supabase = createClient(",
        "  process.env.NEXT_PUBLIC_SUPABASE_URL!,",
        "  process.env.NEXT_PUBLIC_SERVICE_ROLE_KEY! // Leaks master admin privileges to all users!",
        ");",
      ].join("\n"),
      right: [
        "// ✅ Client-side React / Next.js code",
        "import { createClient } from '@supabase/supabase-js';",
        "",
        "// Only expose public anon key; protected by Row-Level Security policies",
        "export const supabase = createClient(",
        "  process.env.NEXT_PUBLIC_SUPABASE_URL!,",
        "  process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!",
        ");",
      ].join("\n"),
      explain:
        "The `service_role` key grants unrestricted superuser access that completely bypasses Row-Level Security. It must only ever reside in backend server environments.",
    },
    tryIt: [
      "Run `npx supabase --version` or review Supabase CLI commands for local development.",
      "Check your project `.env` files: ensure that no `service_role` key has a `NEXT_PUBLIC_` or `VITE_` prefix.",
      "Inspect the Supabase Dashboard: locate Database Logs, Postgres Advisor, and Connection Settings under Project Settings.",
    ],
    challenge: {
      prompt:
        "Create a NestJS backend module configuration that validates that `SUPABASE_SERVICE_ROLE_KEY` is present and prevents startup if the key is missing or suspiciously malformed.",
      hints: [
        "Create a configuration schema using Zod or `class-validator`",
        "Verify `SUPABASE_URL` is a valid URL starting with `https://`",
        "Verify `SUPABASE_SERVICE_ROLE_KEY` is a non-empty string and not equal to the anon key",
      ],
      solution: [
        "import { z } from 'zod';",
        "",
        "export const SupabaseConfigSchema = z.object({",
        "  SUPABASE_URL: z.string().url().startsWith('https://'),",
        "  SUPABASE_ANON_KEY: z.string().min(20),",
        "  SUPABASE_SERVICE_ROLE_KEY: z.string().min(20),",
        "}).refine((data) => data.SUPABASE_ANON_KEY !== data.SUPABASE_SERVICE_ROLE_KEY, {",
        "  message: 'SUPABASE_SERVICE_ROLE_KEY must not be identical to SUPABASE_ANON_KEY',",
        "  path: ['SUPABASE_SERVICE_ROLE_KEY'],",
        "});",
        "",
        "export type SupabaseConfig = z.infer<typeof SupabaseConfigSchema>;",
        "",
        "export function validateSupabaseEnv(config: Record<string, unknown>): SupabaseConfig {",
        "  const result = SupabaseConfigSchema.safeParse(config);",
        "  if (!result.success) {",
        "    console.error('❌ Invalid Supabase environment variables:', result.error.format());",
        "    throw new Error('Supabase environment configuration validation failed.');",
        "  }",
        "  return result.data;",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        question: "What is the key difference between the Supabase `anon` key and the `service_role` key?",
        options: [
          "The `anon` key is for MongoDB while `service_role` is for PostgreSQL",
          "The `anon` key is for client-side use subject to Row-Level Security (RLS), whereas `service_role` is a backend secret that bypasses all RLS",
          "The `anon` key expires every 5 minutes",
          "The `service_role` key cannot execute SELECT queries",
        ],
        answer: 1,
        explanation: "The `anon` key is safe for public clients because RLS guards the tables; `service_role` bypasses RLS completely and must remain secret.",
      },
      {
        question: "What database engine runs at the core of Supabase?",
        options: [
          "SQLite",
          "Native PostgreSQL",
          "DynamoDB",
          "CockroachDB",
        ],
        answer: 1,
        explanation: "Supabase is built directly on native PostgreSQL, giving you full access to Postgres types, schemas, indexes, and extensions.",
      },
      {
        question: "Why should developers use the Supabase CLI (`supabase start`) during local feature development?",
        options: [
          "It converts TypeScript to Rust",
          "It runs an isolated local Docker stack matching production without risking corruption or noisy test data on shared cloud databases",
          "It eliminates the need for Docker",
          "It automatically pays cloud hosting bills",
        ],
        answer: 1,
        explanation: "The Supabase CLI spins up local Docker containers for Postgres, Auth, and Storage, enabling fast, isolated offline development.",
      },
      {
        question: "What happens if a developer prefixes `SUPABASE_SERVICE_ROLE_KEY` with `NEXT_PUBLIC_` in Next.js?",
        options: [
          "Next.js encrypts the variable automatically",
          "The build tool bundles the secret key into client JavaScript files downloaded by any browser, allowing attackers to access all database records",
          "The server refuses to start with an error",
          "Nothing, environment variables cannot be viewed in browsers",
        ],
        answer: 1,
        explanation: "In Next.js, `NEXT_PUBLIC_` prefixes bundle variables directly into client-side code, exposing critical secrets.",
      },
      {
        question: "Where in the Supabase Dashboard can you inspect slow queries and database execution metrics?",
        options: [
          "Storage Bucket Settings",
          "Database Reports, Logs Explorer & Postgres Advisor",
          "Authentication Providers tab",
          "Billing Summary",
        ],
        answer: 1,
        explanation: "Database Reports and the Logs Explorer provide real-time query logs, slow query traces, and performance advisor warnings.",
      },
      {
        question: "How does PostgREST relate to Supabase?",
        options: [
          "It is a plugin for CSS styling",
          "It is the web server component that automatically reflects your PostgreSQL schema as a RESTful HTTP API",
          "It is a backup script for Redis",
          "It replaces SQL with JSON files",
        ],
        answer: 1,
        explanation: "PostgREST reads the PostgreSQL schema and exposes auto-generated REST endpoints that enforce Postgres RLS.",
      },
    ],
    flashcards: [
      {
        front: "What is Supabase?",
        back: "An open-source managed platform built around standard PostgreSQL, adding Auth, Storage, Edge Functions, and Connection Pooling.",
      },
      {
        front: "What is the `anon` key used for?",
        back: "Public client-side requests (browser/mobile). Requests are subject to PostgreSQL Row-Level Security (RLS) policies.",
      },
      {
        front: "What is the `service_role` key used for?",
        back: "Private backend server operations. It bypasses all RLS policies and must never be exposed to client bundles.",
      },
      {
        front: "What CLI command starts a local Supabase environment?",
        back: "`npx supabase start` (requires Docker to be running).",
      },
      {
        front: "What component provides connection pooling in modern Supabase?",
        back: "Supavisor (an Elixir-based high-throughput connection pooler) and PgBouncer.",
      },
      {
        front: "What happens if you use `service_role` in a frontend React app?",
        back: "Any user can inspect network headers or JS bundles to extract the key and read/modify all database tables.",
      },
      {
        front: "Where are database table definitions stored in Supabase?",
        back: "Inside standard PostgreSQL relational schemas (`public`, `auth`, `storage`, etc.).",
      },
      {
        front: "Can standard PostgreSQL drivers (like `pg` and Prisma) connect to Supabase?",
        back: "Yes, Supabase exposes standard PostgreSQL connection strings on ports 5432 and 6543.",
      },
    ],
    recap: [
      "Supabase provides standard, managed PostgreSQL enhanced with Supavisor, GoTrue Auth, and PostgREST.",
      "The `anon` key is public and relies on Postgres RLS; the `service_role` key is server-only and bypasses RLS.",
      "Never prefix backend secrets with `NEXT_PUBLIC_` or `VITE_`.",
      "Use the Supabase CLI for isolated local Docker-based development and testing.",
    ],
    references: [
      { label: "Supabase Architecture Overview", url: "https://supabase.com/docs/guides/getting-started/architecture" },
      { label: "Supabase CLI Documentation", url: "https://supabase.com/docs/guides/cli" },
    ],
    nextBridge: "Now that we understand the Supabase platform, let's master connection pooling architecture: Direct vs Session Pooler vs Transaction Pooler connections in Lesson 2.",
  },
  {
    id: "p16-l2",
    phaseId: "p16",
    title: "Direct vs Session Pooler Connections",
    level: "Backend Developer",
    minutes: 35,
    summary:
      "Master the difference between Direct database connections (Port 5432) and Supavisor Pooler connections (Ports 5432 & 6543). Learn when to use Transaction mode vs Session mode and how to configure SSL.",
    prerequisites: [
      "p16-l1 — Supabase Platform Tour & Key Categories",
      "p13-l4 — Transactions, Isolation & Locks",
    ],
    objectives: [
      "Distinguish between Direct PostgreSQL connections (port 5432) and Supavisor Connection Pooler (ports 5432/6543).",
      "Analyze the mechanics of Transaction Pooling vs Session Pooling modes.",
      "Identify features that fail in Transaction Pooling (e.g. prepared statements, advisory locks, LISTEN/NOTIFY).",
      "Configure dual-connection topologies (`DATABASE_URL` for runtime queries and `DIRECT_URL` for migrations).",
    ],
    simple:
      "PostgreSQL allocates a dedicated operating system process (using ~2–10MB of RAM) for every active direct connection. If your serverless functions or NestJS instances open 200 connections, a small database will run out of memory and crash. Supabase provides Supavisor/PgBouncer connection poolers: thousands of API clients share a small pool of 15–30 real Postgres connections. However, Transaction poolers (port 6543) do not support advisory locks or session variables, so Prisma migrations require a direct connection URL.",
    why:
      "Running Prisma migrations over a Transaction Pooler on port 6543 will fail with advisory lock errors. Conversely, running high-scale serverless runtime traffic over direct port 5432 will exhaust database connections and return `P1001` or `too many clients already` errors.",
    mentalModel: {
      title: "The Airport Taxi Stand",
      body: "A Direct connection is like hiring a dedicated personal chauffeur for the entire day: they wait for you outside every store, consuming resources even when you are idle. Transaction Pooling is like taking an airport taxi: you step into a car, drive to your destination (execute one query/transaction), and immediately release the taxi for the next passenger.",
    },
    sections: [
      {
        heading: "Direct vs Pooler: The Port and Protocol Matrix",
        body: [
          "Supabase exposes three primary connection endpoints. Choosing the right one for each tool is critical.",
        ],
        code: [
          {
            file: "CONNECTION_MATRIX.md",
            lang: "text",
            code: [
              "========================================================================",
              "ENDPOINT TYPE        PORT    HOST PATTERN                      BEST FOR",
              "========================================================================",
              "1. Direct DB         5432    db.[ref].supabase.co              Prisma Migrations",
              "                                                               Local CLI, Admin DDL",
              "",
              "2. Transaction Pool  6543    aws-0-[region].pooler.supabase.com  NestJS Runtime Services",
              "                             (Supavisor / PgBouncer)           Next.js Server Actions",
              "                                                               Serverless / Edge APIs",
              "",
              "3. Session Pool      5432    aws-0-[region].pooler.supabase.com  Prisma Studio",
              "                             (Supavisor Pooler)                Long-lived stateful sessions",
              "========================================================================",
            ].join("\n"),
            caption: "The connection matrix for Supabase: Direct (5432), Transaction Pooler (6543), and Session Pooler (5432).",
          },
        ],
      },
      {
        heading: "Why Transaction Pooling Forbids Session-Level Features",
        body: [
          "In Transaction Pooling mode (port 6543), Supavisor assigns a physical database connection to your client only for the duration of a single transaction or query.",
          "As soon as the query completes, the connection is returned to the pool and given to a completely different user.",
        ],
        code: [
          {
            file: "TRANSACTION_POOLING_RULES.md",
            lang: "text",
            code: [
              "🚫 FORBIDDEN IN TRANSACTION POOLING (Port 6543):",
              "  1. PostgreSQL Advisory Locks (`pg_advisory_lock`) -> Used by `prisma migrate`",
              "  2. `LISTEN` / `NOTIFY` -> Real-time pub/sub triggers",
              "  3. `SET timezone = ...` or `SET search_path = ...` (Session-level state)",
              "  4. Temporary Tables (`CREATE TEMP TABLE ...`)",
              "  5. Server-side Prepared Statements (unless `?pgbouncer=true` is appended to URL)",
              "",
              "✅ SUPPORTED IN TRANSACTION POOLING (Port 6543):",
              "  1. Standard CRUD queries (`SELECT`, `INSERT`, `UPDATE`, `DELETE`)",
              "  2. Multi-statement transactions (`BEGIN ... COMMIT / ROLLBACK`)",
              "  3. Prisma interactive transactions (`prisma.$transaction(async (tx) => { ... })`)",
            ].join("\n"),
            caption: "Capabilities and constraints of PostgreSQL Transaction Pooling mode.",
          },
        ],
      },
      {
        heading: "Interactive Diagnostic: Supabase Connection & Pooler Matrix",
        body: [
          "Use the interactive diagnostic tool below to simulate different database operations, port configurations, and SSL settings.",
        ],
        demo: "supabase-connection-lab",
      },
    ],
    mistake: {
      title: "Running `prisma migrate deploy` Through Transaction Pooler Port 6543",
      wrong: [
        "// ❌ Using Transaction Pooler for CLI migrations",
        "// .env",
        "DATABASE_URL=\"postgresql://postgres.ref:pass@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true\"",
        "",
        "// Running 'npx prisma migrate deploy' -> FAILS with advisory lock error!",
      ].join("\n"),
      right: [
        "// ✅ Dual URL configuration in prisma.config.ts and schema.prisma",
        "// .env",
        "# Runtime queries (Pooler)",
        "DATABASE_URL=\"postgresql://postgres.ref:pass@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require\"",
        "# DDL Migrations (Direct)",
        "DIRECT_URL=\"postgresql://postgres.ref:pass@db.ref.supabase.co:5432/postgres?sslmode=require\"",
      ].join("\n"),
      explain:
        "Prisma Migrate uses PostgreSQL advisory locks to guarantee that two deployment containers don't execute migrations concurrently. Advisory locks require direct or session-level connections.",
    },
    tryIt: [
      "Open your Supabase Project Settings -> Database -> Connection string section.",
      "Compare the URI for 'Transaction mode' (port 6543) vs 'Session mode' (port 5432) vs 'Direct connection' (port 5432).",
      "Notice that the username format changes: poolers require `postgres.[project-ref]` while direct connections use `postgres`.",
    ],
    challenge: {
      prompt:
        "Write a utility function in TypeScript that inspects a PostgreSQL connection string, parses the port and query parameters, and validates whether it is suitable for Prisma Migrate vs runtime queries.",
      hints: [
        "Use `new URL(connectionString)`",
        "Check `url.port` (6543 vs 5432)",
        "Check `url.searchParams.get('pgbouncer')`",
        "Return a health validation object with recommendation messages",
      ],
      solution: [
        "export interface ConnectionValidation {",
        "  isPooler: boolean;",
        "  port: number;",
        "  suitableForMigrations: boolean;",
        "  suitableForRuntime: boolean;",
        "  warnings: string[];",
        "}",
        "",
        "export function validateSupabaseConnectionString(connStr: string): ConnectionValidation {",
        "  const url = new URL(connStr);",
        "  const port = parseInt(url.port || '5432', 10);",
        "  const isPgBouncer = url.searchParams.get('pgbouncer') === 'true';",
        "  const isPoolerHost = url.hostname.includes('pooler.supabase.com');",
        "  const warnings: string[] = [];",
        "",
        "  if (port === 6543) {",
        "    if (!isPgBouncer) {",
        "      warnings.push('Port 6543 detected without ?pgbouncer=true. Prisma Client may throw prepared statement errors.');",
        "    }",
        "    return {",
        "      isPooler: true,",
        "      port,",
        "      suitableForMigrations: false,",
        "      suitableForRuntime: true,",
        "      warnings,",
        "    };",
        "  }",
        "",
        "  if (port === 5432 && !isPoolerHost) {",
        "    warnings.push('Direct port 5432 used. Safe for migrations, but high-concurrency runtime services risk connection pool exhaustion.');",
        "    return {",
        "      isPooler: false,",
        "      port,",
        "      suitableForMigrations: true,",
        "      suitableForRuntime: false,",
        "      warnings,",
        "    };",
        "  }",
        "",
        "  return {",
        "    isPooler: isPoolerHost,",
        "    port,",
        "    suitableForMigrations: true,",
        "    suitableForRuntime: true,",
        "    warnings,",
        "  };",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        question: "Which port does the Supabase Supavisor / PgBouncer Transaction Pooler use by default?",
        options: [
          "Port 80",
          "Port 6543",
          "Port 3000",
          "Port 8080",
        ],
        answer: 1,
        explanation: "Port 6543 is the standard port for Supabase Transaction Pooling.",
      },
      {
        question: "Why does `prisma migrate deploy` fail if pointed directly at port 6543?",
        options: [
          "Prisma doesn't understand SQL",
          "Prisma Migrate relies on PostgreSQL advisory locks (`pg_advisory_lock`), which are session-bound and unsupported in Transaction Pooling mode",
          "Port 6543 is read-only",
          "Postgres drops all tables on port 6543",
        ],
        answer: 1,
        explanation: "Advisory locks require persistent session state across statements; transaction poolers multiplex connections between statements and reject advisory locks.",
      },
      {
        question: "Why is Transaction Pooling recommended for runtime API servers (NestJS / Next.js) in cloud environments?",
        options: [
          "It compresses images automatically",
          "It allows thousands of concurrent API requests to share a small pool of physical Postgres connections without exhausting database RAM",
          "It disables SQL validation",
          "It makes queries run without indexes",
        ],
        answer: 1,
        explanation: "Each direct Postgres connection consumes ~2–10MB of RAM. Transaction pooling multiplexes queries so that idle clients don't hog database memory.",
      },
      {
        question: "What parameter must be appended to the connection URL when using Prisma with PgBouncer/Supavisor on port 6543?",
        options: [
          "`?fast=true`",
          "`?pgbouncer=true`",
          "`?disable_cache=1`",
          "`?turbo=on`",
        ],
        answer: 1,
        explanation: "`?pgbouncer=true` instructs Prisma Client not to use prepared statements, which would otherwise conflict across multiplexed connections.",
      },
      {
        question: "What is the role of `DIRECT_URL` in Prisma configuration?",
        options: [
          "It is used for downloading npm dependencies",
          "It provides a direct (non-pooled) connection string used exclusively by Prisma CLI for running migrations and introspections",
          "It bypasses all passwords",
          "It is an alias for Redis",
        ],
        answer: 1,
        explanation: "`DIRECT_URL` allows Prisma Migrate to use a direct connection for advisory locks while `DATABASE_URL` uses the pooler for app runtime.",
      },
      {
        question: "Does Supabase support unencrypted (non-SSL) database connections in production?",
        options: [
          "Yes, non-SSL is the default",
          "No, Supabase strictly enforces SSL/TLS encryption for all database connections",
          "Only on weekends",
          "Only for SELECT queries",
        ],
        answer: 1,
        explanation: "Supabase requires SSL for all external connections to protect data in transit over public networks.",
      },
    ],
    flashcards: [
      {
        front: "What is the default port for Supabase Transaction Pooler?",
        back: "Port 6543.",
      },
      {
        front: "What is the default port for Supabase Direct DB connection?",
        back: "Port 5432.",
      },
      {
        front: "Why do migrations fail on port 6543?",
        back: "Because transaction poolers do not support PostgreSQL advisory locks (`pg_advisory_lock`).",
      },
      {
        front: "What query parameter is required for Prisma with PgBouncer?",
        back: "`?pgbouncer=true` (disables server-side prepared statements).",
      },
      {
        front: "What is `DIRECT_URL` used for in Prisma?",
        back: "Direct connection (port 5432) used exclusively for DDL migrations and schema generation.",
      },
      {
        front: "What is `DATABASE_URL` used for in production NestJS apps?",
        back: "Transaction Pooler connection (port 6543) for high-concurrency runtime queries.",
      },
      {
        front: "How much memory does a direct PostgreSQL connection typically consume?",
        back: "Approximately 2MB to 10MB of RAM per open connection.",
      },
      {
        front: "What username format is required when connecting through the Supabase Pooler?",
        back: "`postgres.[project-ref]` (e.g. `postgres.xyzcompany:mypassword@aws-0-...`).",
      },
    ],
    recap: [
      "Use Transaction Pooler (port 6543) with `?pgbouncer=true` for NestJS/Next.js runtime `DATABASE_URL`.",
      "Use Direct connection (port 5432) for `DIRECT_URL` so `prisma migrate` can acquire advisory locks.",
      "Transaction poolers multiplex connections between queries; they do not support advisory locks or session-level variables.",
      "Always enforce SSL (`sslmode=require`) on all database connection strings.",
    ],
    references: [
      { label: "Supabase Connection Pooler Guide", url: "https://supabase.com/docs/guides/database/connecting-to-postgres#connection-pooler" },
      { label: "Prisma with Supabase & Connection Pooling", url: "https://www.prisma.io/docs/guides/database/supabase" },
    ],
    nextBridge: "Now let's wire this cleanly into our NestJS + Prisma 7.9.15 architecture in Lesson 3.",
  },
  {
    id: "p16-l3",
    phaseId: "p16",
    title: "Connecting NestJS + Prisma 7.9.15 to Supabase",
    level: "Backend Developer",
    minutes: 40,
    summary:
      "Configure NestJS and Prisma 7.9.15 for seamless Supabase PostgreSQL integration. Implement runtime pooler and direct migration URLs, environment validation with Zod, and custom PrismaService connection handling.",
    prerequisites: [
      "p16-l2 — Direct vs Session Pooler Connections",
      "p14-l2 — prisma.config.ts, ESM & Driver Adapters",
      "p10-l3 — Dependency Injection & Lifecycle Hooks",
    ],
    objectives: [
      "Configure `prisma.config.ts` with both `DATABASE_URL` (pooler) and `DIRECT_URL` (direct).",
      "Set up strict runtime environment validation for database credentials in NestJS.",
      "Implement a robust, production-ready `PrismaService` with graceful connection lifecycle and error hooks.",
      "Execute `prisma migrate deploy` against Supabase cloud databases in CI/CD pipelines.",
    ],
    simple:
      "Connecting NestJS to Supabase combines everything we have learned: we configure `prisma.config.ts` so migrations run over the direct URL, while our NestJS `PrismaService` connects through the Supavisor transaction pooler using `@prisma/adapter-pg`. This gives us infinite runtime connection scalability without breaking migration advisory locks.",
    why:
      "In production, misconfiguring environment variables leads to mysterious connection crashes during traffic spikes or migration lockouts during deployments. Setting up dual URLs and strict validation upfront prevents production outages.",
    mentalModel: {
      title: "The Service Highway and the Construction Access Road",
      body: "Think of your database traffic like a city. High-speed commuter traffic (NestJS API requests) uses the multi-lane service highway (Transaction Pooler). Heavy construction equipment (Prisma Migrations) uses the dedicated construction access road (Direct Connection) to avoid blocking commuter traffic.",
    },
    sections: [
      {
        heading: "Configuring prisma.config.ts with Dual Supabase URLs",
        body: [
          "In Prisma 7.9.15, configurations are defined in `prisma.config.ts` using `defineConfig`.",
          "We specify `datasource.url` for runtime queries and `datasource.directUrl` for migrations.",
        ],
        code: [
          {
            file: "prisma.config.ts",
            lang: "ts",
            code: [
              "import { defineConfig } from 'prisma/config';",
              "",
              "export default defineConfig({",
              "  datasource: {",
              "    // Runtime pooler connection (Port 6543 with ?pgbouncer=true)",
              "    url: process.env.DATABASE_URL!,",
              "    // Direct connection (Port 5432) used exclusively for migrations & advisory locks",
              "    directUrl: process.env.DIRECT_URL!,",
              "  },",
              "  migrations: {",
              "    directory: 'prisma/migrations',",
              "    seed: 'ts-node prisma/seed.ts',",
              "  },",
              "});",
            ].join("\n"),
            caption: "Prisma 7.9.15 configuration binding runtime queries to pooler and migrations to direct URL.",
          },
        ],
      },
      {
        heading: "NestJS Environment Validation with Zod",
        body: [
          "Ensure your NestJS server fails fast at boot if database environment variables are missing or misconfigured.",
        ],
        code: [
          {
            file: "src/config/database.config.ts",
            lang: "ts",
            code: [
              "import { z } from 'zod';",
              "",
              "export const DatabaseEnvSchema = z.object({",
              "  DATABASE_URL: z",
              "    .string()",
              "    .url()",
              "    .includes('pooler.supabase.com')",
              "    .describe('Supabase Transaction Pooler URL (port 6543)'),",
              "  DIRECT_URL: z",
              "    .string()",
              "    .url()",
              "    .describe('Supabase Direct PostgreSQL URL (port 5432)'),",
              "  NODE_ENV: z.enum(['development', 'test', 'production']).default('development'),",
              "});",
              "",
              "export type DatabaseEnv = z.infer<typeof DatabaseEnvSchema>;",
              "",
              "export function validateDatabaseEnv(config: Record<string, unknown>): DatabaseEnv {",
              "  const result = DatabaseEnvSchema.safeParse(config);",
              "  if (!result.success) {",
              "    console.error('❌ Database Environment Validation Failed:', result.error.format());",
              "    throw new Error('Invalid Database Environment Configuration');",
              "  }",
              "  return result.data;",
              "}",
            ].join("\n"),
            caption: "Strict environment schema validation preventing runtime startup on misconfigured URLs.",
          },
        ],
      },
      {
        heading: "Production NestJS PrismaService with Pooler Driver Adapter",
        body: [
          "Use `@prisma/adapter-pg` with a configured Node `pg.Pool` to manage connections cleanly within the NestJS lifecycle.",
        ],
        code: [
          {
            file: "src/prisma/prisma.service.ts",
            lang: "ts",
            code: [
              "import { Injectable, OnModuleInit, OnModuleDestroy, Logger } from '@nestjs/common';",
              "import { PrismaClient } from '@prisma/client';",
              "import { PrismaPg } from '@prisma/adapter-pg';",
              "import { Pool } from 'pg';",
              "",
              "@Injectable()",
              "export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {",
              "  private readonly logger = new Logger(PrismaService.name);",
              "  private pool: Pool;",
              "",
              "  constructor() {",
              "    const connectionString = process.env.DATABASE_URL;",
              "    const pool = new Pool({",
              "      connectionString,",
              "      max: 15, // Match Supavisor client allocation",
              "      idleTimeoutMillis: 30000,",
              "      connectionTimeoutMillis: 5000,",
              "      ssl: { rejectUnauthorized: false },",
              "    });",
              "    const adapter = new PrismaPg(pool);",
              "",
              "    super({",
              "      adapter,",
              "      log: process.env.NODE_ENV === 'development' ? ['query', 'error', 'warn'] : ['error'],",
              "    });",
              "",
              "    this.pool = pool;",
              "  }",
              "",
              "  async onModuleInit() {",
              "    try {",
              "      await this.$connect();",
              "      this.logger.log('✅ Connected to Supabase PostgreSQL via Supavisor Pooler');",
              "    } catch (error) {",
              "      this.logger.error('❌ Failed to connect to Supabase PostgreSQL:', error);",
              "      throw error;",
              "    }",
              "  }",
              "",
              "  async onModuleDestroy() {",
              "    await this.$disconnect();",
              "    await this.pool.end();",
              "    this.logger.log('🔌 Disconnected from Supabase PostgreSQL pool');",
              "  }",
              "}",
            ].join("\n"),
            caption: "Production-ready PrismaService in NestJS using @prisma/adapter-pg and pg.Pool.",
          },
        ],
      },
    ],
    mistake: {
      title: "Omitting DIRECT_URL in Prisma 7.9.15 Configuration",
      wrong: [
        "// ❌ Only specifying pooled DATABASE_URL in prisma.config.ts",
        "export default defineConfig({",
        "  datasource: {",
        "    url: process.env.DATABASE_URL, // Points to pooler on 6543",
        "  },",
        "});",
        "// Result: 'prisma migrate dev' and 'prisma migrate deploy' fail with advisory lock errors!",
      ].join("\n"),
      right: [
        "// ✅ Providing both url and directUrl in prisma.config.ts",
        "export default defineConfig({",
        "  datasource: {",
        "    url: process.env.DATABASE_URL,       // 6543 for runtime",
        "    directUrl: process.env.DIRECT_URL,   // 5432 for migrations",
        "  },",
        "});",
      ].join("\n"),
      explain:
        "Without `directUrl`, Prisma CLI attempts to run migrations through the transaction pooler on port 6543, which rejects required advisory locks.",
    },
    tryIt: [
      "In your project, check `prisma.config.ts`: verify that both `url` and `directUrl` properties are declared.",
      "Check your `.env` file: verify that `DATABASE_URL` ends with `?pgbouncer=true` and `DIRECT_URL` points to port 5432.",
      "Run `npx prisma migrate status` to confirm that Prisma can inspect migration state on Supabase.",
    ],
    challenge: {
      prompt:
        "Implement a NestJS health check indicator (`PrismaHealthIndicator`) for `@nestjs/terminus` that verifies Supabase database connectivity with a 1-second timeout.",
      hints: [
        "Extend `HealthIndicator` from `@nestjs/terminus`",
        "Execute `prisma.$queryRaw` with `SELECT 1`",
        "Wrap the call in a timeout promise (1000ms)",
        "Return `this.getStatus('database', true)` or throw `HealthCheckError`",
      ],
      solution: [
        "import { Injectable } from '@nestjs/common';",
        "import { HealthIndicator, HealthIndicatorResult, HealthCheckError } from '@nestjs/terminus';",
        "import { PrismaService } from './prisma.service';",
        "",
        "@Injectable()",
        "export class PrismaHealthIndicator extends HealthIndicator {",
        "  constructor(private readonly prisma: PrismaService) {",
        "    super();",
        "  }",
        "",
        "  async isHealthy(key = 'database'): Promise<HealthIndicatorResult> {",
        "    try {",
        "      // Execute lightweight ping query with 1000ms timeout",
        "      const pingPromise = this.prisma.$queryRaw`SELECT 1 as ping`;",
        "      const timeoutPromise = new Promise((_, reject) =>",
        "        setTimeout(() => reject(new Error('Database ping timeout (1000ms)')), 1000)",
        "      );",
        "",
        "      await Promise.race([pingPromise, timeoutPromise]);",
        "      return this.getStatus(key, true, { message: 'Supabase PostgreSQL responding' });",
        "    } catch (error: any) {",
        "      throw new HealthCheckError(",
        "        'Database check failed',",
        "        this.getStatus(key, false, { error: error.message })",
        "      );",
        "    }",
        "  }",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        question: "Why should `DATABASE_URL` point to the Supabase pooler while `DIRECT_URL` points to direct port 5432?",
        options: [
          "Because NestJS cannot connect to port 5432",
          "Runtime traffic requires connection pooling to prevent database memory exhaustion, while Prisma Migrate requires a direct connection to acquire advisory locks",
          "Supabase disables migrations on port 5432",
          "To bypass password requirements",
        ],
        answer: 1,
        explanation: "Runtime traffic needs pooling for scale; migrations need direct connections for advisory locking.",
      },
      {
        question: "What driver adapter is used to connect Prisma 7.9.15 to PostgreSQL with a custom `pg.Pool`?",
        options: [
          "`@prisma/adapter-sqlite`",
          "`@prisma/adapter-pg`",
          "`@prisma/mongo-driver`",
          "`@prisma/rest-adapter`",
        ],
        answer: 1,
        explanation: "`@prisma/adapter-pg` binds Prisma Client directly to the standard Node.js `pg` driver and pool.",
      },
      {
        question: "How should NestJS handle database disconnection on application shutdown?",
        options: [
          "Do nothing, let the OS terminate the process",
          "Implement `OnModuleDestroy` and await both `prisma.$disconnect()` and `pool.end()`",
          "Delete the `node_modules` directory",
          "Restart the container immediately",
        ],
        answer: 1,
        explanation: "Graceful shutdown via `OnModuleDestroy` ensures that open pool connections are terminated cleanly without leaving orphaned connections on Postgres.",
      },
      {
        question: "What happens if a NestJS server boots with an invalid or unreachable `DATABASE_URL`?",
        options: [
          "It silently uses an in-memory database",
          "With proper `onModuleInit` connection verification, it throws an error and fails fast, preventing traffic routing to a broken pod",
          "It continues running and returns 200 OK for all requests",
          "It opens a browser window",
        ],
        answer: 1,
        explanation: "Failing fast during `onModuleInit` alerts orchestration systems (like Kubernetes/Cloud Run) that the container is unhealthy and should not receive traffic.",
      },
      {
        question: "What query parameter is needed in `DATABASE_URL` when connecting through Supavisor Transaction Pooler?",
        options: [
          "`?pgbouncer=true`",
          "`?admin=1`",
          "`?sync=true`",
          "`?raw=true`",
        ],
        answer: 0,
        explanation: "`?pgbouncer=true` disables server-side prepared statements in Prisma Client, avoiding collisions in transaction pooling.",
      },
      {
        question: "Which Prisma command is used in production CI/CD deployment scripts to apply pending migrations?",
        options: [
          "`prisma migrate dev`",
          "`prisma migrate deploy`",
          "`prisma db push`",
          "`prisma studio`",
        ],
        answer: 1,
        explanation: "`prisma migrate deploy` applies verified, committed migrations without creating new migrations or modifying schema files.",
      },
    ],
    flashcards: [
      {
        front: "What are the two database URLs needed for Supabase + Prisma?",
        back: "`DATABASE_URL` (pooler on 6543 for runtime) and `DIRECT_URL` (direct on 5432 for migrations).",
      },
      {
        front: "Where are `url` and `directUrl` configured in Prisma 7.9.15?",
        back: "Inside `prisma.config.ts` using `defineConfig({ datasource: { url, directUrl } })`.",
      },
      {
        front: "What NestJS lifecycle hooks should `PrismaService` implement?",
        back: "`OnModuleInit` (connect and verify) and `OnModuleDestroy` (disconnect and close pool).",
      },
      {
        front: "What driver adapter connects Prisma 7.9.15 to Node `pg.Pool`?",
        back: "`@prisma/adapter-pg` (`new PrismaPg(pool)`).",
      },
      {
        front: "Why should database environment variables be validated with Zod at boot?",
        back: "To fail fast if credentials, hostnames, or required query params are missing before serving traffic.",
      },
      {
        front: "What is the recommended pool size (`max`) for `pg.Pool` behind Supavisor?",
        back: "Typically 10–15 per container instance, matching the project's allocated pool size.",
      },
      {
        front: "How do you run migrations in CI/CD without human intervention?",
        back: "Run `npx prisma migrate deploy` with `DIRECT_URL` set in the environment.",
      },
      {
        front: "What does `?sslmode=require` do?",
        back: "Instructs the PostgreSQL client to require TLS encryption for all wire communication with Supabase.",
      },
    ],
    recap: [
      "Configure dual URLs in `prisma.config.ts`: `url` (pooler) for runtime queries, `directUrl` (direct) for migrations.",
      "Validate environment variables at boot using Zod to fail fast on misconfigurations.",
      "Use `@prisma/adapter-pg` in `PrismaService` for granular pool lifecycle management.",
      "Always apply production migrations using `prisma migrate deploy`.",
    ],
    references: [
      { label: "Prisma 7.9.15 Configuration Docs", url: "https://www.prisma.io/docs/reference/api-reference/prisma-config-reference" },
      { label: "NestJS Prisma Integration Guide", url: "https://docs.nestjs.com/recipes/prisma" },
    ],
    nextBridge: "Now let's examine why migrations must remain the single source of truth and how to handle schema drift with Supabase Studio in Lesson 4.",
  },
];

export const LESSON_CONTENT_P16: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P16.map((l) => [l.id, l])
);
