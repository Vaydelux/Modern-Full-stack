import type { LessonContent } from "./types";

/**
 * Pass 012 bounded content batch: Phase 4 closers (L6–L7).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P4C: LessonContent[] = [
  {
    id: "p04-l6",
    phaseId: "p04",
    title: "Lifting State & Derived State",
    level: "React Developer",
    minutes: 35,
    summary:
      "When two siblings need the same state, the state doesn't belong to either — it belongs to their nearest common ancestor, and both receive it as props and report changes as events. This lesson installs lifting state (data down, events up), then the counterweight discipline: derived state is computed, never stored. You'll practice both on a temperature converter, learn where prop drilling becomes honest pain, and get a preview of the tool (context) that Phase 5 brings for the deep cases.",
    prerequisites: ["p04-l5 — forms & controlled inputs", "p04-l2 — composition and children"],
    objectives: [
      "Identify when state must be lifted: two or more components read or write the same value.",
      "Apply the lift: move state to the nearest common ancestor, pass it down as props, collect updates via callback props.",
      "State the rule: data flows down as props, events flow up as callback invocations — the owner decides.",
      "Recognize derived state and compute it instead of storing a second copy that can drift.",
      "Weigh prop drilling's honest limits and name context as the Phase 5 escape hatch.",
      "Refactor a duplicated-state bug (two out-of-sync inputs) into a single lifted source of truth.",
    ],
    simple:
      "State should live as close to where it's used as possible — but no closer. If only one component needs a value, it keeps it. The moment a sibling needs it too, neither can own it fairly, so you move it up to the parent they share. The parent holds the state, hands the current value DOWN to both children as props, and hands each child a little function to call when it wants to change the value — the child reports the intent UP, and the parent, as the owner, performs the update and re-renders everyone with the fresh value. That round trip — data down, events up — is the entire architecture of React data flow.",
    why:
      "Lifting state is the first real architecture decision you make in React, and the instinct to get it right — 'single source of truth, as low as possible' — is the foundation for every larger pattern: context, reducers, and server-state libraries are all variations on 'where does this value live, and who may change it?' The paired discipline (derive, don't duplicate) prevents the most common React bug class: two copies of 'the same' data drifting apart, so the UI contradicts itself. Developers who store derived values create sync bugs they then paper over with effects; developers who derive them make the bugs unrepresentable. This lesson is where that fork in the road appears.",
    mentalModel: {
      title: "The household thermostat",
      body: "Two rooms, one thermostat. The living room and the bedroom both DISPLAY the temperature and both have a dial to CHANGE it — but neither room owns the setting. The thermostat (the common ancestor) holds the one true temperature. Each room gets the current reading piped in (props down), and each room's dial doesn't set the heat directly — it sends a request to the thermostat (event up), which adjusts the single setting and broadcasts the new reading to every room at once. Because there's exactly one thermostat, the rooms can never disagree. If each room kept its OWN thermometer-and-heater (duplicated state), one room would say 68° while the other said 72° about the same house — a contradiction. Lift the state to the shared authority, and disagreement becomes impossible.",
    },
    sections: [
      {
        heading: "The symptom — duplicated state that drifts",
        body: [
          "The bug that motivates lifting: two components each hold their own copy of what should be one value. Two inputs that should mirror each other, two lists that should agree, a parent and child each tracking 'is the form dirty.' Each copy updates independently, so they drift — change one and the other stays stale, and the UI contradicts itself. The tell is always the same: you find yourself writing an effect or a handler whose only job is to copy one piece of state into another. That copy is the smell.",
          "The diagnosis is a question: 'Do two or more components need to read or write this value?' If yes, the value has no single owner yet, and no amount of copying will fix it — you must give it one owner. That owner is the nearest component ABOVE all the consumers, and the fix is to move (lift) the state there.",
        ],
        code: [
          {
            file: "drift.tsx — the bug before the fix",
            lang: "tsx",
            code: [
              "// ✗ Each child owns its own copy — they can disagree.",
              "function Parent() {",
              "  return (",
              "    <div>",
              "      <CelsiusInput />   {/* holds its own celsius state */}",
              "      <FahrenheitInput /> {/* holds its own fahrenheit state */}",
              "    </div>",
              "  );",
              "}",
              "// Type in the Celsius box: only IT updates. The Fahrenheit box",
              "// sits there, oblivious. Two sources of truth, zero agreement.",
              "// An effect copying one into the other is a band-aid, not a fix.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The lift — data down, events up",
        body: [
          "The fix: move the shared state to the nearest common ancestor. The parent holds the ONE value (store the canonical form — here, Celsius — and derive the other), passes the current value DOWN to each child as a prop, and passes each child an on-change callback prop. When a child's input changes, it doesn't set state itself; it calls the callback with the new value, and the PARENT updates the single source. The parent re-renders, both children receive fresh props, and they agree by construction.",
          "Notice the child becomes controlled-but-stateless: it owns no state, just renders a prop and reports keystrokes. This is the 'dumb child, smart parent' shape — children describe and report, the owner decides. The data flows down, events flow up, and there is exactly one place the truth can be changed. That single write-path is what makes the bug class disappear.",
        ],
        code: [
          {
            file: "lifted.tsx — one owner, two views",
            lang: "tsx",
            code: [
              "function toF(c: number) { return (c * 9) / 5 + 32; }",
              "function toC(f: number) { return ((f - 32) * 5) / 9; }",
              "",
              "// The child: stateless. Renders a prop, reports intent.",
              "function TempInput({",
              "  label, value, onChange,",
              "}: { label: string; value: string; onChange: (v: string) => void }) {",
              "  return (",
              "    <label>",
              "      {label}",
              "      <input value={value} onChange={(e) => onChange(e.target.value)} />",
              "    </label>",
              "  );",
              "}",
              "",
              "// The owner: holds the ONE source of truth (celsius).",
              "function Converter() {",
              "  const [celsius, setCelsius] = useState(\"0\");",
              "",
              "  const c = Number(celsius);",
              "  const fahrenheit = celsius === \"\" ? \"\" : String(Math.round(toF(c) * 10) / 10);",
              "",
              "  return (",
              "    <div>",
              "      <TempInput label=\"Celsius\" value={celsius} onChange={setCelsius} />",
              "      <TempInput",
              "        label=\"Fahrenheit\"",
              "        value={fahrenheit}",
              "        onChange={(f) => setCelsius(f === \"\" ? \"\" : String(toC(Number(f))))}",
              "      />",
              "    </div>",
              "  );",
              "}",
              "// Type in EITHER box: the event goes up, Converter updates the",
              "// single celsius, and both boxes re-render in agreement.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Derived state — compute, don't store",
        body: [
          "A close cousin of the lift: values that are a pure function of existing state should be COMPUTED during render, not stored as their own state. A filtered list, a total, a formatted string, a boolean like 'is the form valid' — all derivable from the source of truth. Storing them creates a second copy that must be kept in sync, and every sync point is a potential bug. The rule: if you can write const derived = f(state) in the render body, do that and never call useState on it.",
          "The test for 'should this be state?': Can it change independently of everything else, via a user/external event? A search query — yes, it's state. The list filtered BY that query — no, it's derived. When in doubt, derive; you can always promote to state later if you need to change it independently, but demoting a duplicated state back to a derivation is a refactor you'd rather never owe.",
        ],
        code: [
          {
            file: "derive.tsx — computed, not stored",
            lang: "tsx",
            code: [
              "function Cart({ items }: { items: Item[] }) {",
              "  const [discountCode, setDiscountCode] = useState(\"\");",
              "",
              "  // DERIVED — recomputed each render from the sources of truth.",
              "  const subtotal = items.reduce((s, i) => s + i.price * i.qty, 0);",
              "  const discount = discountCode === \"SAVE10\" ? subtotal * 0.1 : 0;",
              "  const total = subtotal - discount;",
              "  const itemCount = items.reduce((s, i) => s + i.qty, 0);",
              "",
              "  // None of subtotal/discount/total/itemCount is useState.",
              "  // They can't drift because they have no independent existence.",
              "  return (/* render total, itemCount, etc. */ null);",
              "}",
              "",
              "// The question: 'Can this change on its own?'",
              "// discountCode: yes (user types it) → state.",
              "// total: no (always f(subtotal, discount)) → derive.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Prop drilling and its honest limits",
        body: [
          "Passing props down a level or two is healthy and explicit. Prop drilling is the smell: a value that must travel through four or five intermediate components that don't use it, each adding it to its props just to forward it. The intermediates become brittle — their prop contracts grow with values they don't care about, and re-threading them on a change touches every layer. It's not wrong for shallow trees; it's painful for deep ones.",
          "The escape hatch is context (Phase 5): a way to provide a value at some ancestor and consume it at any descendant without threading the middle layers. But context has costs of its own (coupling, re-render spread), so the honest order of operations is: local state first, lift when siblings share it, and reach for context only when the value is genuinely cross-cutting (theme, current user, locale) and the drilling is deep. Knowing where that line is — and not jumping to context at the first two-level pass — is the judgment this phase builds.",
        ],
      },
      {
        heading: "Lab — the converter, two ways",
        body: [
          "The lab makes the lesson concrete by building the temperature converter twice. First the broken way: two stateful children, each owning a value, visibly disagreeing — you can literally watch the drift. Then the lifted way: one owner, two stateless children, agreement by construction. Doing both is the point; the second version isn't just 'better code,' it's a different (correct) ownership model, and feeling the difference is what makes the rule stick.",
          "Extend it with a derived readout — a sentence like '32°F is 0°C, water freezes' — computed from the canonical value, never stored. That single extension exercises lifting AND derivation together, the two skills this lesson installs.",
        ],
      },
    ],
    mistake: {
      title: "Syncing two states with an effect instead of lifting",
      wrong: [
        "function Parent() {",
        "  const [a, setA] = useState(\"\");",
        "  const [b, setB] = useState(\"\");",
        "",
        "  // The band-aid: copy a into b whenever a changes.",
        "  useEffect(() => { setB(a.toUpperCase()); }, [a]);",
        "",
        "  return (",
        "    <div>",
        "      <input value={a} onChange={(e) => setA(e.target.value)} />",
        "      <input value={b} onChange={(e) => setB(e.target.value)} />",
        "    </div>",
        "  );",
        "}",
        "// Two sources of truth plus a cop. Type into B and A overwrites",
        "// it on the next render; type into A and B lags by a render.",
        // They fight. The effect is the symptom, not the solution.",
      ].join("\n"),
      right: [
        "function Parent() {",
        "  const [a, setA] = useState(\"\");   // ONE source of truth",
        "  const b = a.toUpperCase();        // b is DERIVED, not stored",
        "",
        "  return (",
        "    <div>",
        "      <input value={a} onChange={(e) => setA(e.target.value)} />",
        "      <p>{b}</p>  {/* or a read-only/controlled view of the same truth */}",
        "    </div>",
        "  );",
        "}",
        "// One value, one owner, one write-path. There is nothing to keep",
        // in sync because there is nothing duplicated. If B must be",
        // independently editable, then A and B are genuinely two values",
        // — lift/keep them as such deliberately, don't copy.",
      ].join("\n"),
      explain:
        "An effect whose job is to copy one state into another reveals that the second 'state' isn't independent — it's derived data wearing a state costume. Storing it creates two sources of truth that must be policed, and the policing (the effect) introduces lag and fights. Derive it and the duplication — and the entire bug class — disappears. Reach for effects to synchronize with EXTERNAL systems, not to reconcile your own state with itself.",
    },
    tryIt: [
      "Build the broken converter: two children each with their own state. Type in one and watch the other stay stale. Describe the ownership problem in one sentence.",
      "Refactor it to the lifted version with a single celsius source. Confirm typing in EITHER box updates both.",
      "Add a derived readout (a sentence describing the temperature) computed in the parent. Confirm it never needs its own state or effect.",
      "Find or create a piece of state you're storing that could be derived. Replace the useState with a computation and delete any sync logic.",
      "Thread a 'username' prop through three nested components that don't use it, just to reach a deep child. Feel the drilling, then note what context (Phase 5) would remove.",
    ],
    challenge: {
      prompt:
        "Build a 'split bill' calculator. Three sibling components share one bill: a list of line items (each { id, label, cents }), a tip-percent control, and a per-person summary given a party size. Requirements: (1) the line items, tip percent, and party size are state owned by the nearest common ancestor; (2) the line-item list can add/remove items via callback props (events up); (3) tip amount, grand total, and per-person share are ALL derived in the owner (integer cents arithmetic); (4) no child holds state, and no useEffect is used anywhere; (5) explain in two sentences why the totals must be derived rather than stored.",
      hints: [
        "Owner holds: items (array), tipPct (number), partySize (number). Everything else — subtotal, tipCents, totalCents, perPerson — is const math in the render body.",
        "Add/remove: children call onAdd(label, cents) / onRemove(id); the owner updates items immutably (the Phase 2 recipes).",
        "Integer cents: subtotal = items.reduce((s,i)=>s+i.cents,0); tipCents = Math.round(subtotal*tipPct/100); perPerson = Math.ceil((subtotal+tipCents)/partySize). Guard partySize >= 1.",
      ],
      solution: [
        "import { useState } from \"react\";",
        "",
        "interface Item { id: number; label: string; cents: number; }",
        "",
        "// Stateless child #1: the line items. Reports intent up.",
        "function ItemList({",
        "  items, onRemove,",
        "}: { items: Item[]; onRemove: (id: number) => void }) {",
        "  return (",
        "    <ul>",
        "      {items.map((i) => (",
        "        <li key={i.id}>",
        "          {i.label} — ${(i.cents / 100).toFixed(2)}",
        "          <button type=\"button\" onClick={() => onRemove(i.id)}>✕</button>",
        "        </li>",
        "      ))}",
        "    </ul>",
        "  );",
        "}",
        "",
        "// Stateless child #2: the tip control.",
        "function TipControl({",
        "  tipPct, onChange,",
        "}: { tipPct: number; onChange: (pct: number) => void }) {",
        "  return (",
        "    <input",
        "      type=\"range\" min={0} max={30} value={tipPct}",
        "      onChange={(e) => onChange(Number(e.target.value))}",
        "      aria-label=\"Tip percent\"",
        "    />",
        "  );",
        "}",
        "",
        "// Stateless child #3: the summary. Pure display of props.",
        "function Summary({",
        "  subtotal, tipCents, totalCents, perPerson,",
        "}: { subtotal: number; tipCents: number; totalCents: number; perPerson: number }) {",
        "  const fmt = (c: number) => `$${(c / 100).toFixed(2)}`;",
        "  return (",
        "    <dl>",
        "      <dt>Subtotal</dt><dd>{fmt(subtotal)}</dd>",
        "      <dt>Tip</dt><dd>{fmt(tipCents)}</dd>",
        "      <dt>Total</dt><dd>{fmt(totalCents)}</dd>",
        "      <dt>Per person</dt><dd>{fmt(perPerson)}</dd>",
        "    </dl>",
        "  );",
        "}",
        "",
        "// The owner: ALL the state, ALL the derivation, ALL the writes.",
        "function SplitBill() {",
        "  const [items, setItems] = useState<Item[]>([",
        "    { id: 1, label: \"Ramen\", cents: 1400 },",
        "    { id: 2, label: \"Gyoza\", cents: 650 },",
        "  ]);",
        "  const [tipPct, setTipPct] = useState(15);",
        "  const [partySize, setPartySize] = useState(2);",
        "",
        "  // DERIVED — integer cents, recomputed each render.",
        "  const subtotal = items.reduce((s, i) => s + i.cents, 0);",
        "  const tipCents = Math.round((subtotal * tipPct) / 100);",
        "  const totalCents = subtotal + tipCents;",
        "  const perPerson = Math.ceil(totalCents / Math.max(1, partySize));",
        "",
        "  function addItem(label: string, cents: number) {",
        "    setItems((prev) => [...prev, { id: Date.now(), label, cents }]);",
        "  }",
        "  function removeItem(id: number) {",
        "    setItems((prev) => prev.filter((i) => i.id !== id));",
        "  }",
        "",
        "  return (",
        "    <section>",
        "      <ItemList items={items} onRemove={removeItem} />",
        "      <TipControl tipPct={tipPct} onChange={setTipPct} />",
        "      <label>",
        "        Party size",
        "        <input",
        "          type=\"number\" min={1} value={partySize}",
        "          onChange={(e) => setPartySize(Math.max(1, Number(e.target.value)))}",
        "        />",
        "      </label>",
        "      <Summary",
        "        subtotal={subtotal} tipCents={tipCents}",
        "        totalCents={totalCents} perPerson={perPerson}",
        "      />",
        "    </section>",
        "  );",
        "}",
        "",
        "// Why the totals are derived, not stored: they are a pure function of",
        "// items+tipPct+partySize, so storing them would add copies that must be",
        "// kept in sync by hand — and any sync point can lag or lie. Computing",
        "// them each render makes disagreement impossible by construction.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "You should lift state when…",
        options: ["Two or more components need to read or write the same value", "A component is large", "You want to use an effect", "Props get boring"],
        answer: 0,
        explain: "Shared read/write means the value has no single owner. Move it to the nearest common ancestor so there's exactly one source of truth and one write-path.",
      },
      {
        q: "In the lifted pattern, a stateless child…",
        options: ["Renders a prop and reports changes by calling a callback prop", "Holds its own copy and syncs it", "Calls setState from the parent directly", "Reads the parent's state via a ref"],
        answer: 0,
        explain: "Data flows down as props; events flow up as callback invocations. The child describes and reports; the owner decides and updates.",
      },
      {
        q: "A value that is a pure function of existing state should be…",
        options: ["Computed in the render body, not stored as separate state", "Stored so it's faster", "Stored in an effect", "Memoized in localStorage"],
        answer: 0,
        explain: "Deriving keeps a single source of truth; storing creates a second copy that can drift. If you can write const x = f(state), do — no useState.",
      },
      {
        q: "An effect that copies one state into another usually means…",
        options: ["The second value is derived data that shouldn't be state", "Effects are required for sync", "You need more state", "React is misconfigured"],
        answer: 0,
        explain: "The copy-effect is the smell of duplicated truth. Derive the value instead; reserve effects for synchronizing with external systems, not reconciling your own state.",
      },
      {
        q: "The test for 'should this be state?' is…",
        options: ["Can it change independently via a user/external event?", "Is it a number?", "Is it shown in the UI?", "Is it passed as a prop?"],
        answer: 0,
        explain: "State changes on its own (user input, network, timer). Anything computable from other state is derived. A search query is state; the filtered list is derived.",
      },
      {
        q: "The honest order of operations for sharing data is…",
        options: ["Local state → lift to a common ancestor → context only for deep cross-cutting values", "Context always", "A global store always", "Props to every component always"],
        answer: 0,
        explain: "Start local, lift when siblings share, and reach for context only when drilling is deep and the value is genuinely cross-cutting (theme, user, locale). Context has costs too.",
      },
    ],
    flashcards: [
      { front: "Lifting state", back: "Move shared state to the nearest common ancestor; pass the value down as props and collect updates via callback props. One owner, one write-path." },
      { front: "Data down, events up", back: "Props (data) flow parent→child; children report intent by calling callback props handed to them. The state owner decides. React's one-way flow." },
      { front: "Derived state", back: "A value that's a pure function of existing state — compute it in the render body, never store it. Deriving makes drift impossible." },
      { front: "The sync-effect smell", back: "An effect whose job is to copy one state into another signals duplicated truth. Derive instead; effects are for external systems." },
      { front: "State vs derived test", back: "State changes independently (user/network/timer). If you can write const x = f(state), it's derived. When in doubt, derive." },
      { front: "Stateless (controlled) child", back: "A component that owns no state: renders props, reports events. 'Dumb child, smart parent' — describes and reports, never decides." },
      { front: "Prop drilling", back: "Threading a value through intermediate components that don't use it. Fine shallow, painful deep — context (Phase 5) is the deep-tree escape hatch." },
      { front: "Single source of truth", back: "Exactly one place owns a given value; everything else is a view of it or a derivation from it. Disagreement becomes unrepresentable." },
    ],
    recap: [
      "Lift state to the nearest common ancestor when two+ components share it; pass down, report up.",
      "Data down, events up — the owner is the only writer.",
      "Derive anything computable; storing derived values invites drift.",
      "A copy-effect is a smell: it means duplicated truth, not missing sync.",
      "Local → lift → context, in that order; prop drilling is honest pain only when deep.",
    ],
    references: [
      { label: "react.dev — Lifting State Up", url: "https://react.dev/learn/lifting-state-up" },
      { label: "react.dev — Choosing the State Structure", url: "https://react.dev/learn/choosing-the-state-structure" },
      { label: "react.dev — Sharing State Between Components", url: "https://react.dev/learn/sharing-state-between-components" },
      { label: "react.dev — Thinking in React", url: "https://react.dev/learn/thinking-in-react" },
      { label: "react.dev — Passing Data Deeply with Context (preview)", url: "https://react.dev/learn/passing-data-deeply-with-context" },
    ],
    nextBridge:
      "You know where state lives. Last lesson of the phase: HOW React turns your descriptions into a DOM — what triggers a render, how identity (position and keys) decides what survives a re-render, what Strict Mode's double-invocation is really checking, and a debugging lab for the phantom re-render.",
  },
  {
    id: "p04-l7",
    phaseId: "p04",
    title: "Reconciliation, Identity & Strict Mode",
    level: "React Developer",
    minutes: 35,
    summary:
      "Re-rendering is cheap because React doesn't rebuild — it reconciles: it diffs your new description against the old and patches only what changed, using element type, position, and keys to decide what is 'the same thing.' This lesson walks the full list of what causes a render, how positional identity and key identity determine what survives, what Strict Mode's double-invocation is really protecting you from, and how to read React DevTools — capped by a debugging lab for the phantom re-render.",
    prerequisites: ["p04-l6 — lifting state & derived state", "p04-l4 — lists & keys"],
    objectives: [
      "List what causes a component to re-render: its own state change, a parent re-render, or a context change.",
      "Explain reconciliation as diff-and-patch: React reuses what matches, replaces what doesn't.",
      "Distinguish positional identity (same slot, same type = same instance) from key identity (named instances that survive movement).",
      "Predict when state resets vs persists across re-renders based on identity.",
      "Explain Strict Mode's double-invocation as an impurity detector, not a bug.",
      "Profile with React DevTools and diagnose a phantom re-render with the nine-step method.",
    ],
    simple:
      "Every time state changes, React calls your component function again to get a fresh description — but it does NOT tear down and rebuild the page. It compares the new description to the old one, element by element, and only touches the DOM where they differ: change this text, swap that class, insert this node. To know whether two elements across renders are 'the same thing,' React checks their type and their position (or their key, for lists). Same type in the same spot = keep the instance and its state; different type or different key = throw it out and make a new one. Your job is just to produce honest descriptions; React does the minimal surgery.",
    why:
      "This model answers the questions that otherwise make React feel random: 'Why did my input lose its value when I toggled that thing?' (identity changed, state reset), 'Why did my effect run twice?' (Strict Mode), 'Is this component re-rendering too much?' (DevTools). It's also the bridge to everything ahead: memoization (Phase 6) only makes sense once you know what a re-render is and isn't; effect cleanup (Phase 5) only makes sense once you know when React tears an instance down. Developers who understand reconciliation stop fearing re-renders and start reasoning about identity — which is the difference between using React and driving it.",
    mentalModel: {
      title: "The stage manager with a script diff",
      body: "Think of the DOM as a theater stage and each render as handing the stage manager a new script. A naive manager would clear the stage and rebuild every prop from scratch each time — expensive and jarring. React's manager reads the new script against the old one and issues the SMALLEST set of changes: 'the lamp stays, move the chair two feet left, swap the red curtain for blue.' To know the chair is still THE chair, it checks the tag: same type of prop in the same position (or with the same labeled key) means keep it — and whatever was inside it (state) stays put. Change the tag and it's a brand-new prop, delivered empty. Re-rendering is just a script diff; the stage barely notices.",
    },
    sections: [
      {
        heading: "What causes a render — the full list",
        body: [
          "A component re-renders when: (1) its OWN state changes (a setter it called), (2) its PARENT re-renders (children render when their parent does, by default — receiving possibly-new props), or (3) a CONTEXT it consumes changes. That's the whole list. Notably, a prop changing value isn't itself a trigger — it's the parent's re-render that cascades down, whether or not the child's props actually differ. (Memoization in Phase 6 lets a child opt out of that cascade when its props are unchanged.)",
          "The key reframe: re-rendering is normal and cheap. Calling your component function is just producing a description — React only touches the DOM for the parts that actually changed. The performance enemy is not 're-rendered,' it's 're-rendered AND did expensive work' or 're-rendered constantly for no reason.' Measure before optimizing; identity and memoization are surgical tools, not defaults.",
        ],
        code: [
          {
            file: "triggers.tsx — the three render triggers",
            lang: "tsx",
            code: [
              "function Parent() {",
              "  const [n, setN] = useState(0);",
              "  // (1) setN → Parent re-renders.",
              "  return (",
              "    <div>",
              "      <button onClick={() => setN(n + 1)}>{n}</button>",
              "      {/* (2) Parent re-rendered, so Child re-renders too —",
              "          even though its props never changed. */}",
              "      <Child label=\"hello\" />",
              "    </div>",
              "  );",
              "}",
              "",
              "function Child({ label }: { label: string }) {",
              "  console.log(\"Child rendered\"); // logs on every Parent render",
              "  return <p>{label}</p>;",
              "}",
              "// (3) Context is the third trigger — covered in Phase 5.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Reconciliation — diff and patch, not rebuild",
        body: [
          "React reconciles by walking the new element tree against the old. For each position it asks: same element type? If yes, keep the underlying instance (and its state), update only the changed props/attributes/text. If the type differs — even 'visually similar' types like <div> vs <section>, or <TextFieldA> vs <TextFieldB> — React unmounts the old subtree entirely and mounts a fresh one: all state inside is destroyed. This is why swapping a component's type at the same position resets everything nested in it.",
          "The practical consequence: the shape of your JSX is meaningful. Conditionally rendering two different components in the same slot (cond ? <FormA/> : <FormB/>) produces two distinct instances that never share state — usually what you want. But accidentally changing a component's type (e.g., wrapping it in a new element, or switching between two similar components) silently resets its state, because React sees 'different thing, new instance.' Identity is type + position, and both matter.",
        ],
        code: [
          {
            file: "identity.tsx — same slot, different type = reset",
            lang: "tsx",
            code: [
              "function Editor({ mode }: { mode: \"a\" | \"b\" }) {",
              "  // Same position, but DIFFERENT component types:",
              "  return mode === \"a\" ? <FormA /> : <FormB />;",
              "}",
              "// Toggling mode unmounts one and mounts the other —",
              "// any state inside FormA/FormB is discarded. (Usually desired.)",
              "",
              "function Sneaky({ flag }: { flag: boolean }) {",
              "  // Same TYPE, same position → React keeps the instance.",
              "  return flag ? <input placeholder=\"on\" /> : <input placeholder=\"off\" />;",
              "}",
              "// Toggling flag keeps the SAME <input> instance — only the",
              "// placeholder prop updates. Typed text SURVIVES, because the",
              "// element type and position didn't change.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Keys — named identity that survives movement",
        body: [
          "Within a list, position alone is a bad identity, because positions shift. Keys give items a stable NAME so React can match them across renders regardless of order. With keys, reordering moves instances (and their state) to their new positions; without keys (or with index keys), React matches by position and reuses the wrong instance for the wrong data — the corruption you saw in Phase 4-L4. The rule stands: keys are stable, unique, and derived from data (an id), never the index for mutable lists.",
          "A subtler identity fact: you can DELIBERATELY reset a component's state by changing its key. Because a new key = new instance, key={someId} on a form means 'fresh form whenever someId changes.' This is a legitimate, idiomatic tool for 'reset this subtree when the logical entity changes' — far cleaner than wiring reset logic by hand. Same mechanism (key ⇒ identity), used on purpose.",
        ],
        code: [
          {
            file: "keys-identity.tsx — movement vs reset",
            lang: "tsx",
            code: [
              "// Movement: keys let instances (and their state) travel.",
              "{todos.map((t) => <TodoRow key={t.id} todo={t} />)}",
              "// Reorder todos → each TodoRow's internal state moves WITH its id.",
              "",
              "// Deliberate reset: a new key = a brand-new instance.",
              "<InvoiceForm key={invoice.id} invoice={invoice} />",
              "// Switching to a different invoice changes the key → React",
              "// discards the old form instance (its draft state included)",
              "// and mounts a fresh one. Reset-by-identity, no manual wiring.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Strict Mode — the impurity detector",
        body: [
          "In development, <StrictMode> deliberately double-invokes your component functions and certain hook lifecycles (mount → unmount → mount for effects). It renders nothing extra to the DOM — it's running your code twice to smoke out impurities: a render that mutates external state, an effect that doesn't clean up, a ref read during render. If your component is pure (same inputs → same output, no side effects in the render), the double-invocation is invisible. If it isn't, the bug surfaces loudly in development instead of quietly in production.",
          "The famous 'my effect ran twice' is Strict Mode doing its job: it mounts, unmounts (running your cleanup), and remounts — proving your effect can survive teardown. Effects that break under this (leaked subscriptions, timers, listeners) are genuinely broken; the fix is proper cleanup, never removing StrictMode. It runs only in development; production renders once. Treat double-invocation as a free test you didn't have to write.",
        ],
        code: [
          {
            file: "strict-mode.tsx — what the double-mount proves",
            lang: "tsx",
            code: [
              "function Clock() {",
              "  const [now, setNow] = useState(new Date());",
              "",
              "  useEffect(() => {",
              "    const id = setInterval(() => setNow(new Date()), 1000);",
              "    return () => clearInterval(id);   // ← cleanup",
              "  }, []);",
              "",
              "  return <time>{now.toLocaleTimeString()}</time>;",
              "}",
              "",
              "// Strict Mode (dev): mount → run effect → run CLEANUP → run effect.",
              "// With the cleanup, exactly ONE interval survives. Without it,",
              "// you'd leak a second interval — and Strict Mode just showed you.",
              "// Production mounts once; the cleanup is still correct either way.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Debugging lab — the phantom re-render",
        body: [
          "The scenario: an input feels laggy; something is re-rendering constantly. Nine steps. READ: the input stutters per keystroke. LAYER: frontend render path. REPRODUCE: open React DevTools Profiler, check 'Highlight updates,' type — a large subtree flashes every keystroke. GATHER: the Profiler's 'why did this render?' says 'parent rendered.' HYPOTHESIZE: a single keystroke updates a high-level state that the whole page consumes, so every render cascades everywhere. CHANGE ONE THING: lift only the fast-changing state (the draft text) DOWN into the input's own component, or memoize the heavy subtree. VERIFY: type again — only the input re-renders. ROOT CAUSE: state owned too high fanned every keystroke out to the whole tree. PREVENT: keep rapidly-changing state as local as possible; measure with the Profiler before and after any memoization.",
          "The discipline this installs: never guess at render performance. React DevTools tells you WHO rendered and WHY; the Profiler tells you what it COST. Only then do you choose a tool — moving state, memo, or splitting components. Premature memoization without a measurement is decoration, not engineering.",
        ],
      },
    ],
    mistake: {
      title: "Blaming 'too many re-renders' and spraying memo",
      wrong: [
        "// The input lags, so 'optimize' everything in sight:",
        "const Child = memo(function Child({ items }: Props) { … });",
        "const list = useMemo(() => items.map(…), [items]);",
        "const handler = useCallback(() => { … }, []);",
        "// …wrapped around a component that re-renders anyway because its",
        "// PARENT re-renders with a new object prop every time.",
        "// memo/useMemo/useCallback add cost, obscure the code, and fix",
        "// nothing — because the re-render was never the actual problem.",
      ].join("\n"),
      right: [
        "// 1. MEASURE: React DevTools Profiler — who renders, why, what cost?",
        "//    Finding: the whole page re-renders per keystroke because the",
        "//    draft text is state owned by the page root.",
        "",
        "// 2. FIX THE CAUSE: keep the fast-changing state local.",
        "function SearchBox({ onSearch }: { onSearch: (q: string) => void }) {",
        "  const [draft, setDraft] = useState(\"\"); // owned HERE, not at the root",
        "  return (",
        "    <input",
        "      value={draft}",
        "      onChange={(e) => setDraft(e.target.value)}",
        "      onBlur={() => onSearch(draft)}",
        "    />",
        "  );",
        "}",
        "// Now typing re-renders only SearchBox. No memo needed at all.",
      ].join("\n"),
      explain:
        "A re-render is just your component function running — cheap unless the work inside is expensive or the renders are pointless. Memoization can't stop a child from re-rendering when its parent hands it new props every time, so sprinkling memo around a structural problem buys nothing and adds complexity. The real fix is usually architectural: own fast-changing state locally so changes don't cascade. Measure with the Profiler first; reach for memo only when a measurement says so.",
    },
    tryIt: [
      "Enable 'Highlight updates when components render' in React DevTools on any app you've built. Type into an input and watch which components flash. Trace each flash to one of the three triggers.",
      "Render {flag ? <input/> : <div/>} and toggle flag — confirm the input's typed text vanishes (type changed). Then render two <input/>s in the same slot and confirm the text survives (same type).",
      "Build a list of rows with per-row local state (a checkbox). Reorder the list with id keys and confirm the checks travel; switch to index keys and confirm they scramble.",
      "Use key={id} on a form to force a reset when switching between two records. Confirm the draft clears without any reset logic.",
      "Wrap an effect-based component in StrictMode and confirm the effect logs mount/cleanup/mount. Remove the cleanup and watch the leak the double-mount reveals.",
    ],
    challenge: {
      prompt:
        "Build a mini 'settings page' that demonstrates the whole render model: (1) a text input for a display name whose draft state is owned LOCALLY by the input component (so typing doesn't re-render the page); (2) a 'Save' that lifts the committed name up to the page on submit; (3) a list of three toggles keyed by id, each with independent local on/off state, that survives a 'Shuffle' button reordering them; (4) a Profiler note: in two sentences, state what re-renders when you type, and what re-renders when you Save — and why that's the correct split.",
      hints: [
        "NameInput owns `draft`; the page owns `displayName`. NameInput calls onSave(draft) on submit. Typing touches only NameInput's state.",
        "Toggles: rows = [{id, label}], each row a ToggleRow with local useState(true). Key by id. Shuffle reorders the array; local state must travel with each id.",
        "Use React DevTools 'Highlight updates' to verify: typing flashes only the input; Save flashes the page (displayName changed) and anything reading it.",
      ],
      solution: [
        "import { useState } from \"react\";",
        "",
        "// Local owner of the fast-changing draft — typing re-renders ONLY this.",
        "function NameInput({ onSave }: { onSave: (name: string) => void }) {",
        "  const [draft, setDraft] = useState(\"\");",
        "  return (",
        "    <form",
        "      onSubmit={(e) => {",
        "        e.preventDefault();",
        "        onSave(draft);",
        "      }}",
        "    >",
        "      <input value={draft} onChange={(e) => setDraft(e.target.value)} />",
        "      <button type=\"submit\">Save</button>",
        "    </form>",
        "  );",
        "}",
        "",
        "// Each row owns its own on/off state; keyed by id so it survives reorder.",
        "function ToggleRow({ label }: { label: string }) {",
        "  const [on, setOn] = useState(true);",
        "  return (",
        "    <li>",
        "      {label}: {on ? \"on\" : \"off\"}",
        "      <button type=\"button\" onClick={() => setOn((v) => !v)}>toggle</button>",
        "    </li>",
        "  );",
        "}",
        "",
        "interface Row { id: number; label: string; }",
        "const INITIAL: Row[] = [",
        "  { id: 1, label: \"Notifications\" },",
        "  { id: 2, label: \"Sounds\" },",
        "  { id: 3, label: \"Newsletter\" },",
        "];",
        "",
        "function SettingsPage() {",
        "  const [displayName, setDisplayName] = useState(\"\");",
        "  const [rows, setRows] = useState<Row[]>(INITIAL);",
        "",
        "  function shuffle() {",
        "    setRows((prev) => [...prev].reverse()); // reorder; ids travel",
        "  }",
        "",
        "  return (",
        "    <section>",
        "      <h1>{displayName || \"Your settings\"}</h1>",
        "      <NameInput onSave={setDisplayName} />",
        "      <ul>",
        "        {rows.map((r) => (",
        "          <ToggleRow key={r.id} label={r.label} />",
        "        ))}",
        "      </ul>",
        "      <button type=\"button\" onClick={shuffle}>Shuffle</button>",
        "    </section>",
        "  );",
        "}",
        "",
        "// Profiler note: TYPING re-renders only NameInput, because the draft",
        "// state is owned there — the change never reaches the page. SAVING",
        "// re-renders SettingsPage (displayName changed), which re-renders its",
        "// children by default — correct, because the header genuinely depends",
        "// on the new name. The split is right: hot state is local, shared",
        "// (committed) state is lifted, so each change fans out no further",
        "// than the components that actually read it.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A component re-renders when…",
        options: ["Its own state changes, its parent re-renders, or a consumed context changes", "Any prop changes value", "The browser repaints", "It uses a hook"],
        answer: 0,
        explain: "Those are the three triggers. A prop changing isn't itself a trigger — it's the parent's re-render cascading down. Memoization (Phase 6) can opt a child out of that cascade.",
      },
      {
        q: "Reconciliation is best described as…",
        options: ["Diffing the new description against the old and patching only what changed", "Rebuilding the whole DOM each render", "Re-running the browser's HTML parser", "A full page reload"],
        answer: 0,
        explain: "Your component function just produces a description; React compares it to the previous one and makes minimal DOM changes. Re-rendering the function is cheap; DOM churn is what's minimized.",
      },
      {
        q: "Swapping <FormA/> for <FormB/> in the same position…",
        options: ["Unmounts and remounts — all nested state resets, because the type changed", "Keeps state, since the position is the same", "Throws an error", "Does nothing"],
        answer: 0,
        explain: "Identity is type + position. A different element type at the same position is a different instance, so React tears down the old subtree (destroying its state) and mounts fresh.",
      },
      {
        q: "Changing a component's key deliberately…",
        options: ["Resets its state — a new key means a new instance", "Is a syntax error", "Improves performance always", "Has no effect"],
        answer: 0,
        explain: "New key = new identity = new instance with fresh state. It's an idiomatic tool for 'reset this subtree when the logical entity changes,' cleaner than manual reset wiring.",
      },
      {
        q: "Strict Mode's double-invocation exists to…",
        options: ["Expose impure renders and effects missing cleanup — in dev, not production", "Make apps slower", "Render the UI twice for users", "Test memoization"],
        answer: 0,
        explain: "It runs your code twice (and mounts/unmounts/remounts effects) to smoke out side effects in render and missing cleanup. Pure code is unaffected; production renders once.",
      },
      {
        q: "The first step when a UI feels slow from re-rendering is…",
        options: ["Measure with React DevTools Profiler — who renders, why, at what cost", "Wrap everything in memo", "Add useCallback everywhere", "Split every component"],
        answer: 0,
        explain: "A re-render is cheap unless the work is expensive or pointless. Measure to find the real cause (often state owned too high); memoization without measurement is decoration.",
      },
    ],
    flashcards: [
      { front: "The three render triggers", back: "Own state change, parent re-render, or consumed context change. A prop changing value isn't itself a trigger — it's the parent's cascade." },
      { front: "Reconciliation", back: "React diffs the new element tree against the old and patches only what changed. Your function produces a description; React does minimal DOM surgery." },
      { front: "Element identity", back: "type + position (or key, in lists). Same type in same slot = same instance, state persists. Different type = unmount + fresh mount, state resets." },
      { front: "Key identity", back: "A stable, unique, data-derived name for list items so they survive reordering with their state. Index keys break under reorder/insert/delete." },
      { front: "Reset-by-key", back: "Changing a key deliberately creates a new instance — an idiomatic way to reset a subtree when the logical entity changes (key={invoice.id})." },
      { front: "Strict Mode", back: "Dev-only double-invocation (renders + effect mount/unmount/mount) that exposes impure renders and missing cleanup. Production renders once. Never remove it to fix a leak." },
      { front: "Effect cleanup proof", back: "Strict Mode's mount→cleanup→mount shows whether your effect survives teardown. Leaked intervals/listeners surface in dev; the fix is proper cleanup." },
      { front: "Phantom re-render fix", back: "Measure (Profiler: who/why/cost) → usually move hot state down so changes don't cascade → verify. Memoize only when a measurement says so." },
    ],
    recap: [
      "Renders are triggered by own state, parent render, or context — and are cheap by default.",
      "Reconciliation = diff and patch; identity (type + position, or key) decides what survives.",
      "Type change or key change resets state; same type in same slot keeps it.",
      "Strict Mode double-invokes to catch impurity and missing cleanup — in dev only.",
      "Measure render problems with DevTools before optimizing; keep hot state local.",
    ],
    references: [
      { label: "react.dev — Render and Commit", url: "https://react.dev/learn/render-and-commit" },
      { label: "react.dev — Preserving and Resetting State", url: "https://react.dev/learn/preserving-and-resetting-state" },
      { label: "react.dev — Strict Mode", url: "https://react.dev/reference/react/StrictMode" },
      { label: "react.dev — React Developer Tools", url: "https://react.dev/learn/react-developer-tools" },
      { label: "react.dev — Keeping Components Pure", url: "https://react.dev/learn/keeping-components-pure" },
    ],
    nextBridge:
      "Phase 4 complete — you can build interactive, correctly-rendering React from scratch. Phase 5 goes deep on hooks: useState's queue and useEffect's synchronization model, refs, context, reducers, and memoization — ending in the Hooks Boss Battle. Bring the render model; you'll use it constantly.",
  },
];

export const LESSON_CONTENT_P4C: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P4C.map((l) => [l.id, l]),
);
