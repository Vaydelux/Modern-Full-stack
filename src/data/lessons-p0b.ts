import type { LessonContent } from "./types";

/**
 * Pass 002 bounded content batch: Phase 0 completion (L4–L5).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P0B: LessonContent[] = [
  {
    id: "p00-l4",
    phaseId: "p00",
    title: "DevTools & the Debugging Mindset",
    level: "Foundation",
    minutes: 30,
    summary:
      "DevTools is the X-ray machine built into every browser. This lesson teaches the three panels you will use daily — Console, Network, Sources — and installs the nine-step debugging loop that turns 'it broke' into 'here is the root cause'.",
    prerequisites: [
      "p00-l2 — terminal, Node, and Git are set up",
      "p00-l3 — you built a tiny Node server and read its errors",
    ],
    objectives: [
      "Use the Console as an instrument: log levels, console.table, object inspection, $0.",
      "Read a stack trace top-down and jump to the exact file and line that threw.",
      "Inspect any request in the Network tab: status, headers, payload, response, timing.",
      "Replace console.log soup with breakpoints, conditional breakpoints, and logpoints.",
      "Run the nine-step debugging loop on a real defect and state the root cause in one sentence.",
    ],
    simple:
      "Your browser hides a control room behind the F12 key. One panel shows every message and error your code printed (Console). Another records every conversation the page had with a server (Network). A third lets you pause your code mid-sentence and stare at every variable (Sources). Learning these three panels — plus a disciplined loop for using them — is the difference between 'I changed random things until it worked' and 'I know exactly why it broke and why it is fixed'.",
    why:
      "Debugging is the most frequent activity in a professional developer's day, yet it is almost never taught as a method. Beginners guess, sprinkle console.logs, and hard-refresh until the symptom disappears — which means the bug returns, because nobody knows the cause. Professionals treat every defect as a small investigation with evidence. The nine-step loop in this lesson is the same method every Debugging Lab in this course will use, whether the bug lives in React, NestJS, Prisma, or DNS.",
    mentalModel: {
      title: "The Detective's Loop",
      body: "A crime-scene investigator does not rearrange the furniture and hope. She reads the report (the error), walks the scene (the stack trace), gathers evidence (Network, Scope, logs), names a suspect (hypothesis), runs exactly one test (one change), and only then writes the case up (root cause + prevention). DevTools is your forensics kit. The loop below is your procedure — and 'one change at a time' is the rule that keeps the investigation honest.",
    },
    sections: [
      {
        heading: "Open the control room",
        body: [
          "Press F12 (or Cmd+Option+I on macOS) and the DevTools drawer opens. Dock it to the right or bottom so code and instruments share the screen — professionals keep it open most of the day. The tabs that matter this week: Console, Network, Sources, Elements. Application arrives later for cookies and storage.",
          "The Console is not just a printer — it executes JavaScript in your page's context. You can type expressions, call your own functions, and inspect live objects while the page runs. Two console utilities earn a permanent spot in your muscle memory: $0 refers to whatever element you last selected in the Elements panel, and copy(value) puts any value on your clipboard.",
        ],
        code: [
          {
            file: "devtools-lab.js — load from a tiny HTML page, then open the Console",
            lang: "js",
            code: [
              "const users = [",
              "  { name: \"Ada\", role: \"admin\", active: true },",
              "  { name: \"Grace\", role: \"member\", active: false },",
              "];",
              "",
              "console.log(\"plain log — the default\");",
              "console.warn(\"warn — something suspicious\");",
              "console.error(\"error — something failed\");",
              "console.table(users);        // spreadsheets for arrays/objects",
              "console.group(\"checkout\");   // collapse related logs together",
              "console.log(\"cart has 2 items\");",
              "console.log(\"total\", 49.9);",
              "console.groupEnd();",
              "",
              "// Now try these in the Console itself (DevTools utilities):",
              "//   $0            → the element you last clicked in the Elements panel",
              "//   copy(users)   → the value, on your clipboard",
              "//   users.length  → the Console runs real JS in the page's context",
            ].join("\n"),
            caption: "console.table is the single biggest Console upgrade most beginners never discover.",
          },
        ],
      },
      {
        heading: "Read errors like a map: stack traces",
        body: [
          "An error message tells you what failed; the stack trace tells you where, and how the code got there. Each 'at ...' line is a stack frame: the top frame is where execution actually died, and each frame below is a caller further back. Every frame carries file:line:column — and in DevTools each one is a link that opens that exact line in Sources.",
          "Reading top-down answers 'where'. Reading bottom-up answers 'how did the program arrive here with bad data'. Most beginners read only the first line of the message and start guessing; professionals read the trace like a route map before touching anything.",
        ],
        code: [
          {
            file: "broken.js — paste into an HTML page, then read the error like a pro",
            lang: "js",
            code: [
              "function renderUser(user) {",
              "  return `Hi, ${user.name}`;   // line 2: explodes when user is undefined",
              "}",
              "",
              "function main() {",
              "  const found = [\"ada\", \"grace\"].find((n) => n === \"margaret\"); // no match → undefined",
              "  console.log(renderUser(found));",
              "}",
              "",
              "main();",
              "",
              "// The Console prints:",
              "// Uncaught TypeError: Cannot read properties of undefined (reading 'name')",
              "//     at renderUser (broken.js:2:22)   ← the throw site (start here)",
              "//     at main (broken.js:7:15)         ← who called it",
              "//     at broken.js:10:1                ← the entry point",
              "// Click any frame: DevTools opens that exact file and line.",
            ].join("\n"),
            caption: "This exact error — reading a property of undefined — will visit you weekly for years. Make friends with it.",
          },
        ],
      },
      {
        heading: "The Network tab: who said what",
        body: [
          "The Network tab is a wiretap on every conversation the page has: documents, stylesheets, images, and — the interesting ones — fetch/XHR calls to APIs. Open the tab before you reload, because it only records what happens while it is open. Click any row to inspect: Headers shows the true status code and content types, Payload shows what you sent, Response and Preview show what came back, Timing shows where the milliseconds went.",
          "Two checkboxes prevent the two most common beginner confusions. 'Preserve log' keeps history across page navigations. 'Disable cache' forces fresh downloads while DevTools is open — because a stale cached file is the classic cause of 'my change does nothing'. This tab is also how you settle arguments with the server: when your code insists it sent JSON but the API disagrees, the Payload tab is the referee. It extends everything you saw with curl in the previous lesson.",
        ],
        code: [
          {
            file: "network-lab.js — make traffic, then interrogate it",
            lang: "js",
            code: [
              "fetch(\"https://api.github.com/users/torvalds\")",
              "  .then((res) => {",
              "    console.log(res.status, res.headers.get(\"content-type\"));",
              "    return res.json();",
              "  })",
              "  .then((user) => console.log(user.name));",
              "",
              "// In the Network tab the whole story is now recorded for free:",
              "// 1. Filter 'Fetch/XHR' to hide images and fonts.",
              "// 2. Click the request → Headers: status 200, request/response headers.",
              "// 3. → Preview: the JSON, already formatted.",
              "// 4. → Timing: DNS + connect + waiting (TTFB) + download.",
              "// 5. ✓ Preserve log — navigation won't wipe the evidence.",
              "// 6. ✓ Disable cache — your edits can never be hidden by stale files.",
            ].join("\n"),
            caption: "Your console printed two facts; the Network tab recorded twenty more you didn't ask for.",
          },
        ],
      },
      {
        heading: "Breakpoints beat console.log",
        body: [
          "A breakpoint is a pause button set on a line of code. In the Sources panel, open your file and click a line number — reload the page and execution freezes exactly there. The Scope panel now shows every live variable (Local, Closure, Global), the Call Stack panel shows how you arrived, and the step buttons let you move deliberately: F10 steps over the current line, F11 steps into the function it calls, Shift+F11 finishes the current function and returns to the caller.",
          "Two upgrades make breakpoints strictly better than logging. A conditional breakpoint (right-click the line number → Add conditional breakpoint) pauses only when your expression is truthy — perfect for 'pause on the iteration that fails' instead of hitting Resume 47 times. A logpoint (Add logpoint) prints an expression to the Console every time the line runs, without pausing and without editing your source — so no probe ever leaks into a commit. console.log still has its place for quick one-off probes and async timing, but loops and state belong to breakpoints.",
        ],
        code: [
          {
            file: "breakpoints-lab.js — find the poisoned item without logging anything",
            lang: "js",
            code: [
              "const prices = [12, 45, \"19\", 33, 8];   // one of these is a string…",
              "",
              "function total(list) {",
              "  let sum = 0;",
              "  for (let i = 0; i < list.length; i++) {",
              "    sum += list[i];                      // ← set a breakpoint on this line",
              "  }",
              "  return sum;",
              "}",
              "",
              "console.log(total(prices)); // prints 10019 8 — string concatenation snuck in",
              "",
              "// With the breakpoint set:",
              "//  • Scope shows sum and i on every iteration.",
              "//  • Right-click the line number → 'Add conditional breakpoint' → i === 2",
              "//    pauses exactly on the poisoned iteration.",
              "//  • 'Add logpoint' → 'price', list[i], typeof list[i]",
              "//    logs every pass without pausing — and without touching your code.",
              "// Step with F10 and watch sum become a string. Caught red-handed.",
            ].join("\n"),
            caption: "The typeof check in the logpoint is the move that exposes the bug class: 'everything is a string until proven otherwise'.",
          },
        ],
      },
      {
        heading: "The nine-step loop — this course's official method",
        body: [
          "Every Debugging Lab from here to deployment runs the same procedure: (1) Read the error out loud — literally; half of all bugs die here. (2) Identify the layer: browser, network, server, database, or environment? (3) Reproduce it reliably — a bug you cannot summon on demand is not yet understood. (4) Gather evidence before forming opinions: the trace, the Network row, the Scope values. (5) State one hypothesis in one sentence. (6) Change exactly one thing. (7) Re-run the reproduction — did it behave as the hypothesis predicted? (8) Write the root cause in one sentence; if you cannot, return to step 4. (9) Prevent the regression: a test, a lint rule, a constraint, or at minimum a note where the next person will find it.",
          "Apply it to broken.js from section two: the trace says line 2 threw on an undefined user (evidence), so the layer is client JavaScript; the hypothesis is 'find() returned undefined because there is no Margaret'; the one change is handling the miss explicitly; the verification is re-running the original reproduction; the root cause is 'find() returns undefined on a miss, and renderUser assumed success'; prevention is a guard or a test covering unknown names. Twelve minutes with the loop beats two hours of shotgun reloading — and the bug stays dead.",
        ],
      },
      {
        heading: "Elements + the sixty-second accessibility peek",
        body: [
          "The Elements panel is live anatomy: select any node and the Styles pane shows which CSS rules won and which were crossed out, while Computed shows the final values. Since this course treats accessibility as core, start one habit now — the Accessibility pane (Elements → select a node → the 'Accessibility' sidebar) shows the computed Name, Role, and whether the element is keyboard-focusable. A button that reads 'group' with no name is a bug you can see in one click.",
          "Then do the keyboard tour: click the address bar, press Tab, and walk the whole page without a mouse. Whatever Tab cannot reach does not exist for keyboard users. Phase 1 builds the semantics that make this tour pleasant; today you only learn to see the problem.",
        ],
      },
    ],
    mistake: {
      title: "The shotgun reload",
      wrong: [
        "// 14 console.logs, three hard reloads, 'works now' (it doesn't)",
        "console.log(\"here?\");",
        "console.log(\"A\", a);",
        "console.log(\"B\", b);",
        "console.log(\"C\", c);   // …and ten more, left in the code",
        "// fix?? reload. reload again. clear cache. reload.",
      ].join("\n"),
      right: [
        "// One hypothesis, one instrument, one change",
        "// Hypothesis: `found` is undefined when the name is missing.",
        "// Evidence:   breakpoint shows found === undefined (Scope panel).",
        "// Change:     handle the miss.",
        "const found = names.find((n) => n === query);",
        "if (!found) {",
        "  console.warn(`No user named ${query}`);",
        "  return;",
        "}",
        "// Verify: re-run the ORIGINAL repro. Then delete the probe.",
      ].join("\n"),
      explain:
        "Random changes destroy your own evidence: if five things changed at once and the symptom vanished, you do not know which one mattered — so the bug returns in production wearing a disguise, and your 'fix' is a lie you committed to Git. One change at a time, instruments removed when the case closes, and always re-run the original reproduction before declaring victory.",
    },
    tryIt: [
      "Create lab.html with an inline <script> containing the broken.js snippet. Open it and read the stack trace out loud: where did it throw, and who called it?",
      "Open Sources, click line 2 of broken.js to set a breakpoint, and reload. Step with F10. What do user and found hold in the Scope panel?",
      "Convert the breakpoint into a conditional one that pauses only when user === undefined. Reload. How many times does it pause, and why exactly that many?",
      "Delete all your console.logs and repeat the investigation using a single logpoint on the throwing line.",
      "Run network-lab.js. In the Network tab find the GitHub request and record: status, content-type, and TTFB from the Timing tab.",
      "Tab through your page with the keyboard only. Which elements are reachable? Cross-check one of them in the Accessibility pane.",
    ],
    challenge: {
      prompt:
        "A teammate sends you this shopping-cart snippet. It should print the total price but prints 2550 instead of 75. Using DevTools only — no guessing — find the root cause, fix it, and write the one-sentence root cause.\n\nconst cart = [\n  { sku: \"kb-01\", price: \"25\" },\n  { sku: \"ms-02\", price: \"50\" },\n];\nfunction cartTotal(items) {\n  let total = 0;\n  for (const item of items) total += item.price;\n  return total;\n}\nconsole.log(cartTotal(cart)); // 2550 ?!",
      hints: [
        "Set a breakpoint inside the loop and watch total in the Scope panel. What is its type after the first iteration?",
        "In the Console, evaluate: '0' + 25. What does + do when either side is a string?",
        "Prices arrived as strings — JSON and form inputs always do. Where should the conversion happen so no downstream function re-discovers the problem?",
      ],
      solution: [
        "// Root cause: item.price is a string (\"25\"), and + concatenates",
        "// instead of adding when either operand is a string: 0 + \"25\" → \"025\".",
        "function cartTotal(items) {",
        "  let total = 0;",
        "  for (const item of items) total += Number(item.price);",
        "  return total;",
        "}",
        "console.log(cartTotal(cart)); // 75",
        "",
        "// The durable fix converts once, at the trust boundary where the",
        "// strings enter (parsing JSON, reading a form):",
        "const cart = rawCart.map((item) => ({ ...item, price: Number(item.price) }));",
        "",
        "// And a guard turns the whole bug class into a loud, early failure:",
        "if (typeof cart[0].price !== \"number\") throw new Error(\"price must be a number\");",
      ].join("\n"),
    },
    quiz: [
      {
        q: "In a stack trace, which frame do you read first?",
        options: [
          "The top frame — where the error was thrown",
          "The bottom frame — where the program started",
          "The middle frame — errors prefer the middle",
          "Frames don't matter; only the message",
        ],
        answer: 0,
        explain: "The top frame is where execution actually failed; the frames below show how the code got there. Each frame is a link to the exact file and line.",
      },
      {
        q: "What does a conditional breakpoint do?",
        options: [
          "Pauses only when your expression is truthy",
          "Pauses the entire browser",
          "Automatically fixes the bug",
          "Logs to the Network tab",
        ],
        answer: 0,
        explain: "Right-click a line number → Add conditional breakpoint. Ideal for 'pause on the failing iteration' instead of pressing Resume 47 times.",
      },
      {
        q: "Which panel answers 'did the server really return 404, or did my code fail earlier?'",
        options: ["Network", "Console", "Elements", "Application"],
        answer: 0,
        explain: "Network records the actual request/response exchange — status, headers, bodies. It is the evidence that separates server failures from client failures.",
      },
      {
        q: "What is a logpoint?",
        options: [
          "A log statement added from DevTools without editing your code",
          "A console.log that ships to production",
          "A breakpoint that rewrites your file",
          "A new error level",
        ],
        answer: 0,
        explain: "A logpoint prints expressions to the Console each time the line runs — your source stays untouched, so nothing leaks into a commit by accident.",
      },
      {
        q: "'Disable cache (while DevTools is open)' exists because…",
        options: [
          "Browsers reuse files aggressively, which can hide your latest edits",
          "The cache breaks HTTPS",
          "It makes the server faster",
          "Breakpoints require it",
        ],
        answer: 0,
        explain: "A stale cached file is a classic cause of 'my change does nothing'. With the box checked, the browser re-fetches on every load while DevTools is open.",
      },
      {
        q: "In the nine-step loop, what must happen before you change any code?",
        options: [
          "Reproduce the bug reliably and state one hypothesis",
          "Change several things at once to see what helps",
          "Rewrite the feature from scratch",
          "Clear the cache and hope",
        ],
        answer: 0,
        explain: "Reproducibility is evidence; one hypothesis plus one change keeps the experiment honest. Without both, 'fixed' is a rumor.",
      },
    ],
    flashcards: [
      { front: "Stack trace", back: "An error's itinerary: the top frame is where it threw, frames below are callers. In DevTools every frame links to the exact file and line." },
      { front: "Breakpoint", back: "A pause marker set on a line in Sources. Execution stops there so the Scope panel can show every live variable." },
      { front: "Conditional breakpoint", back: "A breakpoint that pauses only when your expression is truthy — perfect for 'the iteration that fails'." },
      { front: "Logpoint", back: "A DevTools-injected log on a line: prints without pausing and without editing source. Nothing ships by accident." },
      { front: "Scope panel", back: "Shows Local, Closure, and Global variables at a pause. First place to look when a value is not what you expected." },
      { front: "Step over / into / out", back: "F10 executes the current line; F11 descends into the function it calls; Shift+F11 finishes the current function and returns to the caller." },
      { front: "Network tab", back: "The wiretap: every request with status, headers, payload, response, timing. It decides whether the client or the server failed." },
      { front: "Nine-step debugging loop", back: "Read error → identify layer → reproduce → evidence → hypothesis → one change → verify → root cause → prevent. The course's official method." },
    ],
    recap: [
      "F12 opens the control room: Console talks, Network listens, Sources pauses.",
      "A stack trace reads top-down — throw site first, callers below — and every frame is a link.",
      "The Network tab settles 'server failed vs my code failed' with hard evidence.",
      "Breakpoints, conditionals, and logpoints replace console.log soup; delete your probes when the case closes.",
      "Run the nine-step loop on every bug so fixes are conclusions, not coincidences.",
    ],
    references: [
      { label: "Chrome DevTools — overview", url: "https://developer.chrome.com/docs/devtools" },
      { label: "Chrome DevTools — Console reference", url: "https://developer.chrome.com/docs/devtools/console" },
      { label: "Chrome DevTools — pause with breakpoints", url: "https://developer.chrome.com/docs/devtools/javascript/breakpoints" },
      { label: "Chrome DevTools — Network reference", url: "https://developer.chrome.com/docs/devtools/network" },
      { label: "Chrome DevTools — keyboard shortcuts", url: "https://developer.chrome.com/docs/devtools/shortcuts" },
    ],
    nextBridge:
      "You can now investigate anything the browser runs. The next lesson moves outside the code entirely: environment variables, secrets, and dev/staging/prod — the configuration mistakes that make correct code fail mysteriously.",
  },
  {
    id: "p00-l5",
    phaseId: "p00",
    title: "Environments, Secrets & Config Hygiene",
    level: "Foundation",
    minutes: 30,
    summary:
      "Code is public and shared; configuration is per-environment, and some of it is secret. This lesson teaches .env files, Node's native --env-file loader, fail-fast startup validation, and the discipline that keeps keys out of Git — learned now, before you have real keys to lose.",
    prerequisites: [
      "p00-l2 — Node, pnpm, and Git working",
      "p00-l3 — your tiny Node server runs",
      "p00-l4 — you can read errors and run the nine-step loop",
    ],
    objectives: [
      "Explain dev, staging, and production: same code, different configuration.",
      "Load environment variables with Node's --env-file (and know when dotenv is the ecosystem default).",
      "Validate required variables at startup so misconfiguration fails loudly in milliseconds.",
      "Keep secrets out of Git: .gitignore first, .env.example committed, leaked keys rotated immediately.",
      "Classify any variable as client-safe or server-only — and never blur the line.",
    ],
    simple:
      "An application has two halves. The code is the same everywhere and safe to share. The configuration changes per environment — your laptop, a preview server, production — and includes secrets like database passwords and API keys. Environment variables are the standard pouch for configuration: the operating system hands them to your program when it starts. You write yours in a .env file, tell Node to load it, and never commit the pouch to Git.",
    why:
      "Leaked secrets are one of the most common real-world breaches, and most start the same way: a .env committed to a public repository 'just for the weekend'. Automated scanners find such keys within minutes. Equally common is silent misconfiguration — apps that boot with PORT undefined and die mysteriously at 2 a.m. instead of announcing the problem at startup. The habits here (env files, startup validation, .env.example, rotation) are cheap on day one and brutal to retrofit after a leak.",
    mentalModel: {
      title: "Code is the recipe; the env is the kitchen",
      body: "The same recipe (code) works in every kitchen, but each kitchen has its own pantry: your home kitchen (dev) has cheap ingredients and duct tape; the test kitchen (staging) rehearses with real equipment; the restaurant kitchen (production) has the expensive oven and tonight's real orders. Environment variables are the pantry list handed to the recipe at the start of service — and some items on that list (the safe combination, the supplier contracts) are secrets you never tape to the front door. Same recipe, different pantries, strict rules about which list items may go public.",
    },
    sections: [
      {
        heading: "Three worlds, one codebase",
        body: [
          "Development is your machine: fast feedback, disposable data, secrets that unlock only test resources. Staging (or preview) is a dress rehearsal: production-like settings, real pipelines, safe-to-break data. Production is real users with real consequences. The professional trick is that the code is identical across all three — only the configuration differs: PORT, DATABASE_URL, API keys, feature flags, log levels.",
          "'Works on my machine' is never a law of nature; it is a configuration mismatch wearing a smug face. Naming the environments explicitly — and keeping a written map of which variable differs where — is what makes deploys boring instead of archaeological.",
        ],
      },
      {
        heading: ".env — the pouch, precisely",
        body: [
          "A .env file is plain text: one KEY=value per line, no spaces around the equals sign, # for comments. Every value arrives as a string — PORT=4000 gives you '4000', so convert at the boundary. Values containing spaces or # get quotes.",
          "Two loaders matter. Modern Node (20.6 and newer; ours is 22 LTS) loads env files natively with node --env-file=.env — zero dependencies, and that is the baseline this course teaches. The ecosystem classic is dotenv (pnpm add dotenv, then import 'dotenv/config' at the top of your entry file), which you will meet in countless tutorials and older tools; knowing both names means neither surprises you.",
        ],
        code: [
          {
            file: ".env — lives next to your code, never in Git",
            lang: "bash",
            code: [
              "PORT=4000",
              "GREETING=\"hello from the pouch\"",
              "# Secrets look like this (Phase 14 brings a real one):",
              "# DATABASE_URL=postgres://user:password@host:5432/db",
            ].join("\n"),
          },
          {
            file: "server-env.js — run with: node --env-file=.env server-env.js",
            lang: "js",
            code: [
              "import http from \"node:http\";",
              "",
              "// Env values are ALWAYS strings — convert at the boundary.",
              "const port = Number(process.env.PORT ?? 3000);",
              "",
              "http.createServer((req, res) => {",
              "  res.setHeader(\"content-type\", \"text/plain\");",
              "  res.end(process.env.GREETING ?? \"pouch is empty\");",
              "}).listen(port, () => {",
              "  console.log(`listening on http://localhost:${port}`);",
              "});",
              "",
              "// dotenv equivalent (older runtimes / tooling without the flag):",
              "//   pnpm add dotenv",
              "//   import \"dotenv/config\";   // first line of the entry file",
            ].join("\n"),
            caption: "Change PORT to 4001 in .env and restart: same code, different configuration.",
          },
        ],
      },
      {
        heading: "Fail loud, fail early: validate at startup",
        body: [
          "Without validation, a missing variable becomes a mystery: PORT is undefined, Number(undefined) is NaN, listen(NaN) throws something cryptic — or worse, the app boots and fails on the first request. With validation, the app dies in the first fifty milliseconds and tells you exactly which key is missing and where to look.",
          "The pattern is a single config module — the only doorway through which configuration enters the app. It reads, checks, converts, and exports a frozen object. Every other file imports config, never touches process.env, and gets typed values for free. Phase 10 formalizes this inside NestJS with environment validation, but the habit starts today.",
        ],
        code: [
          {
            file: "config.js — the app's one doorway for configuration",
            lang: "js",
            code: [
              "import assert from \"node:assert\";",
              "",
              "function required(name) {",
              "  const value = process.env[name];",
              "  assert.ok(",
              "    value && value.trim() !== \"\",",
              "    `Missing required env var: ${name}. Check your .env file.`,",
              "  );",
              "  return value;",
              "}",
              "",
              "export const config = Object.freeze({",
              "  port: Number(required(\"PORT\")),",
              "  greeting: required(\"GREETING\"),",
              "  // databaseUrl: required(\"DATABASE_URL\"),  ← Phase 14, same pattern",
              "});",
              "",
              "// Everything else imports config. If a key is missing, the app dies",
              "// in milliseconds with the exact name — not hours later mid-request.",
            ].join("\n"),
            caption: "Object.freeze stops later code from quietly mutating configuration at runtime.",
          },
        ],
      },
      {
        heading: "Secret discipline — the rules that keep keys out of the news",
        body: [
          "Rule one: .env goes into .gitignore before .env exists — before, not after. Rule two: commit a .env.example that lists every key with safe dummy values; it is the map without the treasure, and it is how teammates and CI learn the shape. Rule three: if a secret ever lands in Git, treat it as burned — rotate it immediately, then untrack the file (git rm --cached .env) and commit. Scrubbing history is optional triage; rotation is mandatory, because anything that lived in Git history must be considered public.",
          "Rule four: never log process.env or whole config objects — logs cross trust boundaries, exactly the point the previous lesson's server already made. Log key names, or masked previews like STRIPE_KEY=sk_...****. Rule five: rotation is normal maintenance, not a punishment. Keys you can rotate in five minutes stop being terrifying.",
        ],
        code: [
          {
            file: "terminal — pouch hygiene from day one",
            lang: "bash",
            code: [
              "# .gitignore — written BEFORE .env exists",
              "node_modules/",
              ".env",
              ".env.local",
              "dist/",
              "",
              "# .env.example — committed: every key, safe dummy values",
              "PORT=3000",
              "GREETING=\"change me\"",
              "# DATABASE_URL=postgres://user:password@localhost:5432/app",
              "",
              "# If .env already slipped into Git:",
              "git rm --cached .env            # untrack it; the file stays on disk",
              "git commit -m \"chore: stop tracking .env\"",
              "# …then rotate every secret it contained. Rotation is the real fix:",
              "# anything that has been in Git history is public, forever.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Client-safe vs server-only — the line that never blurs",
        body: [
          "Anything that reaches the browser is public. Full stop — it can be read in DevTools by anyone, including attackers. Server-only means the value never enters a browser bundle: DATABASE_URL, service-role keys, signing secrets, provider API keys with write power. Client-safe means public by design: the API's base URL, publishable keys (Supabase's anon key is built to be public), feature flags.",
          "Tooling encodes this line with prefixes: Next.js only ships NEXT_PUBLIC_-prefixed variables to the browser; Vite uses VITE_. A missing prefix keeps a value server-side. Phases 8 and 9 make this machinery concrete — but the rule is active today: if a value would grant power when leaked, it is server-only, and a browser needing it means the architecture is wrong. That is precisely why NestJS sits between the browser and the database in this course.",
        ],
      },
    ],
    mistake: {
      title: "'It's just a hobby project'",
      wrong: [
        "git add .               # no .gitignore yet — the pouch rides along",
        "git commit -m \"everything so far\"",
        "git push origin main    # the repo is public. So is the database password.",
        "# \"I'll delete the file tomorrow.\"",
        "# (Automated secret scanners found it in minutes. There is no tomorrow.)",
      ].join("\n"),
      right: [
        "# Day one, BEFORE .env exists:",
        "echo \".env\" >> .gitignore",
        "git add .gitignore && git commit -m \"chore: ignore env files\"",
        "",
        "# .env.example carries the key names; .env stays home:",
        "git add .env.example",
        "git commit -m \"chore: document required env vars\"",
        "",
        "# If it already slipped in: rotate every exposed secret FIRST,",
        "# then git rm --cached .env and commit. History scrubbing is",
        "# optional triage; rotation is mandatory.",
      ].join("\n"),
      explain:
        "Git is permanent and public-by-default: push a secret to any public repository and scanning bots extract it within minutes — deleting the file 'later' changes nothing, because history and clones survive. Order matters too: untracking the file without rotating the key still leaves the key public. Rotation first, always.",
    },
    tryIt: [
      "In a scratch folder: pnpm init, write the .env and server-env.js from section two, then run node --env-file=.env server-env.js. Visit the printed URL.",
      "Delete the PORT line from .env and restart. What happens? Now add config.js from section three and restart — compare the two failure messages.",
      "git init, write .gitignore before creating .env, then run git status. Prove .env is invisible to Git while .env.example is visible.",
      "Stage .env on purpose (git add -f .env), then undo it correctly and commit. Watch how git status changes at each step.",
      "Temporarily add console.log(process.env) to the server and run it. Look at the output. Now delete the line and write down why logging the whole env is a leak.",
    ],
    challenge: {
      prompt:
        "A friend's app crashes in production with 'Missing required env var: STRIPE_KEY', yet it 'worked on my laptop'. They send a screenshot of their laptop terminal — with the full console.log(process.env) output visible, real STRIPE_KEY included. Diagnose: (1) why did production crash, (2) what just happened to that key, (3) write the exact recovery steps in order.",
      hints: [
        "Production runs the same code with a different pouch. What is missing from the production environment — and which habit from section three detected it?",
        "Where has that screenshot been? Who can read the chat it was posted in? What does that make the key, legally and practically?",
        "Recovery has two independent parts: restoring service and retiring the exposed key. Which one is the emergency, and why does order matter?",
      ],
      solution: [
        "1) Why it crashed: env vars are per-environment. The laptop's .env had",
        "   STRIPE_KEY; the production host never received it. The fail-fast",
        "   config did exactly its job — the real bug is config drift.",
        "   Fix: set STRIPE_KEY in the production host's environment",
        "   (hosting dashboard / secret manager), redeploy, verify.",
        "",
        "2) What happened to the key: it was printed to a terminal and shared",
        "   as a screenshot — a chat log is a publication. The key must be",
        "   considered compromised from the moment it left the laptop.",
        "",
        "3) Recovery, in order:",
        "   a. Rotate STRIPE_KEY in the Stripe dashboard NOW (the emergency).",
        "   b. Put the NEW key in production, redeploy, verify a charge flow",
        "      in staging first.",
        "   c. Delete the screenshot from the chat; assume copies exist.",
        "   d. Redact the logger: never console.log(process.env). Log key",
        "      NAMES, or a masked preview: STRIPE_KEY=sk_...****.",
        "   e. Prevent: .env.example + fail-fast validation (already caught",
        "      it!) + a code-review rule: no raw env logging, ever.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "What belongs in .env.example but never in .env?",
        options: ["Real secret values", "Key names with dummy values", "Compiled code", "Git history"],
        answer: 1,
        explain: "The example file is the map: every required key with safe placeholders, committed so teammates and CI know the shape. The real pouch stays untracked.",
      },
      {
        q: "node --env-file=.env requires…",
        options: ["Node 20.6 or newer (ours is 22 LTS)", "The dotenv package", "Windows PowerShell", "A special file extension"],
        answer: 0,
        explain: "Modern Node loads env files natively. dotenv remains the ecosystem classic for older runtimes and tooling that doesn't pass the flag.",
      },
      {
        q: "Why validate env vars at startup?",
        options: [
          "Misconfiguration then crashes loudly in milliseconds with the exact missing name",
          "It makes the app slower but safer",
          "Node requires it",
          "It converts .env into JSON",
        ],
        answer: 0,
        explain: "Fail-fast turns 'mystery crash at 2 a.m.' into 'Missing required env var: PORT' at boot — before a single user is affected.",
      },
      {
        q: "A teammate committed .env an hour ago. First action?",
        options: [
          "Rotate every secret it contained",
          "Delete the file from disk",
          "Rename the repository",
          "Force-push an empty history",
        ],
        answer: 0,
        explain: "Untracking, renaming, and history rewrites do not un-publish what scanners already copied. Retire the keys first; cleanup is secondary.",
      },
      {
        q: "Which variable is always server-only?",
        options: ["DATABASE_URL", "The public API base URL", "A publishable 'anon' key designed for browsers", "A UI feature flag"],
        answer: 0,
        explain: "A connection string is a master key to your memory layer. If a browser ever needs one, the architecture is wrong — which is exactly why NestJS sits in the middle.",
      },
      {
        q: "Values in process.env are always…",
        options: ["Strings", "Numbers", "Booleans", "Typed by the .env parser"],
        answer: 0,
        explain: "PORT=4000 arrives as '4000'. Convert at the boundary — ideally once, inside the config module.",
      },
    ],
    flashcards: [
      { front: "Environment variable", back: "A KEY=value pair the OS hands a process at startup. Configuration, not code — different in every environment." },
      { front: ".env", back: "The local pouch of environment variables. Plain text, untracked, never committed. Node ≥20.6 loads it with --env-file=.env." },
      { front: ".env.example", back: "The committed map: every required key with dummy values. Teammates and CI copy it to build their own .env." },
      { front: "Fail-fast config", back: "Startup validation that throws on missing/empty vars — misconfiguration dies in milliseconds with the exact name, not mid-request." },
      { front: "Secret rotation", back: "Replacing a credential and rolling the new one out. Mandatory after any exposure; routine hygiene even without one." },
      { front: "Server-only variable", back: "Any value that would grant power if public: DB URLs, service keys, signing secrets. Never in a browser bundle." },
      { front: "Client-safe variable", back: "Public by design: API base URLs, publishable keys (Supabase anon), feature flags. Still validated, never secret." },
      { front: "Config drift", back: "'Works on my machine, crashes in prod' — same code, different pouches. Cured by .env.example + fail-fast validation + environment parity." },
    ],
    recap: [
      "Same code, three kitchens: dev, staging, production differ by configuration, never by source.",
      ".env holds the pouch; node --env-file=.env loads it; every value arrives as a string.",
      "config.js validates once at startup — missing keys crash loudly before the first request.",
      ".gitignore first, .env.example committed, leaked secrets rotated immediately — in that order.",
      "Client-safe vs server-only is a hard line: everything in a browser bundle is public.",
    ],
    references: [
      { label: "Node.js CLI — --env-file", url: "https://nodejs.org/api/cli.html#--env-fileconfig" },
      { label: "dotenv — README", url: "https://github.com/motdotla/dotenv" },
      { label: "Next.js — environment variables", url: "https://nextjs.org/docs/app/building-your-application/configuring/environment-variables" },
      { label: "Git — gitignore documentation", url: "https://git-scm.com/docs/gitignore" },
      { label: "GitHub — secret scanning", url: "https://docs.github.com/en/code-security/secret-scanning" },
    ],
    nextBridge:
      "The foundation is complete: the map, the toolkit, raw HTTP, the debugging method, and configuration discipline. Phase 1 turns outward — semantic HTML and landmarks, the structure that makes pages navigable for humans, screen readers, and search engines alike.",
  },
];

export const LESSON_CONTENT_P0B: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P0B.map((l) => [l.id, l]),
);
