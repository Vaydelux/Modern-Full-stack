import type { LessonContent } from "./types";

/**
 * Pass 020 bounded content batch: Phase 7 continues (L3–L4).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P7B: LessonContent[] = [
  {
    id: "p07-l3",
    phaseId: "p07",
    title: "Cookies, Sessions & Bearer Tokens",
    level: "Frontend Developer",
    minutes: 40,
    summary:
      "HTTP has amnesia: every request arrives as a stranger. Identity must therefore travel WITH each request — and the web grew two carriers for it: cookies (a browser-managed pouch with security flags) and bearer tokens (self-contained passports, usually JWTs). This lesson installs the ledger-vs-passport model, the four cookie flags that turn a crumb into a vault, JWT anatomy — and the sentence that saves careers: decode is not verify.",
    prerequisites: ["p07-l2 — methods, status codes & headers", "p07-l1 — request/response anatomy", "p03-l4 — trust boundaries (unknown vs assertions)"],
    objectives: [
      "Explain stateless HTTP and why identity must be carried by every request.",
      "Read a Set-Cookie line and defend each flag: HttpOnly, Secure, SameSite, Max-Age.",
      "Contrast the session (ledger + receipt) and token (passport) models and pick by revocation/scale needs.",
      "Dissect a JWT's three segments and state exactly what the signature does and does not prove.",
      "Apply 'decode is not verify': never make authorization decisions from client-decoded claims.",
      "Wire the course architecture: Supabase issues tokens, the browser carries them, NestJS verifies every request (Phase 17).",
    ],
    simple:
      "Imagine a hotel with a strict rule: the front desk forgets you the instant you walk away. Every time you return — even ten seconds later — you're a stranger again. To be recognized, you carry something: either a key card whose number is in the hotel's registry (a session cookie — the hotel remembers, the card is just a pointer), or a laminated passport with your photo and an anti-tamper seal (a JWT — the card itself carries the proof). Both work, and each has different failure modes: lose the registry entry and the key card is plastic; photocopy the passport and anyone can impersonate you. The entire lesson is learning which card you're holding and what its loss costs.",
    why:
      "This layer is the ancestor of everything from Phase 17 onward: Supabase Auth issues the tokens, the Next.js frontend carries them, NestJS guards verify them, and Phase 18's RBAC decisions rest on claims that were VERIFIED, not decoded. It is also the most misdiagnosed layer in full-stack debugging — 'randomly logged out', 'works in Postman but not the browser', '401s after exactly one hour' all trace to cookie flags, token lifetimes, or the decode-vs-verify confusion. Learn the mechanics now and Phase 17 becomes assembly, not archaeology.",
    mentalModel: {
      title: "The hotel key card vs the laminated passport",
      body: "Sessions are the key-card model: the server keeps a ledger (session store) mapping receipt-ids to identities; the cookie carries only the receipt-id. Revocation is instant (tear the ledger page), but every check consults the ledger (state to scale). JWTs are the passport model: identity plus claims travel inside the token, sealed by a signature only the issuer can produce. Verification is local and fast (no ledger), but revocation before expiry needs extra machinery — and a leaked passport works until its expiry date. The security question is never 'which is encrypted?' (neither is, by default) but 'what does loss cost, and how fast can I revoke?'",
    },
    sections: [
      {
        heading: "The state problem — every request is a stranger",
        body: [
          "HTTP is deliberately forgetful: the protocol keeps no memory between requests, so a server cannot tell your second request from a stranger's first. 'Logged in' is therefore not a server-side condition — it is a claim your browser re-presents on every single request, which the server must re-check every time. That re-presentation has two carriers in practice: a cookie the browser attaches automatically, or an Authorization header your code attaches deliberately.",
          "Two consequences follow. First: there is no 'logged in' boolean anywhere — there is only 'this request carried a credential that verified.' Second: the credential travels on EVERY request, so its theft window is the whole session. The discipline of this lesson — flags, lifetimes, verification — is entirely about shrinking what a stolen credential can do and for how long.",
        ],
        code: [
          {
            file: "amnesia.txt — the same stranger, twice",
            lang: "text",
            code: [
              "GET /dashboard HTTP/1.1            GET /dashboard HTTP/1.1",
              "Host: app.example.com              Host: app.example.com",
              "Cookie: sid=abc123                 (no Cookie header)",
              "",
              "→ 200 OK (your dashboard)          → 302 → /login",
              "",
              "The server holds no 'logged in' flag. It saw a receipt it",
              "recognizes in request one, and a stranger in request two.",
              "Identity is a property of the REQUEST, never of the connection.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Cookies — the browser-managed pouch and its four locks",
        body: [
          "A cookie is a small key:value pair the server plants with a Set-Cookie response header and the browser attaches to matching future requests automatically — no code required. That automatic attachment is the superpower (images, iframes, form posts all carry it) and the hazard (so does every request a malicious site tricks the browser into making — which is what the flags defend against).",
          "The four locks, learned as defenses not attributes. HttpOnly: JavaScript cannot read the cookie — a script injected by XSS (the P02-L4 innerHTML hole) cannot steal the session; this single flag converts 'XSS happened' from 'account stolen' to 'page defaced.' Secure: transmitted only over HTTPS, so no plaintext sniffing. SameSite=Lax (the modern default): the browser withholds the cookie on cross-site POSTs, neutering classic CSRF; Strict withholds even on top-level navigation, None opts out (and then requires Secure). Max-Age/Expires: the lifetime — short means a stolen receipt burns out fast. Read every Set-Cookie line as four locks, and any missing lock as an open window.",
        ],
        code: [
          {
            file: "set-cookie.txt — one header, four locks",
            lang: "text",
            code: [
              "Set-Cookie: sid=9f3a…; HttpOnly; Secure; SameSite=Lax; Max-Age=86400; Path=/",
              "",
              "HttpOnly     → invisible to document.cookie (XSS can't exfiltrate it)",
              "Secure       → HTTPS only (no sniffing on hostile Wi-Fi)",
              "SameSite=Lax → withheld on cross-site POSTs (CSRF's main road closed)",
              "Max-Age      → dies after 24h (theft window has an expiry date)",
              "",
              "Missing HttpOnly? A single innerHTML hole leaks every session.",
              "Missing Secure?  A café network reads the receipt in plaintext.",
              "SameSite=None without Secure? The browser refuses to store it at all.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Sessions vs tokens — the ledger vs the passport",
        body: [
          "The session model stores identity server-side: a random session id in a cookie is a RECEIPT pointing at a ledger row (Redis, database, memory) holding the real state. Revocation is instant — delete the row and every receipt dies — and the cookie carries nothing sensitive. The cost: every verification consults the ledger, so sessions are state you must scale, replicate, and keep alive across restarts.",
          "The token model inverts it: a JWT carries the claims (user id, roles, expiry) INSIDE, sealed by the issuer's signature. Verification is local math — no ledger, trivially horizontal — which is why APIs and multi-service systems favor it. The cost: you cannot revoke what you don't hold; a leaked token works until exp, so access tokens live minutes (not days) and a separate, well-guarded refresh token earns new ones. Neither model is 'more secure' — they trade revocation speed against operational state, and mature systems (including Supabase) run both: refresh tokens behave like sessions, access tokens like passports.",
        ],
        code: [
          {
            file: "models.ts — the tradeoff in two round trips",
            lang: "ts",
            code: [
              "// SESSION (ledger): cookie = receipt; server holds the truth",
              "// request  → Cookie: sid=9f3a…",
              "// server   → look up sid in store → { userId: 7, role: \"member\" }",
              "// revocation: delete the row → dead instantly. Scale: store is state.",
              "",
              "// TOKEN (passport): claims travel sealed; server verifies locally",
              "// request  → Authorization: Bearer eyJhbGciOi…",
              "// server   → verify signature + exp with issuer key → claims",
              "// revocation: NOT before expiry → keep access tokens SHORT (minutes)",
              "//             and rotate a protected refresh token for new ones.",
              "",
              "// Supabase's hybrid (what Phase 17 builds on):",
              "//   access token  ≈ passport   (~1h, verified by NestJS per request)",
              "//   refresh token ≈ session    (stored, revocable, earns new passports)",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "JWT anatomy — three segments, one rule that saves careers",
        body: [
          "A JWT is three base64url segments joined by dots: header (algorithm + type), payload (the claims: sub, iss, aud, exp, iat, plus custom ones), signature (a keyed hash over the first two). The first two segments are ENCODING, not encryption — paste any JWT into a decoder and read everything. The signature is the only security: it proves the payload is intact AND was sealed by whoever holds the key. Verifying requires the issuer's key; decoding requires nothing.",
          "So the rule, tattooed: DECODE IS NOT VERIFY. A client can decode its own token harmlessly — to display a name, to decide which UI to SHOW. But any decision the decode drives ('this claim says admin, so here is the admin API call') is a decision based on a document anyone can forge in a text editor. The server must verify the signature against a trusted key on every request — which is exactly what NestJS guards do in Phase 17, and why 'the frontend showed me the button' is never proof of permission in Phase 18. Also note exp is a promise only the verifier enforces: an expired token decodes beautifully and authorizes nothing.",
        ],
        code: [
          {
            file: "jwt-anatomy.txt — forge it yourself, then feel the fear",
            lang: "text",
            code: [
              "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9            ← header",
              ".eyJzdWIiOiI3Iiwicm9sZSI6ImFkbWluIiwiZXhwIjoxfQ  ← payload (claims)",
              ".SflKxwRJSMeKKF2QT4fwpMeJf36POk6yJV_adQssw5c     ← signature (seal)",
              "",
              "Base64url-decode the middle and READ it:",
              "  { \"sub\": \"7\", \"role\": \"admin\", \"exp\": 1 }",
              "",
              "Now change \"role\" to anything and re-encode. The decoded",
              "result still parses perfectly — because decoding verifies",
              "NOTHING. Only a server holding the signing key can tell the",
              "forgery from the original. The claims are luggage tags,",
              "not ID — the seal is the ID, and only the issuer can check it.",
            ].join("\n"),
          },
        ],
        demo: "token-inspector",
      },
      {
        heading: "Bearer on the wire — where identity travels in this course",
        body: [
          "When your code sends the credential, the convention is Authorization: Bearer <token> — the token rides in a header, never in the URL. URLs leak into server logs, proxy logs, browser history, and Referer headers; a token in a query string is a token published. Headers stay out of all four. The flip side of explicit attachment: the browser only adds Authorization where your fetch says so — an <img src> or a plain link carries nothing, which is why cookie-backed sessions still win for document-scale flows.",
          "The course architecture lands here: Supabase Auth (Phase 17) is the issuer and the refresh-token ledger; the browser (via the Supabase client) stores and attaches the short-lived access token; NestJS verifies the signature and expiry on EVERY protected request and only then trusts the claims inside. The frontend shows permission-aware UI from decoded claims as a courtesy; the backend re-decides everything from verified claims as the law. That split — courtesy vs law — is the whole of Phase 18, and it starts with this lesson's rule.",
        ],
      },
    ],
    mistake: {
      title: "The decoded admin",
      wrong: [
        "// client code, shipped to every user's browser:",
        "const token = getAccessToken();",
        "const payload = JSON.parse(atob(token.split(\".\")[1]));",
        "",
        "if (payload.role === \"admin\") {",
        "  showAdminPanel();",
        "  loadAdminApi();        // calls the REAL admin endpoints…",
        "}                        // …because a string said so.",
        "",
        "// Any user can flip role in devtools, re-encode, and watch",
        "// the panel 'work' — until the backend (correctly) 403s,",
        "// or, in the tragic version, the backend also trusts the claim.",
      ].join("\n"),
      right: [
        "// Decoded claims drive DISPLAY ONLY — a courtesy, not a decision:",
        "const payload = decodeJwt(getAccessToken()); // decode, never verify",
        "if (payload.role === \"admin\") showAdminPanel(); // hide/show UX",
        "",
        "// Every real decision happens where the signature can be checked:",
        "// NestJS guard (Phase 17): verify(sig, issuerKey) → trust claims",
        "// Phase 18: @RequirePermission(\"users.delete\") re-decides server-side.",
        "",
        "// The client may politely render a button.",
        "// The client may NEVER politely grant a permission.",
      ].join("\n"),
      explain:
        "Decoding is reading a luggage tag; anyone can print one. A UI decision from decoded claims is at best cosmetic (fine) and at worst an authorization decision by forged document (catastrophic — the backend that trusts it is the classic JWT CVE). The split is clean: the browser optimizes appearance from decoded claims; the server verifies the seal and re-decides everything. Phase 18's IDOR labs exist precisely because someone, somewhere, made the mistake on the right side of the wire.",
    },
    tryIt: [
      "Log into any site you use, open DevTools → Application → Cookies. Read the session cookie's flags aloud: HttpOnly? Secure? SameSite? Max-Age? Grade the site — which locks are missing, and what does each missing lock cost them?",
      "Paste a real JWT (from a test account only) into jwt.io or the lab above. Decode the payload. Now edit a claim, re-encode, and decode again — watch it parse. Say out loud what DIDN'T happen (verification).",
      "In the Network tab, find a request to an API you're logged into. Where does the identity travel — Cookie or Authorization? If Authorization, confirm the token appears in NO url. If Cookie, confirm HttpOnly is set.",
      "Compute: a site's access token lives 24h, its refresh token 30 days. A token leaks at hour 2. How long is the attacker authenticated? Now recompute with a 10-minute access token and rotating refresh. Feel why lifetimes ARE the revocation policy.",
      "Write out the Supabase/NestJS split from memory: who issues, who stores, who attaches, who verifies, who re-decides. Check it against section five.",
    ],
    challenge: {
      prompt:
        "Build an 'identity on the wire' workshop module (TypeScript, no framework). Deliver: (1) decodeJwt(token: string): JwtPayload that validates the SHAPE (three segments, base64url, JSON) and throws typed errors — and whose name/docs make clear it grants no trust; (2) isFresh(token, skewSeconds = 30): boolean reading exp with clock skew, demonstrating that an expired token still DECODES fine; (3) attachAuth(url, token): a fetch wrapper that sets Authorization: Bearer and refuses (throws) if handed an http:// URL for a non-localhost origin; (4) a handle401(result) sketch implementing refresh-once-then-retry for a SAFE request only (GET), with a comment explaining why a POST must not be auto-retried without an idempotency key; (5) a written note: which of your four functions could an attacker with a forged token fool, and which could they never fool — and why.",
      hints: [
        "Base64url → base64 needs the -_ → +/ swap plus padding; atob throws on bad input, so wrap and rethrow as a typed MalformedTokenError. The point is that decodeJwt succeeds on a FORGED token too — it's a reader, not a guard.",
        "isFresh: exp is seconds since epoch; Date.now()/1000 + skew < exp. Test it against a token with exp: 1 — decodes perfectly, isFresh is false. That's the whole lesson in one assertion.",
        "handle401: refresh once (guard with a flag so a 401 on the refresh itself doesn't loop), then retry ONLY if the original method is GET/HEAD. Your comment should name the danger: a replayed POST creates the thing twice.",
      ],
      solution: [
        "export type JwtPayload = {",
        "  sub?: string;",
        "  role?: string;",
        "  exp?: number;",
        "  iat?: number;",
        "  [claim: string]: unknown;",
        "};",
        "",
        "export class MalformedTokenError extends Error {}",
        "",
        "// (1) a READER, not a guard. The name says what it does NOT do.",
        "export function decodeJwt(token: string): JwtPayload {",
        "  const parts = token.split(\".\");",
        "  if (parts.length !== 3) throw new MalformedTokenError(\"expected 3 segments\");",
        "  try {",
        "    const base64 = parts[1].replace(/-/g, \"+\").replace(/_/g, \"/\");",
        "    const json = atob(base64.padEnd(Math.ceil(base64.length / 4) * 4, \"=\"));",
        "    const parsed: unknown = JSON.parse(json);",
        "    if (typeof parsed !== \"object\" || parsed === null) {",
        "      throw new MalformedTokenError(\"payload is not an object\");",
        "    }",
        "    return parsed as JwtPayload;",
        "  } catch (err) {",
        "    if (err instanceof MalformedTokenError) throw err;",
        "    throw new MalformedTokenError(\"payload is not base64url JSON\");",
        "  }",
        "  // NOTE: forgeries pass this function. That is correct —",
        "  // decoding is reading the luggage tag, not checking ID.",
        "}",
        "",
        "// (2) freshness: expired tokens decode perfectly, authorize nothing.",
        "export function isFresh(token: string, skewSeconds = 30): boolean {",
        "  const { exp } = decodeJwt(token);",
        "  if (typeof exp !== \"number\") return false; // no expiry = never trust",
        "  return Date.now() / 1000 + skewSeconds < exp;",
        "}",
        "",
        "// (3) attach the passport — on the wire, not in the URL.",
        "export async function attachAuth(url: string, token: string): Promise<Response> {",
        "  const target = new URL(url);",
        "  if (target.protocol === \"http:\" && target.hostname !== \"localhost\") {",
        "    throw new Error(\"refusing to send a bearer token over plaintext http\");",
        "  }",
        "  return fetch(target, {",
        "    headers: { Authorization: `Bearer ${token}` }, // header, never query",
        "  });",
        "}",
        "",
        "// (4) 401 → refresh ONCE → retry, SAFE methods only.",
        "let refreshing: Promise<string> | null = null;",
        "export async function handle401(",
        "  url: string,",
        "  method: \"GET\" | \"HEAD\" | \"POST\",",
        "  getToken: () => string,",
        "  refresh: () => Promise<string>,",
        "): Promise<Response> {",
        "  let res = await attachAuth(url, getToken());",
        "  if (res.status !== 401) return res;",
        "",
        "  // dedupe concurrent refreshes:",
        "  refreshing ??= refresh().finally(() => { refreshing = null; });",
        "  const fresh = await refreshing;",
        "",
        "  if (method !== \"GET\" && method !== \"HEAD\") {",
        "    // A replayed POST could create the thing TWICE.",
        "    // Without an idempotency key (Phase 29) we surface instead:",
        "    return new Response(null, { status: 401, statusText: \"re-auth required\" });",
        "  }",
        "  return attachAuth(url, fresh); // safe: repeating GETs changes nothing",
        "}",
        "",
        "/* (5) the note:",
        "   An attacker with a forged token fools decodeJwt and isFresh —",
        "   both read claims without a key; a forged exp in the future even",
        "   passes freshness. attachAuth they can't fool into plaintext, but",
        "   the token they send is theirs to send. The ONE function that can",
        "   never be fooled is the one that isn't here: the SERVER verifying",
        "   the signature against the issuer's key (Phase 17). Everything in",
        "   this file is a courtesy; the guard lives on the other side of the",
        "   wire. That is the architecture, not a limitation. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "'Logged in' on the web is really…",
        options: ["A claim re-presented and re-verified on every request", "A boolean stored on the server at login time", "A persistent TCP connection", "A value in localStorage"],
        answer: 0,
        explain: "HTTP is stateless: no request knows the previous one. Identity travels with each request (cookie or Authorization header) and must verify every time.",
      },
      {
        q: "HttpOnly protects a cookie from…",
        options: ["Theft via injected JavaScript (XSS)", "Network sniffing", "CSRF", "Expiry"],
        answer: 0,
        explain: "HttpOnly hides the cookie from document.cookie, so an innerHTML-injected script can't exfiltrate it. Sniffing is Secure's job; CSRF is SameSite's.",
      },
      {
        q: "The session model's superpower over JWTs is…",
        options: ["Instant revocation — delete the ledger row and every receipt dies", "Smaller cookies", "No server storage", "Faster verification"],
        answer: 0,
        explain: "Sessions keep truth server-side, so revocation is a row delete. Tokens verify fast and scale statelessly but live until expiry — hence short access tokens plus revocable refresh tokens.",
      },
      {
        q: "Base64url-decoding a JWT's payload proves…",
        options: ["Nothing about trust — the segments are encoding, not encryption", "The token is authentic", "The token is unexpired", "The user owns the account"],
        answer: 0,
        explain: "Anyone can decode — or re-encode with forged claims. Only signature verification with the issuer's key proves integrity and origin. Decode is not verify.",
      },
      {
        q: "Reading decoded claims client-side to SHOW admin UI is…",
        options: ["Fine as display courtesy — as long as every real decision is re-made server-side from verified claims", "A security vulnerability in all cases", "The recommended authorization pattern", "Impossible without the signing key"],
        answer: 0,
        explain: "The split is courtesy vs law: the browser optimizes appearance from decoded claims; the server verifies the seal and re-decides. The vulnerability is trusting the claim for actual access.",
      },
      {
        q: "Bearer tokens ride in the Authorization header rather than the URL because…",
        options: ["URLs leak into logs, history, proxies, and Referer headers", "Headers are encrypted", "URLs are limited to 2048 chars", "fetch can't send query params"],
        answer: 0,
        explain: "A token in a query string is published to every log and referrer along the path. Headers stay out of those channels. (Headers are not encrypted — HTTPS encrypts the whole request.)",
      },
    ],
    flashcards: [
      { front: "Stateless HTTP", back: "No memory between requests; every request is a stranger. Identity is a property of the request (cookie or Authorization header), re-verified each time." },
      { front: "The four cookie locks", back: "HttpOnly (invisible to JS — XSS can't steal it), Secure (HTTPS only), SameSite=Lax (withheld cross-site — CSRF defense), Max-Age (theft window expires)." },
      { front: "Session (ledger) model", back: "Server stores identity; cookie carries a receipt-id. Instant revocation, sensitive data server-side; costs stateful scale. The key card." },
      { front: "Token (passport) model", back: "Claims travel sealed in the JWT; verification is local, stateless, horizontal. No revocation before expiry → short access tokens + protected refresh tokens." },
      { front: "JWT anatomy", back: "header.payload.signature, base64url. First two are readable encoding; the signature is the only security — a keyed seal only the issuer can produce/verify." },
      { front: "Decode ≠ verify", back: "Decoding reads claims (anyone can, forgeries too). Verifying checks the signature against the issuer's key. Display from decoded; decide only from verified." },
      { front: "Access vs refresh token", back: "Access: short-lived passport, verified per request (minutes–hour). Refresh: session-like receipt, revocable, earns new access tokens. Supabase runs this hybrid." },
      { front: "Authorization: Bearer", back: "The conventional header for token identity. Never in URLs (logs/history/Referer leaks). Attached explicitly by your code, unlike cookies' automatic attachment." },
    ],
    recap: [
      "HTTP forgets; identity travels per request and re-verifies per request.",
      "Cookies: four locks — HttpOnly, Secure, SameSite, Max-Age — each missing one is an open window.",
      "Ledger vs passport: sessions revoke instantly at the cost of state; tokens scale statelessly at the cost of expiry-bound revocation.",
      "A JWT's readable segments prove nothing; the signature is the ID, and only the key-holder checks it.",
      "This course's law: Supabase issues, the browser carries, NestJS verifies — decode for display, verify for decisions.",
    ],
    references: [
      { label: "MDN — Set-Cookie", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Set-Cookie" },
      { label: "MDN — HTTP cookies", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Cookies" },
      { label: "jwt.io — Introduction to JWT", url: "https://jwt.io/introduction" },
      { label: "OWASP — Session Management Cheat Sheet", url: "https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html" },
      { label: "MDN — Authorization header", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Authorization" },
    ],
    nextBridge:
      "Identity now travels and verifies. But before your frontend may even READ a cross-origin response, the browser runs a permission check the server never sees you fail: CORS. Next lesson demystifies the preflight, teaches you to read the error everyone misreads, and shows why the fix always lives server-side.",
  },
  {
    id: "p07-l4",
    phaseId: "p07",
    title: "CORS & Preflight, Demystified",
    level: "Frontend Developer",
    minutes: 35,
    summary:
      "CORS is the most misdiagnosed error in full-stack development — because the message blames the wrong thing. The request DOES reach the server; the server DOES answer; what fails is the browser's decision to hand the answer to YOUR SCRIPT. This lesson installs the same-origin policy's actual purpose, the simple-vs-preflighted split (and why every JSON POST you've written was preflighted), and the server-owned fix — so 'CORS error' becomes a two-minute header inspection instead of an afternoon of proxies.",
    prerequisites: ["p07-l3 — identity on the wire", "p07-l1 — reading requests in the Network tab"],
    objectives: [
      "State what the same-origin policy protects and why the server still answers a cross-origin request.",
      "Classify a request as simple or preflighted from its method, headers, and Content-Type.",
      "Read the OPTIONS preflight and the four Access-Control-Allow-* response headers.",
      "Diagnose a CORS failure from the Network tab, not the console message.",
      "Apply the credentials rules: explicit origin + Allow-Credentials, never wildcard.",
      "Fix CORS where it belongs — the server's origin allowlist per environment — and refuse the no-cors/proxy non-fixes.",
    ],
    simple:
      "Your browser is a bodyguard for the web's most gullible client: you. It knows that scripts on evil.com can make requests to your-bank.com — and that your cookies for the bank ride along automatically. So it draws a line: any origin may SEND (the bank answers the phone either way), but a script may only READ the answer if the bank's response explicitly says 'yes, this caller may listen' — the Access-Control-Allow-Origin header. CORS is that permission slip, stamped by the server, checked by the browser, and invisible to curl. The 'CORS error' is just the bodyguard saying: the call went through, but you're not on the list.",
    why:
      "Every full-stack developer hits this wall in week one of real APIs — your localhost:5173 frontend calling localhost:3000 is cross-origin, so the course architecture (Next → Nest) meets CORS on day one of Phase 10. And because the console message is famously unhelpful ('No Access-Control-Allow-Origin header present'), the folklore responses — proxy everything, mode: 'no-cors', allow * — ship into production and become either holes or mysteries. Learn the mechanics once, precisely, and Phase 11's Fastify CORS setup and Phase 38's production origin lists are configuration, not exorcism.",
    mentalModel: {
      title: "The bodyguard and the permission slip",
      body: "Same-origin policy is not a firewall — the request crosses the wire and the server answers. The browser is a bodyguard standing between the response and your script: it hands over the answer ONLY if it carries a permission slip (Access-Control-Allow-Origin matching your origin). Unusual requests — JSON bodies, PUT/DELETE, Authorization headers — get extra scrutiny: the bodyguard places a preliminary call first (the OPTIONS preflight) asking 'may this caller do THIS with THESE headers?', and only proceeds if the server's preflight answer allows it. The server writes the slips; the browser enforces them; your script just experiences the outcome as a red console line.",
    },
    sections: [
      {
        heading: "What CORS protects — the request goes through either way",
        body: [
          "The fact that reframes everything: a cross-origin fetch ALWAYS reaches the server. If your POST creates a record, it created one — CORS cannot stop the write, because the browser sends first and asks about READING later. What the same-origin policy gates is the response reaching your JavaScript. This is by design: the policy protects USERS (a malicious page reading your bank's responses with your cookies attached), not servers (which must do their own authentication anyway — the P00-L1 trust line, again).",
          "So a 'CORS error' on a mutation is the dangerous kind of confusing: the action may have SUCCEEDED server-side while your catch block swears it failed. And curl sees no CORS at all — it's a browser enforcement mechanism, which is why 'works in Postman, broken in the app' is CORS until proven otherwise. The debugging reflex: open Network, find the failed request, and look for the OPTIONS line above it and the Access-Control-Allow-* headers on the response — the console message is a summary; the headers are the evidence.",
        ],
        code: [
          {
            file: "cors-evidence.txt — what actually happened",
            lang: "text",
            code: [
              "Console says:",
              "  ✗ Access to fetch at 'https://api.example.com/todos' from origin",
              "    'https://app.example.com' has been blocked by CORS policy:",
              "    No 'Access-Control-Allow-Origin' header is present.",
              "",
              "Network tab proves:",
              "  OPTIONS /todos  → 204   (the preflight — the server DID answer)",
              "    Response headers: (no Access-Control-Allow-Origin)  ← the crime",
              "  POST /todos     → 201   (your row WAS created — writes aren't gated)",
              "",
              "The bodyguard let the call through and the write landed.",
              "It only withheld the 201 from your script. Fix the slip,",
              "not the 'blocked request' — there was no blocked request.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Simple vs preflighted — why your JSON POST was never simple",
        body: [
          "A request earns the 'simple' label (no preflight) only if ALL three hold: method is GET/HEAD/POST; the only custom headers are CORS-safelisted ones (Accept, Accept-Language, Content-Language, Content-Type); and Content-Type is one of application/x-www-form-urlencoded, multipart/form-data, or text/plain. Notice what that excludes: every JSON API call you have written. Content-Type: application/json fails the list; so do PUT/PATCH/DELETE; so does Authorization: Bearer. The preflight exists precisely because the modern API stack is preflighted by default.",
          "The preflight is an OPTIONS request the browser fabricates: Access-Control-Request-Method: PATCH and Access-Control-Request-Headers: content-type, authorization ask the question; the server answers with Access-Control-Allow-Methods, Access-Control-Allow-Headers, Access-Control-Allow-Origin, and Access-Control-Max-Age (cache this answer for N seconds — the reason your OPTIONS traffic drops after the first call). If the preflight answer lacks any asked-for permission, the REAL request never fires. So 'my request wasn't sent' is often true — the preflight was sent, and it was refused.",
        ],
        code: [
          {
            file: "preflight.txt — the permission call",
            lang: "text",
            code: [
              "// fetch(url, { method: \"PATCH\", headers: {",
              "//   \"Content-Type\": \"application/json\",        ← not safelisted",
              "//   \"Authorization\": \"Bearer eyJ…\"             ← not safelisted",
              "// }, body: JSON.stringify(patch) })",
              "",
              "→ OPTIONS /todos/7 HTTP/1.1                     (browser fabricates this)",
              "  Origin: https://app.example.com",
              "  Access-Control-Request-Method: PATCH",
              "  Access-Control-Request-Headers: content-type, authorization",
              "",
              "← HTTP/1.1 204 No Content",
              "  Access-Control-Allow-Origin: https://app.example.com",
              "  Access-Control-Allow-Methods: GET, PATCH, DELETE",
              "  Access-Control-Allow-Headers: content-type, authorization",
              "  Access-Control-Max-Age: 600                   (cache this slip 10 min)",
              "",
              "→ PATCH /todos/7 …                              (only NOW does it fly)",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Reading the failure — the four slips and who forgot to stamp",
        body: [
          "Four slips, four refusals. (1) Allow-Origin missing or wrong: the classic — server sends nothing, or echoes a different origin than yours (check scheme AND port: http://localhost:5173 ≠ http://localhost:3000 ≠ https://app.example.com). (2) Allow-Methods lacks your verb: preflight asked for PATCH, server permits only GET, POST — the real request never launches. (3) Allow-Headers lacks a requested header: Authorization or Content-Type absent from the list. (4) The OPTIONS route itself 404/405s: the framework never wired a preflight handler — rarer since NestJS/Fastify configure this for you, but the signature when hand-rolled servers meet a real API.",
          "The diagnostic ritual is two lines long: in Network, select the OPTIONS request — did it 2xx? with which Allow-* headers? Then select the real request — does its response carry Allow-Origin matching your exact origin? Whatever is missing is the fix, and the fix is ALWAYS server-side: add your origin to the allowlist. The wildcard Access-Control-Allow-Origin: * works only for anonymous APIs — the moment credentials (cookies, Authorization you rely on via cookies) are involved, the browser demands a specific origin plus Access-Control-Allow-Credentials: true, and * is rejected outright.",
        ],
      },
      {
        heading: "Credentials mode — when cookies cross the line",
        body: [
          "fetch only attaches cross-origin cookies when you opt in with credentials: 'include' — and the server must answer with BOTH an explicit Access-Control-Allow-Origin (never *) AND Access-Control-Allow-Credentials: true. That strictness is the point: credentialed cross-origin reads are the exact superpower a malicious page would want, so the spec makes both sides name each other by hand. SameSite rides along: a SameSite=Lax cookie won't cross for a POST even with all the CORS slips stamped — the flags cooperate.",
          "This course sidesteps the credentialed-cookie maze deliberately: the architecture sends Authorization: Bearer from JavaScript (Phase 17), which needs none of the credentials machinery — just Allow-Origin and Allow-Headers. Cookie-based flows still matter (Supabase's own auth endpoints use them; so does every classic session app), but the Bearer path is why 'set Allow-Origin to my frontend's origin list, allow the two headers, done' covers this course end-to-end.",
        ],
      },
      {
        heading: "Fixing it where it belongs — and the two non-fixes",
        body: [
          "The server owns the CORS contract: an explicit origin allowlist per environment — localhost origins in dev, the real domain in staging/prod — configured once in the framework (NestJS's enableCors with a Fastify adapter in Phase 11) and versioned like any other config. Wildcards in development hide origin bugs that resurface in production; a list of three strings is cheaper than a mystery at deploy time.",
          "The two folklore non-fixes, and their actual effects. mode: 'no-cors' does not disable CORS — it downgrades you to an OPAQUE response: status 0, no headers, no body, unreadable; you've traded an error message for silence and still can't read anything. The dev proxy (Vite/Next rewrites) legitimately removes the origin mismatch for local development — and then quietly stops existing in production, where the browser calls the real API origin. Use it knowing it is scaffolding; ship the server-side allowlist anyway. The permanent reflex: CORS is a server setting named after your frontend, not a client hack.",
        ],
        code: [
          {
            file: "cors-config.ts — the whole fix, server-side (Phase 11 preview)",
            lang: "ts",
            code: [
              "// NestJS + Fastify — the origin list IS the CORS policy:",
              "const app = await NestFactory.create<NestFastifyApplication>(AppModule,",
              "  new FastifyAdapter());",
              "",
              "app.enableCors({",
              "  origin: [",
              "    \"http://localhost:5173\",        // dev frontend",
              "    \"https://staging.app.example.com\",",
              "    \"https://app.example.com\",     // production",
              "  ],                                // never \"*\" with credentials;",
              "  methods: [\"GET\", \"POST\", \"PATCH\", \"DELETE\"],  // allow only these",
              "  allowedHeaders: [\"Content-Type\", \"Authorization\"], // and only these",
              "  credentials: false,               // Bearer architecture: not needed",
              "  maxAge: 600,                      // preflight cache: fewer OPTIONS",
              "});",
              "",
              "// Every 'CORS error' from here on is: origin missing from THIS list,",
              "// or an environment where this config didn't deploy. Two-minute fix.",
            ].join("\n"),
          },
        ],
        demo: "cors-sim",
      },
    ],
    mistake: {
      title: "Silencing the bodyguard instead of getting on the list",
      wrong: [
        "// the console said CORS, so 'disable' CORS:",
        "const res = await fetch(\"https://api.example.com/todos\", {",
        "  mode: \"no-cors\",          // ← the folklore fix",
        "  headers: { Authorization: `Bearer ${token}` },",
        "});",
        "console.log(res.status);    // 0",
        "console.log(res.ok);        // false",
        "const todos = await res.json(); // ✗ throws — body is UNREADABLE",
        "",
        "// opaque response: no status, no headers, no body.",
        "// You didn't fix the permission slip — you agreed to never",
        "// see the answer. The error is gone; so is the data.",
      ].join("\n"),
      right: [
        "// CORS is a server contract. The client stays normal:",
        "const res = await fetch(\"https://api.example.com/todos\", {",
        "  headers: { Authorization: `Bearer ${token}` },",
        "});",
        "",
        "// The server adds your origin to its allowlist (see the",
        "// enableCors config above). Same code, readable response,",
        "// and staging/prod behave identically because the fix lives",
        "// where the API lives — not in a dev-only proxy or a mode flag.",
      ].join("\n"),
      explain:
        "mode: 'no-cors' asks the browser for a write-only errand: the request goes, the response comes back sealed (opaque), and your script gets a status-0 husk. It 'fixes' the console by deleting the data you came for — and it masks the real problem, which is that your origin isn't on the server's allowlist. The proxy cousin has the same shape: perfect locally, nonexistent in production. The only durable fix is the server naming your origins, per environment, in versioned config.",
    },
    tryIt: [
      "Reproduce the split: open two local servers (or any API without your origin allowed) and fetch from a page on a different port. In Network, find BOTH the OPTIONS and the real request. Note the real request's actual status — did the write land?",
      "Take any fetch you've written and classify it: simple or preflighted? Change Content-Type from application/json to text/plain and watch the OPTIONS line vanish in Network. Change it back and watch it return. That line is the safelist.",
      "Run the same cross-origin fetch with curl. Count the CORS errors (expected: zero). Explain to the rubber duck why — then explain why that makes curl useless for diagnosing CORS.",
      "Add Authorization to a cross-origin GET and confirm the preflight now asks for it (Access-Control-Request-Headers). Remove the server's Allow-Headers entry (or imagine it) and watch the real request never launch.",
      "Write the enableCors config for this course's three environments from memory. Check it against section five. Which line would you never ship as *?",
    ],
    challenge: {
      prompt:
        "You are handed three incident reports from a team shipping a Next.js frontend (https://app.example.com) against a NestJS API (https://api.example.com). For EACH: (1) name the missing or wrong permission slip, (2) write the exact response header(s) that would fix it, (3) state whether the real request fired or never launched, and (4) name the one thing the frontend team must NOT do. Incident A: console shows 'No Access-Control-Allow-Origin header is present' on GET /todos — Network shows the GET returned 200 with the data. Incident B: PATCH /todos/7 logs a CORS error and Network shows an OPTIONS returning 204 but NO PATCH request exists. Incident C: login works, but authenticated GET /me fails with a CORS error mentioning credentials; the server currently sends Access-Control-Allow-Origin: * with Access-Control-Allow-Credentials: true.",
      hints: [
        "Incident A: the 200 with data is the tell — the bodyguard withheld a readable answer that exists. Which slip is missing, and does the fix require any client change?",
        "Incident B: an OPTIONS that 204s but blocks the real request means the preflight ANSWERED but refused. Compare Access-Control-Request-Method (PATCH) against Allow-Methods — and the two client-side non-fixes to refuse are no-cors and 'just use PUT'.",
        "Incident C: the spec's hardest rule — Allow-Credentials: true REQUIRES an explicit origin; * is ignored precisely when credentials are involved. The fix names https://app.example.com on the server; the client's credentials flag may be correct and must not be the scapegoat.",
      ],
      solution: [
        "/* ——— Incident A: the withheld answer ——— */",
        "// (1) Missing slip: Access-Control-Allow-Origin.",
        "// (3) The GET FIRED and succeeded (200 + data) — writes/reads reach",
        "//     the server regardless; only the script was denied the answer.",
        "// (2) Server adds, on the /todos response (or framework-wide):",
        "//       Access-Control-Allow-Origin: https://app.example.com",
        "// (4) Frontend must NOT add mode: 'no-cors' — it would seal the very",
        "//     response that already works, trading an error for silence.",
        "",
        "/* ——— Incident B: the refused preflight ——— */",
        "// (1) Wrong slip: OPTIONS answered 204 but its",
        "//     Access-Control-Allow-Methods lacks PATCH (or Allow-Headers",
        "//     lacks content-type/authorization the request asked for).",
        "// (3) The PATCH NEVER LAUNCHED — the browser aborted after the",
        "//     preflight refused. Nothing hit /todos/7. (The 204 above is the",
        "//     OPTIONS, not a successful PATCH.)",
        "// (2) Server preflight response must include:",
        "//       Access-Control-Allow-Origin: https://app.example.com",
        "//       Access-Control-Allow-Methods: GET, PATCH, DELETE",
        "//       Access-Control-Allow-Headers: content-type, authorization",
        "// (4) Frontend must NOT 'switch PATCH to POST to skip preflight' —",
        "//     the API's semantics (partial update) are not a CORS variable,",
        "//     and POST is no simpler once JSON + Authorization are involved.",
        "",
        "/* ——— Incident C: the wildcard credential trap ——— */",
        "// (1) Invalid combination: Access-Control-Allow-Origin: * is REJECTED",
        "//     whenever Access-Control-Allow-Credentials: true is in play —",
        "//     the spec demands an explicit origin for credentialed reads.",
        "// (3) The GET fired (credentials attached, cookies crossed); the",
        "//     browser then refused to hand the answer to the script because",
        "//     the slip said '*' instead of the caller's name.",
        "// (2) Server fix — name the origin, keep the credential slip:",
        "//       Access-Control-Allow-Origin: https://app.example.com",
        "//       Access-Control-Allow-Credentials: true",
        "// (4) Frontend must NOT remove credentials: 'include' or delete the",
        "//     cookies — the session is fine; the slip is wrong. And the team",
        "//     should note that this course's Bearer architecture avoids the",
        "//     credentialed-CORS dance entirely (Authorization header instead).",
        "",
        "/* The pattern behind all three: the browser sends first and asks",
        "   about READING later; every fix is a server header naming the",
        "   calling origin; every wrong fix is a client trick that deletes",
        "   data or meaning. CORS is configuration, not code. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A cross-origin fetch to an API that never allows your origin will…",
        options: ["Reach the server and may succeed server-side — only the script is denied the response", "Never leave the browser", "Be blocked at the server firewall", "Throw before sending"],
        answer: 0,
        explain: "CORS gates READING, not sending. The write can land while your catch block swears it failed — which is why the Network tab, not the console, is the evidence.",
      },
      {
        q: "Which request is guaranteed to trigger a preflight?",
        options: ["POST with Content-Type: application/json", "GET with only an Accept header", "POST with a form-urlencoded body and no custom headers", "HEAD with Accept-Language"],
        answer: 0,
        explain: "Simple = GET/HEAD/POST + safelisted headers only + form/multipart/text content types. application/json fails the list — so does any PUT/PATCH/DELETE or Authorization header.",
      },
      {
        q: "The OPTIONS preflight asks the server…",
        options: ["'May this origin perform this method with these headers?' via Access-Control-Request-* headers", "'What is your API version?'", "'Is the user authenticated?'", "'What is your rate limit?'"],
        answer: 0,
        explain: "Access-Control-Request-Method and Access-Control-Request-Headers pose the question; the Access-Control-Allow-* response headers answer. A refusal means the real request never fires.",
      },
      {
        q: "With credentials (cookies) involved, Access-Control-Allow-Origin must be…",
        options: ["The exact calling origin, plus Allow-Credentials: true — never *", "*", "Omitted", "Any wildcard subdomain"],
        answer: 0,
        explain: "The spec rejects wildcards for credentialed reads: both sides must name each other. That strictness is the feature — it's exactly the superpower a malicious page would want.",
      },
      {
        q: "mode: 'no-cors' gives you…",
        options: ["An opaque response — status 0, unreadable body — not a CORS bypass", "Full access without the preflight", "A faster request", "Cookie-free requests"],
        answer: 0,
        explain: "It converts an error into silence: the response arrives sealed. You still can't read anything. The durable fix is the server's origin allowlist.",
      },
      {
        q: "The CORS allowlist belongs…",
        options: ["In server configuration, per environment, versioned like any config", "In the frontend fetch options", "In a production proxy only", "In DNS records"],
        answer: 0,
        explain: "Dev proxies and no-cors are scaffolding or self-deception. enableCors with explicit origins per environment (NestJS/Fastify, Phase 11) is the architecture this course ships.",
      },
    ],
    flashcards: [
      { front: "Same-origin policy", back: "The browser rule: any origin may SEND, but a script may READ a cross-origin response only if the response carries a matching Access-Control-Allow-Origin. Protects users, not servers." },
      { front: "CORS gates reading", back: "The request fires and can succeed server-side; CORS decides whether the script sees the answer. 'Blocked' requests usually aren't blocked writes." },
      { front: "Simple request", back: "GET/HEAD/POST + CORS-safelisted headers only + form/multipart/text content types. No preflight. JSON POSTs, PUT/PATCH/DELETE, and Authorization are never simple." },
      { front: "Preflight (OPTIONS)", back: "Browser-fabricated OPTIONS asking Access-Control-Request-Method/-Headers; answered by Access-Control-Allow-*. A refusal means the real request never launches." },
      { front: "The four slips", back: "Allow-Origin (your exact origin), Allow-Methods (your verb), Allow-Headers (your headers), Allow-Credentials (+ explicit origin, never *). Missing any one = refusal." },
      { front: "Opaque response", back: "The result of mode: 'no-cors': status 0, no headers, no body. It deletes your data instead of fixing the permission. Not a bypass, a blindfold." },
      { front: "max-age", back: "Access-Control-Max-Age caches the preflight answer for N seconds — why OPTIONS traffic drops after first contact per origin+route." },
      { front: "The CORS reflex", back: "Network tab → OPTIONS line (did it 2xx? which Allow-*) → real response (Allow-Origin matches my exact origin?). Fix = server allowlist, per environment. Always." },
    ],
    recap: [
      "The browser sends first and asks about reading later — the server always answers.",
      "JSON POSTs are preflighted by definition; the OPTIONS line is the permission call.",
      "Diagnose from the Network headers, never the console summary.",
      "Credentials demand explicit origins — wildcards are rejected by design.",
      "CORS is server config naming your frontends, per environment. Proxies are scaffolding; no-cors is a blindfold.",
    ],
    references: [
      { label: "MDN — CORS", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CORS" },
      { label: "MDN — Same-origin policy", url: "https://developer.mozilla.org/en-US/docs/Web/Security/Same-origin_policy" },
      { label: "web.dev — Cross-Origin Resource Sharing", url: "https://web.dev/articles/cross-origin-resource-sharing" },
      { label: "Fetch spec — CORS protocol", url: "https://fetch.spec.whatwg.org/#http-cors-protocol" },
      { label: "Fastify — @fastify/cors", url: "https://github.com/fastify/fastify-cors" },
    ],
    nextBridge:
      "The wire is now transparent end to end: anatomy, vocabulary, identity, and the browser's permission layer. Next lesson turns outward from mechanics to DESIGN — REST conventions, pagination strategies, and structured error contracts: the treaty your Phase 10 NestJS API will sign and your Phase 19 client will consume.",
  },
];

export const LESSON_CONTENT_P7B: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P7B.map((l) => [l.id, l]),
);
