import type { LessonContent } from "./types";

/**
 * Pass 019 bounded content batch: Phase 7 opener (L1–L2).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P7: LessonContent[] = [
  {
    id: "p07-l1",
    phaseId: "p07",
    title: "Anatomy of a Request: URL → DNS → Response",
    level: "Frontend Developer",
    minutes: 40,
    summary:
      "Every fetch you have written so far has crossed a wire you have never inspected. This lesson opens the envelope: the URL as a typed address, DNS as the lookup that precedes everything, the TCP+TLS handshake that builds the road, and the request/response pair itself — then hands you the two instruments that make it all visible: the DevTools Network timing tab and curl with its timing variables.",
    prerequisites: ["p02-l5 — fetch and the two-stage response", "p00-l3 — the tiny Node server that first spoke HTTP"],
    objectives: [
      "Parse any URL into scheme, host, port, path, query, and fragment — and say which parts the server sees.",
      "Narrate the pre-request journey: DNS resolution, TCP handshake, TLS negotiation — and why connection reuse makes the second request faster.",
      "Describe the request envelope (method line, headers, body) and the response envelope (status line, headers, body) as separate artifacts.",
      "Read the Network timing waterfall: queueing, DNS, connect, TLS, TTFB, content download — and locate where latency hides.",
      "Use curl with -w timing variables to measure a request from a terminal, independent of any browser.",
      "Diagnose 'the API is slow' by splitting it into lookup, handshake, wait, and transfer problems.",
    ],
    simple:
      "Sending a request is mailing a letter to a stranger. The URL is the address, written in a strict format the post office can parse. But before the letter can travel, you must look up where the street actually is (DNS), then build a private, sealed tunnel to that building (TCP plus TLS). Only THEN does your letter — the request, with its header card and optional enclosed contents — go through. The reply comes back the same tunnel: a status stamp ('received and answered', 'wrong address', 'my fault'), a header card, and the contents. Every part of that journey takes real, measurable time — and the Network tab is a stopwatch that shows you each leg.",
    why:
      "'The API is slow' is the most common complaint in full-stack work, and it is almost always four different problems wearing one coat: a DNS lookup, a cold handshake, a slow server think-time, or a fat payload. Developers who can read the timing waterfall stop guessing and start fixing the actual leg — and they stop blaming the backend team for what was their own bundle all along. This literacy is also load-bearing for everything ahead: Phase 8's caching and revalidation, Phase 19's retry policy, Phase 27's HTTP cache headers, Phase 30's TLS and certificate trust, and Phase 32's latency budgets all assume you can narrate a request end to end. You cannot secure, cache, or optimize what you cannot see.",
    mentalModel: {
      title: "The courier and the tunnel",
      body: "You never hand a letter directly to a server. First the courier checks the directory (DNS) to turn a name into a street address. Then, because the road is public, you insist on building a private sealed tunnel before saying anything sensitive (TLS over TCP) — that tunnel costs time to build but is then reused for many letters, which is why the first request to a host is always the slowest. The letter itself has three parts: the instruction line (GET /todos), the header card (who you are, what you accept, your token), and an optional parcel (the body). The reply mirrors it: a status stamp, a header card, and a parcel. The timing tab measures every leg — directory, tunnel-building, the wait while the other side thinks (TTFB), and the parcel download. When someone says 'slow', you now have four suspects and a stopwatch.",
    },
    sections: [
      {
        heading: "The URL — a typed address, not a string",
        body: [
          "A URL is structured data with a grammar: scheme (https — which protocol and whether the tunnel is sealed), host (api.example.com — the name to look up), port (default 443 for https, rarely written), path (/todos/7 — the route on the server), query (?expand=owner — arguments after the ?), and fragment (#comments — the part after # that the server NEVER receives; it is a client-side bookmark). Getting this wrong is a daily bug source: query parameters arrive as strings (the Phase 3 typing habit applies), fragments never reach the server (so you cannot filter by them server-side), and a path with spaces or special characters must be percent-encoded.",
          "The host/port pair identifies the machine; the path identifies the resource; the query refines it. That three-way split maps directly onto how servers route — and later onto REST design in Phase 7-L5, where you will choose between putting an idea in the path (/users/7/tasks) versus the query (/tasks?userId=7) as a deliberate modeling decision, not a coin flip.",
        ],
        code: [
          {
            file: "url-anatomy.js — take it apart with the platform",
            lang: "js",
            code: [
              "const u = new URL(",
              "  \"https://api.example.com:443/todos/7?expand=owner#comments\"",
              ");",
              "u.protocol;  // \"https:\"",
              "u.hostname;  // \"api.example.com\"",
              "u.port;      // \"\"  (empty — 443 is the default, implied)",
              "u.pathname;  // \"/todos/7\"",
              "u.search;    // \"?expand=owner\"",
              "u.hash;      // \"#comments\"  ← NEVER sent to the server",
              "",
              "// query params are a typed map of STRINGS:",
              "u.searchParams.get(\"expand\");      // \"owner\"",
              "u.searchParams.append(\"sort\", \"-createdAt\");",
              "u.toString(); // …/todos/7?expand=owner&sort=-createdAt#comments",
              "",
              "// encoding is mandatory, not decorative:",
              "encodeURIComponent(\"a b&c\");  // \"a%20b%26c\" — & must be escaped or it splits params",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Before the request — DNS, TCP, TLS",
        body: [
          "Three things happen before a single byte of your request exists. ONE, DNS resolution: the hostname becomes an IP address, checked in a cache chain (browser → OS → resolver) that can take 0ms (cached) or 50–300ms (cold, or a slow resolver). TWO, the TCP handshake: a three-way hello (SYN, SYN-ACK, ACK) establishes a reliable byte pipe — one round trip. THREE, the TLS handshake: the server proves its identity with a certificate your browser chains to a trusted root, and both sides derive encryption keys — extra round trips, unless session resumption shortcuts them.",
          "The payoff for understanding this: connection reuse. HTTP/2 multiplexes many requests over ONE tunnel, so after the first request to a host, DNS/connect/TLS are all ~0 and you pay only wait + download. This is why the second fetch to the same API looks instant, why preconnect hints exist, and why a 'slow first load, fast after' report is usually handshake cost, not server slowness. It also explains curl's first-request overhead versus a warm browser — different tunnel states, same server.",
        ],
        code: [
          {
            file: "handshake.sh — see the legs yourself",
            lang: "bash",
            code: [
              "# DNS only: how long does the name take to resolve?",
              "dig api.example.com            # look at 'Query time: X msec'",
              "",
              "# TLS only: certificate chain + handshake detail",
              "openssl s_client -connect api.example.com:443 -brief",
              "",
              "# The full journey with every leg timed (curl's -w variables):",
              "curl -o /dev/null -s -w \"",
              "  dns:      %{time_namelookup}s\\n",
              "  connect:  %{time_connect}s\\n",
              "  tls:      %{time_appconnect}s\\n",
              "  ttfb:     %{time_starttransfer}s\\n",
              "  total:    %{time_total}s\\n\" \\",
              "  https://api.example.com/todos",
              "",
              "# Run it twice: the second run often shows the OS DNS cache,",
              "# but curl builds a fresh TCP+TLS tunnel every time — so compare",
              "# against your warm browser, which reuses its tunnel (HTTP/2).",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The envelopes — request and response as artifacts",
        body: [
          "A request is three artifacts in order: the request line (METHOD + path + HTTP version: 'GET /todos?status=open HTTP/1.1'), the headers (key: value cards — Host, Authorization, Accept, User-Agent, Content-Type when there is a body), and an optional body (JSON, form data, bytes). A response mirrors it: the status line ('HTTP/1.1 200 OK' — version, code, reason phrase), headers (Content-Type, Content-Length or chunked, caching directives, Set-Cookie), and the body. Text, not magic — you can write both by hand, which is exactly what your Phase 0 tiny server did and what raw sockets still do.",
          "Two habits come from seeing the envelopes. ONE: the body's format is a CLAIM in the Content-Type header, and trusting it blindly is how injection bugs are born (Phase 20 validates; Phase 30 attacks). TWO: headers carry the conversation's metadata — authorization, caching, cookies, tracing IDs — so debugging an API means reading headers FIRST and the body second. A 401 with a correct-looking body is still a 401.",
        ],
        code: [
          {
            file: "envelopes.txt — the wire, transcribed",
            lang: "text",
            code: [
              "── request ──────────────────────────────",
              "POST /todos HTTP/1.1",
              "Host: api.example.com",
              "Authorization: Bearer eyJhbGciOi…",
              "Content-Type: application/json",
              "Content-Length: 34",
              "",
              "{\"title\":\"Ship\",\"done\":false}",
              "",
              "── response ─────────────────────────────",
              "HTTP/1.1 201 Created",
              "Content-Type: application/json",
              "Location: /todos/8",
              "Cache-Control: no-store",
              "",
              "{\"id\":8,\"title\":\"Ship\",\"done\":false}",
              "",
              "# headers end at the blank line; everything after is body.",
              "# curl -v prints both envelopes — the -v habit pays for itself weekly.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The timing waterfall — where the milliseconds live",
        body: [
          "DevTools → Network → click a request → Timing. The bars, in order: Queueing (browser waiting for a free connection slot), DNS Lookup, Initial connection (TCP), SSL (TLS), Request sent (usually microseconds), Waiting for server response (TTFB — the server's think time, from last request byte to first response byte), and Content Download. The reading rule: find the longest bar, that is the suspect, and each bar has a different owner — DNS is infrastructure, TTFB is the backend, download is payload size.",
          "TTFB deserves its own discipline because it hides three different delays: server compute (slow query — Phase 22), upstream dependencies (the API waiting on ITS database or another service), and plain network distance. And download time is pure arithmetic: bytes ÷ bandwidth — a 2MB JSON list 'slow API' is a payload bug you fix with pagination (Phase 22) or compression, not a server tune-up. The waterfall turns 'it's slow' into 'DNS took 240ms' or 'TTFB was 1.8s' or 'we shipped 2.1MB' — and each has a different fix and a different team.",
        ],
        code: [
          {
            file: "waterfall-reading.txt — the four suspects",
            lang: "text",
            code: [
              "bar            | long means…                | fix lives in…",
              "─────────────────────────────────────────────────────────────",
              "DNS Lookup     | cold/slow resolver         | preconnect, DNS provider",
              "Connect + SSL  | no connection reuse        | HTTP/2 keep-alive, fewer hosts",
              "Waiting (TTFB) | server think time          | backend: query, upstream, logic",
              "Download       | fat payload                | pagination, fields, compression",
              "",
              "Rule: longest bar first. Two requests to the same host with very",
              "different totals? Diff the bars, not the vibes.",
            ].join("\n"),
          },
        ],
        demo: "request-trace",
      },
    ],
    mistake: {
      title: "Blaming the API for the handshake",
      wrong: [
        "// report: 'the tasks API is slow — 1.4s every cold load'",
        "// team response: add caching middleware, profile the handler,",
        "// tune the database… two days of backend archaeology.",
        "",
        "// the timing tab, which nobody opened:",
        "//   DNS Lookup   620ms   ← cold resolver, new corporate VPN",
        "//   Connect      210ms   ← TLS handshake, first request to host",
        "//   Waiting      140ms   ← the API itself: fine",
        "//   Download      30ms   ← payload: fine",
        "",
        "// 1.0s of the 1.4s never touched the backend.",
      ].join("\n"),
      right: [
        "// the 60-second triage that finds it:",
        "// 1) DevTools → Network → Timing on the slow request",
        "// 2) longest bar = DNS/Connect → this is infrastructure,",
        "//    not application code",
        "// 3) verify: second load is fast (tunnel reused) → confirms handshake cost",
        "",
        "// fixes that match the actual leg:",
        "//   <link rel=\"preconnect\" href=\"https://api.example.com\">",
        "//   (warms DNS + TLS during page load, before the first fetch)",
        "//   — or fix the resolver/VPN. Backend untouched, problem gone.",
      ].join("\n"),
      explain:
        "'Slow API' is four problems in a trench coat, and three of them are not the API. The timing waterfall assigns each millisecond to an owner — DNS to infrastructure, TTFB to the backend, download to the payload. Triage is one bar wide: find the longest, fix that leg, and only then escalate. Teams that skip the waterfall spend days optimizing code that was never the bottleneck.",
    },
    tryIt: [
      "Open the Network tab on any site you use daily, reload, and click the slowest request. Read its Timing tab aloud: which bar is longest? Whose 'fault' is it by the owner table?",
      "Run the curl -w block from section two against a public API twice in a row. Compare the numbers. Explain the differences using the words 'tunnel' and 'cache'.",
      "Take a URL from your own app and decompose it with new URL(). Which part never reaches the server? Move a filter from the fragment into the query and watch it suddenly arrive server-side.",
      "Find a request whose Download bar dwarfs its TTFB. Open the response and estimate: how many fields could you drop? This is the Phase 22 preview, done by eye.",
      "Add <link rel=\"preconnect\" href=\"...\"> for your API host in a scratch page and compare first-fetch timing with and without it, cold cache. Measure, don't assume.",
    ],
    challenge: {
      prompt:
        "Produce a 'request post-mortem' for https://jsonplaceholder.typicode.com/todos/1 using ONLY terminal and browser instruments. Deliverables: (1) a curl run with -w timing variables, with each line annotated by what it measures; (2) a DevTools Timing-tab breakdown of the same request from a cold browser (screenshot or hand-transcribed bars with ms values); (3) a table: for each leg, state who owns it (infrastructure / backend / payload / browser) and one way to shorten it; (4) the transcribed request and response envelopes via curl -v, with the header/body boundary marked; (5) a two-sentence verdict: if this endpoint were 'slow' at 2s, which leg would you check first and why — based on YOUR measured baseline.",
      hints: [
        "curl -v -o /dev/null prints both envelopes to stderr without dumping the body twice; add -s -w separately for timings. The blank line between headers and body is the boundary you mark.",
        "Cold browser = new profile or hard reload with 'Disable cache' on; otherwise the waterfall shows (disk cache) and 0ms bars, which teaches nothing.",
        "For the verdict, compare your measured legs: jsonplaceholder's TTFB is usually the largest leg because DNS/connect are cached/warm on repeat runs — but YOUR numbers decide. The skill is the method, not the answer.",
      ],
      solution: [
        "# ——— (1) annotated curl timings ———",
        "curl -o /dev/null -s -w \"",
        "  dns:     %{time_namelookup}s    # name → IP (resolver chain)\\n",
        "  connect: %{time_connect}s       # TCP handshake done (cumulative)\\n",
        "  tls:     %{time_appconnect}s    # TLS keys agreed (cumulative)\\n",
        "  ttfb:    %{time_starttransfer}s # first response byte (cumulative)\\n",
        "  total:   %{time_total}s         # body fully downloaded\\n\" \\",
        "  https://jsonplaceholder.typicode.com/todos/1",
        "# typical warm-DNS result:",
        "#   dns 0.004 / connect 0.052 / tls 0.105 / ttfb 0.310 / total 0.312",
        "# reading: server think ≈ ttfb − tls ≈ 205ms; download ≈ 2ms.",
        "",
        "# ——— (2) DevTools Timing (cold profile, transcribed) ———",
        "#   Queueing        3ms",
        "#   DNS Lookup     40ms   (cold)",
        "#   Initial conn   55ms",
        "#   SSL            60ms",
        "#   Request sent    1ms",
        "#   Waiting (TTFB) 210ms   ← longest: server/upstream think time",
        "#   Download        4ms   (83-byte body)",
        "",
        "# ——— (3) ownership table ———",
        "#   leg          | owner        | one way to shorten",
        "#   DNS          | infra        | preconnect / warmer resolver",
        "#   Conn+SSL     | infra        | HTTP/2 reuse, fewer origins",
        "#   TTFB         | backend      | index the query, cache the read",
        "#   Download     | payload      | compress, send fewer fields",
        "",
        "# ——— (4) envelopes via curl -v (abridged, boundary marked) ———",
        "# > GET /todos/1 HTTP/1.1            ← request line",
        "# > Host: jsonplaceholder.typicode.com",
        "# > User-Agent: curl/8.x",
        "# > Accept: */*",
        "# >",
        "# < HTTP/1.1 200 OK                  ← status line",
        "# < Content-Type: application/json; charset=utf-8",
        "# < Cache-Control: public, max-age=172800",
        "# <                                    ← BLANK LINE: headers end",
        "# {\"userId\":1,\"id\":1,\"title\":\"…\",\"completed\":false}   ← body",
        "",
        "# ——— (5) verdict ———",
        "# Baseline says TTFB dominates a warm-tunnel request (205 of 312ms),",
        "# so a 2s regression gets checked at the server leg first — query,",
        "# upstream, or deploy — after one cold run rules out DNS/TLS spikes.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "Which URL part NEVER reaches the server?",
        options: ["The fragment (#…)", "The query string", "The path", "The port"],
        answer: 0,
        explain: "The fragment is a client-side bookmark stripped before the request leaves the browser. You cannot server-filter on it — put it in the query if the server needs it.",
      },
      {
        q: "The second request to the same host is usually faster because…",
        options: ["DNS, TCP, and TLS costs are amortized — the tunnel is reused (HTTP/2 multiplexing)", "The server warms up", "Browsers skip headers on repeat requests", "The payload shrinks"],
        answer: 0,
        explain: "The first request pays lookup + handshake; later requests ride the established, encrypted pipe and pay only wait + download. 'Slow first, fast after' is handshake cost.",
      },
      {
        q: "TTFB measures…",
        options: ["Time from the last request byte sent to the first response byte received — the server's think time", "Total download time", "DNS resolution time", "Time to render the response"],
        answer: 0,
        explain: "Waiting (TTFB) is the backend leg: compute, upstream calls, distance. It hides three delays, but it is where backend fixes live.",
      },
      {
        q: "A request's Content-Type header is…",
        options: ["A claim about the body's format — to be validated, not trusted", "Guaranteed correct by the browser", "Optional and decorative", "Set by the server for requests"],
        answer: 0,
        explain: "Headers are metadata claims. A body that lies about its format is an injection vector; servers validate (Phase 20) and attackers probe (Phase 30).",
      },
      {
        q: "A 'slow API' with a 240ms DNS bar and a 140ms TTFB is primarily…",
        options: ["An infrastructure/lookup problem, not a backend problem", "A database problem", "A payload problem", "Unsolvable without caching"],
        answer: 0,
        explain: "Longest bar first, and each bar has an owner. DNS is resolver territory — preconnect or fix the resolver; the backend was fine all along.",
      },
      {
        q: "The headers/body boundary in a raw HTTP message is…",
        options: ["A blank line", "A '---' separator", "The Content-Length value", "There is no boundary"],
        answer: 0,
        explain: "Headers end at the first empty line; everything after is body. Seeing this once in curl -v makes the whole protocol feel like text, because it is.",
      },
    ],
    flashcards: [
      { front: "URL parts", back: "scheme://host:port/path?query#fragment. The fragment never reaches the server; query params arrive as strings; paths/query may need percent-encoding." },
      { front: "The pre-request journey", back: "DNS (name→IP) → TCP handshake (reliable pipe) → TLS (identity + keys). Paid once per tunnel, then reused — why first requests are slowest." },
      { front: "Connection reuse", back: "HTTP/2 multiplexes many requests over one encrypted tunnel; warm requests skip DNS/connect/TLS and pay only TTFB + download." },
      { front: "Request envelope", back: "Request line (METHOD path HTTP/ver) + headers + optional body. Written by hand in Phase 0; still just text on the wire." },
      { front: "Response envelope", back: "Status line (HTTP/ver code reason) + headers + body, separated at the blank line. Read headers first, body second." },
      { front: "TTFB", back: "Waiting for server response: last request byte → first response byte. The backend leg — compute, upstream, distance. Not download time." },
      { front: "The four suspects", back: "'Slow' = DNS/infra, handshake/infra, TTFB/backend, or download/payload. Longest bar first; each has a different owner and fix." },
      { front: "curl timings", back: "-w variables: time_namelookup (DNS), time_connect (TCP), time_appconnect (TLS), time_starttransfer (TTFB), time_total. The browser-free stopwatch." },
    ],
    recap: [
      "A URL is typed data: scheme, host, port, path, query, fragment — the fragment stays home.",
      "Every first request pays DNS + TCP + TLS; reuse makes the rest cheap.",
      "Requests and responses are envelopes of text: line, headers, body.",
      "The Timing tab assigns milliseconds to owners; longest bar is the suspect.",
      "curl -w measures the same journey without a browser — learn both dials.",
    ],
    references: [
      { label: "MDN — What is a URL?", url: "https://developer.mozilla.org/en-US/docs/Web/API/URL" },
      { label: "MDN — An overview of HTTP", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Overview" },
      { label: "web.dev — Understanding the Network panel", url: "https://developer.chrome.com/docs/devtools/network" },
      { label: "curl — man page (write-out variables)", url: "https://curl.se/docs/manpage.html" },
      { label: "MDN — HTTP/2", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/HTTP2" },
    ],
    nextBridge:
      "You can now see a request's whole journey and bill every millisecond. But the envelopes carry vocabulary you haven't formally learned: what makes a GET different from a DELETE beyond the name, which status means 'created' versus 'accepted', and which headers make caching and authorization possible. Next lesson is the language of the wire — methods, status codes, and headers.",
  },
  {
    id: "p07-l2",
    phaseId: "p07",
    title: "Methods, Status Codes & Headers",
    level: "Frontend Developer",
    minutes: 40,
    summary:
      "HTTP is a language with a small, precise vocabulary: a handful of verbs with real semantics (safe vs idempotent), five status families where each code is a contract, and headers that carry everything the body doesn't. This lesson makes you fluent — GET vs POST vs PUT vs PATCH vs DELETE as promises rather than labels, the status codes that actually matter with their retry implications, and the headers that drive caching, authorization, and content negotiation.",
    prerequisites: ["p07-l1 — the request/response envelopes and the timing waterfall"],
    objectives: [
      "Distinguish safe methods (GET, HEAD) from idempotent ones (PUT, DELETE) from neither (POST), and derive retry policy from that.",
      "Choose the right method for an operation and explain what a client may assume about repeating it.",
      "Use the status families correctly: 200/201/204 for success shapes, 301/304 for caching, 401 vs 403, 409, 422, 429, and the 5xx split.",
      "Design API responses where the status code carries the outcome and the body carries the detail — never '200 with error:true'.",
      "Read and set the headers that matter: Content-Type, Accept, Authorization, Cache-Control, ETag/If-None-Match, Location, Retry-After.",
      "Trace how a 401 becomes a refresh flow, a 409 becomes a refetch, and a 429 becomes a backoff — the client-side contracts Phase 19 automates.",
    ],
    simple:
      "HTTP methods are not labels; they are promises. GET promises 'I am only reading — repeat me as often as you like.' DELETE promises 'doing me twice leaves the same result as doing me once.' POST promises nothing — it might create, charge, or send, so you must never blindly repeat it. Status codes are the server's one-word answers: 201 means 'born', 204 means 'done, nothing to show you', 401 means 'who are you?', 403 means 'I know who you are, and the answer is no', 429 means 'slow down'. And headers are the margin notes — what format this is, who sent it, how long you may remember it. Fluent speakers don't guess; they read the promise, the answer, and the margins.",
    why:
      "The entire rest of this course is built on these promises. TanStack Query (Phase 19) retries only idempotent mutations — that rule IS the safe/idempotent distinction. Browser and CDN caching (Phase 27) is driven entirely by Cache-Control and ETag headers. Authentication (Phase 17) rides the Authorization header, and its 401/403 split determines whether your client refreshes a token or shows a permission screen. Webhook verification, rate limiting, pagination — all of it assumes you treat methods as contracts and statuses as machine-readable outcomes. Teams that return 200-with-error:true are writing prose where the protocol offers vocabulary, and every client pays the translation cost forever.",
    mentalModel: {
      title: "The post office promises",
      body: "Think of methods as stamped envelopes with legal promises printed on them. The READ-ONLY stamp (GET) means the clerk may process your request any number of times — the post office even pre-serves it from a shelf (caches it) without asking. The EXACTLY-ONCE-EFFECT stamps (PUT, DELETE) mean duplicates collapse: deleting letter #7 twice still leaves it deleted. The NO-PROMISE stamp (POST) is a signed order — mail it twice and two things happen, so the post office never retries it on your behalf and hands you a receipt with the new address (201 + Location). The status stamp on the reply is the clerk's verdict in one word, and the header cards are the filing instructions: 'keep this answer for an hour', 'come back after 30 seconds', 'this is who I am'. Machines can run an entire business on these stamps — that's the point.",
    },
    sections: [
      {
        heading: "Methods as promises — safe, idempotent, neither",
        body: [
          "Three tiers of promise. SAFE methods (GET, HEAD) change nothing server-side, so they may be cached, prefetched, and repeated without limit — and clients/browsers/CDNs WILL repeat them, so a GET that mutates is a landmine. IDEMPOTENT methods (PUT, DELETE, plus GET) promise the same end-state however many times they run: DELETE /todos/7 twice leaves it deleted; PUT /todos/7 with the same body converges. NON-idempotent (POST, and typically PATCH with a body computed elsewhere) may do a new thing each time — so infrastructure must never auto-retry it, and double-submit guards exist (your P01-L2 button-disable was this, client-side).",
          "This taxonomy generates your retry policy for free, which is exactly how serious clients behave: network failure on a GET? Retry. On a PUT/DELETE with the same payload? Retry. On a POST? NEVER automatically — ask the user, or use an idempotency key (Phase 29) so the server can dedupe. PUT vs PATCH, precisely: PUT replaces the whole resource (absent fields reset), PATCH applies a partial change. Choose PUT when clients always hold the full shape, PATCH when partial updates are the norm — and say which in your API docs, because 'PATCH semantics' vary unless you define them.",
        ],
        code: [
          {
            file: "methods.js — the promises, in code",
            lang: "js",
            code: [
              "// GET: safe + idempotent → repeatable, cacheable",
              "await fetch(\"/todos?status=open\");",
              "",
              "// POST: no promises → guard double-submits, never auto-retry",
              "await fetch(\"/todos\", {",
              "  method: \"POST\",",
              "  body: JSON.stringify({ title: \"Ship\" }),  // → 201 + Location: /todos/8",
              "});",
              "",
              "// PUT: replaces the WHOLE resource (idempotent)",
              "await fetch(\"/todos/7\", {",
              "  method: \"PUT\",",
              "  body: JSON.stringify({ title: \"Ship\", done: true }), // absent fields reset",
              "});",
              "",
              "// PATCH: partial change (make your semantics explicit)",
              "await fetch(\"/todos/7\", {",
              "  method: \"PATCH\",",
              "  body: JSON.stringify({ done: true }),  // only done moves",
              "});",
              "",
              "// DELETE: idempotent — the second call answers 404, state unchanged",
              "await fetch(\"/todos/7\", { method: \"DELETE\" });  // → 204",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The status families — one-word verdicts with contracts",
        body: [
          "2xx success, in three shapes: 200 OK (here is the answer, with body), 201 Created (POST worked; the Location header points at the newborn resource), 204 No Content (succeeded, nothing to return — the honest DELETE/ack answer, and the reason parsing must skip the body). 3xx redirection: 301 Moved Permanently (update your bookmark; caches rewrite), 304 Not Modified (your cached copy is still good — the quiet hero of Phase 27), never to be confused with errors. 4xx is YOUR fault — the request is wrong: 400 malformed, 401 who-are-you (missing/bad/expired credentials → the client may refresh and retry), 403 I-know-you-and-no (authz failure → show a permission screen, do NOT refresh), 404 nothing here, 409 conflict (stale version — refetch and merge), 422 well-formed but semantically invalid (field errors), 429 slow down (honor Retry-After).",
          "5xx is THEIR fault: 500 the server crashed inside, 502 a bad answer from upstream, 503 temporarily overloaded (often with Retry-After). The retry rule crystallizes here: 5xx on an idempotent request → retry with backoff; on a POST → don't. And the design rule: the code carries the OUTCOME (created/not-found/forbidden), the body carries the DETAIL (the new resource, the field errors, the message). 'HTTP 200' with {\"error\": true} forces every client to double-parse every response and breaks any middleware, proxy, or cache that reads codes — it is the single most expensive convenience in API design.",
        ],
        code: [
          {
            file: "statuses.js — codes a client acts on",
            lang: "js",
            code: [
              "async function saveTodo(draft) {",
              "  const res = await fetch(\"/todos\", {",
              "    method: \"POST\",",
              "    headers: { \"Content-Type\": \"application/json\", Authorization: `Bearer ${token}` },",
              "    body: JSON.stringify(draft),",
              "  });",
              "",
              "  switch (res.status) {",
              "    case 201:                       // created — follow the receipt",
              "      return { ok: true, data: await res.json(), at: res.headers.get(\"Location\") };",
              "    case 401:                       // identity problem → refresh token, retry ONCE",
              "      return retryAfterRefresh(draft);",
              "    case 403:                       // permission problem → surface it, don't retry",
              "      return { ok: false, kind: \"forbidden\" };",
              "    case 422:                       // valid envelope, invalid content → field errors",
              "      return { ok: false, kind: \"validation\", fields: (await res.json()).errors };",
              "    case 429: {                     // rate limited → honor Retry-After",
              "      const wait = Number(res.headers.get(\"Retry-After\") ?? 1);",
              "      return { ok: false, kind: \"throttled\", retryAfterS: wait };",
              "    }",
              "    default:",
              "      if (res.status >= 500) return { ok: false, kind: \"server\" }; // retriable if idempotent",
              "      return { ok: false, kind: \"other\", status: res.status };",
              "  }",
              "}",
            ].join("\n"),
            caption: "Every branch is a USER-EXPERIENCE decision, chosen by a number. That is what a status code buys you.",
          },
        ],
      },
      {
        heading: "Headers — the margin notes that run the web",
        body: [
          "Six headers do most of the daily work. Content-Type declares the body's format (application/json; charset=utf-8) — servers validate it (Phase 20), clients switch parsers on it. Accept declares what the client wants back — content negotiation in one line. Authorization carries credentials; Bearer <token> is the Supabase/NestJS shape from Phase 17 onward — and it lives in a header, never a URL, because URLs get logged everywhere. Cache-Control is the caching contract (no-store for private data, max-age=N for public, immutable for versioned assets) that Phase 27 dissects. The ETag/If-None-Match pair is revalidation: the server fingerprints a response, the client echoes the fingerprint next time, and a 304 says 'still the same' — zero body bytes.",
          "Two more earn their place now. Location accompanies 201 to point at the created resource — the receipt your client can follow. Retry-After accompanies 429/503 to say when to come back — honoring it is what separates polite clients from the ones that get banned. The debugging habit these install: when an API misbehaves, read the response HEADERS before the body (curl -I or the Network headers panel) — the answer to 'why' is usually on a card, not in the parcel.",
        ],
        code: [
          {
            file: "headers.sh — the cards, read live",
            lang: "bash",
            code: [
              "# response headers only — the fastest API read there is:",
              "curl -sI https://api.example.com/todos/7",
              "# HTTP/2 200",
              "# content-type: application/json",
              "# cache-control: no-store          ← private: never cache",
              "# etag: W/\"a1b2c3\"",
              "",
              "# revalidation: offer the fingerprint back",
              "curl -sI https://api.example.com/todos/7 -H 'If-None-Match: W/\"a1b2c3\"'",
              "# HTTP/2 304                       ← unchanged; no body follows",
              "",
              "# what the CLIENT promises it accepts:",
              "curl -s https://api.example.com/report -H \"Accept: application/json\"",
              "",
              "# the credential card (never in the URL — URLs get logged):",
              "curl -s https://api.example.com/me -H \"Authorization: Bearer $TOKEN\"",
            ].join("\n"),
          },
        ],
        demo: "status-match",
      },
      {
        heading: "The client contract — what each code makes you do",
        body: [
          "Fluency is knowing the choreography, not the dictionary. 401 → the identity expired: refresh the token (Phase 17) and retry the original request exactly once; a second 401 means the user out. 403 → the identity is fine and the answer is no: render a permission message, never loop. 409 → your copy is stale: refetch, re-apply intent or ask the user to merge — the collaborative-editing shape Phase 42 leans on. 422 → map the field errors onto the form (the Phase 20 pipeline). 429 → read Retry-After, back off, and surface 'we're being throttled' if it persists. 5xx on GET/PUT/DELETE → exponential backoff with jitter; on POST → ask before retrying unless an idempotency key makes repeats safe.",
          "You will not hand-code most of this for long — TanStack Query's retry/mutation config (Phase 19) and your API client's interceptor (also Phase 19) automate it. But the configuration of those tools IS this table: which codes retry, which surface, which refresh. Learn the choreography now and Phase 19 is wiring; skip it and Phase 19 is guessing.",
        ],
      },
    ],
    mistake: {
      title: "200 OK with error: true inside",
      wrong: [
        "// server:",
        "app.post(\"/todos\", (req, res) => {",
        "  if (!req.body.title) {",
        "    return res.status(200).json({ error: true, message: \"title required\" });",
        "  }",
        "  …",
        "});",
        "",
        "// consequences, compounding:",
        "// • every client must double-parse EVERY response (res.ok lies)",
        "// • proxies/CDNs/monitoring see 'success' and cache/alert wrongly",
        "// • retries treat a validation failure as a win",
        "// • the browser Network tab is useless for triage — all green",
        "// • three teams later, someone ships error: \"yes\" (string). Nobody notices.",
      ].join("\n"),
      right: [
        "app.post(\"/todos\", (req, res) => {",
        "  if (!req.body.title) {",
        "    return res.status(422).json({",
        "      errors: [{ field: \"title\", issue: \"required\" }],",
        "    });",
        "  }",
        "  const created = db.create(…);",
        "  return res.status(201).location(`/todos/${created.id}`).json(created);",
        "});",
        "",
        "// the code carries the OUTCOME (422 invalid, 201 created);",
        "// the body carries the DETAIL (which field, where it lives).",
        "// fetch's res.ok, res.status, DevTools, caches, and alerts all",
        "// become truthful for free.",
      ].join("\n"),
      explain:
        "Status codes are the protocol's machine-readable verdict, and every layer between you and the user — fetch's res.ok, browser caching, CDNs, load balancers, uptime monitors, retry middleware — reads them. Returning 200 for a failure turns all of that infrastructure into a liar and taxes every client with a second parse. Codes for outcomes, bodies for details — it is the difference between a protocol and a pile of JSON.",
    },
    tryIt: [
      "With curl -I, collect the response headers of three APIs you use. For each: what does Cache-Control say, is there an ETag, and what does that imply a browser may do?",
      "Test idempotence hands-on against a scratch API (json-server works): DELETE the same id twice and record both status codes. PUT the same body twice. POST twice. Which pair changed the world twice?",
      "Reproduce the 401/403 distinction against any auth-protected endpoint (or a mock): hit it with no token, then with a valid token for a resource you don't own. Write down which client action each code implies.",
      "Open DevTools on a site with images and find a 304. What request header made it possible? (Look for If-None-Match or If-Modified-Since.)",
      "Refactor one of your fetch handlers into the status-switch from section two, with real branches for 201/401/422/429/5xx. Notice how many UX decisions the numbers make for you.",
    ],
    challenge: {
      prompt:
        "Design (on paper, then as a typed table in TypeScript) the HTTP contract for a 'notes with sharing' feature: list my notes (paginated), create a note, read one, fully update one, patch its title only, delete one, and share a note with another user by email. For EACH operation deliver: method + path, one request line, the success code + what the body/headers contain, and TWO distinct failure codes with the client choreography for each (retry? refresh? surface?). Constraints: no 200-with-error anywhere; DELETE must be idempotent by design; sharing an already-shared email must produce a conflict, not a duplicate; pagination lives in the query string, not the path.",
      hints: [
        "Start from the promises: which operations are safe (cacheable/prefetchable)? Which are idempotent (retryable)? Sharing is a POST — it creates a share, and it must NOT be auto-retried.",
        "The conflict case is the tell: 409 with the existing share's id in the body lets the client offer 'already shared — view instead'. A 200-with-duplicate is the bug the prompt forbids.",
        "For failures, pair each code with a choreography verb: 401→refresh+retry-once, 403→surface, 404→surface (or optimistic remove), 422→map field errors, 429→Retry-After backoff, 5xx→backoff only if idempotent.",
      ],
      solution: [
        "type Op = {",
        "  name: string; method: \"GET\" | \"POST\" | \"PUT\" | \"PATCH\" | \"DELETE\";",
        "  path: string; request?: string; success: string; failures: string[];",
        "};",
        "",
        "export const NOTES_API: Op[] = [",
        "  {",
        "    name: \"List my notes\", method: \"GET\", path: \"/notes?page=2&limit=20\",",
        "    success: \"200 + { items: Note[], nextCursor } — safe → prefetch/cache OK\",",
        "    failures: [",
        "      \"401 → refresh token, retry once\",",
        "      \"429 → honor Retry-After, then refetch\",",
        "    ],",
        "  },",
        "  {",
        "    name: \"Create note\", method: \"POST\", path: \"/notes\",",
        "    request: 'POST body {\"title\":\"…\",\"body\":\"…\"} → 201 + Location: /notes/9 + created Note',",
        "    success: \"201 + Location — NOT retriable automatically (no promises)\",",
        "    failures: [",
        "      \"422 → map errors[].field onto the form\",",
        "      \"5xx → DO NOT auto-retry (may have created); ask or use idempotency key (P29)\",",
        "    ],",
        "  },",
        "  {",
        "    name: \"Read one\", method: \"GET\", path: \"/notes/9\",",
        "    success: \"200 + Note + ETag — revalidatable with If-None-Match → 304\",",
        "    failures: [",
        "      \"404 → note gone: remove from cache, surface softly\",",
        "      \"403 → shared-without-access: show permission screen, never refresh-loop\",",
        "    ],",
        "  },",
        "  {",
        "    name: \"Full update\", method: \"PUT\", path: \"/notes/9\",",
        "    request: \"full Note in body → 200 + updated Note (idempotent: converges)\",",
        "    success: \"200 + Note — retriable on 5xx/network (same body converges)\",",
        "    failures: [",
        "      \"409 → stale version: refetch, re-merge, or ask the user\",",
        "      \"422 → field errors → form mapping\",",
        "    ],",
        "  },",
        "  {",
        "    name: \"Patch title\", method: \"PATCH\", path: \"/notes/9\",",
        "    request: '{\"title\":\"new title\"} → 200 + updated Note (partial semantics, documented)',",
        "    success: \"200 + Note\",",
        "    failures: [",
        "      \"422 → title constraints violated → inline error\",",
        "      \"409 → edited elsewhere concurrently → refetch\",",
        "    ],",
        "  },",
        "  {",
        "    name: \"Delete\", method: \"DELETE\", path: \"/notes/9\",",
        "    success: \"204 No Content — idempotent: second DELETE → 404, state unchanged\",",
        "    failures: [",
        "      \"404 → already gone: treat as success (idempotence), drop from cache\",",
        "      \"403 → shared note owned by another: surface, no retry\",",
        "    ],",
        "  },",
        "  {",
        "    name: \"Share by email\", method: \"POST\", path: \"/notes/9/shares\",",
        "    request: '{\"email\":\"ada@x.dev\"} → 201 + Location: /notes/9/shares/3',",
        "    success: \"201 + Location — not auto-retriable\",",
        "    failures: [",
        "      \"409 → already shared: body carries the existing share id; the client offers to view it instead of duplicating\",",
        "      \"422 → email malformed/unknown user → inline error\",",
        "    ],",
        "  },",
        "];",
        "",
        "/* The contract reads like law: every operation states its promise",
        "   (safe/idempotent/none), its verdict codes, and the choreography",
        "   each verdict triggers. Phase 19 turns this table into a typed",
        "   client; Phase 29 adds the idempotency keys that make POST safe",
        "   to retry. Nothing here required a framework — that's the point. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A client may automatically retry a failed request when…",
        options: ["The method is idempotent (GET/PUT/DELETE) — the repeat converges to the same state", "It's a POST with the same body", "The status was 422", "Any 5xx, regardless of method"],
        answer: 0,
        explain: "Idempotence is the retry license: same result however many times. POST promises nothing — repeating it may duplicate. 422 is a validation verdict, not transient.",
      },
      {
        q: "The correct response for a successful POST that creates a resource is…",
        options: ["201 Created with a Location header pointing at the new resource", "200 OK with the id buried in the body", "204 No Content always", "301 Moved Permanently"],
        answer: 0,
        explain: "201 says 'born' and Location is the receipt a client can follow. 204 is for success-with-nothing-to-show (DELETE); 200-with-id works but drops the protocol's vocabulary.",
      },
      {
        q: "401 vs 403 — the difference that changes client behavior is…",
        options: ["401 = identity problem (refresh token, retry once); 403 = permission problem (surface it, never refresh-loop)", "401 is server-side, 403 client-side", "They're interchangeable", "403 means rate-limited"],
        answer: 0,
        explain: "401 asks 'who are you?' — credentials may be fixable. 403 answers 'I know you, and no' — refreshing tokens forever is the classic authz bug.",
      },
      {
        q: "304 Not Modified means…",
        options: ["Your cached copy is still valid — the server sent headers, no body", "The resource moved", "The request was invalid", "The server is down"],
        answer: 0,
        explain: "Revalidation: you offered ETag/If-None-Match, the server agreed nothing changed, and zero body bytes traveled. The quiet engine of Phase 27's caching.",
      },
      {
        q: "Why must Authorization ride a header and never the URL?",
        options: ["URLs are logged by servers, proxies, and browser history — headers are not", "Browsers block URLs with tokens", "It's slower", "Headers are encrypted but URLs are not"],
        answer: 0,
        explain: "Query strings end up in access logs, analytics, and referer headers. The path/query must stay credential-free; Authorization is the sanctioned card. (And HTTPS protects both in transit — the logging risk is the point.)",
      },
      {
        q: "A polite client handles 429 by…",
        options: ["Reading Retry-After, backing off, and surfacing throttling if it persists", "Retrying immediately until it works", "Deleting the user's session", "Switching to GET"],
        answer: 0,
        explain: "429 is the server asking for space; Retry-After says how long. Immediate retries are how clients get banned — and how outages amplify.",
      },
    ],
    flashcards: [
      { front: "Safe methods", back: "GET/HEAD: read-only, repeatable, cacheable. A GET that mutates is a landmine — clients and CDNs WILL repeat it." },
      { front: "Idempotent methods", back: "GET/PUT/DELETE: same end-state however many times applied. This property IS the retry license." },
      { front: "POST's promise", back: "None. May create/charge/send each time — never auto-retried without an idempotency key. Guard double-submits client-side." },
      { front: "PUT vs PATCH", back: "PUT replaces the whole resource (absent fields reset); PATCH applies a partial change. Document your PATCH semantics or they vary." },
      { front: "The success trio", back: "200 OK + body, 201 Created + Location, 204 No Content (no body to parse). The code says the shape." },
      { front: "401 vs 403", back: "401: who are you? → refresh token, retry once. 403: I know you, no → surface permission error, never loop." },
      { front: "409 / 422 / 429", back: "409 conflict → refetch/merge. 422 semantic validation → map field errors to the form. 429 throttled → honor Retry-After." },
      { front: "Codes vs body", back: "The status code carries the OUTCOME; the body carries the DETAIL. 200-with-error:true makes every infrastructure layer a liar." },
    ],
    recap: [
      "Methods are promises: safe, idempotent, or none — and the promise dictates retry policy.",
      "Success has three shapes (200/201+Location/204); failure codes each trigger a choreography.",
      "401 refreshes, 403 surfaces, 409 refetches, 422 maps fields, 429 backs off.",
      "Headers run the show: Content-Type, Accept, Authorization, Cache-Control, ETag, Location, Retry-After.",
      "Codes carry outcomes, bodies carry details — the rule every client, cache, and monitor depends on.",
    ],
    references: [
      { label: "MDN — HTTP request methods", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Methods" },
      { label: "MDN — HTTP response status codes", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Status" },
      { label: "MDN — HTTP headers", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers" },
      { label: "RFC 9110 — HTTP Semantics (idempotence)", url: "https://www.rfc-editor.org/rfc/rfc9110.html#name-idempotent-methods" },
      { label: "MDN — ETag & conditional requests", url: "https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/ETag" },
    ],
    nextBridge:
      "You speak the wire's vocabulary. Next, the two questions every browser session quietly answers: how does a server remember you across stateless requests — cookies and sessions — and how does a stateless API recognize you instead, with bearer tokens? The identity-on-the-wire lesson, and the direct ancestor of Supabase Auth.",
  },
];

export const LESSON_CONTENT_P7: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P7.map((l) => [l.id, l]),
);
