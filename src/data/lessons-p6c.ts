import type { LessonContent } from "./types";

/**
 * Pass 018 bounded content batch: Phase 6 close (L5–L6).
 * Completes the React Developer stage. Full quality contract. No placeholders.
 */
export const LESSONS_P6C: LessonContent[] = [
  {
    id: "p06-l5",
    phaseId: "p06",
    title: "Then-vs-Now: Outdated React vs Current Stable",
    level: "React Developer",
    minutes: 35,
    summary:
      "React's history is written in its tutorials: class components, lifecycle methods, HOCs, render props — and most of the code you'll be paid to read still contains them. This lesson installs the translation table (class → function, lifecycle → effects, HOC/render-props → hooks), the era-detection instincts that date a snippet in five seconds, and the professional modernization discipline: migrate incrementally at change-boundaries, never rewrite working code for fashion.",
    prerequisites: ["p06-l4 — profiling & memoization judgment", "p05-l2 — useEffect synchronization model"],
    objectives: [
      "Date a code snippet by era (class/HOC/render-props/hooks/concurrent) using five-second tells.",
      "Translate class components to functions: constructor state → useState, lifecycles → effects with deps, this.setState semantics → updater rules.",
      "Map lifecycle methods to their effect equivalents — and name the ones with NO direct equivalent (getDerivedStateFromProps, shouldComponentUpdate).",
      "Explain why HOCs and render props lost to hooks: inversion of control without wrapper hell or nesting pyramids.",
      "Modernize incrementally: coexistence rules, react-codemod, official migration guides, and when to leave class code alone.",
      "Read old tutorials safely: version-check before trusting, and spot the patterns that silently break under Strict Mode.",
    ],
    simple:
      "Languages accumulate sediment: new layers are poured on top, and the old layers don't vanish — they stay load-bearing in millions of apps. React is no different. Code from 2017 (classes, componentDidMount), 2016 (HOCs wrapping HOCs), and 2018 (render props) still runs in production everywhere, and every job interview includes 'can you read this legacy file?' The good news: the translations are mechanical and lossless. A class component is a function component wearing a ceremony costume; componentDidMount is a useEffect with an empty dependency list; an HOC is a custom hook waiting to be unwrapped. Learn the table, and five years of internet tutorials stop being a minefield and become an archive you can date, translate, and modernize on your own schedule.",
    why:
      "This is a job skill dressed as a history lesson. You will inherit codebases where the oldest file predates hooks; you will copy an example that turns out to be from 2018; you will review a PR that mixes eras. Developers without the translation table either freeze (can't read the old) or bulldoze (rewrite working code and ship regressions). The ones who move fast AND safe can do three things this lesson drills: date a pattern instantly, translate it without changing behavior, and modernize incrementally so each commit stays reviewable. The same discipline powers the course's own Then-vs-Now spine — every phase revisits an earlier pattern and asks what changed and why.",
    mentalModel: {
      title: "The museum with working exhibits",
      body: "A living codebase is a museum where the exhibits still have to work: the 2016 wing (HOCs), the 2017 wing (classes), the 2019 wing (hooks), the 2024 wing (concurrent + compiler). Your job is curator, not demolition crew. The curator can walk any wing and explain each exhibit in modern terms — 'this componentDidMount is really a mount-time synchronization', 'this withUser HOC is a useUser hook in a tuxedo'. When an exhibit needs repair, the curator fixes it in place or carefully relocates it to a newer wing — one artifact at a time, with the museum open the whole while. Nobody tears down a wing because a newer wing exists; and nobody refuses to learn the new wing because the old one still stands.",
    },
    sections: [
      {
        heading: "Era detection — dating code in five seconds",
        body: [
          "Five tells, checked in order. ONE: ReactDOM.render(<App/>, el) or import React from 'react' + class extends React.Component → pre-2022 / class era. TWO: withSomething(Component) or compose(withA, withB) exports → the 2015–2018 HOC era (Redux's connect survives as a living fossil). THREE: components that take children as a function — <Mouse>{pos => …}</Mouse> — the 2017–2019 render-props era. FOUR: string refs (ref=\"box\") or legacy this.context → genuinely ancient (2015-ish), handle with codemods, not bare hands. FIVE: createRoot + hooks + possibly use() / actions → current stable.",
          "The dating skill matters because tutorials rarely announce their era: a 2018 blog post renders perfectly until you notice the lifecycle methods, and a 2016 one will quietly teach you patterns that now cause Strict Mode double-fires and wrapper hell. The professional reflex, installed once and used forever: before copying ANY example, ask 'which wing of the museum is this from?' — then check the official docs for the current shape. The Version Matrix habit from Phase 0 applies to tutorials exactly as it does to packages.",
        ],
        code: [
          {
            file: "era-tells.tsx — spot the decade",
            lang: "tsx",
            code: [
              "// 2017 wing — class + lifecycles:",
              "class Ticker extends React.Component {",
              "  componentDidMount() { this.timer = setInterval(…, 1000); }",
              "  componentWillUnmount() { clearInterval(this.timer); }",
              "}",
              "",
              "// 2016 wing — HOC composition:",
              "export default compose(withAuth, withTheme, withLogger)(Panel);",
              "",
              "// 2018 wing — render props:",
              "<WindowSize>{({ width }) => (width < 600 ? <Mobile/> : <Desktop/>)}</WindowSize>",
              "",
              "// 2015 wing — string refs (codemod territory):",
              "<input ref=\"search\" />  // this.refs.search — deprecated since forever",
              "",
              "// current wing — hooks + concurrent:",
              "const [w, setW] = useState(0);",
              "useEffect(() => { const id = setInterval(…); return () => clearInterval(id); }, []);",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Class → function — the translation table",
        body: [
          "The core mapping, line by line. constructor + this.state = {x: 1} → const [x, setX] = useState(1). this.setState({x: 2}) → setX(2) — with ONE semantic difference to memorize: class setState MERGED partial objects and queued updates, while useState REPLACES the value (spread {...prev, ...patch} when you need the merge) and updaters are the functional form. componentDidMount → useEffect(fn, []) (mount-time sync). componentDidUpdate(prev) → effect with the specific deps it read (prev comparisons become the dep array itself). componentWillUnmount → the effect's cleanup return. this.props/this.state reads → plain function parameters and closure reads of the snapshot.",
          "Three translations need care. getDerivedStateFromProps and 'state derived from props' → not an effect at all: compute during render (const sorted = items.toSorted()) — the P05-L2 no-effect escape, which class code often faked with lifecycle. shouldComponentUpdate → React.memo (same shallow-compare idea, declarative). And event handlers: class methods needed .bind(this) or arrow properties because this was dynamic; function components have no this to bind — handlers are closures, full stop, and that is why the entire stale-this bug family disappears.",
        ],
        code: [
          {
            file: "translation.tsx — the same widget, two eras",
            lang: "tsx",
            code: [
              "// THEN — 2017 wing:",
              "class OnlineCount extends React.Component {",
              "  state = { count: 0 };",
              "  componentDidMount() {",
              "    this.id = setInterval(() => this.setState({ count: this.state.count + 1 }), 1000);",
              "    // ⚠ reads this.state.count in a closure — classic staleness",
              "  }",
              "  componentWillUnmount() { clearInterval(this.id); }",
              "  render() { return <p>{this.state.count} online</p>; }",
              "}",
              "",
              "// NOW — same behavior, honest translation:",
              "function OnlineCount() {",
              "  const [count, setCount] = useState(0);",
              "  useEffect(() => {",
              "    const id = setInterval(() => setCount((c) => c + 1), 1000); // updater: no staleness",
              "    return () => clearInterval(id);   // cWU → cleanup, one function apart",
              "  }, []);                              // cDM → empty-deps effect",
              "  return <p>{count} online</p>;",
              "}",
              "// Fewer lines, no this, setup+teardown colocated, staleness gone.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "HOCs & render props → hooks — what actually won, and why",
        body: [
          "Both older patterns solved one problem: sharing logic across components without copy-paste. HOCs (withUser(Profile)) wrapped your component in someone else's, injecting props — but nested HOCs built 'wrapper hell' (dev-tools full of Anonymous wrappers), prop collisions were silent, and types degraded at every layer. Render props inverted control (<User>{user => …}</User>) — flexible, but five of them nested into an indentation pyramid and the logic stayed trapped in JSX position.",
          "Hooks won because they deliver the SAME reuse with neither cost: const user = useUser() pulls the logic into your component without wrapping it, names every value explicitly (no silent prop injection), composes flatly (ten hooks, zero nesting), and types perfectly. The translation is usually trivial: withX(Wrapped) becomes useX() called inside Wrapped; the injected props become the hook's return value. One honest caveat: library authors still sometimes ship HOCs for cross-version compatibility, and Redux's connect() is everywhere in older code — read them fluently, write hooks, and wrap only when a library forces the shape.",
        ],
        code: [
          {
            file: "patterns.tsx — one idea, three costumes",
            lang: "tsx",
            code: [
              "// 2016: the HOC costume",
              "function withMousePosition(Wrapped) {",
              "  return function WithMouse(props) {",
              "    const [pos, setPos] = useState({ x: 0, y: 0 });",
              "    useEffect(() => { /* listener */ }, []);",
              "    return <Wrapped {...props} mouse={pos} />;  // silent prop injection",
              "  };",
              "}",
              "const Panel2016 = withMousePosition(Panel);",
              "",
              "// 2018: the render-prop costume",
              "<MousePosition>",
              "  {(mouse) => <Panel mouse={mouse} />}   // logic trapped in JSX position",
              "</MousePosition>",
              "",
              "// now: the hook — same logic, no wrapper, no pyramid",
              "function Panel() {",
              "  const mouse = useMousePosition();      // flat, typed, named",
              "  return …;",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Modernizing incrementally — the professional discipline",
        body: [
          "The rule that separates seniors from bull-dozers: modernize at CHANGE boundaries, not at calendar boundaries. A class component that works and nobody touches earns its right to stay a class component; the moment a ticket requires changing it, that's when the translation happens — behavior frozen, diff reviewable, tests as the witness (Phase 31 makes this airtight). Wholesale 'let's rewrite the old stuff' projects are how working software dies in committee; file-by-file migration at the point of change is how codebases rejuvenate without ever stopping.",
          "Tooling does the heavy lifting you shouldn't hand-type. jscodeshift's react-codemod converts many mechanical patterns (create-element, pure-render, even class→function for simple cases); the official react.dev migration guides beat any tutorial for createRoot, legacy context, and string refs. And two coexistence facts keep mixed-era codebases sane: hooks and classes interoperate freely in one tree (they're the same runtime underneath), and Strict Mode's double-fires expose class-era assumptions (like 'componentDidMount runs once and only once') in development, before they bite in the concurrent production build.",
        ],
      },
      {
        heading: "Lab — modernize a 2018 widget, line by line",
        body: [
          "The exercise that makes the table reflexive: take a real 2018-shaped component — class state, componentDidMount fetch, componentDidUpdate refetch-on-prop-change, componentWillUnmount teardown, wrapped in a withPolling HOC — and produce the hook version with a written mapping table next to it. The acceptance test is brutal and simple: identical behavior, no this anywhere, one effect (or two honestly-scoped ones) instead of three lifecycles, and the HOC gone — its logic living as a custom hook from P05-L7. If your version is longer than the original, you translated the ceremony instead of the intent; hooks reward you for writing what you MEAN.",
          "The mapping table is the deliverable worth keeping — write it once in your team's docs and onboarding halves. cDM→useEffect([]), cDU(prop)→useEffect([prop]), cWU→cleanup, this.setState merge→spread or updater, HOC→custom hook. Every row you can recite from memory is a legacy file you can read on sight.",
        ],
      },
    ],
    mistake: {
      title: "The ceremonial rewrite",
      wrong: [
        "// ticket: 'fix the off-by-one in LegacyChart'",
        "// developer response: rewrite the whole 400-line class component",
        "// into hooks, rename six props, restructure the state,",
        "// adopt a new library for the tooltip…",
        "",
        "// result: 38-file diff. Reviewers can't tell the bug fix from",
        "// the refactor. The off-by-one ships with two NEW bugs that",
        "// nobody can attribute. The fix is buried; the regressions are",
        "// 'mysterious'. The codebase is 1% more modern and 100% less trusted.",
      ].join("\n"),
      right: [
        "// same ticket, professional shape:",
        "// commit 1: fix the off-by-one INSIDE the class component",
        "//           (three-line diff, instantly reviewable, test added)",
        "// commit 2 (separate PR, only if the file keeps changing):",
        "//           translate to hooks, behavior frozen,",
        "//           tests from commit 1 as the witness — zero behavior diff",
        "",
        "// modernize at change boundaries. each commit answers ONE question.",
        "// the museum stays open the whole time.",
      ].join("\n"),
      explain:
        "Mixed diffs destroy attribution: when the rewrite breaks something, nobody knows whether the translation or the fix caused it, so both get reverted and the bug survives. Separate the behavior change from the era change, freeze behavior during translation, and let tests arbitrate. Legacy code deserves the same commit hygiene as new code — arguably more, because the blast radius is larger.",
    },
    tryIt: [
      "Era-hunt: open any tutorial or library example you've saved and date it using the five tells. Write down the tell that gave it away. Collect three artifacts from three different wings.",
      "Translate the OnlineCount widget from section two by hand — close the lesson, write the hook version from memory, then diff against the answer. Note where your instincts still reach for class shapes.",
      "Find a componentDidUpdate(prevProps) pattern in the wild (search 'componentDidUpdate prevProps'). Translate it to an effect and explain, out loud, where the prevProps comparison went (answer: into the dependency array).",
      "Run a codemod in a scratch repo: npx jscodeshift with react-codemod's pure-internal-component (or create-element) transform on a small class file. Read the diff like a reviewer.",
      "Take your own oldest React code (or the Todo app's P02 vanilla version as a stand-in) and write the five-row mapping table for it. Which row was hardest to justify?",
    ],
    challenge: {
      prompt:
        "Modernize this 2018 widget WITHOUT changing its behavior. Deliver: (1) the hook version, (2) a mapping table (era construct → modern equivalent), (3) one paragraph: what class-era bug risk the translation structurally removed.\n\nclass StockTicker extends React.Component {\n  constructor(props) {\n    super(props);\n    this.state = { price: null, error: null };\n  }\n  componentDidMount() {\n    this.load(this.props.symbol);\n    this.id = setInterval(() => this.load(this.props.symbol), this.props.intervalMs || 5000);\n  }\n  componentDidUpdate(prev) {\n    if (prev.symbol !== this.props.symbol) {\n      clearInterval(this.id);\n      this.load(this.props.symbol);\n      this.id = setInterval(() => this.load(this.props.symbol), this.props.intervalMs || 5000);\n    }\n  }\n  componentWillUnmount() { clearInterval(this.id); }\n  async load(symbol) {\n    try {\n      const res = await fetch(`/api/stocks/${symbol}`);\n      this.setState({ price: await res.json(), error: null });\n    } catch (e) { this.setState({ error: e.message }); }\n  }\n  render() {\n    if (this.state.error) return <p role=\"alert\">{this.state.error}</p>;\n    if (this.state.price == null) return <p>Loading…</p>;\n    return <p>{this.props.symbol}: {this.state.price.cents / 100}</p>;\n  }\n}",
      hints: [
        "The cDM + cDU(symbol) pair is ONE effect with [symbol] in its deps: it runs on mount AND on symbol change, and its cleanup replaces BOTH the unmount clearInterval and the manual clear-then-restart in cDU. That collapse is the whole lesson.",
        "setState merge semantics: { price, error: null } replaces both fields — with useState either use two states and set both, or one object state with the spread merge. The load() closure's this.setState becomes plain setPrice/setError.",
        "The removed bug risk: the class restarts its interval by hand and reads this.props.symbol in a closure at restart time — easy to leak an old interval if a code path forgets clearInterval. The effect's cleanup makes the teardown structural: every dep change and unmount cleans up automatically. Also: no this-binding hazards in the handlers.",
      ],
      solution: [
        "function StockTicker({ symbol, intervalMs = 5000 }: {",
        "  symbol: string;",
        "  intervalMs?: number;",
        "}) {",
        "  const [price, setPrice] = useState<number | null>(null);",
        "  const [error, setError] = useState<string | null>(null);",
        "",
        "  // cDM + cDU(symbol) + cWU → ONE effect, [symbol]:",
        "  useEffect(() => {",
        "    let cancelled = false;                 // stale-answer guard (P05-L2)",
        "    async function load() {",
        "      try {",
        "        const res = await fetch(`/api/stocks/${symbol}`);",
        "        if (cancelled) return;",
        "        const data = await res.json();",
        "        setPrice(data.cents);              // setState merge → two setters",
        "        setError(null);",
        "      } catch (e) {",
        "        if (!cancelled) setError(e instanceof Error ? e.message : String(e));",
        "      }",
        "    }",
        "    load();                                 // what componentDidMount did",
        "    const id = setInterval(load, intervalMs);",
        "    return () => {                          // what cWU did — AND what the",
        "      cancelled = true;                     // cDU clear/restart did, free",
        "      clearInterval(id);",
        "    };",
        "  }, [symbol, intervalMs]);",
        "",
        "  if (error) return <p role=\"alert\">{error}</p>;",
        "  if (price == null) return <p>Loading…</p>;",
        "  return <p>{symbol}: {(price / 100).toFixed(2)}</p>;",
        "}",
        "",
        "/* ——— (2) the mapping table ———",
        "   constructor + this.state        → two useState hooks",
        "   componentDidMount               → effect body (runs on mount)",
        "   componentDidUpdate(prev.symbol) → the SAME effect re-running on [symbol];",
        "                                     the manual clear/restart BECOMES the cleanup",
        "   componentWillUnmount            → the cleanup return",
        "   this.setState({a, b}) merge     → setA + setB (or one object + spread)",
        "   this.props reads                → plain parameters in the closure",
        "   (no HOC here — but withPolling(X) would become a usePolling() hook)",
        "",
        "   ——— (3) structurally removed risk ———",
        "   The class restarted its interval by hand inside cDU; any code path",
        "   that forgets clearInterval leaks a live timer that keeps fetching",
        "   (and setState-on-unmounted in older React). The effect contract makes",
        "   teardown structural — cleanup runs on every dep change and unmount,",
        "   by the framework, not by your memory. Plus: no this-binding, and the",
        "   cancelled guard kills the stale-answer race the class never had. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "componentDidMount translates to…",
        options: ["useEffect(fn, []) — a mount-time synchronization", "useState(fn)", "useMemo(fn, [])", "useLayoutEffect with no deps"],
        answer: 0,
        explain: "Empty deps = run once after the first commit. The effect framing is better than the lifecycle one: it's 'sync with the external system at mount', with cleanup included.",
      },
      {
        q: "The semantic difference between class setState and useState setters is…",
        options: ["Class setState merged partial objects; useState replaces the value", "Class setState was synchronous", "useState merges automatically", "There is no difference"],
        answer: 0,
        explain: "this.setState({a: 1}) kept b; setX(next) discards the old shape. Use spread ({...prev, ...patch}) when you need merge semantics, or split into separate states.",
      },
      {
        q: "getDerivedStateFromProps has no direct hook equivalent because…",
        options: ["Its job — deriving values from props — belongs in render, not in an effect or state", "It was never widely used", "Hooks can't read props", "It maps to useMemo only"],
        answer: 0,
        explain: "State-synced-from-props is the classic derived-state smell. Compute during render (const x = f(props)) and the sync problem never exists — the P05-L2 no-effect escape.",
      },
      {
        q: "HOCs lost to hooks primarily because…",
        options: ["Wrapper hell, silent prop injection, and type degradation — hooks reuse logic flatly and explicitly", "HOCs are slower at runtime", "Hooks can do more things", "The React team deprecated HOCs"],
        answer: 0,
        explain: "Same reuse goal, zero wrapping: useX() pulls logic into your component with named, typed values. HOCs aren't deprecated — they're just no longer the default costume.",
      },
      {
        q: "The professional rule for modernizing legacy components is…",
        options: ["Modernize at change boundaries with frozen behavior — separate fix commits from translation commits", "Rewrite old files before touching them", "Convert everything in one big PR", "Leave all legacy code untouched forever"],
        answer: 0,
        explain: "Mixed diffs destroy attribution and ship entangled regressions. Fix in place first; translate when the file earns attention, with tests as the witness.",
      },
      {
        q: "Strict Mode helps with legacy code because…",
        options: ["Its double-invocation exposes class-era 'runs exactly once' assumptions in development", "It converts classes to functions", "It removes deprecated APIs", "It speeds up legacy components"],
        answer: 0,
        explain: "Effects (and updaters) running twice surface impure mounts and one-shot assumptions before the concurrent production build does — the impurity detector working on old code too.",
      },
    ],
    flashcards: [
      { front: "Era detection tells", back: "ReactDOM.render/class → pre-concurrent; withX/compose → HOC era; children-as-function → render props; string refs → ancient; createRoot + hooks → current. Date before trusting." },
      { front: "cDM → effect", back: "componentDidMount is useEffect(fn, []): mount-time synchronization, with componentWillUnmount folded into the cleanup return." },
      { front: "cDU(prev) → deps", back: "componentDidUpdate's prev-prop comparisons become the dependency array: useEffect(fn, [symbol]) runs on mount AND on symbol change." },
      { front: "setState merge vs replace", back: "Class setState merged partial objects into state; useState setters replace. Spread ({...prev, ...patch}) or separate states when you need merge semantics." },
      { front: "HOC → hook", back: "withX(Wrapped) becomes const x = useX() inside Wrapped. Same logic, no wrapper hell, no silent props, honest types." },
      { front: "Render props → hooks", back: "Children-as-function inverted control but nested into pyramids; hooks deliver the same inversion flatly. Read render props; write hooks." },
      { front: "Change-boundary modernization", back: "Fix the bug in the old shape (tiny diff), then translate in a separate behavior-frozen commit with tests as witness. Never mix the two." },
      { front: "No-equivalent lifecycles", back: "getDerivedStateFromProps → derive during render. shouldComponentUpdate → React.memo. Some lifecycles map to NOTHING because the pattern itself was the bug." },
    ],
    recap: [
      "Date the wing before trusting the tutorial: five tells, five seconds.",
      "The translation table is mechanical — and lossless when behavior stays frozen.",
      "Some lifecycles have no hook equivalent because render-derivation replaces them.",
      "Hooks won by reusing logic flatly: no wrappers, no pyramids, honest types.",
      "Modernize at change boundaries; let tests arbitrate; the museum stays open.",
    ],
    references: [
      { label: "react.dev — Legacy React APIs", url: "https://react.dev/reference/react/legacy" },
      { label: "react.dev — Component (class)", url: "https://react.dev/reference/react/Component" },
      { label: "react-codemod (jscodeshift transforms)", url: "https://github.com/reactjs/react-codemod" },
      { label: "react.dev — createRoot migration", url: "https://react.dev/blog/2022/03/08/react-18-upgrade-guide.html" },
      { label: "react.dev — You Might Not Need an Effect", url: "https://react.dev/learn/you-might-not-need-an-effect" },
    ],
    nextBridge:
      "You can now read five years of React and write the current one. The final Phase 6 lesson packages that skill: the Phase 1 token system becomes a real, accessible design system, and the app around it gets an architecture — feature folders and import rules that survive a team.",
  },
  {
    id: "p06-l6",
    phaseId: "p06",
    title: "Design-System Components & Feature Folders",
    level: "React Developer",
    minutes: 35,
    summary:
      "The Phase 1 token system pays its dividend here: tokens become a small, accessible design system — Button, Field, Modal — with typed variant APIs, focus management, and keyboard behavior baked in. Then the lesson scales outward: feature folders over layer folders, the import-direction rules that keep ten developers from making spaghetti, and the shared-ui boundary that decides what gets reused and what stays local.",
    prerequisites: ["p06-l1 — component APIs & contracts", "p01-l5 — design tokens & custom properties", "p01-l1 — landmarks & keyboard accessibility"],
    objectives: [
      "Structure a design system in layers: tokens → primitives → patterns → features, each consuming only the layer below.",
      "Build Button with a discriminated variant API, loading (aria-busy), and disabled semantics that don't lie.",
      "Build Field with useId-wired labels, aria-describedby hints/errors, and aria-invalid that flips only after interaction.",
      "Implement a safe minimal Modal: role=dialog, focus trap, Escape, focus return, and scroll lock.",
      "Organize code into feature folders with enforced import directions (features ↛ features, shared ↛ features).",
      "Run the design-system acceptance checklist: keyboard, screen-reader semantics, token-only theming, typed misuse rejection.",
    ],
    simple:
      "A design system is a language your UI speaks, and like any language it has three levels: the alphabet (tokens — colors, radii, motion), the words (primitives — Button, Input, Badge), and the sentences (features — the checkout form, the task board). The miracle of doing it in this order is that a theme change is one remapping of the alphabet and every sentence updates; a new feature borrows existing words instead of inventing slang. The folder structure is the grammar: each feature is a paragraph that may quote the shared dictionary but never reaches into another feature's paragraphs. Break that rule and the codebase becomes one run-on sentence nobody can edit safely.",
    why:
      "Every team above three people converges on this architecture or pays the alternative tax: fourteen subtly different buttons, modals that trap nobody's focus, and the 'shared' folder that slowly becomes a dumping ground with import cycles. The payoff compounds through the whole course — Phase 8's Next.js app, Phase 19's typed client, and both capstones are just more features speaking the same vocabulary — and it's directly employable: 'built an accessible, token-driven component library with enforced boundaries' is a sentence that gets interviews. Accessibility isn't a feature of the system; it IS the quality bar the system exists to make automatic.",
    mentalModel: {
      title: "The dictionary and the paragraphs",
      body: "shared/ui is the dictionary: Button, Field, Modal — words with precise definitions (typed props) and correct pronunciation (keyboard + screen-reader behavior). Tokens are the letters those words are spelled from. A feature folder is a paragraph: the task feature, the billing feature — self-contained prose that may use any word from the dictionary but never borrows words from another paragraph, because paragraphs get rewritten, moved, and deleted independently. Import rules are the grammar enforcing this: a paragraph importing another paragraph's private words is how codebases turn into one unreadable run-on sentence. When someone says 'can I just grab TaskCard from the task feature into billing?' — the answer is 'promote it to the dictionary first, with a real contract', which forces the reuse to be deliberate.",
    },
    sections: [
      {
        heading: "The four layers — tokens to features, one direction",
        body: [
          "Layer ONE, tokens: the Phase 1 custom properties — --brand, --surface, --ink, --r, --shadow-2 — declared once, themed by remapping, consumed by everything above. Layer TWO, primitives: small components with typed variant APIs and accessibility baked in (this lesson's Button, Field, Modal, Badge). Layer THREE, patterns: compositions primitives don't know about — a ConfirmDialog (Modal + Buttons), a SearchField (Field + icon + kbd hint). Layer FOUR, features: domain screens that assemble patterns and own domain state.",
          "The law of the stack: each layer consumes only layers BELOW it, never above, never sideways at the same level. A Button may use tokens; it may never import a feature type (the moment Button knows about Task, Button can't ship without Task). A pattern may compose primitives; a feature may use everything below it. Violations are detectable — ESLint import rules or a simple folder-lint in CI (Phase 37) — and every violation you catch early is a circular-dependency build failure you never debug at 2 a.m.",
        ],
        code: [
          {
            file: "layers.txt — who may import whom",
            lang: "text",
            code: [
              "src/",
              "  tokens/          tokens.css            ← layer 1 (Phase 1's system)",
              "  ui/              Button.tsx            ← layer 2: primitives",
              "                   Field.tsx               (tokens + react only)",
              "                   Modal.tsx",
              "  patterns/        ConfirmDialog.tsx     ← layer 3: compositions",
              "                   SearchField.tsx         (ui/ + tokens)",
              "  features/",
              "    task/          TaskBoard.tsx         ← layer 4: domain",
              "                   useTaskMutations.ts     (anything below, never ../billing/)",
              "    billing/       InvoiceList.tsx",
              "",
              "ARROWS POINT DOWN ONLY:",
              "  features → patterns → ui → tokens",
              "  ✗ ui → features        (primitive learns domain — unforgivable)",
              "  ✗ features → features  (promote to patterns/ first)",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Button — variants as a typed contract, states as semantics",
        body: [
          "The anatomy of a primitive: a discriminated prop for look (variant: 'primary' | 'ghost' | 'danger' — exhaustive, so a typo'd variant is a compile error), a coarse prop for size ('sm' | 'md'), and honest props for state. Loading is aria-busy + a spinner that reserves space (no layout jump) with the label preserved for screen readers; disabled is real disabled PLUS the knowledge that disabled buttons don't announce WHY — so dangerous actions get ConfirmDialog (layer 3) instead of silent greyness. Every color is a token; the component has zero hex codes, which is what makes dark mode free in Phase 8's real apps.",
          "One API decision worth the argument: primitive buttons render <button>, full stop. The 'asChild/as any element' escape hatch belongs in the pattern layer when a genuine need appears (a Button-styled Link) — premature polymorphism is how primitives grow config surfaces nobody can learn. The contract stays small enough to memorize: variant, size, loading, disabled, onClick. That's the whole word; that's why the dictionary works.",
        ],
        code: [
          {
            file: "ui/Button.tsx — small word, precise definition",
            lang: "tsx",
            code: [
              "import type { ReactNode } from \"react\";",
              "",
              "type Variant = \"primary\" | \"ghost\" | \"danger\";",
              "type Size = \"sm\" | \"md\";",
              "",
              "const VARIANT_CLASS: Record<Variant, string> = {",
              "  primary: \"btn btn-primary\",   // classes defined once, on tokens (Phase 1)",
              "  ghost: \"btn btn-ghost\",",
              "  danger: \"btn btn-danger\",",
              "};",
              "",
              "export function Button({",
              "  variant = \"primary\",",
              "  size = \"md\",",
              "  loading = false,",
              "  children,",
              "  ...rest",
              "}: {",
              "  variant?: Variant;",
              "  size?: Size;",
              "  loading?: boolean;",
              "  children: ReactNode;",
              "} & React.ButtonHTMLAttributes<HTMLButtonElement>) {",
              "  return (",
              "    <button",
              "      className={`${VARIANT_CLASS[variant]} ${size === \"sm\" ? \"btn-sm\" : \"\"}`}",
              "      aria-busy={loading || undefined}",
              "      disabled={rest.disabled || loading}",
              "      {...rest}",
              "    >",
              "      {loading && <span className=\"spinner\" aria-hidden=\"true\" />}",
              "      {children}   {/* label stays in the tree for screen readers */}",
              "    </button>",
              "  );",
              "}",
              "// <Button variant=\"typo\" /> ← TS2322. The contract defends itself.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Field — one id away from accessible, useId away from collisions",
        body: [
          "A text input is three elements pretending to be one: label, input, and message — wired together by ids. useId generates a collision-proof id (SSR-safe, unique per instance), so two Fields on a page never cross-wire their labels; the label gets htmlFor, the input gets aria-describedby pointing at hint and/or error, and aria-invalid flips true only when an error is actually shown. The message element carries id + role semantics: hints are passive text, errors should be announced — role=\"alert\" when it appears, or a live region from the form (Phase 20 formalizes this with React Hook Form + Zod).",
          "Two details that separate systems from collections: the error appears because the FORM decided (touched + invalid), not because the input felt like it — the Field is a dumb pipe for a smart decision. And the hint/error ids are composed: aria-describedby={[hintId, errorId].filter(Boolean).join(' ')} reads BOTH to a screen reader, in order. This exact shape reappears in Phase 20 as the target every form library error must map into — build it right once and the whole course's forms inherit it.",
        ],
        code: [
          {
            file: "ui/Field.tsx — label, input, message: one contract",
            lang: "tsx",
            code: [
              "import { useId } from \"react\";",
              "",
              "export function Field({",
              "  label, hint, error, ...inputProps",
              "}: {",
              "  label: string;",
              "  hint?: string;",
              "  error?: string;",
              "} & React.InputHTMLAttributes<HTMLInputElement>) {",
              "  const base = useId();",
              "  const inputId = `${base}-input`;",
              "  const hintId = hint ? `${base}-hint` : undefined;",
              "  const errorId = error ? `${base}-error` : undefined;",
              "",
              "  return (",
              "    <div className=\"field\">",
              "      <label htmlFor={inputId}>{label}</label>",
              "      <input",
              "        id={inputId}",
              "        aria-invalid={error ? true : undefined}",
              "        aria-describedby={[hintId, errorId].filter(Boolean).join(\" \") || undefined}",
              "        {...inputProps}",
              "      />",
              "      {hint && !error && <p id={hintId} className=\"field-hint\">{hint}</p>}",
              "      {error && <p id={errorId} role=\"alert\" className=\"field-error\">⚠ {error}</p>}",
              "    </div>",
              "  );",
              "}",
              "// <Field label=\"Email\" type=\"email\" hint=\"We never share it\"",
              "//        error={touched && !valid ? \"Enter a valid email\" : undefined} />",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Modal — the five obligations nobody skips",
        body: [
          "A modal is a promise to the user: while it's open, NOTHING else is interactive, and when it closes, you're back where you were. Five obligations keep that promise. ONE: role=\"dialog\" + aria-modal=\"true\" + an accessible name (aria-labelledby pointing at the title) so assistive tech announces the takeover. TWO: initial focus — focus moves INTO the dialog on open (the close button or first field), never left stranded on the page behind. THREE: focus trap — Tab cycles inside; a minimal implementation handles first/last wraparound. FOUR: Escape closes. FIVE: focus RETURN — the element that opened it gets focus back, which is the detail every home-grown modal forgets and every keyboard user notices.",
          "Body scroll lock (overflow hidden while open, restored on close — in an effect cleanup, because the modal might unmount mid-open) is obligation five-point-five. And the architectural note that matters for the course: this minimal modal is the honest baseline — production apps reach for battle-tested primitives (Radix Dialog, the pattern behind shadcn) precisely because trap/return/inert edge cases multiply; you learn the obligations by building the minimal version once, then you stand on the giants knowingly. The trap below is intentionally simple; the obligations list is what you carry.",
        ],
        code: [
          {
            file: "ui/Modal.tsx — five obligations, one component",
            lang: "tsx",
            code: [
              "import { useEffect, useRef, useId } from \"react\";",
              "import type { ReactNode } from \"react\";",
              "",
              "export function Modal({ open, onClose, title, children }: {",
              "  open: boolean;",
              "  onClose: () => void;",
              "  title: string;",
              "  children: ReactNode;",
              "}) {",
              "  const titleId = useId();",
              "  const dialogRef = useRef<HTMLDivElement>(null);",
              "  const openerRef = useRef<HTMLElement | null>(null);",
              "",
              "  useEffect(() => {",
              "    if (!open) return;",
              "    openerRef.current = document.activeElement as HTMLElement; // remember",
              "    const el = dialogRef.current;",
              "    el?.querySelector<HTMLElement>(\"[data-autofocus]\")?.focus();  // 2: in",
              "    document.body.style.overflow = \"hidden\";                      // 5.5: lock",
              "",
              "    const onKey = (e: KeyboardEvent) => {",
              "      if (e.key === \"Escape\") onClose();                          // 4: esc",
              "      if (e.key === \"Tab\" && el) {                                // 3: trap",
              "        const nodes = el.querySelectorAll<HTMLElement>(",
              "          'button, [href], input, select, textarea, [tabindex]:not([tabindex=\"-1\"])'",
              "        );",
              "        if (nodes.length === 0) return;",
              "        const first = nodes[0];",
              "        const last = nodes[nodes.length - 1];",
              "        if (e.shiftKey && document.activeElement === first) {",
              "          e.preventDefault(); last.focus();",
              "        } else if (!e.shiftKey && document.activeElement === last) {",
              "          e.preventDefault(); first.focus();",
              "        }",
              "      }",
              "    };",
              "    document.addEventListener(\"keydown\", onKey);",
              "    return () => {",
              "      document.removeEventListener(\"keydown\", onKey);",
              "      document.body.style.overflow = \"\";          // restore — cleanup",
              "      openerRef.current?.focus();                  // 5: return",
              "    };",
              "  }, [open, onClose]);",
              "",
              "  if (!open) return null;",
              "  return (",
              "    <div className=\"modal-backdrop\" onMouseDown={(e) => e.target === e.currentTarget && onClose()}>",
              "      <div ref={dialogRef} role=\"dialog\" aria-modal=\"true\" aria-labelledby={titleId}>",
              "        <h2 id={titleId}>{title}</h2>             {/* 1: named */}",
              "        {children}",
              "        <button data-autofocus onClick={onClose}>Close</button>",
              "      </div>",
              "    </div>",
              "  );",
              "}",
            ].join("\n"),
            caption: "Production note: Radix Dialog (the shadcn substrate) industrializes these same five obligations — inert background, body lock, iOS scroll quirks included. Learn them here; stand on the giants knowingly.",
          },
        ],
        demo: "design-system",
      },
      {
        heading: "Feature folders — the grammar that survives a team",
        body: [
          "Layer folders (components/ hooks/ utils/ services/) answer 'what kind of thing is this?' — fine for a weekend project, brutal for a team, because every feature's files are scattered across six folders and deleting a feature means an archeological dig. Feature folders answer 'what does this do?': features/task/ holds TaskBoard, its hooks, its api client slice, its types, its tests — the whole paragraph in one place, movable and deletable as a unit. The migration to the course's monorepo (Phase 35: apps/web, apps/api, packages/) is the same idea one level up: boundaries are the architecture.",
          "Two rules make it hold. ONE, import direction: features may import shared/ui, patterns, and their own internals — never another feature's internals; if billing genuinely needs a task widget, the widget gets PROMOTED to patterns/ with a real typed contract, which forces the reuse conversation to happen in review, not in a sneaky relative import. TWO, the barrel caveat: index.ts re-exports feel tidy but create import cycles and defeat tree-shaking in some bundlers; prefer deep imports (import { TaskBoard } from './TaskBoard') inside a feature and save barrels for genuinely public boundaries. ESLint's import plugin (or a 20-line CI lint in Phase 37) enforces both, so the rules are laws, not vibes.",
        ],
        code: [
          {
            file: "boundaries.ts — the rules, as the linter sees them",
            lang: "ts",
            code: [
              "// ✓ legal — arrows point down:",
              "import { Button } from \"../../ui/Button\";            // feature → ui",
              "import { ConfirmDialog } from \"../../patterns/ConfirmDialog\";",
              "import { useTaskMutations } from \"./useTaskMutations\"; // feature → own internals",
              "",
              "// ✗ illegal — sideways reach:",
              "import { InvoiceRow } from \"../billing/InvoiceRow\";   // feature ↛ feature",
              "//   → the fix is promotion: move InvoiceRow (with a typed contract)",
              "//     into patterns/ and import from there. The diff IS the design review.",
              "",
              "// ✗ illegal — upward reach:",
              "// ui/Button.tsx importing features/task/types — the primitive now",
              "//   can't compile without the feature. Unforgivable, and lintable.",
              "",
              "// eslint (import/no-restricted-paths) makes both laws mechanical:",
              "//   zones: [",
              "//     { target: \"./src/ui\", from: \"./src/features\" },",
              "//     { target: \"./src/features\", from: \"./src/features\", except: [] },",
              "//   ]",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Acceptance — the checklist a system must pass",
        body: [
          "Four audits, run on every primitive before it earns dictionary status. KEYBOARD: every control reachable by Tab in sensible order; Space/Enter activate buttons; Escape closes overlays; arrow keys where the pattern demands (Phase 4's listbox work); visible focus ring in BOTH themes. SEMANTICS: each control announces its role + name + state in the DevTools accessibility pane — Button says button, the dialog says dialog with its title, an invalid field says invalid with its error read. THEMING: grep the component for a hex code; zero is the pass mark — every value routes through a token, so a one-line remap re-skins it.",
          "CONTRACT: misuse must not compile — a bad variant, a missing label, a Modal without onClose are type errors, not runtime surprises. That last audit is why the course's systems feel 'strict': the strictness is front-loaded into the type so it can never be back-loaded into a user's browser. Run these four on the components in the lab; from Phase 8 onward, every capstone component you ship is graded by the same list — and Phase 31 automates the first two with axe and Playwright.",
        ],
      },
    ],
    mistake: {
      title: "The dictionary that learned slang",
      wrong: [
        "// shared/ui/StatusBadge.tsx — layer 2, supposedly domain-free:",
        "import type { Task } from \"../../features/task/types\"; // ✗ upward import",
        "",
        "export function StatusBadge({ task }: { task: Task }) {",
        "  return <span className={`badge badge-${task.status}`}>{task.status}</span>;",
        "}",
        "",
        "// consequences, arriving in order:",
        "// 1) billing imports StatusBadge → now billing transitively imports task",
        "// 2) task/types changes → shared build breaks for everyone",
        "// 3) deleting the task feature? grep finds it living in shared/ui",
        "// 4) a second domain wants a status badge → fork, drift, two badges",
      ].join("\n"),
      right: [
        "// shared/ui/Badge.tsx — the primitive speaks only its own vocabulary:",
        "type Tone = \"neutral\" | \"positive\" | \"warn\" | \"danger\";",
        "export function Badge({ tone = \"neutral\", children }: {",
        "  tone?: Tone;",
        "  children: React.ReactNode;",
        "}) {",
        "  return <span className={`badge badge-${tone}`}>{children}</span>;",
        "}",
        "",
        "// features/task/TaskStatusBadge.tsx — domain knowledge stays in the domain:",
        "const TONE: Record<TaskStatus, Tone> = {",
        "  open: \"neutral\", blocked: \"warn\", done: \"positive\", archived: \"neutral\",",
        "};",
        "export const TaskStatusBadge = ({ status }: { status: TaskStatus }) => (",
        "  <Badge tone={TONE[status]}>{status}</Badge>",
        ");",
        "// generic word in the dictionary; task-specific sentence in the paragraph.",
      ].join("\n"),
      explain:
        "The primitive importing a feature type inverts the entire stack: the shared layer now depends on a domain, so every consumer of 'shared' drags that domain in, and the feature can never be deleted or moved. The fix is always the same shape — split the generic word (Badge, any tone) from the domain sentence (TaskStatusBadge, task semantics). When in doubt, ask: could this component exist in a completely different app? If not, it's a sentence, not a word.",
    },
    tryIt: [
      "Open this platform's own tokens page, then grep src/index.css for the exact variables Button/Field/Modal would consume (--brand, --surface, --r, --line-2…). Sketch the four-layer diagram for THIS site — which components are primitives, which are patterns?",
      "Build Button exactly as specified, then try to misuse it: a fake variant, no children. Read both type errors aloud. That refusal is the contract working.",
      "Render two Fields side by side and inspect their ids in DevTools. Confirm useId made them distinct, then delete one Field and confirm the survivor's wiring didn't shift.",
      "Open the Modal from the lab and run the five-obligation audit with ONLY the keyboard: Tab in, Tab-cycle (does it wrap?), Escape, and — the one everyone misses — confirm focus lands back on the opener.",
      "Take any project of yours and re-folder it mentally into features/. Which current file would you promote to shared? Which shared file is secretly domain-flavored? Write the two lists.",
    ],
    challenge: {
      prompt:
        "Ship a 'settings' feature on top of the mini design system. Deliverables: (1) ui/: Button (variants primary/ghost/danger, loading, disabled), Field (label/hint/error via useId), Badge (tones), Modal (five obligations) — all token-only, zero hex codes, misuse must not compile; (2) patterns/ConfirmDialog.tsx composing Modal + Buttons with a typed danger contract ({ confirmLabel required for variant 'danger' }); (3) features/settings/: a SettingsForm using Field×3 (display name, email, bio with maxlength hint) whose Save button shows loading during a faked async save and whose 'Delete account' opens ConfirmDialog; (4) the feature-folder tree as a text block, plus the two import laws written as they'd appear in an ESLint no-restricted-paths config; (5) the four-audit acceptance checklist, filled out for YOUR Modal.",
      hints: [
        "ConfirmDialog's typed danger contract: a discriminated union — { variant?: 'default'; confirmLabel?: string } | { variant: 'danger'; confirmLabel: string }. Deleting an account without a confirm label should be a compile error, not a judgment call.",
        "The fake save: await new Promise(r => setTimeout(r, 900)) inside the handler with a saving state — Button loading={saving}. Validation is touched-aware: only show errors after blur (a touched Set<string> state).",
        "For the Modal audit, test in order: Tab → focus enters (data-autofocus), Tab×N wraps at the end, Shift+Tab wraps at the start, Escape closes, focus returns to the Delete-account button, body scroll restored (try with a long page behind).",
      ],
      solution: [
        "/* ——— (4) the tree ——— */",
        "src/",
        "  tokens/tokens.css",
        "  ui/",
        "    Button.tsx      // variants primary | ghost | danger; loading; disabled",
        "    Field.tsx       // label + hint + error, useId wiring (as in lesson)",
        "    Badge.tsx       // tones neutral | positive | warn | danger",
        "    Modal.tsx       // five obligations (as in lesson)",
        "  patterns/",
        "    ConfirmDialog.tsx",
        "  features/",
        "    settings/",
        "      SettingsForm.tsx",
        "      types.ts        // SettingsDraft — domain words stay HERE",
        "",
        "/* ——— (2) patterns/ConfirmDialog.tsx ——— */",
        "import { Modal } from \"../ui/Modal\";",
        "import { Button } from \"../ui/Button\";",
        "",
        "// typed danger contract: danger REQUIRES an explicit confirm label",
        "type Props =",
        "  | { open: boolean; onClose: () => void; onConfirm: () => void;",
        "      title: string; body: string; variant?: \"default\"; confirmLabel?: string }",
        "  | { open: boolean; onClose: () => void; onConfirm: () => void;",
        "      title: string; body: string; variant: \"danger\"; confirmLabel: string };",
        "",
        "export function ConfirmDialog(p: Props) {",
        "  const danger = p.variant === \"danger\";",
        "  return (",
        "    <Modal open={p.open} onClose={p.onClose} title={p.title}>",
        "      <p>{p.body}</p>",
        "      <div style={{ display: \"flex\", gap: 8, marginTop: 16 }}>",
        "        <Button variant=\"ghost\" onClick={p.onClose}>Cancel</Button>",
        "        <Button variant={danger ? \"danger\" : \"primary\"} onClick={p.onConfirm}>",
        "          {p.confirmLabel ?? \"Confirm\"}",
        "        </Button>",
        "      </div>",
        "    </Modal>",
        "  );",
        "}",
        "// <ConfirmDialog variant=\"danger\" … /> without confirmLabel → TS2322.",
        "",
        "/* ——— (3) features/settings/SettingsForm.tsx (abridged shape) ——— */",
        "import { useState } from \"react\";",
        "import { Button, Field } from \"../../ui/Button\";  // deep imports, no barrels",
        "import { ConfirmDialog } from \"../../patterns/ConfirmDialog\";",
        "",
        "export function SettingsForm() {",
        "  const [draft, setDraft] = useState({ name: \"\", email: \"\", bio: \"\" });",
        "  const [touched, setTouched] = useState<Record<string, boolean>>({});",
        "  const [saving, setSaving] = useState(false);",
        "  const [confirmOpen, setConfirmOpen] = useState(false);",
        "",
        "  const errors = {",
        "    name: draft.name.trim().length < 2 ? \"At least 2 characters\" : undefined,",
        "    email: !/^\\S+@\\S+\\.\\S+$/.test(draft.email) ? \"Enter a valid email\" : undefined,",
        "  };",
        "",
        "  async function save() {",
        "    setTouched({ name: true, email: true });",
        "    if (errors.name || errors.email) return;   // client check = UX only (P01-L2 law)",
        "    setSaving(true);",
        "    await new Promise((r) => setTimeout(r, 900)); // fake network",
        "    setSaving(false);",
        "  }",
        "",
        "  return (",
        "    <form onSubmit={(e) => { e.preventDefault(); void save(); }} noValidate>",
        "      <Field label=\"Display name\" value={draft.name}",
        "        onChange={(e) => setDraft((d) => ({ ...d, name: e.target.value }))}",
        "        onBlur={() => setTouched((t) => ({ ...t, name: true }))}",
        "        error={touched.name ? errors.name : undefined} />",
        "      <Field label=\"Email\" type=\"email\" value={draft.email}",
        "        onChange={(e) => setDraft((d) => ({ ...d, email: e.target.value }))}",
        "        onBlur={() => setTouched((t) => ({ ...t, email: true }))}",
        "        error={touched.email ? errors.email : undefined} />",
        "      <Field label=\"Bio\" value={draft.bio} maxLength={160}",
        "        onChange={(e) => setDraft((d) => ({ ...d, bio: e.target.value }))}",
        "        hint={`${draft.bio.length}/160`} />",
        "      <Button type=\"submit\" loading={saving}>Save changes</Button>",
        "      <Button type=\"button\" variant=\"danger\" onClick={() => setConfirmOpen(true)}>",
        "        Delete account",
        "      </Button>",
        "      <ConfirmDialog",
        "        variant=\"danger\"                    // confirmLabel now REQUIRED:",
        "        confirmLabel=\"Delete everything\"",
        "        open={confirmOpen}",
        "        onClose={() => setConfirmOpen(false)}",
        "        onConfirm={() => setConfirmOpen(false)}",
        "        title=\"Delete account?\"",
        "        body=\"This cannot be undone. All projects and data are removed.\"",
        "      />",
        "    </form>",
        "  );",
        "}",
        "",
        "/* ——— (4) the import laws, as lint config ——— */",
        "// eslint import/no-restricted-paths:",
        "//   zones: [",
        "//     { target: \"./src/ui\",       from: \"./src/features\" },  // ui ↛ features",
        "//     { target: \"./src/patterns\", from: \"./src/features\" },  // patterns ↛ features",
        "//     { target: \"./src/features\", from: \"./src/features\",",
        "//       message: \"Promote shared widgets to patterns/ first\" },",
        "//   ]",
        "",
        "/* ——— (5) Modal acceptance audit, filled ——— */",
        "// [x] keyboard: Tab enters (Close focused), Tab wraps last→first,",
        "//     Shift+Tab wraps first→last, Escape closes",
        "// [x] focus return: opener (Delete account) focused after close",
        "// [x] semantics: a11y pane shows role=dialog, name from h2, aria-modal",
        "// [x] theming: grep Modal.tsx for '#[0-9a-f]' → zero hits, tokens only",
        "// [x] contract: <Modal /> without onClose/title → TS2322 at every site",
      ].join("\n"),
    },
    quiz: [
      {
        q: "The law of the design-system stack is…",
        options: ["Each layer consumes only layers below it — features → patterns → ui → tokens, never up, never sideways", "Any component may import any other", "Features may import other features' internals", "Primitives may import feature types"],
        answer: 0,
        explain: "A Button that knows Task can never ship without Task. Violations are lintable import-direction rules, enforced in CI — grammar, not vibes.",
      },
      {
        q: "Button's loading state should set aria-busy and…",
        options: ["Keep the label in the tree (spinner aria-hidden) and disable the button", "Replace the label with an icon only", "Remove the button from the DOM", "Do nothing — spinners are visual"],
        answer: 0,
        explain: "Screen readers announce busy state from aria-busy; a preserved label keeps the control identifiable. A spinner-only button announces nothing.",
      },
      {
        q: "useId in Field exists to…",
        options: ["Generate collision-proof, SSR-safe ids for label/describedby wiring", "Style the field uniquely", "Key list items", "Generate keys for memo"],
        answer: 0,
        explain: "Two Fields on a page must never cross-wire labels. useId is unique per instance and stable across server/client renders — exactly what id-wiring needs.",
      },
      {
        q: "The five Modal obligations are…",
        options: ["role/name, initial focus in, focus trap, Escape closes, focus return on close", "Animation, backdrop blur, size, position, z-index", "Open, close, toggle, destroy, recreate", "Mount, render, suspend, hydrate, unmount"],
        answer: 0,
        explain: "The promise: nothing else is interactive while open; you return to where you were. Every forgotten obligation is a keyboard-user paper cut.",
      },
      {
        q: "Billing genuinely needs a widget that lives in the task feature. You…",
        options: ["Promote it to patterns/ with a real typed contract — the diff becomes the design review", "Import it from ../task/", "Copy-paste it into billing", "Move billing into the task folder"],
        answer: 0,
        explain: "Sideways imports turn features into one run-on sentence. Promotion forces the reuse conversation, gives the widget a domain-free API, and keeps both features deletable.",
      },
      {
        q: "The four audits a primitive must pass are…",
        options: ["Keyboard, semantics (role+name+state), token-only theming, typed misuse rejection", "Bundle size, test coverage, storybook count, download stats", "Speed, beauty, novelty, cleverness", "Lint, format, build, deploy"],
        answer: 0,
        explain: "Accessibility is the entry fee, theming proves the token contract, and the type system front-loads misuse as compile errors. Phase 31 automates the first two.",
      },
    ],
    flashcards: [
      { front: "The four layers", back: "Tokens (alphabet) → primitives (words) → patterns (sentences) → features (paragraphs). Each consumes only layers below; the direction is the architecture." },
      { front: "Variant as discriminated prop", back: "variant: 'primary' | 'ghost' | 'danger' with a Record<Variant, className> map. Exhaustive, typo-proof at compile time, trivially extendable." },
      { front: "aria-busy + preserved label", back: "Loading sets aria-busy and shows an aria-hidden spinner; the label stays so screen readers keep identifying the control. Never spinner-only." },
      { front: "useId wiring", back: "Collision-proof ids for htmlFor / aria-describedby. Compose describedby as [hintId, errorId].filter(Boolean).join(' ') — hints AND errors, in order." },
      { front: "The five Modal obligations", back: "Named dialog (role + aria-modal + labelledby), focus in, focus trap, Escape closes, focus return. Plus body scroll lock in the cleanup." },
      { front: "Feature folders", back: "features/<name>/ holds components + hooks + api slice + types + tests. Movable/deletable as a unit; the opposite of scattered layer folders." },
      { front: "The two import laws", back: "Never up (ui ↛ features), never sideways (features ↛ features — promote to patterns/ instead). Enforced with import/no-restricted-paths in CI." },
      { front: "The barrel caveat", back: "index.ts re-exports create import cycles and can defeat tree-shaking. Deep imports inside features; barrels only at genuinely public boundaries." },
    ],
    recap: [
      "Tokens → primitives → patterns → features: one direction, linted.",
      "Primitives are small typed words: variant contracts, honest states, zero hex codes.",
      "Field = label + input + message wired by useId; the form decides, the field displays.",
      "Modals make five promises; production apps stand on Radix knowingly.",
      "Feature folders + import laws are the grammar that lets ten people write one book.",
    ],
    references: [
      { label: "WAI-ARIA APG — Dialog (Modal) pattern", url: "https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/" },
      { label: "react.dev — useId", url: "https://react.dev/reference/react/useId" },
      { label: "Radix UI — Dialog", url: "https://www.radix-ui.com/primitives/docs/components/dialog" },
      { label: "web.dev — Learn Accessibility", url: "https://web.dev/learn/accessibility" },
      { label: "Bulletproof React — feature-based structure", url: "https://github.com/alan2207/bulletproof-react" },
    ],
    nextBridge:
      "The React Developer stage is complete: you can read React's past, write its present, architect its components, and ship them accessibly. The course now turns outward — Phase 7 opens the network itself: the request anatomy your fetches and optimistic round trips have been crossing all along. DNS, headers, status codes, CORS — the layer every later phase assumes you can see through.",
  },
];

export const LESSON_CONTENT_P6C: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P6C.map((l) => [l.id, l]),
);
