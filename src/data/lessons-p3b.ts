import type { LessonContent } from "./types";

/**
 * Pass 009 bounded content batch: TypeScript core close (L3, L5, L6).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P3B: LessonContent[] = [
  {
    id: "p03-l3",
    phaseId: "p03",
    title: "Generics & Utility Types",
    level: "Foundation",
    minutes: 40,
    summary:
      "A generic is a labeled hole in a type that each use fills in — the difference between 'a function for numbers' and 'a function for one type, whichever you bring'. This lesson installs honest generics (identity, first, pick, typed API wrappers), the utility-type toolbox that transforms shapes instead of copying them (Partial/Pick/Omit/Record/ReturnType/NonNullable), constraints that let T be known and unknown at once — and the judgment to NOT write a generic when one concrete type is the truth.",
    prerequisites: ["p03-l2 — unions, narrowing, and the Result<T> pattern"],
    objectives: [
      "Explain generics as type-level parameters and predict the inferred type at each call site.",
      "Write the four workhorse generics: typed identity, first, pick, and a request<T> API wrapper.",
      "Use constraints (extends) to guarantee structure while staying generic over the rest.",
      "Apply utility types as shape transformers: Partial, Required, Pick, Omit, Record, ReturnType, NonNullable.",
      "Compose utilities into domain types (Patch<T>, PublicUser) instead of re-declaring fields.",
      "Refuse generics that relate nothing: recognize noise and concretize it.",
    ],
    simple:
      "Imagine a moving company with one truck. Without generics, you either buy a separate truck for every kind of cargo (numberTruck, stringTruck, userTruck…) or take anything and hope (anyTruck — boxes fall out). A generic is a truck with a label on the door: 'whatever you load in TOWN A, that's exactly what unloads in TOWN B'. Write function identity<T>(x: T): T once, and every call pours a specific type into the hole — identity(5) is (x: number) => number, identity('hi') is the string version. The compiler does the pouring; you just design the shape of the hole.",
    why:
      "Generics are the connective tissue of every serious TypeScript API you will consume this course: TanStack Query's useQuery<TData> infers your data shape from the fetcher; NestJS pipes parse DTOs into typed objects; Prisma's client is one giant generic machine that mirrors your schema; the typed API client of Phase 19 is request<T> plus Result<T>. Developers who skip this lesson read those signatures like weather maps — and when they finally need their own reusable code, they reach for any and the type safety evaporates exactly where reuse begins. Utility types earn their slot the same way: they are the difference between copying a user type five times (and drifting) and deriving five views from one source of truth.",
    mentalModel: {
      title: "Molds, not sculptures",
      body: "A non-generic function is a finished sculpture — beautiful, specific, done. A generic function is the MOLD: it has a real, inspectable shape (the relationships between its holes), but the material — the concrete type — is poured in at every call site. The mold's quality is judged by its relationships: first<T>(arr: T[]): T | undefined relates the array's element to the return; pick<T, K extends keyof T> locks the keys to the object it came from. If a mold has a hole that relates to nothing else (a lone <T> used once), it isn't a mold — it's a sculpture pretending, and you should just carve the concrete type.",
    },
    sections: [
      {
        heading: "The typed hole — why the alternatives lose",
        body: [
          "The problem: write a function that returns its argument unchanged, for any type. Three attempts, two failures. Overloads (number version, string version, user version…) explode combinatorially and still miss tomorrow's type. Returning any accepts everything but AMNESIA follows — the caller's type information is destroyed at the boundary, and every downstream line is unprotected. The generic version threads the needle: <T>(x: T): T says 'one type, whichever the caller brings, and it survives the trip'.",
          "Notice what you did NOT do: you never wrote T = number anywhere. The compiler infers the type argument from the call — identity(41 + 1) is instantiated as the number version automatically. You can supply it explicitly (identity<string>('42')) but explicit type arguments are for the rare ambiguous case; inference at the call site is the default rhythm of generic code.",
        ],
        code: [
          {
            file: "identity.ts — one function, infinite instantiations",
            lang: "ts",
            code: [
              "// ✗ the amnesia version: works, then forgets everything",
              "function identityAny(x: any): any { return x; }",
              "const n = identityAny(42);        // n: any — protection gone",
              "n.toUpperCase();                  // compiles. Explodes at runtime.",
              "",
              "// ✓ the mold: the type travels THROUGH the function",
              "function identity<T>(x: T): T { return x; }",
              "const a = identity(42);           // a: number — inferred at the call",
              "const b = identity(\"42\");         // b: string",
              "// a.toUpperCase();               // ✗ compile error — the type survived",
              "",
              "// multiple holes can relate to each other:",
              "function first<T>(arr: T[]): T | undefined {",
              "  return arr[0];",
              "}",
              "const f = first([1, 2, 3]);       // number | undefined — HONEST,",
              "// because the hole remembers empty arrays exist",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Constraints — T is known AND unknown at once",
        body: [
          "Sometimes the hole needs a minimum shape: a logging function that must read .id, whatever T otherwise is. extends is the constraint: <T extends { id: number }> guarantees the known part (id exists, is a number) while the rest of T stays free. Inside the function you may use ONLY what the constraint promises — writing value.name fails, because some future T might lack it. That tension — knowing the guaranteed slice, respecting the unknown rest — is the entire skill.",
          "The most important constraint in the language is K extends keyof T: it locks a type parameter to the ACTUAL keys of another type parameter. This is how pick works: the keys you ask for must exist on the object you brought, and the result type is built from exactly those keys. Misspell a key and it's a compile error at the call site — the typo class of bugs, extinct.",
        ],
        code: [
          {
            file: "constraints.ts",
            lang: "ts",
            code: [
              "// minimum shape guaranteed, rest unknown:",
              "function logId<T extends { id: number }>(value: T): void {",
              "  console.log(value.id);    // ✓ the constraint promises id",
              "  // console.log(value.name); // ✗ some T has no name — correctly refused",
              "}",
              "logId({ id: 7, name: \"Ada\" });  // T inferred as the full object",
              "",
              "// K locked to the real keys of T:",
              "function pick<T, K extends keyof T>(obj: T, keys: K[]): Pick<T, K> {",
              "  const result = {} as Pick<T, K>;",
              "  for (const key of keys) result[key] = obj[key];",
              "  return result;",
              "}",
              "",
              "const user = { id: 1, name: \"Ada\", email: \"ada@x.dev\", secret: \"s3\" };",
              "const card = pick(user, [\"id\", \"name\"]);   // { id: number; name: string }",
              "// pick(user, [\"nmae\"]);                    // ✗ compile error: typo dies here,",
              "//                                               not at runtime in production",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The utility toolbox — shapes as transformers",
        body: [
          "Utility types take a type and return a transformed type — functions at the type level. The daily set, each in one sentence: Partial<T> makes every field optional (patches and drafts); Required<T> makes every field mandatory (the validated end-state); Pick<T, K> keeps only the listed keys; Omit<T, K> removes the listed keys; Record<K, V> builds a map from key type K to value type V; ReturnType<F> extracts what a function returns; NonNullable<T> strips null and undefined; Readonly<T> locks every field at the type level.",
          "The discipline these enable: ONE source-of-truth type, many derived views. The server's User type is the trunk; PublicUser = Omit<User, 'password'> is the branch that leaves the API; UserPatch = Partial<Pick<User, 'name' | 'email'>> is the update shape. When a field changes, you edit ONE type and the compiler walks every derived view for you. Copy-pasted interfaces drift; derived types cannot.",
        ],
        code: [
          {
            file: "utilities.ts — derive, don't duplicate",
            lang: "ts",
            code: [
              "interface User {",
              "  id: number;",
              "  name: string;",
              "  email: string;",
              "  password: string;",
              "  role: \"admin\" | \"member\";",
              "}",
              "",
              "// views derived from the ONE source of truth:",
              "type PublicUser = Omit<User, \"password\">;          // leaves the server",
              "type UserPatch = Partial<Pick<User, \"name\" | \"email\">>; // an update",
              "type RoleCounts = Record<User[\"role\"], number>;   // { admin: number; member: number }",
              "",
              "function applyPatch(user: User, patch: UserPatch): User {",
              "  return { ...user, ...patch };   // spread, typed end to end",
              "}",
              "",
              "// ReturnType pulls truth out of existing functions:",
              "function makeConfig() {",
              "  return { retries: 3, timeoutMs: 2000 } as const;",
              "}",
              "type Config = ReturnType<typeof makeConfig>;",
              "// { readonly retries: 3; readonly timeoutMs: 2000 }",
            ].join("\n"),
            caption: "Omit<User, 'password'> reappears in Phase 12 as NestJS serialization and in Phase 17 as the auth-response shape — same idea, higher stakes.",
          },
        ],
      },
      {
        heading: "When a generic is over-engineering",
        body: [
          "A generic earns its keep when it relates positions: the argument's type constrains the return's type, the keys relate to the object, the patch relates to the entity. Three honest tests before you write <T>. One: is T used in TWO OR MORE related positions, or does inference flow from one position to another? A lone function shout<T>(message: string): void uses T nowhere that matters — delete it. Two: will this function EVER be called with more than one type family? If your 'generic' wrapper has exactly one caller with one type, write the concrete function — you can generalize later in thirty seconds; removing a false abstraction takes a refactor.",
          "Three: does the generic make the CALL SITE harder to read? When inference can't flow (too many holes, ambiguous arguments), callers must supply type arguments by hand — at that point the abstraction is taxing every user. The mature move is knowing the direction: start concrete, generalize on the SECOND real need, never on the imagined third. Speculative generics are the TypeScript flavor of speculative abstraction the whole course warns against.",
        ],
        code: [
          {
            file: "honesty.ts — concrete beats theatrical",
            lang: "ts",
            code: [
              "// ✗ theatre: T relates to nothing; unknown says it plainer",
              "function logAnything<T>(value: T): void { console.log(value); }",
              "// ✓ honest: the parameter's type needs no holes",
              "function logAnything(value: unknown): void { console.log(value); }",
              "",
              "// ✗ premature: one caller, one type, forever",
              "function wrapUserId<T>(id: T): { userId: T } { return { userId: id }; }",
              "// ✓ concrete until a SECOND need appears:",
              "function wrapUserId(id: number): { userId: number } { return { userId: id }; }",
              "",
              "// The ear for it: 'does the hole CONSTRAIN or CONNECT anything?'",
              "// first<T>(arr: T[]): T | undefined   → connects input to output: KEEP",
              "// merge<A, B>(a: A, b: B): A & B      → connects both: KEEP",
              "// save<T>(data: T): void              → hole connects nothing: DELETE",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The payoff — a typed request<T> for the Todo API",
        body: [
          "Everything in this lesson converges on one function: the typed HTTP wrapper. request<T>(path, init): Promise<Result<T>> carries the RESPONSE SHAPE as its hole — every endpoint instantiates it with its own type, and the parse point is the single place where 'the server said so' becomes 'the types say so'. Combined with the Result<T> discriminated union from the previous lesson, callers branch on ok and hold exactly one side — data typed as T on success, a message on failure.",
          "This is not a toy: it is the literal skeleton of the Phase 19 typed API client (where TanStack Query infers TData from it) and the shape NestJS responses are consumed through. Learn to read request<PaginatedList<Task>> as 'the mold poured with the task-list shape' and the rest of the course's type signatures open like doors.",
        ],
        code: [
          {
            file: "request.ts — the mold every endpoint pours",
            lang: "ts",
            code: [
              "import type { Result } from \"./result\"; // { ok: true; data: T } | { ok: false; error: string }",
              "",
              "async function request<T>(path: string, init?: RequestInit): Promise<Result<T>> {",
              "  try {",
              "    const res = await fetch(`${BASE}${path}`, init);",
              "    if (!res.ok) return { ok: false, error: `HTTP ${res.status}` };",
              "    const data = (await res.json()) as T;  // the trust seam — Phase 20",
              "    return { ok: true, data };             // replaces it with real validation",
              "  } catch (err) {",
              "    return { ok: false, error: err instanceof Error ? err.message : \"network\" };",
              "  }",
              "}",
              "",
              "// every endpoint instantiates the mold:",
              "export const listTodos = () => request<Todo[]>(\"/todos\");",
              "export const getTodo = (id: number) => request<Todo>(`/todos/${id}`);",
              "export const createTodo = (title: string) =>",
              "  request<Todo>(\"/todos\", {",
              "    method: \"POST\",",
              "    headers: { \"Content-Type\": \"application/json\" },",
              "    body: JSON.stringify({ title, done: false }),",
              "  });",
              "",
              "// caller side — one branch, fully typed:",
              "const result = await listTodos();",
              "if (result.ok) {",
              "  render(result.data);   // Todo[] — the hole remembers the shape",
              "} else {",
              "  showBanner(result.error);",
              "}",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The any that ate the generic",
      wrong: [
        "async function fetchData(url: string): Promise<any> {",
        "  const res = await fetch(url);",
        "  return res.json();",
        "}",
        "",
        "const users = await fetchData(\"/users\");",
        "users[0].emial;        // typo — compiles. `any` has no fields to miss.",
        "const names: string[] = users.map((u) => u.nmae); // this too. All of it.",
        "// The function 'worked for every type' by REMEMBERING NONE of them.",
      ].join("\n"),
      right: [
        "async function fetchData<T>(url: string): Promise<T> {",
        "  const res = await fetch(url);",
        "  if (!res.ok) throw new Error(`HTTP ${res.status}`);",
        "  return (await res.json()) as T; // trust seam — validated in Phase 20",
        "}",
        "",
        "const users = await fetchData<User[]>(\"/users\");",
        "// users[0].emial;   // ✗ COMPILE error: 'emial' isn't on User",
        "const names = users.map((u) => u.name); // string[] — typed end to end",
      ].join("\n"),
      explain:
        "Promise<any> is a hole with no walls: every caller's type information drains out at the boundary, and typos become runtime crashes in users' browsers. Promise<T> keeps the walls — the caller pours the shape in, and the compiler enforces it all the way down. The as T cast is a deliberate, single, marked seam (runtime validation arrives in Phase 20); any is an unmarked hole in every wall at once.",
    },
    tryIt: [
      "Write pair<T, U>(a: T, b: U): [T, U] and call it with (1, 'one'). Hover the result — predict [number, string] before you look. Then call pair('x', 'y') and predict again.",
      "Take your Todo type and derive three views without redeclaring a single field: TodoDraft (no id), TodoPatch (Partial of title+done), and TodoSummary (Pick of id+title). Use each in a real function signature.",
      "Break pick on purpose: call pick(user, ['nmae']) and read the compiler's suggestion. Note that it LISTED the valid keys — that autocomplete is the constraint working.",
      "Audit one of your own functions for generic theatre: find a <T> that relates to nothing and replace it with unknown or a concrete type. Feel the diff shrink.",
      "Write loadOr<T>(key: string, fallback: T): T for localStorage with a JSON.parse inside try/catch — what must the catch branch return, and why does the type system force you to answer?",
    ],
    challenge: {
      prompt:
        "Build a typed 'entity store' module with zero any and zero @ts-ignore. Requirements: (1) interface Entity { id: number; createdAt: string } and a generic store factory createStore<E extends Entity>() returning { add(entity: E): void; byId(id: number): E | undefined; patch(id: number, changes: Partial<Omit<E, 'id'>>): E | undefined; all(): readonly E[] } — note that patch must refuse changes to id at the TYPE level; (2) a typed load/save pair for localStorage: save<E>(key: string, value: E): void and load<E>(key: string, validate: (v: unknown) => v is E): E | undefined, where the validator is a type-predicate guard; (3) write isTodoArray(v: unknown): v is Todo[] that checks Array + id number + title string for every element; (4) demonstrate: create a Todo store, add three, patch one's done flag, persist, reload into a NEW store with isTodoArray, and prove patch(todo, { id: 999 }) is a compile error.",
      hints: [
        "Store internals: private state via closure — const items: E[] = [] inside the factory; all() returns a readonly view ([...items] spread).",
        "Partial<Omit<E, 'id'>> is the whole trick for patch: Omit removes id from the shape BEFORE Partial makes the rest optional, so { id: … } is unassignable — the type system enforces your invariant.",
        "The validator narrows unknown: if (!Array.isArray(v)) return false; return v.every(el => typeof el === 'object' && el !== null && typeof (el as Todo).id === 'number' && typeof (el as Todo).title === 'string'). load parses JSON in try/catch and applies it before returning.",
      ],
      solution: [
        "interface Entity { id: number; createdAt: string; }",
        "",
        "interface Todo extends Entity {",
        "  title: string;",
        "  done: boolean;",
        "}",
        "",
        "// ——— (1) the store factory: E is known (Entity) and unknown (the rest) ———",
        "function createStore<E extends Entity>() {",
        "  const items: E[] = [];",
        "  return {",
        "    add(entity: E): void {",
        "      items.push(entity);",
        "    },",
        "    byId(id: number): E | undefined {",
        "      return items.find((e) => e.id === id);",
        "    },",
        "    patch(id: number, changes: Partial<Omit<E, \"id\">>): E | undefined {",
        "      const found = items.find((e) => e.id === id);",
        "      if (!found) return undefined;",
        "      const next = { ...found, ...changes }; // id untouched — Omit removed it",
        "      items[items.indexOf(found)] = next;",
        "      return next;",
        "    },",
        "    all(): readonly E[] {",
        "      return [...items]; // defensive copy; callers can't push into the store",
        "    },",
        "  };",
        "}",
        "",
        "// ——— (2) typed persistence with a type-predicate guard ———",
        "function save<E>(key: string, value: E): void {",
        "  try {",
        "    localStorage.setItem(key, JSON.stringify(value));",
        "  } catch { /* private mode — in-memory only */ }",
        "}",
        "",
        "function load<E>(key: string, validate: (v: unknown) => v is E): E | undefined {",
        "  try {",
        "    const raw = localStorage.getItem(key);",
        "    if (raw == null) return undefined;",
        "    const parsed: unknown = JSON.parse(raw);",
        "    return validate(parsed) ? parsed : undefined; // unknown → E, ONLY via proof",
        "  } catch {",
        "    return undefined; // corrupt JSON is 'missing', not a crash",
        "  }",
        "}",
        "",
        "// ——— (3) the validator: proof, not optimism ———",
        "function isTodoArray(v: unknown): v is Todo[] {",
        "  if (!Array.isArray(v)) return false;",
        "  return v.every((el) => {",
        "    if (typeof el !== \"object\" || el === null) return false;",
        "    const t = el as Record<string, unknown>;",
        "    return (",
        "      typeof t.id === \"number\" &&",
        "      typeof t.title === \"string\" &&",
        "      typeof t.done === \"boolean\" &&",
        "      typeof t.createdAt === \"string\"",
        "    );",
        "  });",
        "}",
        "",
        "// ——— (4) demonstration ———",
        "const store = createStore<Todo>();",
        "const now = new Date().toISOString();",
        "store.add({ id: 1, title: \"Learn generics\", done: true, createdAt: now });",
        "store.add({ id: 2, title: \"Ship the store\", done: false, createdAt: now });",
        "store.add({ id: 3, title: \"Persist it\", done: false, createdAt: now });",
        "",
        "store.patch(2, { done: true });          // ✓ done is patchable…",
        "// store.patch(2, { id: 999 });           // ✗ COMPILE error:",
        "//   'id' does not exist in type 'Partial<Omit<Todo, \"id\">>'",
        "//   The invariant is enforced by the TYPE, before any code runs.",
        "",
        "save(\"todos.v2\", store.all());",
        "",
        "const restored = createStore<Todo>();",
        "const loaded = load(\"todos.v2\", isTodoArray);",
        "if (loaded) {",
        "  for (const todo of loaded) restored.add(todo);",
        "}",
        "restored.all(); // readonly Todo[] — same shape, fresh store,",
        "                // and every step was checked by the compiler.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "In function first<T>(arr: T[]): T | undefined, the T in the return…",
        options: ["Is the SAME type the caller's array elements have — inferred per call", "Is always unknown", "Must be supplied explicitly at every call", "Defaults to any"],
        answer: 0,
        explain: "The hole connects input to output: first([1,2]) instantiates T as number; first(['a']) as string. Inference at the call site is the default rhythm.",
      },
      {
        q: "<T extends { id: number }> lets the function body use…",
        options: ["Only what the constraint guarantees (value.id) — the rest of T stays unknown", "Every field of the first caller's type", "Any field, checked at runtime", "Nothing — constraints lock everything"],
        answer: 0,
        explain: "T is known (id exists) and unknown (everything else) at once. Writing value.name would break for some future T — the compiler correctly refuses.",
      },
      {
        q: "The type of Pick<User, 'id' | 'name'> is…",
        options: ["{ id: …; name: … } — exactly those keys from User", "User with id and name removed", "User with everything optional", "A union of the two fields"],
        answer: 0,
        explain: "Pick keeps the listed keys with their original types; Omit removes them. Both derive from the source type — edit User, and every view updates.",
      },
      {
        q: "Partial<T> is the right shape for…",
        options: ["Update/patch payloads where any subset of fields may arrive", "Data that has been fully validated", "Read-only snapshots", "Removing optional fields"],
        answer: 0,
        explain: "Partial makes every field optional — drafts and patches. Required<T> is its inverse, for the validated end-state. Readonly<T> locks; Omit removes.",
      },
      {
        q: "A generic is over-engineering when…",
        options: ["Its type parameter relates to nothing — used once, constrains nothing, connects no positions", "It has more than one type parameter", "The function is short", "Callers never pass explicit type arguments"],
        answer: 0,
        explain: "The test is connection: does the hole constrain or relate positions? save<T>(data: T): void gains nothing over save(data: unknown): void. Start concrete; generalize on the second real need.",
      },
      {
        q: "Promise<any> vs Promise<T> — the real difference is…",
        options: ["any erases the caller's type at the boundary; T carries it through to every downstream line", "any is faster at runtime", "Promise<T> requires explicit arguments", "There is none — both are unchecked"],
        answer: 0,
        explain: "any is amnesia: typos compile and crash in production. T keeps the walls — the caller pours the shape and the compiler enforces it end to end.",
      },
    ],
    flashcards: [
      { front: "Generic", back: "A type-level parameter — a labeled hole filled per call site. <T>(x: T): T says 'one type, whichever you bring, and it survives the trip'." },
      { front: "Constraint (extends)", back: "<T extends Shape> guarantees the listed structure while the rest stays free. The body may use only what's promised. K extends keyof T locks keys to their object." },
      { front: "Partial / Required", back: "Partial<T>: every field optional (patches, drafts). Required<T>: every field mandatory (validated end-states). Inverses of each other." },
      { front: "Pick / Omit", back: "Pick<T, K> keeps the listed keys; Omit<T, K> removes them. Derived views of ONE source of truth — they cannot drift." },
      { front: "Record<K, V>", back: "A map type from key type K to value V. Record<User['role'], number> exhaustively covers every role — add a role, the map demands a count." },
      { front: "ReturnType<F>", back: "Extracts a function's return type. Derives truth from existing code instead of redeclaring it — the anti-drift tool for config factories and hooks." },
      { front: "The three generic tests", back: "Is T used in related positions? Will it ever see two type families? Does it keep call sites readable? Three nos → write the concrete type." },
      { front: "The as T seam", back: "A marked trust boundary (e.g., parsed JSON) where runtime truth meets compile-time types. One seam per parse point, validated for real in Phase 20. any is an unmarked seam everywhere." },
    ],
    recap: [
      "A generic is a mold: real shape, poured per call. Inference does the pouring; explicit arguments are rare.",
      "Constraints let T be known and unknown at once; keyof-constraints kill the typo class.",
      "Utility types derive views from one source of truth — derive, never duplicate.",
      "Generics must connect positions; lone holes become unknown or concrete types.",
      "request<T> + Result<T> is the course's API shape from here to the capstones.",
    ],
    references: [
      { label: "TypeScript Handbook — Generics", url: "https://www.typescriptlang.org/docs/handbook/2/generics.html" },
      { label: "TypeScript Handbook — Utility Types", url: "https://www.typescriptlang.org/docs/handbook/utility-types.html" },
      { label: "MDN — TypeScript for JavaScript Programmers", url: "https://developer.mozilla.org/en-US/docs/Learn_web_development/Core/Scripting/TypeScript" },
      { label: "TypeScript Handbook — Narrowing (type predicates)", url: "https://www.typescriptlang.org/docs/handbook/2/narrowing.html#using-type-predicates" },
      { label: "Total TypeScript — Generics guide", url: "https://www.totaltypescript.com/workshops/typescript-generics" },
    ],
    nextBridge:
      "Your types now travel, transform, and refuse politely. But none of it protects you if the compiler is asleep at the wheel — next lesson turns every safety dial up: the strict flag dissected, compiler errors read like documentation, and the ten-error diagnostic lab that makes TS2322 a friend instead of a wall.",
  },
  {
    id: "p03-l5",
    phaseId: "p03",
    title: "Strict tsconfig & Compiler Diagnostics",
    level: "Foundation",
    minutes: 35,
    summary:
      "TypeScript is only as strict as its configuration — and every codebase in this course, from Next.js to NestJS, ships with strict on. This lesson dissects the strict family flag by flag, teaches you to read compiler errors as documentation (the code number is a door, not a wall), installs noUncheckedIndexedAccess and verbatimModuleSyntax as course defaults, and drills ten real diagnostics until the red squiggle feels like a colleague, not a critic.",
    prerequisites: ["p03-l3 — generics and utility types", "p03-l2 — unions and narrowing"],
    objectives: [
      "State what strict enables (strictNullChecks, noImplicitAny, useUnknownInCatchVariables, and friends) and why each exists.",
      "Read a TS error structurally: code number, location, expected-vs-actual — and map the six common codes to their fixes.",
      "Model optionality with types instead of silencing it with non-null assertions.",
      "Apply noUncheckedIndexedAccess and handle T | undefined from index access deliberately.",
      "Use import type and verbatimModuleSyntax so erased types never become runtime imports.",
      "Diagnose ten compiler errors without running the code — the error message as the lesson.",
    ],
    simple:
      "TypeScript out of the box is polite to a fault: it guesses types, assumes nothing is null, and lets you import things that don't exist at runtime. strict mode is the switch from polite colleague to meticulous editor — it refuses to let an unproven assumption leave the building. And when it complains, it hands you a numbered ticket (TS2322, TS2532…) that says exactly what was expected, what arrived, and where. The skill isn't avoiding red squiggles; it's reading the ticket. Every error you'll ever see is one of a dozen patterns wearing different clothes.",
    why:
      "The strict-vs-loose decision is the highest-leverage line in any TypeScript project, because it compounds: loose configs let null-bugs and implicit-anys accumulate silently, and teams inherit them as midnight production incidents; strict configs pay a small tax at authoring time and refund it at every future refactor. The ecosystem has already voted — Next.js, NestJS, Prisma's generated client, TanStack Query's docs all assume strict. And the diagnostics skill transfers to every tool that reports in file:line:col form: ESLint, Prisma's validator, even a stack trace. You are installing a reading habit that outlasts any single compiler.",
    mentalModel: {
      title: "The meticulous editor",
      body: "A loose compiler is the friend who reads your draft and says 'looks great!' — pleasant, useless. strict is the editor with a red pen and a checklist: 'This could be null — prove it isn't. This parameter has no type — declare one. You imported a type as if it were a value — it vanishes at runtime.' The editor never rewrites your prose; it only refuses to let unsupported claims through. The red pen marks are numbered (TS2322, TS2532…) and every number has a page in the manual. Writers who fight the editor lose; writers who read the marks ship clean drafts and eventually stop making the marked mistakes altogether.",
    },
    sections: [
      {
        heading: "strict is a family, not a flag",
        body: [
          "\"strict\": true in tsconfig switches on a bundle, and knowing the members tells you what each red squiggle is protecting. The headline act is strictNullChecks: null and undefined stop being assignable to every type, so 'could be absent' becomes a TYPE you must handle — the single biggest source of runtime crashes in JavaScript history, made structural. noImplicitAny refuses to guess parameter types (function f(x) — x of WHAT?). useUnknownInCatchVariables (TS 4.4+) makes catch (err) hand you unknown instead of any, forcing you to narrow before you blame. strictFunctionTypes and strictBindCallApply police function-parameter direction and call signatures; strictPropertyInitialization demands class fields are assigned in the constructor (NestJS developers will meet it, then configure around it deliberately with decorators).",
          "Two flags the course adds on top of strict. noUncheckedIndexedAccess makes arr[i] and obj[key] include undefined in their type — because AT RUNTIME they genuinely might, and Phase 2's array lesson proved it. It adds friction at every index access, which is exactly the point: each friction is a decision instead of a hope. verbatimModuleSyntax enforces import type for type-only imports, so erased types never masquerade as runtime dependencies — critical for bundlers, ESM interop, and the NestJS world where a wrong import style fails at server startup, not build time.",
        ],
        code: [
          {
            file: "tsconfig.json — the course baseline",
            lang: "json",
            code: [
              "{",
              "  \"compilerOptions\": {",
              "    \"target\": \"ES2022\",",
              "    \"module\": \"ESNext\",",
              "    \"moduleResolution\": \"bundler\",",
              "    \"strict\": true,",
              "    \"noUncheckedIndexedAccess\": true,",
              "    \"verbatimModuleSyntax\": true,",
              "    \"skipLibCheck\": true,",
              "    \"noEmit\": true",
              "  }",
              "}",
              "// strict expands to: strictNullChecks, noImplicitAny,",
              "// strictFunctionTypes, strictBindCallApply,",
              "// strictPropertyInitialization, noImplicitThis,",
              "// alwaysStrict, useUnknownInCatchVariables…",
              "// Every project from Phase 4 onward inherits this file.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Anatomy of an error — the ticket, not the wall",
        body: [
          "Every TypeScript error is a four-line ticket: a code (error TS2322), a location (src/app.ts:14:7), the claim ('Type X is not assignable to type Y'), and usually the evidence ('Property name is missing' / 'string is not assignable to number'). Read in that order: the code tells you the PATTERN (hover it — editors show the manual page), the location tells you WHERE, expected-vs-actual tells you WHY. Beginners read bottom-up and drown; professionals read the code first and usually know the fix before reading the rest.",
          "The working catalog, six codes that cover most daily life: TS2322 'Type A is not assignable to Type B' — the assignment mismatch, fix by changing the value or widening the declared type honestly; TS2339 'Property does not exist on type' — typo, or you're on the wrong union member (narrow first!); TS2345 'Argument of type A is not assignable to parameter of type B' — same as 2322 but at a call boundary; TS2532 'Object is possibly undefined' — strictNullChecks doing its job, fix with a guard or optional chain, never a reflex !; TS7006 'Parameter implicitly has an any type' — noImplicitAny asking you to say what you mean; TS2769 'No overload matches this call' — read the LAST overload's mismatch, usually the real story.",
        ],
        code: [
          {
            file: "reading-errors.ts — the same bug, six tickets",
            lang: "ts",
            code: [
              "interface Todo { id: number; title: string; done: boolean; }",
              "",
              "const todo: Todo = { id: 1, title: \"Ship\", done: false };",
              "",
              "// TS2339 — the typo ticket (fix: it's 'title'):",
              "// todo.titel.toUpperCase();",
              "//          ~~~~~ Property 'titel' does not exist on type 'Todo'.",
              "//                Did you mean 'title'?",
              "",
              "// TS2532 — strictNullChecks earns its salary:",
              "const maybe: Todo | undefined = undefined;",
              "// maybe.title;  // Object is possibly 'undefined'.",
              "if (maybe) maybe.title;          // ✓ narrow — the guard IS the proof",
              "maybe?.title;                    // ✓ optional chain — yields string | undefined",
              "",
              "// TS7006 — the compiler asking you to mean it:",
              "// [1, 2].map((n) => n.toFixed());  // fine, n is inferred",
              "const handler = (event: SubmitEvent) => event.preventDefault();",
              "//                 ^^^^^ declared, because inference can't see the future",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The non-null assertion — debt with a syntax",
        body: [
          "The ! operator (user!.email) tells the compiler 'I know this isn't null — stop asking'. It compiles, and it is a loan: every ! is a claim the compiler can no longer verify, repaid with interest the day the claim is wrong in production. The professional alternatives, in order: narrow with a control-flow guard (if (user) …); model the absence honestly (the type SHOULD include undefined, and the code SHOULD handle it); restructure so the value can't be absent (initialize earlier, pass it as a required parameter).",
          "The same logic kills @ts-ignore and @ts-expect-error used as sedatives. One legitimate niche: @ts-expect-error WITH an explanatory comment and an issue link, for a known external-library type bug — and it self-heals, because the build FAILS when the error disappears. A bare @ts-ignore never fails when it becomes unnecessary; it just hides the next real bug. Audit rule for code review: every ! and every ignore gets a comment justifying why the type system can't know what you know.",
        ],
        code: [
          {
            file: "assertions.ts — the loan and its alternatives",
            lang: "ts",
            code: [
              "const users: User[] = [];",
              "const firstUser = users[0];   // User | undefined (noUncheckedIndexedAccess)",
              "",
              "// ✗ the loan: compiles today, lies tomorrow",
              "// console.log(firstUser!.email);",
              "",
              "// ✓ alternative 1 — narrow:",
              "if (firstUser) {",
              "  console.log(firstUser.email); // User here. Proven.",
              "}",
              "",
              "// ✓ alternative 2 — restructure so absence is impossible:",
              "function renderEmail(user: User) {   // caller must provide a real User",
              "  return user.email;                 // no question left to ask",
              "}",
              "",
              "// The one sanctioned ignore — self-healing and documented:",
              "// @ts-expect-error -- upstream types wrong until lib v3.2 (issue #482)",
              "weirdLibCall();",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Types that vanish — import type and the runtime boundary",
        body: [
          "TypeScript types are a compile-time fiction: they evaporate during the build, leaving plain JavaScript. Most of the time that's invisible — except when an import blurs the line. import { Todo } from './types' asks the bundler to fetch a RUNTIME binding named Todo from a module that exports only types; under verbatimModuleSyntax this is a compile error, and in the NestJS/ESM world the equivalent mistake crashes the server at startup with a cryptic 'does not provide an export' message.",
          "The discipline is two words: import type. import type { Todo } from './types' declares intent — 'this name exists only in the type world' — and the compiler erases the whole statement, guaranteeing no runtime trace. Value imports (functions, classes, constants) stay plain. When a module gives you both, split the line: import { createStore, type Todo } — inline type markers keep one import, zero ambiguity. This is also how Prisma keeps its client out of browser bundles (Phase 14): type-only imports cross boundaries; value imports don't.",
        ],
        code: [
          {
            file: "imports.ts — two worlds, marked at the door",
            lang: "ts",
            code: [
              "// types.ts — exports ONLY types:",
              "export interface Todo { id: number; title: string; done: boolean; }",
              "export type TodoPatch = Partial<Omit<Todo, \"id\">>;",
              "",
              "// consumer.ts:",
              "import type { Todo } from \"./types\";        // ✓ erased at build",
              "import { createStore, type Store } from \"./store\"; // value + type, split",
              "",
              "// import { Todo } from \"./types\";",
              "// ✗ verbatimModuleSyntax: 'Todo' is a type and must be imported",
              "//   using a type-only import. The bundler would have chased a",
              "//   runtime export that never exists.",
              "",
              "const list: Todo[] = createStore().all(); // Store used inline as a type",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The ten-error lab — diagnosis without running",
        body: [
          "The drill that makes diagnostics reflexive: read each snippet, name the error CODE you expect, and state the fix — before the compiler gets a vote. Ten patients, in rising difficulty. (1) const n: number = '42' — 2322, string isn't number; fix the value or the type, never both dishonestly. (2) 'hello'.toUppercase() — 2339 with a did-you-mean; a typo, not a design flaw. (3) users[0].email under noUncheckedIndexedAccess — 18048/2532; guard it. (4) function add(a, b) { return a + b } — 7006 twice; name the types. (5) a catch block reading err.message with useUnknownInCatchVariables — 18046; narrow: if (err instanceof Error). (6) Passing Todo where Todo[] is expected — 2345; wrap or unwrap. (7) Assigning role: 'guest' where role is 'admin' | 'member' — 2322 again; widen the union deliberately if guest is real. (8) readonly list with .push — 2339/2540; readonly means readonly — produce a new array. (9) import { Todo } from a type-only module — verbatimModuleSyntax error; import type. (10) An exhaustive switch missing a new union member — 2322 in the never assignment; add the case — the exhaustiveness guard from P03-L2 doing its job.",
          "Run each through tsc with the course tsconfig and compare your diagnosis to the ticket. The goal isn't memorizing numbers; it's the reflex — code first, pattern second, fix third — that turns a wall of red into a to-do list you can finish in order.",
        ],
      },
    ],
    mistake: {
      title: "Sprinkling ! to make the red go away",
      wrong: [
        "function renderUser(user: User | undefined) {",
        "  const el = document.querySelector(\"#card\")!;",
        "  el.textContent = user!.name;      // two loans, zero proofs",
        "  const orders = user!.orders!;     // a third — we're on a streak",
        "  renderRows(orders.map((o) => o.total));",
        "}",
        "// Compiles. Then one production page lacks #card, or an API",
        "// change makes user undefined, and the crash happens in a",
        "// USER'S browser with none of the three guards you deleted.",
      ].join("\n"),
      right: [
        "function renderUser(user: User | undefined) {",
        "  const el = document.querySelector(\"#card\");",
        "  if (!el || !user) return;        // absence is HANDLED, not denied",
        "",
        "  el.textContent = user.name;      // el: Element, user: User — proven",
        "  const orders = user.orders ?? []; // model the maybe with data",
        "  renderRows(orders.map((o) => o.total));",
        "}",
        "// Same line count. The type system now OWES you nothing,",
        "// and the early return documents the empty state for free.",
      ].join("\n"),
      explain:
        "Each ! converts a provable fact into an unverifiable claim — and claims rot. The compiler wasn't being difficult; it was reporting real absences that the code then pretended away. Guards (if), modeling (??, optionals), and restructuring (required parameters) discharge the question instead of silencing the asker. Code-review rule: no ! or @ts-ignore without a written justification.",
    },
    tryIt: [
      "Copy the course tsconfig into a scratch project and write const x = null; x.toFixed() — read the ticket aloud: code, location, claim. Then break five more things and name each code before hovering.",
      "Take a function with two untyped parameters and watch TS7006 demand meaning. Type them, then deliberately type one WRONG and follow the new errors downstream — watch the compiler walk the graph for you.",
      "Enable noUncheckedIndexedAccess and fix the fallout in your Todo app's render loop: every list[i] is now T | undefined. How many sites needed a guard, and how many needed for…of instead?",
      "Write a try/catch that logs err.message and accept the useUnknownInCatchVariables error. Fix it with instanceof narrowing, then with a hasMessage type predicate — compare which reads better.",
      "Audit one file for type-only imports: convert them to import type, enable verbatimModuleSyntax, and confirm the build output (or your bundler's view) no longer references the type module at runtime.",
    ],
    challenge: {
      prompt:
        "You inherit a module written against a LOOSE tsconfig. Without changing any behavior, make it compile under the strict course baseline — zero any, zero !, zero @ts-ignore. The module: (1) function formatPrice(cents) returns `$${(cents / 100).toFixed(2)}`; (2) function findUser(users, id) { return users.filter(u => u.id === id)[0]; } whose caller does findUser(users, 7).name; (3) a catch (err) { log(err.message) } handler; (4) export interface Config { retries: number } plus import { Config } from './config' in a consumer; (5) const first = items[0]; first.title; over Todo[]. Deliver: the strict tsconfig.json, the corrected module, and a two-column 'before/after' note for each fix naming the error code you'd expect in strict mode.",
      hints: [
        "formatPrice: TS7006 on the parameter — type it number. findUser: the [0] access is the interesting one — under noUncheckedIndexedAccess the return becomes User | undefined, and the CALLER's .name access is where TS2532 fires. Fix at the boundary: return type User | undefined, and let the caller guard.",
        "catch: useUnknownInCatchVariables gives you TS18046 on err.message. Narrow with if (err instanceof Error) — or write const message = err instanceof Error ? err.message : String(err).",
        "The Config import fails verbatimModuleSyntax — it's type-only. import type { Config } makes the erase explicit. Your before/after codes: 7006, 2532/18048, 18046, verbatimModuleSyntax error, and whichever index-access code your TS version reports.",
      ],
      solution: [
        "// ——— tsconfig.json (course baseline) ———",
        "{",
        "  \"compilerOptions\": {",
        "    \"target\": \"ES2022\", \"module\": \"ESNext\",",
        "    \"moduleResolution\": \"bundler\",",
        "    \"strict\": true,",
        "    \"noUncheckedIndexedAccess\": true,",
        "    \"verbatimModuleSyntax\": true,",
        "    \"noEmit\": true",
        "  }",
        "}",
        "",
        "// ——— prices.ts ———",
        "// BEFORE: function formatPrice(cents) — TS7006 (implicit any parameter)",
        "export function formatPrice(cents: number): string {",
        "  return `$${(cents / 100).toFixed(2)}`;",
        "}",
        "",
        "// ——— users.ts ———",
        "export interface User { id: number; name: string; }",
        "",
        "// BEFORE: findUser(users, id) { return users.filter(u => u.id === id)[0]; }",
        "//   → TS7006 ×3 (untyped params + callback), then the caller's",
        "//     findUser(users, 7).name → TS2532/18048: possibly undefined.",
        "// AFTER: the function ADMITS absence; the caller handles it.",
        "export function findUser(users: User[], id: number): User | undefined {",
        "  return users.find((u) => u.id === id); // find > filter()[0]: honest intent",
        "}",
        "",
        "const maybe = findUser(users, 7);",
        "const display = maybe ? maybe.name : \"unknown user\"; // guard = proof",
        "",
        "// ——— logging.ts ———",
        "// BEFORE: catch (err) { log(err.message) } → TS18046 ('err' is unknown)",
        "export async function run(job: () => Promise<void>): Promise<void> {",
        "  try {",
        "    await job();",
        "  } catch (err) {",
        "    const message = err instanceof Error ? err.message : String(err);",
        "    console.error(`job failed: ${message}`); // narrowed, no assumption",
        "  }",
        "}",
        "",
        "// ——— consumer.ts ———",
        "// BEFORE: import { Config } from \"./config\"",
        "//   → verbatimModuleSyntax error: type-only export imported as a value.",
        "import type { Config } from \"./config\";",
        "",
        "export function withDefaults(partial: Partial<Config>): Required<Config> {",
        "  return { retries: 3, ...partial };",
        "}",
        "",
        "// ——— the before/after ledger ———",
        "// (1) formatPrice      TS7006            typed the parameter: number",
        "// (2) findUser/caller  TS7006 → TS2532   honest User | undefined + guard",
        "// (3) catch handler    TS18046           instanceof narrowing",
        "// (4) Config import    verbatimModule    import type",
        "// (5) items[0].title   TS18048/2532      guard the index access",
        "// Behavior identical; every absence is now a decision the compiler audits.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "strictNullChecks changes the language by…",
        options: ["Making null and undefined their own problem — no longer assignable to every type", "Removing null from JavaScript", "Making every type nullable", "Enforcing runtime null checks"],
        answer: 0,
        explain: "Absence becomes part of the TYPE: User | undefined must be handled before you can use it. TypeScript does nothing at runtime — the check happens at compile time.",
      },
      {
        q: "The professional reading order for a TS error is…",
        options: ["Code number → location → expected vs actual", "Bottom of the message up", "The squiggle color first", "Run the code and see"],
        answer: 0,
        explain: "The code names the PATTERN (2322 assignment, 2532 maybe-undefined, 7006 implicit any) — often the whole diagnosis. Location pins it; expected/actual confirms.",
      },
      {
        q: "user!.email is acceptable when…",
        options: [
          "Almost never — prefer a guard, honest modeling, or restructuring; if used, it needs a justification comment",
          "Whenever the developer feels confident",
          "Only in tests",
          "Only on Mondays",
        ],
        answer: 0,
        explain: "! converts a provable fact into an unverifiable claim. The compiler was reporting a real absence; silencing it moves the crash to production. Audit rule: every ! gets a written justification.",
      },
      {
        q: "Under noUncheckedIndexedAccess, arr[0] has type…",
        options: ["T | undefined — because at runtime it genuinely might be", "T", "never", "any"],
        answer: 0,
        explain: "Empty arrays and missing keys are runtime facts; this flag makes them compile-time facts too. Each index access becomes a decision (guard, ??, for…of) instead of a hope.",
      },
      {
        q: "import type { Todo } differs from import { Todo } because…",
        options: ["The whole statement is erased at build — no runtime binding is chased", "It's faster at runtime", "It imports the interface and its implementation", "It only works with classes"],
        answer: 0,
        explain: "Types vanish at compile time; a value import of a type-only export chases a binding that never exists — a build error under verbatimModuleSyntax, a server crash in ESM/NestJS.",
      },
      {
        q: "@ts-expect-error beats @ts-ignore because…",
        options: ["It fails the build when the error disappears — self-healing debt", "It suppresses more errors", "It works at runtime", "It doesn't need a comment"],
        answer: 0,
        explain: "The expectation is checked: fix the underlying issue and the stale directive becomes a compile error, forcing removal. ts-ignore just accumulates, hiding the next real bug.",
      },
    ],
    flashcards: [
      { front: "strict: true", back: "A bundle: strictNullChecks, noImplicitAny, useUnknownInCatchVariables, strictFunctionTypes, strictBindCallApply, strictPropertyInitialization, noImplicitThis, alwaysStrict. The ecosystem default." },
      { front: "strictNullChecks", back: "null/undefined are no longer assignable to every type — absence becomes part of the type and must be handled. The biggest crash-prevention flag." },
      { front: "Error ticket anatomy", back: "Code (the pattern) → file:line:col (where) → expected vs actual (why). Read the code first; six codes cover daily life: 2322, 2339, 2345, 2532, 7006, 2769." },
      { front: "TS2322 vs TS2345", back: "2322: assignment mismatch. 2345: same mismatch at an argument boundary. Both mean 'these types don't agree' — change the value or honestly widen the type." },
      { front: "Non-null assertion (!)", back: "A loan against truth: tells the compiler to stop verifying a claim. Prefer guards, ?? modeling, or restructuring. Every surviving ! owes a comment." },
      { front: "noUncheckedIndexedAccess", back: "arr[i] and obj[key] become T | undefined. Friction by design: every index access is a decision. Course default from Phase 3 onward." },
      { front: "import type", back: "Marks a type-only import so the compiler erases the statement — no runtime binding chased. verbatimModuleSyntax enforces the marking." },
      { front: "@ts-expect-error", back: "A documented, self-healing suppression: the build fails when the expected error disappears. The only sanctioned ignore; always with a comment and reason." },
    ],
    recap: [
      "strict is a family — strictNullChecks is the headline, useUnknownInCatchVariables the modern addition; the course adds noUncheckedIndexedAccess and verbatimModuleSyntax.",
      "Errors are tickets: code → location → expected/actual. Six codes cover most days.",
      "! and ts-ignore are debt; guards, modeling, and restructuring are payment.",
      "Index access includes undefined; every access is a decision.",
      "Types vanish at build — import type marks the door. The red pen is a colleague.",
    ],
    references: [
      { label: "TypeScript — tsconfig reference (strict)", url: "https://www.typescriptlang.org/tsconfig#strict" },
      { label: "TypeScript — noUncheckedIndexedAccess", url: "https://www.typescriptlang.org/tsconfig#noUncheckedIndexedAccess" },
      { label: "TypeScript — verbatimModuleSyntax", url: "https://www.typescriptlang.org/tsconfig#verbatimModuleSyntax" },
      { label: "TypeScript — TS2322 error reference", url: "https://typescript.tv/errors/#ts2322" },
      { label: "Matt Pocock — Total TypeScript essentials", url: "https://www.totaltypescript.com/workshops/beginners-typescript" },
    ],
    nextBridge:
      "Theory becomes muscle memory in the migration lab: the vanilla Todo app — the one you've carried since Phase 2 — goes file-by-file into strict TypeScript, and the compiler interviews it into confessing two real bugs it had been hiding all along.",
  },
  {
    id: "p03-l6",
    phaseId: "p03",
    title: "Lab: Migrate the Todo App to TypeScript",
    level: "Foundation",
    minutes: 45,
    summary:
      "No rewrites, no rewrites, no rewrites. This lab takes the working vanilla Todo app from Phases 1–2, renames the files .js → .ts, and lets the strict compiler interview every line: What shape is a todo? Can this be undefined? What does the server actually return? Along the way it confesses two real bugs — a swallowed 204 and a NaN id — that JavaScript had been hiding since the day they shipped. Then the payoff: a refactor that the old version could only attempt with crossed fingers.",
    prerequisites: ["p03-l5 — strict tsconfig and diagnostics", "p02-l4 — the vanilla Todo skeleton", "p02-l5 — the HTTP api module"],
    objectives: [
      "Run an incremental migration: allowJs-era patience, strict from line one, one file at a time, behavior frozen.",
      "Type an HTTP layer end-to-end: Todo, TodoPatch, Result<T>, request<T> — and watch the compiler audit the server contract.",
      "Type the DOM honestly: querySelector's T | null, SubmitEvent vs MouseEvent, delegation over data-attributes, dataset's string-or-undefined.",
      "Hunt the two planted-in-plain-sight bugs and explain why only the type system could catch them at authoring time.",
      "Perform a confident rename refactor and count the sites the compiler finds.",
      "Articulate the Then-vs-Now delta: nothing changed visually; whole bug classes became unrepresentable.",
    ],
    simple:
      "Migrating to TypeScript is not a rewrite — it's an interview. You hand the compiler code that 'works' and it asks polite, relentless questions: 'This users[0] — what if the list is empty? This response — what if it's a 204 with no body? This dataset.id — you're calling Number() on something that might not exist.' JavaScript answered those questions with silence and hoped for the best. TypeScript refuses to hope. You answer each question by writing the truth as a type, and the astonishing part: when the interview is over, the app behaves EXACTLY the same — except the two crashes you didn't know you had are gone, and every future refactor comes with a receipt.",
    why:
      "Every real codebase you join adopts TypeScript this way — incrementally, under deadline, with behavior frozen — so the skill that matters is migration, not greenfield perfection. This lab also delivers the Foundation stage's core Then-vs-Now moment: the SAME app, reviewed side by side, where the typed version isn't 'more code' but 'more truth per line'. And the payoff is not theoretical: the rename refactor at the end demonstrates the compiler-as-safety-net on a change that, in the JS version, would have been a weekend of manual hunting and a prayer. This is the habit the rest of the course runs on — React props, NestJS DTOs, Prisma models, TanStack Query keys all inherit the same discipline.",
    mentalModel: {
      title: "The inspector, not the architect",
      body: "The TypeScript compiler is not an architect sketching your app from scratch — it's a building inspector walking rooms that are already standing. It doesn't move walls; it taps each one and asks: load-bearing or not? 'This beam (users[0]) — where's its support?' 'This doorway (toggleTodo's return) — you wrote that it always carries a body; it doesn't when the server sends 204.' You don't rebuild the house to satisfy the inspector; you add the supports and update the blueprints (the types) to match reality. When the walkthrough is done, the house looks identical — but now it has a certificate, and any future renovation that threatens a load-bearing wall gets flagged before the paint is bought.",
    },
    sections: [
      {
        heading: "The plan — freeze behavior, let the compiler talk",
        body: [
          "Three rules make migrations survivable. One: behavior is FROZEN — no refactors, no new features, no 'while I'm here' improvements; the diff must stay reviewable and the app must work identically after every file. Two: strict from line one — migrating under a loose config just relocates the debt; turn on the course tsconfig and treat the error list as a work queue, top to bottom. Three: one file at a time, API layer first — types flow outward: define the data shapes (Todo, Result<T>), then the layer that produces them (api.ts), then the layers that consume them (app.ts). By the time the UI file compiles, almost everything it touches already has a name.",
          "Practical setup: rename api.js → api.ts, app.js → app.ts, add the course tsconfig.json, run tsc --noEmit and count the errors. A medium app shows 15–30. That number is not a verdict; it's a to-do list, and it shrinks monotonically — every fix is permanent because the compiler never forgets.",
        ],
        code: [
          {
            file: "migration-plan.sh — the whole ceremony",
            lang: "bash",
            code: [
              "# 1) the course baseline lands first — no loose interim",
              "cp course-tsconfig.json tsconfig.json",
              "",
              "# 2) data shapes before anything that uses them",
              "touch types.ts          # Todo, TodoPatch, Result<T>",
              "",
              "# 3) rename outward: producer → consumer",
              "mv api.js api.ts        # now: npx tsc --noEmit (expect ~8 errors)",
              "mv app.js app.ts        # now: npx tsc --noEmit (expect ~15 more)",
              "",
              "# 4) fix top to bottom; NEVER change behavior mid-migration",
              "npx tsc --noEmit --watch  # the interview, live",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Typing the API layer — and bug #1 confesses",
        body: [
          "types.ts states the contract the scratch server actually keeps: Todo with id/title/done/createdAt; TodoPatch = Partial<Omit<Todo, 'id'>>; and the Result<T> union from Phase 3. api.ts then instantiates request<T> per endpoint — and immediately the compiler clears its throat about removeTodo: the original code ran res.json() unconditionally, but DELETE answers 204 NO CONTENT. Parsing an empty body throws — a crash that only fired when a user deleted the LAST visible todo on a slow network, which is to say: it fired in production, intermittently, forever. The type system finds it in the authoring minute: response.json(): Promise<any> becomes a typed parse helper that returns null for 204, and removeTodo's contract becomes Promise<Result<null>> — the absence is now in the signature.",
          "This is the pattern to internalize: the compiler didn't invent the bug, it surfaced a lie the code was telling — 'every response has a body'. Every migration finds two or three of these. They are the real deliverable; the types are just how you keep them found.",
        ],
        code: [
          {
            file: "api.ts — the interview transcript, abridged",
            lang: "ts",
            code: [
              "import type { Result, Todo, TodoPatch } from \"./types\";",
              "",
              "const BASE = \"http://localhost:3000\";",
              "",
              "// the 204-aware parse — bug #1's fix lives HERE, once:",
              "async function parse<T>(res: Response): Promise<T> {",
              "  if (res.status === 204) return null as T;   // no body, by contract",
              "  return (await res.json()) as T;",
              "}",
              "",
              "async function request<T>(path: string, init?: RequestInit): Promise<Result<T>> {",
              "  try {",
              "    const res = await fetch(`${BASE}${path}`, {",
              "      headers: { \"Content-Type\": \"application/json\" },",
              "      ...init,",
              "    });",
              "    if (!res.ok) return { ok: false, error: `HTTP ${res.status}` };",
              "    return { ok: true, data: await parse<T>(res) };",
              "  } catch (err) {",
              "    return { ok: false, error: err instanceof Error ? err.message : \"network\" };",
              "  }",
              "}",
              "",
              "export const listTodos = () => request<Todo[]>(\"/todos\");",
              "export const createTodo = (title: string) =>",
              "  request<Todo>(\"/todos\", {",
              "    method: \"POST\",",
              "    body: JSON.stringify({ title, done: false }),",
              "  });",
              "export const patchTodo = (id: number, patch: TodoPatch) =>",
              "  request<Todo>(`/todos/${id}`, { method: \"PATCH\", body: JSON.stringify(patch) });",
              "export const removeTodo = (id: number) =>",
              "  request<null>(`/todos/${id}`, { method: \"DELETE\" });",
              "//            ^^^^ the absence, typed. The old res.json() crash:",
              "//            unrepresentable now, because null has no .json lie.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Typing the DOM — and bug #2 confesses",
        body: [
          "app.ts meets the DOM's honest types. querySelector<T>('#todos') returns HTMLUListElement | null — because the selector might miss, and the old code assumed it wouldn't; the fix is a startup guard (throw new Error('missing #todos') — fail loud at boot, not mysterious at runtime). Event handlers get real parameter types: (event: SubmitEvent) for the form, (event: MouseEvent) for the delegated listener. dataset.id arrives as string | undefined, and Number(undefined) is NaN — bug #2: the original toggle handler called toggleTodo(Number(row.dataset.id)) on ANY click target that bubbled up, and a click on the list padding produced id NaN, which PATCHed the URL /todos/NaN — a request the server answered 404, silently, on every stray click. strictNullChecks plus a missing-row guard kills the whole family.",
          "Note what did NOT change: the createElement/textContent render loop needed exactly one annotation (the todo parameter). DOM code migrates cheaply because the browser's lib.dom.d.ts already knows every element and event — you mostly just stop contradicting it.",
        ],
        code: [
          {
            file: "app.ts — two guards, zero rewrites",
            lang: "ts",
            code: [
              "import { listTodos, createTodo, patchTodo, removeTodo } from \"./api\";",
              "import type { Todo } from \"./types\";",
              "",
              "let todos: Todo[] = [];",
              "",
              "// honest DOM lookups: | null is the truth, so say it once, loudly:",
              "const listEl = document.querySelector<HTMLUListElement>(\"#todos\");",
              "const formEl = document.querySelector<HTMLFormElement>(\"#todo-form\");",
              "if (!listEl || !formEl) throw new Error(\"app scaffolding missing\");",
              "",
              "function render(): void {",
              "  listEl.textContent = \"\";",
              "  for (const todo of todos) { /* unchanged builder: createElement +",
              "     textContent + dataset.id = String(todo.id) — now typed */ }",
              "}",
              "",
              "formEl.addEventListener(\"submit\", async (event: SubmitEvent) => {",
              "  event.preventDefault();",
              "  const data = Object.fromEntries(new FormData(formEl));",
              "  const title = String(data.title ?? \"\").trim();",
              "  if (!title) return;",
              "  const result = await createTodo(title);",
              "  if (result.ok) todos = [...todos, result.data]; // server shape, typed",
              "  render();",
              "});",
              "",
              "listEl.addEventListener(\"click\", async (event: MouseEvent) => {",
              "  const actionEl = (event.target as HTMLElement).closest<HTMLElement>(\"[data-action]\");",
              "  if (!actionEl) return;",
              "  const row = actionEl.closest<HTMLElement>(\"li.todo\");",
              "  const rawId = row?.dataset.id;            // string | undefined — the truth",
              "  if (row == null || rawId == null) return; // bug #2 dies here:",
              "  const id = Number(rawId);                 // no NaN ever reaches the URL",
              "  const action = actionEl.dataset.action;",
              "  if (action === \"toggle\") {",
              "    const current = todos.find((t) => t.id === id);",
              "    if (!current) return;",
              "    const result = await patchTodo(id, { done: !current.done });",
              "    if (result.ok) {",
              "      todos = todos.map((t) => (t.id === id ? result.data : t));",
              "      render();",
              "    }",
              "  } else if (action === \"delete\") {",
              "    const result = await removeTodo(id);",
              "    if (result.ok) {",
              "      todos = todos.filter((t) => t.id !== id);",
              "      render();",
              "    }",
              "  }",
              "});",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Then vs Now — the same app, different physics",
        body: [
          "Put the two versions side by side and the delta is eerie: nearly every line that SURVIVED is character-identical. What changed is what became IMPOSSIBLE. In the JS version: a delete could throw on the 204 parse; a stray click could PATCH /todos/NaN; a renamed field survived in exactly one forgotten callsite until a user found it; a new todo field (say, priority) required auditing every consumer by eye. In the TS version: the 204 absence is in removeTodo's signature; NaN ids are unreachable past the guard; the rename refactor (below) is compiler-driven; adding priority: string to Todo makes every missing consumer a compile error before lunch.",
          "This is the honest sales pitch for the rest of the course: TypeScript doesn't make the app faster, prettier, or shorter. It converts 'hope' — the invisible dependency every JavaScript codebase runs on — into receipts. The Foundation stage closes on this receipt: the same developer, the same app, minus an entire bug class, plus a refactor superpower.",
        ],
      },
      {
        heading: "The payoff — a rename refactor with receipts",
        body: [
          "The ceremony that proves the safety net: rename Todo's done field to completed. In the JavaScript version this is grep-and-pray — grep misses dataset attributes, string-keyed objects, and the server response; you ship and discover the miss through a bug report. In the typed version: edit the interface, run tsc, and the compiler LISTs every affected site — the render builder, the toggle action, the patch payload — each as a numbered error you clear one by one until the list is empty. Empty list = done. Not 'probably done': DONE, because the type system has examined every consumer of the shape, including ones you forgot existed.",
          "Do this refactor now, while the app is small and the receipt is cheap to read. The habit you're installing — make the change, let the compiler enumerate the damage, clear the list — is exactly how Phase 15 evolves Prisma schemas and how Phase 40 refactors production modules. Same loop, bigger stakes.",
        ],
        code: [
          {
            file: "refactor.log — the compiler as moving company",
            lang: "text",
            code: [
              "$ # types.ts: interface Todo { …; completed: boolean }   (was: done)",
              "$ npx tsc --noEmit",
              "",
              "app.ts(19,42): error TS2339: Property 'done' does not exist on type 'Todo'.",
              "  → the render builder's li.classList.toggle call",
              "app.ts(41,58): error TS2339: Property 'done' does not exist on type 'Todo'.",
              "  → the toggle action's patch payload",
              "api.ts(31,52): error TS2353: Object literal may only specify known properties,",
              "  and 'done' does not exist in type 'TodoPatch'.",
              "  → the create payload — the one you would have MISSED in JS",
              "",
              "$ # fix the three sites…",
              "$ npx tsc --noEmit   # (silent)",
              "$ # silent = done. Not 'probably done'. Done.",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The 'while I'm here' rewrite",
      wrong: [
        "// migration, day 2:",
        "// \"while I'm converting app.js, this render loop is ugly —",
        "//  let me restructure it into smaller functions, change the",
        "//  state shape to { byId, allIds }, and swap the event wiring…\"",
        "",
        "// result: 40 files changed, behavior diff UNKNOWN,",
        "// type errors now entangled with logic errors,",
        "// code review impossible, rollback pointless.",
        "// The migration and the refactor now sink together.",
      ].join("\n"),
      right: [
        "// migration = add types, freeze behavior. Every commit passes:",
        "//   1) tsc --noEmit   (fewer errors than yesterday)",
        "//   2) the app works exactly as before (manual or smoke test)",
        "",
        "// The refactor DESIRES — 'ugly render loop', 'better state shape' —",
        "// go into a list. AFTER the migration lands, each gets its OWN",
        "// commit, where the type system (now installed) audits it for free.",
        "// Two small, reviewable changes beat one heroic unreviewable one.",
      ].join("\n"),
      explain:
        "A migration that also refactors destroys its own audit trail: when something breaks, you can't tell whether the TYPES were wrong or the BEHAVIOR changed — so you trust nothing and revert everything. Freeze behavior, land the types, THEN refactor with the compiler as your witness. This is the expand/contract discipline the course applies to databases (Phase 15) and deployments (Phase 38), practiced early where the blast radius is one todo app.",
    },
    tryIt: [
      "Take YOUR Phase 2 Todo app (filters, counter, persistence included) and run the ceremony: strict tsconfig, types.ts first, rename outward. Count the initial errors and track the number down to zero — it must shrink monotonically; if a 'fix' adds errors elsewhere, you changed behavior.",
      "Reproduce bug #1 deliberately: revert parse() to unconditional res.json(), run the app, delete the last todo, and watch the crash the types were preventing. Then restore the fix and feel the difference between hoping and knowing.",
      "Reproduce bug #2: remove the rawId guard and click the empty list padding with the Network tab open. Count the 404s to /todos/NaN. Restore the guard; count again (expected: zero).",
      "Run the done→completed rename refactor end to end. Record how many sites the compiler listed and how many you would have found by grep — include the api.ts payload you'd have missed.",
      "Add a priority: 'low' | 'high' field to Todo (required). Without fixing anything yet, run tsc and read the error list as a map of every consumer. That list IS your refactoring plan.",
    ],
    challenge: {
      prompt:
        "Migrate this working-but-untyped 'weather widget' to strict TypeScript under the same rules (behavior frozen, zero any/!/@ts-ignore). The widget: fetches a forecast, renders today + a 3-day list, has a unit toggle (°C/°F), and caches the last fetch in localStorage.\n\n// weather.js\nconst KEY = 'weather.v1';\nasync function boot() {\n  const cached = JSON.parse(localStorage.getItem(KEY));\n  if (cached) render(cached);\n  const res = await fetch('/api/forecast');\n  const data = await res.json();\n  localStorage.setItem(KEY, JSON.stringify(data));\n  render(data);\n}\nfunction render(data) {\n  document.querySelector('#now').textContent = `${data.today.temp}° ${data.today.summary}`;\n  const list = document.querySelector('#days');\n  list.textContent = '';\n  data.days.forEach((d, i) => {\n    const li = document.createElement('li');\n    li.textContent = `${d.name}: ${d.high}/${d.low}`;\n    list.appendChild(li);\n  });\n}\nlet unit = 'c';\ndocument.querySelector('#unit').addEventListener('click', () => {\n  unit = unit === 'c' ? 'f' : 'c';\n  render(JSON.parse(localStorage.getItem(KEY)));\n});\nboot();",
      hints: [
        "Shape the data first: interface DayForecast { name: string; high: number; low: number } and interface Forecast { today: { temp: number; summary: string }; days: DayForecast[] }. Everything downstream is consequences.",
        "Three lies to confess: JSON.parse returns any (route it through unknown + a isForecast guard, or a load helper); res.json() runs even when !res.ok (gate it); querySelector returns | null (guard at boot). The unit toggle re-parses the cache — what if the cache is corrupt or missing? That's your third 'bug the types find'.",
        "Keep render(data: Forecast) pure-ish and let the toggle path share ONE safe read-cache function. Behavior frozen means the happy path renders identically — only the crashes-on-edges disappear.",
      ],
      solution: [
        "// ——— types.ts ———",
        "export interface DayForecast { name: string; high: number; low: number; }",
        "export interface Forecast {",
        "  today: { temp: number; summary: string };",
        "  days: DayForecast[];",
        "}",
        "",
        "// ——— weather.ts ———",
        "import type { Forecast } from \"./types\";",
        "",
        "const KEY = \"weather.v1\";",
        "",
        "// proof, not optimism: unknown → Forecast only via checks",
        "function isForecast(v: unknown): v is Forecast {",
        "  if (typeof v !== \"object\" || v === null) return false;",
        "  const f = v as Record<string, unknown>;",
        "  const today = f.today as Record<string, unknown> | undefined;",
        "  return (",
        "    typeof today === \"object\" && today !== null &&",
        "    typeof today.temp === \"number\" &&",
        "    typeof today.summary === \"string\" &&",
        "    Array.isArray(f.days)",
        "  );",
        "}",
        "",
        "// bug the types found #1: the cache may be missing OR corrupt.",
        "// The old JSON.parse(null) → crash on first-ever visit's toggle.",
        "function readCache(): Forecast | undefined {",
        "  try {",
        "    const raw = localStorage.getItem(KEY);",
        "    if (raw == null) return undefined;",
        "    const parsed: unknown = JSON.parse(raw);",
        "    return isForecast(parsed) ? parsed : undefined;",
        "  } catch {",
        "    return undefined;",
        "  }",
        "}",
        "",
        "function writeCache(data: Forecast): void {",
        "  try {",
        "    localStorage.setItem(KEY, JSON.stringify(data));",
        "  } catch { /* private mode */ }",
        "}",
        "",
        "// honest DOM: | null stated once, loudly, at the boundary",
        "const nowEl = document.querySelector<HTMLElement>(\"#now\");",
        "const daysEl = document.querySelector<HTMLUListElement>(\"#days\");",
        "const unitEl = document.querySelector<HTMLButtonElement>(\"#unit\");",
        "if (!nowEl || !daysEl || !unitEl) throw new Error(\"widget scaffolding missing\");",
        "",
        "function render(data: Forecast): void {",
        "  nowEl.textContent = `${data.today.temp}° ${data.today.summary}`;",
        "  daysEl.textContent = \"\";",
        "  for (const d of data.days) {",
        "    const li = document.createElement(\"li\");",
        "    li.textContent = `${d.name}: ${d.high}/${d.low}`;",
        "    daysEl.appendChild(li);",
        "  }",
        "}",
        "",
        "// bug the types found #2: res.json() ran on error responses too —",
        "// a 500 with an HTML body threw mid-boot with no state to show.",
        "async function boot(): Promise<void> {",
        "  const cached = readCache();",
        "  if (cached) render(cached);",
        "  try {",
        "    const res = await fetch(\"/api/forecast\");",
        "    if (!res.ok) return;               // stale cache (if any) stays up",
        "    const data = (await res.json()) as Forecast; // trust seam (Phase 20",
        "    if (!isForecast(data)) return;     // replaces this cast with proof)",
        "    writeCache(data);",
        "    render(data);",
        "  } catch {",
        "    /* offline: cached render already showing — nothing to do */",
        "  }",
        "}",
        "",
        "let unit: \"c\" | \"f\" = \"c\";   // a union, because those are the only two",
        "unitEl.addEventListener(\"click\", () => {",
        "  unit = unit === \"c\" ? \"f\" : \"c\";",
        "  const cached = readCache();",
        "  if (cached) render(cached);          // corrupt/absent cache: no crash",
        "});",
        "",
        "void boot();",
        "",
        "/* acceptance: happy path renders identically (behavior frozen).",
        "   First-ever-visit toggle no longer throws; a 500 no longer",
        "   bricks boot; a hand-corrupted localStorage key degrades to",
        "   'no cache' instead of a white screen. Zero any / ! / ignores. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "The first rule of an incremental migration is…",
        options: ["Behavior is frozen — no refactors or features until the types land", "Rewrite each file cleanly while converting it", "Migrate under a loose config, tighten later", "Convert the UI layer first"],
        answer: 0,
        explain: "A migration that also refactors destroys its audit trail — you can't separate type fixes from behavior changes. Land the types, THEN refactor with the compiler as witness.",
      },
      {
        q: "Types should be defined…",
        options: ["Before the layers that use them — data shapes first, then producers, then consumers", "In the UI layer, where they're rendered", "After everything compiles loosely", "Only in the server code"],
        answer: 0,
        explain: "types.ts (Todo, Result<T>) → api.ts → app.ts. Types flow outward; by the time consumers compile, their inputs already have names and the error count shrinks monotonically.",
      },
      {
        q: "Bug #1 (the 204 crash) was invisible to JavaScript because…",
        options: ["The code LIED — 'every response has a body' — and only an empty-body edge exposed the lie", "204 responses don't exist", "res.json() handles empty bodies", "DELETE requests never fail"],
        answer: 0,
        explain: "removeTodo's signature now carries the absence: Promise<Result<null>> with a 204-aware parse. The compiler surfaced a contractual lie; the types keep it surfaced.",
      },
      {
        q: "Bug #2 (NaN ids) died because…",
        options: ["dataset.id became string | undefined, and the guard made NaN unreachable before the URL was built", "NaN was removed from JavaScript", "closest() now throws", "The server learned to reject NaN"],
        answer: 0,
        explain: "strictNullChecks forced the truth (row and dataset.id can be absent) into a guard. The PATCH /todos/NaN 404-spam simply cannot be expressed anymore.",
      },
      {
        q: "The done→completed rename proves the safety net because…",
        options: ["An empty tsc run means EVERY consumer was checked — including ones you'd forgotten", "grep finds the same sites", "The app is too small to miss anything", "Renames are always safe"],
        answer: 0,
        explain: "The compiler enumerates every consumer of the shape — including the api.ts payload grep would miss. Silent tsc = done, not 'probably done'. That receipt is the whole payoff.",
      },
      {
        q: "After this migration, the honest claim about the typed app is…",
        options: ["Same behavior, fewer representable bug classes, and compiler-audited refactors", "It runs faster", "It has more features", "It no longer needs tests"],
        answer: 0,
        explain: "TypeScript converts hope into receipts: crashes-on-edges become unrepresentable, and every future change gets an enumerated impact list. It changes nothing about speed or features — and tests still matter (Phase 31).",
      },
    ],
    flashcards: [
      { front: "Incremental migration", back: "Freeze behavior, strict from line one, one file at a time outward from data shapes. The error list is a monotonic work queue; every fix is permanent." },
      { front: "The compiler as interviewer", back: "It doesn't rewrite your app — it asks what's true: Can this be null? Does every response have a body? You answer with types; two hidden bugs confess." },
      { front: "Types-first ordering", back: "types.ts (contract) → api.ts (producer) → app.ts (consumer). By the time the UI compiles, everything it touches already has a name." },
      { front: "The 204 lesson", back: "DELETE answers with no body; parsing it throws. Model the absence in the signature (Result<null>) so the lie 'every response has a body' can't be written." },
      { front: "dataset honesty", back: "dataset.x is string | undefined. Number(undefined) is NaN. Guard the absence and NaN never reaches a URL — bug classes die at the guard." },
      { front: "Rename-with-receipts", back: "Change the interface → tsc lists every consumer → clear the list → silence means done. grep-and-pray is the untyped version of the same wish." },
      { front: "Behavior freeze", back: "Migrations and refactors are separate commits. Together they destroy the audit trail; apart, the type system audits the second one for free." },
      { front: "Then-vs-Now delta", back: "Same app, nearly identical lines — but whole bug classes unrepresentable and refactors compiler-audited. TypeScript converts hope into receipts." },
    ],
    recap: [
      "Migration is an interview: strict from day one, behavior frozen, types flowing outward from the data contract.",
      "The compiler surfaced two real bugs — a 204 parse crash and NaN ids — that JavaScript hid by never asking.",
      "DOM typing is cheap: the lib already knows everything; you mostly stop contradicting it with guards at the edges.",
      "The rename refactor's empty tsc run is a receipt grep can never write.",
      "Foundation stage, sealed: the same developer, the same app, minus a bug class. Phase 4 awaits — React automates the render loop you built by hand.",
    ],
    references: [
      { label: "TypeScript — Migration guide (from JavaScript)", url: "https://www.typescriptlang.org/docs/handbook/migrating-from-javascript.html" },
      { label: "TypeScript Handbook — The DOM lib", url: "https://www.typescriptlang.org/docs/handbook/dom-manipulation.html" },
      { label: "Matt Pocock — Total TypeScript migration workshop", url: "https://www.totaltypescript.com/workshops/real-world-typescript" },
      { label: "TypeScript — tsc CLI reference", url: "https://www.typescriptlang.org/docs/handbook/compiler-options.html" },
      { label: "web.dev — TypeScript best practices", url: "https://web.dev/articles/typescript-best-practices" },
    ],
    nextBridge:
      "The Foundation stage is complete: web, HTML/CSS, JavaScript, and a strict TypeScript safety net — proven on a real migration. Everything from here builds on it. Phase 4 opens with the question your vanilla app answered by hand — 'who re-renders when state changes?' — and React's answer: nobody. You describe; it reconciles. Declarative UI begins.",
  },
];

export const LESSON_CONTENT_P3B: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P3B.map((l) => [l.id, l]),
);
