import type { LessonContent } from "./types";

/**
 * Pass 014 bounded content batch: Phase 5 continuation (L3–L4).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P5B: LessonContent[] = [
  {
    id: "p05-l3",
    phaseId: "p05",
    title: "useRef, useContext & useReducer",
    level: "React Developer",
    minutes: 40,
    summary:
      "Three hooks complete the state-management core. useRef is the render's private memory — a mutable box that survives re-renders without causing them (DOM handles, timer ids, previous values). useContext replaces prop chains for values the whole subtree needs (theme, auth, locale) — with honest limits. useReducer replaces tangled webs of setX calls with a typed action vocabulary and one pure transition function, reuniting with Phase 3's discriminated unions. Together they answer: what must not render, what must skip the chain, and what has outgrown useState.",
    prerequisites: ["p05-l2 — effects and cleanup", "p03-l2 — discriminated unions (for typed reducer actions)"],
    objectives: [
      "Distinguish the two ref jobs — DOM handles and mutable values that must NOT trigger renders — and know what refs must never hold (render-relevant data).",
      "Implement the previous-value pattern and the latest-callback pattern with refs.",
      "Wire context end-to-end (createContext, Provider, useContext, default values) and state its two fitness tests.",
      "Avoid context misuse: high-churn data, everything-bagel providers, and splits that re-render the tree.",
      "Model a complex component as a reducer: a discriminated-union Action type, a pure reducer, and exhaustive transitions.",
      "Choose deliberately among useState, useReducer, context, and refs for any given value.",
    ],
    simple:
      "Every value in a component answers one question: 'who needs to know when this changes?' State says 'the render needs to know' — changing it repaints. A ref says 'nobody needs to know' — it's the notebook you scribble in between renders: the interval's id, the input's element, what the previous value was. Context says 'many descendants need to know, and threading it through every hand is silly' — the building's announcement system instead of door-to-door whispers. A reducer says 'this value changes in many interlocking ways, so let's name the changes' — instead of twenty setX calls, one dispatch({ type: 'add-item', … }) handled by a single pure function. Same component, four kinds of information, four honest homes.",
    why:
      "These three are the difference between Phase 4 apps that squeak and Phase 9 apps that scale. Without refs, you'll store timer ids in state (phantom re-renders) or read DOM nodes by querySelector (fighting React's ownership). Without context judgment, you'll either prop-drill auth through eleven layers or dump everything into one provider and wonder why every keystroke re-renders the tree. Without reducers, a cart or a wizard becomes 'state spaghetti' — six useState calls that must change in lockstep and drift when they don't. And the reducer's typed actions are where Phase 3's discriminated unions finally pay rent: impossible transitions become unrepresentable. The Boss Battle (BB-1) will hand you a buggy component and ask which of the four homes each value belongs in — this lesson is the training ground.",
    mentalModel: {
      title: "The notebook, the intercom, the rulebook",
      body: "State is the scoreboard: change it and the whole arena looks up. A ref is the referee's notebook — scribbled constantly, read on demand, and nobody in the stands notices a page turn (DOM handle, timer id, previous value). Context is the intercom: say 'theme: dark' once at the control room (Provider) and every room that has a speaker (useContext) hears it — but every announcement makes every listening room look up, so use it for rare, widely-needed facts, not a running commentary. The reducer is the rulebook: instead of players improvising ('setScore, setFouls, setClock, setPhase…'), every play is a named move ('score-three', 'foul-out') resolved by one official function — reviewable, testable, and unable to break the rules it doesn't contain.",
    },
    sections: [
      {
        heading: "useRef — the render's private notebook",
        body: [
          "A ref is { current: T } that survives every re-render, mutable at will, and — the whole point — writing to it NEVER triggers a render. Two legitimate jobs. DOM refs: pass ref={inputRef} to an element and React assigns the live node after commit — focus(), select(), measurement. Value refs: anything you must remember across renders without repainting — an interval id, a mounted flag, the previous value, a latest-callback for effects.",
          "The law: ref.current must never hold something the render reads to decide what to show — that's state's job, and hiding it in a ref yields the classic 'the UI didn't update' bug. Conversely, state must never hold what only effects/handlers need (timer ids) — that's the phantom re-render. The boundary test: does the next paint depend on it? Yes → state. No → ref. Two patterns earn their keep immediately: previous-value (render writes prevRef.current = value AFTER computing with the old one) and latest-callback (an effect reads handlerRef.current so it never resubscribes).",
        ],
        code: [
          {
            file: "refs.tsx — two jobs, one law",
            lang: "tsx",
            code: [
              "function Timer() {",
              "  const [seconds, setSeconds] = useState(0);      // render-relevant → state",
              "  const intervalRef = useRef<number | null>(null); // housekeeping → ref",
              "  const prevSeconds = useRef(seconds);",
              "",
              "  useEffect(() => {",
              "    intervalRef.current = window.setInterval(",
              "      () => setSeconds((s) => s + 1), 1000);",
              "    return () => {",
              "      if (intervalRef.current != null) clearInterval(intervalRef.current);",
              "    };",
              "  }, []);",
              "",
              "  // previous-value pattern: read old, THEN update the notebook",
              "  const delta = seconds - prevSeconds.current;",
              "  useEffect(() => { prevSeconds.current = seconds; });",
              "",
              "  return (",
              "    <p>",
              "      {seconds}s (Δ{delta})",
              "      {/* ✗ const [id, setId] = useState(0) for the interval id would",
              "          re-render every second for a value no paint needs */}",
              "    </p>",
              "  );",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "useContext — the intercom, not the plumbing",
        body: [
          "The ceremony, all four parts: createContext(default) makes the channel; <Ctx.Provider value={…}> speaks from some ancestor; useContext(Ctx) listens from any descendant — no props in between; the default applies when no Provider exists above (and powers testing/standalone use). The type flows: createContext<Theme>(\"dark\") makes every useContext call typed.",
          "Context passes two fitness tests or it shouldn't be context. Test one — breadth: does a genuinely wide subtree need this (theme, current user, locale, router)? A value used by two siblings is a prop, not a context. Test two — churn: does it change rarely relative to renders? Context re-renders EVERY consumer when the value's identity changes — a provider passing a fresh object every render ({ user, setUser }) re-renders the world each keystroke; memoize the value object or split high-churn slices into separate contexts. The anti-pattern to recognize on sight: the EverythingProvider — one context carrying theme AND user AND cart AND toasts. Split by change frequency; collocate providers where they're actually needed.",
        ],
        code: [
          {
            file: "theme-context.tsx — the full ceremony, typed",
            lang: "tsx",
            code: [
              "type Theme = \"dark\" | \"light\";",
              "interface ThemeCtx { theme: Theme; toggle: () => void; }",
              "",
              "// the channel, with a default (used when no Provider is above):",
              "const ThemeContext = createContext<ThemeCtx>({",
              "  theme: \"dark\",",
              "  toggle: () => {}, // no-op default; real toggle comes from the Provider",
              "});",
              "",
              "export function ThemeProvider({ children }: { children: ReactNode }) {",
              "  const [theme, setTheme] = useState<Theme>(\"dark\");",
              "  const value = useMemo<ThemeCtx>(        // stable identity — consumers",
              "    () => ({ theme, toggle: () => setTheme((t) => (t === \"dark\" ? \"light\" : \"dark\")) }),",
              "    [theme],                              // only re-render when theme flips",
              "  );",
              "  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;",
              "}",
              "",
              "export function ThemeToggle() {",
              "  const { theme, toggle } = useContext(ThemeContext); // no props, any depth",
              "  return <button onClick={toggle}>now: {theme}</button>;",
              "}",
              "",
              "export const useTheme = () => useContext(ThemeContext); // hook wrapper:",
              "// a single import point + a place to error if used outside the Provider",
            ].join("\n"),
            caption: "The useMemo on the value object is the churn fix — without it, every ThemeProvider render hands consumers a new identity and re-renders them all.",
          },
        ],
      },
      {
        heading: "useReducer — the rulebook for tangled transitions",
        body: [
          "The trigger to reach for useReducer is not 'many states' but 'many INTERLOCKING updates': a form where submit touches status + error + touched, a cart where checkout touches items + total + phase. useReducer concentrates every transition in one pure function — (state, action) => state — called via dispatch(action). Benefits compound: impossible states become unrepresentable (a single status union replaces five booleans), transitions are unit-testable without React (the reducer is a plain function), and the action log is a free audit trail.",
          "Actions are Phase 3's discriminated unions wearing React clothes: type Action = { type: 'add'; title: string } | { type: 'toggle'; id: number } | …. The reducer narrows on action.type with an exhaustive switch and an assertNever default — add an action variant and every reducer that must handle it breaks at compile time. Purity law: the reducer may compute the next state from (state, action) and nothing else — no Date.now(), no Math.random(), no side effects (the random seed arrives IN the action, dispatched by the handler). This is the same purity contract as useState updaters, enforced architecturally.",
        ],
        code: [
          {
            file: "cart-reducer.tsx — unions meet React",
            lang: "tsx",
            code: [
              "interface CartItem { id: number; title: string; qty: number; }",
              "interface CartState { items: CartItem[]; phase: \"open\" | \"checking-out\" | \"done\"; }",
              "",
              "// the vocabulary — a discriminated union (P03-L2, paying rent):",
              "type CartAction =",
              "  | { type: \"add\"; item: CartItem }",
              "  | { type: \"remove\"; id: number }",
              "  | { type: \"checkout\" }",
              "  | { type: \"reset\" };",
              "",
              "function cartReducer(state: CartState, action: CartAction): CartState {",
              "  switch (action.type) {",
              "    case \"add\":",
              "      if (state.phase !== \"open\") return state;      // guarded transition",
              "      return { ...state, items: [...state.items, action.item] };",
              "    case \"remove\":",
              "      return { ...state, items: state.items.filter((i) => i.id !== action.id) };",
              "    case \"checkout\":",
              "      if (state.items.length === 0) return state;    // rules live HERE, once",
              "      return { ...state, phase: \"checking-out\" };",
              "    case \"reset\":",
              "      return { items: [], phase: \"open\" };",
              "    default:",
              "      return assertNever(action);   // new action variant → compile error",
              "  }",
              "}",
              "",
              "function Cart() {",
              "  const [state, dispatch] = useReducer(cartReducer, { items: [], phase: \"open\" });",
              "  return (",
              "    <button onClick={() => dispatch({ type: \"checkout\" })}>",
              "      checkout ({state.items.length})",
              "    </button>",
              "  );",
              "}",
              "",
              "// unit-testable with zero React: cartReducer(prev, { type: \"add\", … })",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Choosing the home — the four-question audit",
        body: [
          "Every value in a component gets an audit, in order. Question one — is it constant? Module scope, not a hook at all. Question two — is it derivable from other state/props? Compute it during render; no hook. Question three — does the paint depend on it? Yes → useState (or a reducer field if it transitions interlockingly). No → question four — must it survive renders for effects/handlers (timer id, previous value, DOM node)? useRef. Values needed by a wide subtree with low churn graduate to context — and the STATE itself usually stays where it is; context carries the value plus a setter down, it doesn't relocate ownership.",
          "Two failure modes bookend the spectrum. Under-modeling: one giant state object plus manual sync code ('when X changes I also set Y in this handler…') — the reducer's cue. Over-modeling: context for two siblings, reducer for a toggle, refs for render data — ceremony without payoff. The audit exists so you can defend each choice in code review; 'it works' is not a home assignment. Write the audit as comments when you build the Phase 9 shells — future-you is the reviewer.",
        ],
        code: [
          {
            file: "audit.tsx — one component, four homes",
            lang: "tsx",
            code: [
              "const PAGE_SIZE = 20;                    // constant → module scope",
              "",
              "function IssueBoard({ projectId }: { projectId: string }) {",
              "  // paint-relevant, interlocking (filter affects page, page resets on",
              "  // filter change) → reducer:",
              "  const [board, dispatch] = useReducer(boardReducer, initialBoard);",
              "",
              "  // paint-relevant, independent → plain state:",
              "  const [title, setTitle] = useState(\"\");",
              "",
              "  // derivable → compute, don't store:",
              "  const visibleCount = board.issues.filter(board.matchesFilter).length;",
              "",
              "  // survives renders, no paint depends on it → ref:",
              "  const searchInput = useRef<HTMLInputElement>(null);",
              "  const lastProjectId = useRef(projectId);",
              "",
              "  // wide + low-churn → context (provided higher up):",
              "  const { user } = useAuth();",
              "  return <section>{/* … */}</section>;",
              "}",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The EverythingProvider (and its re-render tax)",
      wrong: [
        "// app-context.tsx — one context to rule them all:",
        "const AppCtx = createContext<AppShape>(null!);",
        "",
        "export function AppProvider({ children }: { children: ReactNode }) {",
        "  const [user, setUser] = useState<User | null>(null);",
        "  const [theme, setTheme] = useState(\"dark\");",
        "  const [cart, setCart] = useState<CartItem[]>([]);   // changes on",
        "  const [toasts, setToasts] = useState<Toast[]>([]);  // EVERY action…",
        "  return (",
        "    <AppCtx.Provider value={{ user, setUser, theme, setTheme, cart, setCart, toasts, setToasts }}>",
        "      {children}   {/* ✗ fresh object identity every render →",
        "    </AppCtx.Provider>   every consumer re-renders on EVERY change,",
        "  );                        even ones that only read `theme`. */",
        "}",
      ].join("\n"),
      right: [
        "// Split by CHANGE FREQUENCY; memoize each value:",
        "export function ThemeProvider({ children }: { children: ReactNode }) {",
        "  const [theme, setTheme] = useState<Theme>(\"dark\");",
        "  const value = useMemo(() => ({ theme, setTheme }), [theme]);",
        "  return <ThemeCtx.Provider value={value}>{children}</ThemeCtx.Provider>;",
        "}",
        "// CartProvider, ToastProvider, AuthProvider — separate contexts,",
        "// separate identities: a toast no longer re-renders the theme",
        "// consumers. Collocate providers where the subtree actually needs them.",
        "",
        "// High-churn values (keystroke-scale) often shouldn't be context",
        "// at all — keep them local, or reach for Phase 19's server-state tools.",
      ].join("\n"),
      explain:
        "Context re-renders every consumer whenever the value's identity changes. One mega-object changes identity on every provider render — so adding a toast repaints components that only read the theme. The fix is architectural, not tactical: split contexts by change frequency, memoize the value objects, and keep high-churn data out of context entirely. Context is an intercom for rare announcements — a running commentary through it taxes every listener.",
      },
    tryIt: [
      "Build the Timer from section one and add a Pause button using intervalRef (clearInterval + null). Confirm pausing causes ZERO re-renders (log in the component body).",
      "Implement usePrevious<T>(value: T): T as a custom hook using the previous-value pattern. Test it with a counter: the UI should show current and previous side by side.",
      "Thread a prop through four components, then replace the chain with context. Count the files that changed (expected: provider wrapper + leaf) — that diff IS the value of context.",
      "Take the cart reducer and add a { type: 'apply-coupon'; pct: number } action WITHOUT touching the reducer. The compiler should reject the dispatch — that error is the discriminated union earning its keep. Then implement the case with an exhaustiveness-checked switch.",
      "Audit one of your Phase 4 components with the four-question audit. Write the home assignment as a comment next to each value. Move at least one misplaced value and feel the render count drop.",
    ],
    challenge: {
      prompt:
        "Build a <Wizard> component for a three-step signup (account → profile → review) using ALL FOUR homes deliberately, with the audit written as comments. Requirements: (1) step + per-step data + overall status modeled in ONE useReducer with a discriminated Action union ({ type: 'next' } | { type: 'back' } | { type: 'field'; step: 1|2; name: string; value: string } | { type: 'submit' }), and transitions guarded so 'next' from an invalid step is a no-op in the REDUCER, not the UI; (2) a per-step <input> that autofocuses on step change via a DOM ref (ref + effect, with cleanup); (3) a previousStep display using a previous-value ref; (4) a StepIndicator consuming step from a WizardContext (Provider wraps the tree, value memoized); (5) a submit handler that dispatches 'submit' and — because reducers are pure — performs the async save OUTSIDE the reducer. Predict, then verify: which actions cause re-renders, which ref writes cause zero.",
      hints: [
        "The reducer owns legality: next only advances when state.step < 3 AND the current step's required fields are non-empty — read those from state.fields. The UI disabling the button is courtesy; the reducer's no-op is the law.",
        "Autofocus pattern: const inputRef = useRef<HTMLInputElement>(null); useEffect(() => { inputRef.current?.focus(); }, [state.step]); — the dep is the step, not the ref. Cleanup isn't needed for focus, and that's fine — say why in a comment (nothing to release).",
        "The async split: the handler awaits saveAccount(...), THEN dispatches({ type: 'submit-result', ok }) — add that action to the union. Randomness/dates belong in the action payload or the handler, never the reducer.",
      ],
      solution: [
        "import {",
        "  createContext, useContext, useEffect, useMemo, useReducer, useRef, useState,",
        "  type ReactNode,",
        "} from \"react\";",
        "",
        "// ——— the rulebook: one state shape, one action vocabulary ———",
        "interface WizardState {",
        "  step: 1 | 2 | 3;",
        "  fields: { email: string; name: string };",
        "  status: \"editing\" | \"submitting\" | \"done\" | \"error\";",
        "}",
        "",
        "type WizardAction =",
        "  | { type: \"field\"; name: \"email\" | \"name\"; value: string }",
        "  | { type: \"next\" }",
        "  | { type: \"back\" }",
        "  | { type: \"submit\" }              // sets submitting; the HANDLER does I/O",
        "  | { type: \"submit-result\"; ok: boolean };",
        "",
        "const stepValid = (s: WizardState) =>",
        "  s.step === 1 ? s.fields.email.includes(\"@\") : s.fields.name.trim().length > 0;",
        "",
        "function wizardReducer(state: WizardState, action: WizardAction): WizardState {",
        "  switch (action.type) {",
        "    case \"field\":",
        "      return { ...state, fields: { ...state.fields, [action.name]: action.value } };",
        "    case \"next\":",
        "      // the law lives here: invalid step → no-op, whoever asked:",
        "      return state.step < 3 && stepValid(state)",
        "        ? { ...state, step: (state.step + 1) as 1 | 2 | 3 }",
        "        : state;",
        "    case \"back\":",
        "      return state.step > 1 ? { ...state, step: (state.step - 1) as 1 | 2 | 3 } : state;",
        "    case \"submit\":",
        "      return state.step === 3 ? { ...state, status: \"submitting\" } : state;",
        "    case \"submit-result\":",
        "      return { ...state, status: action.ok ? \"done\" : \"error\" };",
        "    default:",
        "      return assertNever(action);   // exhaustiveness: new actions break the build",
        "  }",
        "}",
        "",
        "// ——— the intercom: step, widely needed, low-churn ———",
        "const StepContext = createContext<{ step: number }>({ step: 1 });",
        "const useStep = () => useContext(StepContext);",
        "",
        "function StepIndicator() {",
        "  const { step } = useStep();        // reads context — no props drilled",
        "  return <ol aria-label=\"Steps\">{[1, 2, 3].map((n) => (",
        "    <li key={n} aria-current={n === step ? \"step\" : undefined}>{n}</li>",
        "  ))}</ol>;",
        "}",
        "",
        "export function Wizard() {",
        "  // HOME: reducer — interlocking transitions (step + fields + status)",
        "  const [state, dispatch] = useReducer(wizardReducer, {",
        "    step: 1, fields: { email: \"\", name: \"\" }, status: \"editing\",",
        "  });",
        "",
        "  // HOME: context value — memoized so consumers re-render only on step change",
        "  const stepCtx = useMemo(() => ({ step: state.step }), [state.step]);",
        "",
        "  // HOME: DOM ref — autofocus needs the live node, no paint depends on it",
        "  const inputRef = useRef<HTMLInputElement>(null);",
        "  useEffect(() => {",
        "    inputRef.current?.focus();       // no cleanup: focus has nothing to release",
        "  }, [state.step]);",
        "",
        "  // HOME: value ref — previous step for display; writing causes no render",
        "  const prevStep = useRef(state.step);",
        "  useEffect(() => { prevStep.current = state.step; });",
        "",
        "  // async lives OUTSIDE the pure reducer:",
        "  async function onSubmit() {",
        "    dispatch({ type: \"submit\" });",
        "    const ok = await saveSignup(state.fields);   // handler does I/O…",
        "    dispatch({ type: \"submit-result\", ok });     // …reducer records it",
        "  }",
        "",
        "  const field = state.step === 1 ? \"email\" : \"name\";",
        "  return (",
        "    <StepContext.Provider value={stepCtx}>",
        "      <StepIndicator />",
        "      <p>Came from step {prevStep.current}</p>",
        "      <input",
        "        ref={inputRef}",
        "        aria-label={field}",
        "        value={state.fields[field]}",
        "        onChange={(e) => dispatch({ type: \"field\", name: field, value: e.target.value })}",
        "      />",
        "      <button onClick={() => dispatch({ type: \"back\" })} disabled={state.step === 1}>back</button>",
        "      {state.step < 3",
        "        ? <button onClick={() => dispatch({ type: \"next\" })}>next</button>",
        "        : <button onClick={onSubmit} disabled={state.status === \"submitting\"}>",
        "            {state.status === \"submitting\" ? \"saving…\" : \"finish\"}",
        "          </button>}",
        "      {state.status === \"done\" && <p role=\"status\">Welcome aboard.</p>}",
        "      {state.status === \"error\" && <p role=\"alert\">Could not save — try again.</p>}",
        "    </StepContext.Provider>",
        "  );",
        "}",
        "",
        "declare function saveSignup(fields: WizardState[\"fields\"]): Promise<boolean>;",
        "",
        "/* Render audit — predict, then verify:",
        "   dispatch('field')        → re-render (state changed)",
        "   dispatch('next') invalid → NO re-render (reducer returned same state —",
        "                              React bails out on identical references!)",
        "   inputRef.current?.focus()→ zero re-renders (ref write)",
        "   prevStep.current = …     → zero re-renders (ref write)",
        "   step change              → StepIndicator re-renders via memoized context value */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "Writing to ref.current…",
        options: ["Never triggers a re-render — that's the point of refs", "Triggers a re-render like setState", "Triggers an effect", "Throws in Strict Mode"],
        answer: 0,
        explain: "Refs are mutable boxes outside the render cycle. If the next paint must reflect a value, that value is state — hiding it in a ref is the 'UI didn't update' bug.",
      },
      {
        q: "The previous-value pattern works by…",
        options: ["Reading prevRef.current during render, then updating it in an effect afterward", "Calling setState twice", "Using useLayoutEffect synchronously", "Storing history in context"],
        answer: 0,
        explain: "The render computes against the old notebook value; an effect updates the notebook after commit. The ref survives renders without causing them.",
      },
      {
        q: "Context re-renders a consumer when…",
        options: ["The provider's value changes identity (Object.is) — regardless of which field the consumer reads", "Any ancestor renders", "The consumer's props change", "Only on mount"],
        answer: 0,
        explain: "No field-level subscription: a new value object republishes everything. Hence memoized values and contexts split by change frequency.",
      },
      {
        q: "The EverythingProvider anti-pattern is cured by…",
        options: ["Splitting contexts by change frequency and memoizing each value object", "Wrapping consumers in React.memo alone", "Using useLayoutEffect", "Passing everything as props instead"],
        answer: 0,
        explain: "One mega-context republishes on every included change, taxing all consumers. Separate providers with stable identities scope the re-renders to interested subtrees.",
      },
      {
        q: "useReducer earns its place when…",
        options: ["Updates interlock — many fields must change together under shared rules", "There is exactly one boolean", "The component renders slowly", "Props are deeply drilled"],
        answer: 0,
        explain: "Interlocking transitions + named actions + one pure function: impossible states become unrepresentable and the logic unit-tests without React. A lone toggle stays a useState.",
      },
      {
        q: "A reducer must be pure, so randomness and dates…",
        options: ["Arrive in the action payload or are computed in the handler before dispatch", "Are forbidden entirely", "Use Math.random inside carefully", "Are read from refs inside the reducer"],
        answer: 0,
        explain: "Same contract as useState updaters: (state, action) → state, nothing else read or touched. Impure reducers break replay, testing, and Strict Mode's double-invocation.",
      },
    ],
    flashcards: [
      { front: "useRef", back: "{ current: T } that survives renders; writing never re-renders. Jobs: DOM handles + effect/handler housekeeping. Never render-relevant data." },
      { front: "Ref vs state test", back: "Does the next paint depend on it? Yes → state. No, but it must survive renders → ref. The 'UI didn't update' bug is render data hidden in a ref." },
      { front: "Previous-value pattern", back: "Compute against prevRef.current during render; update it in an effect after commit. The notebook updates one page behind the scoreboard." },
      { front: "Context ceremony", back: "createContext(default) → <Provider value> → useContext(Ctx). The default powers standalone/test use; wrap in a useX hook for one import point." },
      { front: "Context fitness tests", back: "Breadth: a wide subtree genuinely needs it. Churn: it changes rarely. Fail either → props or local state. Split providers by change frequency." },
      { front: "EverythingProvider", back: "One context carrying unrelated values: every change re-renders every consumer. Cure: split by frequency, memoize value objects, collocate providers." },
      { front: "useReducer", back: "(state, action) => state + dispatch. For interlocking updates: one pure transition function, a typed action vocabulary, impossible states unrepresentable." },
      { front: "Typed actions", back: "Actions are discriminated unions; the reducer narrows on type with an exhaustiveness-checked default. New variants break every stale reducer at compile time." },
    ],
    recap: [
      "Refs: the notebook — DOM handles and housekeeping, never paint-relevant data.",
      "Context: the intercom — wide reach, low churn, memoized values, split by frequency.",
      "Reducers: the rulebook — interlocking transitions, typed actions, pure by law.",
      "The four-question audit assigns every value a defensible home.",
      "Phase 3's unions and Phase 4's state now compose: the Boss Battle's vocabulary is installed.",
    ],
    references: [
      { label: "react.dev — Referencing Values with Refs", url: "https://react.dev/learn/referencing-values-with-refs" },
      { label: "react.dev — Manipulating the DOM with Refs", url: "https://react.dev/learn/manipulating-the-dom-with-refs" },
      { label: "react.dev — Passing Data Deeply with Context", url: "https://react.dev/learn/passing-data-deeply-with-context" },
      { label: "react.dev — Extracting State Logic into a Reducer", url: "https://react.dev/learn/extracting-state-logic-into-a-reducer" },
      { label: "react.dev — useReducer", url: "https://react.dev/reference/react/useReducer" },
    ],
    nextBridge:
      "The four homes are assigned — but one question remains: when is doing LESS work the right move? Next lesson covers useMemo, useCallback, and useId: memoization as a measured optimization (not decoration), identity stability for memoized children, and the React Compiler's arrival on the scene — plus the honest 'don't memoize yet' default.",
  },
  {
    id: "p05-l4",
    phaseId: "p05",
    title: "useMemo, useCallback & useId",
    level: "React Developer",
    minutes: 35,
    summary:
      "Memoization is a trade, not a virtue: you spend memory and per-render comparison work to skip recomputation — and it only pays when the skipped work outweighs the overhead AND something downstream cares about the cached identity. This lesson installs the measurement-first discipline: when useMemo earns its keep (expensive computation, stable identities for deps/props), when useCallback matters at all (memoized children, effect deps — not 'stability' for its own sake), when both are noise, how useId gives SSR-safe unique ids for accessible wiring, and where the React Compiler is taking all of this.",
    prerequisites: ["p05-l3 — refs, context, reducers", "p04-l7 — reconciliation and what causes renders"],
    objectives: [
      "State the memoization trade precisely: comparison cost + memory vs skipped recomputation — and when it's negative.",
      "Apply useMemo to genuinely expensive computation and verify the win with render instrumentation.",
      "Explain identity stability as the REAL reason for useCallback — memoized children and effect deps — and reject ceremonial wrapping.",
      "Recognize premature memoization on sight and know the profiling workflow that replaces guessing.",
      "Use useId for accessible label/aria wiring (never keys, never styles) and explain why counter-based ids fail under SSR.",
      "Position the React Compiler: automatic memoization is arriving — which changes your defaults, not your understanding.",
    ],
    simple:
      "Every render, React asks your component for a fresh description — and by default it recomputes everything inside, every time. Usually that's correct: most render work is cheap, and React's job is to diff, not to hoard. Memoization is a deliberate exception: 'this computation is expensive (or its identity matters to someone), so cache the result and only redo it when these inputs change.' useMemo caches a VALUE; useCallback caches a FUNCTION (which is just a value that runs). The trap is treating them as hygiene — sprinkling them everywhere adds bookkeeping to every render and hides the real question, which is always 'did we MEASURE a problem?' If you can't name the expensive work or the identity consumer, the memo is decoration, and decoration has a maintenance cost.",
    why:
      "Two career bugs live here. Under-memoizing: a 5,000-row filter recomputed on every keystroke, a fresh array identity defeating a child's memo, an inline options object re-triggering an effect every render (you met that one in P05-L2 — useMemo is its cure). Over-memoizing: components wrapped in so many useMemo/useCallback that the comparison overhead exceeds the saved work, dependencies lying by omission, and juniors taught that 'stable identities' are a moral duty rather than a contract with a specific consumer. The professionals in this course measure first (render counters, the Profiler) and memoize second — and they know why the React Compiler (verified stable per the Version Matrix before reliance) is shifting the default toward 'don't hand-memoize unless profiling says so.' Understanding BOTH failure modes is what separates Phase 32 performance work from folklore.",
    mentalModel: {
      title: "The prep kitchen",
      body: "A restaurant could chop every vegetable from scratch per order — fine for a taco stand, ruin for a banquet hall. useMemo is the prep kitchen: dice the onions once, refrigerate, reuse while the order ticket (deps) is unchanged. But prep has costs — fridge space (memory), labeling and checking every container (comparison per render), and staleness risk if the label lies (wrong deps). So a good kitchen preps ONLY the slow, high-volume items and cooks the quick things to order. useCallback is prep for a specific reason: the expo (a memoized child) refuses any dish whose ticket looks different, so you keep the ticket (function identity) identical across services. And when the React Compiler arrives, it's like a prep robot that watches the whole kitchen and automates the worthwhile prep — you still need to know what's worth prepping, but you stop doing it by hand.",
    },
    sections: [
      {
        heading: "useMemo — cache the expensive, not the obvious",
        body: [
          "The shape: const result = useMemo(() => expensiveWork(input), [input]) — compute on first render and whenever input changes (Object.is), otherwise reuse the cache. 'Expensive' means something a profiler can SEE: filtering/sorting thousands of rows, parsing large structures, building complex derived collections. It does NOT mean 'I wrote a map()' — a ten-element map recomputed per render is cheaper than the memo's own comparison and memory.",
          "Two correctness rules. One: the computation must be pure — Strict Mode double-invokes it, and impure memos (fetches, mutations, Math.random inside) misbehave; side effects belong in effects. Two: the deps must be EVERYTHING the computation reads — a lying dep array returns a stale cache, the sneakiest bug in React because it looks like working code. The diagnostic workflow before ANY useMemo: instrument the render (a counter or the Profiler), confirm the work is actually dominant, memoize, re-measure. No measurement, no memo — that is the whole discipline.",
        ],
        code: [
          {
            file: "memo-measure.tsx — earn the cache",
            lang: "tsx",
            code: [
              "function Leaderboard({ players, query }: { players: Player[]; query: string }) {",
              "  // 5,000 rows filtered + sorted on EVERY keystroke? Measure first…",
              "  // (a render counter shows 8ms of this 9ms render is this line)",
              "  const ranked = useMemo(",
              "    () =>",
              "      players",
              "        .filter((p) => p.name.toLowerCase().includes(query.toLowerCase()))",
              "        .toSorted((a, b) => b.score - a.score),",
              "    [players, query],   // everything it reads — no lying",
              "  );",
              "",
              "  // ✗ decoration, not memoization:",
              "  // const label = useMemo(() => `${ranked.length} shown`, [ranked]);",
              "  //   a template literal is cheaper than the cache's own overhead.",
              "",
              "  return (",
              "    <ol>{ranked.map((p) => <li key={p.id}>{p.name}: {p.score}</li>)}</ol>",
              "  );",
              "}",
              "",
              "// purity law: useMemo(() => { log(); return work(); }, deps) double-logs",
              "// under Strict Mode. Compute here; act in effects or handlers.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "useCallback — identity is a contract with a consumer",
        body: [
          "useCallback(fn, deps) is useMemo(() => fn, deps) in a vest: it caches the FUNCTION so its identity is stable across renders. Stability only matters to specific consumers — learn the two and the hook stops being mystical. Consumer one: a child wrapped in React.memo, which skips re-rendering when its props are unchanged by Object.is — an inline handler (new identity every render) defeats the memo, so the parent stabilizes it with useCallback. Consumer two: a dependency array — a function read inside an effect must be a dep, and an unstable one re-triggers the effect every render; useCallback (or restructuring to remove the function from deps) is the fix.",
          "Without those consumers, useCallback is pure overhead: caching a function nobody compares. The common ceremony — wrapping every handler in useCallback 'for performance' — actively hurts: more code, more deps to maintain, and a false sense of optimization while the real work goes unmeasured. The decision rule is one sentence: 'Does a memoized child receive this, or does an effect dep read it?' No → plain function. And when you DO use it, the deps rules are exactly useMemo's: everything the callback reads, honestly listed.",
        ],
        code: [
          {
            file: "callback-consumers.tsx — the only two reasons",
            lang: "tsx",
            code: [
              "const Row = memo(function Row({ onPick, item }: RowProps) {",
              "  // memo: re-render only when props are Object.is-equal…",
              "  return <button onClick={() => onPick(item.id)}>{item.name}</button>;",
              "});",
              "",
              "function List({ items }: { items: Item[] }) {",
              "  const [picked, setPicked] = useState<number | null>(null);",
              "",
              "  // consumer #1: a memoized child. Without useCallback this handler",
              "  // is a fresh identity each render → Row's memo is useless:",
              "  const onPick = useCallback((id: number) => setPicked(id), []);",
              "",
              "  // consumer #2: an effect dep. load reads `refresh`, so refresh must",
              "  // be stable or the effect re-runs every render:",
              "  const refresh = useCallback(() => { /* …uses nothing external… */ }, []);",
              "  useEffect(() => { load(refresh); }, [refresh]);",
              "",
              "  // ✗ ceremony: no memoized child, no effect dep — just overhead:",
              "  const onCancel = useCallback(() => setPicked(null), []);",
              "  // ✓ a plain function serves exactly as well here:",
              "  // const onCancel = () => setPicked(null);",
              "",
              "  return (",
              "    <div>",
              "      {items.map((i) => <Row key={i.id} item={i} onPick={onPick} />)}",
              "      <button onClick={onCancel}>clear</button>",
              "    </div>",
              "  );",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The profiling workflow — replace guessing with receipts",
        body: [
          "The professional loop has four beats and no folklore. One: instrument — a render counter (useRef + a body log) or React DevTools Profiler recording an interaction. Two: locate — which component renders, how often, and how long; is the cost renders themselves or work inside one render? Three: treat the CAUSE — often it's not memoization at all: state placed too high (a keystroke re-rendering a tree), a fresh object in props, an effect re-triggering renders. Moving state down or splitting a component beats any amount of memo. Four: only if expensive work or identity churn survives the first three — memoize, then re-measure to CONFIRM the win; a memo that doesn't move the number gets deleted.",
          "The ordering matters because render count is not the enemy — wasted expensive work is. A component rendering 60 times a second with 0.1ms renders is healthier than one rendering twice with 40ms of sorting inside. This is why 'why is it re-rendering' questions often end in 'and that's fine' — and why Phase 32's performance engineering starts with budgets and profiles, never with a memoization spray.",
        ],
      },
      {
        heading: "useId — unique ids without the SSR trap",
        body: [
          "Accessibility wiring needs ids: <label htmlFor>, aria-describedby, aria-labelledby. Generating them with Math.random() or a module counter breaks under server rendering — the server's counter and the client's counter disagree, producing a hydration mismatch (the server-rendered HTML says id-0, the client expects id-1). useId() exists precisely here: React generates ids that are STABLE across server and client renders and unique per component instance — ':r1:', ':r2:', opaque strings you wire but never style.",
          "Two boundaries of useId to respect: never use it for list keys (keys must track DATA identity across renders — useId is stable per component instance, so a list of items would share structure wrong) and never for styling hooks (the format is an implementation detail and not a public styling API). For a single component needing one or two wired ids, it is the only correct answer — which is why every accessible form field you build from Phase 9 onward should reach for it (or a library that does) instead of hand-rolling counters.",
        ],
        code: [
          {
            file: "use-id.tsx — wiring, not keys, not styles",
            lang: "tsx",
            code: [
              "function EmailField({ label, error }: EmailFieldProps) {",
              "  const id = useId();                    // stable across SSR + client",
              "  const errorId = `${id}-error`;",
              "  return (",
              "    <div>",
              "      <label htmlFor={id}>{label}</label>",
              "      <input",
              "        id={id}",
              "        type=\"email\"",
              "        aria-invalid={error ? true : undefined}",
              "        aria-describedby={error ? errorId : undefined}",
              "      />",
              "      {error && <p id={errorId} role=\"alert\">{error}</p>}",
              "    </div>",
              "  );",
              "}",
              "",
              "function FieldList({ items }: { items: Item[] }) {",
              "  return items.map((item) => (",
              "    <EmailField key={item.id} /* ← DATA identity for the list… */",
              "      label={item.label} error={item.error} />",
              "    // …while useId handles each field's INNER wiring",
              "  ));",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The React Compiler — the defaults are moving",
        body: [
          "The React team's compiler (stable releases verified against the Version Matrix before any course reliance) analyzes components and inserts memoization automatically — the prep robot of the mental model. Its arrival changes the DEFAULT, not the understanding: hand-written useMemo/useCallback remain correct and sometimes necessary (stable identities for effect deps and library contracts, genuinely algorithmic caches the compiler won't model), but the era of sprinkling them as hygiene is ending — the compiler will out-memoize a human on coverage and never lie about a dep array.",
          "What this means practically for this course: learn the mechanics deeply (this lesson), adopt the measurement-first workflow (it's compiler-proof), and prefer plain code unless profiling proves a case — because plain code compiles better than hand-tuned code and reads better in review. When you encounter a codebase dense with manual memos, you're looking at pre-compiler engineering; the refactor question is 'does the profile still justify this?' not 'is this best practice?'. Best practice is now: understand memoization, measure, and let the tooling carry the routine.",
        ],
      },
    ],
    mistake: {
      title: "The memoization spray",
      wrong: [
        "function Card({ user }: { user: User }) {",
        "  // 'stability hygiene' — no consumer, no measurement:",
        "  const fullName = useMemo(",
        "    () => `${user.firstName} ${user.lastName}`,   // a template literal…",
        "    [user.firstName, user.lastName],",
        "  );",
        "  const onEdit = useCallback(() => openEditor(user.id), [user.id]);",
        "  const onArchive = useCallback(() => archive(user.id), [user.id]);",
        "  const initials = useMemo(",
        "    () => fullName.split(\" \").map((p) => p[0]).join(\"\"), [fullName],",
        "  );",
        "  // Four caches, four dep arrays to keep honest, comparison cost on",
        "  // EVERY render — saving microseconds of string concatenation.",
        "  // Net performance: WORSE. Net readability: worse. Net bugs waiting",
        "  // in the dep arrays: four.",
        "}",
      ].join("\n"),
      right: [
        "function Card({ user }: { user: User }) {",
        "  // cheap derivations: compute during render, every time.",
        "  const fullName = `${user.firstName} ${user.lastName}`;",
        "  const initials = fullName.split(\" \").map((p) => p[0]).join(\"\");",
        "",
        "  // plain handlers — this child isn't memoized and no effect reads them:",
        "  const onEdit = () => openEditor(user.id);",
        "  const onArchive = () => archive(user.id);",
        "",
        "  // memoize ONLY what a profile proved:",
        "  // const heavy = useMemo(() => buildReport(user.transactions), [user.transactions]);",
        "  // (8,000 rows reduced per render — measured 11ms before, 0.02ms after)",
        "  return <article>{/* … */}</article>;",
        "}",
        "",
        "// The discipline: no memo without a measured cost OR a named",
        "// identity consumer. 'Performance' is a number, not a feeling.",
      ].join("\n"),
      explain:
        "Every memo is a small machine: it compares deps on each render and stores a result. For cheap work, the machine costs more than the work — and each dep array is a new place to lie about what's read, caching stale results that look correct until production. The spray also masks the real question (is anything expensive here?) under a costume of diligence. Measure, name the expensive line or the identity consumer, memoize exactly that, and re-measure. The React Compiler makes this even clearer: routine memoization is automation's job; judgment is yours.",
    },
    tryIt: [
      "Instrument a list component with a render counter (useRef + log). Type into an unrelated input in the same parent and count how many times a 5,000-row filter recomputes. That number is the problem statement for useMemo.",
      "Add useMemo to the filter and re-measure. Then deliberately OMIT one dep and change it — observe the stale cache render. Feel why dep honesty is the price of caching.",
      "Wrap a child in memo() and pass an inline onClick. Log the child's renders per parent render (expected: every time). Then useCallback the handler and count again (expected: zero extra).",
      "Delete all your useCallbacks from the previous exercise EXCEPT the ones with a named consumer. Re-run the profile. If the numbers didn't change, you've proven the ceremony was noise.",
      "Build two instances of an EmailField using useId and inspect the rendered ids. Then hand-roll a module-counter version and imagine the server rendering the first instance while the client starts at zero — write one sentence on why hydration breaks.",
    ],
    challenge: {
      prompt:
        "Optimize a deliberately wasteful <SalesTable> — but only where you can PROVE it. The component: 8,000 transaction rows; every render it (a) filters by a search input, (b) sorts by date, (c) formats each row's cents with toFixed, (d) computes a totals object, (e) passes an inline onRowClick to each row (rows are memo-ized), and (f) builds an inline options object passed to a useEffect that logs analytics. Your job: (1) instrument renders and per-section timing with counters before touching anything; (2) apply the minimum set of useMemo/useCallback that the profile justifies, each with a comment naming its measured cost or its identity consumer; (3) re-measure and record before/after numbers in comments; (4) write a short 'what I did NOT memoize and why' list. Bonus: explain which of your memos the React Compiler would likely have inserted automatically.",
      hints: [
        "Sort order matters for the memo: filter → sort → format should be ONE memoized pipeline keyed on [rows, query, sortKey], not three chained memos — chained caches multiply comparison overhead.",
        "onRowClick's consumer is the memo-ized row: without useCallback, every parent render hands rows a fresh identity and their memo is dead weight. The options object's consumer is the effect's dep array: either useCallback/useMemo it or inline its primitive fields into deps.",
        "Row formatting (toFixed per row) is per-item work the memoized pipeline already covers — do NOT add a per-row memo. The totals object derives from the memoized pipeline: cheap to recompute, and memoizing a sum is decoration.",
      ],
      solution: [
        "import { memo, useCallback, useEffect, useMemo, useRef, useState } from \"react\";",
        "",
        "interface Tx { id: number; date: string; cents: number; note: string; }",
        "",
        "const Row = memo(function Row({ tx, onRowClick }: {",
        "  tx: Tx & { display: string };",
        "  onRowClick: (id: number) => void;",
        "}) {",
        "  return (",
        "    <tr onClick={() => onRowClick(tx.id)}>",
        "      <td>{tx.date}</td><td>{tx.note}</td><td>{tx.display}</td>",
        "    </tr>",
        "  );",
        "});",
        "",
        "export function SalesTable({ rows }: { rows: Tx[] }) {",
        "  const [query, setQuery] = useState(\"\");",
        "  const [selected, setSelected] = useState<number | null>(null);",
        "  const [analyticsTick, setAnalyticsTick] = useState(0);",
        "",
        "  // instrumentation (measure BEFORE optimizing — these logs drove every decision below):",
        "  const renders = useRef(0); renders.current += 1;",
        "",
        "  /* MEASURED: filter+sort over 8,000 rows took ~9ms of a ~10ms render.",
        "     One memoized pipeline, keyed on everything it reads: */",
        "  const visible = useMemo(() => {",
        "    const q = query.toLowerCase();",
        "    return rows",
        "      .filter((r) => r.note.toLowerCase().includes(q))",
        "      .toSorted((a, b) => b.date.localeCompare(a.date))",
        "      .map((r) => ({ ...r, display: `$${(r.cents / 100).toFixed(2)}` }));",
        "  }, [rows, query]);   // AFTER: ~9ms only when rows/query change; ~0.02ms otherwise",
        "",
        "  /* NOT memoized: the totals. Derives from `visible` in O(n) additions —",
        "     cheaper than the memo's own compare+store. Decoration declined. */",
        "  const totalCents = visible.reduce((s, r) => s + r.cents, 0);",
        "",
        "  /* MEASURED consumer #1: Row is memo-ized. The inline handler gave every",
        "     row a fresh prop each render → 8,000 wasted row renders per keystroke.",
        "     useCallback: stable identity, rows skip re-render. */",
        "  const onRowClick = useCallback((id: number) => setSelected(id), []);",
        "",
        "  /* MEASURED consumer #2: the effect dep. An inline options object is a",
        "     fresh identity each render → the analytics effect re-ran on every",
        "     keystroke. Memoize the object, keyed on its only real input: */",
        "  const analyticsOptions = useMemo(",
        "    () => ({ event: \"table-view\", count: visible.length }),",
        "    [visible.length],",
        "  );",
        "  useEffect(() => {",
        "    logAnalytics(analyticsOptions);   // now fires when count actually changes",
        "  }, [analyticsOptions]);",
        "",
        "  return (",
        "    <div>",
        "      <input value={query} onChange={(e) => setQuery(e.target.value)} aria-label=\"Search notes\" />",
        "      <table>",
        "        <tbody>",
        "          {visible.map((tx) => <Row key={tx.id} tx={tx} onRowClick={onRowClick} />)}",
        "        </tbody>",
        "      </table>",
        "      <p>Total: ${(totalCents / 100).toFixed(2)} · renders: {renders.current}</p>",
        "    </div>",
        "  );",
        "}",
        "",
        "declare function logAnalytics(o: { event: string; count: number }): void;",
        "",
        "/* ——— 'what I did NOT memoize and why' ———",
        "   · totalCents: O(n) additions over the memoized pipeline; the memo",
        "     overhead would exceed the work. (Would a compiler skip it too? Likely yes.)",
        "   · the query input's onChange: fires on every keystroke by design;",
        "     caching a setter-wrapper changes nothing downstream.",
        "   · per-row formatting: already inside the single memoized pipeline —",
        "     a per-row memo would be 8,000 caches for work that runs once.",
        "   ——— compiler notes ———",
        "   The auto-memoizer would likely have inserted the `visible` pipeline memo",
        "   and the onRowClick identity (it sees the memo-ized child). The analytics",
        "   object is the human-judgment case: a compiler memoizes by value equality,",
        "   but choosing `visible.length` as the dep encoded a PRODUCT decision",
        "   (fire only when the count changes) no compiler could infer. That is",
        "   the durable skill: knowing what to cache, and what the cache is FOR. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "useMemo(() => work(), [deps]) recomputes when…",
        options: ["deps change by Object.is — otherwise the cached result is reused", "The component re-renders for any reason", "Every second render", "Only on mount"],
        answer: 0,
        explain: "It's a cache keyed on deps. The computation must be pure, and the deps must honestly list everything it reads — a lying array yields a stale cache.",
      },
      {
        q: "The ONLY legitimate reasons for useCallback are…",
        options: ["A memoized child receives the function, or an effect dep array reads it", "All handlers, as hygiene", "Arrow functions specifically", "Event handlers in forms"],
        answer: 0,
        explain: "Identity stability is a contract with a consumer: React.memo's prop comparison or a deps array. Without a consumer, the cache is pure overhead.",
      },
      {
        q: "Before adding any memoization, the professional first…",
        options: ["Measures — render counters or the Profiler — to confirm expensive work or identity churn", "Wraps everything defensively", "Refactors to classes", "Adds React.memo to the parent"],
        answer: 0,
        explain: "Memo without measurement is decoration with a maintenance cost. Profile, locate the dominant cost, treat causes (state placement, fresh objects) first, memoize last, re-measure.",
      },
      {
        q: "A component renders often. The correct concern is…",
        options: [
          "Whether the renders are EXPENSIVE — cheap frequent renders are fine",
          "Any re-render is a bug",
          "The render count must be minimized",
          "Memoize until renders stop",
        ],
        answer: 0,
        explain: "Render count is not the enemy; wasted expensive work is. Many 0.1ms renders beat two 40ms renders. 'Why is it re-rendering' often ends in 'and that's fine'.",
      },
      {
        q: "useId exists because…",
        options: ["Server and client must generate matching unique ids — counters/random drift and break hydration", "Ids must be human-readable", "It's faster than uuid libraries", "Keys need stable ids"],
        answer: 0,
        explain: "React generates ids stable across SSR and client renders, unique per instance. Never for list keys (data identity's job) or styling (implementation detail).",
      },
      {
        q: "The React Compiler changes the practitioner's default to…",
        options: ["Plain code unless profiling proves a case — routine memoization becomes automation's job", "More hand-written useCallback everywhere", "Removing all effects", "Manual dependency management"],
        answer: 0,
        explain: "The compiler auto-memoizes with perfect dep honesty. Humans keep judgment: measured expensive work, identity contracts, product-encoded deps. Understanding outlasts the tooling.",
      },
    ],
    flashcards: [
      { front: "The memoization trade", back: "Memory + per-render comparison cost vs skipped recomputation. Negative for cheap work. No measurement, no memo." },
      { front: "useMemo", back: "Cache a computed value keyed on deps (Object.is). For profile-proven expensive work; pure computation only; deps must be honest." },
      { front: "useCallback", back: "useMemo for functions. Legitimate ONLY for a memoized child's props or an effect dep array. Ceremony without a consumer is overhead." },
      { front: "Identity stability", back: "Object.is-equal references across renders. memo() children and effect deps are the consumers; 'stability' without a consumer means nothing." },
      { front: "Profile → treat cause → memo → re-measure", back: "The four-beat workflow. Often the fix is state placement or fresh-object props, not memoization. A memo that doesn't move the number gets deleted." },
      { front: "Render count ≠ problem", back: "Cheap frequent renders are healthy; expensive renders are the enemy. Budgets and profiles decide, not intuition." },
      { front: "useId", back: "SSR-safe unique ids for label/aria wiring. Stable across server+client, unique per instance. Never for keys, never for styles." },
      { front: "React Compiler era", back: "Automatic memoization shifts the default to plain code + measurement. Judgment remains: what's expensive, what needs an identity, what encodes a product decision." },
    ],
    recap: [
      "Memoization is a trade with two legitimate invoices: measured expense and named identity consumers.",
      "useMemo caches values; useCallback caches functions for memo children and effect deps — nothing else.",
      "Profile first; often the cure is architectural, not a cache.",
      "useId wires accessibility across SSR; keys stay data-driven.",
      "The compiler automates the routine — your judgment about WHY is the durable asset.",
    ],
    references: [
      { label: "react.dev — useMemo", url: "https://react.dev/reference/react/useMemo" },
      { label: "react.dev — useCallback", url: "https://react.dev/reference/react/useCallback" },
      { label: "react.dev — useId", url: "https://react.dev/reference/react/useId" },
      { label: "react.dev — memo", url: "https://react.dev/reference/react/memo" },
      { label: "react.dev — React Compiler", url: "https://react.dev/learn/react-compiler" },
    ],
    nextBridge:
      "The toolbox is complete and measured. Two modern capabilities remain before you can design hooks of your own: marking work as non-urgent so urgent paints win (useTransition, useDeferredValue, and the use hook for promises/context) — and then extracting all of this into custom hooks with a decision matrix to rule them.",
  },
];

export const LESSON_CONTENT_P5B: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P5B.map((l) => [l.id, l]),
);
