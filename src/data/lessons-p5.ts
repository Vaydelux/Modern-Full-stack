import type { LessonContent } from "./types";

/**
 * Pass 013 bounded content batch: Phase 5 opener (L1–L2).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P5: LessonContent[] = [
  {
    id: "p05-l1",
    phaseId: "p05",
    title: "useState Deep Dive: Queues & Functional Updates",
    level: "React Developer",
    minutes: 40,
    summary:
      "useState is not a variable — it is a window onto the CURRENT render's snapshot plus a mailbox for requesting the next one. This lesson installs the queue model: why state reads are frozen per render, why React batches every update in an event, why three setCount(c + 1) calls produce +1 and not +3, when functional updates are mandatory, and how to model state so two values never disagree. Strict Mode's double-render gets its explanation here too — and it is a friend.",
    prerequisites: ["p04-l3 — state, events & immutable updates", "p02-l6 — the event loop (tasks queue in order)"],
    objectives: [
      "Explain the snapshot-plus-mailbox model: reads are frozen per render; updates are queued requests.",
      "Predict batching across event handlers, timeouts, and promises (automatic since React 18).",
      "Diagnose the stale-read trap and fix it with functional updates.",
      "Keep updater functions pure — no side effects inside setX(fn) — and explain why Strict Mode double-invokes them.",
      "Model state deliberately: primitives vs objects, when two states should merge, when one should split.",
      "Enumerate what does NOT belong in state (derivable values, refs, constants) before reaching for useState.",
    ],
    simple:
      "Imagine a restaurant kitchen with a ticket window. During service (a render), the chef reads the orders pinned there — a fixed set that never changes mid-service. When you call setCount(...), you're not rewriting the chef's current ticket; you're sliding a NEW ticket into the mailbox. The kitchen finishes the current service, reads the mailbox, applies the tickets, and opens the window with a fresh snapshot. Three 'add one' tickets slid in during one service produce three applications on the NEXT snapshot — but if all three tickets say 'make it 6' (because each read the same old window), you get 6, not 8. Functional updates are tickets that say 'add one to whatever the kitchen has when it gets to me' — and that is the whole mystery of stale state.",
    why:
      "Every confusing state bug in React — 'my counter only goes up by one', 'my form shows the previous value', 'two fields drifted apart' — reduces to someone treating the snapshot like a variable or the mailbox like an assignment. Phase 5's Boss Battle (BB-1) grades exactly this model: given a handler, predict the final state before running it. And the modeling half (what to store, where, and how much) determines whether your Phase 9/19 features stay debuggable or become 'state spaghetti'. useState looks like the simplest hook; it is the one whose mental model pays rent for the entire rest of the course.",
    mentalModel: {
      title: "Snapshots and the mailbox",
      body: "Each render is a photograph: every useState read inside it is a value frozen at exposure time. setState is not 'change the photo' — it is 'mail a request for the next photograph'. React processes the mailbox AFTER the event handler (or timeout, or promise callback) finishes, batching all tickets into one re-render. Functional updates (c => c + 1) are self-referential tickets evaluated against the latest queued state, which is why they survive batching. Once you can narrate any handler as 'snapshot read → tickets mailed → one new snapshot', stale-state bugs stop happening to you and start happening FOR you, on quizzes.",
    },
    sections: [
      {
        heading: "The snapshot: reads are frozen, updates are queued",
        body: [
          "The contract, stated once and relied on forever: calling setX(value) does not change X in the running render — X is a const for this snapshot. It asks React to render again with the new value, and the new value is visible in the NEXT render only. Handlers run to completion against one snapshot; log the state after calling set and you'll see the old value — not a bug, the model working.",
          "This is why the 'update then read' pattern from imperative code betrays you: setQuery(text); search(query) searches the OLD query. The fix is structural, not timing: pass the new value onward (search(text)), or let the next render react to the changed state (Phase 5-L2's synchronization). Imperative 'set then use' is the number one beginner state bug; the snapshot model makes it impossible to write once internalized.",
        ],
        code: [
          {
            file: "snapshot.tsx — the photo, not the darkroom",
            lang: "tsx",
            code: [
              "function SearchBox() {",
              "  const [query, setQuery] = useState(\"\");",
              "",
              "  function onSubmit() {",
              "    setQuery(query.trim());     // mails a ticket…",
              "    console.log(query);         // …prints the OLD snapshot value",
              "    // search(query);           // ✗ searches the untrimmed value",
              "    search(query.trim());       // ✓ hand the NEW value forward",
              "  }",
              "",
              "  return (",
              "    <form onSubmit={(e) => { e.preventDefault(); onSubmit(); }}>",
              "      <input value={query} onChange={(e) => setQuery(e.target.value)} />",
              "      <button type=\"submit\">Search</button>",
              "    </form>",
              "  );",
              "}",
              "// The input shows the fresh value on the NEXT render —",
              "// React re-rendered with the trimmed snapshot. Nobody lied;",
              "// the render you were standing in was a photograph.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Batching: one event, one re-render — everywhere",
        body: [
          "React batches: every update requested inside one event handler is collected and applied in a single re-render. Since React 18 this is automatic everywhere — inside timeouts, promise callbacks, and native event listeners, not just React events. Batching is why UIs feel fast: ten setX calls in a submit handler cost one render, not ten.",
          "The flip side is the classic quiz question: three setCount(count + 1) calls produce +1, not +3, because all three tickets were written from the SAME snapshot value. Each ticket says 'set it to N+1'. The mailbox applies: N+1, N+1, N+1 → N+1. If you ever find yourself about to write sequential updates that depend on each other, stop — you are in functional-update territory (next section) or your state is modeled wrong (section four).",
        ],
        code: [
          {
            file: "batching.tsx — predict the log before running",
            lang: "tsx",
            code: [
              "function Counter() {",
              "  const [count, setCount] = useState(0);",
              "",
              "  function triple() {",
              "    setCount(count + 1); // ticket: 'set to 1'",
              "    setCount(count + 1); // ticket: 'set to 1' (same snapshot!)",
              "    setCount(count + 1); // ticket: 'set to 1'",
              "    console.log(count);  // 0 — the snapshot never changes mid-handler",
              "  }",
              "  // one re-render after the handler: count === 1. NOT 3.",
              "",
              "  function tripleSlow() {",
              "    setTimeout(() => {",
              "      setCount((c) => c + 1); // self-referential tickets:",
              "      setCount((c) => c + 1); // 0→1, then 1→2, then 2→3",
              "      setCount((c) => c + 1);",
              "    }, 0);",
              "  }",
              "  // Even inside a timeout (React 18+ batches these too),",
              "  // the FUNCTIONAL form lands +3 — each ticket reads the queue.",
              "",
              "  return <button onClick={triple}>+3?</button>;",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Functional updates — tickets that read the queue",
        body: [
          "When the next value depends on the previous value, pass an updater: setCount(c => c + 1). React stores the function and calls it against the LATEST queued state when processing the mailbox — so chained updates compose instead of clobbering. The rule is mechanical, not stylistic: depends on current state → functional form; independent value → direct form. setQuery('ada') is fine; setQuery(query + '!') is a stale-read waiting for a second event.",
          "Two disciplines keep updaters honest. One: updaters must be PURE — same input, same output, no side effects. No fetches, no console-driven mutations, no other setX inside. React may call an updater twice (Strict Mode) or replay it (concurrent features), and impure updaters double their damage. Two: the updater receives the queued value, not 'whatever the closure saw' — never reference the snapshot variable inside a functional updater; use the parameter. Mixing the two (c => c + step where step is snapshot-fresh) is fine; referencing the state variable itself is the bug.",
        ],
        code: [
          {
            file: "updaters.tsx — pure, parameter-driven",
            lang: "tsx",
            code: [
              "function Likes() {",
              "  const [likes, setLikes] = useState(0);",
              "  const [step, setStep] = useState(1);",
              "",
              "  function burst() {",
              "    setLikes((prev) => prev + step); // step from snapshot: OK",
              "    setLikes((prev) => prev + step); // prev from the QUEUE: correct",
              "    // setLikes(likes + step);        // ✗ three identical tickets",
              "  }",
              "",
              "  // ✗ impure updater — double-counts under Strict Mode:",
              "  // setLikes((prev) => { log(); return prev + 1; });",
              "",
              "  return (",
              "    <div>",
              "      <button onClick={burst}>+{2 * step} likes</button>",
              "      <button onClick={() => setStep((s) => s + 1)}>step up</button>",
              "      <p>{likes}</p>",
              "    </div>",
              "  );",
              "}",
            ].join("\n"),
            caption: "Strict Mode calls updaters twice with the same input and keeps one result — impurity shows up as doubled side effects, which is exactly the detector working.",
          },
        ],
      },
      {
        heading: "Modeling state — the decisions that prevent whole bug classes",
        body: [
          "Before writing useState, ask three questions. One: can it be derived? A 'remaining' counter computed from total and used is not state — computing it during render keeps it true by construction and eliminates a synchronization job. Two: can two values disagree? If every change to firstName must accompany a change to lastName, store one object; if they update independently, two primitives read clearer. The drift bug (storing 'fullName' plus 'firstName') is always a modeling failure, never a React failure.",
          "Three: does anything need to change over time, or is it identity? A userId prop never 'updates' within a component's life — it isn't state. Timer IDs, input element handles, and previous-values are mutable but must NOT trigger renders — that is useRef territory (P05-L3). The state checklist: varies over time, can't be derived, affects what renders. Fail any of the three and the value belongs somewhere else. This checklist is the difference between Phase 9 apps that debug cleanly and ones where 'the state lives in six places'.",
        ],
        code: [
          {
            file: "modeling.tsx — derive, merge, or store",
            lang: "tsx",
            code: [
              "// ✗ drift-prone: two sources of truth for one fact",
              "const [items, setItems] = useState<Item[]>([]);",
              "const [count, setCount] = useState(0); // must be kept in sync… forever",
              "",
              "// ✓ derived: computed each render, true by construction",
              "const [items, setItems] = useState<Item[]>([]);",
              "const count = items.length;",
              "",
              "// ✓ one object when fields change together:",
              "const [range, setRange] = useState({ from: \"2026-01-01\", to: \"2026-12-31\" });",
              "setRange((r) => ({ ...r, to: \"2026-06-30\" }));",
              "",
              "// ✓ separate primitives when they move independently:",
              "const [firstName, setFirstName] = useState(\"\");",
              "const [lastName, setLastName] = useState(\"\");",
              "",
              "// ✗ NOT state — never changes for this component:",
              "function Profile({ userId }: { userId: string }) { /* it's a prop */ }",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Strict Mode's double-render — the impurity detector",
        body: [
          "In development, <StrictMode> invokes every component function twice (and, as P05-L2 shows, effects too) and uses ONE result. The point is not punishment — it is detection: a pure render called twice produces identical output and nobody notices; an impure one (reading Date.now() into state, mutating external objects, incrementing a module counter) shows its face immediately, in development, where fixes are cheap. Production renders once; Strict Mode costs nothing there.",
          "The practical read: if your UI behaves differently under Strict Mode, the bug is real and Strict Mode merely exposed it early. Common confessions: a render that mutates an array prop, an effect-free 'initialize once' block that runs per render, a Math.random() sprinkled into markup. The fix is always toward purity — move the work into an event handler, an effect with proper deps (next lesson), or a lazy initializer: useState(() => expensive()) runs the initializer once per mount, the sanctioned place for setup math.",
        ],
        code: [
          {
            file: "strict.tsx — confessions and their fixes",
            lang: "tsx",
            code: [
              "// ✗ impure render — different output on the double-call:",
              "function Stamp() {",
              "  const [now] = useState(new Date()); // actually fine (initializer runs once per mount)…",
              "  return <p>{new Date().toLocaleTimeString()}</p>; // ✗ THIS drifts between the two calls",
              "}",
              "",
              "// ✓ lazy initializer — sanctioned one-shot setup:",
              "function Board() {",
              "  const [cells] = useState(() => buildEmptyBoard(64)); // runs once per mount",
              "  return <Grid cells={cells} />;",
              "}",
              "",
              "// ✗ per-render side effect — double under Strict Mode:",
              "// function Leak() { metrics.track(\"render\"); return <p/>; }",
              "// ✓ tracking belongs in an effect (P05-L2) or an event handler.",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The +1 that should have been +3",
      wrong: [
        "function Score() {",
        "  const [score, setScore] = useState(0);",
        "",
        "  function onHit() {          // one click = three score events",
        "    setScore(score + 1);      // ticket: 'set to 1'",
        "    setScore(score + 1);      // ticket: 'set to 1' — same snapshot",
        "    setScore(score + 1);      // ticket: 'set to 1'",
        "  }",
        "  // click once → score is 1. Not 3. All three tickets were",
        "  // written against the SAME frozen photograph.",
        "}",
      ].join("\n"),
      right: [
        "function Score() {",
        "  const [score, setScore] = useState(0);",
        "",
        "  function onHit() {",
        "    setScore((s) => s + 1);   // 'add one to whatever is queued'",
        "    setScore((s) => s + 1);   // 0→1, then 1→2, then 2→3",
        "    setScore((s) => s + 1);",
        "  }",
        "  // click once → 3. Each ticket reads the QUEUE, not the photo.",
        "}",
        "",
        "// The mechanical rule: next value depends on previous value",
        "// → functional form. Independent value → direct form.",
      ].join("\n"),
      explain:
        "The snapshot is a const for the handler's lifetime: every score + 1 expression evaluated to the same number, so all three tickets requested the identical state and the last one won. Functional updates defer the arithmetic until mailbox time, where each sees the result of the previous. This exact pattern resurfaces as stale props in memoized children (P05-L4) and stale closures in effects (P05-L2) — same photograph, different darkroom.",
    },
    tryIt: [
      "Take the Counter's triple() from section two and, WITHOUT running it, write down the exact final count and the exact console output. Then run it. If you predicted 3, re-read the snapshot paragraph until 1 feels inevitable.",
      "Add a fourth button that mixes forms: setCount(count + 1); setCount(c => c + 1); setCount(c => c + 1). Predict the result from count = 0 (answer: 2), then verify.",
      "Break purity on purpose: put console.log('update') inside a functional updater and run under StrictMode. Count the logs per click (expected: 2). Remove StrictMode and count again (1).",
      "Audit your Phase 4 Todo app's state against the checklist: which values are derived and could be computed? Which pairs always change together? Refactor one and feel the sync code disappear.",
      "Write useState(() => Array.from({ length: 1000 }, (_, i) => i * i)) and log inside the initializer. Confirm it runs once per mount even though the component re-renders on every click.",
    ],
    challenge: {
      prompt:
        "Build a 'reaction tester' game in one component. State: phase ('idle' | 'arming' | 'go' | 'result'), armingDelay (ms), reactionMs (number | null), and best (number | null). Behavior: (1) Click 'Start' → phase 'arming', schedule a random 1–4s delay with setTimeout; when it fires, phase 'go' and record the timestamp. (2) Click the big pad during 'go' → reactionMs = now - startStamp, phase 'result', and best updated via a FUNCTIONAL update that compares to the previous best (handling null). (3) Click during 'arming' → 'Too soon!' result with reactionMs null. (4) A Reset returns everything to idle. Constraints: every update that depends on previous state uses the functional form; the timer id lives in a plain let inside the handler scope or a ref-free local (no leaks across games — clear the old timer on every Start/Reset); predict-then-verify: before running, write out the exact state after this event sequence: Start → (wait) → Go-click → Start → Too-soon-click, assuming best was 240 after the first round.",
      hints: [
        "The Start handler must clear any pending timer first (clearTimeout on the stored id) — two overlapping games is the classic timer-state bug. A component-level let timerId works because you only ever need the latest one.",
        "Updating best is the functional-form exam: setBest(prev => prev == null ? ms : Math.min(prev, ms)). The null case is why ?? and functional forms are friends.",
        "For the prediction: after round one best is 240 and phase 'result'. Start → 'arming'. Too-soon click → phase 'result', reactionMs null, best STILL 240 — the updater must not clobber best with null.",
      ],
      solution: [
        "import { useRef, useState } from \"react\";",
        "",
        "type Phase = \"idle\" | \"arming\" | \"go\" | \"result\";",
        "",
        "export function ReactionTester() {",
        "  const [phase, setPhase] = useState<Phase>(\"idle\");",
        "  const [reactionMs, setReactionMs] = useState<number | null>(null);",
        "  const [best, setBest] = useState<number | null>(null);",
        "  const [tooSoon, setTooSoon] = useState(false);",
        "",
        "  const timerId = useRef<number | null>(null);   // latest timer only (P05-L3 formalizes refs;",
        "  const goStamp = useRef(0);                     // a plain variable here would reset per render)",
        "",
        "  function start() {",
        "    if (timerId.current != null) clearTimeout(timerId.current); // no overlapping games",
        "    setTooSoon(false);",
        "    setReactionMs(null);",
        "    setPhase(\"arming\");",
        "    const delay = 1000 + Math.random() * 3000;",
        "    timerId.current = window.setTimeout(() => {",
        "      goStamp.current = performance.now();",
        "      setPhase(\"go\");",
        "    }, delay);",
        "  }",
        "",
        "  function onPad() {",
        "    if (phase === \"arming\") {",
        "      // jumped the gun — cancel the pending reveal:",
        "      if (timerId.current != null) clearTimeout(timerId.current);",
        "      setTooSoon(true);",
        "      setReactionMs(null);",
        "      setPhase(\"result\");          // best untouched — correct by omission",
        "      return;",
        "    }",
        "    if (phase === \"go\") {",
        "      const ms = Math.round(performance.now() - goStamp.current);",
        "      setReactionMs(ms);",
        "      // FUNCTIONAL: reads the queued best, handles the null start:",
        "      setBest((prev) => (prev == null ? ms : Math.min(prev, ms)));",
        "      setPhase(\"result\");",
        "    }",
        "  }",
        "",
        "  function reset() {",
        "    if (timerId.current != null) clearTimeout(timerId.current);",
        "    setPhase(\"idle\");",
        "    setReactionMs(null);",
        "    setTooSoon(false);",
        "    // best intentionally survives Reset — it's the scoreboard",
        "  }",
        "",
        "  return (",
        "    <div>",
        "      <button onClick={start}>Start</button>",
        "      <button",
        "        onClick={onPad}",
        "        disabled={phase !== \"arming\" && phase !== \"go\"}",
        "        style={{ padding: \"2rem\", fontSize: \"1.5rem\" }}",
        "      >",
        "        {phase === \"go\" ? \"CLICK!\" : phase === \"arming\" ? \"wait…\" : \"pad\"}",
        "      </button>",
        "      <button onClick={reset}>Reset</button>",
        "      <p>",
        "        {tooSoon && \"Too soon! \"}",
        "        {reactionMs != null && `Reaction: ${reactionMs} ms · `}",
        "        Best: {best ?? \"—\"}",
        "      </p>",
        "    </div>",
        "  );",
        "}",
        "",
        "/* Predicted trace (best 240 after round 1):",
        "   Start          → arming (old timer cleared, reactionMs null)",
        "   go fires       → go (stamp recorded)",
        "   pad click      → result, reactionMs = e.g. 312,",
        "                    best = min(240, 312) = 240  (functional!)",
        "   Start          → arming again",
        "   pad click      → result, tooSoon = true, reactionMs null,",
        "                    best = 240 — untouched. Exactly the plan. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "Inside a handler, calling setCount(count + 1) three times from count = 0 leaves count at…",
        options: ["1 — three identical tickets written from the same snapshot", "3 — one per call", "0 — the last call wins", "It throws"],
        answer: 0,
        explain: "The snapshot is frozen for the handler; every count + 1 evaluated to 1, so all tickets said 'set to 1'. Batching applies them all — to the same value.",
      },
      {
        q: "Since React 18, automatic batching applies to updates inside…",
        options: ["Event handlers, timeouts, and promise callbacks — essentially everywhere", "Only React synthetic events", "Only render-phase code", "Only class components"],
        answer: 0,
        explain: "React 18 batches all updates regardless of origin, so one re-render follows each burst of work. The mental model is one mailbox, one processing pass.",
      },
      {
        q: "Functional updates (setX(x => …)) are required when…",
        options: ["The next value depends on the previous value", "The value is a string", "You're inside a timeout", "The component renders often"],
        answer: 0,
        explain: "Updaters run against the latest queued state, so dependent updates compose. Independent values (setQuery('ada')) are fine as direct sets.",
      },
      {
        q: "A functional updater must be pure because…",
        options: ["React may invoke it more than once (Strict Mode, concurrent replay)", "Impure updaters are slower", "Purity is enforced by TypeScript", "Side effects are handled elsewhere"],
        answer: 0,
        explain: "Double-invocation is a feature: same input must give same output, and side effects would fire twice. Keep fetches/logs/mutations outside updaters.",
      },
      {
        q: "Which of these should NOT be useState state?",
        options: ["A list length that is always items.length", "A search query the user edits", "A selected tab id", "A form draft object"],
        answer: 0,
        explain: "Derivable values are computed during render — storing them creates a synchronization job and a drift bug class. The checklist: varies, not derivable, affects rendering.",
      },
      {
        q: "Strict Mode's double-render exists to…",
        options: ["Expose impure renders and effect leaks in development", "Warm a render cache", "Speed up production rendering", "Test accessibility twice"],
        answer: 0,
        explain: "Pure code is invisible under doubling; impure code confesses. Production renders once. If behavior differs under Strict Mode, the bug is real.",
      },
    ],
    flashcards: [
      { front: "The snapshot rule", back: "State read in a render is frozen for that render. setState mails a request for the NEXT snapshot; it never mutates the current one." },
      { front: "Batching", back: "All updates from one unit of work (event, timeout, promise — everywhere since React 18) are collected and applied in a single re-render." },
      { front: "Functional update", back: "setX(x => next) — evaluated against the latest queued value. Required whenever the next value depends on the previous one." },
      { front: "The +1-not-+3 bug", back: "Three setCount(count+1) calls write three identical tickets from one snapshot. The fix is functional updates, not more calls." },
      { front: "Updater purity", back: "Updaters must be pure — same input, same output, no side effects. React may call them twice (Strict Mode) or replay them." },
      { front: "State checklist", back: "Store it only if it (1) varies over time, (2) can't be derived, (3) affects what renders. Fail any → derive it, merge it, or use a ref." },
      { front: "Derive, don't store", back: "Values computable from existing state (totals, filters, counts) are computed during render. Storing them is a synchronization bug waiting." },
      { front: "Lazy initializer", back: "useState(() => expensive()) — runs once per mount, not per render. The sanctioned place for setup computation." },
    ],
    recap: [
      "Renders are photographs; setX mails tickets. Handlers never see their own updates.",
      "One burst of updates, one re-render — batching is everywhere since React 18.",
      "Dependent updates use functional form; updaters stay pure.",
      "Model first: derive what's derivable, merge what moves together, ref what mustn't render.",
      "Strict Mode doubles on purpose — the impurity detector you keep, never remove.",
    ],
    references: [
      { label: "react.dev — State: A Component's Memory", url: "https://react.dev/learn/state-a-components-memory" },
      { label: "react.dev — Queueing a Series of State Updates", url: "https://react.dev/learn/queueing-a-series-of-state-updates" },
      { label: "react.dev — Updating Objects/Arrays in State", url: "https://react.dev/learn/updating-objects-in-state" },
      { label: "react.dev — StrictMode", url: "https://react.dev/reference/react/StrictMode" },
      { label: "react.dev — useState", url: "https://react.dev/reference/react/useState" },
    ],
    nextBridge:
      "Your snapshots and mailboxes are disciplined. But some work doesn't fit inside a render at all — subscriptions, timers, fetches, the outside world. Next lesson gives it its proper home: useEffect as a synchronization tool, with dependency arrays as the contract and cleanup as the exit clause.",
  },
  {
    id: "p05-l2",
    phaseId: "p05",
    title: "useEffect: Synchronization, Deps & Cleanup",
    level: "React Developer",
    minutes: 45,
    summary:
      "useEffect is not 'on mount' and not 'after render runs my code' — it is how a component synchronizes with an EXTERNAL system: the network, a timer, a subscription, a browser API, another library's DOM. This lesson installs that contract: the render→commit→effect→cleanup lifecycle, dependency arrays as a precise 'reads' declaration compared with Object.is, cleanup as the leak-proof exit clause, the infinite-loop family and its cures, race-proofing async effects — and the liberating skill of recognizing when NO effect is needed at all.",
    prerequisites: ["p05-l1 — the snapshot/queue model", "p02-l5 — fetch and async/await", "p02-l6 — the event loop and races"],
    objectives: [
      "State the effect contract: synchronize with an external system, after commit, re-run when declared reads change.",
      "Write dependency arrays as honest 'what does this effect read' lists and predict run timing from them.",
      "Implement cleanup for subscriptions, timers, and in-flight requests — and explain Strict Mode's mount-cleanup-mount.",
      "Diagnose and cure the infinite-loop family (fresh object deps, self-feeding setState).",
      "Race-proof async effects with guards and AbortController.",
      "Reject effects for pure computation and event-driven work — compute in render, act in handlers.",
    ],
    simple:
      "Your component lives in React's world — a tidy place of pure descriptions and snapshots. Everything else (servers, clocks, websockets, localStorage, third-party widgets) lives outside. useEffect is the component's job site for that outside world: after React puts the UI on screen, the effect steps out, connects to the external system, and — critically — hands back a cleanup function that disconnects when the job's conditions change or the component leaves. The dependency array is the work order: 're-run me exactly when THESE values change.' Most effect bugs are someone using the job site for interior work (deriving state, handling clicks) that never needed to leave the building.",
    why:
      "Effects are where React developers lose the plot — and where the course's later phases are won or lost. The Phase 9 data-fetching patterns, Phase 19's TanStack Query (which exists precisely to move most effects OUT of your code), Phase 24's Realtime subscriptions, Phase 33's error reporters — all of it rides on correct synchronization discipline. The failure modes are infamous: fetch loops that hammer servers, listeners that leak until the tab dies, race conditions that show yesterday's search results. Every one has a mechanical cause (missing/lying deps, missing cleanup, unguarded async) and a mechanical fix you can learn today. Master the contract and the whole 'when do I use an effect' question answers itself: only when the work involves a system outside React.",
    mentalModel: {
      title: "The job site outside the building",
      body: "Render is interior design: pure, repeatable, snapshot-bound — you may rearrange the blueprint a hundred times and nothing outside notices. Commit is opening the doors. The effect is the crew that walks OUT: they plug into the power grid (a subscription), set up a delivery schedule (a timer), phone the supplier (a fetch). Their work order — the dependency array — lists exactly the interior measurements they depend on; when those change, the crew comes back in, undoes the old wiring (cleanup), and re-wires with the new measurements. When the building is demolished (unmount), they disconnect everything. A crew that never disconnects is a leak; a work order that lists 'everything' means constant re-wiring; a crew hired to repaint interior walls is simply the wrong contractor — that's a render-phase computation wearing a hard hat.",
    },
    sections: [
      {
        heading: "The contract: after commit, outward only",
        body: [
          "The lifecycle, precisely: React renders (computes the description), commits (updates the DOM), then runs effects. If an effect returns a cleanup function, React runs it (a) before the effect re-runs due to changed deps, and (b) on unmount. Effects run in declaration order; cleanups run in the same order. None of this blocks painting — the browser may paint between commit and effects, which is why visual measurements sometimes want useLayoutEffect (rarely; note it exists and move on).",
          "The question that gates every effect: 'What EXTERNAL system am I synchronizing with?' Legitimate answers: a websocket, an interval, the document title, a media query listener, localStorage, a third-party chart library, an IntersectionObserver. Illegitimate answers: 'I need to update my own state from my own props' (that's render-phase derivation), 'I want to run this when the user clicks' (that's an event handler), 'I want to memoize this' (that's useMemo). If you cannot name the external system, you probably don't need an effect — and the discipline of naming it is most of the skill.",
        ],
        code: [
          {
            file: "contract.tsx — the full ceremony, once",
            lang: "tsx",
            code: [
              "function Room({ roomId }: { roomId: string }) {",
              "  useEffect(() => {",
              "    // setup: connect to the external system",
              "    const connection = createConnection(roomId);",
              "    connection.connect();",
              "",
              "    // cleanup: disconnect — runs before re-run AND on unmount",
              "    return () => connection.disconnect();",
              "  }, [roomId]); // the work order: re-run exactly when roomId changes",
              "",
              "  return <h1>Welcome to {roomId}!</h1>;",
              "}",
              "",
              "// Timeline: mount → effect (connect A)",
              "//           roomId A→B → cleanup (disconnect A) → effect (connect B)",
              "//           unmount → cleanup (disconnect B)",
              "// Every connect is matched by exactly one disconnect. That is the whole law.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Dependency arrays — the honest work order",
        body: [
          "Three shapes, three contracts. No array: the effect re-runs after EVERY render — occasionally right (rare 'resync every paint' cases), usually a mistake. Empty array: run once after mount, clean up on unmount — the 'connect for the component's life' shape. [a, b]: re-run when a or b changes, compared with Object.is — so primitives behave as expected, but a fresh object/array/function identity counts as a change even if contents match. The golden rule: the array is not configuration to tune until the lint quiets down — it is a factual list of everything the effect reads. If you reference a prop or state inside, it belongs in the array; if you don't want to re-run when it changes, restructure the effect, don't omit the dep.",
          "The Object.is consequence bites with inline definitions: an options object built during render ({ retries: 3 }) is a NEW identity every render, so [options] means 're-run always'. The cures, in order: depend on the primitive fields ([options.retries] isn't readable — instead destructure before the effect or pass primitives as props), hoist the object to module scope if it's constant, or wrap genuinely-dynamic-but-expensive values in useMemo (P05-L4). eslint-plugin-react-hooks' exhaustive-deps rule is a colleague, not a nag: it catches both missing deps (stale closures) and the temptation to lie about them.",
        ],
        code: [
          {
            file: "deps.tsx — what the array really says",
            lang: "tsx",
            code: [
              "function Chat({ roomId, theme }: { roomId: string; theme: Theme }) {",
              "  // reads roomId → re-runs when roomId changes; theme is NOT read,",
              "  // so it is NOT listed — no reconnection on theme change.",
              "  useEffect(() => {",
              "    const conn = connect(roomId);",
              "    return () => conn.close();",
              "  }, [roomId]);",
              "",
              "  // ✗ the classic lie-and-omit:",
              "  // useEffect(() => { poll(serverUrl); }, []); // reads serverUrl!",
              "  //   → stale closure: forever polls the FIRST serverUrl.",
              "",
              "  // ✗ the identity trap:",
              "  const options = { retries: 3 };            // new object every render",
              "  // useEffect(() => { sync(options); }, [options]); // re-runs ALWAYS",
              "",
              "  // ✓ depend on what's actually read:",
              "  const retries = 3;                          // primitive, stable",
              "  useEffect(() => { sync(retries); }, [retries]);",
              "  return <p>{theme}</p>;",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Cleanup — the exit clause that makes effects safe",
        body: [
          "Every connection needs a disconnection, and React gives you exactly two moments to provide it: before the effect re-runs, and at unmount. Timers: clearInterval/clearTimeout in the cleanup. Subscriptions: unsubscribe. Listeners: removeEventListener. In-flight fetches: AbortController.abort(). The pattern to internalize: setup and cleanup are mirror images — whatever the setup acquires, the cleanup releases. If your setup has no release valve (a fire-and-forget mutation), ask whether an effect was even the right tool.",
          "Strict Mode makes the contract visible: in development it runs effect → cleanup → effect on mount, simulating a fast unmount/remount. A correct effect survives invisibly (connect, disconnect, connect — the user sees one connection); a leaky one doubles its connections or listeners and announces itself immediately. This is why 'my effect runs twice' is never the bug — the bug is an effect whose cleanup doesn't fully undo it. And when a re-render happens with unchanged deps, neither cleanup nor effect runs: the work order wasn't triggered. The three run timings (mount, dep-change, unmount) are the entire schedule — commit them to reflex.",
        ],
        code: [
          {
            file: "cleanup.tsx — mirror-image pairs",
            lang: "tsx",
            code: [
              "// timers:",
              "useEffect(() => {",
              "  const id = setInterval(() => tick(), 1000);",
              "  return () => clearInterval(id);",
              "}, []);",
              "",
              "// listeners:",
              "useEffect(() => {",
              "  const onKey = (e: KeyboardEvent) => { if (e.key === \"Escape\") close(); };",
              "  window.addEventListener(\"keydown\", onKey);",
              "  return () => window.removeEventListener(\"keydown\", onKey);",
              "}, [close]);",
              "",
              "// fetches:",
              "useEffect(() => {",
              "  const controller = new AbortController();",
              "  fetch(`/api/rooms/${roomId}`, { signal: controller.signal })",
              "    .then((r) => r.json())",
              "    .then(setRoom)",
              "    .catch((err) => {",
              "      if (err.name !== \"AbortError\") setError(err); // aborted = expected",
              "    });",
              "  return () => controller.abort();   // roomId changed OR unmounted:",
              "}, [roomId]);                        // kill the stale request",
            ].join("\n"),
            caption: "The fetch cleanup does double duty: it is both the leak fix (unmount) and the race fix (stale roomId) from P02-L6 — same mechanism, two enemies.",
          },
        ],
      },
      {
        heading: "The infinite-loop family — and the no-effect escapes",
        body: [
          "Two loops account for nearly all of them. Loop one, the self-feeder: the effect sets state it also depends on — useEffect(() => setItems(fix(items)), [items]) — where fix returns a fresh array every time, so deps 'change' every cycle: render → effect → new items → render → effect… The cure is structural: if fix is pure, compute it during render (const fixed = fix(items)) and delete the effect; the state never needed to exist. Loop two, the fresh-identity feeder: a dep that is a new object/array/function each render, re-triggering the effect that sets state. Cure: depend on primitives, memoize the identity, or restructure.",
          "The liberating counterpart: most proposed effects dissolve under two questions. 'Is this pure computation from state/props?' → move it to the render body (it re-computes automatically when inputs change — that's just rendering). 'Is this a response to a user action?' → move it to the event handler (handlers may set state, fetch, mutate the outside world freely). Effects remain for the residue: ongoing synchronization that must start/stop with conditions, independent of user gestures. The 'react to X' phrasing is the tell — React already reacts to state changes by re-rendering; you rarely need an effect to notice what the render already knows.",
        ],
        code: [
          {
            file: "loops.tsx — the self-feeder and its cure",
            lang: "tsx",
            code: [
              "// ✗ infinite loop: effect feeds its own dependency",
              "function TagList({ tags }: { tags: string[] }) {",
              "  const [sorted, setSorted] = useState(tags);",
              "  useEffect(() => {",
              "    setSorted(tags.toSorted());   // new array every time…",
              "  }, [tags, sorted]);             // …so this dep 'changes' forever",
              "  return <ul>{sorted.map((t) => <li key={t}>{t}</li>)}</ul>;",
              "}",
              "",
              "// ✓ the cure — it was never a synchronization job:",
              "function TagList({ tags }: { tags: string[] }) {",
              "  const sorted = tags.toSorted(); // pure: computed during render,",
              "  return <ul>{sorted.map((t) => <li key={t}>{t}</li>)}</ul>;",
              "}                                  // always in sync, zero effects",
              "",
              "// ✓ event-driven work belongs in handlers, not effects:",
              "function AddTag() {",
              "  const [tags, setTags] = useState<string[]>([]);",
              "  return (",
              "    <button onClick={() => setTags((t) => [...t, \"new\"])}>",
              "      add — no effect needed for a click",
              "    </button>",
              "  );",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Async effects — races, guards, and the title-tag classic",
        body: [
          "Async work inside an effect has one extra hazard on top of leaks: the component may move on before the answer arrives — roomId changed, or the user left. Two defenses, use both. The AbortController in the cleanup (previous section) cancels the request outright. A boolean guard (let stale = false; cleanup sets it true) covers APIs you can't abort. The guard check wraps EVERY state set after an await: if (stale) return; — because any await is a potential unmount or dep-change.",
          "The rite-of-passage example earns its place because it combines everything small: document.title. Setup writes it; cleanup restores the previous title (mirror image again — save what you found). Deps: only what's read. Under Strict Mode you'll see title flicker through the old value for an instant — proof the cleanup ran, exactly as designed. When this six-line effect feels inevitable, the contract is installed: from here, Phase 9's data loading is this same ceremony with better seating (and Phase 19's TanStack Query is the ceremony you no longer hand-write).",
        ],
        code: [
          {
            file: "title.tsx — the whole contract in six lines",
            lang: "tsx",
            code: [
              "function Page({ title }: { title: string }) {",
              "  useEffect(() => {",
              "    const previous = document.title;   // remember the outside world's state",
              "    document.title = title;            // synchronize outward",
              "    return () => { document.title = previous; }; // restore — mirror image",
              "  }, [title]);                          // re-run exactly when title changes",
              "  return <main>{title}</main>;",
              "}",
              "",
              "// The guard pattern for unabortable APIs:",
              "useEffect(() => {",
              "  let stale = false;",
              "  legacySubscribe(roomId, (msg) => {",
              "    if (!stale) onMessage(msg);   // never touch state after move-on",
              "  });",
              "  return () => { stale = true; legacyUnsubscribe(roomId); };",
              "}, [roomId]);",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The effect that fetches itself into a loop",
      wrong: [
        "function Profile({ userId }: { userId: string }) {",
        "  const [user, setUser] = useState<User | null>(null);",
        "  const [filters, setFilters] = useState({ role: \"all\" }); // new object…",
        "",
        "  useEffect(() => {",
        "    fetch(`/api/users/${userId}?role=${filters.role}`)",
        "      .then((r) => r.json())",
        "      .then(setUser);",
        "  }, [userId, filters]);   // …so this dep changes EVERY render →",
        "                           //   fetch → setUser → render → fetch…",
        "}",
        "",
        "// Bonus bug: no cleanup. Change userId mid-flight and the slow",
        "// first response lands last, overwriting the fresh one (the",
        "// P02-L6 race, wearing a React costume).",
      ].join("\n"),
      right: [
        "function Profile({ userId }: { userId: string }) {",
        "  const [user, setUser] = useState<User | null>(null);",
        "  const [role, setRole] = useState(\"all\");   // primitive dep — honest",
        "",
        "  useEffect(() => {",
        "    const controller = new AbortController();",
        "    fetch(`/api/users/${userId}?role=${role}`, { signal: controller.signal })",
        "      .then((r) => r.json())",
        "      .then(setUser)",
        "      .catch((err) => {",
        "        if (err.name !== \"AbortError\") console.error(err);",
        "      });",
        "    return () => controller.abort();  // kills stale fetches: loop AND race cured",
        "  }, [userId, role]);",
        "}",
        "",
        "// (Phase 19: this entire ceremony becomes",
        "//  useQuery({ queryKey: [\"user\", userId, role], queryFn }) —",
        "//  the library IS the cleanup + guard + cache. You still need",
        "//  to know the ceremony to read what it does for you.)",
      ].join("\n"),
      explain:
        "Two violations, one disaster: the dependency was an object with fresh identity each render (the effect re-triggered constantly), and its own setState re-rendered the component, manufacturing the next 'change'. The abort cleanup fixes the loop's visible symptom AND the hidden race — one mechanism, both enemies. And the deeper lesson: depend on the primitives you actually read. Objects in dep arrays should be deliberate (memoized or stable), never incidental.",
    },
    tryIt: [
      "Write the Room connection effect from section one and log connect/disconnect. Change roomId twice, then unmount. Count connections and disconnections (expected: 3 connects, 3 disconnects — plus the Strict Mode pair on mount).",
      "Break cleanup on purpose: delete the return statement. Change roomId five times quickly and count live connections in your log. This is what a leak feels like.",
      "Reproduce loop one: the self-feeding sorted-tags effect. Watch the console/render counter spin, then apply the render-phase cure and feel the silence.",
      "Build the title effect and observe Strict Mode's flicker. Explain out loud why the flicker is CORRECT behavior, then remove StrictMode and confirm it disappears.",
      "Race the fetch effect: set up a local delay (or network throttle), change userId rapidly, and confirm only the last response renders — first with the abort, then predict what happens without it.",
    ],
    challenge: {
      prompt:
        "Build an <AutosaveDraft title> component that practices the whole contract at once. It renders a <textarea> for a draft plus a status line ('Editing…', 'Saving…', 'Saved HH:MM:SS', 'Offline — will retry'). Behavior: (1) typing updates local state and marks the draft dirty; (2) an effect DEBOUNCES the save: 800ms after the last keystroke it calls await saveDraft(title, text) (an async function you stub with a setTimeout promise), showing 'Saving…' then 'Saved …'; (3) if the user keeps typing while a save is in flight, the in-flight save's completion must NOT overwrite the newer status (guard it); (4) unmounting mid-save must not set state ('cannot update unmounted' is the ancient smell — your cleanup prevents the attempt); (5) window 'online'/'offline' listeners flip the status and are removed on unmount. Constraints: dependency arrays list exactly what's read; every acquisition in setup is released in cleanup; no effect derives state that could be computed; predict the cleanup/run sequence for: mount → type 3 chars → pause → type again mid-save → unmount.",
      hints: [
        "Two effects, two systems: the debounce-save effect (deps: the draft text — the debounce timer IS the cleanup: clearTimeout on re-run) and the connectivity effect (deps: [] with add/removeEventListener pair).",
        "The in-flight guard is the stale boolean pattern: let cancelled = false inside the effect; cleanup sets it true; every setState after an await checks if (cancelled) return. This single flag handles BOTH the dirty-overwrite and the unmount cases.",
        "For the prediction: mount (connectivity listeners added) → typing (timer rescheduled 3×, one pending) → pause (timer fires: Saving… → await) → typing mid-save (new timer; the old save completes but its setState is guarded ONLY if its effect re-ran — think about whether text-change re-runs the save effect and resets the guard) → unmount (listeners removed, pending timer cleared, cancelled = true).",
      ],
      solution: [
        "import { useEffect, useState } from \"react\";",
        "",
        "// stub for the external system:",
        "function saveDraft(title: string, text: string): Promise<void> {",
        "  return new Promise((resolve) => setTimeout(resolve, 600));",
        "}",
        "",
        "export function AutosaveDraft({ title }: { title: string }) {",
        "  const [text, setText] = useState(\"\");",
        "  const [status, setStatus] = useState<{",
        "    kind: \"editing\" | \"saving\" | \"saved\" | \"offline\";",
        "    at?: number;",
        "  }>({ kind: \"editing\" });",
        "",
        "  // ——— system 1: debounced save (the timer IS the external system) ———",
        "  useEffect(() => {",
        "    if (status.kind === \"offline\") return;     // read status? it's in deps below — honestly",
        "    let cancelled = false;                     // guard: in-flight + unmount in one flag",
        "",
        "    const timerId = window.setTimeout(async () => {",
        "      setStatus((s) => (s.kind === \"offline\" ? s : { kind: \"saving\" }));",
        "      await saveDraft(title, text);",
        "      if (cancelled) return;                   // a newer effect superseded us,",
        "      setStatus({ kind: \"saved\", at: Date.now() }); // or the component is gone",
        "    }, 800);",
        "",
        "    // cleanup: cancel the pending save (re-run or unmount) —",
        "    // clearTimeout stops the NOT-yet-started save; `cancelled`",
        "    // neuters the ALREADY-started one. Both doors closed.",
        "    return () => {",
        "      cancelled = true;",
        "      clearTimeout(timerId);",
        "    };",
        "  }, [title, text, status.kind]);   // exactly what the effect reads",
        "",
        "  // ——— system 2: connectivity (listeners) ———",
        "  useEffect(() => {",
        "    const goOffline = () => setStatus({ kind: \"offline\" });",
        "    const goOnline = () => setStatus({ kind: \"editing\" });",
        "    window.addEventListener(\"offline\", goOffline);",
        "    window.addEventListener(\"online\", goOnline);",
        "    return () => {                        // mirror image: remove both",
        "      window.removeEventListener(\"offline\", goOffline);",
        "      window.removeEventListener(\"online\", goOnline);",
        "    };",
        "  }, []);",
        "",
        "  const label =",
        "    status.kind === \"saving\" ? \"Saving…\" :",
        "    status.kind === \"saved\" ? `Saved ${new Date(status.at!).toLocaleTimeString()}` :",
        "    status.kind === \"offline\" ? \"Offline — will retry\" :",
        "    \"Editing…\";",
        "",
        "  return (",
        "    <div>",
        "      <h2>{title}</h2>",
        "      <textarea",
        "        value={text}",
        "        onChange={(e) => {",
        "          setText(e.target.value);",
        "          setStatus((s) => (s.kind === \"saved\" ? { kind: \"editing\" } : s));",
        "        }}",
        "        aria-label={`${title} draft`}",
        "      />",
        "      <p role=\"status\">{label}</p>",
        "    </div>",
        "  );",
        "}",
        "",
        "/* Predicted sequence:",
        "   mount            → connectivity listeners added; save effect runs,",
        "                      text empty → timer pending (harmless first save;",
        "                      a real app would skip empty drafts)",
        "   type 3 chars     → effect re-runs 3×: each cleanup clears the old",
        "                      timer & flags cancelled; one timer survives",
        "   pause 800ms      → timer fires: 'saving' → await saveDraft…",
        "   type mid-save    → effect re-runs: previous effect's cleanup set",
        "                      cancelled = true → when its saveDraft resolves,",
        "                      the guarded setState is skipped. New timer pending.",
        "   unmount          → listeners removed; pending timer cleared;",
        "                      any in-flight save's completion is no-oped.",
        "   Every acquire released. Every async landing guarded. The contract held. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "An effect's job, by contract, is to…",
        options: ["Synchronize the component with an external system (network, timers, subscriptions, browser APIs)", "Run code after every render by default", "Replace event handlers", "Derive state from props"],
        answer: 0,
        explain: "If you can't name the external system, you probably don't need an effect. Pure computation belongs in render; user responses belong in handlers.",
      },
      {
        q: "useEffect(fn, [a, b]) re-runs when…",
        options: ["a or b changes by Object.is comparison", "The component re-renders for any reason", "Only on mount", "Whenever the parent renders"],
        answer: 0,
        explain: "The array is a factual list of what the effect reads, compared with Object.is. Fresh object identities count as changes even when contents match.",
      },
      {
        q: "The cleanup function runs…",
        options: ["Before each re-run caused by changed deps, and on unmount", "Only on unmount", "After every render", "Before the effect's first run"],
        answer: 0,
        explain: "Setup and cleanup are mirror images: whatever the setup acquires, the cleanup releases — both when conditions change and when the component leaves.",
      },
      {
        q: "Strict Mode's effect → cleanup → effect on mount exists to…",
        options: ["Expose effects whose cleanup doesn't fully undo the setup", "Warm a connection cache", "Double network traffic in production", "Test dependency arrays"],
        answer: 0,
        explain: "A correct effect survives the simulated remount invisibly. Leaky effects double their connections/listeners and confess in development.",
      },
      {
        q: "useEffect(() => setItems(items.toSorted()), [items]) loops because…",
        options: ["The effect feeds its own dependency: each run creates a new array identity, re-triggering itself", "toSorted is impure", "setState is forbidden in effects", "The array is too large"],
        answer: 0,
        explain: "The sorted list is pure derivation — compute it during render (const sorted = items.toSorted()) and delete the effect. The state never needed to exist.",
      },
      {
        q: "The two defenses against stale async results in an effect are…",
        options: ["AbortController in the cleanup and a stale-flag guard around post-await setStates", "setTimeout(0) and forceUpdate", "useMemo and useLayoutEffect", "Deeper dependency arrays"],
        answer: 0,
        explain: "Abort cancels the request when deps change or the component unmounts; the guard covers APIs you can't abort. Together they close both doors on yesterday's answer.",
      },
    ],
    flashcards: [
      { front: "Effect contract", back: "Synchronize with an EXTERNAL system: setup after commit, cleanup before re-run and on unmount. No external system → probably no effect." },
      { front: "Dependency array", back: "A factual list of what the effect reads. No array = every render; [] = mount-only; [a,b] = on change via Object.is. Never lie to quiet the lint." },
      { front: "Cleanup", back: "The mirror image of setup: release everything setup acquired (clearInterval, removeEventListener, unsubscribe, abort). Missing cleanup = leak." },
      { front: "Strict Mode remount", back: "Dev-only effect → cleanup → effect on mount. Correct effects survive invisibly; leaky ones double up and confess. Keep Strict Mode; fix the effect." },
      { front: "The infinite-loop family", back: "(1) Effect feeds its own deps with fresh identities — cure: derive during render. (2) Fresh object deps re-trigger always — cure: primitives, hoisting, or memoization." },
      { front: "No-effect escapes", back: "Pure computation → render body. User-action response → event handler. Effects are for ongoing sync that starts/stops with conditions." },
      { front: "Async effect guards", back: "AbortController.abort() in cleanup + a stale boolean checked after every await. Closes the race window and the unmounted-component window." },
      { front: "Object.is in deps", back: "Primitives compare by value; objects/arrays/functions by identity. A freshly-created object dep means 're-run every render' — depend on its primitive fields instead." },
    ],
    recap: [
      "Effects synchronize with external systems — name the system or delete the effect.",
      "Deps are a factual reads-list compared with Object.is; identity traps cause phantom re-runs.",
      "Cleanup mirrors setup: every acquire has a release, at re-run AND unmount.",
      "Self-feeding effects are misfiled derivations — compute them during render.",
      "Async effects get abort + guard. The title-tag ceremony is the template for Phase 9's data loading.",
    ],
    references: [
      { label: "react.dev — Synchronizing with Effects", url: "https://react.dev/learn/synchronizing-with-effects" },
      { label: "react.dev — You Might Not Need an Effect", url: "https://react.dev/learn/you-might-not-need-an-effect" },
      { label: "react.dev — Lifecycle of Reactive Effects", url: "https://react.dev/learn/lifecycle-of-reactive-effects" },
      { label: "react.dev — Removing Effect Dependencies", url: "https://react.dev/learn/removing-effect-dependencies" },
      { label: "react.dev — useEffect", url: "https://react.dev/reference/react/useEffect" },
    ],
    nextBridge:
      "Synchronization is disciplined — but two questions remain in the toolbox: where do values live that must survive renders WITHOUT causing them (refs), and how do deeply-shared values (theme, auth) skip the prop chain (context)? Next lesson adds useRef, useContext, and useReducer — the rest of the state-management core — before memoization tunes it.",
  },
];

export const LESSON_CONTENT_P5: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P5.map((l) => [l.id, l]),
);
