import type { LessonContent } from "./types";

/**
 * Pass 008 bounded content batch: Phase 3 opener (L1–L2).
 * TypeScript for the full stack. Every lesson fulfills the full quality
 * contract. No placeholders.
 */
export const LESSONS_P3: LessonContent[] = [
  {
    id: "p03-l1",
    phaseId: "p03",
    title: "Inference, Annotations & Objects",
    level: "Foundation",
    minutes: 40,
    summary:
      "TypeScript is JavaScript with a meticulous proofreader that reads every line before the code ever runs. This lesson installs the two ways types enter your code — inference (the compiler figuring it out) and annotations (you telling it) — then covers the shapes you'll use daily: objects, arrays, optional and readonly fields, literal types, and the practical type-vs-interface rule. By the end, the Todo app's state is fully typed.",
    prerequisites: ["p02-l3 — arrays, objects & immutability", "p02-l6 — the vanilla Todo app, reviewed"],
    objectives: [
      "Predict when inference is sufficient and when an explicit annotation is required (empty arrays, function returns, public API).",
      "Type objects with required, optional (?) and readonly fields, and nested shapes.",
      "Type arrays and the array-of-objects patterns that dominate real code.",
      "Apply the practical type-vs-interface rule and defend it in a code review.",
      "Use literal types and const assertions to lock values down.",
      "Type the Todo app's state so a misspelled field is a compile error, not a runtime bug.",
    ],
    simple:
      "JavaScript lets you put anything in any box and only finds out it's wrong when the code runs — sometimes in front of a user. TypeScript adds a proofreader: before anything executes, it reads every line and checks that each box only ever holds what its label allows. Most of the time it infers the label itself (const n = 5 is obviously a number), so you write almost no extra code. You only speak up — annotate — where the proofreader genuinely can't guess: an empty array, a function's return, a variable that starts null. The result: entire categories of bugs become impossible to even type.",
    why:
      "Every serious codebase in this course — the React frontend, the NestJS API, the Prisma layer — is TypeScript, because at scale 'it worked on my machine' isn't a quality bar. Types are how a team communicates intent across files and months: a function's signature tells you what it needs and returns without reading its body. The compiler is the only teammate that reviews every single line you ever write, never gets tired, and never has a bad day. Learning to let it help — inferring by default, annotating with purpose — is the single highest-leverage skill in this stage.",
    mentalModel: {
      title: "The proofreader with red ink",
      body: "Imagine a proofreader who sits between you and the printing press. In JavaScript, there is no proofreader — your manuscript goes straight to print and typos reach readers. TypeScript inserts one who reads everything first and marks problems in red ink BEFORE a single copy is made. The catch: the proofreader can only check what it can understand. When your writing is ambiguous, it either makes a reasonable guess (inference) or leaves a blank and asks you to fill it in (annotation). The skill isn't memorizing rules — it's learning what the proofreader can already figure out on its own, and only intervening where it can't.",
    },
    sections: [
      {
        heading: "Inference first — the compiler already knows most of it",
        body: [
          "When you write const count = 5, TypeScript infers count: number — you wrote zero type code. It infers from initializers, from return values, from how a variable is used. Good TypeScript is mostly inference: you annotate at the EDGES (function parameters, public returns, empty collections) and let the middle stay quiet. Over-annotating (const n: number = 5) is noise that hides the signal and rots when the value's type legitimately changes.",
          "There are a few places inference has nothing to work with, and silence becomes a trap. const items = [] is inferred as never[] — TypeScript literally cannot know what will go in. A variable declared let user; then assigned later is any. And a function with several returns but no annotation gets a union the caller may not expect. The rule of thumb: if the type is obvious from the line itself, say nothing; if you have to scroll or guess, write it down.",
        ],
        code: [
          {
            file: "inference.ts — let the proofreader do its job",
            lang: "ts",
            code: [
              "// inferred — no annotation needed, and none wanted:",
              "const count = 5;              // number",
              "const label = \"done\";        // \"done\" (a literal! more below)",
              "const flags = [true, false];  // boolean[]",
              "const doubled = flags.map((f) => !f); // boolean[] — flows through",
              "",
              "function area(width: number, height: number) {",
              "  return width * height;      // return inferred as number — fine",
              "}",
              "",
              "// ✗ inference has nothing to go on:",
              "// const items = [];          // never[] — you must say what goes in",
              "// let user;                  // any — the proofreader gave up",
              "",
              "// ✓ speak up only where it can't guess:",
              "const items: string[] = [];",
              "let user: { name: string } | null = null;",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Objects — the shape of almost everything",
        body: [
          "An object type is a contract listing its fields and each field's type. Required fields are non-negotiable; optional fields (?) may be absent; readonly fields can be read but not reassigned — the compiler's way of enforcing the immutability discipline from P02-L3 at the type level. Nesting composes naturally: a task has an assignee, which is itself an object type.",
          "Two behaviors surprise beginners. First, object types are structural — TypeScript cares about SHAPE, not name: any value with the right fields satisfies the type, even if it came from somewhere else entirely. That's what makes typing API responses painless. Second, excess-property checking applies only to fresh object literals: passing { id: 1, title: 'x', typo: true } directly where a Task is expected errors, but assigning the same object through a variable first slips through. Freshness is the proofreader being extra careful at the point of creation.",
        ],
        code: [
          {
            file: "shapes.ts — contracts for data",
            lang: "ts",
            code: [
              "type Task = {",
              "  readonly id: number;      // set once, never reassigned",
              "  title: string;",
              "  done: boolean;",
              "  dueDate?: string;         // optional — may be undefined",
              "  tags: string[];",
              "  assignee?: {",
              "    name: string;",
              "    email: string;",
              "  };",
              "};",
              "",
              "const t: Task = {",
              "  id: 1,",
              "  title: \"Ship the API\",",
              "  done: false,",
              "  tags: [\"backend\"],",
              "};",
              "",
              "// t.id = 2;        ✗ error: readonly",
              "t.done = true;      // ✓ mutable field",
              "",
              "// structural: shape matches, name doesn't matter",
              "function summarize(task: Task) { return task.title; }",
              "const fromApi = { id: 9, title: \"x\", done: false, tags: [] };",
              "summarize(fromApi); // ✓ — it has the shape of a Task",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Arrays, optionals, and the undefined trap",
        body: [
          "Arrays are typed as element[] (or Array<element>). An array of objects is just object-type[] — the workhorse of every list endpoint you'll ever type. The subtle part is what OPTIONAL really means: dueDate?: string is shorthand for dueDate: string | undefined, so reading it gives you string | undefined and you must handle the absent case before using it as a string. The compiler forces that handling — which is exactly the null-discipline from P02-L1, now enforced automatically.",
          "Optional chaining (?.) and nullish coalescing (??) remain your friends, but now they're load-bearing: the type system tracks that a value may be undefined and refuses to let you call .toUpperCase() on it until you've narrowed. Turn on noUncheckedIndexedAccess later (P03-L5) and even arr[0] becomes T | undefined, because index access can genuinely miss — TypeScript can be made to model reality as strictly as you want.",
        ],
        code: [
          {
            file: "arrays.ts",
            lang: "ts",
            code: [
              "type Task = { id: number; title: string; done: boolean; dueDate?: string };",
              "",
              "const tasks: Task[] = [",
              "  { id: 1, title: \"one\", done: false },",
              "  { id: 2, title: \"two\", done: true, dueDate: \"2026-03-01\" },",
              "];",
              "",
              "const open = tasks.filter((t) => !t.done);   // Task[]",
              "const titles = open.map((t) => t.title);     // string[]",
              "",
              "const first = tasks[0];",
              "// first.dueDate.toUpperCase();  ✗ string | undefined has no toUpperCase",
              "first.dueDate?.toUpperCase();    // ✓ string | undefined, handled",
              "const when = first.dueDate ?? \"no date\"; // ✓ string",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "type vs interface — the practical rule",
        body: [
          "type and interface overlap heavily; both name object shapes and both are structural. The practical rule that survives real codebases: use type by default, reach for interface when you're declaring the public contract of something others will EXTEND (a plugin, a library option bag) — because interface supports declaration merging and extends reads more naturally for OOP-shaped APIs. In this course, type everywhere is a defensible, consistent choice, and consistency beats cleverness.",
          "One genuine capability difference matters later: interfaces can be re-opened and merged (declaring interface Window twice adds fields), which is how libraries augment globals. type aliases can represent things interfaces cannot — unions, tuples, mapped types — so type is the more general tool. When your team's style guide disagrees, follow the guide; the rule is 'be consistent', not 'be right'.",
        ],
        code: [
          {
            file: "type-vs-interface.ts",
            lang: "ts",
            code: [
              "// type: the general-purpose tool (this course's default)",
              "type Status = \"open\" | \"done\";        // unions: type only",
              "type Pair = [string, number];          // tuples: type only",
              "type Task = { id: number; title: string };",
              "",
              "// interface: best for extendable public contracts",
              "interface PluginOptions {",
              "  verbose?: boolean;",
              "}",
              "interface MyPluginOptions extends PluginOptions {",
              "  endpoint: string;",
              "}",
              "",
              "// both are structural — interchangeable for plain shapes:",
              "const ok: Task = { id: 1, title: \"x\" };",
              "const alsoOk: { id: number; title: string } = ok;",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Literal types & const assertions — lock it down",
        body: [
          "const label = \"done\" is inferred as the LITERAL type \"done\", not string — TypeScript knows it can never be anything else, because const can't be reassigned. Literal types are the atoms of precise modeling: a Status of \"open\" | \"done\" is a type with exactly two inhabitants, and assigning anything else is a compile error. This is how you encode business rules (a priority is low|medium|high, never 'urgentt') directly in the type system.",
          "const assertions (as const) freeze an entire structure: every property becomes readonly and every value becomes a literal. This turns a config object into a precise type you can derive other types from (keyof, P03-L4). Without as const, const config = { mode: \"dark\" } widens mode to string and the precision evaporates. The pattern — declare a literal source of truth with as const, derive types from it — is the foundation of typed config, typed routes, and typed status machines.",
        ],
        code: [
          {
            file: "literals.ts",
            lang: "ts",
            code: [
              "let widened = \"done\";        // string (let can change)",
              "const literal = \"done\";     // \"done\" (const can't)",
              "",
              "type Status = \"open\" | \"done\";",
              "const s: Status = \"open\";    // ✓",
              "// const bad: Status = \"urgent\";  ✗ not in the union",
              "",
              "type Priority = \"low\" | \"medium\" | \"high\";",
              "function escalate(p: Priority): Priority {",
              "  return p === \"low\" ? \"medium\" : \"high\"; // returns are checked too",
              "}",
              "",
              "const config = { mode: \"dark\", retries: 3 } as const;",
              "// config.mode is the literal \"dark\", config is deeply readonly",
              "// type Mode = typeof config.mode;  // \"dark\"",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Typing the Todo app — the payoff",
        body: [
          "The moment of truth: point the proofreader at the Phase 2 Todo app. The state becomes let todos: Task[] = []; the render function becomes (tasks: Task[]) => void; the event handler's dataset read (a string) gets an explicit Number() with the type proving why. Immediately, two classes of Phase 2 bug become un-typeable: misspelling a field (t.titel), and treating the dataset string id as a number without converting.",
          "This is the value proposition in one sitting: you didn't rewrite logic, you added shapes. The refactor from JS to TS touched signatures, not behavior — and every place the compiler complained was either a genuine latent bug or a place the code was vaguer than it pretended to be. P03-L6 repeats this as a full guided migration lab; today you've seen the whole arc.",
        ],
        code: [
          {
            file: "todo-types.ts — state with a spine",
            lang: "ts",
            code: [
              "type Task = {",
              "  readonly id: number;",
              "  title: string;",
              "  done: boolean;",
              "};",
              "",
              "let todos: Task[] = [];",
              "let nextId = 1;",
              "",
              "function addTodo(title: string): void {",
              "  todos = [...todos, { id: nextId++, title, done: false }];",
              "}",
              "",
              "function toggle(id: number): void {",
              "  todos = todos.map((t) =>",
              "    t.id === id ? { ...t, done: !t.done } : t",
              "  );",
              "}",
              "",
              "// const el = document.querySelector(\"li\");",
              "// const id: number = el.dataset.id;   ✗ string is not number",
              "// const id = Number(el.dataset.id);   ✓ the type made you say it",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "Annotating the obvious, silencing the ambiguous",
      wrong: [
        "const count: number = 5;            // noise — inference knew",
        "const name: string = \"Ada\";         // noise — inference knew",
        "",
        "const items = [];                    // never[] — SHOULD have spoken up",
        "items.push(\"hello\");                 // ✗ error, but the error message",
        "                                     //   blames the push, not the declaration",
        "",
        "function getUser() {                 // returns any|union — caller has",
        "  if (Math.random() > 0.5) return { name: \"Ada\" };",
        "  return null;                       //   to read the BODY to know",
        "}",
      ].join("\n"),
      right: [
        "const count = 5;                     // inferred — clean",
        "const name = \"Ada\";                 // inferred — clean",
        "",
        "const items: string[] = [];          // annotated at the edge —",
        "items.push(\"hello\");                 // ✓ intent stated once, enforced everywhere",
        "",
        "function getUser(): { name: string } | null {",
        "  if (Math.random() > 0.5) return { name: \"Ada\" };",
        "  return null;",
        "}",
        "// ✓ callers see the contract in the signature, not the body",
      ].join("\n"),
      explain:
        "The instinct is backwards: beginners annotate the obvious (const n: number = 5) because it feels productive, and leave the ambiguous spots silent because inference 'probably works'. But the obvious needs no help — the annotation is noise that rots — while the ambiguous spots are exactly where silence becomes never[], any, or a surprise union. Annotate at the edges (parameters, returns, empty collections, public API); let the middle infer.",
    },
    tryIt: [
      "Hover over every variable in a file of yours (VS Code shows inferred types). Count how many annotations you could delete without the compiler complaining.",
      "Write const items = [] and push three strings; read the error. Then annotate it string[] and watch the error move from the push sites to nothing. Say the rule out loud.",
      "Define a Task type with a readonly id, an optional dueDate, and a tags array. Deliberately violate each rule (reassign id, skip title, pass a number tag) and read the three distinct error messages.",
      "Take the literal \"done\" and try to assign it to a variable of type \"open\" | \"done\" — fine. Now widen it with let and try again. Explain why const preserved precision.",
      "Add as const to a small config object and hover typeof config — note every value became a literal and every key readonly. Remove as const and note what widened.",
    ],
    challenge: {
      prompt:
        "Type a 'reading list' module. Requirements: (1) a Book type with readonly isbn (string), title, author, an optional publishedYear (number), and a required tags array of a Tag type where Tag is a literal union of \"fiction\" | \"nonfiction\" | \"technical\"; (2) a library: Book[] with at least two entries, one omitting publishedYear; (3) a function addBook(library: readonly Book[], book: Book): Book[] that returns a NEW array (immutability, enforced by the readonly parameter) — prove the input parameter cannot be mutated; (4) a function titlesByTag(books: Book[], tag: Tag): string[] using filter+map; (5) demonstrate that addBook(library, { isbn: \"1\", title: \"x\", author: \"y\", tags: [\"poetry\"] }) is a COMPILE error, and quote the exact part of the message that names the problem.",
      hints: [
        "readonly Book[] as the parameter type makes .push/.splice on the parameter a compile error — that's the immutability enforcement. Return a fresh [...library, book].",
        "Tag must be its own named literal union, then used in Book.tags: Tag[] — so a stray \"poetry\" fails against Tag, not against a loose string.",
        "For the compile-error demonstration, leave the bad call in a comment with the error text, or in a scratch file — the point is reading the message: it will say something like '\"poetry\" is not assignable to type Tag'.",
      ],
      solution: [
        "type Tag = \"fiction\" | \"nonfiction\" | \"technical\";",
        "",
        "type Book = {",
        "  readonly isbn: string;",
        "  title: string;",
        "  author: string;",
        "  publishedYear?: number;      // optional: string | undefined on read",
        "  tags: Tag[];",
        "};",
        "",
        "const library: Book[] = [",
        "  { isbn: \"978-0-13-468599-1\", title: \"The Pragmatic Programmer\",",
        "    author: \"Hunt & Thomas\", publishedYear: 1999, tags: [\"technical\"] },",
        "  { isbn: \"978-0-06-112008-4\", title: \"To Kill a Mockingbird\",",
        "    author: \"Harper Lee\", tags: [\"fiction\"] },   // no publishedYear — OK",
        "];",
        "",
        "// readonly param = the compiler blocks in-place mutation:",
        "function addBook(library: readonly Book[], book: Book): Book[] {",
        "  // library.push(book);   ✗ error: property 'push' does not exist on",
        "  //                              readonly Book[]  (exactly the point)",
        "  return [...library, book];  // fresh array, input untouched",
        "}",
        "",
        "function titlesByTag(books: Book[], tag: Tag): string[] {",
        "  return books",
        "    .filter((b) => b.tags.includes(tag))",
        "    .map((b) => b.title);",
        "}",
        "",
        "const updated = addBook(library, {",
        "  isbn: \"978-0-262-03384-8\",",
        "  title: \"Introduction to Algorithms\",",
        "  author: \"Cormen et al.\",",
        "  publishedYear: 1990,",
        "  tags: [\"technical\"],",
        "});",
        "",
        "console.log(titlesByTag(updated, \"technical\").length); // 2",
        "",
        "// ✗ THE COMPILE ERROR, verbatim intent:",
        "// addBook(library, { isbn: \"1\", title: \"x\", author: \"y\",",
        "//                    tags: [\"poetry\"] });",
        "// error TS2322: Type '\"poetry\"' is not assignable to type 'Tag'.",
        "//   — the message names the exact field (tags) and the exact",
        "//     offending literal. Caught before any code ever ran.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "const n = 5 — what is n's type?",
        options: ["number (inferred)", "5 (literal)", "any", "It needs an annotation"],
        answer: 0,
        explain: "A numeric const infers to number (numeric literals widen for let/const numbers in most positions; string consts keep their literal). No annotation needed — adding one is noise.",
      },
      {
        q: "const items = [] is inferred as…",
        options: ["never[] — you must annotate what goes in", "any[]", "unknown[]", "It's a runtime error"],
        answer: 0,
        explain: "With no initializer context, the element type is never. The fix is to annotate: const items: string[] = []. This is the canonical 'speak up at the edge' case.",
      },
      {
        q: "A field declared dueDate?: string, when read, has type…",
        options: ["string | undefined", "string", "null", "optional"],
        answer: 0,
        explain: "Optional (?) means 'may be absent', which is string | undefined. The compiler forces you to handle the undefined case before using it as a string.",
      },
      {
        q: "The practical type-vs-interface rule is…",
        options: ["type by default; interface for extendable public contracts", "interface always", "type only for primitives", "They're unrelated features"],
        answer: 0,
        explain: "type is the general tool (unions, tuples, mapped types). interface shines for contracts others extend (declaration merging, extends). Consistency matters most.",
      },
      {
        q: "What does as const do to { mode: \"dark\" }?",
        options: ["Makes mode the literal \"dark\" and the object deeply readonly", "Makes it a class", "Widens mode to string", "Freezes it at runtime only"],
        answer: 0,
        explain: "as const narrows every value to its literal and marks everything readonly — turning a config object into a precise type you can derive from (keyof, typeof).",
      },
      {
        q: "TypeScript object types are 'structural'. That means…",
        options: ["Any value with the right shape satisfies the type, regardless of its name", "Objects must be created with new", "Types are checked by class name", "Shapes can't nest"],
        answer: 0,
        explain: "TypeScript matches by shape, not by declaration name — so API responses and ad-hoc objects satisfy your types as long as the fields line up.",
      },
    ],
    flashcards: [
      { front: "Type inference", back: "The compiler deduces types from initializers, returns, and usage. Default to it; annotate only where it genuinely can't guess." },
      { front: "When to annotate", back: "Empty arrays, function parameters, public return types, vars starting null/undefined — the edges. Never const n: number = 5." },
      { front: "Optional field (?)", back: "field?: T is shorthand for field: T | undefined. Reading it forces you to handle the absent case." },
      { front: "readonly", back: "Blocks reassignment of that field at the type level — immutability (P02-L3) enforced by the compiler, not by discipline." },
      { front: "type vs interface", back: "type is general (unions/tuples/mapped). interface for extendable public contracts (merging, extends). Use type by default; be consistent." },
      { front: "Literal type", back: "A type with a single value: \"done\", 200, true. The atom of precise modeling; unions of literals encode business rules." },
      { front: "as const", back: "Freezes a value: all properties readonly, all values literal. Turns config objects into precise derivable types." },
      { front: "Structural typing", back: "Types are satisfied by shape, not name. Any object with the right fields is a Task — which makes typing API data natural." },
    ],
    recap: [
      "TypeScript = JavaScript plus a proofreader that checks everything before it runs.",
      "Let inference carry the load; annotate at the edges where it can't guess.",
      "Objects are structural contracts: required, optional (?), and readonly fields.",
      "type by default, interface for extendable contracts, and be consistent.",
      "Literal types + as const lock values down; the Todo app's state is now un-mistypable.",
    ],
    references: [
      { label: "TypeScript — Everyday Types", url: "https://www.typescriptlang.org/docs/handbook/2/everyday-types.html" },
      { label: "TypeScript — Type Inference", url: "https://www.typescriptlang.org/docs/handbook/type-inference.html" },
      { label: "TypeScript — Narrowing", url: "https://www.typescriptlang.org/docs/handbook/2/narrowing.html" },
      { label: "TypeScript — Literal Types", url: "https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#literal-types" },
      { label: "TypeScript — const assertions", url: "https://www.typescriptlang.org/docs/handbook/release-notes/typescript-3-4.html#const-assertions" },
    ],
    nextBridge:
      "Your data has shapes and your shapes have spines. Next: the modeling superpower — unions, narrowing, and discriminated unions. You'll learn to say 'this is a success OR an error, and the compiler will make you handle both' — the pattern that becomes the course's default way to model states, events, and API results.",
  },
  {
    id: "p03-l2",
    phaseId: "p03",
    title: "Unions, Narrowing & Literal Types",
    level: "Foundation",
    minutes: 40,
    summary:
      "Real values are rarely 'just a string' — they're a success or an error, an open task or a done one, a circle or a square. Unions let you say exactly that, and narrowing is how the compiler tracks which case you're in. This lesson installs discriminated unions as the course's default modeling tool, the never type and exhaustiveness checking, and builds a typed status machine. By the end, impossible states are unrepresentable.",
    prerequisites: ["p03-l1 — inference, annotations & objects"],
    objectives: [
      "Model 'this OR that' data with union types instead of optional-field soup.",
      "Narrow unions with typeof, in, truthiness, and equality checks — and explain why the compiler trusts them.",
      "Design discriminated unions with a literal discriminant field as the course default.",
      "Use never and exhaustive switch statements so adding a case breaks compilation everywhere it must.",
      "Type a status machine (open → done) so illegal transitions are compile errors.",
    ],
    simple:
      "An optional field (dueDate?: string) quietly allows nonsense: an object can be 'done' yet have no completion date, or 'open' yet have one. A union says it plainly instead: this value is an OpenTask OR a DoneTask — two different shapes, and the compiler knows which one you're holding by looking at one field. Narrowing is just the compiler paying attention: inside if (task.status === \"done\"), it knows task is the DoneTask shape and lets you read completionDate safely. You stop defending against impossible states because the type system refuses to represent them.",
    why:
      "Discriminated unions are the single most-used modeling tool in professional TypeScript, and this course leans on them everywhere: API results ({ ok: true, data } | { ok: false, error }), Redux/TanStack states, NestJS exception shapes, Realtime events. The alternative — one bag of optional fields with runtime if-checks — is where 'undefined is not a function' bugs breed, because the type system can't help you. Exhaustiveness checking turns a forgotten case from a 2 a.m. outage into a red squiggle at the moment you add the new case. Learning this pattern now means every state you model from here on is self-defending.",
    mentalModel: {
      title: "Shipping labels with a carrier field",
      body: "A package arrives with a label that says CARRIER: FedEx or USPS or DHL. You don't open it and guess — you read the carrier, and that one field tells you exactly how to unpack it: FedEx needs a signature, USPS goes in the mailbox, DHL needs a customs form. A discriminated union is that label: one literal field (the discriminant) tells the compiler which shape you hold, and inside the matching branch it hands you the right 'unpacking instructions'. Exhaustiveness is the rule 'every carrier on the label must have a procedure' — add a new carrier and the compiler lists every desk that now needs a procedure for it.",
    },
    sections: [
      {
        heading: "Unions — say it's an OR, mean it",
        body: [
          "A union A | B is a type whose values may be an A or a B. The compiler then requires you to handle both before doing anything specific: you can't call .toFixed() on number | string because a string might be in there. Unions replace the common sin of optional-field soup — an object with maybeThis?: X and maybeThat?: Y where the combinations aren't checked — with two honest, complete shapes.",
          "The payoff is visible immediately: function render(result: Success | Failure) cannot accidentally read result.data when result is a Failure, because the Failure shape has no data field. The error you'd have shipped becomes a compile error. Unions also compose with literals (Status = \"open\" | \"done\") and with arrays (Array<Task | ArchivedTask>), making them the connective tissue of precise modeling.",
        ],
        code: [
          {
            file: "unions.ts — optional soup vs honest OR",
            lang: "ts",
            code: [
              "// ✗ optional soup: allows (done && !completionDate), etc.",
              "// type TaskSoup = { done: boolean; completionDate?: string };",
              "",
              "// ✓ a union: exactly two complete, honest shapes",
              "type OpenTask = { status: \"open\"; title: string };",
              "type DoneTask = { status: \"done\"; title: string; completionDate: string };",
              "type Task = OpenTask | DoneTask;",
              "",
              "function describe(task: Task): string {",
              "  // task.completionDate;  ✗ error — an OpenTask has no such field",
              "  return task.status === \"done\"",
              "    ? `Done on ${task.completionDate}`   // ✓ narrowed to DoneTask",
              "    : `Open: ${task.title}`;",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Narrowing — the compiler pays attention",
        body: [
          "Narrowing is control-flow analysis: inside a branch whose condition rules out some members of a union, TypeScript narrows the type to the survivors. The standard tools are typeof (\"string\" | \"number\"), truthiness (filters out null | undefined), the in operator (\"field\" in obj), instanceof (class hierarchies), and equality on a discriminant field. Each is a fact the compiler can verify, so it trusts the branch.",
          "Two habits make narrowing reliable. First, prefer a single discriminant field checked with === — it reads clearly and the compiler tracks it perfectly, even when you destructure or assign to a variable in some positions. Second, return or throw early: the guard-clause style (if (typeof x === \"string\") return …) leaves the rest of the function with the narrowed type, no nesting. If the compiler ever refuses to narrow, you're usually testing something it can't tie to the union — restructure around a discriminant.",
        ],
        code: [
          {
            file: "narrowing.ts",
            lang: "ts",
            code: [
              "function format(value: string | number): string {",
              "  if (typeof value === \"string\") {",
              "    return value.trim();        // string here",
              "  }",
              "  return value.toFixed(2);      // number here — the else is implied",
              "}",
              "",
              "function firstWord(input: string | null | undefined): string {",
              "  if (!input) return \"\";        // truthiness strips null | undefined",
              "  return input.split(\" \")[0];    // string here",
              "}",
              "",
              "type Circle = { kind: \"circle\"; radius: number };",
              "type Square = { kind: \"square\"; side: number };",
              "function area(shape: Circle | Square): number {",
              "  if (\"radius\" in shape) return Math.PI * shape.radius ** 2;",
              "  return shape.side ** 2;        // only Square is left",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Discriminated unions — the course default",
        body: [
          "A discriminated union is a set of object types that all share one literal field — the discriminant — with a different value in each. status: \"open\" vs status: \"done\", kind: \"circle\" vs \"square\", ok: true vs ok: false. The pattern is so reliable because the compiler narrows on the discriminant with zero ambiguity, and a switch over it reads like documentation.",
          "This is the shape of every serious API result, and the course standard for them: type Result<T> = { ok: true; data: T } | { ok: false; error: string }. Callers write if (result.ok) { …result.data… } else { …result.error… } and cannot touch the wrong side. When you reach TanStack Query, NestJS responses, and Realtime events, you'll recognize this exact skeleton under different names — which is why it's worth making automatic now.",
        ],
        code: [
          {
            file: "result.ts — the shape of every API call to come",
            lang: "ts",
            code: [
              "type Result<T> = { ok: true; data: T } | { ok: false; error: string };",
              "",
              "async function loadTasks(): Promise<Result<Task[]>> {",
              "  try {",
              "    const res = await fetch(\"/api/tasks\");",
              "    if (!res.ok) return { ok: false, error: `HTTP ${res.status}` };",
              "    return { ok: true, data: await res.json() };",
              "  } catch (err) {",
              "    return { ok: false, error: err instanceof Error ? err.message : \"network\" };",
              "  }",
              "}",
              "",
              "const result = await loadTasks();",
              "if (result.ok) {",
              "  render(result.data);           // ✓ Task[] — error is unreachable here",
              "} else {",
              "  showBanner(result.error);      // ✓ string — data is unreachable here",
              "}",
            ].join("\n"),
            caption: "The ok discriminant makes the two sides mutually exclusive — you literally cannot read data in the error branch.",
          },
        ],
      },
      {
        heading: "never & exhaustiveness — make forgetting impossible",
        body: [
          "never is the type with no values — the bottom of the type system. Its killer use is exhaustiveness checking: in a switch over a discriminant with a default that assigns the leftover to never, adding a new union member makes that default fail to compile, because the leftover is no longer never. The compiler then points at every switch that now has an unhandled case. A forgotten handler becomes a build failure, at the exact moment you add the case.",
          "The assertNever helper is three lines you'll paste forever: function assertNever(x: never): never { throw new Error(\"unhandled: \" + x); }. It satisfies the type and, if reality ever disagrees with the types at runtime, throws with the offending value in the message. Exhaustiveness is how large codebases evolve safely — it converts 'did we handle the new status everywhere?' from a hope into a checklist the compiler runs for you.",
        ],
        code: [
          {
            file: "exhaustive.ts",
            lang: "ts",
            code: [
              "type Status = \"open\" | \"in-progress\" | \"done\";",
              "",
              "function assertNever(x: never): never {",
              "  throw new Error(`Unhandled status: ${x}`);",
              "}",
              "",
              "function badge(s: Status): string {",
              "  switch (s) {",
              "    case \"open\":        return \"grey\";",
              "    case \"in-progress\": return \"amber\";",
              "    case \"done\":        return \"green\";",
              "    default:            return assertNever(s);",
              "  }",
              "}",
              "",
              "// Add \"blocked\" to Status and the build breaks HERE,",
              "// because s in the default is now \"blocked\", not never —",
              "// the compiler points at every switch that needs the new case.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "A typed status machine — open → done, and only that",
        body: [
          "A status machine is a set of states plus the transitions allowed between them. Modeled naively (status: string plus if-checks), nothing stops task.status = \"don\" or a done-task moving back to open. Modeled as a discriminated union plus transition functions that return a NEW union member, the rules live in the types: only openTask can become done, and the function that does it is the only place a completionDate is ever minted.",
          "This is the seed of everything Phase 42 (the Inventory capstone) does with order lifecycles, and what P02-L4's TODO toggle already hinted at. The transition function takes an OpenTask and returns a DoneTask — the signature IS the business rule. Any caller holding a DoneTask who tries to 'complete' it again won't find a function that accepts it. Illegal states aren't guarded; they're unrepresentable.",
        ],
        code: [
          {
            file: "status-machine.ts",
            lang: "ts",
            code: [
              "type OpenTask = { status: \"open\"; title: string };",
              "type DoneTask = { status: \"done\"; title: string; completionDate: string };",
              "type Task = OpenTask | DoneTask;",
              "",
              "// the signature encodes the rule: only open tasks complete",
              "function complete(task: OpenTask, on: string): DoneTask {",
              "  return { status: \"done\", title: task.title, completionDate: on };",
              "}",
              "",
              "// and reopening only applies to done tasks:",
              "function reopen(task: DoneTask): OpenTask {",
              "  return { status: \"open\", title: task.title };",
              "}",
              "",
              "const t: Task = { status: \"open\", title: \"Ship\" };",
              "// complete(t, \"2026-03-01\");  ✗ until narrowed — Task might be DoneTask",
              "if (t.status === \"open\") {",
              "  const d = complete(t, \"2026-03-01\"); // ✓ t narrowed to OpenTask",
              "  // complete(d, \"2026-03-01\");  ✗ d is a DoneTask — won't type",
              "}",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "One bag of optionals, checked at runtime",
      wrong: [
        "type ApiResult = {",
        "  data?: Task[];       // present on success",
        "  error?: string;      // present on failure",
        "};",
        "",
        "function handle(r: ApiResult) {",
        "  render(r.data!.map((t) => t.title));  // the ! is a lie —",
        "}                                       // on failure r.data is undefined",
        "                                        // and this crashes at runtime",
      ].join("\n"),
      right: [
        "type ApiResult =",
        "  | { ok: true;  data: Task[] }",
        "  | { ok: false; error: string };",
        "",
        "function handle(r: ApiResult) {",
        "  if (!r.ok) {",
        "    showBanner(r.error);   // data unreachable here",
        "    return;",
        "  }",
        "  render(r.data.map((t) => t.title)); // ✓ r.data is Task[]",
        "}",
      ].join("\n"),
      explain:
        "The optional-bag version makes the IMPOSSIBLE representable: an object with both data and error, or neither. So every consumer must re-check at runtime, and the ! non-null assertion is where someone eventually lies and ships a crash. The discriminated union makes the two outcomes mutually exclusive at the type level — the guard isn't a runtime hope, it's a compile-time fact, and the compiler narrows r.data for you.",
    },
    tryIt: [
      "Define Circle | Square with a kind discriminant and write an area function using a switch. Hover over shape inside each case — note the narrowed type.",
      "Take a function of yours that checks if (x === null || x === undefined) and rewrite it with a truthiness guard; confirm the compiler narrows after the early return.",
      "Build the Result<T> type and write a loader that returns both branches. Then, in the error branch, try to read .data and read the error that proves it's unreachable.",
      "Copy the exhaustive badge() function, add \"blocked\" to the Status union, and watch the default case turn red. Read the message — it names the unhandled member.",
      "Write complete() and reopen() for a 3-state machine (open → in-progress → done). Make an illegal transition (done → in-progress) fail to compile, and note which signature refused it.",
    ],
    challenge: {
      prompt:
        "Model a 'payment' domain with discriminated unions and exhaustiveness. Requirements: (1) a Payment union of three shapes sharing a discriminant method: \"card\" (with last4: string), \"paypal\" (with email: string), and \"bank\" (with iban: string) — each with an amountCents: number; (2) a function describePayment(p: Payment): string using an exhaustive switch with the assertNever default; (3) a function fee(p: Payment): number returning 0 for bank, 30 for paypal, and 2.9% of amountCents (Math.round) for card — using narrowing, not any casts; (4) add a fourth method \"crypto\" to the union and LIST every place the compiler now errors (there should be two), then implement both; (5) explain in one sentence why fee could not be written with optional fields without lying.",
      hints: [
        "The discriminant is method, with literal values \"card\" | \"paypal\" | \"bank\". Each member carries its own method-specific field plus the shared amountCents.",
        "assertNever(x: never): never { throw new Error(...) } in each default. When you add \"crypto\", both describePayment and fee defaults break — that's the two errors.",
        "For fee, narrow with if (p.method === \"bank\") return 0; etc. The card branch reads p.amountCents — no cast needed because narrowing already proved the shape.",
      ],
      solution: [
        "type CardPayment  = { method: \"card\";   amountCents: number; last4: string };",
        "type PaypalPayment = { method: \"paypal\"; amountCents: number; email: string };",
        "type BankPayment  = { method: \"bank\";   amountCents: number; iban: string };",
        "// (4) the fourth member, added later:",
        "type CryptoPayment = { method: \"crypto\"; amountCents: number; wallet: string };",
        "",
        "type Payment = CardPayment | PaypalPayment | BankPayment | CryptoPayment;",
        "",
        "function assertNever(x: never): never {",
        "  throw new Error(`Unhandled payment method: ${JSON.stringify(x)}`);",
        "}",
        "",
        "// (2) exhaustive — the default only type-checks when every case is handled",
        "function describePayment(p: Payment): string {",
        "  switch (p.method) {",
        "    case \"card\":   return `Card ending ${p.last4}`;",
        "    case \"paypal\": return `PayPal (${p.email})`;",
        "    case \"bank\":   return `Bank transfer to ${p.iban}`;",
        "    case \"crypto\": return `Crypto to ${p.wallet}`;   // added after the error",
        "    default:       return assertNever(p);",
        "  }",
        "}",
        "",
        "// (3) narrowing, no casts:",
        "function fee(p: Payment): number {",
        "  if (p.method === \"bank\") return 0;",
        "  if (p.method === \"paypal\") return 30;",
        "  if (p.method === \"crypto\") return 0;             // added after the error",
        "  // here p is narrowed to CardPayment:",
        "  return Math.round(p.amountCents * 0.029);",
        "}",
        "",
        "const p: Payment = { method: \"card\", amountCents: 1000, last4: \"4242\" };",
        "console.log(describePayment(p)); // \"Card ending 4242\"",
        "console.log(fee(p));             // 29",
        "",
        "// (4) Adding \"crypto\" produced exactly TWO errors — the default in",
        "//     describePayment and the implicit card-fallthrough in fee —",
        "//     because in both, the leftover stopped being never.",
        "//     The compiler listed every site that needed the new case.",
        "",
        "// (5) With optional fields (last4?, email?, iban?) the type would allow",
        "//     an object with none of them or several at once, so fee would need",
        "//     runtime checks and could still hit an unhandled combination —",
        "//     the union makes each payment EXACTLY one method, so narrowing is total.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A discriminated union is…",
        options: ["Object types sharing one literal field with a different value each", "Any union of two types", "A class hierarchy", "A union that can't be narrowed"],
        answer: 0,
        explain: "The shared literal field (the discriminant — status, kind, ok, method) lets the compiler narrow with zero ambiguity on a single === check or switch.",
      },
      {
        q: "Inside if (task.status === \"done\"), the compiler…",
        options: ["Narrows task to the DoneTask shape, so task.completionDate is safe", "Still treats task as the full union", "Throws at runtime", "Requires a cast"],
        answer: 0,
        explain: "Control-flow analysis narrows on the discriminant equality. In the branch, task is DoneTask; after an early return, the rest is OpenTask.",
      },
      {
        q: "The never type is used for exhaustiveness by…",
        options: ["Assigning the switch default's leftover to never, so an unhandled case fails to compile", "Marking functions that return nothing", "Preventing null", "Disabling a type"],
        answer: 0,
        explain: "If every union member is handled, the default's value is never. Add a member and the leftover stops being never → compile error at every unhandled switch.",
      },
      {
        q: "Why is { ok: true; data } | { ok: false; error } better than { data?; error? }?",
        options: ["The union makes success and failure mutually exclusive and lets the compiler narrow", "It's shorter", "Optionals are deprecated", "Unions are faster at runtime"],
        answer: 0,
        explain: "The optional-bag allows impossible states (both/neither) and forces runtime checks plus ! assertions. The union encodes exclusivity so narrowing is total and safe.",
      },
      {
        q: "Which is NOT a valid narrowing tool?",
        options: ["Assigning to a loosely-typed variable", "typeof checks", "the in operator", "equality on a discriminant field"],
        answer: 0,
        explain: "typeof, in, instanceof, truthiness, and discriminant equality all narrow. Assigning a union to an any/loose variable erases the type — the opposite of narrowing.",
      },
      {
        q: "A typed status machine makes illegal transitions…",
        options: ["Unrepresentable — no function accepts the wrong source state", "Runtime warnings", "Logged but allowed", "Impossible only by convention"],
        answer: 0,
        explain: "complete(task: OpenTask) only accepts open tasks. A DoneTask has no path back except reopen(). The signature IS the business rule.",
      },
    ],
    flashcards: [
      { front: "Union type", back: "A | B — a value that is an A or a B. The compiler forces you to handle each member before using member-specific fields." },
      { front: "Narrowing", back: "Control-flow analysis: inside a branch that rules out members (typeof, in, truthiness, discriminant ===), the type shrinks to the survivors." },
      { front: "Discriminant", back: "The shared literal field in a discriminated union (status, kind, ok, method) whose value identifies the shape. Narrow on it with === or switch." },
      { front: "Result<T> pattern", back: "{ ok: true; data: T } | { ok: false; error: string }. The course-standard API result; callers branch on ok and get exactly one side." },
      { front: "never", back: "The type with no values. Used in an exhaustive switch default so an unhandled union member becomes a compile error." },
      { front: "assertNever", back: "function assertNever(x: never): never { throw … }. Satisfies the exhaustive default and throws with the offender if types and runtime ever disagree." },
      { front: "Exhaustiveness checking", back: "Ensuring every union member is handled; adding a member breaks every switch that lacks it. Forgetting a case becomes a build failure." },
      { front: "Status machine, typed", back: "States as a discriminated union + transition functions whose signatures encode the legal moves. Illegal transitions don't type." },
    ],
    recap: [
      "Unions model honest ORs; they replace optional-field soup with complete shapes.",
      "Narrow with typeof / in / truthiness / discriminant equality; the compiler tracks the branch.",
      "Discriminated unions (one literal field) are the course default — Result<T> is the canonical form.",
      "never + assertNever give exhaustiveness: new cases break the build everywhere they must.",
      "Typed status machines put business rules in signatures — illegal states are unrepresentable.",
    ],
    references: [
      { label: "TypeScript — Narrowing", url: "https://www.typescriptlang.org/docs/handbook/2/narrowing.html" },
      { label: "TypeScript — Unions & Literals", url: "https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#union-types" },
      { label: "TypeScript — The never type", url: "https://www.typescriptlang.org/docs/handbook/2/narrowing.html#exhaustiveness-checking" },
      { label: "TypeScript — Discriminated Unions", url: "https://www.typescriptlang.org/docs/handbook/2/narrowing.html#discriminated-unions" },
      { label: "basarat — Discriminated Union", url: "https://basarat.gitbook.io/typescript/type-system/discriminated-unions" },
    ],
    nextBridge:
      "You can model states so precisely that illegal ones don't exist. Next: generics — honest placeholders that let one function serve every type without losing precision — plus the utility types (Partial, Pick, Omit, Record) that turn one shape into many. This is where TypeScript stops being a checker and becomes a design tool.",
  },
];

export const LESSON_CONTENT_P3: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P3.map((l) => [l.id, l]),
);
