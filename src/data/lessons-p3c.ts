import type { LessonContent } from "./types";

/**
 * Pass 010 bounded content batch: Phase 3 close (L4).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P3C: LessonContent[] = [
  {
    id: "p03-l4",
    phaseId: "p03",
    title: "keyof, typeof, unknown & Assertions",
    level: "Foundation",
    minutes: 35,
    summary:
      "Types can be derived FROM values (typeof) and the keys of a type can become a type themselves (keyof) — two operators that let a single source of truth drive whole families of types. This lesson installs types-from-values, indexed access and mapped types, unknown as the honest stand-in for 'I don't know yet', and assertions — the places where you deliberately tell the compiler something it can't prove. It closes Phase 3 by drawing the line TypeScript itself cannot cross: types exist only at compile time, so untrusted data still needs a runtime gate.",
    prerequisites: ["p03-l3 — generics, constraints, and the utility toolbox", "p03-l2 — unions and narrowing"],
    objectives: [
      "Derive types from values with typeof and from types' keys with keyof, and combine them with indexed access T[K].",
      "Build mapped types ({ [K in keyof T]: … }) to transform whole shapes in one expression.",
      "Use unknown for untrusted boundaries and narrow it with typeof/instanceof/in/type-predicates instead of any.",
      "Apply assertions (as, as const) only where you hold knowledge the compiler cannot, and never to silence it.",
      "Explain why JSON.parse returns any and why compile-time types cannot validate runtime data — and name the gate that can.",
      "State why keyof/typeof power the typed API contracts of Phase 19 and the config systems of real apps.",
    ],
    simple:
      "So far, types have been things you WRITE. Now they become things you DERIVE. typeof asks the compiler 'what shape is this value?' and hands back its type as a reusable blueprint. keyof asks 'what are the legal keys of this type?' and hands back a union of them — a type made of names. Combined, they mean one declaration (a config object, an API response type) becomes the single source of truth that every related type is computed from. Change the source, and every derived type updates — the compiler does the bookkeeping you'd otherwise do by hand and get wrong.",
    why:
      "Every serious TypeScript codebase leans on derivation. The typed API client of Phase 19 keys its endpoints off a route map with keyof; Prisma's generated client (Phase 14) is built from your schema via these exact operators; form libraries infer field names from your object type. And unknown is the bridge to the course's deepest recurring idea — that the type system stops at the trust boundary. JSON from a server, an env var, an uploaded file: all of it arrives as data the compiler has never seen. Treating it as any pretends otherwise; treating it as unknown and narrowing it is how professionals meet the outside world. Phase 20's runtime validation is this lesson's logical conclusion.",
    mentalModel: {
      title: "Blueprints and buildings",
      body: "Think of a type as a blueprint and a value as the built house. typeof walks up to a finished house and reverse-engineers its blueprint (typeof config gives you Config's shape). keyof reads a blueprint and lists every room name on it (keyof Config = 'retries' | 'timeoutMs'). Indexed access asks 'what's in THIS room?' (Config['retries'] = number). A mapped type takes every room in the blueprint and redesigns them all by one rule — 'make each room optional', 'make each room readonly'. The power move: you only ever draw ONE blueprint (the source of truth); every other drawing is computed from it, so the buildings can never disagree with each other.",
    },
    sections: [
      {
        heading: "typeof — reverse-engineering a blueprint from a building",
        body: [
          "In type position, typeof value yields the value's type — so a well-shaped runtime object becomes a type you can reuse without redeclaring it. This shines for config objects and constants: declare the actual object once (with as const to freeze literals), then derive its type. The object and the type can never drift, because there is only one artifact. This is how feature-flag maps, route tables, and API base configs stay honest in real apps.",
          "The rule of thumb: when a value and its type would be written side by side and must match, derive the type from the value with typeof instead of writing both. The compiler becomes the single author.",
        ],
        code: [
          {
            file: "typeof.ts — one artifact, two views",
            lang: "ts",
            code: [
              "// the source of truth is the VALUE:",
              "const config = {",
              "  retries: 3,",
              "  timeoutMs: 2000,",
              "  endpoint: \"/api\",",
              "} as const; // freeze: literals, readonly",
              "",
              "// derive the blueprint from the building:",
              "type Config = typeof config;",
              "// { readonly retries: 3; readonly timeoutMs: 2000; readonly endpoint: \"/api\" }",
              "",
              "// now the type is reusable, and always matches the object:",
              "function withDefaults(partial: Partial<Config>): Config {",
              "  return { ...config, ...partial };",
              "}",
              "withDefaults({ retries: 5 }); // ✓ only real keys allowed",
              "// withDefaults({ retryz: 5 }); // ✗ compile error — no such key",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "keyof & T[K] — a type made of names",
        body: [
          "keyof T produces a union of T's keys — a type whose VALUES are property NAMES. That lets you constrain a generic to 'one of the real keys' (K extends keyof T) and, combined with indexed access T[K], ask 'what type lives at this key?'. Together they turn whole classes of string-typed bugs (misspelled keys, wrong lookups) into compile errors.",
          "This is the engine under pick(), under form libraries that infer field names, and under the route maps of Phase 19: a Record of endpoint → response type, consumed through keyof so a typo'd endpoint is rejected before it ever becomes a fetch.",
        ],
        code: [
          {
            file: "keyof-indexed.ts",
            lang: "ts",
            code: [
              "interface User { id: number; name: string; email: string; }",
              "",
              "type UserKey = keyof User;        // \"id\" | \"name\" | \"email\"",
              "type NameType = User[\"name\"];    // string — indexed access",
              "",
              "// generic constrained to real keys, returning the right type:",
              "function get<T, K extends keyof T>(obj: T, key: K): T[K] {",
              "  return obj[key];",
              "}",
              "const u: User = { id: 1, name: \"Ada\", email: \"a@x.dev\" };",
              "get(u, \"name\");   // string  — the return type FOLLOWS the key",
              "get(u, \"id\");     // number",
              "// get(u, \"nmae\"); // ✗ 'nmae' is not assignable to 'id'|'name'|'email'",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Mapped types — redesign every room by one rule",
        body: [
          "A mapped type walks every key of a type and rebuilds the shape by a rule: { [K in keyof T]: … }. The built-in utility types you met in P03-L3 are literally mapped types in disguise — Partial is { [K in keyof T]?: T[K] }, Readonly adds readonly, Record<K,V> maps a key union to a value. Writing your own mapped type is how you express 'the same shape, but every field is a validator / a string / optional' without enumerating fields.",
          "Modifiers matter: +? adds optionality, -? removes it, +readonly / -readonly add or strip readonly. Remapping keys with as (key remapping) lets you rename or filter keys — the tool behind 'make a setter type' or 'drop all methods' transforms.",
        ],
        code: [
          {
            file: "mapped.ts — the rule, once",
            lang: "ts",
            code: [
              "interface Todo { id: number; title: string; done: boolean; }",
              "",
              "// every field becomes a function that validates that field:",
              "type Validators<T> = { [K in keyof T]: (value: T[K]) => boolean };",
              "const todoChecks: Validators<Todo> = {",
              "  id: (n) => Number.isInteger(n) && n > 0,",
              "  title: (s) => s.trim().length > 0,",
              "  done: (b) => typeof b === \"boolean\",",
              "};",
              "// Miss a field or get a signature wrong → compile error. Exhaustive by construction.",
              "",
              "// Partial/Readonly are just mapped types:",
              "// type Partial<T>   = { [K in keyof T]?: T[K] };",
              "// type Readonly<T>  = { readonly [K in keyof T]: T[K] };",
              "",
              "// key remapping (as): build setter names",
              "type Setters<T> = { [K in keyof T as `set${Capitalize<string & K>}`]: (v: T[K]) => void };",
              "type TodoSetters = Setters<Pick<Todo, \"done\">>; // { setDone: (v: boolean) => void }",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "unknown — the honest 'I don't know yet'",
        body: [
          "unknown is the type-safe counterpart of any. Anything can be assigned INTO unknown, but you can DO nothing with an unknown value — no property access, no calls, no arithmetic — until you narrow it. That's the point: it models 'data arrived from outside; prove what it is before using it'. any says 'trust me'; unknown says 'show me'.",
          "The narrowing toolkit you already know applies: typeof guards, instanceof for classes/errors, the in operator for object keys, and user-defined type predicates (v is T) for shapes. catch variables under strict are unknown for exactly this reason. The pattern — receive as unknown, narrow with proof, use the narrowed type — is the course's standard for every trust boundary until Phase 20 layers real schema validation on top.",
        ],
        code: [
          {
            file: "unknown.ts — trust, but verify",
            lang: "ts",
            code: [
              "function handle(input: unknown) {",
              "  // input.toFixed();        // ✗ Object is of type 'unknown'",
              "  if (typeof input === \"number\") {",
              "    return input.toFixed(2);  // ✓ narrowed to number here",
              "  }",
              "  if (input instanceof Error) {",
              "    return input.message;     // ✓ narrowed to Error",
              "  }",
              "  if (typeof input === \"object\" && input !== null && \"id\" in input) {",
              "    return (input as { id: unknown }).id; // ✓ has an id key",
              "  }",
              "  return undefined;",
              "}",
              "",
              "// user-defined predicate: the reusable gate",
              "interface Todo { id: number; title: string; done: boolean; }",
              "function isTodo(v: unknown): v is Todo {",
              "  return (",
              "    typeof v === \"object\" && v !== null &&",
              "    \"id\" in v && \"title\" in v && \"done\" in v",
              "  );",
              "}",
              "const parsed: unknown = JSON.parse(rawFromServer);",
              "if (isTodo(parsed)) {",
              "  render(parsed.title); // ✓ parsed is Todo inside this branch",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Assertions — lying to the compiler, carefully",
        body: [
          "An assertion (value as Type) tells the compiler 'I know more than you; stop checking'. It performs NO runtime conversion and NO validation — it is erased at compile time. Legitimate uses are narrow: as const to freeze literals into a readonly tuple/const type; asserting after you've done a check the compiler can't express (document it!); and the JSON.parse boundary, where the value is genuinely any and you're taking responsibility. The danger: assertions silence exactly the errors the type system exists to catch.",
          "The litmus test: can you state, in a comment, WHY you know something the compiler can't? If yes, assert and document. If the assertion exists only to make a red squiggle disappear, you're not adding knowledge — you're removing a safety net. never assert away nullability (!), and prefer a type predicate over an assertion whenever the check is expressible.",
        ],
        code: [
          {
            file: "assertions.ts — the narrow gate",
            lang: "ts",
            code: [
              "// ✓ GOOD: as const — freeze literals into a readonly type",
              "const STATUSES = [\"open\", \"done\"] as const;",
              "type Status = (typeof STATUSES)[number]; // \"open\" | \"done\"",
              "",
              "// ✓ GOOD: JSON.parse is any — assert at the boundary, THEN validate",
              "const raw: unknown = JSON.parse(text); // treat as unknown, not any",
              "// (asserting raw as User directly would skip the gate — don't)",
              "",
              "// ✓ GOOD: you checked something the type system can't see — document it",
              "const el = document.getElementById(\"app\");",
              "// el is HTMLElement | null; we created #app in index.html above this script:",
              "const app = el as HTMLDivElement; // SAFE: #app is a <div> in index.html",
              "",
              "// ✗ BAD: assertion as sedative — removes the check, keeps the bug",
              "// const user = data as User; data.email.toUpperCase();",
              "// If data lacks email, this crashes at runtime. The assertion",
              "// didn't add email — it only told the compiler to look away.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The line TypeScript can't cross — compile time vs runtime",
        body: [
          "Everything in this course so far lives at COMPILE time. Types are erased when the code builds — they add zero bytes and run zero checks in the browser or on the server. So a value's TypeScript type is a CONTRACT about what your code believes, not a guarantee about what's actually there. JSON.parse returns any precisely because the compiler has no idea what the string contains; a server response typed as User is a belief, not a fact.",
          "This is the trust-boundary insight the whole backend half of the course rests on. The browser's native validation (Phase 1) is UX; TypeScript's types are authoring-time proof about YOUR code; but data crossing a boundary — an HTTP response, an env var, a file upload — is only as trustworthy as a RUNTIME check. Phase 20 adds that layer (Zod schemas on the client, NestJS DTOs on the server), and Phase 13 puts the final guard in PostgreSQL. The mental model: types narrow the world you can MISTYPE; validation narrows the world you can MISRECEIVE. You need both, and neither replaces the other.",
        ],
        code: [
          {
            file: "boundary.ts — belief vs proof",
            lang: "ts",
            code: [
              "interface User { id: number; name: string; }",
              "",
              "// TypeScript's view (compile time — a belief about your code):",
              "async function fetchUser(): Promise<User> {",
              "  const res = await fetch(\"/api/user\");",
              "  const data = (await res.json()) as User; // asserted, NOT proven",
              "  return data;",
              "}",
              "",
              "// The runtime reality: if the server sent { id: \"7\" } (a string),",
              "// `data.id + 1` silently produces \"71\". TypeScript never ran.",
              "",
              "// The fix arrives in Phase 20 — a runtime gate at the boundary:",
              "//   const user = UserSchema.parse(await res.json()); // throws on mismatch",
              "// Until then: treat parsed data as unknown + a type predicate.",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "Casting the unknown into shape",
      wrong: [
        "const user = JSON.parse(text) as User;",
        "renderName(user.name.toUpperCase());",
        "",
        "// If `text` was '{\"id\": 7}' (no name), user.name is undefined.",
        "// .toUpperCase() throws at runtime — in a user's browser.",
        "// The `as User` didn't create a name; it only told the compiler",
        "// to stop looking. This is any wearing a costume.",
      ].join("\n"),
      right: [
        "const parsed: unknown = JSON.parse(text);",
        "if (isUser(parsed)) {",
        "  renderName(parsed.name.toUpperCase()); // ✓ proven to have name",
        "} else {",
        "  showError(\"Unexpected user payload\");",
        "}",
        "",
        "function isUser(v: unknown): v is User {",
        "  return (",
        "    typeof v === \"object\" && v !== null &&",
        "    typeof (v as User).id === \"number\" &&",
        "    typeof (v as User).name === \"string\"",
        "  );",
        "}",
        "// The gate runs at RUNTIME, where the data actually is.",
      ].join("\n"),
      explain:
        "An assertion is compile-time only — it changes what the compiler believes, never what the data is. At a trust boundary (parsed JSON, a network response, an env var) there is no compiler; there is only data. Casting unvalidated data into a type converts a detectable mismatch into a runtime crash. Receive as unknown, narrow with a real check (a type predicate now, a schema validator in Phase 20), and use the proven type.",
    },
    tryIt: [
      "Declare a const routeMap = { users: [], posts: [] } as const and derive its type with typeof, then a union of its keys with keyof. Change a key and watch every consumer of the derived union update.",
      "Write get(obj, key) from section two and call it on three objects with three keys. Hover each call — verify the return type tracks the specific key you passed.",
      "Build your own mapped type: Stringify<T> = { [K in keyof T]: string }. Apply it to your Todo type and try assigning a number field — confirm the error.",
      "Take a JSON.parse call in your Todo app and change its result type from any to unknown. Fix every new error with narrowing, not assertions. Count how many the compiler made you prove.",
      "Find one place you could replace a hand-written duplicate type with typeof + keyof. Make the swap and delete the duplicate.",
    ],
    challenge: {
      prompt:
        "Build a typed 'endpoint registry' for a small API, using ONLY derived types — you may not redeclare a field that already exists elsewhere. Requirements: (1) const endpoints = { '/users': { method: 'GET', response: null as unknown as User[] }, '/users/:id': { method: 'GET', response: null as unknown as User }, '/users': { … } … } — actually, define a routes object mapping three path strings to { method, response } pairs, frozen with as const where the method is a literal; (2) derive type RoutePath = keyof typeof routes and a generic fetchRoute<P extends RoutePath>(path: P) that returns Promise<(typeof routes)[P]['response']> — the return type must follow the path; (3) derive a MappedType EndpointMethods = { [K in RoutePath]: (typeof routes)[K]['method'] }; (4) write parseResponse(path: RoutePath, raw: unknown): unknown that narrows via a per-route type predicate stored in a Record<RoutePath, (v: unknown) => boolean>; (5) demonstrate that fetchRoute('/users') is typed User[] and fetchRoute('/users/1') is typed User, and that a misspelled path is a compile error.",
      hints: [
        "You don't need the response VALUES — you need their TYPES. A common trick is to store the type in a parallel const or use a helper type; but the cleanest is: declare the routes object's type via a helper interface, then derive everything from keyof typeof routes and indexed access routes[P]['response'].",
        "The per-route predicate map: Record<RoutePath, (v: unknown) => v is unknown> won't narrow per-route; instead, have parseResponse switch on path and call the right predicate so each branch returns the narrowed type. This is the discriminated-union pattern applied to a key union.",
        "To make fetchRoute's return follow the path, index into the routes type: ReturnType follows P. Test by assigning the result to a wrongly-typed variable and reading the error.",
      ],
      solution: [
        "interface User { id: number; name: string; }",
        "interface Post { id: number; title: string; authorId: number; }",
        "",
        "// (1) the source of truth: route table. `response` is typed via a",
        "//     helper so we capture the TYPE without needing real values.",
        "declare const usersResponse: User[];",
        "declare const userResponse: User;",
        "declare const postsResponse: Post[];",
        "",
        "const routes = {",
        "  \"/users\":    { method: \"GET\", response: usersResponse },",
        "  \"/users/:id\":{ method: \"GET\", response: userResponse },",
        "  \"/posts\":    { method: \"GET\", response: postsResponse },",
        "} as const;",
        "",
        "// (2) derive the path union + a fetcher whose return follows the path",
        "type RoutePath = keyof typeof routes;   // \"/users\" | \"/users/:id\" | \"/posts\"",
        "type ResponseOf<P extends RoutePath> = (typeof routes)[P][\"response\"];",
        "",
        "async function fetchRoute<P extends RoutePath>(path: P): Promise<ResponseOf<P>> {",
        "  const res = await fetch(`/api${path}`);",
        "  if (!res.ok) throw new Error(`HTTP ${res.status}`);",
        "  const raw: unknown = await res.json();",
        "  const parsed = parseResponse(path, raw);",
        "  return parsed as ResponseOf<P>; // boundary assert — validated by parseResponse",
        "}",
        "",
        "// (3) derive the method map — no redeclaration",
        "type EndpointMethods = { [K in RoutePath]: (typeof routes)[K][\"method\"] };",
        "// { \"/users\": \"GET\"; \"/users/:id\": \"GET\"; \"/posts\": \"GET\" }",
        "",
        "// (4) per-route runtime gates",
        "const isUserArray = (v: unknown): v is User[] =>",
        "  Array.isArray(v) && v.every((x) => typeof x === \"object\" && x !== null && \"id\" in x && \"name\" in x);",
        "const isUser = (v: unknown): v is User =>",
        "  typeof v === \"object\" && v !== null && \"id\" in v && \"name\" in v;",
        "const isPostArray = (v: unknown): v is Post[] =>",
        "  Array.isArray(v) && v.every((x) => typeof x === \"object\" && x !== null && \"id\" in x && \"title\" in x);",
        "",
        "function parseResponse(path: RoutePath, raw: unknown): unknown {",
        "  switch (path) {",
        "    case \"/users\":     return isUserArray(raw) ? raw : fail(path);",
        "    case \"/users/:id\": return isUser(raw) ? raw : fail(path);",
        "    case \"/posts\":     return isPostArray(raw) ? raw : fail(path);",
        "  }",
        "}",
        "function fail(path: RoutePath): never {",
        "  throw new Error(`Invalid payload for ${path}`);",
        "}",
        "",
        "// (5) the return type FOLLOWS the path — and typos die at compile time",
        "async function demo() {",
        "  const users = await fetchRoute(\"/users\");       // User[]",
        "  const user = await fetchRoute(\"/users/:id\");    // User",
        "  const posts = await fetchRoute(\"/posts\");       // Post[]",
        "  users[0].name.toUpperCase();                    // ✓",
        "  user.title;                                     // ✗ 'title' not on User",
        "  // await fetchRoute(\"/userz\");                  // ✗ not a RoutePath",
        "}",
        "",
        "/* Everything downstream of `routes` is DERIVED: add a route and",
        "   RoutePath, EndpointMethods, and fetchRoute's accepted paths all",
        "   update — and every consumer that must handle the new route",
        "   becomes a compile error until it does. One source of truth. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "typeof config (in type position) produces…",
        options: ["The type of the value config — a blueprint derived from the object", "A string containing 'config'", "The runtime constructor of config", "A copy of config"],
        answer: 0,
        explain: "In type position, typeof reverse-engineers a value's type. With as const, literals and readonly are preserved, so the derived type exactly matches the object.",
      },
      {
        q: "keyof User where User has id, name, email is…",
        options: ["'id' | 'name' | 'email' — a union of the key names", "The values of those fields", "string", "A tuple [id, name, email]"],
        answer: 0,
        explain: "keyof yields a union of property NAMES as literal types. It's the tool that lets you constrain a generic to 'one of the real keys'.",
      },
      {
        q: "In get<T, K extends keyof T>(obj: T, key: K): T[K], the return type is…",
        options: [
          "The type of the specific property named by K — it follows the key",
          "Always any",
          "T itself",
          "string",
        ],
        answer: 0,
        explain: "Indexed access T[K] resolves to the property type at K, so get(u,'name') is string and get(u,'id') is number. The return tracks the key you pass.",
      },
      {
        q: "The key difference between any and unknown is…",
        options: ["unknown must be narrowed before use; any can be used immediately (unsafely)", "any is stricter than unknown", "They are aliases", "unknown can only hold objects"],
        answer: 0,
        explain: "Anything assigns into unknown, but you can do nothing with it until narrowed. any opts out of checking entirely. unknown is the safe choice for untrusted boundaries.",
      },
      {
        q: "An assertion (x as T) at runtime…",
        options: ["Does nothing — it's erased at compile time and performs no validation", "Converts x into T", "Throws if x isn't a T", "Deep-clones x as T"],
        answer: 0,
        explain: "Assertions only change what the compiler believes. They're legitimate when you hold knowledge the compiler can't express (and you document it), never as a way to skip validation.",
      },
      {
        q: "Why can't TypeScript alone validate a server response?",
        options: ["Types are erased at compile time — they never run where the data arrives", "TypeScript doesn't support fetch", "Responses are always any by law", "It can, if you use an interface"],
        answer: 0,
        explain: "A type is a compile-time belief about your code, not a runtime check on incoming bytes. Untrusted data needs a runtime gate — a type predicate now, a schema validator (Phase 20) in production.",
      },
    ],
    flashcards: [
      { front: "typeof (type position)", back: "Derives a value's type as a reusable blueprint: type Config = typeof config. With as const, literals/readonly are frozen. One artifact, type and value can't drift." },
      { front: "keyof", back: "Produces a union of a type's key names: keyof User = 'id'|'name'|'email'. Constrains generics to real keys, turning typos into compile errors." },
      { front: "Indexed access T[K]", back: "The type of property K on T. User['name'] = string. In generics, T[K] makes a return type follow the key argument." },
      { front: "Mapped type", back: "{ [K in keyof T]: … } rebuilds every field of T by one rule. Partial/Readonly/Record are mapped types. Modifiers: +?/-?/readonly; as remaps keys." },
      { front: "unknown", back: "The safe any: anything assigns in, nothing usable out until narrowed (typeof/instanceof/in/predicate). The correct type for untrusted boundaries." },
      { front: "as const", back: "Freezes a value into a readonly type with literal types: ['a','b'] as const becomes readonly ['a','b']. The source for (typeof x)[number] unions." },
      { front: "Assertion (as)", back: "Tells the compiler 'I know more than you' — compile-time only, no runtime effect. Use with a documented reason at true boundaries; never to silence errors." },
      { front: "Compile time vs runtime", back: "Types are erased at build — a contract about your code, not a check on incoming data. JSON.parse is any because only a runtime gate (predicate/schema) can validate it." },
    ],
    recap: [
      "typeof derives types from values; keyof derives key-unions from types; T[K] reads the type at a key. One source of truth, everything else computed.",
      "Mapped types transform whole shapes by a rule — the machinery under the utility toolbox.",
      "unknown models untrusted data honestly; narrow with proof, never with any.",
      "Assertions are compile-time claims, not runtime checks — document every one.",
      "TypeScript narrows what you can mistype; validation narrows what you can misreceive. Phase 20 adds the runtime half. Phase 3: complete.",
    ],
    references: [
      { label: "TypeScript Handbook — keyof and typeof", url: "https://www.typescriptlang.org/docs/handbook/2/keyof-types.html" },
      { label: "TypeScript Handbook — Mapped Types", url: "https://www.typescriptlang.org/docs/handbook/2/mapped-types.html" },
      { label: "TypeScript Handbook — unknown", url: "https://www.typescriptlang.org/docs/handbook/2/narrowing.html#the-unknown-type" },
      { label: "TypeScript Handbook — Type Assertions", url: "https://www.typescriptlang.org/docs/handbook/2/everyday-types.html#type-assertions" },
      { label: "Matt Pocock — Total TypeScript (keyof/typeof)", url: "https://www.totaltypescript.com/tutorials" },
    ],
    nextBridge:
      "Phase 3 is complete — you can derive, transform, and defend types. Now the Foundation stage hands off to the React Developer stage. Everything you've typed is about to be rendered: Phase 4 opens with the mental flip that defines React — stop commanding the DOM, start describing it. Declarative UI and JSX begin.",
  },
];

export const LESSON_CONTENT_P3C: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P3C.map((l) => [l.id, l]),
);
