import type { LessonContent } from "./types";

/**
 * Pass 016 bounded content batch: Phase 6 opener (L1–L2).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P6: LessonContent[] = [
  {
    id: "p06-l1",
    phaseId: "p06",
    title: "Component APIs & State Ownership",
    level: "React Developer",
    minutes: 40,
    summary:
      "A component is an appliance: the props are the dials on the front panel, the state is the wiring nobody should see, and the best manuals fit on one card. This lesson teaches API drafting — minimal surfaces, defaults as decisions, variants as discriminated unions — the compound-component pattern that turns configuration objects into composition, mature controlled/uncontrolled duality, and the state-ownership map that settles every 'where does this live?' dispute before it starts.",
    prerequisites: ["p05-l7 — custom hooks & the Hook Decision Matrix", "p04-l6 — lifting state & derived state"],
    objectives: [
      "Draft props as a typed contract: required means indispensable, optional ships with a right-most-times default.",
      "Model variants with discriminated unions so impossible combinations (danger-without-confirm) are compile errors.",
      "Collapse boolean clusters into single status unions and let the compiler walk every consumer.",
      "Build compound components (Accordion/Tabs) that share implicit state through a private context.",
      "Support controlled AND uncontrolled usage from one implementation with the value !== undefined switch.",
      "Apply the ownership map — derived/local/lifted/context/ref — and defend every placement on the spectrum.",
    ],
    simple:
      "You don't want forty exposed wires on a toaster — you want a dial, a lever, and a slot, each labeled, each doing one obvious thing. Props are those dials: the small named surface people touch. State is the wiring inside: essential, but the appliance's private business. When someone asks for a new feature, the amateur solders another wire to the front; the professional asks whether it's a new DIAL (a prop, rare), or a new ATTACHMENT POINT (children, a slot, a compound part — usual). The machines inside stay as complex as the job requires. The panels stay one card.",
    why:
      "You will consume ten components for every one you write — and the ones you write will be consumed by teammates for years, because shared code outlives its author. API quality compounds in both directions: a clean contract makes every future feature cheap and every refactor safe; a leaky one taxes every consumer forever with workarounds, forked variants, and prop soups nobody dares delete. The patterns here — discriminated variants, compound parts, dual controlled/uncontrolled — are the house style of Radix, shadcn/ui, and essentially every design system you will join. Learning them from the producer's seat, with the consumer's memory fresh, is the shortest path to components people trust.",
    mentalModel: {
      title: "The owner's manual",
      body: "Every component ships two artifacts: the manual (its props) and the machine (its state and behavior). Great manuals are short — a few labeled controls, each with an obvious job, and switches that physically cannot be set to 'both'. Bad manuals grow a new unlabeled knob for every request until the panel is a wall of guesswork. The professional move when the manual grows: don't add knobs, add attachment points — children for content, compound parts for structure — and let callers compose. And underneath it all runs one rule from the Hook Decision Matrix, promoted to architecture: every VALUE has exactly one rightful home, and most 'React is confusing' bugs are actually ownership disputes with the wrong filing.",
    },
    sections: [
      {
        heading: "Props are a contract — small, typed, hard to misuse",
        body: [
          "The props interface is a legal agreement with every future caller, and three drafting rules keep it enforceable. One: required means the component cannot work without it; everything else is optional, and optional means the default is right most of the time — a Toggle without a considered default is pushing a decision onto every caller. Two: group related options into objects (position={{ side: 'top', align: 'start' }}) so variants compose instead of multiplying into positional soup. Three: misuse should not compile — the compiler is the only reviewer who reads every call site.",
          "Variants belong in discriminated unions, never boolean clusters. A Button with isGhost, isDanger, isLoading booleans can be all three at once — including combinations you never designed or tested. variant: 'primary' | 'ghost' | 'danger' makes the menu explicit, autocompletes, and refuses invention. When a variant carries its own data (destructive needs a confirmMessage), model it as a true discriminated union so the pairing is enforced — 'destructive without a confirm message' becomes a type error instead of a 2 a.m. incident.",
        ],
        code: [
          {
            file: "contracts.tsx — dials, not wiring",
            lang: "tsx",
            code: [
              "// ✗ boolean soup: four switches, sixteen 'legal' combinations,",
              "//   several of them nonsense:",
              "type BadProps = {",
              "  isLoading?: boolean;",
              "  isDisabled?: boolean;",
              "  isDanger?: boolean;",
              "  isGhost?: boolean;",
              "};",
              "// <Button isLoading isDanger isGhost isDisabled /> — what does this draw?",
              "",
              "// ✓ a menu, not switches — defaults ARE the API:",
              "type ButtonProps = {",
              "  variant?: \"primary\" | \"ghost\" | \"danger\";",
              "  size?: \"sm\" | \"md\";",
              "  loading?: boolean;",
              "  disabled?: boolean;",
              "  onClick?: () => void;",
              "  children: ReactNode;               // content is REQUIRED — it's the point",
              "};",
              "",
              "export function Button({",
              "  variant = \"primary\",",
              "  size = \"md\",",
              "  loading = false,",
              "  disabled = false,",
              "  onClick,",
              "  children,",
              "}: ButtonProps) {",
              "  return (",
              "    <button",
              "      type=\"button\"",
              "      className={`btn btn-${variant} btn-${size}`}",
              "      disabled={disabled || loading}",
              "      onClick={onClick}",
              "    >",
              "      {loading ? \"Working…\" : children}",
              "    </button>",
              "  );",
              "}",
              "",
              "// ✓ when a variant carries its own data, discriminate for real:",
              "type SaveProps =",
              "  | { kind: \"normal\"; onSave: () => void }",
              "  | { kind: \"destructive\"; confirmMessage: string; onSave: () => void };",
              "",
              "export function SaveButton(props: SaveProps) {",
              "  if (props.kind === \"destructive\") {",
              "    // confirmMessage can't be forgotten — the union enforces it",
              "    return (",
              "      <button type=\"button\" onClick={() => window.confirm(props.confirmMessage) && props.onSave()}>",
              "        Delete…",
              "      </button>",
              "    );",
              "  }",
              "  return <button type=\"button\" onClick={props.onSave}>Save</button>;",
              "}",
            ].join("\n"),
            caption: "The union made 'destructive without a confirm message' unrepresentable. The compiler is your code reviewer now — and it reads every call site.",
          },
        ],
      },
      {
        heading: "Impossible states — one status beats five booleans",
        body: [
          "The single most common API disease is parallel booleans describing ONE fact: isLoading, isError, isSuccess, isEmpty. Four switches for one dial — and all sixteen combinations are 'legal', including isLoading && isError && isSuccess. The cure is the pattern Phase 3 installed: a single discriminated union. status: 'idle' | 'loading' | 'error' | 'success', with the payload attached to the state that owns it. Consumers switch over one field, and the compiler checks every branch — add a 'retrying' status and every unhandled consumer becomes a build error.",
          "The same move cleans component-to-component traffic: a child receiving isValidating, submitError, and isSubmitting from its parent should receive a status union instead. The audit rule writes itself: if you can imagine two of your boolean props being true at once and wince, they are one dial wearing several costumes. Data states get the four-position dial; everything downstream gets simpler, because nonsense is no longer expressible.",
        ],
        code: [
          {
            file: "status.tsx — one dial, four positions",
            lang: "tsx",
            code: [
              "// ✗ four dials for one fact — sixteen 'legal' combinations:",
              "type FeedBad = {",
              "  isLoading: boolean;",
              "  isError: boolean;",
              "  isSuccess: boolean;",
              "  posts?: Post[];",
              "  error?: string;",
              "}; // { isLoading: true, isSuccess: true, posts: [...] } — sure, why not?",
              "",
              "// ✓ one dial — nonsense is unrepresentable:",
              "type FeedState =",
              "  | { status: \"idle\" }",
              "  | { status: \"loading\" }",
              "  | { status: \"error\"; message: string }",
              "  | { status: \"success\"; posts: Post[] };",
              "",
              "function Feed({ state }: { state: FeedState }) {",
              "  switch (state.status) {",
              "    case \"idle\":",
              "    case \"loading\":",
              "      return <FeedSkeleton />;",
              "    case \"error\":",
              "      return <ErrorNote message={state.message} />; // posts can't exist here",
              "    case \"success\":",
              "      return <PostList posts={state.posts} />;       // error can't exist here",
              "  } // exhaustive — a new status breaks the build at EVERY site",
              "}",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Compound components — attachments instead of knobs",
        body: [
          "When the manual grows, add attachment points. An Accordion that takes items={[{title, body}]} works until someone needs a custom header, a badge in the trigger, a different panel class — and the items prop becomes a configuration language of its own, with a changelog. The compound pattern inverts it: <Accordion><AccordionItem value=\"shipping\"><AccordionTrigger>…</AccordionTrigger><AccordionPanel>…</AccordionPanel></AccordionItem></Accordion>. The parent owns the open state once and shares it through a PRIVATE context; each part is a stateless translator between markup and that context. Callers compose freely; you keep exactly one state model.",
          "Rules of the house, all enforced in thirty lines: the context is an implementation detail — never export it, and parts throw a friendly error outside their parent ('<AccordionTrigger> must live inside an <Accordion>'). Behavior modes live on the PARENT as one dial (type=\"single\" | \"multiple\"), never scattered across items. Content decisions stay with the caller — that's the whole trade: you give up configuration convenience and gain a surface that cannot be misused. This is the Radix/shadcn shape; you are learning the house style of modern component libraries from the inside.",
        ],
        code: [
          {
            file: "accordion.tsx — state once, parts everywhere",
            lang: "tsx",
            code: [
              "import { createContext, useContext, useState, type ReactNode } from \"react\";",
              "",
              "// ——— the private contract between the parts ———",
              "type AccordionContextValue = { open: Set<string>; toggle: (v: string) => void };",
              "const AccordionContext = createContext<AccordionContextValue | null>(null);",
              "",
              "function useAccordion(part: string) {",
              "  const ctx = useContext(AccordionContext);",
              "  if (!ctx) throw new Error(`<${part}> must live inside an <Accordion>`);",
              "  return ctx;",
              "}",
              "",
              "// ——— the parent owns the ONLY state ———",
              "export function Accordion({ children, defaultOpen = [] }: {",
              "  children: ReactNode;",
              "  defaultOpen?: string[];",
              "}) {",
              "  const [open, setOpen] = useState<Set<string>>(() => new Set(defaultOpen));",
              "  const toggle = (value: string) =>",
              "    setOpen((prev) => {",
              "      const next = new Set(prev);          // immutable, per P02-L3",
              "      if (next.has(value)) next.delete(value);",
              "      else next.add(value);",
              "      return next;",
              "    });",
              "  return (",
              "    <AccordionContext.Provider value={{ open, toggle }}>",
              "      <div className=\"accordion\">{children}</div>",
              "    </AccordionContext.Provider>",
              "  );",
              "}",
              "",
              "// ——— the parts: zero state, pure translators ———",
              "export function AccordionItem({ value, children }: { value: string; children: ReactNode }) {",
              "  return <div className=\"accordion-item\" data-value={value}>{children}</div>;",
              "}",
              "",
              "export function AccordionTrigger({ value, children }: { value: string; children: ReactNode }) {",
              "  const { open, toggle } = useAccordion(\"AccordionTrigger\");",
              "  const isOpen = open.has(value);",
              "  return (",
              "    <button type=\"button\" aria-expanded={isOpen} onClick={() => toggle(value)}>",
              "      {children} <span aria-hidden=\"true\">{isOpen ? \"−\" : \"+\"}</span>",
              "    </button>",
              "  );",
              "}",
              "",
              "export function AccordionPanel({ value, children }: { value: string; children: ReactNode }) {",
              "  const { open } = useAccordion(\"AccordionPanel\");",
              "  if (!open.has(value)) return null;",
              "  return <div role=\"region\">{children}</div>;",
              "}",
              "",
              "// ——— callers compose — the API is composition, not configuration ———",
              "// <Accordion defaultOpen={[\"shipping\"]}>",
              "//   <AccordionItem value=\"shipping\">",
              "//     <AccordionTrigger value=\"shipping\">Shipping</AccordionTrigger>",
              "//     <AccordionPanel value=\"shipping\">",
              "//       2–3 business days. <Badge>EU</Badge>   ← any markup the caller dreams up",
              "//     </AccordionPanel>",
              "//   </AccordionItem>",
              "// </Accordion>",
            ].join("\n"),
            caption: "State lives once (the Set in the parent). Every part is stateless — which is why adding a badge, a class, or a custom header never touches your code.",
          },
        ],
      },
      {
        heading: "Controlled vs uncontrolled — support both, fork nothing",
        body: [
          "An uncontrolled component keeps its own state and is seeded with a default (defaultValue); a controlled one takes value + onChange and the PARENT keeps the state — the component is a pure reporter. Mature libraries support both from a single implementation: if value !== undefined you are controlled (render it, call onChange, never write internal state); otherwise run internal state seeded by defaultValue. One component, two contracts, zero forking — and callers pick per situation.",
          "Choosing for your app: forms whose values matter only at submit are happiest uncontrolled (Phase 20's React Hook Form rides exactly this); values that drive other UI while typing — live filters, character counters, the split-bill total from P04-L6 — want controlled. The anti-pattern is half-controlled: passing value without onChange makes a read-only field that frustrates users, and React warns about it for a reason. And remember the ownership map: a controlled input's state lives where the DECISION lives — often the parent, sometimes a shared form object, never the input itself.",
        ],
        code: [
          {
            file: "dual.tsx — one implementation, both contracts",
            lang: "tsx",
            code: [
              "import { useState, type ChangeEvent } from \"react\";",
              "",
              "type CounterInputProps = {",
              "  value?: number;          // present → controlled by the caller",
              "  defaultValue?: number;   // absent value → uncontrolled seed",
              "  onChange?: (value: number) => void;",
              "};",
              "",
              "export function CounterInput({ value, defaultValue = 0, onChange }: CounterInputProps) {",
              "  const isControlled = value !== undefined;   // decided ONCE, up front",
              "  const [internal, setInternal] = useState(defaultValue);",
              "  const current = isControlled ? value : internal;  // one source of truth",
              "",
              "  function handleChange(e: ChangeEvent<HTMLInputElement>) {",
              "    const next = Number(e.target.value) || 0;",
              "    if (!isControlled) setInternal(next);     // own it ONLY when uncontrolled",
              "    onChange?.(next);                         // always report upward",
              "  }",
              "",
              "  return <input type=\"number\" value={current} onChange={handleChange} aria-label=\"Counter\" />;",
              "}",
              "",
              "// <CounterInput />                               — owns itself",
              "// <CounterInput value={qty} onChange={setQty} /> — parent owns it",
              "// <CounterInput value={qty} />                   — ✗ read-only trap; React warns",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The state-ownership map — who holds which dial",
        body: [
          "Every value in an app has exactly one rightful home, and the map reads top-down: (1) derivable values are owned by NOBODY — compute them during render (P04-L6). (2) Values one component needs live in that component. (3) Values siblings share live in their nearest common ancestor — data down as props, events up as callbacks. (4) Values a whole subtree needs without a prop chain — theme, auth user, toast queue — live in context, but only when the audience is broad and the churn is low (the P05-L3 fitness test). (5) Values that must survive renders but never drive paint live in refs.",
          "Two failure modes bookend the map, and both are loud once named. Prop drilling — threading a value through six layers of components that don't care — is the signal for context, but only when the audience is genuinely broad; the moment a value has one producer and one consumer, props are simpler, fully typed, and trivially traceable, and context is ceremony for nothing. The opposite failure is context-for-everything: a global store of app state where every change re-renders half the tree and nobody can trace where a value came from. The map is a spectrum with a filing rule per value — not an ideology.",
        ],
        code: [
          {
            file: "ownership.tsx — the map, as code",
            lang: "tsx",
            code: [
              "function App() {",
              "  const [user, setUser] = useState<User | null>(null);   // broad + low-churn",
              "  const [toasts, setToasts] = useState<Toast[]>([]);     //   → context",
              "  return (",
              "    <UserContext.Provider value={user}>",
              "      <ToastContext.Provider value={{ toasts, push: setToasts }}>",
              "        <Dashboard />        // the whole subtree reads user via context",
              "      </ToastContext.Provider>",
              "    </UserContext.Provider>",
              "  );",
              "}",
              "",
              "function Dashboard() {",
              "  const [range, setRange] = useState<Range>(defaultRange); // two siblings need it",
              "  return (                                                  // → nearest common",
              "    <>                                                      //   ancestor OWNS it",
              "      <RangeFilter value={range} onChange={setRange} />     // data down,",
              "      <Chart range={range} />                               // events up",
              "    </>",
              "  );",
              "}",
              "",
              "function Chart({ range }: { range: Range }) {",
              "  const points = useMemo(() => compute(range), [range]); // derived: nobody owns it",
              "  const lastHover = useRef<Point | null>(null);          // paint-irrelevant: a ref",
              "  return <svg>{points.map((p) => <Dot key={p.id} {...p} />)}</svg>;",
              "}",
            ].join("\n"),
            caption: "Four values, four different homes — and each placement is a sentence you can say out loud. That's the skill.",
          },
        ],
      },
      {
        heading: "The six-point contract review",
        body: [
          "Before any component graduates to a shared folder, walk the checklist — two minutes now, a week of consumer workarounds saved later. (1) Minimal surface: every prop earns its place; delete anything callers never set. (2) Impossible states unrepresentable: unions over boolean clusters; nonsense should be a compile error. (3) Defaults are decisions: optional means right-most-of-the-time, and the choice is documented in the type's neighborhood. (4) Composable: content flows through children and compound slots, not configuration objects with changelogs. (5) One state owner: the component's internals are its private business; shared state follows the ownership map and no value has two homes. (6) Typed at the edge: the props interface is exported, variants are discriminated, and the friendly outside-parent errors tell people what they did wrong.",
          "Notice this is the same receipt logic as the TypeScript rename refactor (P03-L6) and the Hooks Gauntlet's audit (P05-L8): the type system and the checklist convert 'probably fine' into 'verifiably fine'. Teams that run this review ship components people trust; teams that skip it ship components people wrap in other components to work around.",
        ],
      },
    ],
    mistake: {
      title: "The god component",
      wrong: [
        "// every request becomes another knob — the panel is now a wall:",
        "type DataGridProps = {",
        "  rows: Row[];",
        "  columns: Column[];",
        "  isLoading?: boolean;",
        "  isError?: boolean;",
        "  errorMessage?: string;",
        "  isEmpty?: boolean;",
        "  emptyMessage?: string;",
        "  selectable?: boolean;",
        "  selectedIds?: string[];",
        "  onSelect?: (ids: string[]) => void;",
        "  sortable?: boolean;",
        "  sortKey?: string;",
        "  sortDir?: \"asc\" | \"desc\";",
        "  onSort?: (k: string, d: \"asc\" | \"desc\") => void;",
        "  paginated?: boolean;",
        "  page?: number;",
        "  pageSize?: number;",
        "  onPage?: (p: number) => void;",
        "  expandable?: boolean;",
        "  renderExpanded?: (row: Row) => ReactNode;",
        "  toolbar?: ReactNode;",
        "  footer?: ReactNode;",
        "}; // 23 knobs. Callers fear it. Reviewers skim it. Corners go untested.",
      ].join("\n"),
      right: [
        "// one status union kills four booleans; compound slots return",
        "// content decisions to the caller; behaviors become hooks:",
        "type DataGridState =",
        "  | { status: \"loading\" }",
        "  | { status: \"error\"; message: string }",
        "  | { status: \"success\"; rows: Row[] };",
        "",
        "<DataGrid state={state} columns={columns}>",
        "  <DataGrid.Toolbar>{/* the caller's markup, not your config */}</DataGrid.Toolbar>",
        "  <DataGrid.Expanded>{(row) => <OrderDetails row={row} />}</DataGrid.Expanded>",
        "  <DataGrid.Footer>…</DataGrid.Footer>",
        "</DataGrid>",
        "",
        "// selection, sorting, pagination? Hooks the caller composes:",
        "// const selection = useSelection(rows);",
        "// const sort = useSort(columns);",
        "// Still powerful — but misuse doesn't compile, content composes,",
        "// and each concern has ONE owner.",
      ].join("\n"),
      explain:
        "Config-object components metastasize: every feature request is a new optional prop, every caller passes a different subset, and the combinatorial corners (isLoading + errorMessage + rows?) are never tested because they're 'impossible' — right up until they happen. Unions collapse boolean clusters, compound slots return content decisions to callers, and sub-behaviors become composable hooks. The goal was never fewer lines; it's a surface where misuse is unrepresentable and composition is natural.",
    },
    tryIt: [
      "Take a component you wrote in Phase 4 or 5 and re-draft its props interface WITHOUT looking at the implementation. Compare: which props are truly required, which earned a default, which were configuration that should have been children?",
      "Find a loading/error boolean pair in your code and convert it to a status union. Delete the now-illegal combinations and let the compiler walk you to every render site — count how many it finds.",
      "Build the Accordion from section three. Then use <AccordionTrigger> outside an <Accordion> on purpose and read your own error message. Friendly? Specific? Fixable? That's the standard.",
      "Add type=\"single\" support to the Accordion (at most one open item) as a parent prop. Notice what changes inside toggle — and what changes in the parts (nothing). That silence is the payoff.",
      "Render CounterInput twice, side by side: once uncontrolled, once controlled by a parent with a log. Re-render the parent and verify the uncontrolled instance keeps its own value — then explain ownership out loud.",
    ],
    challenge: {
      prompt:
        "Design and build a Tabs compound component with a contract you'd be proud to ship: <Tabs defaultValue=\"a\"><TabsList><TabsTrigger value=\"a\">…</TabsTrigger>…</TabsList><TabsPanel value=\"a\">…</TabsPanel></Tabs>. Requirements: (1) a private context shares the active value; parts throw a friendly error outside <Tabs>; (2) BOTH controlled (value + onValueChange) and uncontrolled (defaultValue) usage from ONE implementation; (3) the active trigger gets role=\"tab\", aria-selected, and ids wired via useId; panels get role=\"tabpanel\" with matching aria-labelledby; (4) only the active panel renders; (5) a written two-sentence API note: what you made required vs optional, and which decisions you absorbed as defaults vs pushed onto callers.",
      hints: [
        "The context carries { value, activate, baseId }. Tabs computes value as controlled ?? internal exactly like CounterInput — and calls onValueChange in BOTH modes, because controlled parents usually want the event too.",
        "Wire accessibility from one useId inside Tabs: baseId = useId(); each trigger gets id={`${baseId}-tab-${v}`} and aria-controls pointing at `${baseId}-panel-${v}`; panels reverse the pair. One id factory, zero collisions across instances.",
        "Test the duality honestly: render two instances — one with defaultValue, one with value+onValueChange driven by a parent's select. Click around; the controlled one must never drift from its parent, the uncontrolled one must never lose its own state on parent re-renders.",
      ],
      solution: [
        "import {",
        "  createContext, useContext, useId, useState, type ReactNode,",
        "} from \"react\";",
        "",
        "type TabsContextValue = { value: string; activate: (v: string) => void; baseId: string };",
        "const TabsContext = createContext<TabsContextValue | null>(null);",
        "",
        "function useTabs(part: string) {",
        "  const ctx = useContext(TabsContext);",
        "  if (!ctx) throw new Error(`<${part}> must live inside <Tabs>`);",
        "  return ctx;",
        "}",
        "",
        "type TabsProps = {",
        "  children: ReactNode;",
        "  defaultValue?: string;            // uncontrolled seed",
        "  value?: string;                   // present → controlled",
        "  onValueChange?: (value: string) => void;",
        "};",
        "",
        "export function Tabs({ children, defaultValue = \"\", value, onValueChange }: TabsProps) {",
        "  const isControlled = value !== undefined;",
        "  const [internal, setInternal] = useState(defaultValue);",
        "  const current = isControlled ? value : internal;  // ONE source of truth",
        "  const baseId = useId();",
        "",
        "  function activate(next: string) {",
        "    if (!isControlled) setInternal(next);  // own it only when uncontrolled",
        "    onValueChange?.(next);                 // always report upward",
        "  }",
        "",
        "  return (",
        "    <TabsContext.Provider value={{ value: current, activate, baseId }}>",
        "      {children}",
        "    </TabsContext.Provider>",
        "  );",
        "}",
        "",
        "export function TabsList({ children }: { children: ReactNode }) {",
        "  return <div role=\"tablist\">{children}</div>;",
        "}",
        "",
        "export function TabsTrigger({ value, children }: { value: string; children: ReactNode }) {",
        "  const { value: active, activate, baseId } = useTabs(\"TabsTrigger\");",
        "  const selected = active === value;",
        "  return (",
        "    <button",
        "      type=\"button\"",
        "      role=\"tab\"",
        "      id={`${baseId}-tab-${value}`}",
        "      aria-selected={selected}",
        "      aria-controls={`${baseId}-panel-${value}`}",
        "      tabIndex={selected ? 0 : -1}   // roving tabindex: one tab stop for the group",
        "      onClick={() => activate(value)}",
        "    >",
        "      {children}",
        "    </button>",
        "  );",
        "}",
        "",
        "export function TabsPanel({ value, children }: { value: string; children: ReactNode }) {",
        "  const { value: active, baseId } = useTabs(\"TabsPanel\");",
        "  if (active !== value) return null;   // only the active panel exists",
        "  return (",
        "    <div",
        "      role=\"tabpanel\"",
        "      id={`${baseId}-panel-${value}`}",
        "      aria-labelledby={`${baseId}-tab-${value}`}",
        "    >",
        "      {children}",
        "    </div>",
        "  );",
        "}",
        "",
        "/* API note (the written piece):",
        "   Required: the values on triggers/panels — identity IS the API — and children",
        "   everywhere. Absorbed as defaults: an empty defaultValue (a cold start is",
        "   legal), the controlled/uncontrolled switch inside Tabs so callers never fork,",
        "   and roving tabindex so keyboard users get one tab stop. Pushed to callers:",
        "   panel CONTENT (composition, not config) and the choice of controlled vs",
        "   uncontrolled — the only two decisions a component can't make for them. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A component receives isLoading, isError, and isSuccess booleans. The redesign is…",
        options: [
          "A single status union — illegal combinations stop compiling",
          "Add an isEmpty boolean for completeness",
          "Document which combinations are valid",
          "Make all three required",
        ],
        answer: 0,
        explain: "Four booleans describe one fact and legalize sixteen combinations — most of them nonsense. One discriminated union makes the nonsense unrepresentable and exhaustiveness checkable.",
      },
      {
        q: "In the compound pattern, the shared state lives…",
        options: [
          "In the parent, distributed through a private context",
          "Duplicated in each part for speed",
          "In a global store",
          "In the DOM as data attributes",
        ],
        answer: 0,
        explain: "One state model in the parent; parts are stateless translators. The context is private (unexported) and parts throw friendly errors outside their parent.",
      },
      {
        q: "A component is 'controlled' when…",
        options: [
          "The parent passes value AND receives onChange — the component owns no state for that field",
          "It uses defaultValue",
          "Its props are typed",
          "The parent renders it conditionally",
        ],
        answer: 0,
        explain: "Controlled = the parent is the source of truth and the component reports. value without onChange is the read-only trap React warns about.",
      },
      {
        q: "defaultValue exists so that…",
        options: [
          "An uncontrolled component starts sensibly while owning its own state",
          "Controlled components have a fallback",
          "Props can be omitted safely",
          "React can memoize the initial render",
        ],
        answer: 0,
        explain: "defaultValue seeds internal state once, at mount. If the component is controlled (value present), defaultValue is ignored — the parent owns the value entirely.",
      },
      {
        q: "The signal that a value should move from props to context is…",
        options: [
          "A broad audience with low churn — many layers need it, it rarely changes",
          "Any prop passed more than once",
          "The value is an object",
          "The component re-renders often",
        ],
        answer: 0,
        explain: "Context pays off for theme/auth/toast-shaped values: everyone needs them, nobody changes them often. One producer + one consumer stays props — simpler, typed, traceable.",
      },
      {
        q: "Which is NOT on the six-point contract review?",
        options: [
          "The component renders fast on first paint",
          "Impossible states are unrepresentable",
          "Defaults are decisions",
          "Exactly one state owner per value",
        ],
        answer: 0,
        explain: "Performance is measured later with the profiler (P06-L4) — the contract review is about API shape: minimal surface, unrepresentable nonsense, real defaults, composability, single ownership, typed edges.",
      },
    ],
    flashcards: [
      { front: "Props contract", back: "The typed agreement between component and callers: required = cannot work without; optional = default is right; variants = discriminated unions. Misuse should not compile." },
      { front: "Impossible states", back: "One fact, one union: status: 'loading' | 'error' | 'success' with payloads per state. Boolean clusters are one dial wearing costumes — and all their combinations are 'legal'." },
      { front: "Compound components", back: "Parent owns the state, parts share it through a private context, callers compose markup instead of configuring objects. The Radix/shadcn house style." },
      { front: "Controlled component", back: "value + onChange: the parent keeps the state, the component reports. Needed when the value drives other UI while typing." },
      { front: "Uncontrolled component", back: "defaultValue + internal state: the component owns the value; the parent reads on demand. Ideal when only the submit matters." },
      { front: "The dual switch", back: "const isControlled = value !== undefined — one implementation serves both contracts: render the prop when controlled, run internal state otherwise. Never fork." },
      { front: "State-ownership map", back: "Derived → nobody (compute it). One consumer → local. Siblings → nearest common ancestor. Broad + low-churn → context. Survives renders, never paints → ref." },
      { front: "Prop drilling signal", back: "Threading a value through many don't-care layers. The fix is context ONLY when audience is broad and churn low; one producer + one consumer stays props." },
    ],
    recap: [
      "Props are the owner's manual: small, defaulted, union-varianted — misuse must not compile.",
      "One fact, one union: boolean clusters are lies waiting for a 3 a.m. combination.",
      "Compound parts + private context beat configuration objects; composition is the API.",
      "Controlled and uncontrolled are one implementation with a value !== undefined switch.",
      "Ownership map first: derived / local / lifted / context / ref — every value has exactly one home.",
    ],
    references: [
      { label: "react.dev — Thinking in React", url: "https://react.dev/learn/thinking-in-react" },
      { label: "react.dev — Choosing the State Structure", url: "https://react.dev/learn/choosing-the-state-structure" },
      { label: "react.dev — Passing Data Deeply with Context", url: "https://react.dev/learn/passing-data-deeply-with-context" },
      { label: "Radix UI — Accordion (compound reference)", url: "https://www.radix-ui.com/primitives/docs/components/accordion" },
      { label: "Kent C. Dodds — Compound Components", url: "https://kentcdodds.com/blog/compound-components-with-react-hooks" },
    ],
    nextBridge:
      "Your components now have manuals worth reading. But manuals don't cover what happens when the machine catches fire mid-render, or when a room is still under construction when the guests arrive. Next: error boundaries, Suspense, and transitions — the resilience layer that keeps one broken panel from sinking the ship.",
  },
  {
    id: "p06-l2",
    phaseId: "p06",
    title: "Error Boundaries, Suspense & Transitions",
    level: "React Developer",
    minutes: 40,
    summary:
      "Without a net, one render error unmounts your entire app — that is React's default, by design. This lesson installs the resilience layer: error boundaries that seal render-phase fires per feature (and the four kinds of fire they never catch), Suspense as the declarative contract for pending content, React.lazy code-splitting with intent-based preloading, and the transition trick that swaps slow views without a skeleton flash. The result is a shell where one broken or slow panel never takes down a sibling.",
    prerequisites: ["p06-l1 — component APIs & state ownership", "p05-l5 — transitions, deferred values & use"],
    objectives: [
      "State exactly what error boundaries catch — render-phase errors below them — and the four things that escape.",
      "Implement a typed, reusable ErrorBoundary with fallback, reset, and an onError reporting seam.",
      "Place boundaries per feature and explain why one root boundary is a white-screen machine with manners.",
      "Use Suspense correctly: cached promises only, layout-holding fallbacks, composed inner boundaries.",
      "Code-split routes with React.lazy and preload chunks on hover/focus intent.",
      "Combine boundary + Suspense + useTransition so slow swaps dim instead of flash and crashes stay contained.",
    ],
    simple:
      "An error boundary is a bulkhead in a ship: when one compartment floods, the door seals and the rest of the vessel keeps sailing — dinner service continues upstairs while the engine room apologizes. A Suspense boundary is a construction curtain: 'this room isn't ready yet — here's the planned placeholder instead.' And useTransition is the crew's judgment: when the renovation is quick, they let you keep walking the old route until the new one opens, instead of hanging a curtain for eighty milliseconds. Three mechanisms, one promise: no single room ever takes the ship down.",
    why:
      "Users do not forgive white screens — and React's default behavior is brutal on purpose: a render error unmounts the WHOLE tree, because rendering halfway-broken UI would be worse. Without boundaries, one renamed API field in one chart widget is a full-app outage. With per-feature boundaries, it's one panel saying 'try again' while the rest of the product keeps working. Suspense earns its slot the same way: every data-driven UI has a pending phase, and Suspense is THE declarative contract for it — the one Phase 9 (Next.js) and Phase 19 (TanStack Query) are built directly on. Learn the contract now and those frameworks read like footnotes.",
    mentalModel: {
      title: "Bulkheads, curtains, and crew judgment",
      body: "Picture the app as a cruise ship. No bulkheads: one hull breach floods every deck — that's a render error with no boundary; the whole tree unmounts, white screen, user gone. A boundary is a sealed door around a compartment: the flooded room shows its own emergency sign (a fallback with a reset) while everything else continues. Suspense is the renovation curtain on the promenade — behind it, work is genuinely happening, and the curtain IS the announcement; hang a cheap inner curtain per room so one slow renovation never blocks the fast ones. And useTransition is crew judgment: for quick work, don't hang the curtain at all — dim the old route and open the new one when it's ready. The discipline is placement: seal around the volatile, curtain around the slow, and keep the stairwells (nav, header) outside every risk so users always have a way home.",
    },
    sections: [
      {
        heading: "Error boundaries — the last class component you'll write",
        body: [
          "React made one deliberate exception to function components: catching a child's render error requires lifecycle methods hooks don't have, so boundaries are classes. The API is two methods. static getDerivedStateFromError(error) is pure and returns the fallback state — 'switch to the emergency sign'. componentDidCatch(error, info) runs after commit and is the side-effect hook: report to Sentry (Phase 33), queue a toast, phone home. When anything below the boundary throws during render, React swaps that subtree for your fallback — and ONLY that subtree.",
          "Type the boundary like any other component and make the fallback useful: say what broke in human words, offer a retry, never apologize for the whole app. The retry is the reset trick — clearing the error state remounts the children fresh, a second chance with a clean slate. And expose onError as a prop: a boundary that catches silently is a bug factory, because nobody ever learns what broke.",
        ],
        code: [
          {
            file: "ErrorBoundary.tsx — typed, resettable, reporting",
            lang: "tsx",
            code: [
              "import { Component, type ErrorInfo, type ReactNode } from \"react\";",
              "",
              "type Props = {",
              "  fallback?: (error: Error, reset: () => void) => ReactNode;",
              "  onError?: (error: Error, info: ErrorInfo) => void;  // → Sentry, Phase 33",
              "  children: ReactNode;",
              "};",
              "type State = { error: Error | null };",
              "",
              "export class ErrorBoundary extends Component<Props, State> {",
              "  state: State = { error: null };",
              "",
              "  static getDerivedStateFromError(error: Error): State {",
              "    return { error };                      // pure: 'show the fallback'",
              "  }",
              "",
              "  componentDidCatch(error: Error, info: ErrorInfo) {",
              "    this.props.onError?.(error, info);     // report — never swallow",
              "  }",
              "",
              "  reset = () => this.setState({ error: null }); // children remount fresh",
              "",
              "  render() {",
              "    const { error } = this.state;",
              "    if (error) {",
              "      return this.props.fallback?.(error, this.reset) ?? (",
              "        <div role=\"alert\" className=\"panel p-5\">",
              "          <p>Something broke in this panel.</p>",
              "          <button type=\"button\" onClick={this.reset}>Try again</button>",
              "        </div>",
              "      );",
              "    }",
              "    return this.props.children;",
              "  }",
              "}",
              "",
              "// containment, not blanket coverage:",
              "<ErrorBoundary onError={reportToSentry} fallback={(err, reset) => (",
              "  <PanelError message={err.message} onRetry={reset} />",
              ")}>",
              "  <RevenueChart />          {/* a fire here stays HERE */}",
              "</ErrorBoundary>",
            ].join("\n"),
            caption: "The reset button works because clearing the error re-renders the children from scratch — a fresh subtree, a second chance.",
          },
        ],
      },
      {
        heading: "What boundaries never catch — and what to do instead",
        body: [
          "Boundaries catch render-phase errors in the subtree below them — full stop. Four fires escape the net, and each has its own extinguisher. (1) Event handlers: an onClick that throws is your code running outside render — wrap it in try/catch and surface a toast; the app keeps running. (2) Async code: a rejected promise inside an effect needs its own .catch — the boundary finished rendering long before the rejection lands. (3) Server-side rendering: boundaries are a client mechanism; frameworks provide server error UI (Next.js's error.tsx in Phase 9). (4) The boundary's own render: a broken fallback can't catch itself — nest a cheap outer boundary around fancy inner ones when the fallback is non-trivial.",
          "Placement is the actual skill, and the anti-pattern is one boundary at <App> — a white-screen machine with better manners: every error still nukes everything, just behind a full-page apology. The professional layout wraps the volatile, the third-party, and the data-heavy, each in its OWN boundary, while cheap chrome (nav, header, the way home) stays outside every risk. The lab below lets you break things three ways and watch exactly which nets hold.",
        ],
        code: [
          {
            file: "escapes.tsx — the four fires and their extinguishers",
            lang: "tsx",
            code: [
              "// 1) event handlers — try/catch + toast, NOT a boundary:",
              "async function onSave() {",
              "  try {",
              "    await api.save(draft);",
              "  } catch (err) {",
              "    toasts.push(\"Couldn't save — your draft is still here.\");",
              "  }",
              "}",
              "",
              "// 2) async inside effects — the effect's own catch:",
              "useEffect(() => {",
              "  const controller = new AbortController();",
              "  fetchStats(controller.signal).catch((err) => {",
              "    if (err.name !== \"AbortError\") reportToSentry(err);",
              "  });",
              "  return () => controller.abort();",
              "}, []);",
              "",
              "// 3) a boundary can't catch ITSELF — nest for fancy fallbacks:",
              "<ErrorBoundary fallback={<PlainSorry />}>        {/* cheap outer */}",
              "  <ErrorBoundary fallback={<FancyPanelError />}> {/* may break */}",
              "    <VolatileWidget />",
              "  </ErrorBoundary>",
              "</ErrorBoundary>",
            ].join("\n"),
          },
        ],
        demo: "error-lab",
      },
      {
        heading: "Suspense — the declarative curtain",
        body: [
          "Suspense answers one question: what shows while a subtree isn't ready? A component suspends by throwing a PROMISE mid-render — designed behavior, not an error. React catches it, renders the nearest <Suspense fallback>, and retries the subtree when the promise settles. In current stable React, three things suspend: use() reading a cached promise, React.lazy components mid-load, and framework data fetching (Next.js, Phase 9). Note the word cached: you may not fetch inside render — the promise must exist OUTSIDE the render pass, or every render throws a brand-new promise and the curtain never lifts.",
          "Fallback design is UX, not afterthought: hold the layout (a skeleton the same size — never a spinner that reflows the page), match the content's shape, and compose boundaries: an outer one for the slow initial load, inner ones per independent panel, so a slow chart never blocks a fast table. The fast/slow split is Suspense's whole economy — the curtain should hang only where work is genuinely pending.",
        ],
        code: [
          {
            file: "suspense.tsx — the protocol, composed",
            lang: "tsx",
            code: [
              "import { Suspense, use } from \"react\";",
              "",
              "// the promise lives OUTSIDE render — created once, read many times:",
              "const statsPromise: Promise<Stats> = fetchStats();",
              "",
              "function StatsPanel() {",
              "  const stats = use(statsPromise);    // suspends until settled",
              "  return <StatsGrid stats={stats} />;",
              "}",
              "",
              "export function Dashboard() {",
              "  return (",
              "    <>",
              "      <ErrorBoundary fallback={(e, r) => <PanelError error={e} onRetry={r} />}>",
              "        <Suspense fallback={<PanelSkeleton height={180} />}>",
              "          <RevenueChart />             {/* slow: its own curtain */}",
              "        </Suspense>",
              "      </ErrorBoundary>",
              "      <Suspense fallback={<TableSkeleton />}>",
              "        <OrdersTable />                {/* fast: paints immediately */}",
              "      </Suspense>",
              "    </>",
              "  );",
              "}",
              "// in real apps the promises come from the data layer —",
              "// TanStack Query (Phase 19) and Next.js manage this cache for you.",
              "// The protocol is what you're learning; the plumbing gets automated.",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "Code-splitting — lazy() is Suspense's first citizen",
        body: [
          "React.lazy turns a dynamic import() into a component that suspends until its chunk arrives — the canonical Suspense citizen. Route-level splitting is the default win: each route becomes its own chunk, and first paint stops paying for pages the user never visits. The ceremony: call lazy() ONCE at module scope (the import promise IS the cache — calling it in render creates a new promise per render and suspends forever, the same rule as section three), and wrap the swap in a Suspense whose fallback holds the layout.",
          "Two refinements separate the professionals: preload on intent — start the import on hover or focus, before the click lands; import() is idempotent, so this is free — and never lazy-load above-the-fold content you need at first paint, which would just add a waterfall where there was none. Verify in the Network tab: one small entry bundle, plus chunks that appear exactly when their routes are visited.",
        ],
        code: [
          {
            file: "routes.tsx — split, and preload on intent",
            lang: "tsx",
            code: [
              "import { lazy, Suspense } from \"react\";",
              "",
              "// ONCE, at module scope — the import promise is the cache:",
              "const SettingsPage = lazy(() => import(\"./pages/Settings\"));",
              "const ReportsPage = lazy(() => import(\"./pages/Reports\"));",
              "",
              "// preload on intent — free, because import() is idempotent:",
              "const preloadSettings = () => { void import(\"./pages/Settings\"); };",
              "",
              "export function Router({ route }: { route: string }) {",
              "  return (",
              "    <Suspense fallback={<PageSkeleton />}>   {/* one curtain for route swaps */}",
              "      {route === \"settings\" ? <SettingsPage /> : <ReportsPage />}",
              "    </Suspense>",
              "  );",
              "}",
              "",
              "// <a href=\"#/settings\"",
              "//    onMouseEnter={preloadSettings} onFocus={preloadSettings}>",
              "//   Settings — the chunk is likely waiting before the click lands",
              "// </a>",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The resilient shell — bulkheads + curtains + judgment",
        body: [
          "Assembled, the pattern is a sandwich around each risky region: ErrorBoundary OUTSIDE, Suspense INSIDE. A panel that loads slowly gets a curtain; a panel that crashes gets a bulkhead; a panel whose skeleton itself breaks is still caught, because the boundary wraps the curtain too. Order matters — and so does what you leave OUTSIDE every boundary: navigation, header, the way home. Users forgive a broken chart; they don't forgive losing their bearings.",
          "The final refinement kills the fallback flash. When switching between two data-heavy views, wrap the update in startTransition: the swap becomes interruptible, so React keeps the OLD view painted — dimmed via isPending — until the new view's data resolves, instead of flashing a skeleton for eighty milliseconds. Fast switches feel instant; slow ones show honest pending state. This is the entire concurrency story from P05-L5 applied: urgent paint wins, slow work waits, and the user never sees the machinery.",
        ],
        code: [
          {
            file: "shell.tsx — the sandwich, assembled",
            lang: "tsx",
            code: [
              "import { Suspense, useState, useTransition } from \"react\";",
              "import { ErrorBoundary } from \"./ErrorBoundary\";",
              "",
              "export function Dashboard() {",
              "  const [tab, setTab] = useState<\"revenue\" | \"orders\">(\"revenue\");",
              "  const [isPending, startTransition] = useTransition();",
              "",
              "  const switchTab = (next: typeof tab) =>",
              "    startTransition(() => setTab(next));   // interruptible: no flash",
              "",
              "  return (",
              "    <main>",
              "      <TabBar tab={tab} onSwitch={switchTab} pending={isPending} />",
              "      <div style={{ opacity: isPending ? 0.6 : 1, transition: \"opacity 150ms\" }}>",
              "        <ErrorBoundary fallback={(e, r) => <PanelError error={e} onRetry={r} />}>",
              "          <Suspense fallback={<PanelSkeleton />}>",
              "            {tab === \"revenue\" ? <RevenuePanel /> : <OrdersPanel />}",
              "          </Suspense>",
              "        </ErrorBoundary>",
              "      </div>",
              "    </main>",
              "  );",
              "}",
              "// slow switch → old panel stays, dims to 60%, new one paints when ready.",
              "// crash in RevenuePanel → THAT panel apologizes; the Orders tab is untouched.",
              "// TabBar lives OUTSIDE every boundary — the stairwell is never sealed.",
            ].join("\n"),
          },
        ],
      },
    ],
    mistake: {
      title: "The root-only boundary",
      wrong: [
        "// one net under the whole ship — 'we have error handling':",
        "root.render(",
        "  <ErrorBoundary fallback={<FullScreenSorry />}>",
        "    <App />",
        "  </ErrorBoundary>",
        ");",
        "",
        "// fourteen months later: a chart widget's API renames a field,",
        "// its render throws — and the ENTIRE app white-screens behind",
        "// FullScreenSorry. The nav, the search, the user's unsaved form",
        "// on another screen: all gone. The boundary 'worked'.",
        "// The PLACEMENT was the bug.",
      ].join("\n"),
      right: [
        "root.render(<App />);",
        "",
        "function App() {",
        "  return (",
        "    <Shell>",
        "      <Header />   {/* cheap chrome stays OUTSIDE every risk */}",
        "      <main>",
        "        <ErrorBoundary fallback={<PanelError />}>",
        "          <Suspense fallback={<ChartSkeleton />}><RevenueChart /></Suspense>",
        "        </ErrorBoundary>",
        "        <ErrorBoundary fallback={<PanelError />}>",
        "          <Suspense fallback={<TableSkeleton />}><OrdersTable /></Suspense>",
        "        </ErrorBoundary>",
        "        <ErrorBoundary fallback={<PanelError />}>",
        "          <ThirdPartyWidget />   {/* the volatile gets its own door */}",
        "        </ErrorBoundary>",
        "      </main>",
        "    </Shell>",
        "  );",
        "}",
        "// the chart breaks → one panel apologizes, everything else works,",
        "// and the user never loses their place.",
      ].join("\n"),
      explain:
        "A single root boundary converts every component bug into a full-app outage with better manners — containment without segmentation is theater. The skill is placement: seal around the volatile, the third-party, and the data-heavy; keep the way home outside every risk. The instinct scales directly to Phase 8, where Next.js error.tsx files ARE per-route boundaries — same discipline, framework plumbing.",
    },
    tryIt: [
      "In the lab below, detonate the render bomb, then hit reset. Notice the inner counter's state is GONE (it remounted fresh) — that's the reset trick. Then throw in the event handler and confirm the counter keeps ticking.",
      "Wrap one panel of your Phase 4/5 app in the typed ErrorBoundary and force a render error (throw when a prop equals a magic value). Read your own fallback as a user: does it say what broke, and offer a way back?",
      "Nest a cheap boundary around a fancy one whose fallback itself throws. Confirm the outer catches it — then explain in one sentence why a boundary can never catch itself.",
      "Build the cached-promise component from section three with a 1.5s delay and a layout-holding skeleton. Then wrap the switch that shows it in startTransition and watch the skeleton flash disappear.",
      "Lazy-load a route in a scratch app. Open the Network tab and confirm a second chunk appears only on navigation; add hover-preload and watch the chunk arrive before the click.",
    ],
    challenge: {
      prompt:
        "Build the resilient dashboard shell as a portfolio piece: three panels — RevenueChart (1.2s load), OrdersTable (0.4s load), and a third-party WeatherWidget that crashes every third render. Requirements: (1) each panel in its OWN ErrorBoundary + Suspense pair, skeletons sized to hold layout; (2) the crashing widget's boundary shows a PanelError with a WORKING retry (a ref counts renders, so the remount after reset succeeds); (3) the header and nav live outside every boundary and always work; (4) switching panels uses startTransition — no skeleton flash, a subtle dim instead; (5) every boundary reports through onError into a corner incident log (your stand-in for Sentry); (6) three written sentences: which errors your boundaries catch, which they can't, and what catches those.",
      hints: [
        "The crash counter: const renders = useRef(0); renders.current += 1; if (renders.current % 3 === 0) throw new Error('widget exploded'). The boundary's reset remounts the widget with a fresh ref — so the first render after retry is safe. The boundary ENABLES the recovery.",
        "The incident log wants an array of { at, message } lifted to the shell — three producers (the boundaries' onError), one consumer (the log panel). Per the ownership map: nearest common ancestor owns it, callbacks flow down.",
        "For the no-flash proof: cache each panel's promise at module scope; switch to Revenue and back quickly. With startTransition the old panel dims and stays; remove the transition and watch the skeleton flash. Both behaviors are 'correct' — one is professional.",
      ],
      solution: [
        "import { Suspense, useRef, useState, useTransition, type ReactNode } from \"react\";",
        "import { ErrorBoundary } from \"./ErrorBoundary\";",
        "",
        "// ——— cached promises: created once, outside render ———",
        "const wait = (ms: number) => new Promise((r) => setTimeout(r, ms));",
        "const revenuePromise = wait(1200).then(() => [42, 58, 51, 73, 66]);",
        "const ordersPromise = wait(400).then(() => [\"#1041 paid\", \"#1042 shipped\"]);",
        "",
        "function RevenueChart() {",
        "  const data = use(revenuePromise);   // suspends until settled",
        "  return <ChartBars data={data} />;",
        "}",
        "function OrdersTable() {",
        "  const rows = use(ordersPromise);",
        "  return <Rows rows={rows} />;",
        "}",
        "",
        "// ——— the widget that crashes every third render ———",
        "function WeatherWidget() {",
        "  const renders = useRef(0);",
        "  renders.current += 1;",
        "  if (renders.current % 3 === 0) {",
        "    throw new Error(\"widget exploded (render #\" + renders.current + \")\");",
        "  }",
        "  return <p>☁ 17° — renders so far: {renders.current}</p>;",
        "}",
        "",
        "// ——— the shell: boundaries, curtains, transitions, the log ———",
        "type Incident = { at: string; message: string };",
        "",
        "export function Dashboard() {",
        "  const [tab, setTab] = useState<\"revenue\" | \"orders\" | \"weather\">(\"revenue\");",
        "  const [incidents, setIncidents] = useState<Incident[]>([]);",
        "  const [isPending, startTransition] = useTransition();",
        "",
        "  const report = (panel: string) => (error: Error) =>",
        "    setIncidents((log) => [...log, { at: new Date().toLocaleTimeString(), message: `${panel}: ${error.message}` }]);",
        "",
        "  const switchTab = (next: typeof tab) => startTransition(() => setTab(next));",
        "",
        "  const panel = (name: string, node: ReactNode, fallback: ReactNode) => (",
        "    <ErrorBoundary",
        "      onError={report(name)}",
        "      fallback={(err, reset) => (",
        "        <PanelError message={err.message} onRetry={reset} />  // reset remounts fresh",
        "      )}",
        "    >",
        "      <Suspense fallback={fallback}>{node}</Suspense>",
        "    </ErrorBoundary>",
        "  );",
        "",
        "  return (",
        "    <div>",
        "      {/* the stairwell: OUTSIDE every boundary, always works */}",
        "      <header>",
        "        <nav>",
        "          {([\"revenue\", \"orders\", \"weather\"] as const).map((t) => (",
        "            <button key={t} type=\"button\" onClick={() => switchTab(t)} aria-pressed={tab === t}>",
        "              {t}",
        "            </button>",
        "          ))}",
        "        </nav>",
        "      </header>",
        "",
        "      <main style={{ opacity: isPending ? 0.6 : 1, transition: \"opacity 150ms\" }}>",
        "        {tab === \"revenue\" && panel(\"RevenueChart\", <RevenueChart />, <PanelSkeleton height={180} />)}",
        "        {tab === \"orders\" && panel(\"OrdersTable\", <OrdersTable />, <PanelSkeleton height={120} />)}",
        "        {tab === \"weather\" && panel(\"WeatherWidget\", <WeatherWidget />, <PanelSkeleton height={60} />)}",
        "      </main>",
        "",
        "      {/* the incident log — one consumer, three producers via onError */}",
        "      <aside aria-label=\"Incident log\">",
        "        <h2>Incidents</h2>",
        "        <ul>",
        "          {incidents.map((i, n) => <li key={n}><code>{i.at}</code> {i.message}</li>)}",
        "        </ul>",
        "      </aside>",
        "    </div>",
        "  );",
        "}",
        "",
        "/* The three sentences:",
        "   1) The boundaries catch RENDER-phase errors in their subtree —",
        "      the widget's third-render explosion seals into one panel.",
        "   2) They can't catch event-handler throws, rejected promises in",
        "      effects, SSR errors, or their own broken fallbacks.",
        "   3) Those get try/catch + toasts, effect-level .catch, the",
        "      framework's server error UI (Phase 9), and a cheaper outer",
        "      boundary — one extinguisher per kind of fire. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "Error boundaries catch…",
        options: [
          "Render-phase errors in the subtree below them",
          "Every error anywhere in the app",
          "Errors thrown in event handlers",
          "Rejected promises inside effects",
        ],
        answer: 0,
        explain: "One mechanism, one scope: render-phase errors below the boundary. Events, async, SSR, and the boundary's own render all escape — each has its own tool.",
      },
      {
        q: "The reset trick works by…",
        options: [
          "Clearing the boundary's error state so the children remount fresh",
          "Reloading the page",
          "Clearing the Suspense cache",
          "Calling componentDidCatch again",
        ],
        answer: 0,
        explain: "error → null re-renders the children from scratch: new refs, new effects, a clean slate. That's why crash-counters in refs recover after a retry.",
      },
      {
        q: "Which fire does a boundary NOT catch?",
        options: [
          "An error thrown inside an onClick handler",
          "A render error in a child component",
          "A render error in a grandchild",
          "An error thrown while rendering a Suspense fallback inside it",
        ],
        answer: 0,
        explain: "Handlers run outside the render phase, so their errors bypass boundaries entirely — try/catch + a toast is the extinguisher. The app keeps running.",
      },
      {
        q: "A component suspends by…",
        options: [
          "Throwing a cached promise during render; React shows the nearest fallback until it settles",
          "Returning null",
          "Calling setState during render",
          "Throwing an Error with a special name",
        ],
        answer: 0,
        explain: "That's the protocol — designed behavior, not an error. And the promise must be cached outside render, or every render throws a new one and the curtain never lifts.",
      },
      {
        q: "React.lazy must be called at module scope because…",
        options: [
          "The import promise is the cache — in render, a new promise per render suspends forever",
          "It's faster there",
          "React forbids dynamic imports in components",
          "Chunks can only load before mount",
        ],
        answer: 0,
        explain: "Same rule as use(): suspendables need a stable promise. lazy() in render manufactures a fresh import per render — infinite curtain. Module scope (or a loader) is the cache.",
      },
      {
        q: "startTransition + Suspense prevents…",
        options: [
          "The fallback flash — the old view stays painted (dimmed) until the new one is ready",
          "Errors during the transition",
          "The need for Suspense at all",
          "Bundle loading",
        ],
        answer: 0,
        explain: "The transition marks the swap interruptible: React keeps the stale content showing (isPending dims it) while the new view resolves. Fast switches feel instant; slow ones show honest pending state.",
      },
    ],
    flashcards: [
      { front: "Error boundary", back: "A class component that catches render-phase errors in its subtree and swaps in a fallback. The ship's bulkhead — placement per feature is the skill." },
      { front: "The four escapes", back: "Boundaries don't catch: event handlers, async/effect rejections, SSR errors, or their own render. Extinguishers: try/catch, .catch, framework UI, nested outer boundary." },
      { front: "getDerivedStateFromError vs componentDidCatch", back: "The first is pure and returns fallback state (render phase). The second is the side-effect hook for reporting (commit phase). Never swallow — always report." },
      { front: "The reset trick", back: "Clear the boundary's error state → children remount fresh. A second chance with clean refs and effects; crash-counters recover." },
      { front: "The Suspense protocol", back: "A component suspends by throwing a promise; React renders the nearest fallback and retries on settle. The promise must be cached OUTSIDE render." },
      { front: "React.lazy", back: "Dynamic import() as a suspendable component: route-level code splitting, preload on hover/focus intent (import() is idempotent), never lazy the first paint." },
      { front: "The resilience sandwich", back: "ErrorBoundary outside, Suspense inside: slow panels get curtains, broken panels get bulkheads, and even a broken skeleton is still caught." },
      { front: "Transition + Suspense", back: "startTransition makes the swap interruptible: old view stays (dimmed via isPending) until the new one resolves. No skeleton flash — urgent paint wins." },
    ],
    recap: [
      "Boundaries seal render-phase fires; per-feature placement is the actual skill, one root boundary is theater.",
      "Events, async, SSR, and the boundary itself escape the net — own each extinguisher explicitly.",
      "Suspense is the declarative pending contract: cached promises, layout-holding fallbacks, composed curtains.",
      "lazy splits routes at module scope; preload on intent; never lazy the first paint.",
      "Boundary outside + Suspense inside + transitions coordinating = a ship that never sinks whole.",
    ],
    references: [
      { label: "react.dev — Catching rendering errors with an error boundary", url: "https://react.dev/reference/react/Component#catching-rendering-errors-with-an-error-boundary" },
      { label: "react.dev — Suspense", url: "https://react.dev/reference/react/Suspense" },
      { label: "react.dev — lazy", url: "https://react.dev/reference/react/lazy" },
      { label: "react.dev — use", url: "https://react.dev/reference/react/use" },
      { label: "MDN — import() (dynamic imports)", url: "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/import" },
    ],
    nextBridge:
      "The ship seals its bulkheads and hangs its curtains. Next: the state classification map — local, URL, server, form, auth, global — and optimistic UI that lies beautifully, rolls back honestly, and never lets a pretty animation become an untruth.",
  },
];

export const LESSON_CONTENT_P6: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P6.map((l) => [l.id, l]),
);
