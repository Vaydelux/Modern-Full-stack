import type { LessonContent } from "./types";

/**
 * Pass 017 bounded content batch: Phase 6 continues (L3–L4).
 * Every lesson fulfills the full quality contract. No placeholders.
 */
export const LESSONS_P6B: LessonContent[] = [
  {
    id: "p06-l3",
    phaseId: "p06",
    title: "Optimistic UI & the State Classification Map",
    level: "React Developer",
    minutes: 40,
    summary:
      "Every value in an app has a home, and most 'state bugs' are values living in the wrong one. This lesson installs the six-state classification map — local, derived, form, URL, server, auth — that routes every decision from here through the capstones, then teaches optimistic UI the trustworthy way: intent painted instantly, the network still gets its say, rollback on rejection, errors surfaced rather than swallowed — with a live failure-injection lab to prove the round trip.",
    prerequisites: ["p06-l1 — state ownership & component APIs", "p05-l6 — useOptimistic & action hooks", "p05-l2 — async effects & races"],
    objectives: [
      "Classify any value into one of six state homes (local, derived, form, URL, server, auth) and name its owner and tool.",
      "Describe the optimistic round trip: intent → overlay → network → commit or rollback — and why rollback must be structural, not improvised.",
      "Apply the optimism boundary: cheap + reversible intents get overlays; costly or irreversible actions get pending + confirmation.",
      "Design post-rollback UX: the error is surfaced with recovery, and the user is never blamed for the lie.",
      "Anticipate double-fire and overlapping mutations, and explain what idempotency keys will solve in Phase 29.",
      "Keep committed server state as the single source of truth — optimistic values never persist or feed calculations.",
    ],
    simple:
      "A good waiter takes your order and walks away with it — you don't stand at the counter until the kitchen confirms the potatoes. Optimistic UI is that confidence applied honestly: the moment you click 'Like', the heart fills (the order is taken), the request travels to the server (the kitchen), and when it confirms, the filled heart simply becomes the truth. If the kitchen rejects it — sold out, no permission, network died — a good waiter comes back, tells you, and removes it from the bill. The bad version is the waiter who writes 'potatoes' on your bill before asking the kitchen and then quietly erases it when told no. Users can forgive a visible correction; they cannot forgive a silent lie.",
    why:
      "Network round trips are the felt cost of every modern app — a like button that waits 400ms feels broken even when it works. But optimism mishandled is worse than latency: phantom likes that vanish, toggles that snap back with no explanation, totals computed from values the server never accepted. The classification map earns its place for the same reason: from Phase 8 onward, every feature you build asks 'where does this value live?' — URL filters, server caches, auth sessions, form drafts — and each wrong answer becomes a bug class (stale caches, duplicated sources, unshareable views). These two ideas are the architectural floor the rest of the course builds on.",
    mentalModel: {
      title: "The confident waiter",
      body: "Optimism is a service pattern, not a data pattern. The overlay is the waiter's pad: a confident record of INTENT, shown immediately, understood by everyone to be provisional. The server is the kitchen: the only place where the order becomes real. Commit is the kitchen nodding; rollback is the waiter returning with an apology and a corrected bill. The rule that keeps the restaurant trustworthy: the bill (your committed state) is only ever written from the kitchen's word — the pad never gets copied into the ledger, never feeds the totals, never survives a refresh. And some orders — 'cancel the whole reservation', 'charge this card' — no good waiter takes optimistically: those get 'one moment, please' and a confirmation.",
    },
    sections: [
      {
        heading: "The six-state map — where every value lives",
        body: [
          "Six homes cover every value in every app you will build, and each has exactly one legitimate owner. LOCAL UI state: things the UI needs that nobody else cares about — a dropdown open flag, the selected tab — owned by useState at the lowest component that needs it. DERIVED state: anything computable from other state — filtered lists, totals, 'has errors' — owned by NOBODY, computed during render; storing it is the sync-bug factory from P05-L2. FORM state: what the user is mid-typing — owned by the form library (React Hook Form in Phase 20) or controlled inputs, explicitly not yet truth.",
          "URL state: the shareable view — filters, sort, page, the open panel — owned by the router (searchParams in Phase 9), because it must survive refresh and live in a link. SERVER state: the cache of remote truth — tasks, users, inventory — owned by a cache layer (TanStack Query from Phase 19), never by scattered useStates that each fetch independently. AUTH/session state: who the user is and what they may do — owned by Supabase Auth (Phase 17), read-only to your components. The diagnostic question for any bug: 'which home does this value live in, and is something else squatting there?' Server data in local state, URL-worthy filters in useState, derived counts stored as facts — every one is a known failure with a known eviction notice.",
        ],
        code: [
          {
            file: "classification.ts — one value, six possible homes",
            lang: "ts",
            code: [
              "// LOCAL — UI-only, lowest owner:",
              "const [menuOpen, setMenuOpen] = useState(false);",
              "",
              "// DERIVED — compute, never store:",
              "const openCount = tasks.filter((t) => !t.done).length;",
              "",
              "// FORM — provisional by design:",
              "const { register } = useForm<TaskDraft>();   // Phase 20",
              "",
              "// URL — shareable, refresh-surviving:",
              "const [params] = useSearchParams();          // ?status=open&page=2",
              "const status = params.get(\"status\") ?? \"all\";",
              "",
              "// SERVER — cached remote truth, one owner:",
              "const { data: tasks } = useQuery({           // Phase 19",
              "  queryKey: [\"tasks\", status],",
              "  queryFn: () => api.listTasks(status),",
              "});",
              "",
              "// AUTH — read-only identity:",
              "const { data: session } = supabase.auth.getSession(); // Phase 17",
            ].join("\n"),
            caption: "The same 'filter' can be URL state (shareable dashboard) or local state (ephemeral modal). The map isn't dogma — it's the question you answer deliberately.",
          },
        ],
      },
      {
        heading: "The optimistic round trip — anatomy of an honest overlay",
        body: [
          "Four beats, in order. BEAT ONE, intent: the user acts; you apply the expected outcome to a PROVISIONAL layer instantly — useOptimistic's overlay, or a cache-layer optimistic update — and paint. Zero waiting. BEAT TWO, the network: the real request flies; the overlay carries a quiet 'pending' affordance (a subdued style, a spinner in the corner) for anyone paying attention. BEAT THREE, commit: the server confirms; the provisional value is replaced by the COMMITTED value the server returned — note: by the server's value, not your guess, since the server may normalize (timestamps, ids, trimmed fields). BEAT FOUR, rollback: rejection reverts the overlay to the last committed truth AND surfaces a recoverable error — toast with Retry, inline message, never a silent snap-back.",
          "The structural insight: rollback is free when optimism lives in a layer the framework discards for you (useOptimistic's overlay evaporates on settle; TanStack Query's optimistic data unwinds on error). Hand-rolled optimism — writing the guess straight into your source of truth and 'fixing it later' — is where silent lies breed, because the revert is now YOUR responsibility under every failure mode: network error, 4xx, 5xx, timeout, tab closed mid-flight. Prefer the layer that owns unwinding; when you must hand-roll, the revert path is written FIRST, not after the happy path works.",
        ],
        code: [
          {
            file: "roundtrip.tsx — the four beats with useOptimistic",
            lang: "tsx",
            code: [
              "function LikeButton({ postId, liked, onToggle }: Props) {",
              "  // overlay state: what the UI shows WHILE the round trip is open",
              "  const [optimisticLiked, setOptimisticLiked] = useOptimistic(liked);",
              "",
              "  async function handleClick() {",
              "    setOptimisticLiked(!optimisticLiked);   // beat 1: instant intent",
              "    try {",
              "      const result = await onToggle();      // beat 2: the network speaks",
              "      // beat 3 happens twice: the overlay evaporates on settle,",
              "      // and the parent commits result.liked — the SERVER's value.",
              "      if (!result.ok) throw new Error(result.error);",
              "    } catch {",
              "      // beat 4: overlay already reverted; surface + offer recovery:",
              "      toast(\"Couldn't save your like.\", {",
              "        action: { label: \"Retry\", onClick: handleClick },",
              "      });",
              "    }",
              "  }",
              "",
              "  return (",
              "    <button onClick={handleClick} aria-pressed={optimisticLiked}>",
              "      {optimisticLiked ? \"♥\" : \"♡\"}",
              "    </button>",
              "  );",
              "}",
            ].join("\n"),
          },
        ],
        demo: "optimistic-lab",
      },
      {
        heading: "The boundary of optimism — what you may promise instantly",
        body: [
          "Two questions gate every optimistic feature. ONE: is it cheap to reverse? A like, a reorder, a status toggle — yes; a payment capture, an account deletion, a sent email — no. TWO: is it reversible at all? Reordering a list can always be put back; a webhook fired to a third party cannot be un-fired. Only cheap-AND-reversible intents earn an overlay; everything else gets a pending state (button spinner, disabled controls) plus — for the irreversible ones — explicit confirmation before the request even leaves. Optimism on a payment button isn't confidence; it's a support ticket with a timestamp.",
          "The second boundary is subtler: optimistic values must stay provisional in every sense. Never persist them (a localStorage save of the optimistic list resurrects unconfirmed data after refresh). Never compute from them (totals, badges, and 'all done' celebrations read COMMITTED state — an optimistic checkmark must not fire the confetti). Never treat their absence of error as success (the overlay reverting IS the error signal; your monitoring reads the committed path). The phrase to carry: optimism is presentation, the commit is truth — the same trust line the course drew at forms (P01-L2) and will draw again at webhooks (P29).",
        ],
      },
      {
        heading: "When round trips collide — and what Phase 29 will make of it",
        body: [
          "Real users don't wait for round trips to close: they toggle twice in 300ms, they retry on a flaky connection, they submit and navigate away. Each pattern produces a collision: two in-flight mutations for one row (which response wins?), a retry that double-applies (two likes from one intent), a stale response landing last (yesterday's answer overwriting today's — the P02-L6 race wearing a mutation costume). The professional answers arrive in order across the course: mutation-scoped pending states (disable the ONE row, not the app), request ids + 'latest wins' guards (P19), and server-side idempotency keys — a client-generated key the server dedupes, so 'same intent twice' applies exactly once (P29).",
          "The lab above lets you feel the collision: toggle fast while a save is in flight and watch statuses overlap. Nothing breaks visibly — yet — because the demo's reverts are row-scoped. In a real system this is precisely where totals drift and duplicate rows appear, which is why the fix is architectural (idempotent mutations) rather than UI polish (more spinners).",
        ],
      },
    ],
    mistake: {
      title: "The overlay that became the ledger",
      wrong: [
        "function TaskRow({ task }: { task: Task }) {",
        "  const [done, setDone] = useState(task.done); // local copy of server truth",
        "",
        "  async function toggle() {",
        "    setDone(!done);              // 'optimistic'… into the SOURCE of truth",
        "    saveProgress();              // writes `done` to localStorage — unconfirmed",
        "    updateBadge(done);           // badge computed from the GUESS",
        "    await api.toggle(task.id);   // no rollback, no error path, no result",
        "  }",
        "}",
        "// Network fails: checkbox stays checked, badge stays wrong,",
        "// localStorage now resurrects the lie after every refresh,",
        "// and nobody — user or code — knows the server never agreed.",
      ].join("\n"),
      right: [
        "function TaskRow({ task, onToggle }: { task: Task; onToggle: () => Promise<Result<Task>> }) {",
        "  const [overlayDone, setOverlayDone] = useOptimistic(task.done);",
        "",
        "  async function toggle() {",
        "    setOverlayDone(!task.done);             // beat 1: provisional ONLY",
        "    const result = await onToggle();        // beats 2–3: server decides",
        "    if (!result.ok) {",
        "      toast(\"Couldn't update the task.\", {  // beat 4: surfaced + retryable",
        "        action: { label: \"Retry\", onClick: toggle },",
        "      });",
        "    }",
        "    // overlay evaporates either way; the cache commits result.data.",
        "    // badge reads the COMMITTED task. persistence reads the cache.",
        "  }",
        "}",
      ].join("\n"),
      explain:
        "Three sins in one handler: the guess was written into a local source of truth (no structural rollback), it was persisted (the lie outlives the session), and it was computed from (badges downstream of the guess). The fix isn't adding a catch — it's moving optimism into a provisional layer that evaporates on settle, letting the server's committed value be the only thing anything else reads. Presentation optimistic, ledger honest.",
    },
    tryIt: [
      "Take your Phase 4 Todo app and classify every value it holds against the six-home map. Write the table down. Which values are server state pretending to be local? Which derived values did you store?",
      "In the lab: set latency to 1500ms, arm a failure, toggle a row, and watch all four beats. Then toggle the SAME row again at ~700ms (mid-flight). Describe exactly what the statuses do and why the final state is still honest.",
      "Convert one hand-rolled optimistic update in your own code to useOptimistic (or sketch it). Delete the revert code you wrote by hand — notice what the framework now owns.",
      "Find an irreversible action in any app you use (delete, send, pay). Does it optimize for speed or for certainty? Note the confirmation pattern it uses.",
      "Write the badge rule for the lab's list: 'N of 3 open' — and make it read ONLY committed values (statuses[id] !== 'saving'). Watch it refuse to flinch while a save is in flight.",
    ],
    challenge: {
      prompt:
        "Build an optimistic 'reaction bar' for a comment: three emoji counters (👍 💡 🎉), each independently toggleable per user. Requirements: (1) each counter toggles optimistically with its own pending state (disabling one emoji never blocks another); (2) a 'chaos mode' toggle that fails the NEXT mutation for a RANDOM emoji (not necessarily the one clicked — simulate a flaky backend); (3) on failure: the overlay reverts structurally (no hand-rolled revert bookkeeping beyond what the pattern demands), a toast names WHICH emoji failed and offers Retry; (4) a 'total reactions' badge that reads committed counts only — prove it by watching it stay flat during in-flight saves; (5) a written note: what would happen to that badge if it read optimistic values, and which Phase 29 mechanism would make double-clicking an emoji safe at the server.",
      hints: [
        "Model it as server state + overlay: committed: Record<Emoji, number>, yourToggled: Set<Emoji>, and the overlay derives: count + (yourToggled.has(e) ? 1 : 0). The overlay is COMPUTED — revert is just removing from the Set.",
        "Chaos mode: on mutate, roll the dice before awaiting; if the rolled emoji matches the clicked one, reject after the latency. If it doesn't match… the request 'succeeded' but a different counter lied — that's the lesson: failures don't announce which value they'll poison.",
        "The badge reads committed only: Object.values(committed).reduce(…). During a save, committed hasn't moved, so the badge can't flinch. Your note: idempotency keys (client-generated, server-deduped) make 'clicked twice' apply once.",
      ],
      solution: [
        "import { useState } from \"react\";",
        "",
        "type Emoji = \"thumbs\" | \"bulb\" | \"party\";",
        "const EMOJI: Record<Emoji, string> = { thumbs: \"👍\", bulb: \"💡\", party: \"🎉\" };",
        "",
        "// ——— pretend server ———",
        "let chaosArmed = false;",
        "function serverToggle(emoji: Emoji, your: boolean): Promise<{ emoji: Emoji; delta: number }> {",
        "  return new Promise((resolve, reject) => {",
        "    setTimeout(() => {",
        "      if (chaosArmed) {",
        "        chaosArmed = false;",
        "        reject(new Error(`server rejected ${EMOJI[emoji]}`));",
        "      } else {",
        "        resolve({ emoji, delta: your ? 1 : -1 });",
        "      }",
        "    }, 800);",
        "  });",
        "}",
        "",
        "export function ReactionBar() {",
        "  // COMMITTED truth — the only thing badges and persistence read:",
        "  const [committed, setCommitted] = useState<Record<Emoji, number>>({",
        "    thumbs: 12, bulb: 4, party: 7,",
        "  });",
        "  const [mine, setMine] = useState<Set<Emoji>>(new Set());      // my toggled-on set",
        "  const [pending, setPending] = useState<Set<Emoji>>(new Set()); // per-emoji in-flight",
        "  const [toasts, setToasts] = useState<string[]>([]);",
        "",
        "  // OVERLAY is derived: committed ± my intent. Revert = Set removal. No bookkeeping.",
        "  const display = (e: Emoji) => committed[e] + (mine.has(e) ? 1 : 0);",
        "  const badge = Object.values(committed).reduce((s, n) => s + n, 0); // committed ONLY",
        "",
        "  async function toggle(e: Emoji) {",
        "    if (pending.has(e)) return;               // one round trip per emoji at a time",
        "    const adding = !mine.has(e);",
        "    setMine((m) => { const n = new Set(m); n.add(e); return n; }); // intent: instant",
        "    setPending((p) => new Set([...p, e]));",
        "    try {",
        "      await serverToggle(e, adding);",
        "      setCommitted((c) => ({ ...c, [e]: c[e] + (adding ? 1 : -1) })); // the server's word",
        "    } catch (err) {",
        "      setMine((m) => { const n = new Set(m); n.delete(e); return n; }); // structural revert",
        "      setToasts((t) => [...t, `${String((err as Error).message)} — retry?`]);",
        "    } finally {",
        "      setPending((p) => { const n = new Set(p); n.delete(e); return n; });",
        "    }",
        "  }",
        "",
        "  return (",
        "    <div>",
        "      <p>Total reactions: <strong>{badge}</strong> (committed only — watch it stay flat mid-save)</p>",
        "      <div style={{ display: \"flex\", gap: 8 }}>",
        "        {(Object.keys(EMOJI) as Emoji[]).map((e) => (",
        "          <button",
        "            key={e}",
        "            onClick={() => toggle(e)}",
        "            disabled={pending.has(e)}",
        "            aria-pressed={mine.has(e)}",
        "            style={{ opacity: pending.has(e) ? 0.6 : 1 }}",
        "          >",
        "            {EMOJI[e]} {display(e)}",
        "          </button>",
        "        ))}",
        "      </div>",
        "      <label>",
        "        <input type=\"checkbox\" onChange={(ev) => { chaosArmed = ev.target.checked; }} />",
        "        chaos mode (fail my next mutation)",
        "      </label>",
        "      {toasts.map((t, i) => <p key={i} role=\"alert\">⚠ {t}</p>)}",
        "    </div>",
        "  );",
        "}",
        "",
        "/* The written note:",
        "   1) If the badge read display() values, it would tick up the instant",
        "      you clicked — then tick DOWN on failure. Users watch numbers lie",
        "      and lose trust in the ones that stay. Committed-only badges never flinch.",
        "   2) Double-click safety at the server = idempotency keys (Phase 29):",
        "      the client sends a key per INTENT (e.g. `${userId}:${emoji}:${commentId}:${ts}`);",
        "      the server stores results keyed by it and replays the stored result",
        "      on duplicates — 'applied twice' becomes 'remembered once'. */",
      ].join("\n"),
    },
    quiz: [
      {
        q: "A 'selected tab id' that must survive refresh and be shareable belongs in…",
        options: ["URL state — it's a view of the page, owned by the router", "useState in the tab component", "Server state", "localStorage only"],
        answer: 0,
        explain: "Shareable + refresh-surviving = URL. useState dies on refresh; localStorage survives but can't be linked. The map routes by the value's JOB, not its shape.",
      },
      {
        q: "The single most important rule of optimistic UI is…",
        options: ["The committed server value is the only truth; optimistic values are provisional presentation", "Show the overlay for at least 300ms", "Always disable the whole form during a save", "Optimistic values should be persisted for offline support"],
        answer: 0,
        explain: "Overlays evaporate on settle; badges, totals, and persistence read committed state. Persisting or computing from optimistic values is how silent lies ship.",
      },
      {
        q: "Which action should NEVER be optimistic?",
        options: ["Capturing a payment", "Liking a post", "Reordering a list", "Toggling a done flag"],
        answer: 0,
        explain: "The gate is cheap-AND-reversible. Payments are irreversible and costly: they get pending state plus explicit confirmation, not a confident overlay.",
      },
      {
        q: "After a failed optimistic mutation, good UX does…",
        options: ["Reverts the overlay AND surfaces a recoverable error (toast + Retry)", "Reverts silently to avoid alarming the user", "Leaves the overlay and retries forever", "Reloads the page"],
        answer: 0,
        explain: "A silent snap-back reads as a glitch and hides the failure. Revert + named error + recovery path respects the user's intent without lying about the outcome.",
      },
      {
        q: "'Derived state' on the map means…",
        options: ["A value computable from other state — owned by nobody, computed in render", "State derived from the server", "A read-only prop", "State stored in a parent"],
        answer: 0,
        explain: "Filtered lists, totals, flags from data. Storing derived values creates synchronization jobs and drift; compute them and they're true by construction.",
      },
      {
        q: "Two overlapping mutations for the same row are made safe at the server by…",
        options: ["Idempotency keys — the server dedupes same-intent requests (Phase 29)", "More spinners", "Disabling all buttons app-wide", "Local request queuing alone"],
        answer: 0,
        explain: "Client guards (pending per row, latest-wins) handle the UI; only a server-side idempotency key guarantees 'same intent twice applies once' under retries and flaky networks.",
      },
    ],
    flashcards: [
      { front: "The six state homes", back: "Local UI, derived, form, URL, server (cache), auth/session. Each has one owner: useState, render computation, form lib, router, query cache, Supabase Auth." },
      { front: "Diagnostic question", back: "'Which home does this value live in, and is something else squatting there?' Most state bugs are values in the wrong home." },
      { front: "The optimistic round trip", back: "Intent → provisional overlay → network → commit (server's value) or rollback + surfaced error. Four beats; skip the fourth and you're lying." },
      { front: "The optimism gate", back: "Cheap AND reversible → overlay. Costly or irreversible → pending + confirmation. Payments and deletes are never optimistic." },
      { front: "Structural rollback", back: "Prefer layers that unwind for you (useOptimistic overlays, TanStack Query optimistic data). Hand-rolled reverts are written FIRST, under every failure mode." },
      { front: "Optimism is presentation", back: "Never persist, never compute from, never treat as success. Badges/totals read committed state; the commit is the only truth." },
      { front: "Mutation collisions", back: "Double-toggles, retries, stale-last responses. Answered by pending-per-row, latest-wins guards (P19), and server idempotency keys (P29)." },
      { front: "The confident waiter", back: "The pad (overlay) records intent instantly and provisionally; the kitchen (server) makes it real; the bill (committed state) is written only from the kitchen's word." },
    ],
    recap: [
      "Every value has one of six homes; misfiled values are the root of most 'state bugs'.",
      "Optimism is four beats — and beat four (revert + surfaced error) is non-negotiable.",
      "Gate on cheap-and-reversible; payments and deletes wait for confirmation.",
      "The overlay is presentation; only the commit feeds badges, totals, and storage.",
      "Collisions are architectural: Phase 19 guards them client-side, Phase 29 dedupes them server-side.",
    ],
    references: [
      { label: "react.dev — useOptimistic", url: "https://react.dev/reference/react/useOptimistic" },
      { label: "react.dev — Choosing the State Structure", url: "https://react.dev/learn/choosing-the-state-structure" },
      { label: "TanStack Query — Optimistic Updates", url: "https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates" },
      { label: "Stripe — Idempotent requests", url: "https://docs.stripe.com/api/idempotent_requests" },
      { label: "web.dev — The PRPL pattern (perceived performance)", url: "https://web.dev/articles/apply-instant-loading-with-prpl" },
    ],
    nextBridge:
      "You now know what to show instantly and where every value lives. The remaining question is cost: when the render work itself is heavy, how do you PROVE where the milliseconds go before you change anything? Next lesson is the profiler, the flamegraph, and the memoization judgment that keeps you from optimizing what was never slow.",
  },
  {
    id: "p06-l4",
    phaseId: "p06",
    title: "Performance Profiling & Memoization Judgment",
    level: "React Developer",
    minutes: 40,
    summary:
      "Rendering is cheap; committing pixels is what users feel — and most 'slow React' is neither. This lesson installs the measure-first workflow: read the Profiler flamegraph and ranked list, name which of the three real problems you actually have (too many, too often, too slow), apply the matching tool (composition, stable props + memo, moving work out), and re-measure — with a live lab where memo visibly fails, succeeds, and then gets beaten by treating the cause.",
    prerequisites: ["p06-l3 — the state map (state placement is half of performance)", "p05-l4 — memo/useCallback/useId", "p04-l7 — reconciliation & identity"],
    objectives: [
      "Run a Profiler session and read the flamegraph: actual durations, the ranked view, and why-did-this-render.",
      "Classify a symptom into one of three problems: too many components, too often, or too slow per render.",
      "Match each problem to its tool: composition/colocation, memo + stable props, useMemo/workers/virtualization.",
      "Explain why memo() without stable props is theater — and demonstrate it live.",
      "Apply the workflow card: measure → name the expensive thing → treat the cause → memoize only if still needed → re-measure.",
      "Recognize the React Compiler's role: it automates the memoization layer, not the judgment layer.",
    ],
    simple:
      "Everyone has a story about a 'slow' screen, but slowness is a measurement, not a mood. React's Profiler records a session and shows you, in milliseconds, exactly which components rendered, how long each took, and why it rendered at all. Armed with that, nearly every performance problem turns out to be one of three things: you rendered a hundred rows when ten were visible (too many), the whole page re-rendered because a search box typed (too often), or one component does ten milliseconds of math on every pass (too slow). Each has a different cure, and the cures are almost never 'wrap everything in memo' — that's a coat of paint on a wall you haven't inspected.",
    why:
      "Memoization has a cost — comparison on every render, memory for cached values, and dep arrays that lie — so spraying it trades a measured nothing for an unmeasured something. The developers who ship fast apps are the ones who can stand in a code review and say 'this subtree cost 14ms, here's the flamegraph, memoizing the row fixes 12 of them'. That skill is also your defense against performance folklore: the teammate who insists every callback needs useCallback, the tutorial that memoizes leaf text. From Phase 19 onward (TanStack Query render counts, Phase 32's end-to-end latency budgets) measurement is the shared language — and the Profiler is where the course's 'why did this render?' discipline becomes a number.",
    mentalModel: {
      title: "The restaurant inspector",
      body: "A slow restaurant doesn't get faster by telling every waiter to hurry (memo-spray). The inspector times the actual service: is the kitchen cooking 40 dishes when only 12 were ordered (rendering too many)? Is the whole staff re-plating every table whenever one guest asks for ketchup (rendering too often)? Or does one sauce genuinely take four minutes (one expensive render)? The cures are different — trim the menu, section the staff, or pre-make the sauce — and the inspector's stopwatch is the only thing that tells you which restaurant you're in. The Profiler flamegraph is your stopwatch; 'it feels slow' is a guest's anecdote.",
    },
    sections: [
      {
        heading: "The instrument — a Profiler session in four moves",
        body: [
          "The tool: React DevTools → Profiler tab → gear → 'Record why each component rendered' on, then hit record, perform the exact interaction that feels slow (type three characters, open the modal once), stop. The flamegraph shows one bar per component per render: width is DURATION, color is cost band, and clicking any bar answers three questions — how long (actual time), when (which commit), and WHY (props changed / state changed / parent rendered). The Ranked tab re-sorts the same data slowest-first, which is usually where you should start reading.",
          "Two reading habits separate signal from noise. ONE: judge commits, not components — a 0.4ms render is fine even if it happens twice a keystroke; a single 60ms commit is a dropped frame regardless of who caused it. TWO: follow the 'why did this render' chain upward — if a deep component rendered because 'parent rendered', keep climbing until you find the commit's true origin (often a setState high up, or a context publish). The origin is where fixes live; the deep component is just where the cost showed.",
        ],
        code: [
          {
            file: "profiler-protocol.txt — the four moves",
            lang: "text",
            code: [
              "1. React DevTools → Profiler → ⚙ \"Record why each component rendered\"",
              "2. ● record → perform ONE interaction (type 3 chars / open modal once)",
              "3. ■ stop → switch to RANKED → read the slowest commit first",
              "4. click the bar → \"Why did this render?\" → climb to the origin",
              "",
              "Budgets for calibration:",
              "  < 1ms    invisible — move on",
              "  1–16ms   fine at 60fps IF it's not every frame",
              "  > 16ms   a dropped frame — this commit needs a fix",
              "  > 50ms   the user felt it — measure first, then act",
            ].join("\n"),
          },
        ],
      },
      {
        heading: "The three real problems — and the impostors",
        body: [
          "Problem ONE, too many: the commit renders components the user can't even see — a 5,000-row list, a tree re-rendered because a distant cousin updated. Cures are structural, not memoization: window/virtualize what's visible (TanStack Virtual; server pagination arrives properly in Phase 22), colocate state so updates only re-render their neighborhood (the ownership map from P06-L1 is a performance document), split contexts so a publish only reaches real consumers. Problem TWO, too often: renders triggered by identity churn — inline objects in deps, fresh callbacks to memoized children, a context value rebuilt every render. The cure is stable references where the consumer compares them (the lab below proves the exact combination) — and note this is the ONLY problem memo addresses.",
          "Problem THREE, too slow: one render does real work — parsing, sorting ten thousand rows, regex over a document. Cures: cache the derivation with useMemo on honest deps, move it out (event handler, worker, server), or shrink the input. And the two impostors to learn to spot: 'renders are slow' is usually 'commits are frequent' (fix frequency, not cost), and Strict Mode's dev-time doubling is not production behavior at all. Measure in a production build before declaring anything slow — dev React includes checks that vanish in the build your users run.",
        ],
      },
      {
        heading: "Memoization judgment — the lab that settles it",
        body: [
          "React.memo skips a child's re-render when its props compare equal (Object.is, shallow). Read that sentence backwards and the failure mode is obvious: if ANY prop has a fresh identity each render — an inline arrow, a {} literal, a new array — the comparison fails and memo is pure overhead. So memo is never a solo act; it is a CONTRACT with the parent: 'you promise stable props, I promise to skip.' The parent keeps that promise with useCallback (for handlers the child compares) and useMemo (for objects/arrays) — and breaks it the moment someone refactors a handler inline without noticing.",
          "The judgment rule: memoize a component only when (a) profiling showed its re-renders are frequent AND costly, and (b) its props can actually be stabilized. A memoized leaf that renders 0.3ms of text is paying comparison tax for nothing; an unmemoized 40ms chart that re-renders per keystroke is the real invoice. The lab below makes the contract visible: flip memo alone and watch it do nothing; add the stable callback and the render counter freezes; then split the state and realize the best fix removed the re-render entirely — memo never got a vote.",
        ],
        demo: "perf-lab",
      },
      {
        heading: "The workflow card — and what the Compiler changes",
        body: [
          "Five steps, in order, no skipping: MEASURE (profile the real interaction, production build), NAME (which commit, which component, which of the three problems), TREAT THE CAUSE (restructure, colocate, move work out — the fixes that change the shape of the work), MEMOIZE if the cause-treated render is still frequent-and-costly (and only with stable props), RE-MEASURE (same interaction, same protocol; if the number didn't move, the memo didn't matter — remove it). Every optimization that can't survive step five was a guess.",
          "The React Compiler (stable, per the Version Matrix's verified baseline) shifts the ground under step four: it auto-memoizes components and values where safe, which removes most of the manual useCallback/useMemo layer — including the dep-array lies humans introduce. What it does NOT automate: the measurement, the three-problem diagnosis, the structural fixes, or the decision that a list should be virtualized instead of memoized. The Compiler raises the floor of the easy layer; judgment remains the ceiling. Learn the manual discipline first — you will debug the Compiler's output with exactly the tools this lesson installs.",
        ],
      },
    ],
    mistake: {
      title: "Memo-spray: optimizing what was never measured",
      wrong: [
        "const App = memo(function App() {",
        "  const [query, setQuery] = useState(\"\");",
        "  // hygiene-driven memoization, zero measurements:",
        "  const style = useMemo(() => ({ color: \"teal\" }), []);",
        "  const onInput = useCallback((e) => setQuery(e.target.value), []);",
        "  const rows = useMemo(() => data, [data]);      // memoizing a reference!",
        "",
        "  return (",
        "    <div style={style}>",
        "      <input onChange={onInput} />",
        "      {/* 200 leaf components, each wrapped in memo, each rendering",
        "         ~0.2ms of text — 200 comparisons per keystroke to save 0ms */}",
        "      {rows.map((r) => <MemoRow key={r.id} row={r} onSelect={() => pick(r.id)} />)}",
        "      {/*        ^^^^^^ fresh arrow anyway — the contract is already broken */}",
        "    </div>",
        "  );",
        "});",
      ].join("\n"),
      right: [
        "// measured first: the 60ms commit was the list rendering all 200 rows",
        "// per keystroke because query lived above the list.",
        "function App() {",
        "  return (",
        "    <div>",
        "      <SearchPanel />        {/* state moved DOWN: typing re-renders",
        "                                a small panel, not the page — 60ms → 2ms */}",
        "      <RowList />            {/* no memo needed: it simply doesn't",
        "                                re-render on keystrokes anymore */}",
        "    </div>",
        "  );",
        "}",
        "// If profiling later shows RowList re-rendering often AND costly,",
        "// THEN: memo(RowList) + a stable onSelect. With the flamegraph as",
        "// the receipt. Not before.",
      ].join("\n"),
      explain:
        "Three wastes and one broken contract: memoizing a constant object, memoizing a reference passthrough, and wrapping 200 cheap leaves that cost less than their own comparisons — while the inline arrow guaranteed every memo's comparison would fail anyway. The actual problem (state placed too high, forcing a page-wide commit per keystroke) was structural and cost nothing to fix. Memoization answered a question nobody had asked; the flamegraph would have asked the right one.",
    },
    tryIt: [
      "Open any app you've built, run the four-move protocol on one interaction, and screenshot the ranked list. Name the slowest commit and its 'why'. If everything is under 1ms — congratulations, you have no performance problem; file the session and move on.",
      "In the lab: enable memo alone and type. Count renders for five keystrokes (expect: climbing). Add the stable callback and repeat (expect: flat). Then split state (expect: parent flat too). Write down why each combination behaved that way.",
      "Take a component with a useMemo and delete the memo. Profile before/after. If the number doesn't move, delete the useMemo permanently — you just removed a dep array that could lie.",
      "Find a list in your work over ~200 items. Profile scrolling or filtering. Without virtualizing yet, predict what colocation (moving the filter input's state next to the list) would change — then do it and check.",
      "Build with your production build command and re-profile the same interaction. Compare dev vs production numbers. This is why 'it's slow in dev' is not a ticket.",
    ],
    challenge: {
      prompt:
        "A teammate ships a 'slow' settings page and proposes wrapping six components in memo + useCallback 'to be safe'. You get the ticket. Deliver: (1) a Profiler session transcript (the four moves, with plausible numbers you justify), (2) a diagnosis naming which of the three problems each symptom is — the page has: a 45ms commit on every keystroke of a 'site title' input, a color-picker that re-renders the whole form, and a timezone list of 400 rows rendered inline; (3) a fix plan ordered by expected impact, where memo appears AT MOST once and only with its receipt; (4) the re-measurement you'd require before closing the ticket; (5) one sentence explaining why 'wrapping six components to be safe' would have failed, referencing the memo contract.",
      hints: [
        "The keystroke commit is a placement problem (problem 2, too often): the input's state sits at the page root. Colocate it — the 45ms commit becomes the input's 1ms commit. That's fix #1 and it's free.",
        "The color-picker republishing the form is a context/granularity problem (also 'too often', via identity churn): split the picker's value out or pass it as a narrow prop. The 400-row timezone list is problem 1 (too many): filter-as-you-type or virtualize — memo won't help rows that must render.",
        "Memo's single legitimate receipt: if profiling still shows a costly subtree re-rendering frequently WITH stable props after fixes 1–2, memoize that ONE component. Your sentence: memo without stable props is a comparison tax with no refund.",
      ],
      solution: [
        "/* ——— (1) session transcript ——— */",
        "// Profiler ⚙ record-why ON → type \"Acme\" in Site Title → stop.",
        "// Ranked (production build):",
        "//   SettingsPage        45.2ms  ×4 commits   why: state changed (query)",
        "//   TimezoneList        31.8ms  ×4           why: parent rendered",
        "//   ColorField           6.1ms  ×4           why: parent rendered",
        "//   4 leaf fields        ~0.4ms each         why: parent rendered",
        "// Reading: ONE origin (the title input's setState at page root)",
        "// cascading into 400 rows + form per keystroke. The leaves are",
        "// noise; the rows and the cascade are the invoice.",
        "",
        "/* ——— (2) diagnosis, one symptom at a time ——— */",
        "// 45ms per keystroke   → problem: TOO OFTEN (page-wide commits from",
        "//                          a state placed at the root) + TOO MANY",
        "//                          (each commit drags 400 rows along)",
        "// color-picker repaint → problem: TOO OFTEN via granularity — the",
        "//                          picker's value flows through the whole form",
        "// 400-row timezone list→ problem: TOO MANY — rows rendered that the",
        "//                          user will scroll past; a memoized row that",
        "//                          never needed to render is still rendered",
        "",
        "/* ——— (3) fix plan, ordered by expected impact ——— */",
        "// FIX 1 (biggest): colocate the title input.",
        "//   function SiteTitleField() { const [v, setV] = useState(initial);",
        "//     return <input value={v} onChange={…} onBlur={commitToForm} />; }",
        "//   Impact: 45ms page commit → ~1ms field commit. Rows untouched.",
        "// FIX 2: narrow the color picker's blast radius.",
        "//   Keep its live value inside <ColorField/>, commit on change-end —",
        "//   or split the form context so only ColorField consumes the draft.",
        "//   Impact: form no longer re-renders per drag.",
        "// FIX 3: stop rendering 400 timezones.",
        "//   Filter-as-you-type (rows drop to <20) now; virtualize or",
        "//   server-paginate in Phase 22 if the full list must stay browsable.",
        "// FIX 4 (conditional, receipt required): ONLY if re-measurement still",
        "//   shows a frequent AND costly subtree, memo(ThatOneComponent) with",
        "//   a stabilized prop list. Expected: unnecessary after 1–3.",
        "",
        "/* ——— (4) closing criteria ——— */",
        "// Re-run the identical session (production build, same three chars):",
        "//   - no commit over 16ms on keystroke      (was 45ms)",
        "//   - TimezoneList absent from keystroke commits (was ×4)",
        "//   - ranked list total < 8ms               (was ~85ms)",
        "// Same interaction, same protocol — or the ticket stays open.",
        "",
        "/* ——— (5) why 'wrap six components to be safe' fails ——— */",
        "// memo is a contract: 'stable props in, skipped renders out'.",
        "// The page root re-rendered with fresh inline props every keystroke,",
        "// so all six comparisons would FAIL every time — paying comparison",
        "// tax on 6 components to save 0ms, while the 45ms origin (state at",
        "// the root) and the 400 rendered rows went untouched.",
        "// Paint on a wall you never inspected.",
      ].join("\n"),
    },
    quiz: [
      {
        q: "The first step of any performance work is…",
        options: ["Profile the actual interaction in a production build", "Wrap suspicious components in memo", "Add useCallback to every handler", "Upgrade React"],
        answer: 0,
        explain: "Slowness is a measurement. The flamegraph names the commit, the component, and the why — everything before that is folklore.",
      },
      {
        q: "A 0.4ms component rendering on every keystroke is…",
        options: ["Usually fine — judge commits against the 16ms frame budget, not render counts", "Always a bug", "A memo() candidate", "Proof of a stale closure"],
        answer: 0,
        explain: "Sub-millisecond renders are noise even when frequent. The invoice is the commit: total work per frame. Fix frequency only when the commit crosses the budget.",
      },
      {
        q: "React.memo alone (no stable props) typically…",
        options: ["Does nothing but add comparison overhead — fresh prop identities fail the check", "Halves render time", "Fixes stale closures", "Prevents context updates"],
        answer: 0,
        explain: "memo skips renders only when props compare equal. An inline arrow or fresh object fails every comparison — the parent must keep the contract.",
      },
      {
        q: "'The whole page re-renders when I type in one input' is problem category…",
        options: ["Too often (and probably too many) — fix by colocating the state or splitting context", "Too slow — memoize everything", "A React bug", "Unfixable without a rewrite"],
        answer: 0,
        explain: "The render count/frequency is the disease; the cure is structural (state placement, granularity), which is why it beats memo: there's nothing left to skip.",
      },
      {
        q: "The React Compiler changes…",
        options: ["The memoization layer (auto-memoization) — not measurement, diagnosis, or structural judgment", "The reconciliation algorithm", "Bundle output format", "How hooks are called"],
        answer: 0,
        explain: "It raises the floor of manual memo/useCallback hygiene. Profiling, the three-problem diagnosis, and decisions like virtualization remain human judgment.",
      },
      {
        q: "You may close a performance ticket when…",
        options: ["The re-measurement of the SAME interaction shows the commit under budget", "The code has more memos than before", "The reviewer approves the diff", "DevTools shows green"],
        answer: 0,
        explain: "Step five is the contract: same interaction, same protocol, production build. If the number didn't move, the fix didn't matter — and the memo comes back off.",
      },
    ],
    flashcards: [
      { front: "Profiler protocol", back: "Record-why on → record ONE interaction → ranked view → click the slowest bar → read 'why did this render' → climb to the origin." },
      { front: "The frame budget", back: "~16ms at 60fps. Judge COMMIT totals against it, not render counts. <1ms is invisible; >50ms is felt." },
      { front: "The three real problems", back: "Too many (render what's visible: virtualize/colocate/split), too often (stable refs, placement, granularity), too slow (useMemo, move out, shrink input). One cure each." },
      { front: "The memo contract", back: "memo skips a render only if ALL props compare equal (Object.is, shallow). The parent must supply stable identities — useCallback/useMemo — or memo is pure tax." },
      { front: "When to memoize", back: "Only when profiling shows frequent AND costly re-renders AND props can be stabilized. Measured cause first; memo is step four, not step one." },
      { front: "The impostors", back: "'Renders are slow' is usually 'commits are frequent'; dev-mode Strict doubling isn't production. Profile production builds before declaring slowness." },
      { front: "Treat the cause first", back: "Structural fixes (state placement, colocation, shrinking work) beat memoization because they remove renders instead of skipping them." },
      { front: "Compiler's role", back: "Auto-memoizes where safe (killing memo-spray and dep-lies); does not measure, diagnose, or virtualize. Judgment stays human." },
    ],
    recap: [
      "Profile first: flamegraph, ranked view, why-did-this-render, climb to the origin.",
      "Three problems, three cures: too many → shrink scope; too often → stabilize/placement; too slow → cache/move out.",
      "memo is a contract with the parent — stable props or it's theater.",
      "Workflow: measure → name → treat cause → memoize if needed → re-measure.",
      "The Compiler automates the easy layer; your judgment is the ceiling.",
    ],
    references: [
      { label: "react.dev — Profiling with React DevTools", url: "https://legacy.reactjs.org/blog/2018/09/10/introducing-the-react-profiler.html" },
      { label: "react.dev — React.memo", url: "https://react.dev/reference/react/memo" },
      { label: "react.dev — useMemo", url: "https://react.dev/reference/react/useMemo" },
      { label: "react.dev — React Compiler", url: "https://react.dev/learn/react-compiler" },
      { label: "web.dev — INP and interaction responsiveness", url: "https://web.dev/articles/inp" },
    ],
    nextBridge:
      "The React Developer stage has one lesson pair left: reading the ecosystem's past so its present makes sense (Then-vs-Now React), and packaging everything into components and folders that survive a team (design systems + feature folders). After that, the course turns outward — Phase 7 opens the network: the request anatomy your optimistic round trips have been crossing all along.",
  },
];

export const LESSON_CONTENT_P6B: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P6B.map((l) => [l.id, l]),
);
