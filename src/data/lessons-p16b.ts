import type { LessonContent } from "./types";

/**
 * Phase 16 Supabase Platform + Database Integration (L4–L6).
 */
export const LESSONS_P16B: LessonContent[] = [
  {
    id: "p16-l4",
    phaseId: "p16",
    title: "Migrations Stay Authoritative; Studio Doesn't",
    level: "Backend Developer",
    minutes: 30,
    summary:
      "Maintain strict schema authority in version control. Learn why manually clicking 'Add Column' in the Supabase Dashboard causes production schema drift, and how to use `prisma migrate diff` to detect and repair drift.",
    prerequisites: [
      "p16-l3 — Connecting NestJS + Prisma 7.9.15 to Supabase",
      "p14-l3 — Migrate, Generate, Seed & Studio",
    ],
    objectives: [
      "Explain the concept of Schema Drift and why ad-hoc GUI modifications break CI/CD pipelines.",
      "Establish a strict 'Git as Single Source of Truth' database workflow.",
      "Use `prisma migrate diff` to compare live database schemas against version-controlled Prisma schemas.",
      "Recover from accidental dashboard schema modifications without data loss.",
    ],
    simple:
      "The Supabase Studio Table Editor is great for viewing data, but clicking 'Add Column' or changing constraints directly in the web UI modifies PostgreSQL without creating a Git-tracked migration file. When your CI/CD pipeline deploys the next release, `prisma migrate deploy` will fail or crash because the live database no longer matches what's in your repository. Migrations must always remain the single source of truth.",
    why:
      "Schema drift is a leading cause of broken production deployments. When a developer modifies a column in Supabase Studio on Friday afternoon and someone else deploys code on Monday morning, the deployment will fail or overwrite data because the change was never recorded in a migration file.",
    mentalModel: {
      title: "The Architectural Blueprint vs The Renovator",
      body: "Think of your Prisma schema and migration folder as the official architectural blueprints of a skyscraper, and Supabase Studio as a visitor inspecting the lobby. If a renovator knocks down a wall inside the lobby without updating the city architectural blueprints, the next construction crew building the 10th floor will calculate load-bearing math based on false blueprints and cause a collapse.",
    },
    sections: [
      {
        heading: "The Dangers of GUI Schema Mutations (Schema Drift)",
        body: [
          "When you click 'Add Column' in Supabase Table Editor, PostgreSQL executes an immediate `ALTER TABLE`, but your Git repository receives zero updates.",
          "The diagram below illustrates how manual GUI changes break automated CI/CD pipelines.",
        ],
        code: [
          {
            file: "SCHEMA_DRIFT_LIFECYCLE.md",
            lang: "text",
            code: [
              "1. Developer adds 'phoneNumber' column via Supabase Web Dashboard.",
              "   └── PostgreSQL public.users schema now has: id, email, phoneNumber.",
              "",
              "2. prisma/schema.prisma in Git still only contains: id, email.",
              "   └── Git repo has NO record of phoneNumber migration.",
              "",
              "3. Another developer creates migration for 'bio' column in Git:",
              "   └── Generated migration: ALTER TABLE users ADD COLUMN bio TEXT;",
              "",
              "4. CI/CD runs 'prisma migrate deploy' on Staging/Production:",
              "   └── Migration history table (_prisma_migrations) checksum mismatch or",
              "       subsequent migration attempts to alter unrecorded table state.",
              "   💥 DEPLOYMENT PIPELINE FAILS WITH SCHEMA DRIFT ERROR!",
            ].join("\n"),
            caption: "How manual GUI schema modifications cause fatal CI/CD migration pipeline failures.",
          },
        ],
      },
      {
        heading: "Detecting and Resolving Drift with prisma migrate diff",
        body: [
          "Use `prisma migrate diff` to compare your live database against your local Prisma schema.",
          "This generates the exact SQL statements required to reconcile any drift.",
        ],
        code: [
          {
            file: "terminal",
            lang: "bash",
            code: [
              "# 1. Compare live Supabase database against local Prisma schema",
              "npx prisma migrate diff \\",
              "  --from-url \"$DIRECT_URL\" \\",
              "  --to-schema-datamodel prisma/schema.prisma \\",
              "  --script > drift_reconciliation.sql",
              "",
              "# 2. Inspect drift_reconciliation.sql to see discrepancies",
              "cat drift_reconciliation.sql",
              "",
              "# 3. If someone added a column in Studio that should be kept:",
              "#    - Update prisma/schema.prisma to include the column",
              "#    - Run 'prisma migrate diff' from migrations to live DB to generate migration file",
              "#    - Mark migration as applied with 'prisma migrate resolve --applied [migration_name]'",
            ].join("\n"),
            caption: "Using prisma migrate diff to detect and reconcile differences between live DB and Git schema.",
          },
        ],
      },
    ],
    mistake: {
      title: "Editing Production Table Constraints in Supabase Dashboard",
      wrong: [
        "// ❌ Clicking 'Edit Column' -> 'Set NOT NULL' in Supabase Table Editor",
        "// Result: Live DB enforces constraint, but schema.prisma in Git doesn't know.",
        "// Prisma Client generated types are out of sync with actual DB behavior.",
      ].join("\n"),
      right: [
        "// ✅ Always modify prisma/schema.prisma first",
        "// 1. Edit schema.prisma",
        "// 2. Run 'npx prisma migrate dev --name set_phone_not_null'",
        "// 3. Commit migration file to Git",
        "// 4. CI/CD automatically runs 'npx prisma migrate deploy'",
      ].join("\n"),
      explain:
        "Database changes must be version-controlled, reviewed in Pull Requests, and applied predictably via migration files.",
    },
    tryIt: [
      "In your local environment, run `npx prisma migrate status` to verify migration consistency.",
      "Practice running `npx prisma migrate diff --from-empty --to-schema-datamodel prisma/schema.prisma --script` to see the full DDL generated for your schema.",
      "Establish the team rule: Supabase Table Editor is for READ-ONLY inspection; all DDL changes go through Prisma migrations.",
    ],
    challenge: {
      prompt:
        "Create a GitHub Actions CI workflow step that runs before deployment to check for unexpected database drift and fails the build if uncommitted migrations exist.",
      hints: [
        "Use `actions/checkout` and `actions/setup-node`",
        "Run `npx prisma migrate status`",
        "Capture exit code: non-zero means pending unapplied migrations or schema drift",
      ],
      solution: [
        "name: Database Migration & Drift Verification",
        "on:",
        "  pull_request:",
        "    branches: [main]",
        "",
        "jobs:",
        "  verify-migrations:",
        "    runs-on: ubuntu-latest",
        "    steps:",
        "      - name: Checkout Code",
        "        uses: actions/checkout@v4",
        "",
        "      - name: Setup Node.js",
        "        uses: actions/setup-node@v4",
        "        with:",
        "          node-version: 20",
        "          cache: 'npm'",
        "",
        "      - name: Install Dependencies",
        "        run: npm ci",
        "",
        "      - name: Verify Migration Consistency & Schema Drift",
        "        env:",
        "          DATABASE_URL: ${{ secrets.DATABASE_URL }}",
        "          DIRECT_URL: ${{ secrets.DIRECT_URL }}",
        "        run: |",
        "          echo \"🔍 Checking Prisma migration status...\"",
        "          npx prisma migrate status",
        "          echo \"✅ Migrations in sync!\"",
      ].join("\n"),
    },
    quiz: [
      {
        question: "What is 'Schema Drift'?",
        options: [
          "A CSS animation technique",
          "When the live database schema diverges from the schema definitions and migration history tracked in version control (Git)",
          "A type of hard drive failure",
          "When a table has more than 10,000 rows",
        ],
        answer: 1,
        explanation: "Schema drift occurs when changes are made directly to the database without recording corresponding migration files in Git.",
      },
      {
        question: "Why should developers avoid using the Supabase Studio Table Editor to add or alter columns in production?",
        options: [
          "Supabase Studio is incompatible with PostgreSQL",
          "GUI edits bypass Git version control, break CI/CD automated deployments, and cause type mismatches with Prisma Client",
          "The Supabase Dashboard deletes data automatically",
          "Prisma forbids web browsers",
        ],
        answer: 1,
        explanation: "Dashboard GUI mutations bypass version control, leading to untracked schema changes that break deployment pipelines.",
      },
      {
        question: "What does `prisma migrate diff` do?",
        options: [
          "It formats TypeScript code",
          "It compares two database schema sources (URL, schema file, or migration history) and outputs the SQL statements needed to make them identical",
          "It deletes all database records",
          "It sends an email to the database administrator",
        ],
        answer: 1,
        explanation: "`prisma migrate diff` compares schemas or database instances and outputs the SQL diff.",
      },
      {
        question: "How should you resolve schema drift if a column was legitimately added via Supabase Dashboard during an emergency hotfix?",
        options: [
          "Delete the database and start over",
          "Update `prisma/schema.prisma` to match, generate the corresponding migration SQL using `prisma migrate diff`, and record it as applied with `prisma migrate resolve`",
          "Ignore it and never run migrations again",
          "Uninstall Prisma",
        ],
        answer: 1,
        explanation: "Reconciling drift requires updating the Prisma schema, generating the migration file, and marking it as applied in the database migration log.",
      },
      {
        question: "Where does Prisma track which migrations have already been applied to a PostgreSQL database?",
        options: [
          "In the browser `localStorage`",
          "Inside the `_prisma_migrations` system table in the PostgreSQL database",
          "In a text file on the developer's laptop",
          "In DNS records",
        ],
        answer: 1,
        explanation: "Prisma maintains the `_prisma_migrations` table inside the target database, recording migration names, checksums, and execution timestamps.",
      },
      {
        question: "What is the single source of truth for your database structure in a disciplined engineering team?",
        options: [
          "The local development SQLite file",
          "The version-controlled `prisma/schema.prisma` file and `prisma/migrations` directory in Git",
          "The Supabase Studio UI",
          "A spreadsheet on Google Drive",
        ],
        answer: 1,
        explanation: "Git-tracked schema and migration files are the sole authoritative blueprint for database structure.",
      },
    ],
    flashcards: [
      {
        front: "What is Schema Drift?",
        back: "Divergence between the live database schema and the migration history recorded in Git.",
      },
      {
        front: "What command compares live DB schema against local Prisma schema?",
        back: "`npx prisma migrate diff --from-url $DIRECT_URL --to-schema-datamodel prisma/schema.prisma`.",
      },
      {
        front: "What table stores applied migration history in PostgreSQL?",
        back: "`_prisma_migrations`.",
      },
      {
        front: "Why shouldn't you edit production schema directly in Supabase Studio?",
        back: "Because GUI edits are not recorded in migration files, breaking future CI/CD deployments.",
      },
      {
        front: "How do you mark a manually applied migration as resolved?",
        back: "`npx prisma migrate resolve --applied <migration_name>`.",
      },
      {
        front: "What command verifies whether all local migrations have been applied?",
        back: "`npx prisma migrate status`.",
      },
      {
        front: "What should Supabase Studio Table Editor be used for in production?",
        back: "Read-only data inspection, log viewing, and operational monitoring.",
      },
      {
        front: "What happens if a migration file in Git is edited after being applied to production?",
        back: "Prisma detects a checksum mismatch and halts future migrations to prevent corrupted state.",
      },
    ],
    recap: [
      "Keep `prisma/schema.prisma` and `prisma/migrations` in Git as the sole source of truth.",
      "Never execute schema DDL directly in Supabase Studio Table Editor.",
      "Use `prisma migrate diff` to detect and reconcile schema discrepancies.",
      "Automate migration verification with `prisma migrate status` in CI/CD pipelines.",
    ],
    references: [
      { label: "Prisma Migrate Diff Documentation", url: "https://www.prisma.io/docs/reference/api-reference/command-reference#prisma-migrate-diff" },
      { label: "Managing Database Schema Drift", url: "https://www.prisma.io/docs/guides/database/production-troubleshooting#schema-drift" },
    ],
    nextBridge: "Now let's tackle real-world production triage: debugging P1001 errors, port issues, IPv4/IPv6 resolver quirks, and pooler traps in Lesson 5.",
  },
  {
    id: "p16-l5",
    phaseId: "p16",
    title: "Debugging P1001, Ports, IPv4/IPv6 & Pooler Mistakes",
    level: "Backend Developer",
    minutes: 40,
    summary:
      "Master the production triage playbook for PostgreSQL and Supabase connection failures. Diagnose and resolve Prisma P1001 connection timeouts, IPv6 DNS resolution traps in cloud containers, and SSL errors.",
    prerequisites: [
      "p16-l2 — Direct vs Session Pooler Connections",
      "p16-l3 — Connecting NestJS + Prisma 7.9.15 to Supabase",
    ],
    objectives: [
      "Systematically diagnose and fix Prisma error code `P1001` (Can't reach database server).",
      "Explain why direct Supabase hostnames resolve to IPv6 and how this fails in IPv4-only cloud environments (e.g. Vercel, AWS Lambda, Render, GitHub Actions).",
      "Identify the 4 most common pooler configuration mistakes in production.",
      "Verify TLS/SSL certificate requirements for Supabase PostgreSQL connections.",
    ],
    simple:
      "When your backend throws `P1001: Can't reach database server`, it rarely means the database is completely offline. In 95% of cases, the error is caused by: (1) an IPv4-only cloud server trying to connect to a direct IPv6 Supabase domain, (2) using port 5432 instead of pooler port 6543 on serverless, (3) missing `?sslmode=require`, or (4) a special character in the database password breaking URL parsing. This lesson gives you the step-by-step diagnostic checklist.",
    why:
      "Connection issues block deployments and wake up on-call engineers. A developer who understands DNS resolution, IPv6 constraints, and pooler mechanics can resolve a P1001 incident in 2 minutes instead of 4 hours of panic.",
    mentalModel: {
      title: "The Phone Line Diagnostic Checklist",
      body: "When a phone call fails, you check: Is the phone plugged in (Network)? Did you dial the right area code (Port & Protocol)? Is the line encrypted (SSL)? Are you speaking the same language (IPv4 vs IPv6)? By testing each layer sequentially, you isolate the exact point of failure.",
    },
    sections: [
      {
        heading: "The 5-Step P1001 Triage Checklist",
        body: [
          "Follow this systematic diagnostic runbook whenever encountering `P1001` in development or production.",
        ],
        code: [
          {
            file: "P1001_TRIAGE_RUNBOOK.md",
            lang: "text",
            code: [
              "========================================================================",
              "PRISMA P1001 DIAGNOSTIC RUNBOOK",
              "========================================================================",
              "STEP 1: Check Password URL Encoding",
              "  - Does your password contain special characters like '@', ':', '#', '?'?",
              "  - Example: 'p@ssword' in URL becomes username:p@ssword@db... -> Breaks URL parser!",
              "  - Fix: URL-encode the password (e.g. 'p%40ssword') using encodeURIComponent().",
              "",
              "STEP 2: Check IPv4 vs IPv6 DNS Resolution",
              "  - Direct URL ('db.[ref].supabase.co') resolves to IPv6 by default.",
              "  - Many cloud container hosts (Vercel, AWS Lambda, Docker) lack IPv6 egress.",
              "  - Fix: Switch to the Supabase Pooler domain ('aws-0-*.pooler.supabase.com')",
              "         which resolves to dual-stack IPv4/IPv6.",
              "",
              "STEP 3: Check Port and Pooler Mode",
              "  - Runtime services MUST use port 6543 with '?pgbouncer=true'.",
              "  - Prisma Migrate MUST use port 5432 (direct or session).",
              "",
              "STEP 4: Verify SSL Configuration",
              "  - Supabase requires SSL encryption on all connections.",
              "  - Ensure '?sslmode=require' is present in connection strings.",
              "",
              "STEP 5: Check Supabase Project Pause State",
              "  - In free-tier projects, inactive databases pause after 1 week.",
              "  - Check Supabase Dashboard to see if project is in 'Paused' status.",
            ].join("\n"),
            caption: "The production P1001 diagnostic checklist resolving 95%+ of connection failures.",
          },
        ],
      },
      {
        heading: "The IPv4 vs IPv6 Trap in Cloud Container Hosts",
        body: [
          "Direct Supabase hostnames (`db.[ref].supabase.co`) only provide `AAAA` (IPv6) DNS records by default.",
          "If your Docker container or cloud platform (AWS Lambda, GitHub Actions, Vercel) does not support IPv6 routing, direct connections will timeout with P1001.",
        ],
        code: [
          {
            file: "terminal",
            lang: "bash",
            code: [
              "# Testing DNS records for Direct host (IPv6 only by default):",
              "dig db.yourprojectref.supabase.co AAAA",
              "# Returns: 2001:db8:... (IPv6 address)",
              "",
              "# Testing DNS records for Pooler host (Dual-stack IPv4 + IPv6):",
              "dig aws-0-us-east-1.pooler.supabase.com A",
              "# Returns: 54.x.x.x (IPv4 address) -> Works everywhere!",
            ].join("\n"),
            caption: "Comparing IPv6-only direct hostnames vs dual-stack IPv4 Supabase pooler hostnames.",
          },
        ],
      },
    ],
    mistake: {
      title: "Unencoded Special Characters in Database Password",
      wrong: [
        "// ❌ Password containing unencoded '@' character",
        "DATABASE_URL=\"postgresql://postgres.ref:Secret@123@aws-0-us-east-1.pooler.supabase.com:6543/postgres\"",
        "// Result: URL parser treats 'Secret' as password and '@123' as hostname -> Fails with P1001!",
      ].join("\n"),
      right: [
        "// ✅ URL-encoded password ('@' -> '%40')",
        "DATABASE_URL=\"postgresql://postgres.ref:Secret%40123@aws-0-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require\"",
      ].join("\n"),
      explain:
        "Special characters in URI components must be URL-encoded (percent-encoded) to avoid breaking standard URL parsing.",
    },
    tryIt: [
      "Check your database password: if it contains `@`, `#`, `!`, or `/`, verify that it is properly URL-encoded in `.env`.",
      "Run `ping` or `nslookup` against `aws-0-us-east-1.pooler.supabase.com` to confirm IPv4 DNS resolution on your machine.",
      "Check your Supabase Project Settings to ensure the project has not been paused due to inactivity.",
    ],
    challenge: {
      prompt:
        "Write a robust TypeScript helper that constructs a safe PostgreSQL connection string from separate parts (user, pass, host, port, db, params), automatically URL-encoding credentials to prevent connection string bugs.",
      hints: [
        "Use `encodeURIComponent` on username and password",
        "Construct `URL` object with `postgresql:` protocol",
        "Append required parameters (`pgbouncer`, `sslmode`)",
      ],
      solution: [
        "export interface PostgresConnectionConfig {",
        "  user: string;",
        "  password: string;",
        "  host: string;",
        "  port: number;",
        "  database: string;",
        "  isPooler?: boolean;",
        "  sslRequired?: boolean;",
        "}",
        "",
        "export function buildSafePostgresUrl(config: PostgresConnectionConfig): string {",
        "  const encodedUser = encodeURIComponent(config.user);",
        "  const encodedPass = encodeURIComponent(config.password);",
        "  ",
        "  const baseUrl = `postgresql://${encodedUser}:${encodedPass}@${config.host}:${config.port}/${config.database}`;",
        "  const url = new URL(baseUrl);",
        "",
        "  if (config.sslRequired !== false) {",
        "    url.searchParams.set('sslmode', 'require');",
        "  }",
        "",
        "  if (config.isPooler) {",
        "    url.searchParams.set('pgbouncer', 'true');",
        "  }",
        "",
        "  return url.toString();",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        question: "What does Prisma error code `P1001` mean?",
        options: [
          "SQL syntax error in query",
          "Can't reach database server (connection refused, timeout, or DNS resolution failure)",
          "Table already exists",
          "User lacks admin permissions",
        ],
        answer: 1,
        explanation: "P1001 indicates that Prisma Client could not establish a network connection to the database server.",
      },
      {
        question: "Why do direct Supabase connections (`db.[ref].supabase.co`) fail with P1001 on some cloud providers like AWS Lambda or GitHub Actions?",
        options: [
          "Direct Supabase hostnames resolve to IPv6 by default, and IPv4-only container environments cannot route IPv6 packets",
          "AWS Lambda prohibits SQL databases",
          "GitHub Actions blocks port 5432",
          "PostgreSQL requires Windows OS",
        ],
        answer: 0,
        explanation: "Direct Supabase endpoints use IPv6 addresses. In environments without IPv6 routing, connections timeout unless the IPv4 pooler domain is used.",
      },
      {
        question: "What happens if a database password contains the `@` symbol without URL-encoding in the connection string?",
        options: [
          "PostgreSQL accepts it normally",
          "The URL parser splits the connection string at the wrong `@`, resulting in an invalid hostname and a P1001 connection failure",
          "The password is automatically replaced with 'password'",
          "The database deletes all user accounts",
        ],
        answer: 1,
        explanation: "The `@` character delimits user credentials from the hostname in URIs. Unencoded `@` in passwords corrupts URI parsing.",
      },
      {
        question: "How can free-tier Supabase projects trigger sudden P1001 errors after a week of inactivity?",
        options: [
          "The project is automatically deleted forever",
          "Free-tier projects automatically pause after 7 days of inactivity; unpausing in the dashboard restores connectivity",
          "The database password changes randomly",
          "The database drops all tables",
        ],
        answer: 1,
        explanation: "Inactive free projects pause compute to save resources. Resuming the project in the Supabase Dashboard reactivates the instance.",
      },
      {
        question: "What parameter must be included in database URLs when connecting through Supavisor Transaction Pooler on port 6543?",
        options: [
          "`?pgbouncer=true`",
          "`?no_pool=1`",
          "`?debug_sql=true`",
          "`?force_ipv6=true`",
        ],
        answer: 0,
        explanation: "`?pgbouncer=true` is required for Prisma to disable prepared statements when connecting to transaction poolers.",
      },
      {
        question: "Which Supabase hostname provides dual-stack IPv4 and IPv6 connectivity for cloud environments?",
        options: [
          "`db.[ref].supabase.co`",
          "`aws-0-[region].pooler.supabase.com`",
          "`localhost:5432`",
          "`api.supabase.io`",
        ],
        answer: 1,
        explanation: "The Supabase Pooler domain (`pooler.supabase.com`) resolves to IPv4 and IPv6, making it compatible with all cloud platforms.",
      },
    ],
    flashcards: [
      {
        front: "What does Prisma error P1001 signify?",
        back: "Network connectivity failure: Prisma cannot reach the database server.",
      },
      {
        front: "Why do direct Supabase URLs fail on IPv4-only hosts?",
        back: "Direct URLs (`db.[ref].supabase.co`) resolve to IPv6 by default.",
      },
      {
        front: "How do you connect from IPv4-only hosts without paid add-ons?",
        back: "Use the Supabase Pooler hostname (`aws-0-[region].pooler.supabase.com`).",
      },
      {
        front: "How should passwords containing special characters (e.g. `@`, `#`) be formatted in connection strings?",
        back: "They must be URL-encoded (e.g. `@` becomes `%40`).",
      },
      {
        front: "What does `sslmode=require` enforce?",
        back: "It requires the client to establish an encrypted TLS/SSL connection with the database.",
      },
      {
        front: "Why might a hobby project suddenly return P1001 after 7 days?",
        back: "Free-tier Supabase projects pause after 7 days of inactivity; unpausing restores the database.",
      },
      {
        front: "What port does Supavisor use for Transaction Pooling?",
        back: "Port 6543.",
      },
      {
        front: "What port does Supavisor use for Session Pooling?",
        back: "Port 5432.",
      },
    ],
    recap: [
      "Follow the 5-step triage checklist when debugging `P1001`: URL encoding, IPv6 vs IPv4, port/pooler mode, SSL, and project pause state.",
      "Use `pooler.supabase.com` hostnames to guarantee IPv4 compatibility across all cloud container providers.",
      "Always URL-encode passwords containing special characters (`@`, `#`, `?`).",
      "Ensure `?pgbouncer=true` is appended to runtime pooler URLs.",
    ],
    references: [
      { label: "Supabase IPv6 and Connection Troubleshooting", url: "https://supabase.com/docs/guides/platform/ipv6" },
      { label: "Prisma Error Code Reference: P1001", url: "https://www.prisma.io/docs/reference/api-reference/error-reference#p1001" },
    ],
    nextBridge: "With database connectivity hardened, let's trace the complete End-to-End request flow from Browser → NestJS → Prisma → PostgreSQL in Lesson 6.",
  },
  {
    id: "p16-l6",
    phaseId: "p16",
    title: "End-to-End Verification: Browser → Nest → Prisma → Postgres",
    level: "Backend Developer",
    minutes: 35,
    summary:
      "Trace and verify the complete full-stack request pipeline from user click in the browser to NestJS controller, Prisma 7.9.15 query, Supavisor pooler, and PostgreSQL disk commit. Audit latency, connection pooling, and error propagation.",
    prerequisites: [
      "p16-l3 — Connecting NestJS + Prisma 7.9.15 to Supabase",
      "p10-l5 — Production Build, Docker & Containerized NestJS",
      "p07-l1 — HTTP/1.1 vs HTTP/2 vs HTTP/3 & Request-Response Anatomy",
    ],
    objectives: [
      "Trace an HTTP request across every hop in the full-stack architecture.",
      "Implement latency and timing telemetry across each architectural layer.",
      "Verify connection pool reuse and ensure zero connection leaks under load.",
      "Handle and translate database errors into clean, user-friendly HTTP responses.",
    ],
    simple:
      "A complete full-stack application is a chain of specialized layers: (1) Browser sends an HTTP request, (2) NestJS validates DTOs and authenticates tokens, (3) Prisma translates TypeScript method calls into SQL, (4) Supavisor multiplexes the query over a pooled TCP socket, (5) PostgreSQL executes the query against disk and index pages, and (6) the result travels back up the chain as JSON. Understanding this end-to-end trace makes you a master system architect.",
    why:
      "When a user experiences a 2-second delay or an unexpected 500 error, developers who only understand their frontend will blame the backend, and backend developers will blame the database. Full-stack engineers know how to trace the exact millisecond cost and error state at every hop in the chain.",
    mentalModel: {
      title: "The Relay Race",
      body: "Think of an end-to-end request like a 4x100m relay race. The runner with the baton (the request payload) passes it cleanly to the next runner at each zone (Browser → Gateway → NestJS Controller → Prisma ORM → Postgres Engine). If any runner drops the baton or hesitates, the total race time suffers.",
    },
    sections: [
      {
        heading: "The 6-Hop End-to-End Architecture Trace",
        body: [
          "Below is the exact hop-by-hop breakdown of a production write request (`POST /api/v1/orders`).",
        ],
        code: [
          {
            file: "E2E_REQUEST_TRACE.md",
            lang: "text",
            code: [
              "┌────────────────────────────────────────────────────────────────────────┐",
              "│ HOP 1: Client Browser / React App                                      │",
              "│  - User clicks 'Submit Order'                                          │",
              "│  - Sends HTTP POST /api/v1/orders with Authorization: Bearer JWT      │",
              "│  - Network wire latency: ~20–40ms                                      │",
              "├────────────────────────────────────────────────────────────────────────┤",
              "│ HOP 2: Reverse Proxy / Cloud Run / Nginx                               │",
              "│  - Terminates TLS, forwards HTTP to Node container on port 3000        │",
              "├────────────────────────────────────────────────────────────────────────┤",
              "│ HOP 3: NestJS Application Pipeline                                     │",
              "│  - Global ValidationPipe validates CreateOrderDto (class-validator)    │",
              "│  - AuthGuard verifies JWT signature and extracts user.id              │",
              "│  - OrdersService initiates business logic (~2ms)                       │",
              "├────────────────────────────────────────────────────────────────────────┤",
              "│ HOP 4: Prisma 7.9.15 Data Layer                                        │",
              "│  - Translates `prisma.order.create({ ... })` into parameterized SQL    │",
              "│  - Passes query to @prisma/adapter-pg and Node pg.Pool (~1ms)          │",
              "├────────────────────────────────────────────────────────────────────────┤",
              "│ HOP 5: Supavisor Transaction Pooler (Port 6543)                        │",
              "│  - Assigns query to available physical Postgres connection (~3–5ms)    │",
              "├────────────────────────────────────────────────────────────────────────┤",
              "│ HOP 6: PostgreSQL Database Engine (Supabase Cloud)                     │",
              "│  - Executes INSERT, verifies FK constraints, writes to WAL & table     │",
              "│  - Returns new record ID (~2–4ms)                                      │",
              "└────────────────────────────────────────────────────────────────────────┘",
              "Total End-to-End Roundtrip Time: ~30–55ms",
            ].join("\n"),
            caption: "Complete end-to-end request lifecycle and millisecond latency budget across all 6 hops.",
          },
        ],
      },
      {
        heading: "Adding Server-Timing Headers to Expose Hop Latencies",
        body: [
          "Use the standard W3C `Server-Timing` HTTP response header to expose backend and database execution latencies directly in the browser's DevTools Network tab.",
        ],
        code: [
          {
            file: "src/common/interceptors/server-timing.interceptor.ts",
            lang: "ts",
            code: [
              "import { Injectable, NestInterceptor, ExecutionContext, CallHandler } from '@nestjs/common';",
              "import { Observable } from 'rxjs';",
              "import { tap } from 'rxjs/operators';",
              "import { Response } from 'express';",
              "",
              "@Injectable()",
              "export class ServerTimingInterceptor implements NestInterceptor {",
              "  intercept(context: ExecutionContext, next: CallHandler): Observable<any> {",
              "    const start = performance.now();",
              "    const ctx = context.switchToHttp();",
              "    const response = ctx.getResponse<Response>();",
              "",
              "    return next.handle().pipe(",
              "      tap(() => {",
              "        const duration = (performance.now() - start).toFixed(2);",
              "        // Exposes timing in Chrome DevTools -> Network -> Timing tab",
              "        response.setHeader('Server-Timing', `app;dur=${duration};desc=\"NestJS + Prisma Execution\"`);",
              "      })",
              "    );",
              "  }",
              "}",
            ].join("\n"),
            caption: "Exposing backend execution telemetry via the standard W3C Server-Timing HTTP header.",
          },
        ],
      },
    ],
    mistake: {
      title: "Leaking Raw Database Errors to HTTP Clients",
      wrong: [
        "// ❌ Letting Prisma errors bubble up raw to the client",
        "@Post()",
        "async create(@Body() dto: CreateUserDto) {",
        "  // If email is duplicate, throws PrismaClientKnownRequestError P2002",
        "  // Result: Client receives 500 Internal Server Error with database column names leaked!",
        "  return this.usersService.create(dto);",
        "}",
      ].join("\n"),
      right: [
        "// ✅ Catching database errors in a Global Exception Filter",
        "@Catch(Prisma.PrismaClientKnownRequestError)",
        "export class PrismaExceptionFilter implements ExceptionFilter {",
        "  catch(exception: Prisma.PrismaClientKnownRequestError, host: ArgumentsHost) {",
        "    if (exception.code === 'P2002') {",
        "      // Clean 409 Conflict without leaking raw DB internals",
        "      return response.status(409).json({ statusCode: 409, message: 'Resource already exists' });",
        "    }",
        "  }",
        "}",
      ].join("\n"),
      explain:
        "Raw database exceptions leak table names and constraint details while returning unhelpful 500 status codes. Use Exception Filters to map them to standard HTTP status codes.",
    },
    tryIt: [
      "Open your browser DevTools Network tab: inspect the response headers of any API request for `Server-Timing` or latency metrics.",
      "Send a request that triggers a unique constraint violation (e.g. duplicate email) and verify that your backend returns 409 Conflict instead of 500.",
      "Verify that your `PrismaService` connection remains stable under multiple concurrent curl/ab requests.",
    ],
    challenge: {
      prompt:
        "Implement a complete NestJS global Prisma exception filter that maps Prisma error codes `P2002` (Unique constraint), `P2025` (Record not found), and `P2003` (Foreign key constraint violation) to their corresponding HTTP status codes.",
      hints: [
        "Use `@Catch(Prisma.PrismaClientKnownRequestError)`",
        "P2002 -> 409 Conflict",
        "P2025 -> 404 Not Found",
        "P2003 -> 400 Bad Request (Invalid reference)",
        "Default -> 500 Internal Server Error",
      ],
      solution: [
        "import { ExceptionFilter, Catch, ArgumentsHost, HttpStatus, Logger } from '@nestjs/common';",
        "import { Prisma } from '@prisma/client';",
        "import { Response } from 'express';",
        "",
        "@Catch(Prisma.PrismaClientKnownRequestError)",
        "export class PrismaClientExceptionFilter implements ExceptionFilter {",
        "  private readonly logger = new Logger(PrismaClientExceptionFilter.name);",
        "",
        "  catch(exception: Prisma.PrismaClientKnownRequestError, host: ArgumentsHost) {",
        "    const ctx = host.switchToHttp();",
        "    const response = ctx.getResponse<Response>();",
        "",
        "    let status = HttpStatus.INTERNAL_SERVER_ERROR;",
        "    let message = 'Internal database error';",
        "",
        "    switch (exception.code) {",
        "      case 'P2002': {",
        "        status = HttpStatus.CONFLICT;",
        "        const target = (exception.meta?.target as string[])?.join(', ') || 'field';",
        "        message = `A record with this ${target} already exists.`;",
        "        break;",
        "      }",
        "      case 'P2025': {",
        "        status = HttpStatus.NOT_FOUND;",
        "        message = 'The requested database record was not found.';",
        "        break;",
        "      }",
        "      case 'P2003': {",
        "        status = HttpStatus.BAD_REQUEST;",
        "        message = 'Foreign key constraint violated: referenced entity does not exist.';",
        "        break;",
        "      }",
        "      default: {",
        "        this.logger.error(`Unhandled Prisma error [${exception.code}]:`, exception.message);",
        "        message = 'An unexpected database error occurred.';",
        "        break;",
        "      }",
        "    }",
        "",
        "    response.status(status).json({",
        "      statusCode: status,",
        "      error: HttpStatus[status],",
        "      message,",
        "      timestamp: new Date().toISOString(),",
        "    });",
        "  }",
        "}",
      ].join("\n"),
    },
    quiz: [
      {
        question: "What is the standard W3C HTTP response header used to expose backend timing metrics to browser DevTools?",
        options: [
          "`X-Debug-Timer`",
          "`Server-Timing`",
          "`Cache-Control`",
          "`Content-Duration`",
        ],
        answer: 1,
        explanation: "`Server-Timing` is the standardized HTTP response header supported by browser DevTools to display server execution timings.",
      },
      {
        question: "How should a unique constraint violation in Prisma (`P2002`) be mapped in an HTTP API?",
        options: [
          "200 OK with empty body",
          "409 Conflict (or 422 Unprocessable Entity)",
          "500 Internal Server Error",
          "301 Moved Permanently",
        ],
        answer: 1,
        explanation: "409 Conflict accurately signals to client applications that the resource cannot be created because a conflicting record already exists.",
      },
      {
        question: "What Prisma error code corresponds to 'Record not found' when updating or deleting a record (`P2025`)?",
        options: [
          "`P1001`",
          "`P2002`",
          "`P2025`",
          "`P3005`",
        ],
        answer: 2,
        explanation: "`P2025` occurs when an operation (like `delete` or `update`) expects a record to exist but finds none.",
      },
      {
        question: "In the 6-hop architecture, where is the SQL generated and validated?",
        options: [
          "In the user's browser",
          "In the NestJS backend via Prisma Client (@prisma/client)",
          "Inside Nginx",
          "Inside the CSS style sheet",
        ],
        answer: 1,
        explanation: "Prisma Client running inside the NestJS Node.js process transforms method calls into parameterized SQL queries.",
      },
      {
        question: "Why should raw Prisma exceptions never be returned directly in HTTP error responses?",
        options: [
          "They are written in binary",
          "They expose internal database table names, constraint details, and column schemas to potential attackers while returning vague 500 errors",
          "Browsers reject JSON containing errors",
          "Prisma Client crashes if exceptions are logged",
        ],
        answer: 1,
        explanation: "Unfiltered database errors leak internal schema structure and fail to provide clean, actionable HTTP error messages to clients.",
      },
      {
        question: "What component multiplexes client queries across physical PostgreSQL connections in Supabase?",
        options: [
          "Supavisor / PgBouncer Connection Pooler",
          "PostgREST",
          "Kong API Gateway",
          "GoTrue Auth",
        ],
        answer: 0,
        explanation: "Supavisor (and PgBouncer) acts as the connection pooler multiplexing client queries to minimize PostgreSQL connection overhead.",
      },
    ],
    flashcards: [
      {
        front: "What are the 6 hops of an end-to-end request?",
        back: "1. Browser, 2. Reverse Proxy/Gateway, 3. NestJS App Pipeline, 4. Prisma 7.9.15 Data Layer, 5. Supavisor Pooler, 6. PostgreSQL Engine.",
      },
      {
        front: "What is the Server-Timing header?",
        back: "A standard W3C HTTP header that communicates server execution latencies directly to browser DevTools.",
      },
      {
        front: "What HTTP status code should map to Prisma error P2002 (unique constraint)?",
        back: "HTTP 409 Conflict.",
      },
      {
        front: "What HTTP status code should map to Prisma error P2025 (record not found)?",
        back: "HTTP 404 Not Found.",
      },
      {
        front: "What HTTP status code should map to Prisma error P2003 (foreign key violation)?",
        back: "HTTP 400 Bad Request.",
      },
      {
        front: "What NestJS feature cleanly intercepts Prisma exceptions across all controllers?",
        back: "A Global Exception Filter (`@Catch(Prisma.PrismaClientKnownRequestError)`).",
      },
      {
        front: "Why is parameterized SQL critical in the Prisma hop?",
        back: "It prevents SQL injection vulnerabilities by separating SQL query structure from user-supplied values.",
      },
      {
        front: "What is typical total roundtrip latency for a pooled write request?",
        back: "Approximately 30ms to 60ms in modern cloud regions.",
      },
    ],
    recap: [
      "Trace requests across all 6 hops: Browser → Proxy → NestJS → Prisma → Supavisor → PostgreSQL.",
      "Use `Server-Timing` headers to expose fine-grained backend execution timings in browser DevTools.",
      "Map Prisma database exceptions (P2002, P2025, P2003) to standard HTTP status codes (409, 404, 400) using Exception Filters.",
      "Never leak raw internal database errors or column schemas in production API responses.",
    ],
    references: [
      { label: "W3C Server Timing Specification", url: "https://www.w3.org/TR/server-timing/" },
      { label: "NestJS Exception Filters", url: "https://docs.nestjs.com/exception-filters" },
    ],
    nextBridge: "Congratulations on completing Phase 16: Supabase Platform + Database Integration! Next, we dive into Phase 17: Supabase Auth + Next.js + NestJS.",
  },
];

export const LESSON_CONTENT_P16B: Record<string, LessonContent> = Object.fromEntries(
  LESSONS_P16B.map((l) => [l.id, l])
);
